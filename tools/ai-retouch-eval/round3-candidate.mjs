/* 12a 第三轮**候选**口令 —— 🔴 未经店主批准,未跑,不许自动使用
 *
 * 这不是猜的,是从第二轮的两个客观量倒推出来的:
 *   ① **死白区**(亮度>225 的像素占比):参考图 0.0–0.1% · 原片 0.0–8.4% · 模型出图 1.7–26.3%
 *      ⇒ 死白是模型造出来的,原片和参考图都没有。所以要给**上限**,不能只说「不新增高光」。
 *   ② **冷暖 R/B**:参考图 1.17–1.32 全偏暖 · 原片 1.13–1.41 偏暖 · 模型出图 0.91–1.20 偏冷
 *      ⇒ 第二轮保留的「白平衡轻微偏冷,让白色回到中性白」**方向跟参考图相反**,是它在拉冷。
 *
 * 与第二轮的差别只有两处,其余逐字不动(一次只动该动的,才知道是哪一句起的作用):
 */
import { PREFIX, SUFFIX } from './round2-prompt.mjs'
export { PREFIX, SUFFIX }

/* 第二轮原句(留作对照,便于逐句 diff):
 *   '白平衡轻微偏冷,让白色回到中性白,但不发灰不发青'
 *   '不新增任何高光,只压掉甲面上杂乱的多点反光'
 */
export const PRESET = `参照专业相机在窗边自然光下拍摄的美甲客照质感:保留原图的光影方向和阴影层次,不整体提亮;只在明显欠曝时把曝光提到正常,亮度已正常的区域不动;色温以原图为准,保留原图本来的暖调,不做冷暖校正,不要把画面调冷调灰;皮肤肤色干净通透、保留真实纹理和指节褶皱,不做美白磨皮式的平涂;不新增任何高光,只压掉甲面上杂乱的多点反光,画面里不允许出现成片的纯白或接近纯白的区域,最亮处必须保留细节和层次;背景保持原样,只允许轻微虚化以增加景深;整体像大光圈定焦镜头拍出来的样子,有明暗对比、有空气感。`

export const PROMPT = PREFIX + PRESET + SUFFIX

/* 改动说明(给店主看的,一句一条)
 * 【删】「白平衡轻微偏冷,让白色回到中性白,但不发灰不发青」
 * 【加】「色温以原图为准,保留原图本来的暖调,不做冷暖校正,不要把画面调冷调灰」
 *        ——— 理由:你的四张参考图 R/B 全在 1.17 以上(偏暖),模型出图被拉到 1.0 以下。
 * 【加】「画面里不允许出现成片的纯白或接近纯白的区域,最亮处必须保留细节和层次」
 *        ——— 理由:「不新增任何高光」这句第二轮里已经有了,但没拦住 ——
 *             模型造出的死白区仍有 1.7–26.3%,而参考图是 0.0–0.1%。需要给出**上限**而不只是禁止动作。
 *
 * 验收判据(跑完直接算,不靠感觉):
 *   · 死白区 ≤ 1%(参考图 0.0–0.1%,原片多数 <1%)
 *   · R/B ≥ 1.10(参考图 1.17–1.32)
 *   · ①a/①b 不得比第二轮差
 */
