/* 12a 第三轮 · 先分桶再修(小批 12c §二)
 *
 * 店主原话:「模型拿到一张照片应该先判断它适合哪一种修法,再修。不是所有图统一修法。」
 * 🔴 **但桶由「量」来分,不让模型自己判** —— L5 已经证明模型同图同令三次都不一样,
 *    让它自己判"这张算不算欠曝"只会更不稳。量是确定的,模型判不是。
 *
 * 🔴 **全部删掉「冷」字**(12c §二):白平衡一律「以原图为准」。
 *    第二轮留着「白平衡轻微偏冷,让白色回到中性白」,实测把 R/B 从 1.065 further 压到 1.033,
 *    而店主参考图是 1.17–1.32 —— 方向反了。这轮一个「冷」字都不许有,配 check 守住。
 */

/* 三桶共用的「绝对不改」前缀 —— 含 11z 那句解打架的 */
export const PREFIX = `这是一张美甲客照的保真修图任务。输出必须与原图是同一张照片、同一构图、同一双手、同一背景,只做下面要求的调整。
【绝对不改】指甲的形状、长度、颜色、图案、贴钻位置和数量与原图完全一致;手的姿势、手指位置、手指粗细、关节位置不改;背景内容和构图不改;画面比例与原图一致。肤色、白平衡、亮度的调整只作用于皮肤与背景,指甲区域的颜色、明暗、反光一律保持原样。
【本次调整】`

export const SUFFIX = `
【输出】清晰度不低于原图,不做风格化,不加滤镜色,不加任何文字或水印。`

/* 桶 A:暗调片 —— 店主原话「暗光片不该整体加亮,加亮没质感」 */
export const PRESET_A = `这是一张暗调照片,**不要整体提亮**,原图的暗就是它该有的样子。只做四件事:一,清理皮肤上的杂质和指缝暗角,保留皮肤真实纹理和指节褶皱,不做平涂;二,压掉甲面上杂乱的多点反光,不新增原图没有的高光;三,白平衡以原图为准,保留原图本来的暖调,不做色温校正;四,只把最暗处的死黑轻轻抬一点点,抬到能看出纹理就停,阴影层次全部保留,画面整体的明暗关系不变。空间感靠背景轻微虚化来做,不靠提亮。`

/* 桶 B:正常曝光 */
export const PRESET_B = `这张照片曝光正常,**不要改变曝光**。只做四件事:一,皮肤干净通透,保留真实纹理和指节褶皱,不做美白平涂;二,压掉甲面上杂乱的多点反光,不新增原图没有的高光;三,白平衡以原图为准,保留原图本来的暖调,不做色温校正;四,背景轻微虚化增加景深。画面最亮的地方必须保留细节和层次,不允许出现成片的纯白。`

/* 桶 C:偏亮/过曝 —— 本批 41 张一张都没落进来,口令先写好备用 */
export const PRESET_C = `这张照片偏亮,**把过亮的地方压下来,找回最亮处的细节和层次**,不允许出现成片的纯白。其余同样只做:皮肤干净通透保留真实纹理不平涂;压掉甲面上杂乱的多点反光,不新增原图没有的高光;白平衡以原图为准,保留原图本来的暖调,不做色温校正;背景轻微虚化增加景深。`

export const PRESETS = { A: PRESET_A, B: PRESET_B, C: PRESET_C }
export const promptFor = (bucket) => PREFIX + PRESETS[bucket] + SUFFIX

/* ── 分桶阈值(12c §二 原样照抄,做成配置)── */
export const THRESHOLDS = {
  A: { 说明: '暗调片', 判据: '均亮 < 70 或 暗部占比(L<40) > 45%' },
  C: { 说明: '偏亮/过曝', 判据: 'p95 > 240 且 均亮 > 140' },
  B: { 说明: '正常曝光', 判据: '其余' },
}
export const bucketOf = ({ 均亮, 暗部, p95 }) => {
  if (均亮 < 70 || 暗部 > 45) return 'A'
  if (p95 > 240 && 均亮 > 140) return 'C'
  return 'B'
}
/* §四:原片已在参考范围内 ⇒ 可以提示「不用修」,少花一次钱 */
export const noNeedRetouch = (t) => bucketOf(t) === 'B' && t.p95 < 200 && t.RB >= 1.1 && t.RB <= 1.4

/* ── 判据:白名单 + 黑名单,两层(判据三)── */
export const MUST_BE_GONE = ['冷', '中性白', '提到正常', '整体曝光']
export const MUST_BE_PRESENT_ALL = [
  '指甲区域的颜色、明暗、反光一律保持原样',
  '白平衡以原图为准',
  '保留原图本来的暖调',
  '不做色温校正',
  '不新增原图没有的高光',
  '画面比例与原图一致',
]
export const MUST_BE_PRESENT = {
  A: ['不要整体提亮', '最暗处的死黑轻轻抬一点点', '阴影层次全部保留', '不靠提亮'],
  B: ['不要改变曝光', '最亮的地方必须保留细节和层次', '不允许出现成片的纯白'],
  C: ['把过亮的地方压下来', '找回最亮处的细节和层次', '不允许出现成片的纯白'],
}
