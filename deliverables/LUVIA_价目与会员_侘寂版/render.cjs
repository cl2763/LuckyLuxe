const {chromium}=require('/Users/changliu/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const path=require('path');
(async()=>{const browser=await chromium.launch({headless:true,channel:'chrome'});const page=await browser.newPage({viewport:{width:1800,height:1800},deviceScaleFactor:2});await page.goto('file://'+path.join(__dirname,'LUVIA_价目与会员_可编辑.html'));await page.evaluate(()=>document.fonts.ready);for(const [id,name] of [['price','价目表'],['member','会员体系']]){const el=page.locator('#'+id); console.log(id,await el.evaluate(e=>({height:e.clientHeight,content:e.scrollHeight,children:[...e.children].map(c=>({tag:c.tagName,bottom:Math.round(c.getBoundingClientRect().bottom-e.getBoundingClientRect().top)}))})));await el.screenshot({path:path.join(__dirname,'LUVIA_'+name+'_侘寂版.png')});}await browser.close()})();
