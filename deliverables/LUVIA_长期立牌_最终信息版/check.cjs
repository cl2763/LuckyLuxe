const {chromium}=require('/Users/changliu/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const path=require('path');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage();await p.goto('file://'+path.join(__dirname,'LUVIA_价目与会员_可编辑.html'));console.log(await p.evaluate(()=>{let errors=[];for(let col of document.querySelectorAll('.col')){let last=col.lastElementChild;if(last.getBoundingClientRect().bottom>document.querySelector('.price footer').getBoundingClientRect().top)errors.push('column overlaps footer');}for(let e of document.querySelectorAll('.row,.benefit,.prices-head'))if(e.scrollWidth>e.clientWidth+1)errors.push('horizontal overflow');return {errors,sections:document.querySelectorAll('.price section').length,priceRows:document.querySelectorAll('.price .row').length,tiers:document.querySelectorAll('.tier').length,benefits:document.querySelectorAll('.benefit').length,background:getComputedStyle(document.querySelector('.sheet')).backgroundImage};}));await b.close()})();
