# 开发日志 2026-08-15 · S1+S3 施工批(服务与价目合并页,图=合同 v1.2)

## 交付内容
- **后端**:services 加 `storefront`(顾客橱窗上架位,独立于 is_active)+ `is_timecard` 两列(try/catch ALTER);s1_storefront 幂等迁移(49 租户归位,NULL 残留 0,次卡候选 0——「焕颜足护」unit=once 正确未误伤);公开 `/services` 过滤=main+上架+非次卡(规则①);`startingPriceCentsOf` 起价=最低可用价档,serializeService 下发 `startingPriceCents/priceFromLabelZh·En`(「¥xxx 起」前端零运算,规则⑤);次卡编辑两路由 400 `TIMECARD_MIGRATED`(规则③防御);三条建服务口子(pricing items POST/老 /admin/services POST/平台路由 POST)全部落 storefront 默认——平台口不补=运行时新租户顾客端全空,施工中自查堵住;PATCH items 接受 storefront(员工 403=staffMayRead GET-only 结构性保证)。
- **网页端**(build 20260815a-s1s3):侧栏「服务管理+价目表」合并为「服务与价目」;两模块页签;模块①行卡(已上架 chip/关联项目/展示价 ¥xxx 起(价档描述)/约 N 分钟/开关/修改)+新建选择器(未上架项目一键上架/全新创建);模块②=原三子页签并入,主项目行价档 chips(含「疗程价(未挂 · 开单时不出现)」划线 na 态)+加项段+次卡迁出段(去会员与营销)+两处 callout 原文;chips 币符从裸数字改 money();模块②新增项目默认不上架(图注 B7);旧 services 路由/书签兼容跳转。
- **小程序**:商家管理宫格「服务与目录」两页签镜像屏(①上架服务:老板可拨 storefront 开关、员工只读;②结算单目录:价档 chips 全量只读+🔒 文案);顾客端服务列表换「¥xxx 起」(`priceFromLabelZh/En` 独立字段,**不动喂 AI 的 priceLabelZh——matrix 66 零波及,AI 语料自查 is_active 全集不走公开过滤,已查证**);顾客网页端列表同病同修(fromPriceLabel,详情页保留原语义)。
- **组合矩阵抓获并修复**:目录停用(isActive=0)×模块① 会显「已上架」但顾客看不见=自相矛盾 → 双端同口径隐藏已停用项。

## 验证(全部一次连续)
- CI ㉛ 组 16 条断言入 test-noshow-aftersales.mjs(套件 143 断言);**42 套件全量回归 EXIT=0 全绿**
- 双租户全路径实点:jics 13/13 + lucky 10/10;矩阵闸门 20 户全绿
- 镜像屏 L5:5 次进出+开关 element.tap() 实点往返,每轮 API+顾客接口双端复核;页签② 17 项目+12 加项;闭环③ 反向(网页拨→镜像屏即见);顾客端实拍全带「起」(三张截图入 handoff/核验截图_2026-08-15/)
- 网页 L5:5 轮进出+开关实点往返,公开条目数 17→16→17 每轮吻合

## 质询答复(店主随报质询:守护/足护真次卡在哪)
现库任何表都无此两卡——合同图示例数据,jics 从未建过(SQL 全表扫描证据入交付清单)。次卡的家=`membership_packages.kind='times'`(全库唯一真次卡=lucky「美甲10次卡」pkg_k926,已在 S2 目的地,零迁移债);services 侧剥离机制为防御性兜底+导航。

## 事故与处理
- 第一轮全量回归 bash 进程挂死(套件请求日志停在启动后 30 秒,EXIT trap 已走完但 bash 不退,原因未定;输出走管道缓冲无法取证)。处理:杀挂死进程→**完整重跑**(直写日志文件),42 套件 EXIT=0;还原 4128 时误杀一次店主服务,已按 restore_local 同款命令立即拉回并 /health 验证。教训入册:后台跑回归一律 `> file 2>&1` 直写文件,不走管道缓冲(可取证、可监控)。
- s1_mirror 首跑 connect 挂起:devtools 长开自动化桥失活,整只重启 devtools 后一次通过(工具箱纪律①的邻近场景)。

## 遗留
- 次卡行为断言(编辑 400)现为机制级(源码扫描),等 S2 有 is_timecard 数据后升级为行为断言
- 旧 service-edit 小程序页已断链未删文件(devtools 缓存纪律),归 S2 卫生批清除
- 差异①(价档编辑器「单项目×四价档」vs 图上「单价档×多项目弹层」)等店主裁决

## 追批:店主生产复核-1/2 并案(当日)
- 复核-1(次卡混价目):railway ssh 直连生产库 SQL 全 dump——jics 27 行/lucky 7 行**零次卡**,packages/recharge_tiers 两租户空;观感来源=模块②次卡段照抄图例点名「守护/足护」(生产不存在的卡名),已改条件化文案。
- 复核-2(起字全无):**真凶=生产网页顾客端整页白屏**——customer.js 顶层文案字面量立即执行 moneyY(50) 撞 CUR 的 TDZ(3c948e0 币种红线批引入,白屏 5 天,无任何断言兜)。修复=CUR 块前置;同类扫尽(仅 copy 两处中招);**复发登记**:①上次没兜住=currency-scan 只扫写死币符不跑运行时+网页顾客端不在走查清单;②永久护栏=CI ㉜ stub-DOM 顶层加载冒烟(customer/share),坏版反证可抓;③手机小程序无起=旧包待发版;devtools 无起=编译缓存需整只重启(既有工具箱纪律)。build bump 20260815b-s1fix。
