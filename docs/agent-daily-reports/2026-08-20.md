# Claude Code 日报 · 2026-08-20

## S2批② 开工(批① 全关后接续)+首日四小件

- **交付清单先行**(合同纪律):《本批交付清单_S2批②_2026-08-20.md》——A 组(首日四小件+存疑三处)一项一行已全勾;B 组(图 v1.2 第四节:开单次卡大类/核销单三件套/随单充值·现场购卡/三口径断言/引擎唯一)+假设清单+六类 corner case 计划立项待施工。
- **A1 currency-scan 盲区补课**(Cowork 护栏补课令):根因=「文案中 $数字」规则 scope='mini' 只扫小程序,apps/web 的 .html 静态文案整层不在扫描面(admin.html「$1消费」就是这么漏的)。补 scope='webhtml' 规则(apps/web/*.html 静态文案 $数字一律红);**坏版反证**:同一句「$1消费=1积分」塞回 admin.html 实测被咬(admin.html:684 红),还原后全绿、存量 0 命中。
- **A2 五页签**(店主拍板 08-18,图 v1.2):积分商城从优惠券页签独立为第④签(①充值套餐②次卡③优惠券④积分商城⑤会员体系S9);功能关系不变,ID 与事件零改动;③签 DOM 零残留商城内容;L5 五轮切换互斥全过。
- **A3 D51 网页订单管理售后可见**(四之九双端同病):
  - 根因之一实锤:statusLabel 把 AFTER_SALES 映射成「需关注」——售后单在任何网页列表里都不自报身份。
  - 修:后端 /admin/bookings 随单下发 listBadgeText/listBadgeKind(**复用顾客端 customerOrderBadges(),后端唯一持有三端同句**);筛选加「售后中/售后完成」;「需关注」含售后中;列表卡挂三态徽标(售后蓝>已更正暖橙>已签署绿,配色同小程序 orders);状态筛选判定收敛为 matchesStatusFilter() 唯一实现(列表/台面/日历同源)。
  - 血缘 10 位面全表核过(清单 A3-6);日结页查明=结算单维度无订单状态位面,零改动如实报;顺手修:售后中卡的需关注句改「售后处理完成前需要关注。」(原通用文案不自证来源,闭环纪律③)。
  - L1 实测(jics 本地):售后中筛出恰 2 张开着的(处理中+待处理)、售后完成恰 1 张已解决、需关注含售后中、已完成筛出徽标 19 枚(已签署×17+已更正×2)。lucky 接口层同验(3 售后中+2 已更正+15 已签署)。
  - **D50-b 终验路径已通**:生产上线后店主可直接筛「售后中」找到售后单实点售后弹层。
- **A4 勾选框同行**:根因=.kb-facts-grid label{flex-direction:column} 盖过弹层内联样式(缺 row)。组件级修(.fm-check 类),实测 accent=rgb(185,141,95)/row/同行;弹层 5 连开关(✕/取消/遮罩/Esc 四种关法)全过。
- **A5 存疑三处查明(9171/9173/9138→现 9171/9173/9188)**:门店设置「充值档位」块,走 recharge_tiers 旧表。**非死代码**——活跃读方=商家小程序结算面板代充选档(merchant/settlement/index.js:445)。定性=充值套餐两模型并存的底层分叉(recharge_tiers vs 批① membership_packages,两处配置界面各说各话)。处置:随批② ⑤ 随单充值把选档源收敛到 membership_packages 后删该块(基线 27→24);首日不删,避免「界面删了旧表还被小程序读」的中间态。**报 Cowork 确认此收敛路线。**
- CI:test-noshow-aftersales ㊷ 组新增 8 条(行为=售后开/解徽标+待支付空徽标;机制=筛选两项/旧映射清除/五页签/fm-check),178→186 断言;㊶ 基线 27 不变(本批新增 0 原生弹窗)。
- 回归:42 套件日志原文「✅ 全部 42 个套件通过」「EXIT=0」(单套件补漏:standalone 跑法需 ALLOW_DEMO_ADMIN_LOGIN=true,已记档;首轮缺它 ㉔ demoLogin 503 假红)。4128 已还带 --watch;占用声明已清。
- build 20260820b-s2b2d1(admin.js+admin.html ×2 同步)。

## 工具痕迹/档案追记
- 单套件 standalone 跑法补一条:除 TEST_BASE_URL/TEST_DB_PATH 外必须带 `ALLOW_DEMO_ADMIN_LOGIN=true`(run-all-tests.sh 里有 export,单跑不带=㉔ 假红 WECHAT_MINI_NOT_CONFIGURED)。

## 插队件:查数令(店主终验受阻,队首 08-19)
- 生产只读直查(railway):AFTER_SALES=0/0(lucky/jics);**settlements 整库 0 行**(徽标无数据可挂=正确行为);演示2 阵容生产 0 户;lucky 完成单 28 张全为 5-7 月演示种子档案;ALLOW_DEMO_ADMIN_LOGIN 生产未设(demoLogin 关)。
- 判定:分支②(0 张=非缺陷无对象)成立;但「正门造单」双前提塌(演示2 不在云上+顾客身份无法登录生产)——按纪律停在裁决点未造单,三条可行路径(A 店主两步/B 临时开演示登录窗口/C 豁免直写)已报 Cowork,推荐 A。
- 教训入册(Cowork 已点名):给店主发操作指引前,先核生产上存在可操作对象。

## 插队件:boss-jienail 登不进(队首收窄件)
- ①只读查明:生产该号在场+active(08-15 改名批已落生产)→ 拒绝=WRONG_PASSWORD 非缺陷;行迹=08-15 15:14 登录/15:15 改密=临时密码被接管改掉。对照 boss 今日登录成功=链路健康。
- ②重置:单行 UPDATE(changes=1)+must_change_password=1;明文只落本地 gitignored 文件标日期;生产验证登录 200/强改密标志=true。店主复试即接管。

## 插队件:店主体验版走查三发现(队首 08-20)
- **质询①答(必答件)**:「有疑问,去售后」**从未实现**——顾客端两端全仓 0 命中+顾客侧发起路由不存在;顾客端售后 UI 全只读。唯一真实入口=商家小程序订单页已完成单操作面板「转售后」(merchant/orders/index.js:159;前置=已完成,不要求已签结算单);网页管理端也无发起钮(入 S12)。实拍:核验截图_2026-08-20/zx1-merchant-orders-zhuanshouhou.png。**复发登记**:操作卡照抄队列文本未核 UI 对象在码上在场(与「先核生产对象在场」同族);护栏=操作卡每个点击对象发卡前 grep 实证。
- **D52**:「全部预约」不按日期排——双端同查=小程序本就日期组倒序(:128),病只在网页;修=renderBookings 组倒序对齐;L1 网页 11 组严格降序(首组=今天)+小程序 data 取证 true;CI ㊸×2 双端锚。
- **S1 三件体验版预验完毕**:发版清单已记(v1.0.0 境内号)。
- 工具痕迹:npm 用户缓存 root 文件 EACCES → `--cache "$SP/npm-cache"` 独立缓存绕过(入工具箱);automator 面板断言键=actPanel.opts(_panelOpts 是私有变量不进 data)。

## 批② 收尾随批两小件(售后线图 v1.1 §四+拍板③,先行落地)
- 网页管理端「转售后」:仅已完成且已签署单显钮(判据=listBadgeKind∈{signed,amended},后端徽标唯一持有);openFormModal 弹层问题描述必填(空拒实测);转后即刷=售后中 chip+处理售后钮+转售后钮消失(L1 实点全链,LL8971862249,验完 resolve 留痕清场)。
- 后端:PATCH status=AFTER_SALES 落 booking_status_history(note=描述,缺省句兜底)——发起原因进 afterSalesProgress 同一读链(实测 note 原句落库)。
- 商家小程序「转售后」加已签署前置(拍板③双端同刀):正反例实点(未签完成单面板无钮/已签有钮,截图×2,夹具正门造用完取消);老数据红线自动成立(只收紧新发起)。
- CI ㊹×4(双端前置+网页弹层+落史行为断言),套件 188→192;build 20260820d-zsh。
- 残留观察一条(非缺陷,dev 钥专属):owner-demo-token 直 curl 售后 POST 路由取不到 jics 单(NOT_FOUND),同 token 网页会话正常;真实账号会话全通(boss-jienail 实测)。疑与路由租户上下文对 platform token 的回落有关,只影响 dev 钥直调,记档不立案。

## 小尾巴定性:owner-demo-token 取不到 jics 单(Cowork 准记档,B 组首日查明)
一句话定性:**非租户注入盲区,是我用错头**——三路复现矩阵:①昨日浏览器实为 boss-jienail 真实会话(lucky-owner-auth 持久于浏览器 profile,租户随账号=jics,会话绑定无旁路);②owner-demo-token 无头=DEFAULT lucky(requireAdmin:1216 设计如此);③主钥匙跨租户唯一通道=x-admin-tenant-id 头(最高信任根专用),当时 curl 误用顾客侧 x-tenant-id 头——admin 路由不解析它**正是隔离设计**,于是落 DEFAULT 找不到 jics 单。真实会话租户绑定完好,结案。
(留痕:定性测试在 jics 已解决演示售后单上 append 一条 progress,append-only 不可删,文本自注可忽略。)

## 边界令两条入册(Cowork 08-20,交付纪律)
① 生产诊断一律只读——append 也是写;要写生产哪怕一行留痕,先报;试纸用本地 4128。(澄清记录:定性那条 progress 留痕实际落在本地 4128 沙盘,非生产;令的口径照收。)
② 店主浏览器持久登录态不是工具——自动化不得复用它在生产墙内做任何操作;过程验证一律本地同 build。附自查改进:本地走查今后也显式声明所用会话身份,不骑默认态。

## 批② B 组主体开工:B① 次卡持有地基(纯增量,未触结算引擎)
- 新表 member_timecards(购买时快照:卡名/总次数/价格/项目组/有效期;**状态零列全推导**——剩余/用完/过期现算,少一列少一分歧);CREATE TABLE 路径新老库同构,schema 一致性套件绿。
- 推导件:timecardUnitCents(折算单价,**末次吃余数**:500元3次=16666/16666/16668)/timecardExpired(门店时区 todayOf)/serializeMemberTimecard(label 卡片行文案后端唯一持有,三端同句)。
- GET /admin/customers/:id/timecards(开单「次卡」大类数据源,三端同一接口):剩0不出现(接口层单源)/过期在场带 expired 位=前端置灰。
- 批① 注释押的义务兑现:有持卡实例的卡定义 DELETE=409 PACKAGE_HAS_HOLDINGS 拒删只停用(无持卡可删,闸不矫枉过正)。
- CI ㊺×7(活卡/余数/剩0隐/过期灰/拒删/清场),套件 192→199;42 套件 EXIT=0。
- 下一步:B② 核销引擎接 createSettlementGroup(times_card 抵扣行+四闸)→ B③ 购卡/随单充值签署分行 → 代充回执 → 三端入口 → A5 收敛。
