# 开发日志 2026-08-11 —— 终局批(模拟器/浏览器批)

提交:`5ddaae8`(41 套件全绿,跑完 4128 已还回店主)
占用声明:开工改「占用中」→ 收工改回「未占用」,全程如实。

---

## 一、这批最值钱的一件事:停服实测证明了「断言绿 ≠ 做完了」

D17 上一轮自请标 🔧,欠的就是这一步。补上之后,**断言层全绿的三页里塌了两页**:

| 页面 | 断言层 | 停服实测(店主会看到的那一层) |
|---|---|---|
| services | ✅ | ✅ 失败态居中,与书面标准一致 |
| home | ✅ | ❌ 失败态**叠在残缺骨架上** |
| booking | ✅ | ❌ **纯白屏**,连「加载失败」都没有 |

- **booking 白屏**根因:失败态块被写在 `wx:if="{{service}}"` 闸门**里面**,而
  `getService()` 本身就会抛 —— service 取不到 → 闸门为假 → 失败态跟着一起不渲染。
  上一轮我自己说过「把假数据换成白屏所以不推」,结果推上去的正是一处白屏。
- **home 残缺骨架**根因:只加 `wx:if` 没排除正文。一边说"加载失败",下面继续摆
  半张门店卡(缓存店名 +空地址/空营业时间/空电话),再往下还悬着两个「人气美甲/
  人气美睫」空标题 —— 正是《闭环回归纪律》禁止的"单独蹦出来找不到上下文"。

**教训**:这两处都不是写漏了逻辑,是**只在能断言的那一层验过**。
L1 末端验证律不是流程装饰,它是这两个缺陷唯一的出口。

---

## 二、L2 同类扫尽:mock 回落又捞出 4 处,而且都在顾客眼前

上一轮宣布"D17 全套齐:六处 throw + 三页失败态"。这一轮扫下来还有:

1. `store-location` **整页从来没调过接口** —— data 摆 mock.store,onShow 又把
   mock.store 重新 localize 塞回去。顾客点「门店信息」看到的永远是编造的
   「Lucky Luxe」+「演示版暂用占位门店信息」。**真门店是「Jie's Nail 小婕」** ——
   小婕店的顾客一直在看别家店的信息。修完实测显示真店名(截图 D17-22)。
2. `checkout:39` `mock.findService(...) || item.service` —— **假数据排在真数据前面**。
3. `api.getService` 的 `|| mock.findService(id)` —— 第六处 mock 回落,本店没这个项目
   就从演示表里翻一个编造的项目连价格给顾客。
4. `booking` 初始值 `mock.timeSlots / mock.addOns` —— 停服实测时真渲染出 4 个不存在的
   加项;`refreshAvailability` 末尾 `: mock.timeSlots` 更狠:**当天约满时列出一排
   不存在的可约时间**,顾客选一个就下单。

`booking`/`checkout` 的 `require('mock-data')` 一并删掉 —— 留着就会被再次连上。

### 复发登记(第三次了)
这是「只删被举报的那一个、没问同类还有多少」的**第三次**:08-04 假报价 → 08-11 上轮
五处 throw → 这一轮又 4 处。前两次都补了护栏,但护栏都是**针对已知函数名**的断言,
挡不住"换个地方再写一次"。

**这次装的护栏换了思路 —— 从"点名"改成"数量归零"**:
- 全仓 `input/textarea` 共 89 个,**非受控 0 个**(见第三节)
- 全仓 `wx:for` 共 168 处,**缺 wx:key 0 处**
这类"总数 vs 违规数"的机械口径,新写的代码一进来就会把违规数顶上去,比枚举函数名耐用。

⚠️ 仍未归零的一项:`mock-data.js` 本身还在(还有 store-location 之外的引用)。
它的最终清除已在待办总方案,建议下一批直接定"顾客端 require mock-data 数量归零"。

---

## 三、D9 结案:工具的锅和真缺陷,各一半

两轮没定论,这次用**三路对照实验**当场分开了。同一个手机号框、同一时刻:

| 路径 | data | 框里显示 |
|---|---|---|
| 触发 `onDsPhone`(等价人手敲键) | `"13800130000"` | `"13800130000"` ✅ |
| automator `.input()` | `"13900139000"` | **`"undefined"`** ❌ |
| 对照:`.input()` 写 `type=text` 的框 | 正确 | 正确 ✅ |

**结论**:字面 "undefined" 是 **miniprogram-automator 写 `type="number"` 输入框时
自己造出来的**,不是产品缺陷 —— 工具的锅,和 D15 一样。

**但产品这边有一处真缺陷,正是它让工具的痕迹变成店主看得见的鬼**:
那两个框**没有 value 绑定(非受控)**。后果两层:
① 显示值与 data 脱钩;② `tapFree()` 每次开面板都把 dsName/dsPhone 重置成 `''`,
但没有 value 绑定这个重置**清不掉框里的字** —— 这就是店主说的「改不动删不掉」。

已改受控。L5 5/5:开面板必空 → 显示恒等于 data → 关掉重开必清。

**排查纪律入档**(写进 `handoff/自动化占用中.txt`):用自动化验输入框之前先做这个三路
对照;`type="number"` 的框一律不用 `.input()` 验,改用 callMethod 触发真实 handler。
不立这条,下一个输入框缺陷还会再耗两轮。

> 店主复验说明:按拍板,这条的最终定性只算店主手工那一次。预期见占用声明文件末尾。

---

## 四、D10 / D12

- **D10**:`showSheets` 里写着"恰好一张待签就直接 `openSign`" —— 商家想**看单**,
  拿到的是顾客端「确认本人并绑定」。改成「查看」永远只是看(列清哪几张什么状态,
  顺带满足 D7 点名到单);要递给顾客签必须明确点「递给顾客签」。
  L4 反例数据:恰好 1 张待签(正是以前会跳走的那种)/ 混合 / 0 张待签,三种都验。L5 5/5。
- **D12**:售后行原来**没有 bindtap**,点了没反应。后端 `/admin/bookings` 补下发
  `afterSales`(原因取转售后那条 `booking_status_history.note`,处理情况**复用顾客端
  同一份 `afterSalesProgress`**,不另写一套进度口径);前端只读弹层,无任何写入按钮。
  L5 5/5;越权断言:非售后单不误带该字段。
  ⬜ 无设计图,弹层沿用现有 dsheet 结构与留白,已记假设清单。

---

## 五、自评 L1–L5

| | 做到没 | 说明 |
|---|---|---|
| L1 末端验证 | ✅ | 全部结论都有界面截图;D17 两个缺陷就是这一层抓的 |
| L2 同类扫尽 | ✅ | mock 回落 4 处 / 非受控输入 89→0 / wx:for 168→0 缺 key,都有机械计数 |
| L3 无图不结案 | ✅ | D12 弹层与 booking 时段空态都标 ⬜ 记假设,没标已验证 |
| L4 反例数据 | ⚠️ 部分 | D10 三种数据、D12 空原因态都验了;**D8 没验到 —— 真实库近 8 天没有任何"已确认日结",那一态取不到数据** |
| L5 重复稳定 | ✅ | D9 / D10 / D12 各 5/5 |

**这批没做到的、以及为什么(不粉饰)**:
1. **D8 两端核验 —— 阻塞在数据,不是我偷懒**。「已确认日结 → 只读态 + 重开入口」
   这个状态需要一条已确认的日结,店主真实库近 8 天一条都没有。造一条 = **写财务账本**,
   我不代做。请 Cowork 在演示租户补一条,或店主本人确认一次日结。
   已核验到的部分:商家端日结页「未确认」那一态(截图 D8-01)。
2. **D6 网页并排核验 —— 没做**。当前 4128 是回归脚本用 `restore_local` 拉起来的,
   **没带 `ALLOW_DEMO_ADMIN_LOGIN`**,我进不去网页老板端;店主的密码我不输(脚本红线)。
   下一轮开场用 `启动服务器.command` 起服务(它带这个开关)即可做。
3. **AI 日报实际渲染 —— 半done**。商家端 `aiEnabled=true` 已在界面上兑现(D16 的
   GET 路由落地后终于跟服务端对上了,截图 AI-01);订阅页 AI 智能包徽标显示
   「长期开通」= D1 的口径在界面上也对了(截图 AI-02)。**但 AI 每日总结卡本身
   `aiSummary` 为空**,没拍到有内容的日报 —— 是"今天还没有可总结的经营数据"还是缺陷,
   我没有下结论的依据,按纪律不猜,列为待裁定。
4. **过去日期文案 —— 没做**,余量不够,不硬塞。

---

## 六、另开文件:三处**不敢自己动**的红条

`handoff/待拍板_顾客端下单链路三处红条_2026-08-11.md`。都在生产上活着,都属 D17
同一类,但修它们要动业务口径(门店归属/支付语义/金额),按纪律先报不动:

1. **顾客端写死 `STORE_ID = 'store-ontario-01'`** —— 非旗舰店顾客技师列表为空、
   可约时段直接 404、**下单还会带旗舰店的 storeId**(顾客端方向的租户串味)。
2. **下单接口失败后照样跳「支付成功」** —— catch 之后没有 return,继续写一张
   `paymentStatus:'paid'` 的本地订单、清空购物车、跳成功页。顾客以为约成功且付过款了。
3. **`createBooking` 接口没回 booking 就自己编一张** —— 假技师 **Mia Chen**
   (拍板④预言的那个名字,源头在这)+ 写死 ¥50 定金(与 R5 已定口径冲突,本店真实
   定金是 ¥100)+ 自算尾款。

**这三处值得记一笔的地方**:它们是**清掉假数据之后才露出来的**。红条① 的报错以前被
`mock.timeSlots` 兜住,页面照样列出一排时间段,看起来完全正常。
**假数据不只是骗顾客,它还替真缺陷打掩护** —— 这是这一批最该记住的一句。

---

# 第二批(同日) · D9 根治 + D19-21 红条 + 爽约定金/售后完成态补图批

提交:(见 git log 本批 commit)· 回归 42 套件全绿(新增 test-noshow-aftersales 32 断言)· 4128 已按启动脚本口径还回。

## 一、D9 根治(图 v1.1 = 合同)

- 「新客·输手机号」页签**整体删除**(wxml/js/wxss 全清,L2 扫 dsTab/dsName/dsPhone 0 残留);
  找人=唯一入口:单框受控即搜(≤5 条,姓名·脱敏手机号·到店N次)+「＋建档并排单」一步到位
  (≥7 位纯数字存手机号、姓名记「未命名」;空输入=「未命名顾客」)+扫会员码原样。
- 规则⑤新客签署强提醒:QR 层「新客·尚未绑定档案——请顾客扫码签署(扫码即自动建立绑定)」
  显著提示;pushedText 如实说「未绑微信无法推送」;**手签入口对未绑定客整体不出现**;
  台面「递给顾客签」遇未绑定客跳 qrFor 纯出码模式(同一份 QR 实现)。
- 验收:四条路各 L5 5/5(老客搜到/新名建档/会员码带出[applyMemberCode 同一处理器]/新客单自动弹码+提示)。
- **路④第一轮 0/5 抓出一个真缺陷**:QR 弹层被关在 `wx:if="{{ready}}"` 价目表闸门里,
  qrFor 模式不加载价目表 → 弹层永不渲染 —— **与 D17 booking 白屏同一类病(状态块被无关闸门连坐)**,
  已修(弹层移到闸门外)。复发登记:这一类第二次出现,护栏=浮层/失败态一律放顶层闸门外,
  本批已把两处都搬出;后续新浮层默认放页面根层。
- 店主复验是否编译到新包:**无法从我这边定论**;支持"旧包"假设的客观事实=受控化修复后我在同一
  模拟器 L5 5/5(真实路径),且受控框不可能显示 data 里没有的字面 undefined。此案随页签删除已无对象。

## 二、D19/D20/D21 红条

- **D19**:STORE_ID 写死删除,storeId 按租户从 /stores 缓存(storeIdKey 带租户);后端
  validateBookingInput 断言 storeId∈当前租户(STORE_TENANT_MISMATCH 400)。
  两店实测单:jics-nail `booking_msnqw7qm_46vm9d`(顾客端全流程)/ lucky-luxe `booking_msnr77eb_lq3q8p`。
  顾客端 booking 页实测:技师 4 人(小婕/翠花/苏苏/鹤辰)、时段 15 个 —— 修复前恒 0/报错。
- **D20**:失败=失败(catch 有 return;停服实测停在结算页、购物车原样);成功页「预约已提交」;
  本地回执剥离 paymentStatus/transactionId;全仓「支付成功」扫描断言常驻(0 处,无白名单)。
- **D21**:假订单路径删除;Mia Chen/tech-mia/Ava Lin 顾客端 0 残留(L2 捞出 6 处合同没点名的:
  booking 页 picker 兜底/buildCartItem/service-detail 快捷加购/cart 显示/order-detail 假技师名,全清);
  技师未选=validate 拦截,不再静默归到假技师名下。
- 顺带修:checkout 储值提示 `{{}}` 套 `{{}}` 字面量渲染(顾客看到原文),L2 全仓 0 同类。

## 三、爽约定金处置(A⓪-A⑤ 全对照)

后端:no_show_at 标(ALTER)/ deposit_disposals 表(append-only)/ 处置接口仅老板 /
留存=deposit_retains(source=no_show,expires_at 按 retainValidDays,不填=长期)/
没收=「定金收入·爽约没收」独立行(tags=处置 id)/ 到期惰性自动转没收(note 留痕)/
下次预约自动带出为收取记录(pay_channel=retain_carry,reason 引用来源)/ 守恒审计四去向补齐 / 撤销冲销。
断言(套件):无收取记录=400、员工=403、重复处置=409、留存零收入、没收不进日结不进业绩(+1 红线)、
到期自动没收+守恒 0 条、带出金额=留存额。UI:两端徽标三态+小程序弹层照图 A-2;客户档案留存卡。

## 四、售后完成态(B⓪-B④ 全对照)

状态机 pending→processing→resolved/closed 后端唯一持有;after_sales_events 时间线 append-only
(时间+操作人);写进展/标已解决=当单技师+老板(resolve 必填结果),关闭=仅老板(必填原因);
接口无金额字段(带 amountCents 请求实测不产生账目行);状态只前进(resolve×2=409,resolved 后 close=409)。
三端实测同一句话:商家端面板(时间线+三按钮)/ 顾客端徽标「售后已解决」+结果文案+三步卡 / 网页端卡片状态+入口。

## 五、解锁项

- D6 ✅:网页 2026-08-09 日结,10 行全部「查看签署单 + 金额有误?发起更正」并排(与小程序同源)。
- D8 ✅:jics-nail 08-10 真实「已确认日结 12:31 · 重开过 2 次」,网页+小程序两端核验只读态+重开入口
  (无需造 fixture,零写入)。
- AI 日报 ✅:网页 Dashboard「AI 今日简报」生成实测(今日 4 预约+建议行动/机会/风险);
  财务页 AI 智能总结常驻渲染;商家端小程序 AI 状态/订阅页「长期开通」上一批已核。
- 过去日期文案(一行):台面/排班非当期=「返回今天/返回本周」、日结过去日期单带「次晨补签/次日补签」注、
  休息日临时接单说明常驻 —— 已在 08-09 网页日结截图与台面实测复核,无「找不到上下文」状态。
- 追问答复:store-location/checkout/getService/booking 假时段 4 处 mock 回落 **已在上一批 5ddaae8 修毕**,零挂账。

## 六、自评 L1-L5 与残留

- L1 ✅ 截图 15+ 张(handoff/核验截图_2026-08-11/);网页端证据经浏览器实操(Browser 工具截图不落盘,
  证据在会话内 —— 工具限制如实标注;店主开网页 10 秒可复核)。
- L2 ✅ 机械口径:非受控 input 0/89、缺 wx:key 0/168、Mia Chen 顾客端 0、「支付成功」全仓 0、裸 ¥ 扫描绿。
- L3 ✅ 图=合同逐项对照(本批交付清单文件);⬜ 项 5 条记假设清单。
- L4 ✅ 反例数据:恰好 1 张待签 / 无收取记录爽约单 / 别的技师写售后 / 空结果标解决 / 跨租户 storeId /
  停服提交 / 到期留存。
- L5 ✅ D9 四路各 5/5。
- **残留/差异(等裁决)**:①网页端处置与售后为 prompt 三步交互,功能与后端同源但**弹层视觉未与小程序同源**
  (A①「同一弹层」严格口径未达)—— 纯前端工作量,挂账;②test-scan-sign 一条断言随规则⑤更新
  (未绑定客 pushedText 由"空"改"如实说无法推送"—— 合同变更,非为绿改测试);③本批在 jics-nail 留了
  数条演示/实测数据(单号在报告里),需要的话可撤销。

---

# 第三批(同日) · D22 金额红线 + D23(店主终验抓出)

## D22:58 块钱的来路,一项抓准

- **反推修正**:Cowork 假设的「水晶卸甲(58/98)+免卸甲(0/约60)」组合**不成立** ——
  免卸甲实价 0/0,凑不出原价差 158。真凶是**一项**:「手部精修前置」会员 58 / 原 158,
  与差额(604−546=58,1114−956=158)分毫不差。
- **根因定性:不是计价错**。同一组合直接打 preview,后端算出 546/956 完全正确;
  是「**跨大类已选主项目不可见**」:主项目列表只渲染当前大类,店主在护理大类勾过
  手部精修前置(或误触),切到简单款式后它从屏上消失但一直在计费 —— R2 同族第 3 例
  (看不见≠没选)。模拟器逐步复现:同组合+幽灵 = 604/1114,与店主看到的一致。
- **修三层**:①跨大类已选芯片常显(名字+大类+✕,取消即重算);②金额区「N 行明细·
  点开对账」逐行展开,每行原价/档位价/✕(店主点名的自助对账);③前端护栏 ——
  预览行与勾选不一致(多/少)当场 toast 报警,不许沉默。
- **断言常驻**(套件 ⑫,3 条):预览行↔请求项一一对应 / 合计≡勾选行之和 / 取消场景
  (去掉的项不再出行、合计同步降)。
- **L5 5/5**:¥604(幽灵在+芯片可见+明细 5 行)→ 芯片 ✕ → ¥546,与店主手算一致。
- 复发登记(R2 族第 3 例):R2 是"点了没反应以为选上了",D22 是"选上了看不见以为没选"——
  同一面墙的两面。护栏从"修交互"升级为"**账面自证**":明细行数钉在合计旁边,
  勾选状态与账单行数不一致时前端主动报警,这一类以后靠账面而不是靠眼睛。

## D23

自选行显示金额(「钻球 ¥50」,storeMoney 门店币符出口);名称/金额输入框 min-height 72rpx,
文字不再上下截断。发版清单已记 wxacode 真码「码对应到具体单」验证项(店主点名)。

回归 42 套件全绿(新套件 35 断言);4128 已按启动脚本口径还回;店主复核指引写在占用声明文件。

---

# 第四批(同日) · D22 追加件:跨大类二选一弹层(店主拍板)

- 触发:已选某大类主项目后,去勾**另一大类**主项目 → 弹「本单已选『×大类』」:
  [加选(保留原选择)] / [替换(清除其他大类选择)];点空白处=取消(不加这项)。
- 边界照拍板:同大类不弹;取消勾选不弹;加项(全局目录)不弹;选过一次本单内**同方向记忆**
  不再重复弹;误触取消**不记忆**,下次照弹;按指主项目 0→1 也过同一道闸门。
- 替换语义:只清**其他大类的主项目**,同大类主项目/加项/自选行都不动。
- 弹层按本批护栏放**顶层闸门外**;芯片与逐行明细保留不动。
- L5:加选 5/5(含记忆后静默加)· 替换 5/5 · 误触取消 5/5(含"不记忆再弹")+ 同大类对照不弹。

---

# 第五批(同日) · 主项目选择机制终版(店主二次拍板,弹层拆除)

- 上一批弹层(8cb694f)验收通过但机制升级:**默认单选自动替换**(同/跨大类一律,无弹窗)+
  **「＋加第二个主项目」显式加选**(点了才能加一项;不点永远单项,误触物理不可能)。
- 弹层整段移除(js/wxml/wxss),L2 扫 miniprogram 0 残留;护栏断言、幽灵芯片、逐行明细全保留 ——
  上一批的工作没白做,拆的只是弹层本体。
- 实现要点:_mainOrder 记选择顺序(替换=换最近加的那一项,保住先选的);armed 态一次一消耗;
  移除(行/芯片/步进器减到 0)一律回单选态;按指主项目 0→1 走同一套规则(不留步进器后门)。
- L5 五路各 5/5:同类替换/跨类替换/加第二项(含封顶再点=替换最近、主项数≤按下次数+1)/
  移除回单选/加项不受影响。

---

# 第六批(同日) · 结算单服务分组完整版(图 v2.2,图=合同)

- **结构**:结算页重排为「组卡片 ×N + 单级区」。每组六件套 —— ①价格体系独立段选
  (默认系统会员判定,可改,改档留痕落组)②服务项目组内单选自动替换(e8ea109 逻辑作用域=本组)
  ③加项目录按本组主项目类别过滤(未归类各组都显示)④自选行本组 ⑤技师本组 1–2 位
  ⑥被服务者本组。「＋添加第二个服务项目」在组①末尾后;组② 整组 ✕,组① 只可替换不可移除;
  已收起组显示摘要行。组=一张结算单,落既有 settlement_group(双单口径现成路径),
  签署 1/N、日结分列、顾客端零改动。
- **单级四件**:定金开关(有收取记录才出)/支付构成菜单式(储值·定金·线下·现场充值再抵扣可组合,
  无余额不显示储值项)/「去帮顾客充值→」内嵌充值面板(复用代充,老板+财务解锁门禁不放宽,
  档位赠额自动算,充完自动回单勾储值)/分组明细+合计(组标题行→逐行原价划线→原价合计(划线)
  →档位小计→共优惠(绿)→抵扣行→到店应收)。
- **金额红线(分组版)**:后端 `/admin/settlements/preview` 新增组预览分支 —— sheets 逐单
  computeSettlement + 组级合计 + buildPaymentLegs 一次给出,前端零运算;页面 D22 护栏升级为按组
  (每组预览行≡该组勾选);CI 套件⑬ 14 条常驻(组合计≡Σ各单/每组行≡该组请求项/快照价档·
  改档留痕·被服务者随组/1-N 顺序/未签可撤),套件 35→49 断言。
- **L5 六路各 5/5**:单组全流程(956/546/410)/加组②(不同价档技师被服务者,六件套齐全)/
  移除组②(合计回落组①不动)/换主项目加项自动清(自选行保留)/支付菜单组合(含充值面板
  进出充完回单再抵扣,轻档案代充)/两组互不串(组① 六字段+金额行逐字节不变)。
  每轮都把页面 formBody 原样重放后端直连,组级 JSON 逐字节比对。
- **工具痕迹新增一条**:automator `page.data()` 把 null 读成 undefined —— L5-充值面板首轮 0/5
  全因断言 `=== null`;截图定性(面板确已关)后改宽松判空,产品无缺陷。已记入占用声明排查模板。
- **复发登记(自查抓的,未出门)**:gToggleTech/gServedName 只 renderAll 不 refresh →
  明细区组标题技师/被服务者滞后 —— 属「同一事实两处说两句话」类,当场补 refresh;
  护栏=L6 每轮比对明细标题与组内选择。
- **测试污染自查**:standalone 跑套件误连 4128 建了 nsas-a-msok023f 测试租户(已停用待清)、
  L5 代充在轻档案「自动化验证-勿动」上留 ¥330 储值余额(充值无既有冲销路径,待裁决);
  fixture 定金已走 revoke 留痕、预约已取消、测试充值档已删;守恒审计 broken=[]。
  另发现存量 pending_sign 单 JI-20260811-J7R4(刘畅 ¥604,疑店主 D22 复核手验单)—— 未动,待店主处置。
- 回归 42 套件全绿;4128 已还;店主终验四件指引在占用声明文件。假设清单 6 条(券单级/整单规则单级/
  甲睫域启发式/档位充值单笔记账/充值权限沿用门禁待裁决/明细逐行划线)在交付清单文件。

---

# 第七批(2026-08-12) · 分组批验收回执:三项裁决落地 + 技师代充放行 + 组合矩阵(纪律四之六首跑)

- **三项裁决执行**:①¥330(现 ¥495)测试储值 → 挂账清单立档(handoff/待办挂账清单.md 挂-001,
  S2 红字调整路由上线后清);②误建测试租户 nsas-a-msok023f 停用即隔离不删库(挂-002 备查),
  「standalone 一律 scratch 服务器」护栏写进套件文件头;③刘畅 JI-20260811-J7R4(¥604 含幽灵 58
  手验单)已走 /void 留痕撤销,券已自动退回券包。
- **技师代充放行(店主拍板)**:`/admin/stored-value/recharge` 老板或技师可调,**豁免财务钥匙**
  (充值=预收轻动作);技师会话**经手人强制=当前技师**(body 冒名不认),created_by=员工账号留痕;
  `/admin/recharge-tiers` GET 放技师(选套餐用),POST/PATCH/DELETE 仍 403;
  耗卡(=确认收入)/储值总览/财务页门禁寸步不让。结算页面板拆掉财务解锁弹窗,
  过期文案「需老板会话+财务解锁」同步改。CI 套件⑭ 8 条常驻(49→57 断言)。
  L5 技师路 5/5:staff 会话无钥匙开面板→选套餐→确认→余额+33→回单自动抵扣,
  留痕直连库断言(technician_id=小婕,created_by=staff);跑完老板会话已还原。
- **组合矩阵(纪律四之六首跑,已入 CLAUDE.md)**:场景A 全功能长链(14 功能同场景顺序触发,
  两两对 C(14,2)=91 全覆盖,30 步每步验三不变量:formBody重放逐字节≡/每组行≡勾选/未操作组冻结)
  + 场景B 涉钱全组合(定金×储值×面板 8 态,支付腿等式全断言)= **38/38 全过 0 缺陷**。
  Cowork 点名的三组合(价档×组②/充值链接×菜单勾选/换主项×自选×技师)全在内。
  券×各功能 N/A(轻档案无券,待补数据);提交×各功能由下游一遍覆盖;充值确认入账由两轮 L5 覆盖
  (矩阵面板只开选关,挂账不再涨)。报告:handoff/组合矩阵_结算单服务分组_2026-08-12.md。
- 矩阵定金 fixture 即建即撤(revoke 留痕+预约取消),临时充值档已删,守恒审计 broken=[]。

---

# 第八批(2026-08-12) · 券列补跑:组合矩阵终版 0 N/A(裁决①执行)

- **发券→跑列→撤场**全走既有留痕路径:既有模板「满200减30」一次性发轻档案 →
  场景C 22 步(选券落组级 −30/券×定金×储值×面板 8 态/券×主项替换/券×组② 单级挂组①/取消回弹)
  + **跌破门槛真实反例 6 步**(换「精品单色」会员 ¥198 → 券自动落下、选项标「不可用:未满 ¥200」、
  选中态清空不留幽灵券;回 298 恢复可用需重选)→ 券作废留痕(status=revoked+原因)、
  定金 revoke、预约取消、临时档位删除,守恒 broken=[]。
- 矩阵终版:场景A 30 + 场景B 8 + 场景C 28 = **66 步全绿 0 缺陷,数据类 N/A 清零**
  (剩「疗程价×各功能」一列=范围外,S3 落地随批补)。
- 裁决②照办:member 页代充入口不动(老板管理台语境;技师需求已由结算面板满足)。
- 工具痕迹复发一次:automator ws 走神(600s 零输出)——按占用声明排查模板重连 cli auto 一次通过。

---

# 第九批(2026-08-12) · 终验二轮:D24/D25 落地 + 随机 22 组三方对账(抓出并根治「券被吞」)

- **165 之谜坐实**:当日冲卡 165 = 技师路 L5 的 5×¥33,全部 created_by=staff、note「结算单内代充」,
  无其他来源(全租户技师归属充值逐笔核对);日结行不加产品级"测试标注"(不为测试数据改产品),
  流水 note 本身可见,随挂-001 S2 清账后该统计自动归零。
- **D25(《财务总逻辑》3-1b)**:后端 recharge 对未绑定档案 400 UNBOUND_NO_RECHARGE(技师/老板同拦);
  结算面板链接禁用态+提示、member 页选人带绑定态+按钮压灰+提示条;CI ⑮ 3 条(57→59);
  L5 拦截 5/5 + 放行 5/5(放行走已绑定测试档案「店主验签」手输 ¥1,挂-003 +¥5)。
- **D24 现状回报+改造**:排单原读 owner-only 的旧 /admin/services 平铺口径(与结算目录同表不同序列化,
  这就是"排单粗结算细");改为同源 /admin/pricing/categories+items 两级选择(大类→小类,只列主项,
  员工也读得到),时长联动;预约 serviceId 与结算主项目同表同 id → 带入精确对上。L5 5/5。
- **储值链路端到端**:面板充值→余额+100c(逐笔 DB 断言)→财务页储值卡片(负债 25714/月充值 14200,
  API≡独立 DB 复算逐分一致,截图)→今日 20 笔充值零收入行(预收红线)→守恒 broken=[]。
  商家端暂无储值**逐笔**流水页(只有聚合卡+顾客端逐笔)——如实报,要不要补视图请 Cowork 定。
- **随机 22 组三方对账(种子可复现)**:页面≡后端≡独立计算器,22/22 逐分一致。
  过程中抓出**真缺陷「点了券被吞」**(响应乱序覆盖用户意图,金额无差但选择丢失):
  根治=响应序号+禁止无券响应回写券选择;复发登记 D22 家族第 2 例,永久护栏=随机对账脚本入库
  tools/sim/grp_random.mjs。独立计算器建模教训:共优惠口径=原价差+券(页面标签早写了「含券」,
  第一版计算器没读全,5 组假红一轮修正)。
- **测试纪律教训一枚**:被 kill 的自动化脚本 finally 不执行 → 会话"快照恢复"恢复了被污染的快照,
  模拟器挂了一段员工会话(此前批口头说"已还原老板会话"不实,如实更正);已用已知常量强制还原,
  规矩写进占用声明工具痕迹档案:**恢复会话用常量,不用快照**。
- fixture 全撤(券作废/定金 revoke/预约取消,D24 十张排单全 CANCELLED);守恒 broken=[];
  存量 COMPLETED 两张(19:00 D2D3 存量、20:10 疑店主终验手验)非本批产物未动。
- **D25 波及面收口(闭环回归纪律)**:首轮回归 [stored-value] 红 —— 存量套件给「导入客/邮箱注册客」
  (非绑定)充值,正是 3-1b 要拦的对象。合同变更修 fixture 不改断言:4 个套件
  (stored-value/audit-fix/daily-close/salary-v2)统一直连库绑定后再充,且 stored-value 顺手把
  「未绑定=400」立成常驻断言。

---

# 第十批(2026-08-12) · 收尾三件(图 v2.3):绑定前置+技师内容分配恢复+储值逐笔

- **规则⑦ 绑定前置**:archive_bind_tokens 表+三条路由(铸码=员工/老板、公开确认卡、公开确认;
  绑定复用 S4 claimUserByOpenId,冲突进合并队列不覆盖;沙盒 openid 旁路与签署 claim 同法);
  新页 pages/bind(本人确认卡:只有称呼/店名,无单无金额;成功页「已绑定 ✓ 请交还技师」);
  结算页拦截提示条+顾客区小字双入口 → 顶层弹层(占位码+链接+复制,护栏:闸门外)→ 2s 轮询
  绑定事件 → 自动关层+徽标刷新+充值解锁。L5 路A(顾客链)5/5 + 路B(面板事件)5/5;CI ⑯ 8 条。
  **沙盒已验:扫码之后的一切;真机欠:摄像头扫真码那一下**——发版清单真码项已并入绑定码(互扫断言)。
- **规则⑧ 技师内容分配(原样恢复)**:考据 git b5c4f62 旧版(techItems 映射+buildTechRows+minichips)
  →落回组结构:编号=后端预览行号(不自己编,金额红线同源),双技师出数字排、可共做、单技师不出、
  组② 从①起;itemNos 随单落库(既有 settlement_technicians)→日结分成参考(既有渲染,零后端改动)。
  **考据结论:换主项后原实现=数字位保留(不跟随不清空),照原样恢复**;悬空编号按原样提交,
  要不要提交前过滤报 Cowork 裁决(别加戏)。L5 5/5 含落库回读断言;矩阵补「分配×组②/×换主项」两列。
- **储值逐笔视图**:GET /admin/stored-value/txns(owner+财务钥匙,本月充值明细,纯 cents 下发)
  → 财务页聚合卡下逐笔列表(时间/顾客/金额/经手人/来源,storeMoney);页面层求和 ¥14,200≡聚合卡;
  CI ⑰ 4 条(求和≡/五要素/币种红线/技师 403)。截图 G-SV。
- CI 套件 59→71 断言;fixture:绑定验证轻档案 ×10 留存(纯档案无钱),排单全撤;
  被 kill 首轮的 1 张残留排单已撤(工具痕迹:kill 不跑 finally,占用档案已有此条)。

---

# 第十一批(2026-08-12) · D27 冻 App 根治 + D26 弹层控件复活 + 储值逐笔补消耗

- **D27(🔴)定案**:无密码店点「本月流水」→ 旧代码只认 getFinanceKey(),没钥匙就 toast+
  400ms navigateBack —— **回退撞进场转场动画**,导航栈卡死,微信转场透明层残留顶层吃掉全 App
  点击(店主"透明遮罩残留"的直觉正确;层是导航转场层,不是自绘弹层)。修:①两页 onShow 先问
  lock-status,门禁关直接进,不再误拦;②要拦也等转场走完(700ms);③常驻护栏=无可见浮层时
  页面 0 遮罩节点。L5 进出×5 全程实点(实点「本月流水」/筛选芯片),5/5。
- **D26 扩定案(与 D27 不同根)**:复制断=微信隐私校验拦 setClipboardData
  (「api scope is not declared in the privacy agreement」),旧代码无 fail 回调→静默吞;
  链接断=压根没挂点击处理器。修:两枚码复制统一「已复制 ✓」/失败提示;绑定码链接实点直落
  本人确认卡;沙盒 app.json __usePrivacyCheck__:false 放行,**正式版剪切板 scope 声明入发版清单**
  (真机复验条款)。L5 三路各 5/5(复制读剪贴板断言、链接落卡、签署码同跑)。
- **复发登记(店主点名)**:上一批断言链全绿、店主一点就断 —— 漏在 callMethod 直呼处理器,
  绕过「触点→控件」前半段;遮罩层级/隐私校验/无处理器死链,只有真实点触能暴露。
  **永久护栏:弹层交互控件 L5 必须 element.tap() 实点;涉剪贴板必须读回剪贴板断言。**
- **储值逐笔补消耗**:路由扩充值+消耗(消耗=结算扣卡行 note 解析单号回链+手工耗卡;金额负值
  纯 cents);财务页筛选芯片 全部|充值|消耗、聚合卡月充值/月消耗两行并列、消耗行单号实点看
  单快照。红线零新口径。CI ⑰ 8 条(71→75);界面层合计≡卡片×2+筛选/回链实点全过。
- 工具补账:automator evaluate 不解 Promise(剪贴板读回两步走);--watch 未接住后端大编辑,
  重启后生效 —— 记占用档案。

---

# 第十二批(2026-08-12) · D28:排单单据入口死链根治 + 单据预览弹层(图 v1)

- **根因**:D10 批 showModal confirmText「递给顾客签」5 汉字超微信 4 字限 → 整窗 fail 无回调=静默,
  「查看结算单」从 D10 起全状态死;**非 D24 带断**。D24 L5 没抓住=实点只圈了本批改动控件,
  且该按钮死于「原生对话框 fail 静默」这个断言盲层。同族连抓:mixin 覆盖导致我首版 orders 补丁
  静默失效(教训:合成页改 mixin 单点,别在页面上加会被覆盖的同名方法)+ 4 处存量超限 confirmText
  (points/staff/points-mall/salary-month —— 此前也全是静默死弹窗)一并修。
- **护栏**:CI ⑱ confirmText/cancelText ≤4 汉字机械扫描常驻;⑲ 排版件 7 条(合计≡各单之和/
  未签无签名区/单号可查/纯 cents/撤后 410);套件 75→83。
- **实现**:后端 /admin/settlements/:idOrCode/preview-card(整组排版件,前端零运算);
  components/sheet-preview 统一弹层(排单面板/COMPLETED 票据/日结 mixin 三入口同源);
  状态分流:待签→「继续结算」(结算页 boot 自动回出码态,关码留页,qrFor 模式不变);
  已签→弹层;关闭三法+原图按钮(未签不出)。
- **L5 全程实点 25/25**:已签弹层/续办/日结/关闭三法轮换/原图进出各 5/5;对图截图 6 张。
- 工具痕迹:组件内节点两段查(页面选择器进不了组件域);截图 API 中途罢工重连即愈;
  自吞一次「replace 没 assert 静默没生效」——以后批量 patch 一律带 assert(本批已犯已改)。

---

# 第十三批(2026-08-12,末批) · D29 门禁误拦补漏 + D30 两弹层双钮统一 + 弹层 v1.1

- **D29 复发登记(D27 同族第 2 例)**:上次按「toast+回退撞转场」的**症状**圈类,漏了按
  「getFinanceKey 前置门禁」的**机制**圈类 —— 工资页 keyMissing 空态不崩不回退,逃出扫描。
  本批按机制全仓扫 6 处用点逐处判定归档(salary-month 与 member 代充 modal 两处误拦,均修
  lock-aware;其余 4 处判定合规),护栏=新增财务门禁入口必须 lock-aware。工资入口实点 5/5。
- **D30 对账(认账)**:2784799 批「链接实点 5/5」验的是绑定码弹层;店主打不开的是签署码弹层
  (链接从来没挂处理器)—— L2 漏在没把「弹层里的链接」当一类扫。修法按店主拍板:两弹层统一
  「打开链接」「复制链接」双钮(沙盒占位期形态,真码上线随占位撤);实点各 5/5(打开落对应页、
  复制读回剪贴板断言)。
- **弹层 v1.1**:底栏「关闭」删除、已签通栏原图、未签底栏不显示;两法关+遮罩 0+原图进出 5/5。
- **⑱ 口径答复**:官方文档=4 个字符(中英同计),报错原文按汉字表述;⑱ 升级为总字符>4 即红
  (更严口径),全仓预扫 0 超限。
- fixture 即建即撤(待签单 void/两张排单 CANCELLED);42 回归见收尾。
