# 2026-08-23

## D66 五裁落地+多张单详情表达(一刀修完)
- **裁A 到店次数唯一定义**:`visitDaysCount()`=有消费(已签署组,按 settlementServiceDate 服务日)或已完成预约的**自然日数**(门店时区去重;同日多组=1;取消/未到不算)。三读方全改调:顾客端我的(原=COMPLETED 单数,售后漏计)/商家客户列表(原=COUNT 全部预约,取消也算)/画像页(原=另抄一份)——裁E 分叉债同步销账。㋋ 断言:三读方同数=2(签署组+同日完成预约=1 天/后日完成 +1/取消不算)。
- **裁B 即时预约写方闭环**:createSettlementGroup 无预约=组级自动建一张 COMPLETED 即时预约共享(source=settlement_instant,与建组**同事务**防孤儿幽灵卡;不做时段冲突检查=事实记录);存量回填迁移随启动(幂等 booking_id IS NULL;同组优先挂现有预约,全组无=建一张共享)——沙箱实测**回填 12 张**、全库零无挂靠;㋋ 常驻断言=零无挂靠+即时单三性(COMPLETED/settlement_instant/归卡主)+顾客列表可见。施工中抓一件:首刀只改了 computeSettlement 入参,**settlements INSERT 行的 booking_id 另有取值处**没跟上=静默失效(写方两处取值,断言当场咬住)。
- **裁C 售后上日历**:schedule-day 白名单+AFTER_SALES(arrivalState=done),蓝徽标「售后」句后端唯一,mini 台面卡 as-blue 标(网页端无台面日视图=无同病面,已核)。㋋ 断言:售后单在日历行+徽标句。
- **裁D/E**:并入裁A 单一出口(wiring 断言盯 visitDaysCount ≥3 处调用)。
- **L3 多张单详情**:payment.sheets=逐张(第 n/N 张+状态+各自 flow 五步账+头条)+组汇总行「组到店支付(N 张)」=Σ各张头条(与组卡同构,不合并假账);mini/web 双端同构渲染,单张组保持现状。㋋ 断言:sheets=2 各带五步账+Σ=列表「已结清」同数。列表标题首项目名净化(次卡核销行长尾截「 · 」前主名)。
- **复发登记(时区红线,测试层)**:套件 dateStr 用裸 new Date() 推日期——店主机器 CST 刚过午夜=08-23,门店多伦多还是 08-22,夹具订到「明天」、日结查「今天」,断言凭空红。①上次没兜住:红线只钉了产品代码(storeToday/process.env.TZ),测试文件不在扫描面;②永久护栏:六个套件 dateStr/localDateStr/futureDate 全部改 `toLocaleDateString('en-CA',{timeZone:'America/Toronto'})`(test-schedule-week 免改=基于服务端 weekStart 推,天生安全);③本条即当日复发小标题。
- **D67① 续办模式补测**:结算页正常模式(非 qrFor)callMethod openQr→顾客签→落台面(automator 实测,桥两次挂死按工具箱冷启法)。
- 验证:批②套件 413→**424 断言**全绿(㋋×9+wiring×3);42 套件 EXIT=0(handoff/回归日志_42套件_2026-08-23_五裁批.txt);沙箱五数复核:测试C 订单卡 2→**4 张**(7 张签署单全有家)、me/列表/画像到店同=2、积分行逐张有单可自证。build 20260823a-5c。
