# 2026-08-23

## D66 五裁落地+多张单详情表达(一刀修完)
- **裁A 到店次数唯一定义**:`visitDaysCount()`=有消费(已签署组,按 settlementServiceDate 服务日)或已完成预约的**自然日数**(门店时区去重;同日多组=1;取消/未到不算)。三读方全改调:顾客端我的(原=COMPLETED 单数,售后漏计)/商家客户列表(原=COUNT 全部预约,取消也算)/画像页(原=另抄一份)——裁E 分叉债同步销账。㋋ 断言:三读方同数=2(签署组+同日完成预约=1 天/后日完成 +1/取消不算)。
- **裁B 即时预约写方闭环**:createSettlementGroup 无预约=组级自动建一张 COMPLETED 即时预约共享(source=settlement_instant,与建组**同事务**防孤儿幽灵卡;不做时段冲突检查=事实记录);存量回填迁移随启动(幂等 booking_id IS NULL;同组优先挂现有预约,全组无=建一张共享)——沙箱实测**回填 12 张**、全库零无挂靠;㋋ 常驻断言=零无挂靠+即时单三性(COMPLETED/settlement_instant/归卡主)+顾客列表可见。施工中抓一件:首刀只改了 computeSettlement 入参,**settlements INSERT 行的 booking_id 另有取值处**没跟上=静默失效(写方两处取值,断言当场咬住)。
- **裁C 售后上日历**:schedule-day 白名单+AFTER_SALES(arrivalState=done),蓝徽标「售后」句后端唯一,mini 台面卡 as-blue 标(网页端无台面日视图=无同病面,已核)。㋋ 断言:售后单在日历行+徽标句。
- **裁D/E**:并入裁A 单一出口(wiring 断言盯 visitDaysCount ≥3 处调用)。
- **L3 多张单详情**:payment.sheets=逐张(第 n/N 张+状态+各自 flow 五步账+头条)+组汇总行「组到店支付(N 张)」=Σ各张头条(与组卡同构,不合并假账);mini/web 双端同构渲染,单张组保持现状。㋋ 断言:sheets=2 各带五步账+Σ=列表「已结清」同数。列表标题首项目名净化(次卡核销行长尾截「 · 」前主名)。
- **复发登记(时区红线,测试层)**:套件 dateStr 用裸 new Date() 推日期——店主机器 CST 刚过午夜=08-23,门店多伦多还是 08-22,夹具订到「明天」、日结查「今天」,断言凭空红。①上次没兜住:红线只钉了产品代码(storeToday/process.env.TZ),测试文件不在扫描面;②永久护栏:六个套件 dateStr/localDateStr/futureDate 全部改 `toLocaleDateString('en-CA',{timeZone:'America/Toronto'})`(test-schedule-week 免改=基于服务端 weekStart 推,天生安全);③本条即当日复发小标题。
- **D67① 续办模式补测**:结算页正常模式(非 qrFor)callMethod openQr→顾客签→落台面(automator 实测,桥两次挂死按工具箱冷启法)。
- 验证:批②套件 413→**424 断言**全绿(㋋×9+wiring×3);42 套件 EXIT=0(handoff/回归日志_42套件_2026-08-23_五裁批.txt);沙箱五数复核:测试C 订单卡 2→**4 张**(7 张签署单全有家)、me/列表/画像到店同=2、积分行逐张有单可自证。build 20260823a-5c。

## D68 三件+文案拍板+双店取证
- **文案(店主拍板+口径澄清)**:详情/组卡汇总行=「**到店服务项目(N)**」,右侧金额不变;**N=主项目数量**(店主层级 大类→项目→加项,N 数项目):加项/自选填写行/现场购卡都不计、大类不是计数单位。后端新增唯一出口 `groupMainItemCount()`(kind main∪timecard;次卡核销行=用卡做掉的那个项目,照样是项目),「等N项」与汇总行同锚点——当前模型每单恰一主项目故数值=张数,但实现与断言都按主项目计数写(㋌ 夹具:2 主项目+1 自选加项 → N=2 不是 3)。
- **L2 文案扫尽**:清单入册 handoff/D68文案扫尽清单_组张内部话术_2026-08-23.md——已改 10 处(汇总行/组说明/逐份行首/出码金额行/连签提示+接续钮/出码状态行/预告句/标题口径),保留 4 类并附理由(商家侧「单/张」作业用语 3 处待店主定夺 + 报表日志代码注释按店主明示不限)。护栏=㋌ 机械扫描八个渲染面(剥注释后扫)出现「组到店支付/本组还有/第 n/N 张/整组单据」即红。
- **D68① 签署链返回栈**:连签接续改**替换式导航**(sign.html `location.replace`,原 `<a href>` 压栈=签 N 份要按 N 次返回);原件入口改浮层后不再产生任何压栈跳转;商家端出码链本就是弹层(D67① 全组签完 relaunch 台面)。
- **D68② 原件悬浮查看器**:现状=原件是整页(小程序 web-view 包网页 /sign、网页 target=_blank 新页)。改为:小程序**浮层 + swiper 左右滑动**(遮罩/关闭钮/页码 n/N/长按存图),网页**lightbox 同构**(上一份·下一份钮 + 触摸滑动 + ←/→ 键 + Esc 关);图源=后端签署快照 SVG(`snapshotUrl` 随 payment.sheets 下发,待签份无快照=点了走签字动线)。
- **双店取证(四之九)**:沙箱建 `jics-store`(小婕的店)租户+技师+两服务,开双份组签完 → 顾客端渲染同刀:标题「水光裸甲 等2项」/汇总行「到店服务项目(2)」/「已结清 CAD $416」(168+228+20 加项;加项不进 N)/逐份行「服务确认单 1/2 · 已签署」/lightbox 浮层左右切换实拍——证明改动全租户生效,非旗舰店特例。
- 验证:批②套件 424→**436 断言**全绿(㋌×9 新增 + 5 条旧断言随文案改版翻写);42 套件 EXIT=0(handoff/回归日志_42套件_2026-08-23_D68批.txt);build 20260823b-d68。
- **途中抓一件(CI 偶发红的真因)**:`entitlements` 套件「upgrade request recorded」在本批终轮回归红——查明=`latestPlanRequest` 取「最新一条」用 `ORDER BY created_at DESC LIMIT 1`,同毫秒插入的两条(renew→upgrade 连发)并列时排序不定,取回旧那条。L2 同类扫尽:全仓 7 处「取最新一条」单行选取(套餐申请/售后发起原因×2/签署令牌/试用申请/定金处置冲销)一律加 `rowid DESC` 兜底;护栏=㋌ 断言扫描零残留。这类偶发以前只会被当成「重跑就好」,现在有断言钉住。
- **D68② 补件(店主 08-23 小件)**:浮层查看器加**左右半透明切换键**——图片中部高度(垂直居中)、两侧各一;第一份隐左键、最后一份隐右键;**滑动手势保留并存**(箭头与手势同一状态源:swiper `current` 绑 `viewer.index`)。网页 lightbox 按钮样式同构对齐(底部大按钮撤掉,改两侧悬浮箭头 + 保留触摸滑动/方向键/Esc)。断言 ㋌②补:双端箭头 wiring + 首末隐边条件 + wxss 类在场;437 断言全绿。**实拍抓到一处自造缺陷**:引入 .viewer-stage 包裹后 flex 主轴变横向,swiper 高度塌成默认值——白底只占半屏、箭头落在图片外的暗区;改 `flex-direction:column`+`height:100%` 修好(教训:加包裹层必看高度链,断言只能证元素在场、证不了布局对——这就是 L1 末端截图存在的意义)。build 20260823c-d68b。
