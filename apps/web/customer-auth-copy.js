/* 网页顾客端「登录那一屏」的文案 —— 以及它为什么长这样(裁 #103,2026-09-15)
 *
 * 从 `customer.js` 摘出来(裁 #89:顶破棘轮的正确反应是摘出去;`admin-copy.js` 是同一做法)。
 *
 * ══ 裁 #103:网页顾客端永远不是「新顾客的入口」══
 *
 * 不是网页做不了登录,是**网页做不了「认出老顾客」**:
 * 网页只拿得到**一个手机号**,小程序拿得到**微信身份 + 手机号**两个东西。
 * 所以一个号在库里找不到时,三种人网页一个都分不出来:
 *   · 从没来过的新顾客 · 老顾客但号没录进去 · 老顾客但换了号
 * **而如果网页这时候让她「注册」,那就是又开一份档案 —— 正是 D191 的症状。**
 *
 * 还有更简单的一层:**新顾客第一次来,一定是到店,或者在小程序里。**
 * 没有人会先在网页上注册一个美甲店账号 ⇒
 * **网页允许注册,不会带来一个新顾客,只会制造重复档案。**
 *
 * 款文(已写进 D190 图的「网页目标态」一节,即合同):
 *   > **网页顾客端只负责「让已经存在的人登进来」,永远不负责「让新的人进来」。**
 *   > **建档案只在两个地方发生:小程序扫码(走严格四条),或者店主在后台建。**
 *
 * 三条硬规矩(判据在 `apps/api/test-web-not-signup.mjs`,各自造病验过):
 *   ①不许显示「用户不存在」—— 那是把内部状态甩给顾客,且等于告诉外人这个号在不在库里;
 *   ②不许出现「注册 / 创建账号」类入口,**一个都不许有**;
 *   ③号在与不在,提示与返回体**一致到看不出差别** ——
 *     两个提示不一样,等于给任何人一个工具:输一串号就能试出谁是这家店的顾客。
 *     **美甲店的顾客名单是隐私。**
 */
/* 挂在 window 上给 `customer.js` 读。**它必须在 customer.js 之前加载**(index.html 已按序挂),
   但 `customer.js` 那边仍然写成「取不到就用内置兜底」—— 判据 ㉜ 验的正是「顶层加载不炸」:
   一个只在加载顺序对的时候才不炸的页面,换个人改 html 就炸给顾客看。 */
window.CustomerAuthCopy = {
  zh: {
    registerTitle: '用微信登录',
    registerText: '在店里消费过?用微信扫一下下面的码 —— 微信认得出你,你的余额和卡包都在。'
      + '还没来过?欢迎到店,我们当场给你开一份。你也可以先以游客身份浏览。',
  },
  en: {
    registerTitle: 'Sign in with WeChat',
    registerText: 'Been to the salon? Scan the code below with WeChat — it recognises you, and your balance '
      + 'and coupons are all there. First time? Come by and we will set you up in person. You can also browse as a guest.',
  },
}
