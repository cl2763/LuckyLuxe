/* 真机调试主机地址(店主 08-23 立件)——**这台开发机的局域网 IP**,由「更新真机调试地址.command」自动写入。
   为什么需要它:手机上的 127.0.0.1 指的是手机自己,永远连不到 Mac 上的本地服务;
   同一 Wi-Fi 下手机要用 Mac 的局域网 IP。IP 换了(换网络/重启路由)双击那个 .command 重写即可。
   只在沙箱联调用(USE_LOCAL_SANDBOX=true);连生产时完全不参与。 */
module.exports = {
  // 自动写入:2026-08-23
  lanHost: '192.168.2.15',
  port: 4310,
  updatedAt: '2026-08-23'
}
