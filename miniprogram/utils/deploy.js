/* 部署配置(店主 2026-08-23 裁定,假数回落同族第四例):
   **顾客端任何地方都不许硬编码租户名。** 拿不到租户上下文时,不许静默替换成任何一家店 ——
   小婕的顾客会看到 Lucky Luxe 的服务和加币价,这是最深的一例"回落到别人家的事实"。

   单店部署可以有默认,但必须走**这一个配置项**(构建/环境层改这里),而不是散在代码里:
     - 多租户构建:保持空字符串 = 结构性禁止回落,拿不到就去选店/扫码;
     - 单店部署:填该店 tenantId(例如 'lucky-luxe'),小程序即直接进这家店。
   网页顾客端同款配置在 apps/web/deploy-config.js(四之九:两端同刀)。 */
module.exports = {
  defaultTenantId: ''
}
