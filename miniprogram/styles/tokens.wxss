/* 设计令牌 · 小程序端 —— 值与 `apps/web/design-tokens.css` **逐条相等**(店主 05t 段 4 第 1 条)
 *
 * 同一把刀 `tools/design-token-diff.mjs` 同时比三份:合同图 / 网页 / 小程序,**三方一致**。
 * 值一个字都没改;**只有选择器换了**,因为 WXSS 里没有 `:root`:
 *   `:root{...}`                                   → `page{...}`
 *   `@media (prefers-color-scheme: dark){:root:not([data-theme="light"]){...}}` → 同一段,选择器换 `page`
 *
 * 🔴 **第三段(`:root[data-theme="dark"]`)小程序没有对应物,这是如实说明不是漏做**:
 *    微信小程序的深色由**系统**给(`app.json` 的 `darkmode` + `@media (prefers-color-scheme: dark)`),
 *    没有「站内再选一次主题」这个入口 —— 页面根节点上挂不了 `data-theme`。
 *    所以小程序只有「跟系统」这一种深色。要做站内主题开关是**新功能**,不在本批。
 *    刀里对这一段有专门的说明分支,不会因为「找不到」而误判成绿(静默失败器族)。
 */
page{--paper:#faf8f3;--ink:#23241f;--sub:#6d6f66;--line:#e3e0d6;--card:#fff;--wall:#f1eee6;
--acc:#0e6e62;--accd:#0a5a50;--brand:#b09468;--brandd:#8a6f45;
--hero:#2a2521;--hero2:#3a332c;--heroink:#f5efe3;--herosub:#bfb4a2;--herogold:#d9b97e;--herogrid:rgba(245,239,227,.12);--herolive:#6fd0a0;
--good:#2e8b57;--goodbg:#e6f3ea;--bad:#b3413a;--badbg:#f7e7e5;--flat:#7b7f76;--flatbg:#ededea;
--tagm:#8a5a00;--tagmbg:#faf3e3;--tagw:#274f8f;--tagwbg:#eef2fa;
--done:#cdbfa3;--doing:#d9b97e;--next:#7b7f76}
@media (prefers-color-scheme: dark){page{--paper:#191b19;--ink:#e8e7e0;--sub:#9aa096;--line:#33362f;--card:#20241f;--wall:#242822;
--acc:#4cc0b0;--accd:#63d2c3;--brand:#d3b483;--brandd:#e2c79c;
--hero:#15130f;--hero2:#221d18;--heroink:#f5efe3;--herosub:#a79c8a;--herogold:#e2c48d;--herogrid:rgba(245,239,227,.10);--herolive:#6fd0a0;
--good:#6fd0a0;--goodbg:#1c2f25;--bad:#e5867e;--badbg:#33201e;--flat:#9aa096;--flatbg:#2a2d28;
--tagm:#e0b45f;--tagmbg:#2b2618;--tagw:#8fb0e8;--tagwbg:#1d2434;--done:#6a5f4c;--doing:#e2c48d;--next:#6c7066}}


/* ── D183 · 站内显式选的那两档(店主 2026-09-09)────────────────────────
   `page{}` 那一段是浅色底、`@media` 那一段是「跟系统」;
   下面两段是**用户在通用设置里自己选**的那两档,挂在页面最外层那个 view 的 class 上
   (WXSS 没有 `:root`,根节点也挂不了属性 —— 见 `utils/theme.js` 抬头)。
   值与上面两段**逐字相同**,只是选择器换了 —— 这样「同一个语义只有一组值」。
   ⚠️ 令牌刀(`design-token-diff`)比的是 `page{}` 与 `@media` 那两段;
      这两段是它们的副本,改上面必须同步改这里(下一批把这条也纳进刀里)。 */
.theme-light{min-height:100vh;background:var(--paper);color:var(--ink);--paper:#faf8f3;--ink:#23241f;--sub:#6d6f66;--line:#e3e0d6;--card:#fff;--wall:#f1eee6;
--acc:#0e6e62;--accd:#0a5a50;--brand:#b09468;--brandd:#8a6f45;
--hero:#2a2521;--hero2:#3a332c;--heroink:#f5efe3;--herosub:#bfb4a2;--herogold:#d9b97e;--herogrid:rgba(245,239,227,.12);--herolive:#6fd0a0;
--good:#2e8b57;--goodbg:#e6f3ea;--bad:#b3413a;--badbg:#f7e7e5;--flat:#7b7f76;--flatbg:#ededea;
--tagm:#8a5a00;--tagmbg:#faf3e3;--tagw:#274f8f;--tagwbg:#eef2fa;
--done:#cdbfa3;--doing:#d9b97e;--next:#7b7f76}
/* 🔴 现测(裁 #21 拍到真机之后才看见的):只换令牌**不够** ——
   `page{background:...}` 挂在页面根节点上,而主题类挂在里面那个 `view.page` 上,
   于是深色下「文字翻成浅色、底还是白的」→ 浅字压白底,几乎看不见。
   所以两个类各自**把底色与字色也带上**、并撑满一屏。
   ⚠️ 这三条写在**各自块里**,不写成 `.theme-light,.theme-dark{...}` 的共用规则 ——
      令牌刀按 `.theme-dark{` 这个串找块,共用规则会让它找错块(现测红过一次)。
      刀只认 `--x: y` 这种声明,所以块里多几条普通样式不影响比对。 */
.theme-dark{min-height:100vh;background:var(--paper);color:var(--ink);--paper:#191b19;--ink:#e8e7e0;--sub:#9aa096;--line:#33362f;--card:#20241f;--wall:#242822;
--acc:#4cc0b0;--accd:#63d2c3;--brand:#d3b483;--brandd:#e2c79c;
--hero:#15130f;--hero2:#221d18;--heroink:#f5efe3;--herosub:#a79c8a;--herogold:#e2c48d;--herogrid:rgba(245,239,227,.10);--herolive:#6fd0a0;
--good:#6fd0a0;--goodbg:#1c2f25;--bad:#e5867e;--badbg:#33201e;--flat:#9aa096;--flatbg:#2a2d28;
--tagm:#e0b45f;--tagmbg:#2b2618;--tagw:#8fb0e8;--tagwbg:#1d2434;--done:#6a5f4c;--doing:#e2c48d;--next:#6c7066}
