# Lucky Luxe — Claude 工作说明(Cowork 与 Claude Code 共享)

美甲美睫店业务系统:网页老板端/员工端 + AI 客服 + 财务 + 小程序,演进方向是多商户 SaaS。
店主 chang,用中文交流。店在多伦多(America/Toronto),店主人常在其他时区——所有"今天"按门店时区算,前端 `storeToday()`、后端 `process.env.TZ` 已钉死,不要用裸 `new Date()` 推日期。

## 关键路径
- 后端(单文件): `apps/api/local-server.mjs`(Node 22,node:sqlite,零依赖)
- 前端: `apps/web/admin.js` / `admin.html` / `styles.css`(老板端+员工端同一套,按角色渲染)
- 数据库: `apps/api/local-data/lucky-luxe.sqlite`(真实经营数据,**已 gitignore,绝不入库**)
- 启动: `启动服务器.command`(--watch 自动重载,端口 4128);重置: `重置数据.command`(先备份)
- 规划与决策记录: `/Users/changliu/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/` 下的
  `ROADMAP_2026-07-04.md`、`财务系统设计方案.md`、`后台全面评审报告.md`、`员工端评审报告.md`

## 交付纪律(店主明确要求,必须遵守)
1. **先答疑/给方案,店主说"可以/继续"再动代码**(小修复除外)。
2. **loop engineering**:每次交付自测;全量回归 `bash apps/api/run-all-tests.sh`(21 套件,全新库可跑);交付报告附证据。
3. 前端改动后 **bump 版本**:`admin.js` 顶部 `ADMIN_BUILD` + `admin.html` 两处 `?v=`(侧栏可见,用于排查缓存)。
4. **绝不破坏已训练的 AI 客服行为**(matrix 66 项是底线);重构只在测试全绿下做。
5. 敏感文件不入 git:`local-data/`、`apps/api/data/`、`backups/`、`.env`、初始密码文件。
6. 收入只走财务账本口径,账本只追加(红字冲销纠错);财务数据有独立密码门禁。
7. **所有 `/admin/*` 路由必须注册在租户上下文闸门之后**(`tenantContext.enterWith` 那一行往下)。排在闸门之前的路由即使自己调了 `requireAdmin`,`currentTenantId()` 也会回落到 `DEFAULT_TENANT_ID`——非旗舰商家会看到旗舰店的数据。确有必要排在前面(如登录本身)的,必须在 `requireAdmin` 之后手动 `tenantContext.enterWith({ tenantId: admin.tenantId || DEFAULT_TENANT_ID })`。2026-08-08 `/admin/staff-accounts` 三条路由就是这么串味的。
8. **表结构改动:列一律走 `try/catch ALTER TABLE ADD COLUMN`**,只写进 `CREATE TABLE` 等于只对全新库生效,老库(含生产)不会跟上。`test-schema-consistency` 会把「空库新建 schema」与「老库跑完迁移后的 schema」逐表逐列 diff,忘写 ALTER 直接红。2026-08-08 签署快照四列就是这么漏的,差点让生产第一张单一签字就 500。

## 合同式交付制度(店主 2026-08-09 定,永久生效)

**设计图 = 合同。做出来必须跟图一模一样,差异即缺陷,不是"抽查式"通过就算完。**

1. **开工前逐屏提取交付清单**:元素 / 每个状态 / 每处交互 / 每句文案 / 双端覆盖,**一项一行**写进本批工作文件(`handoff/本批交付清单_*.md`)。没提清单不许开工。
2. **按清单实现**:图上没画到的情况,按最合理方式做并**记入假设清单**;不许悄悄发挥。
3. **逐屏并排核验**:每屏在浏览器 / 微信开发者工具里与设计图**并排比对**,清单逐项打勾。**「代码写了」≠「做完了」** —— 2026-08-09 那次 3b/3c 虚报就是只写了代码没并排比。
4. **报验必附**:对图核验单(逐项打勾)+ 假设清单 + 差异说明。与图不符的地方要么改,要么写清为什么改不了并等店主裁决。

## 测试标准(店主 2026-08-09 定):corner case 强制覆盖

每个功能至少覆盖这六类,每批报验**列出覆盖了哪几类,缺的写原因**:

1. **边界值** —— 0 / 负数 / 超大值 / 恰好等于门槛 / 分币余数(末行吃余数那种)
2. **空态** —— 没有数据时页面长什么样、接口回什么
3. **并发时序** —— 双代付烧同一张卡、一张券被两单抢、签字那一瞬余额被别处扣走
4. **越权** —— 员工调老板端点、跨店接口级断言(A 店 token 拿 B 店数据)、未登录
5. **幂等** —— seed 脚本 / 数据迁移重跑一遍,数字一分不动
6. **异常输入** —— 缺必填、超长字符串、悬空引用(指向不存在的 id)

**+1 财务红线断言(必须常驻)**:一笔大额充值(如 ¥10,000)记在某技师促成名下 →
断言 **日结业绩列 / 排行 / 员工端我的业绩 / 目标进度 全都不含它**,只有**冲卡列**与**首充/续卡提成**体现。
首充与续充、线下与线上,四种组合同样断言。充值不是业绩,混进去等于按充值额给技师发提成两遍。

## 脚本红线(店主 2026-08-09 定)

1. **任何脚本不得动老板账号的密码。** 只许动员工测试账号,且必须同步写
   `handoff/本地自查账号.txt`。后端 `reset-password` 路由本身就带 `role = 'staff'` 过滤,
   不要绕过它直连库改老板密码。
2. **回归跑完必须把店主的本地服务还回去。** `run-all-tests.sh` 会 `pkill local-server.mjs`
   —— 它打死的是店主正在用的 4128。跑完不还,店主打开小程序就是"每个接口都连不上"的空壳,
   看起来像登录坏了。脚本已内置 `restore_local`(开跑前记录在不在,结束时拉回来)。
   2026-08-09 店主登不进就是这个坑,不是密码问题。

## 闭环回归纪律(店主 2026-08-10 定,永久生效)

**任何新事物落地必须先列影响面清单**,再动手:

1. 这个动作在**小程序 + 网页全部环节**引起哪些改动 —— 逐个环节点名,不是"大概涉及日结"。
2. **上游从哪来 / 下游到哪去**:数据在哪产生、经过哪几层、最终显示在哪几处。
3. **每个显示处是否一致** —— 同一个事实在两端、在列表/详情/弹窗里必须说同一句话。
4. 逐项核验,**回归测试常驻检查完整性**(新字段要有断言,不能只在一处生效)。

**不允许出现「单独蹦出来找不到上下文」的界面状态** —— 比如台面说"本日休息"、
日结却冒出 2 单待确认,而行上不解释这 2 单是哪天的。用户看到的每一个数字都要能自证来源。

## 报告与结论纪律(店主 2026-08-09 定,永久生效)

1. **测试报告以 corner case 为主,正常路径一笔带过。** 店主要看的是"哪些刁钻情况被挡住了",不是"正常流程能跑通"——后者是底线不是成绩。
2. **「不下结论」的现象不许挂着。** 凡是能靠店主一次操作当场裁定的(点一下、敲一下、扫一次),就当场请店主裁定:真手也不行=真缺陷立刻修;点得动=工具限制,归档销项。不许留一堆"待真机复验"悬在那里当免责声明。

## 双单口径(店主 2026-08-09 拍板,《待办总方案》小拍板备忘)

**一个预约一张有效结算单**:
- 未签 → 可撤回重开(`POST /admin/settlements/:id/void`);
- 已签要补消费 → **新建即时预约再开单**,或走更正链;不许在同一预约上再开一张;
- **同一顾客同日多单合法**(挂不同预约即可)。
后端在 `createSettlementGroup()` 入口拦截,重复开单回 409 `BOOKING_ALREADY_SETTLED`,
已签/未签两种措辞不同。不拦的话同一次服务会出两张单,业绩、券、定金全部重复计。

## 账号体系
- 老板主账号 `boss`(首登强制改密);员工账号由老板在排班页生成/重置/停用。
- OWNER_TOKEN(启动日志里显示)是开发主钥匙:可解锁财务、跑测试;生产环境保密。
- 演示邮箱白名单登录仍兼容(上线前移除)。

## Cowork ↔ Claude Code 交接协议
店主的主工作台是 Cowork(规划/写码/沙箱测试/浏览器走查);Claude Code 负责本机操作(git 推送、.env 密钥、装环境、起停服务)。
交接方式:Cowork 端把任务写进 `handoff/本机任务.md`,店主双击 `执行本机任务.command` 触发无头执行,结果写回 `handoff/本机任务结果.md` 供 Cowork 审查。
Claude Code 收到"读取 handoff/本机任务.md"类指令时:严格按文件里的任务清单执行,不自由发挥,拿不准的记入结果文件;完成后把任务标记 [已完成]。
若店主直接在 Claude Code 里改了代码:提交信息里写清改动,方便 Cowork 端从 git 同步认知。

**指令格式约定(店主 2026-07-06 定)**:Cowork 端给出的操作指令一律写成【给 Code 的指令】——整段可直接粘进 Claude Code 执行,Code 视其为来自 Cowork 规划端的任务;只有 Code 做不了的事(输密码、OAuth 亲自点击授权、支付、删 PII 文件、物理操作)才单列为【需要店主本人】。Code 收到这类整段指令时照常遵守安全规则:拿不准就问店主,不越权。

## 当前阶段(2026-07-06 深夜)
阶段 0-3 完成;老板端+员工端评审落地;真实账号体系上线;**已完成云部署与数据迁移**:
生产地址 https://www.luckyluxeatelier.com(Railway US East/iad,Volume 持久化,每日自动备份),真实数据已上云(users 33/bookings 160/财务锁随库),云端演示登录已禁用。
部署方式:**GitHub 自动部署**(Source 已连 cl2763/LuckyLuxe@main,Wait for CI 已开:推送 main → Actions 全绿 → 自动上线;已端到端验证),不再用 `railway up`。
**云端 AI 已是真实 Qwen(专属部署 cn-hongkong/qwen3.6-plus)但未冒烟验证——最高优先级待办。**
后续:真实模型冒烟 → 微信支付(境内商户号)→ 企微申请 → 小程序老板/员工端 → 产品官网。
