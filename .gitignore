.DS_Store
node_modules/
*.log
# 🔴 白名单式放行(店主 02t 裁定一):回归原始日志是交付的**唯一原始证据**,
# 而 `*.log` 从 08-27 起把它们全挡在库外 —— 店主 01u 立过「我读不到的证据不算数」,
# 结果历批她能读到的只有我回执里的转述。那 40 条虚高就是这么来的:拿转述当了证据。
# 只放行每批一份收官全量日志(不是每次试跑的),文件名带批次号与套件数。
!handoff/regression-logs/*.log
__pycache__/
*.pyc
.env
apps/api/.env
.env.bak.*
apps/api/.env.bak.*

# Local demo database/runtime data (真实经营数据/初始密码文件,绝不入库)
# 注意: apps/api/data/ 里的 AI 知识库种子(ai-customer-service/)必须入库,只挡数据库文件
apps/api/local-data/
# 沙箱库(演示/走查用,2026-08-24 从 /private/tmp 搬进仓内固定目录:临时目录被系统清理=4310 屡次数据不见的根因)
apps/api/sandbox-data/
apps/api/data/*.sqlite
backups/
tmp/

# 本机任务执行结果(临时交接文件,不留历史)
handoff/本机任务结果.md

# Local IDE/private WeChat DevTools settings
project.private.config.json
miniprogram/project.private.config.json

# Generated video transcript/audio outputs
tools/video-transcript/outputs/
tmp/video-transcript-test/
tmp/video-transcript-test-xhs/
tmp/video-transcript-login-small/
handoff/本地自查账号.txt

# 设计/宣传交付物(2026-08-28 店主定:真身在桌面交付包里,不入 git)
# 立这条的原因:它们是 png/zip/mp4/dwg 和一个带 node_modules 的 Remotion 视频工程,
# 一次 `git add -A` 就能把上万个文件推进仓库,而仓库里留着也没人从这儿取。
# 一律用 / 打头钉死在仓库根:不加的话 `output/` 这种通用名会连累以后任何一层的同名目录。
/LuckyLuxe-ClaudeDesign-confirmed-v*/
/LuckyLuxe-*.zip
/LuckyLuxe-*.mp4
/LuckyLuxe-room-tour-poster-*.png
/LuckyNail-127sqm-*/
/LuckyNail-127sqm-*.zip
/lucky-nail-brief/
/luckyluxe-source/
/luckyluxe-output/
/luckyluxe-remotion-tour/
/radius-luvia-phase1*/
/radius-luvia-logo-proof-*/
/output/
