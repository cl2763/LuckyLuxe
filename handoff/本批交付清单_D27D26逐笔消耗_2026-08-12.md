# 本批交付清单 · D27/D26扩/储值逐笔补消耗 · 2026-08-12

## D27(🔴 财务页冻全 App)

| # | 项 | 状态 |
|---|---|---|
| 1 | **根因**:无密码店点「本月流水」→ 旧代码只认 getFinanceKey(),没钥匙就 toast+400ms navigateBack —— 回退撞进场转场动画,导航栈卡死,转场透明层残留顶层吃掉全 App 点击(店主"透明遮罩残留"直觉正确,层=微信导航转场层,非自绘弹层) | ✅ 已定案 |
| 2 | ①无密码店不再误拦:流水/记一笔两页 onShow 先问 lock-status,门禁关=直接进(条件在渲染/跳转前) | ✅ |
| 3 | ②要拦也安全:门禁开+无钥匙时,回退延迟 400→700ms(等转场走完,不再撞栈) | ✅ |
| 4 | ③常驻护栏断言:无可见浮层时页面 0 个全屏遮罩节点(L5 内每轮 page.$$('.mask')===0) | ✅ |
| 5 | L5 进出财务页 ×5 **全程实点**(本月流水按钮/筛选芯片),每轮断言页面可点+流水页停留不被弹走 | ✅ 5/5(G-D27-流水页正常进入) |

## D26 扩(绑定码弹层复制/链接全断)

| # | 项 | 状态 |
|---|---|---|
| 1 | **与 D27 排查结论:不同根**。复制断=微信隐私校验拦 setClipboardData(fail「api scope is not declared in the privacy agreement」),旧代码无 fail 回调→静默;链接断=本无点击处理器(实现缺口) | ✅ 定案 |
| 2 | 复制:成功 toast「已复制 ✓」/失败也有提示;签署码+绑定码两弹层同待遇(L2:弹层复制全扫;页面级复制入口 5 处均已有成功提示,列清单不改) | ✅ |
| 3 | 沙盒放行:app.json `__usePrivacyCheck__: false`(仅开发生效);**正式版须后台声明剪切板 scope** —— 已入发版清单并要求真机复验 | ✅ |
| 4 | 链接实点可跳:绑定码链接 → 直落本人确认卡(与真码扫后同一落点) | ✅ |
| 5 | L1+L5 全程实点:复制→读剪贴板≡铸码 url 5/5;链接→落确认卡 5/5;签署码复制 5/5 | ✅(G-D26 截图 ×2) |

## 储值逐笔补消耗(书面标准代图)

| # | 项 | 状态 |
|---|---|---|
| 1 | 列表扩两类:类型/时间/顾客/金额(充值 + 正显、消耗 − 负显,storeMoney)/经手人(充值)/关联单号(消耗,实点看单快照)/来源(结算抵扣/手工耗卡) | ✅ |
| 2 | 筛选芯片 全部\|充值\|消耗,默认全部;聚合卡「本月充值」「本月消耗」两行并列 | ✅ |
| 3 | 数据只读自既有留痕账本(消耗=结算扣卡行 note 解析单号+手工耗卡);红线:充值=预收不进收入、消耗=预收负债减少,零新统计口径 | ✅ |
| 4 | 断言:充值Σ≡卡片月充值 + 消耗Σ≡卡片月耗卡 + 结算扣卡行回链解析(CI ⑰ 8 条,套件 71→75) | ✅ |
| 5 | 浏览器实拍:合计≡卡片×2、筛选实点、单号实点落快照页 | ✅(G-SV2 截图 ×2) |

## 复发登记(店主点名写清)

**上一批断言链全绿、店主一点就断 —— 漏在哪层**:L5 用 `callMethod` 直呼处理器,绕过了
「触点→控件→处理器」前半段;遮罩层级、隐私校验(剪贴板 fail)、无处理器的死链接,
这三类只能被**真实点触**暴露。**永久护栏(纪律追加)**:凡弹层交互控件,L5 必须
`element.tap()` 实点;涉剪贴板必须读回剪贴板断言,不以 toast 为准。已写入占用声明工具痕迹档案。

## 假设清单
- A-1 D27 拦截兜底延时 700ms(转场约 300ms,留双倍余量);
- A-2 逐笔视图消耗回链只对「结算扣卡」行;手工耗卡显示「—」(无单可回);
- A-3 `__usePrivacyCheck__: false` 是沙盒态,上线由发版清单剪切板声明项收口。
