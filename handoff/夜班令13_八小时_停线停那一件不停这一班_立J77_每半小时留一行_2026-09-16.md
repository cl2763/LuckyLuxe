# 夜班令 13 · 八小时 · **停线停的是那一件,不是这一班** · 立 J-77

日期:2026-09-16 夜
上游:小批 08d 执行单(**顺序唯一真相仍是 08d §二**,本令把它摊到八小时里,不另起顺序)
店主原话:**「你要保持它可以一直运转满八个小时,不然我每天起来看的时候感觉已经停了很久。」**

---

## 〇 · 🔴 立 J-77 · 停线停的是那一件事,不是那一班

这是本令最要紧的一条,先说:

> **J-77 · 一条停线触发时,默认动作是「这一件停下 → 写清为什么 → 换下一件」,不是「整夜停在这里等人」。**
>
> **只有 §五 那张「停整夜」名单上的几条,才停全班。** 名单之外的一切,**一律按「挂起这一件、继续这一班」处理。**
>
> **第二款**:🔴 **夜里没有人可以答你。** 凡需要我裁的,**写进 `待裁队列_夜13.md` 然后往下走** —— **不许在原地等,不许 sleep 轮询,不许把「等裁」当成一段工作。**

**为什么立这条**:过去几夜的停线都写得很硬(这是对的),**但它们没说清「停的是哪一层」** —— 结果一条通道打不开,整夜就停在那里。**停线是用来防止做错事的,不是用来停止做事的。**

---

## 一 · 每 30 分钟留一行,不许补写

建 `handoff/夜13_进度行.md`,**每 30 分钟写一行**,格式固定:

```
[HH:MM] 段N · 正在做:XXX · 刚完成:XXX · 下一步:XXX · 挂起:N 件
```

**三条规矩**:
1. 🔴 **时刻必须是写那一行的真实时刻**,不许事后补写、不许一次补几行(J-38 同族:**不许手抄**)。
2. **最后一行必须是**:`[HH:MM] 本班结束于 HH:MM · 原因:正常收尾 / 停整夜(哪一条)`。
3. **如果某 30 分钟确实没有可写的进展,照样写一行**,写清「在做什么、卡在哪一步」——**空白比坏消息更难看。**

**店主一睁眼看的就是这份文件的最后一行。** 如果最后一行是 02:15,她立刻知道停在哪;如果是 08:00,她知道跑满了。

---

## 二 · 八小时分段(按 08d §二 的序,不改)

| 段 | 时长 | 做什么 | 提前做完 / 被挂起怎么办 |
|---|---|---|---|
| **一** | 0:00–0:45 | 🔴 **Railway 通道三条只读现查**(`railway status` 完整输出 / `--version` / `whoami`)→ 判 **A / B / C**。**判 A(参数没对上)→ 直接用对的参数重试;通了就跑那四条痕迹查询**(一字不改,带库路径+指纹+底数行) | **判 B 或 C → 挂起,写进待裁队列,立刻进段二。**🔴 **不许加钥匙、不许改 Railway 任何配置、不许找别的路进生产** |
| **二** | 0:45–1:15 | **D195 小图复制到桌面交接夹 + 两处 md5**(我醒来批)· **J-61④ 判据物归位**(靶子/夹具/造病物/probe 样本落到统一可排除目录 + 两面都断言的那条判据)· **重报 J-60 正门欠账真值**(确认 28 没被污染) | 做完即进段三 |
| **三** | 1:15–2:45 | **5 把刀补长相(J-73)**,按序:**① `test-web-not-signup`**(裁 #103 的判据 —— 守错层 + 只认一种写法,两个缺口叠在同一条上)→ ② `assert-reads-fact`(缺模板串,本仓最常见写法)→ ③ `success-state-trace` → ④ `customer-paths-scan` → ⑤ `success-state-scan`。**每把报「补前 / 补后 / 差」三数**;🔴 **历史上报过的 0 按 J-56 全部标存疑**,有差按 J-50 归因 | 补不完就补到哪算哪,**剩下的进待办,不许为了赶完而省掉「重跑出表」那一步** |
| **四** | 2:45–6:00 | 🔴 **主段 · 40 个口的破坏测试开跑**。**先用 30 分钟把 11 条追不到口的断言归口**(「追不到口」=「不知道涉不涉钱」,按 J-66③ 先按最坏那格对待),然后**涉钱 35 优先**。**每个口按六格报并闭合到底数**:红 N〔**逐条点名 + 因果链**〕/ 无关 / 该咬没咬 / 说不清 / 没跑到 | **这一段是兜底段,做不完是正常的** —— 它保证你永远有活干 |
| **五** | 6:00–6:45 | **推 main 准备件**(CI 全绿 · 备份脚本就位 · 推后只读核验四项清单)—— 🔴 **准备好就停,不许推** · 门关档 **红 3**(A 族)· **J-60 至少降 5** · **9 条冻结理由按 J-70 复核**(有「红在真病上」的改挂缺陷号) | 做完即进段六 |
| **六** | 6:45–7:30 | `customer.js` **先摘后动**(裁 #105):中英文案表 → `customer-copy.js`;报 **摘前 / 摘后 / 新棘轮** 三数,**棘轮跟着降,不留余量** | 做完即进段七 |
| **七** | 7:30–8:00 | **收尾**:早报 `handoff/早报_2026-09-16.md` · `待裁队列_夜13.md` 定稿 · 三个棘轮现测 · 账本领先数 · 断言数与增量对账 · 进度行最后一行 | — |

---

## 三 · 🔴 兜底队列(任何一段提前完成或被挂起,**立刻从这里取,不许空转**)

按序取,取到哪写到哪:

1. **其余 42 个口**继续造病(顺序:顾客能碰到的 → 后台内部的)
2. **J-60 正门欠账剩余**(每多降一处就记一处,带归因)
3. **12 把刀里还没补长相的那几把**(按 J-73 各报靶子长相数并补)
4. **门关档 A 族 3 套**的修法(夹具换正门签发),每套报「**夹具现在走的是哪条路**」,不许只报绿
5. **路径总数重量**(40 条已知偏少,J-56 要回去改产出它的那份文件)
6. **那 10 张 `unused` 券的四样事实** —— **只查不改**
7. **生产 37 行是否种子数据**的查询**准备**(按 J-75 三条:同构库跑通 · 每列先探存在 · **阳性对照**)——**只准备,不跑**,等通道
8. **小程序档 mp 3 套红**(`MP_AUTOMATOR` 没设、仓外模块没装)—— 先把「为什么没装、装了会怎样」写清楚,**不许自己往仓外装东西**

**队列取空了(不太可能),就回段四继续造口。**

---

## 四 · 待裁队列(写下来就走,不等)

建 `handoff/待裁队列_夜13.md`,每条一行:

```
[HH:MM] 事由 · 我查到的事实 · 我倾向哪一边(可以没有)· 为什么要她裁
```

**凡属下列情形一律进队列并继续往下做,不许停在原地**:
- 通道判成 B 或 C(要不要加钥匙)
- D195 图我还没批
- 造病里出现「说不清该进哪一格」的
- 判据与合同图冲突、或要改合同图的
- 任何「我觉得这样更好但令里没写」的改动

---

## 五 · 🔴 停整夜的名单(**只有这几条**,其余一律只停那一件)

1. **发现第二个 D194 级别的洞**(无认证写库 / 涉钱 / 涉顾客隐私)→ **停手,写完整报告,转只读盘点,不继续改任何代码。**
2. **发现生产被打过的痕迹**(痕迹查询非 0)→ **立刻停,只报不动,一行都不许删。**
3. **主档回归红且 30 分钟定位不到** → **停「落刀类」工作**,转段三/兜底队列里的**只读盘点类**工作;**这一条不停全班。**
4. **沙箱模型 > 600 万 token** → 停 AI 相关,其余照常。
5. **环境坏了**(磁盘满、库打不开、工具链崩)→ 修复优先;修不好则停全班并写清现场。

**名单之外的一切** —— 通道打不开、图没批、判据说不清、某把刀补不完、某个口造不出病 —— **一律:挂起这一件、写进待裁队列、换下一件、这一班继续。**

---

## 六 · 停线(照旧,一条不松)

**生产 / 通道**:不许加钥匙 · 不许改 Railway 任何配置变量 volume · 不许找别的路进生产(数据库直连串 / 新服务 / 临时端点)· **不许向生产发任何会写入的请求**(append 也是写)· 痕迹查询非 0 立刻停只报不动 · **不许把「本地已删」写成「已修复」** · **D194 从线上消失之前不许接通境内代理、不许把备案域名指到后端**。

**判据 / 报数**:分类数加不上底数就停在那里报 · **红 N 没有名字这份报告不算交** · 看到欠账数变大第一反应不许是降回去(先问它怎么会变大)· 凡落刀必经造病台(含「只是快速试一下」)· **不许松判据 / 抬棘轮 / 换尺子 / 改合同图来凑代码** · 要落生产的查询三条批准条件缺一不批(同构库跑通 · 每列先探存在 · 阳性对照)。

**产品**:🔴 **凡要在网页顾客端加「注册 / 创建账号 / 新建顾客」的,一律停下来问,不管理由多充分 —— 这一条没有例外,因为它的每一次例外都长得很合理。**

**其余**:不推 main · 不碰生产(只读)· 不看密钥文件 · **不许 sleep 轮询** · 不许 `git checkout --`。

---

## 七 · 早报要什么(店主一睁眼看这两份)

**`handoff/夜13_进度行.md`** —— 16 行左右,最后一行写「本班结束于 HH:MM」。

**`handoff/早报_2026-09-16.md`** —— 五段,每段短:
1. **八小时干了什么**(按段,一段两行)
2. 🔴 **挂起了几件、各因为什么**(这是店主最想看的)
3. **待裁队列几条**(一句话一条)
4. **数**:主档 · 门关档(判据欠账 N / 挂缺陷号 M)· 断言(带增量对账)· 三个棘轮(带基准)· 账本领先 · **40 个口做了几个 / 六格闭合没有**
5. **🔴 有没有触发停整夜的那五条**(没有就写「没有」)

**本令 md5 两处一致**(桌面 + 仓库 `handoff/`),回执里报。
