# 小批05r — 🔴 网页新首页与合同图 §三 **UI 不是一回事**(D154:按图重排,退役旧卡)· 🔴 小程序调 AI 走的是另一条路(D155:三入口同一出口)· 小程序首页**未动**(段 9–11 还没开始)· 修完这两条再续段 7

> 2026-09-08 · 店主亲看 4310 新首页后原话:「他跟我的合同图完全就是在 UI 方面是不一样的」「你更新小程序的首页了吗?还是只更新了网页的主页」「确保之前训练的所有 AI 在小程序接入外部 API 的时候,是可以正常使用的,就像网页一样」「把这些修改完之后再去做下一段,也就是续段 7」· Cowork 现查在下 · 两处各一份:桌面交接夹 + 仓内 `handoff/`

---

## 〇、先答店主三问(Code 回执里照抄这三句给店主)

1. **网页首页**:段 6(`999c549`)只把**数据节点**接上了,**页面没有按图 §三 排**——旧的四张卡还在页顶,大屏三块是竖着叠的,图里的「此刻四格 / 下一位卡 / 右四小牌 / AI 今日一句 / 今日台面嵌入 / 两栏」一个都没有。本批 D154 按图重排。
2. **小程序首页**:**没动**。`git log --since=2026-09-05 -- miniprogram` 为空;小程序老板视角(段 9)、员工打卡门(段 10)、全屏态(段 11)都还没开始,顺序仍按夜班令 4,排在段 7、8 之后。
3. **小程序调 AI**:**现在不是「就像网页一样」**。小程序 `pages/ai-chat` 调的是 `/ai/customer-service`,那条路直接调模型,**绕过了** `handleWecomInbound`——预约采集、报价采集、事实闸、转人工、会话流水、待审、D147/D148 全都没走。本批 D155 把它并成同一出口。

## 一、🔴 D154 · 网页首页按图 §三 重排(图=合同,布局也是合同)

**Cowork 现查(4310,真登录截图 + 源码)**:`admin.html:411` `#metricGrid`(已确认 / 待支付 / 月服务 / 总服务,`admin.js:1362–1372` 前端从 bookings 自算)仍在页顶;`#aiBriefPanel` 仍是独立块;`#fullDemoSeed`「演示数据」按钮仍在标题行;`dashboard-home.js` 只输出三个竖叠的 `<section class="card">`(大数+小牌 / 此刻 / 要处理),无两栏、无下一位卡、无折线(`pulse` 已经给了 `spark` 数组,前端没画)、无 AI 一句、无台面嵌入。段 6 回执写「DOM 现测有证据」——证据证明的是**节点在**,不是**页面按图**。Cowork 自己也只核了 DOM 文件没开页面,一并记 J-32(见 §四)。

**裁(逐块对照图 §三,每块一个稳定选择器,判据锚选择器不锚文案)**:

| 图 §三 的块 | 做法 | 选择器 |
|---|---|---|
| 顶行旧卡(已确认/待支付/月服务/总服务) | **退役**:首页不再渲染 `#metricGrid`;它们违反「整页只吃三个接口、前端零计算」 | 首页内 `#metricGrid` **无子节点或不存在** |
| 独立 `#aiBriefPanel` | 退役为块;其内容改成右栏通栏「AI 今日一句」 | `#aiBriefPanel` 首页不存在 |
| 「演示数据」按钮 | 首页不出现;搬到通用设置页 | 首页内 `#fullDemoSeed` 不存在 |
| 英雄区两栏 | 左栏 + 右栏并排(≥900px);左栏撑到与右栏齐平,**不留空白** | `[data-dh-hero]` 含 `[data-dh-hero-left]` `[data-dh-tiles]` |
| 周期条 + ⤢ 全屏占位 | 照旧 | `[data-dh-periods]` |
| 「营业收入 · 服务 + 耗卡 + 产品」大数 + 币种小字 | 大数由 `money()` 出口出;小字币种码由后端 `currency` 给 | `[data-dh-metric="revenue"]` 内 `<small data-dh-cur>` |
| 月目标 % | **只有门店设了月目标才出**;现在没有这个配置项 → 本批**不出这行字**,回执写明「月目标待门店设置项,另批」;**不许编 62%** | — |
| 比上期 | 人话:今日「比昨日」/ 本周「比上周」/ 本月「比上月」/ 本年「比去年」;上期 0 仍是「上期 0,没法比」 | `[data-delta]` |
| 近 N 点折线 | 用 `pulse.metrics[revenue].spark` 画 `<svg>`,点数 = 数组长度;**全 0 不画**(图 §六) | `svg[data-dh-spark]` `circle` 数 = spark 长度 |
| 此刻四格 | 在做 / 待到店 / 已完成 / 今日预约,**数字不是句子** | `[data-dh-now]` ×4,位于 `[data-dh-hero-left]` 内 |
| 下一位卡 | 时间大字 + 顾客 + 项目 · 技师 · 时长 + 「看台面 ›」→ 工作台;没有下一位 → 「后面没有待到店的了」 | `[data-dh-next]` 含 `[data-dh-next-time]` `[data-dh-next-go]` |
| 右四小牌 | 现金业绩 · 实收 / 总卡耗(+「次卡 N 次」小字,没次卡流水出「—」)/ 新增持卡 / 到店人次 | `[data-dh-tiles] [data-dh-metric]` ×4 |
| AI 今日一句(通栏) | 来源 = 现有日报 `createDailyBrief` 的**已生成**结果 + 生成时刻;今天没生成过 → 「今天还没有一句」,**不为首页新起模型调用** | `[data-dh-ai-line]` 或 `[data-dh-ai-none]` 互斥 |
| 今日台面(左下) | **原样嵌入** `window.TodayBoard.mountInto`(`today-board.js`),不另画;静态刀:`dashboard-home.js` 里不许出现台面格子 HTML | `[data-dh-board]` 内的根节点 class 与工作台台面根节点**同名** |
| 今日要处理(右下) | 按急缓排:`aiHandoff`、`quotePending` 标 `data-urgent="1"` 排前;0 项无节点(照旧) | `[data-dh-todo] [data-urgent="1"]` 在前 |
| 休息日 | **只替换**此刻四格 + 下一位卡为「今日休息」;大数、小牌、要处理照出 | `[data-dh-truth-closed]` 在 `[data-dh-hero-left]` 内,且 `[data-dh-now]` 数 = 0 |
| < 900px | 退化为竖排(小程序同款) | 媒体查询;截图一张 |

**证据(交齐必附,否则不收)**:三店真登录截图各一张(≥1280 宽,今日态),外加:一张 <900 窄屏、一张休息日(造景:把北京店当天设休)、一张失败态(造态:停后端接口)。附**逐块对照表**:图 §三 的块 → 选择器 → 截图里的位置,一行一块(上表 17 行)。DOM 判据在段 6 的 30 条上**只增不删**,新增 ≥ 12 条(上表右列每条至少一条);造病至少三处:恢复 `#metricGrid` 渲染 → 红;把台面改成自画 → 红;spark 全 0 仍画 → 红。

**「客服待人工 660」口径(店主截图里那个数)**:4310 库现查 `lucky-luxe` 的 `wechat_conversations`:`needs_human` 656 + `human_active` 4 = 660,全是历次评测跑机留下的会话。裁两条:① **待人工只数 `needs_human`**;`human_active` 是同事已经在接,不算「待处理」;② **J-33 跑机收尾律**:五把评测跑机(`booking-rate` / `fact-hit` / `human-five` / `run-200` / `run-edge-80`)结束时把**本次执行自己开的**会话置 `closed`(按 J-31 的执行随机段认领,不许按店清);历史 656 条一次性关闭——**写 4310 沙箱库** `apps/api/sandbox-data/lucky-luxe.sqlite`,只改 `status`,先 `tools/db-backup.mjs`,回执报「改前 656 → 改后 N」+ `receipt-db-proof.sh`;jics-store 515 条同法;**4128 不动**(那里的 1 条 `human_active` 是小婕店真会话)。

预算 **150 分**(超一倍停,写原因)。

## 二、🔴 D155 · 小程序调 AI 与网页**同一出口**(D153 扩到三入口)

**Cowork 现查**:
- `miniprogram/utils/api.js:768` `aiCustomerService(message, history)` → `POST /ai/customer-service`,`history` 由**客户端**带;`pages/ai-chat/index.js:37` 调它。
- `local-server.mjs:11513–11550`:`/ai/customer-service` 自己拼 `knowledgeContext` 后**直接** `createCustomerServiceReply(...)`(全仓第二个调用点;第一个在 `handleWecomInbound` 里 `:3885`)。它不经 `handleWecomInbound`(`:3373`),所以 `bookingIntake.step`(`:3768/3907`)、报价工作流、`factGate.check`(`:3915`)、转人工、`conversation_messages` 流水、待审、D147/D148 口吻**一个都没过**;它的「未开通」文案(`:11524`)也和 D147 那句是两套话。
- 走 `handleWecomInbound` 的入口有三个:企微 `sync_msg`(`:1642`)、模拟器 `/admin/wechat/mock-chat-message`(`:11657`)、`/admin/wechat/mock-message`(`:11703`)。
- 小程序里现在**没有**任何顾客可见的 AI 入口(08-04 店主裁下线;`open-type="contact"` 也没接)——`pages/ai-chat` 只是留着的页。

**裁**:
1. `/ai/customer-service` 改成**薄壳**:`normalizeWecomInbound({ channel: 'miniprogram', externalUserId: 小程序登录用户 id, tenant, message })` → `handleWecomInbound(inbound, req)` → 按原返回结构回 `reply`;**客户端不再传 `history`**(会话流水是唯一记忆,与企微/模拟器同);未开通时的话就是 D147 那句(平台状态格同步)。
2. 判据(静态白名单式,D153 同族):全仓 `createCustomerServiceReply(` **只剩 `handleWecomInbound` 里那一处**;`handleWecomInbound(` 调用点 = 企微 / 模拟器 / mock-message / `/ai/customer-service` 各一行,多一处少一处都红;造病:让 `/ai/customer-service` 绕回直调 → 红。
3. 同答判据:同一店、同一句(六通里通一「疼不疼」、通三「9 月 7 日能约吗」、通六「做个法式多少钱」),从 `/ai/customer-service` 与 `/admin/wechat/mock-chat-message` 各发一次(新会话,J-31),`intent` 相同、文本相同(模型 mock 下)、`conversation_messages` 各落两行;三店都跑。
4. **小程序真跑**:入口按店主 08-04 裁定**仍不显示**;DevTools 自动化里用 `wx.navigateTo` 直开 `pages/ai-chat`,北京店发通三那句,截图里回复与模拟器同一句;小程序三把刀改前改后各跑一次。
5. D151 合并窗(段 7 里做)落在 `handleWecomInbound` 入口处,**小程序自然也吃到**——回执里写一句。
6. 真外部通道(微信客服 / 企微)接通仍卡 ICP → 固定 IP → 可信 IP,不在本批;本批证明的是「小程序一旦调这个 API,答案与网页、模拟器**是同一个**」。

预算 **90 分**。

## 三、然后才续段 7

D154、D155 都交齐(提交号各一)后,按**夜班令 4 原序**做段 7(05q §二 D150–D153 + §四 六通 v4)→ 段 8(D149)→ 段 9–11(小程序:老板视角 / 员工打卡门 / 全屏态)。段 9 才是「小程序首页」——回执里向店主说清楚。

## 四、Cowork 自记两条(登总表,不用 Code 做)

- **J-32**:段 6 我只核了 `dashboard-home.js` 与 DOM 证据文件,没开页面对图;「数据节点齐」被当成了「页面按图落地」。以后**凡带图的段,Cowork 收之前必开页面截图与图并排**,DOM 证据不替代截图。
- **J-33**:跑机收尾律(见 §一 末)。

## 五、交齐

D154:三店 + 窄屏 + 休息日 + 失败态截图六张、逐块对照表 17 行、新增判据 ≥12 条、造病三处、`#metricGrid`/`#aiBriefPanel`/`#fullDemoSeed` 首页无节点证据、660 → N 的 `receipt-db-proof` · D155:两处调用点白名单刀、三店同答表、小程序 DevTools 截图、小程序三把刀前后 · 提交号 · 回归/断言增量 · 整跑时长 · 棘轮两数(`local-server.mjs` ≤ 17,751、`admin.js` ≤ 8,465,D154 搬出旧卡应再降)· 未推 main。
