# 模拟器/浏览器 自动化占用声明(开关文件)

状态:未占用(2026-08-12 末批已收工,模拟器已还;商家端=老板会话、无财务钥匙=店主原始态)

店主末次复核(先点一次「编译」,三小件):
① **D29**:直接进「工资」(试算/发放)→ 应直进,不再提示「去财务页解锁」;member 页给会员加储值同验;
② **D30**:任一出码弹层(继续结算的签署码 / 出示绑定码)→ 占位码下两颗按钮:
   「打开链接」→ 落对应页(签署码→签署页,绑定码→本人确认卡);「复制链接」→ toast「已复制 ✓」;
③ **弹层 v1.1**:排单/日结点开单据预览 → 底栏没有「关闭」了(✕ 或点空白关);
   已签单=一颗通栏「查看签署原图」;未签单底栏整条不显示。
全过 → Cowork 宣布「开检反馈 🔴🟠 全清+缺陷线闭环」,S 组开工。

店主复核 D28(先点一次「编译」)—— **从排单把整条链走通**:
① 台面点一单**已签署**的(如 08-09 任一)→「查看结算单」→ 预览弹层(单号+绿章/明细排版件/签名区);
   「查看签署原图」进全屏、✕/关闭/点空白三法都能关;
② 台面点一单**待签**的 →「继续结算」→ 自动回出码态;关掉码,页面还在:
   未绑定客走【出示绑定码】→ 顾客扫码绑定 → 面板解锁 → 充值 → 抵扣 → 推送签署 —— 整链应一路通;
③ 日结里「查看签署单」→ 同一个弹层(不再开新页面);
④ 顺手:此前 4 个静默死弹窗已复活(积分兑换撤销/员工账号生成/券到账提示/工资发放确认)。
全过+D26/D27/逐笔三件已 ✅ → Cowork 宣布「开检反馈 🔴🟠 全清+缺陷线闭环」。

店主复核(先点一次「编译」,三件):
① **D27**:直接开财务页 → 点「本月流水」→ 应直接进流水页(不再闪"解锁"提示、不再冻死);
   进出几次,全程各页可点;
② **D26**:未绑定顾客开单 → 出示绑定码 → 弹层里「复制绑定链接」→ toast「已复制 ✓」;
   点链接本身 → 直落本人确认卡;签署码弹层「复制链接」同验;
③ **逐笔视图**:财务页储值卡片 → 芯片切「全部/充值/消耗」;消耗行负显、点单号能看单;
   月充值/月消耗两行与逐笔合计一致。
全过+前批 D24/D25 已 ✅ → Cowork 宣布「开检反馈 🔴🟠 全清+缺陷线闭环」。

店主复核(先点一次「编译」;D24/D25 两件 + 本批三件顺手看):
① **D24 排单粒度**:台面排单 → 服务区两级(大类→小类,与结算目录同源);排单选的项带入结算精确对上;
② **D25+绑定前置**:选未绑定顾客开单 → 充值链接灰化、提示条带【出示绑定码】→ 点开弹层(沙盒占位码+链接)
   → (真机流程等真码;沙盒可复制链接给顾客端"带参编译"看确认卡)→ 绑定完成后面板自动解锁充值;
③ **技师内容分配(原样恢复)**:组内选 2 位技师 → 条目编号清单+每人一排数字按钮,点自己做的编号,
   同一编号可两人都点;单技师不出现;组② 编号重新从①起;
④ **储值逐笔**:财务页储值卡片下,本月充值逐笔(时间/顾客/金额/经手人/来源),合计=卡片月充值。
全过 → Cowork 宣布「开检反馈 🔴🟠 全清+缺陷线闭环」。

说明(《交付自检闭环》四之四):
- Code 跑任何自动化(miniprogram-automator / 浏览器脚本)**之前**,把「状态」改成
  「占用中 · 开始 YYYY-MM-DD HH:MM · 用途 xxx」;跑完立刻改回「未占用」。
- 店主开检 / 人工复验时:先看这一行。写着「占用中」就**别碰模拟器** ——
  自动化会往输入框写值、会强制 reLaunch 页面,人工同时操作必然撞出假象。
- **灵异现象第一排查项**:出现"改不动删不掉的字符""页面自己跳走""底部导航消失"
  这类现象时,先回头看当时这一行是不是「占用中」,再谈是不是产品缺陷。

工具痕迹档案(排查模板,持续累积):
- D9 结案:automator 的 `.input()` 写 `type="number"` 框会显示字面 "undefined" —— 工具的锅;
  `type="number"` 的框一律用 callMethod 触发真实 handler 验。
- 本批新增:automator 的 `page.data()` 把 data 里的 **null 读成 undefined** ——
  断言 `=== null` 会全红。判空一律用宽松 `== null`,先截图定性再改脚本(L5-充值面板那轮 0/5 就是这个)。

案发记录(立这条规矩的由来):
2026-08-10 19:00–00:03 Code 连续 5 小时跑自动化未声明,期间店主开检撞出两个"缺陷":
  · D15 点「我的」跳进客户库、底部导航消失、2 次后自愈
    = Code 的 customers L5 脚本在连续 reLaunch 那一页(已实锤)
  · D9 手机号框里字面 "undefined"、改不动删不掉
    = automator 的 .input() 写非受控 type="number" 框(已结案,工具的锅+受控修复)
两个假缺陷,双方各花了好几轮。
