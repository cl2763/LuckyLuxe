# 三样退回 · 修复与实拍自证(2026-08-30)

---

## ① 小程序账户调整页白屏

**文件层查明(三层证据)**:四件套(wxml/js/json/wxss)**都在磁盘上**、app.json **已注册**(pages[18])、
git 提交 `313078e` **里都有**(wxml 90 行)。语法排除(页面没用需增强编译的写法,且 `enhance:true`)。

**抓到一处真 diff 并修掉**:我用 `json.dump` 重写 app.json 时**吃掉了文件尾换行**(`\ No newline at end of file`)——
已补,并加常驻断言钉住。

**你要的常驻断言已立**(`test-mini-account-adjust.mjs` ⓪ 族,双向白名单式):
- app.json **64 个注册页**,每页 wxml/js/json 三件必须都在(缺一件=白屏 `__route__` 那个病);
- **反向**:磁盘上每个页面目录必须在 app.json 里 —— **当场立功:抓出两个历史死页**
  `pages/admin/` 与 `pages/admin-login/`(零引用、未注册、被 merchant-login 取代)→ 已删(git 有底);
- app.json 尾换行断言。
- **变异**:把新页 wxml 挪走 → 断言当场红;还原回绿。**以后新建小程序页,这条就是底线。**

**如实报**:文件三层全对,我复现不了她的 not found —— 最像**开发者工具的编译缓存**
(工具在新文件落盘前打开着,产物没含新页)。请店主一次操作裁定:工具里「清缓存 → 重新编译」再进;
若仍白屏,把报错截图给我(那就是新病,立案再查)。

## ② 网页「今天台面」

**逐嫌疑查完,证据链如下**:

1. **手动 restore 版本嫌疑 —— 排除**:`start-sandbox.sh` 起的是工作区代码;她报的 `v38225bfc`
   **就是当前最新构建的 LL_BUILD**(我本机现测同值)。指纹机制**是活的**(改一字节 → `103501a0`,
   还原 → 回 `38225bfc`,实测)。她加载的页**含今天台面代码**。
2. **入口接线 —— 在**:`adminView` 初始值就是 `'today'`,进订单管理默认即台面。
3. **同路径实拍复现**:4310/admin → 订单管理 → **台面就在**(日期条/pills「今日 5 单 · 在岗 2 人 · 空档 13.5h」/
   技师网格/徽标,与我交付时同一张图;`LL_BUILD=38225bfc` 与她一致)。

**修了两件让这类事以后当场现形**:
- **`/health` 新增 `adminBuild`**(现算的 LL_BUILD,与页面左下角同源)—— 你报的版本串与
  `curl /health` 一对,「服务实发的是哪版前端」不再靠猜;restore 拉错版本立刻现形;
- 我复现下她看到月历的唯一路径 = **停在「日历」tab**(日历本来就没改,所以"没有任何新东西")。
  请店主一次操作裁定:**刷新后点最左边的「今天」tab** —— 台面应当就在;若点了「今天」仍是月历,
  截图带左下角版本串给我,立案。

**另:她走查的时间可能撞上我 pkill 事故窗**(4128/4310 当时 DOWN,上批已自首)——服务打不开时
浏览器展示的旧标签页什么都不会变。现两口已在线且实测台面在。

## ③ 按钮形制统一

客户档案四个动作改成**同一形制两行 hbtn**(与「写服务小记」同规格):
> 第一行:「＋ 写服务小记」「顾客画像 ›」 第二行:「账户调整 ›」「发优惠券 ›」

长条 `linkbtn` 不再用于这两个动作(常驻断言钉住;`linkbtn` 样式保留给别处既有用途)。

---

**纪律栏**:60 套件回归(数字回帖)· 棘轮 local-server +11(/health adminBuild)后现测见回帖 ·
变异两刀(页面完整性 / 上批四刀不重列)红过还原 · **生产库未动 · 本机库未动 · 沙箱库未动 · 未推 main**。
走查单不变,店主重走三样。
