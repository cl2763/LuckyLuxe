# 小批 12d 补三 —— 你我量出的「死白」对不上,先对尺子;P1/P1b 改报手框指标;P1b 出图也过三档回贴

停线不变。不加口令变体、不加钱(回贴 ¥0)。

## §一 尺子对不上(必须先解决,否则对比包上的数没一个能信)
我在店主机上对 `round4_hand/` 六张全分辨率自量(L 通道 >225 占比,Pillow `convert('L')`):

| | 你报的 P1 死白 | 我量的 |
|---|---|---|
| 23 | **1.57%** | **6.83%** |
| 02 | 6.29% | 6.03% |
| 18 | 0.40% | 0.57% |

02/18 差一点点,**23 差四倍** —— 不是舍入,是两把尺子。可能的原因(你查,不要猜):量的不是同一张文件(缩略图/裁切件)、阈值或亮度公式不同、量的是手框不是全图。
裁:①`region-metrics.mjs` 里**只留一个** `deadWhite()`,写明:输入哪张文件、哪个通道、哪个阈值、全图还是框内;②对比包、回执、`_回贴量.json`、`_跑批日志.json` 全部改从这一个函数出数;③把 23 号按这一个函数重量,回执里贴「旧数 / 新数 / 差在哪」。**J-112 第二款**:一个数两把尺子量出两个值,先对尺子再谈结论。

## §二 P1/P1b 的 R:B 与死白改报**手框**(全图数保留作对照)
「略白净」这句的宾语是手部皮肤,拿全图 R:B 判它,背景一起被平均进去。皮肤掩膜你已证准(§七-1),所以:每张报**手框 R:B、手框死白**,判据线沿用(手框 R:B ∈ [原图手框 −0.08, +0.15]);全图数照报但不判红。4/6 拉冷这条**先不改口令**,等她看图 —— 她要的就是「略冷」,超没超是她的眼睛定,数字只标红。

## §三 P1b 六张出图**也过 LM-Y / LM-YC / LM-50**(¥0)
对比包手部区每张改为:原图 | 第三轮 | P1 | P1b | P1b+LM-50 | P1b+LM-YC。LM-YC 会把色度贴回原图 —— 它正好是「拉冷过头」的解药,让她在 P1b 与 P1b+LM-YC 之间挑,比再改口令便宜。

## §四 记录
- 你对「整句换成 HAND」的推定读法(删矛盾分句、HAND 留原位)**收**,结果等价、语序对。
- 02/23 换桶造成的双变量混淆**收**,对比包脚注写明「仅 18 号可干净归因」。
- 判据加一条:对比包每个数旁边标 `[全图]` 或 `[手框]`,缺标签视为无数。
