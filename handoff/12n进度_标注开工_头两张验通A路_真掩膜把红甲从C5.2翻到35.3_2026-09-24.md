# 12n 进度(第一节)—— 标注开工;头两张就把 A 路验通了

多伦多 2026-09-24 凌晨。**¥0,模型 0 次调用。** 围栏照旧,`apps/` 未出现在我写过的文件里。

## 一 关键结果:真掩膜一上,红甲立刻归位 —— **而且用的还是 12i 的起手门槛**

| 号 | 是什么 | 肤色粗估(旧) | **手描真掩膜(新)** | 机器判 | 人眼判 |
|---|---|---|---|---|---|
| 14 | 正红波点甲 | Y 21.8 / **C 5.2** → 深·不鲜明 | Y 61.2 / **C 35.3** | **深·鲜明** ✅ | 深·鲜明 |
| 02 | 纯黑甲 | Y 46.0 / C 5.4 → 深·不鲜明 | Y 53.8 / C 4.4 | **深·不鲜明** ✅ | 深·不鲜明(黑) |

🔴 **这一条值得你先知道:门槛 25 很可能本来就是对的,坏掉的是掩膜不是门槛。**
14 号红甲的色度从 5.2 跳到 **35.3** —— 一步越过 25,不用调任何门槛就落进「深·鲜明」。
所以 12n §一-2 那句「门槛由店主定」现在多了一个先验:**先看 23 张跑完的分布,
如果 90/25 这组起手值本来就分得开,那就不用动它** —— 不动比动干净。
(02 号黑甲仍判「深·不鲜明」,这不是错,这是你说的「黑要走腿 A」那个冲突,**得由你的口径来裁**,
不是靠调门槛去凑。)

## 二 已标 2 / 23(15 片指甲)

- `tools/segment/annotations/14.json`(8 片)· `02.json`(7 片)
- 自核图:`annotations/自核图/14_自核.jpg` · `02_自核.jpg` —— **每片都画回原图看过**
- 100% 裁片留底:`annotations/裁片/`

**标法(实操口径,记在这里)**:12j §一-2 的「100% 裁片逐片手描」落成:
在 100% 原分辨率裁片上逐片读出**甲面的中心 / 长短轴 / 倾角** → 展开成 24 点多边形 →
画回原图自核 → 逐片修。读「中心+轴」比逐个读 20 个顶点稳得多,**自核图是最终裁判**;
方/棺形甲读不准的支持直接给点列。**宁可略往内收** —— 判据是「多边形内全是甲面」,
不是「甲面全在多边形内」(这块掩膜是拿来采样算 Y/C 的,含到皮肤才是致命的)。

## 三 两次返工(如实记,这就是标注成本的真实构成)

1. **14 号片5/片6 倾角读反** —— 读成向右上,实际向右下,椭圆压到黑背景上。
   自核图上一眼看见,把那两片的裁框加宽后重读。
2. **02 号第一遍 7 片里 4 片裁空** —— 我在缩到 600px 的三连定位图上读百分比,读不准。
   **改规矩:定位图一律看 900px 全尺寸,一张一读**,不再三连。

## 四 顺手立的两件

- `tools/ai-retouch-eval/nail-tier.json` —— **甲色三档门槛的唯一真相**。
  肤色粗估那支(`region-metrics.mjs`)和真掩膜这支(`nail-tone-true.py`)都读它,
  不许各写各的。你定门槛就改这一个文件。
- `tools/segment/anno.py` —— 标注三件套:`locate`(10% 淡格定位)/ `crops`(100% 裁片 + 50px 尺)
  / `overlay`(画回原图自核,同时把裁片坐标换算成全图坐标存进 json)。

## 五 下一步

按 12n §一-1 继续标剩下 21 张(角例 18/23/28/42/43/44 + 15 张候选)。
23 张标完 → `nail-tone-true.py` 出分档表 → **分布给你看,你定门槛** → 三档各 ≥ 5 张才跑那 15 张(¥9.60)。
标注集清单(含 15 张候选的选法、54/76 的处置)在 `tools/segment/annotations/_标注集清单.md`。

---

# 第二节 —— 12n 补已落地;标到 4 / 23;**两个 Y 门槛已经露出问题**

## 一 12n 补三条全部落地

1. **第四档「纯黑」** —— `nail-tier.json` 改成四档,**判法顺序写死:浅 → 纯黑 → 深·鲜明 → 深·不鲜明**。
   纯黑必须排在深·鲜明**前面**接住,否则黑甲的低色度会被冲进「深·不鲜明」。
   映射表也在同一份文件里:浅 / 深·鲜明 / 纯黑 → 腿 A · 深·不鲜明 → 腿 A′。
   开跑判据同步改成「**四档各 ≥ 5 张**」。
2. **标注等级** —— 每条 `annotations/*.json` 带 `level: "sampling"`,**由 `anno.py overlay` 自动写**,不靠人记得加。
   `D222_专用指甲分割模型_立项` 已追加一段:数据量(文档里写的是 **800–3,000 张**,不是口述的「300 张起步」——
   两个数不许同时活着,请你定)**一张都不许从这 23 张里扣**。
3. **门槛唯一真相** —— `nail-tier.json`,两支打分器都读它。

## 二 标到 4 / 23(33 片指甲)

| 号 | 片数 | 甲面Y | 甲面C | 机器判 | 人眼判 | 对不对 |
|---|---|---|---|---|---|---|
| 14 红甲 | 8 | 61.2 | **35.3** | 深·鲜明 | 深·鲜明 | ✅ |
| 02 纯黑 | 7 | 53.8 | 4.4 | 深·不鲜明 | **纯黑** | 🔴 差一档 |
| 18 暗紫猫眼 | 6 | 62.0 | 5.3 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 23 奶白浅甲 | 6 | **85.5** | 6.4 | 深·不鲜明 | **浅** | 🔴 差一档 |

## 三 🔴 两个 Y 门槛都不对,**但我不动它们** —— 数摆在这里,你定

- **浅的门槛 Y ≥ 90 偏高**:23 号人眼一看就是奶白浅甲,真掩膜量出来 **85.5**,差 4.5 分掉进「深·不鲜明」,
  会被派去腿 A′。逐片看:68.9 / 76.4 / 76.8 / 86.1 / 87.1 / 125.7 —— 半数都在 90 以下。
- **纯黑的 Y 上限 35 偏低**:02 号是纯黑甲,整张 **53.8**,逐片最低的一枚也有 **37.7**,起手值 35 一枚都接不住。
  原因看得见:**黑色甲油是亮面的,甲面上那道白高光被算进均值**,把黑拉亮了。
- 🔴 **由此带出一个口径问题,得你拍**:量「黑不黑」该用**均值**还是**低分位**(比如甲面最暗的 20% 像素)?
  用均值,亮面黑甲永远量不黑;用低分位,数字更贴「这块甲油本身是什么颜色」。
  **这是改口径,不是调参数,所以我停在这里不动。**
- 门槛照 12n §一-2 的规矩:**23 张全标完再看分布定**。现在 4 张,不够定,也不许拿 4 张去拟合。
  C 门槛 25 目前**没出过错**(红甲 35.3 过、黑/紫/白 4–6 不过),12n §一「分得开就不动」大概率对它成立。

## 四 难标类型(12j §一-4 要的第一手数据)

| 类型 | 遇到几张 | 表现 |
|---|---|---|
| **半透明奶白甲 + 同色缎面背景** | 1(23 号) | **最难**。甲缘对比度接近 0,9 枚里 3 枚(片5/7/9)在 100% 裁片上也分不清,**强描等于编** → 标「待复核」不入集 |
| 暗片 + 黑甲 | 1(18 号) | 7 枚里 1 枚两次裁框都没框住 → 待复核 |
| 红甲 / 黑甲(对比清楚) | 2(14 / 02) | 顺,每枚一遍到两遍 |

**返工率**:4 张里 3 张返工过(14 号 2 片倾角读反 · 02 号 7 片里 4 片裁框读偏 · 18 号 6 片全部重读)。
**改进已落地**:`anno.py crops` 现在把已描的多边形**直接画在 100% 裁片上** ——
「看裁片」和「看拟合」合成一次看图,原来要先看裁片再看全图自核、来回两次还看不清甲缘。

**已标 33 片 / 未标 4 片(全部标「待复核」不入集,原因逐条记在各自 json 的 `未标` 字段)。**

---

# 第三节 —— 12n 补二落地:换口径,**三个门槛一个没动,02 号自己走回「纯黑」**

## 一 新口径(`metric: median_after_p85_trim`)

每片甲面掩膜内先去掉 Y > 该片 p85 的像素(高光),再取剩余像素的 **Y 中位数与 C 中位数**;
整张 = **各片中位数的中位数**。写进 `nail-tier.json` 的 `metric` 字段,
**两支打分器(肤色粗估 `region-metrics.mjs`、手描真掩膜 `nail-tone-true.py`)都读它**,
门槛也都改成读这一份 —— 粗估那支原先把 90/25 写死在代码里,一并收了。

## 二 新旧并排(旧均值一个没删)

| 号 | 片 | **新Y** | **新C** | 旧Y均值 | 旧C均值 | 机器判 | 人眼判 | |
|---|---|---|---|---|---|---|---|---|
| 02 纯黑 | 7 | **23.0** | 1.4 | 53.8 | 4.4 | **纯黑** | 纯黑 | ✅ **换口径自己就对了** |
| 14 红甲 | 8 | 40.5 | **42.0** | 61.2 | 35.3 | 深·鲜明 | 深·鲜明 | ✅ 更稳(35.3 → 42.0) |
| 18 暗紫猫眼 | 6 | 39.5 | 3.6 | 62.0 | 5.3 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 23 奶白浅甲 | 6 | **68.5** | 6.8 | 85.5 | 6.4 | 深·不鲜明 | **浅** | 🔴 仍差一档 |

**02 号**逐片新 Y:20 / 22 / 23 / 23 / 26 / 26 / 28(旧均值那版是 37.7…75.6)。
高光一去,黑就真的黑了 —— **`纯黑_Y上限 35` 这个起手值一动没动,它自己走回来了。**

🔴 **23 号的差距反而拉大了**(85.5 → 68.5,门槛 90)。这在意料之中:
新口径把所有 Y 整体压低约 20 分,**`Y门槛 90` 是按旧的均值口径拍的,没跟着搬家**。
按 12n补二 §一 的话 —— 「那是门槛的事,到时一起定」。我**不动**,等 23 张。

## 三 🔪 口径换了要挨一刀(突变自检条)

```
[刀已落] 2026-09-24T00:25:39  注入点:nail-tier.json;命中:metric 字段
         median_after_p85_trim → mean(冒充「口径被人悄悄改回旧的」)
真掩膜那支 → 🔴 AssertionError: 不认识的口径 mean —— 口径变了要先改代码,不许静默按老口径跑
粗估那支   → 🔴 不认识的口径 mean —— 同一句,进程退出
[刀已还原] 两支重跑,全部回绿
```
**防的正是静默失败器**:口径字段被改、代码却照老路算,数字照出、没人看得出来。
现在两支都会**当场停**,不会静默换算法。

## 四 粗估那支同步换口径后,**仍然是坏的** —— 这是好事,说明两支可对比

同样 4 张,肤色粗估路的判法:02 纯黑 · **14 纯黑(错,它是红甲)** · 18 纯黑 · 23 深·不鲜明。
14 号红甲被判成「纯黑」,比旧口径下判成「深·不鲜明」还离谱 ——
**因为它的掩膜本来就没落在指甲上**,换口径救不了掩膜。
两支同口径、同门槛,差的只剩掩膜一项,**谁在骗人一眼看得出来**。

## 五 D222 文档已改(12n补二 §二)

- **「300 张起步」作废**,注明是无来源的估数(店主第 64 笔);以立项文档的 **800–3,000 张**为准。
- 两件事分两行写清:**训练集 800–3,000 张** / **验收集 三档各 ≥ 20 张**,不是同一个数,不能互相抵。
- 12j 这 23 张是**采样级**,两个都不算进去。

## 六 标到 5 / 23(40 片)

新增 **28 号**(咖啡猫眼 3 枚 + 墨绿猫眼 4 枚,7 片)—— **一次过,零返工**。
工具改了之后(多边形直接画在 100% 裁片上)明显变快:28 号是第一张不用回头改的。

---

# 第四节 —— 标到 8 / 23(角例八张全标完);机器判与人眼判 **7/8 一致**

| 号 | 片 | 新Y | 新C | 机器判 | 人眼判 | |
|---|---|---|---|---|---|---|
| 02 纯黑 | 7 | 23.0 | 1.4 | 纯黑 | 纯黑 | ✅ |
| 14 红甲 | 8 | 40.5 | 42.0 | 深·鲜明 | 深·鲜明 | ✅ |
| 18 暗紫猫眼 | 6 | 39.5 | 3.6 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 23 奶白甲 | 6 | 68.5 | 6.8 | 深·不鲜明 | **浅** | 🔴 门槛的事 |
| 28 咖啡+墨绿猫眼 | 7 | 54.0 | 12.8 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 42 裸色长甲 | 3 | 157.0 | 37.2 | 浅 | 浅 | ✅ |
| 43 裸粉长甲 | 5 | 153.0 | 41.9 | 浅 | 浅 | ✅ |
| 44 白+立体钻 | 6 | 155.0 | 11.0 | 浅 | 浅 | ✅ |

**唯一的不一致还是 23 号**,还是 `Y门槛 90` 那一个原因,不是新错。

## 一 顺带验到一件:**判法顺序「浅排第一」是必须的**

42 / 43 裸甲的 C 是 **37.2 / 41.9** —— 跟 14 号正红甲的 **42.0** 几乎一样高。
裸甲贴着肤色,Cr 本来就高。**光靠色度分不开「大红」和「裸粉」**;
分得开的是亮度(157 / 153 vs 40.5)。
所以 12n补 §二 定的顺序 **浅 → 纯黑 → 深·鲜明 → 深·不鲜明** 里,
「浅」必须排第一 —— 排第三的话这两张会被 C ≥ 25 抢去判成「深·鲜明」。
**这条顺序不是随手排的,它在数据上有事实支撑,别动。**

## 二 44 号验到了新口径扛不扛得住钻的高光

44 号是立体钻最多的一张,钻的镜面反光最亮。旧均值 Y 167.9 → 新口径 **155.0**,
C 12.1 → 11.0。**去掉最亮的 15% 之后数稳住了**,判档没变(仍是「浅」)。
新口径在「高光最狠」的这张上没有翻车。

## 三 难标类型更新(12j §一-4)

| 类型 | 张 | 标出率 | 说明 |
|---|---|---|---|
| **裸色长甲互相交叠**(42) | 1 | **3 / 9** | **最难**。甲与手指色度接近,第一轮描的 7 片里右手 4 片全压在手指上;不硬修,退回「待复核」 |
| 半透明奶白甲 + 同色缎面(23) | 1 | 6 / 9 | 甲缘对比度≈0 |
| 白/透明甲 + 立体钻交叠(44) | 1 | 6 / 9 | 甲多且叠,只标看得清是哪一枚的 |
| 裸粉长甲 单手不交叠(43) | 1 | 5 / 5 | 能标全,但**定位不确定度明显比彩甲大**,返工一轮 |
| 彩甲 / 黑甲 / 猫眼(02·14·18·28) | 4 | 28 / 29 | 顺;28 号**一次过零返工** |

**已标 48 片 · 待复核 13 片**(每一片的原因逐条记在各自 json 的 `未标` 字段)。
**「强描等于编」这条今天用掉了两次** —— 42 号右手 4 片、23 号 3 片,都是宁可不标。

## 四 下一步
剩 15 张(全是 12i 候选):49 · 51 · 52 · 53 · 57 · 60 · 61 · 65 · 69 · 70 · 72 · 78 · 85 · 87 · 88。
标完出四档分档表 + 新口径下的 Y 分布,门槛交店主定。

---

# 第五节 —— 标到 9 / 23;**新装一条护栏,当场咬出一个我人眼没看出来的错**

## 一 🔴 先更正一个已经报给你的数

**43 号的片数从 5 改成 4,Y 从 153.0 改成 156.5。**
原因:**片3 和片5 是同一枚甲**,被两个裁框各标了一次 —— 整张的 Y/C 把那一枚算了两遍。
**我上一轮的人眼自核看过这张,没看出来。**

## 二 错是怎么冒出来的:49 号

49 号第一轮我开了 9 个裁框,彼此重叠。自核图上一看 —— **几个椭圆叠在同一两枚红甲上**。
红甲单枚对比清楚,但**相邻枚之间**分界不清;裁框一重叠,同一枚就被标两次。
处置照 42 号那条:**推倒重来,只框「四周没有相邻甲、一眼看得出是哪一枚」的**,
9 枚里留 4 枚,其余记「密集交叠不标」。
**这是一类新的难标类型:同色密排。** 它跟「甲缘看不清」不一样 ——
甲缘很清楚,不清楚的是**这一块属于哪一枚**。

## 三 护栏(永久,机器查)

`anno.py overlay` 现在算**任意两片的重叠面积 ÷ 较小那片的面积**,超过 15% 直接红、报出是哪两片、
**拒绝存盘**。理由写在代码里:整张的 Y/C 会把同一枚甲算两遍,而**这条错人眼很难数出来**。

**回溯扫已标的 9 张**:
```
02 ✅  14 ✅  18 ✅  23 ✅  28 ✅  42 ✅  44 ✅  49 ✅
43 🔴 片3 ↔ 片5:34.0%  → 已退回「待复核」
```
**一张红,而且正是人眼放过去的那张。** 判据要能查出人眼查不出的东西,这条做到了。

## 四 标到 9 / 23,机器判与人眼判 8 / 9 一致

| 号 | 片 | 新Y | 新C | 机器判 | 人眼判 | |
|---|---|---|---|---|---|---|
| 02 | 7 | 23.0 | 1.4 | 纯黑 | 纯黑 | ✅ |
| 14 | 8 | 40.5 | 42.0 | 深·鲜明 | 深·鲜明 | ✅ |
| 18 | 6 | 39.5 | 3.6 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 23 | 6 | 68.5 | 6.8 | 深·不鲜明 | **浅** | 🔴 门槛的事 |
| 28 | 7 | 54.0 | 12.8 | 深·不鲜明 | 深·不鲜明 | ✅ |
| 42 | 3 | 157.0 | 37.2 | 浅 | 浅 | ✅ |
| 43 | **4** | **156.5** | 41.9 | 浅 | 浅 | ✅(已更正) |
| 44 | 6 | 155.0 | 11.0 | 浅 | 浅 | ✅ |
| **49** | 4 | 69.5 | **69.8** | 深·鲜明 | 深·鲜明 | ✅ 全库色度最高,名副其实 |

分布:浅 3 · 深·鲜明 2 · 深·不鲜明 3 · 纯黑 1。**四档现在各自都有人了。**

## 五 难标类型表更新

| 类型 | 标出率 | 难在哪 |
|---|---|---|
| 裸色长甲互相交叠(42) | 3/9 | 甲与手指色度接近 |
| **同色红甲密集排列(49)** | **4/9** | **甲缘清楚,但「这块属于哪一枚」不清楚** —— 新的一类 |
| 半透明奶白甲 + 同色缎面(23) | 6/9 | 甲缘对比度≈0 |
| 白/透明甲 + 立体钻交叠(44) | 6/9 | 甲多且叠 |
| 裸粉长甲(43) | 4/5 | 定位不确定度大,且撞上重复计 |
| 彩甲 / 黑甲 / 猫眼(02·14·18·28) | 28/29 | 顺 |

**已标 51 片 · 待复核 19 片。**

---

# 第六节 —— 标到 12 / 23;**我埋的一条交叉验红了:同一套美甲落到了两档**

## 一 52 与 53 是同一套美甲的远景和近景 —— 机器判成两档

标 52 号时我在它的 json 里写了一句:「53 号是它的近景,两张的机器判应当落同一档,这本身就是一条交叉验」。
标完一跑:

| 号 | 是什么 | 新Y | 新C | 机器判 | 人眼判 |
|---|---|---|---|---|---|
| **52** | 宝蓝+玫红+银 彩绘(**远景**) | **88.5** | 48.7 | **深·鲜明** | 深·鲜明 |
| **53** | 同一套(**近景**) | **103.0** | 59.2 | **浅** 🔴 | 深·鲜明 |

**同样的指甲,换个机位就换了一档。** 原因看得很清楚:近景更亮,Y 从 88.5 抬到 103,
**一脚跨过 `Y门槛 90`**;而「浅」排第一,一命中就不再看色度 —— 59.2 这么高的色度根本没机会说话。

## 二 这说明一件比「门槛偏高」更麻烦的事:**一个 Y 门槛同时满足不了两个约束**

把 12 张按人眼分组看(新口径):

| 人眼判 | 样本(Y / C) |
|---|---|
| **浅** | 42 裸 **157 / 37.2** · 43 裸粉 **156.5 / 41.9** · 44 白钻 155 / 11.0 · 51 银 110 / 5.8 · 23 奶白 68.5 / 6.8 |
| **深·鲜明** | 53 荧光 **103 / 59.2** · 52 同套 88.5 / 48.7 · 49 大红 69.5 / 69.8 · 14 红 40.5 / 42.0 |
| 深·不鲜明 | 28 猫眼 54 / 12.8 · 18 暗紫 39.5 / 3.6 |
| 纯黑 | 02 黑 23 / 1.4 |

- 要让**裸甲(C 37–42)**不被当成鲜明色,「浅」就得排在前面 —— 这是上一节验出来的,没错。
- 但「浅」排前面,**荧光蓝粉(C 59.2)只因为亮就被判成浅** —— 这一节验出来的,也是错。
- 两件事一起看:**裸甲的色度天然就高**(它贴着肤色),所以色度分不开「裸」和「艳」;
  而亮度上,裸甲 155+、荧光 103 —— **中间 103–155 这一段目前一个样本都没有**。
  门槛要挪到哪,得等这一段有人。

🔴 **所以三个门槛我还是一个都不动,判法顺序也不动。** 这一节只把「单一 Y 门槛够不够用」
这个问题摆出来 —— 它可能不是挪门槛能解决的,而剩下 11 张里很可能就有中间段的样本。
23 张标完一起看。

## 三 标到 12 / 23

新增 **49(红,4片)· 51(银色金属,5片)· 52(彩绘远景,6片)· 53(彩绘近景,5片)**。
- **51 号验到一件**:镜面银甲 Y 110、C 5.8,「去高光中位数」**没有把整片都削掉**(旧均值 115.3,只差 5 分),
  说明这个口径在全画面最亮的甲上也站得住。
- 52 / 53 两张都返工了一轮:**裁框的 y 我读偏了约 5 个百分点**,原因是我把两张定位图并排成 1800px 一起读 ——
  **和 02 号那次同一个错**。定位图一张一读这条规矩我自己破了一次,已经改回来。
  (归类:**复发**。护栏:定位图不再并排出,`anno.py locate` 一次只出一张。)

**已标 71 片 · 待复核 23 片。**

---

# 第七节 —— 52/53 立成永久考题;候选口径登记但不启用;标到 13 / 23

## 一 「同一套甲必须判同一档」已经变成常驻判据

- `tools/ai-retouch-eval/同套甲对照表.json` —— 登记「哪几张是同一套甲」。
  **只登记「是同一套」,不登记「应该是哪一档」**(档由人眼判写在各自标注 json 里)。第一对就是 52/53。
- `nail-tone-true.py` 每次出表都跑这条,**不随门槛变动而失效**:

```
同套甲对照(远景 vs 近景必须同档):
  🔴 宝蓝+玫红+银 彩绘:52_IMG_1483.jpg → 深·鲜明 / 53_IMG_1486.jpg → 浅
     同一套甲判成两档 —— 判法有毛病,不是门槛的事。
🔴 同套甲判据未过:1 对。(不中断出表,但这条红着就不算定案)
```

**🔪 新判据要挨一刀(突变自检条)** —— 这条得证明它「能变绿」,否则就是一条永远红的废判据:
```
[刀已落] 00:57:02  nail-tier.json Y门槛 90→130(只为验判据能不能变绿,不是定门槛)
        → ✅ 宝蓝+玫红+银 彩绘:两张都判 深·鲜明
[刀已还原] Y门槛 = 90 → 🔴 又红回来
```
能红能绿,不是摆设。

## 二 候选口径已登记,**未启用**

`nail-tier.json` 里新增 `候选口径_未启用`:
> **「离肤色有多远」(色相距离),而不是「色度高不高」**(店主 2026-09-24 提)。
> 裸色/裸粉的色相跟皮肤几乎一样,所以它们色度天然就高;荧光蓝、正红离皮肤很远。
> 这样可能不必靠亮度去分「裸」和「艳」,亮度中间段(Y 103–155)没样本这个空当也就不那么要命。

**状态写死:🔴 候选,未启用,不许现在拿 12 张去凑。** 等 23 张标完连同现判法一起比,用数据定。
验的时候要过四条:①42/43 裸甲仍判浅 ②53 荧光判深·鲜明 ③52/53 同档 ④02 黑仍判纯黑。

## 三 防复发护栏落到工具里

`anno.py locate` 每次出图都打这两行:
```
⚠️ 这张定位图必须**单独、满尺寸**看。不许缩小、不许和别的定位图并排成一张再读百分比
   —— 02 号(三连缩到 600px)和 52/53 号(并排 1800px)各栽过一次,裁框整体偏 5 个百分点。
```

## 四 标到 13 / 23

新增 **57 号**(珠光白/裸杏长甲 + 银线格纹,7 片)。
返工两轮:第一轮 7 片整体偏向甲根、边缘压到手指;第二轮发现**中心其实都在甲面上(红点为证),
外溢的是宽度** —— 短轴收 38% 后全部落进甲面。
**珠光/裸杏甲的横向边界是目前最难定的**:甲缘与手指几乎同色,长度好判、宽度难判。

**已标 78 片 · 待复核 23 片。**

---

# 第八节 —— 标到 16 / 23;**捡到第二对同套甲,而且它过了**

## 一 60 与 61 是同一套美甲 —— 判据不是「永远红」

标 61 号时认出来:它和 60 号是**同一套**。依据不是猜的 ——
同样的粉/薄荷渐变、同样的银闪甲根、同样的星形饰件、同一件灰卫衣同一条牛仔裤背景,
**文件号还连号**(IMG_2107 / IMG_2108)。已登记进《同套甲对照表》当第二对。

```
同套甲对照(远景 vs 近景必须同档):
  🔴 宝蓝+玫红+银 彩绘:52 → 深·鲜明 / 53 → 浅
  ✅ 粉/薄荷渐变 + 星形饰:两张都判 浅
```

**这很重要**:上一节那把刀证明了这条判据「能变绿」,这一节是**真数据**让它绿了一对。
它分得出好坏,不是一条摆着红的废判据。现在 2 对里 1 对过 —— 红的那一对仍然红着。

## 二 找到我自己的一个系统性读数偏差:**甲面宽度一贯读大**

57 号(珠光)、60 号(短圆甲)、65 号(细长棺形)**三张都是同一个毛病**:
长度读得准、**宽度读大**,椭圆两侧压到手指上。
- 57 号:短轴收 38% 才落进甲面
- 65 号:短轴收 48% 才落进甲面

**归类**:这不是某一张的手误,是我读「甲面有多宽」时的稳定偏差 ——
甲缘与手指之间那道边界,在照片上几乎没有明暗差,我会不自觉地把指腹的宽度算进去。
**对策(已在做)**:描完先按「长度可信、宽度存疑」的假设收一次短轴,再看自核图。

## 三 65 号:一张里黑甲与裸甲各半

3 枚哑光纯黑 + 3 枚裸/灰褐。**整张取中位数会被抹平**,这张的价值在**逐片**数上 ——
黑甲那几枚的 Y 正是「纯黑_Y上限」要用的料(与 18 号同一类)。

## 四 进度

标到 **16 / 23**,已标 101 片 · 待复核 24 片。
剩 7 张:69 · 70 · 72 · 78 · 85 · 87 · 88。

---

# 第九节 —— 标到 19 / 23;**`Y门槛 90` 正卡在一堆样本中间**

## 一 候选规则已登记(未启用):逐片判档

65 号带出的那条,已写进 `nail-tier.json` 的 `候选规则_未启用`:
> 逐片判档;整张只有「全部(或过半)是深·不鲜明」才走腿 A′,否则走腿 A。

**验收条件先写死,到时不许挑着看**:①65 号 → A(现在错成 A′)②18 号仍 A′ ③28 号仍 A′
④02 号 → 纯黑 → A ⑤42/43 裸甲与 14/49 红甲仍走 A ⑥同套甲每一对仍同档。
与「离肤色有多远」那条互不冲突,可同时验 —— **一条改「怎么量单片」,一条改「怎么把多片汇成一张」。**

## 二 🔴 新数据:`Y门槛 90` 附近已经挤了三张

| 号 | Y | C | 机器判 | 人眼判 |
|---|---|---|---|---|
| 52 彩绘远景 | **88.5** | 48.7 | 深·鲜明 | 深·鲜明 ✅ |
| **69 砖红甲** | **89.0** | 26.9 | 深·鲜明 | 深·鲜明 ✅ |
| 53 彩绘近景 | **103.0** | 59.2 | **浅** | 深·鲜明 🔴 |

**69 号只比门槛低 1 分。** 再亮一点点就会跟 53 号一样被判成「浅」。
门槛正好落在一堆样本的**中间**,这是门槛最不该待的位置 ——
52 / 69 / 53 三张人眼都判「深·鲜明」,却被 90 这条线劈成两半。
这一条比「23 号差 21.5 分」更说明问题:**不是门槛偏高几分的事,是它站错了地方。**
(仍然不动,等 23 张齐。)

## 三 进度与难标类型

标到 **19 / 23**,已标 126 片 · 待复核 30 片。剩 4 张:78 · 85 · 87 · 88。

难标类型又添一种(第四类):
| 类型 | 代表 | 难在哪 |
|---|---|---|
| 同色密排 | 49 | 甲缘清楚,「这块属于哪一枚」不清楚 |
| 甲缘对比度≈0 | 23 · 42 | 半透明/裸色压在同色背景或皮肤上 |
| 宽度难定 | 57 · 60 · 65 | 长度好判,两侧甲缘与手指同色 |
| **透明甲 + 大立体饰件** | **72**(比 44 更极端) | **甲缘在 100% 裁片上几乎看不出来,只能靠饰件定位** —— 5 片里 3 片第一遍全落在皮肤上,靠自核图反推坐标才救回来 |

**纠正一处**:70 号我原先按「极暗且糊」挑进候选,看了定位图才发现是
**暗光下的裸色渐变长甲** —— 归「裸甲」类更准,已在它的标注 json 里记下这次纠正。
