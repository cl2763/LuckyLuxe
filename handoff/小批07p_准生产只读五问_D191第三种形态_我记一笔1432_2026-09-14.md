# 小批 07p · 准生产只读五问 · D191 的第三种形态 · 我记一笔

**下达时刻**:2026-09-14
**Code 第一件事**:落进桌面夹 + 仓里 `handoff/`,报两处**全 32 位** md5。

---

## 〇 这一批最值钱的不是那三个 0,是你给它加的那个限定

> 「但有个要紧的限定:**我量的是本机库,而决定上线风险的是生产库** —— 按停线我没碰。」

**我问的问题,你答不了 —— 因为我问的是生产库的事,而你按停线不碰生产。**
**而你没有拿本机库的 0 冒充生产库的 0。**

这正是这套体系全部的意义。「三家都是 0」是一句听着让人放心的话,**而你把它后面那个「但是」写出来了 —— 那个「但是」比那三个 0 重要一百倍。**

### 另外记你五功

1. 🔴 **`danger-cmd` 那件,你的第一个修法比原来的病更坏 —— 而且是你自己发现的。**
   原病:把 `echo "…不用 pkill…"` 当成执行了(J-61①,数执行不数提及)。
   你第一版「整段剥字符串」—— **`pkill -f "local-server.mjs"` 的模式串就住在引号里,剥完把三条真护栏一起判红。**
   **「修法让判据变瞎」是最危险的一类修法**,因为它换来的是一片绿。你没让它过夜。
2. **白名单上限撞了两次,两次都没抬。** `danger-cmd` 上限 3 → 改成一处 `pkill` 都不用;旧路冻结上限 5 → 换个不碰棘轮的证法。
3. 🔴 **「为了证一条判据去顶另一条棘轮,不划算。」** 这句是好判断。你试的前两种都要顶破别的账,**你没硬来,换了第三种。**
4. **`cli quit` 那一跤又是 J-37③**:按端口收只杀掉监听子进程,**整只工具还活着** ——「端口没了 ≠ 进程没了」。你认出来了。
5. **§四 回放没做成,你说「不算验过」。** 代码装上了、机制在那儿、三次靶子都把套件弄红了 —— **你完全可以写「④ 已装上」然后过去。你没有。**

**数我核过的**:4,004 + 9 + 1 = **4,014** ✅ · 309 + 2 = **311** ✅ · 主档 118 → **119** ✅ · 40 = 32 + 8(上一批)✅。

---

## 一 🔴 准:**生产库只读查询** —— 两步走

边界令① 写的是「**生产诊断一律只读**,append 也是写,凡写生产先报 Cowork 批」。
**只读诊断本来就是准的。** 但按两步走:

### 第一步:**先把查询原文贴出来,我批了再跑**

### 第二步:跑的时候要报

- 连的是**哪个库**(路径/连接串脱敏后的标识);
- 跑了**几条语句**,每条**原文**;
- **一条写语句都没有**(含 `CREATE TEMP` / `ATTACH` / 改任何 `PRAGMA` / append 任何日志)。

**硬禁**:不许顺手修任何东西,**一行都不许**。看到脏数据也只许记下来报上来。

### 要问的**五个数**(不是三个)

| # | 问 | 为什么 |
|---|---|---|
| 1 | 每家店:**手机号对应多于一条档案**的手机号个数 + 涉及顾客数 | 段 C 的位置由它定 |
| 2 | 每家店:**没有手机号的顾客数 / 总顾客数**,并分开 `is_migrated=1` 与非导入 | 见 §三 —— 这可能比 #1 大得多 |
| 3 | `lucky-luxe` 的**预约总数** | 见 §二 —— 结我那笔账 |
| 4 | **有储值余额**的顾客里,#1 和 #2 各占多少 | **那才是会吵起来的那批人** |
| 5 | **有卡包 / 有积分**的顾客里,#1 和 #2 各占多少 | 同上 |

**#4 #5 是我加的,而且是这五问里最要紧的两问。**
理由:一个没有余额的老顾客变成新人,她大概不会发现;**一个有 ¥3,000 余额的老顾客变成新人,她当场就在店里。**
**我们要先知道那批人有多少个。**

---

## 二 🔴 记我一笔(第 11 笔):**1,432 还是 1,242?**

你报的:**「CLAUDE.md 记的生产库是 users 77 / bookings 1,242」。**

**而我一直在说「`lucky-luxe` 1,432 条真实预约历史」** —— 写进台账、写进总账、写进给店主的话、写进 D191 的立案理由。

**两个数对不上,而且一直在传播那个数的是我。**

- 我没有量过它。**它是我从早期某处抄来的,而且按 J-63 第一问,我从来没问过它「是量出来的还是抄来的」。**
- **我用它做过论证**(「1,432 条历史 → 每个老顾客都会变新人」)—— 结论方向不变,**但支撑它的数是没校准的。**

**处置**:

1. **从现在起,我不再说「1,432」这个数**,改说「**约 1.2–1.4 千条,待生产现测**」。
2. **§一 第 3 问就是为它设的** —— 现测出来之后,我回去把台账和总账里所有出现过这个数的地方**原地更正**(J-55)。
3. **这一笔记我头上,不派给 Code。**

**顺带一句给你**:CLAUDE.md 里那两个数,**它们自己也要过 J-63 三问** —— 是谁在什么时候量的?一并现测。

---

## 三 🔴 D191 的第三种形态:**没有手机号的档案**

你顺手量到的这件,我要提级:

> 「严格四条的第二条是『号完全一致』,**没手机号的档案连这一条都过不去** —— 症状和 D191 一模一样。」

**D191 那一族现在有三种形态**:

| | 卡在哪一条 | 本机库 | 生产库 |
|---|---|---|---|
| ① 有身份行 | 第三条 | **已修** ✅ | 已修 ✅ |
| ② 同号多条 | 第四条「唯一一条」 | 0 | **未知(§一 #1)** |
| ③ **没有手机号** | 第二条「号完全一致」 | 52/74,**但全是测试残留** | **未知(§一 #2)** |

**③ 很可能比 ② 范围大得多,而且我有理由这么猜**:

美甲店的老顾客名单,**有些是微信加的、有些是店里手写在本子上的** —— 导入的时候未必每个人都有手机号。
**每一个没手机号的老顾客,上线后扫码都会变新人。**

**本机库那 52 个是测试残留(`Identity Test` / `自动化验证-勿动`,`is_migrated=1` 的 0 个)—— 这一点你查清了,好。**
**但它只证明了本机库干净,一个字都没证明生产库。**

**③ 的修法要等数出来再定**,因为它不像 ① 那样有一条干净的改法:
没有手机号的人,**本来就没法用手机号认** —— 那是数据缺口,不是代码缺陷。
可能的出路(**先不要做,等数**):上线前补采、首次扫码时引导她填、或者给店主一个「把这个微信号认到这份老档案上」的后台动作。
**这三条哪一条都要图,而图要等数。**

---

## 四 §四 回放的出路:**造一个玩具套件**

你三次选的靶子都把套件弄红了,所以 ④ 没机会说话 —— 然后你按停线停了,**停得对。**

但这件事有一条现成的出路,说破了很简单:

> **不要在真套件上回放。造一个专门的玩具套件:两条断言,一条必然绿,一条挂在造病点上。**

- 造病打下去 → 玩具套件**保持绿**(因为那条必然绿的还在)→ **④ 一定会被触发**,要求先证咬到;
- 然后把造病点改成「直接抛」→ 那条断言红 → 证明路径真被执行到;
- 再回到真造病 → 仍不红 → 才判「守住了」。

**玩具套件本身要具名冻结**(它是判据的夹具,按 J-61② 默认排除自身)。

---

## 五 积分「0 条空态」不算验过

> 「卡包 1 张(库 `active` 1 行 → 顾客端 1 张)· **积分 0 条(空态)**。」

**卡包那条收下 —— 库里 1 行、顾客端 1 张,对得上。**
**积分那条不收**:0 条只证明了「读得到 0」,**没证明「积分跟过去了」。**

**要做**:给那个顾客**造一条真积分**(走正门:导入带初始积分,或消费产生),再验一次 —— **按行 id 比,和你验余额时一样。**

**这是 J-62 第二款**:空态验不出「搬过去了」,只验得出「没崩」。

---

## 六 追问一个

**门关档的链条你更正了一次**(07n 实际 +1 不是 +2,本批 22 套)。**更正收下。**
但把链条补完整报一次:**19 →(07n +1)→ 20 →(07o +2)→ 22**,那 +2 分别是哪两套?
—— 不是挑刺,是这个数从今天起要机械对账,链条一次说清就再也不用回头了。

---

## 七 顺序

```
① §一  贴出五问的查询原文 → 等我批 → 跑 → 报数(本批第一件,而且做完停一下)
② §五  积分造一条真数据再验
③ §四  玩具套件 → ④ 回放做成
④      造病台搬离 4128/4310(连欠两批,不许再欠)
⑤      造病台开跑前检查工作区干净
⑥      237 条按口聚类 → 出口数 → 涉钱 13 / 顾客可见 20 先造病
⑦      门关档红 8 · J-60 继续降 · 9 条冻结理由
⑧      段 C —— **位置等 §一 #1 的数出来当天定**
```

**④ 我要单说**:造病台碰 4128/4310 这件已经连欠两批了。**店主那两台是她自己在用的,造病期间它们是坏的而她不知道。这一条不许再往后挪。**

---

## 八 停线

照旧。**生产只读那一条再写死一遍**:

- **只许 `SELECT`。** 一条写语句都不许,**append 也是写**。
- **查询原文先贴,我批了才许跑。**
- **看到脏数据只许记下来报上来,一行都不许修。**
- **跑完报连的是哪个库、跑了几条、每条原文。**
- 任何一条不确定是不是「只读」→ **停下来问,不要赌。**

---

## 九 回执

1. 第一行:两处全 32 位 md5。
2. §一 第一步:**五问的查询原文**(贴完停下,等我批)。
3. 批了之后:五个数 + 连的哪个库 + 跑了几条 + 每条原文 + 「零写语句」的证据。
4. §五:积分那条的实测值(按行 id 比)。
5. §四:玩具套件 + ④ 真触发的那一行输出。
6. §六:门关档链条补完整。
7. 三档通过数/总数 + **断言数(收尾报一次)** + 跨批账本自检结果。
8. 底数闭合 · 未动对照结构化标记 · `batch-footer` 现出的数。
9. **每个手写的数带一句「怎么来的」**。
10. 没做完的写「未做,原因 X」。

