# 小批05g — 05f 验收(`3541540`/`7c8c4d3`:收,**门已换**)· 矩阵预期同步 v1.2 · 放行 ② 事实闸

> 2026-09-04 · Cowork 亲核于店主 Mac · 两处各一份

## 〇、亲核
- 默认门 `model`(ai-gate.mjs:155,`AI_GATE=keyword` 回滚);三轮结果文件六份在仓(200 句 + 安全四线 × 3);走查文档 20,310 字节,汇总 18/23/1 对 4/12/21;旧口径断言残留 0(除 test-ai-gate 验旧门);`receipt-db-proof.sh` 在;94 套 2,970;`local-server.mjs` 棘轮持平;4128/4310 200,沙箱真模型自动还回;未推 main 105。
- 三轮 90.0 / 91.2 / 93.8,中位 91.2;实质作答三轮 0;安全三轮 0000;合计 293.9 万 token。**三轮各自都过线**,不是靠中位拉过——换门成立,收。
- J-17(裸「余额」+ 自加动作句)修在三跑后 → 数字偏保守,收;「未动」贴原文改成跑脚本,收。

## 一、裁
1. **矩阵预期同步 v1.2**:04d §三 A4 末轮、B4 末轮预期由「静默」改「3a 礼貌拒绝不静默」;C3 人工接管中静默为唯一合法静默。我改矩阵文档,Code 不动。
2. **换门后的口径**:默认模型门为产品口径;上线批加一行「生产环境 `AI_GATE` 不设或 =model;`AI_GATE=keyword` 只作回滚开关且回滚要留痕」。
3. 走查里两处 UX 老病记入 ③ 预约采集的靶子:A2/A3「改天再来 / 上次那款还能做吗」仍被丢一张 7 项表(`quote_intake_template`)——③ 的「一句一问、只问缺的、引用历史」正是为它。

## 二、放行 → ② 事实闸(图 §三)
六个事实槽(营业时间/休息日 · 地址电话停车 · 定金三项 · 价目 · 可约时段 · 顾客历史)只从唯一来源注入;**出口校验**:回复里出现的数字与「可否抵扣」必须能在事实槽里找到,找不到 → 换成「这个我帮您问一下」并 3b。判据 4(造病:夹具注入槽外数字 → 拦下)+ D136 并排表为锚(金额/可否抵扣/三档比例三项逐项一致)+ 租户隔离(A 店地址不得出现在 B 店回复)+ 价目:需报价项目只说「需技师确认」不出数字。一步一提交一回执;评测集里事实类句子(营业时间/定金/地址组)三跑取中位报「事实命中率」。

## 三、交齐清单
② 的判据与造病;两库对照(先跑 `receipt-db-proof.sh` 贴原文);回归 + 断言增量;棘轮;未推 main 用 `origin/main..HEAD`。
