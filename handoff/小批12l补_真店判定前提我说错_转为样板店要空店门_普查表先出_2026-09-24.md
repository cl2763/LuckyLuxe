# 小批 12l 补 —— 我 12l 的前提说错了(记第 62 笔);「转为样板店」加空店门;普查表先出再改;顺序对

## §零 记我第 62 笔
12l 写「真店判定改读样板店标记,不再靠写死常量」—— 判定**早就是**读 `tenants.kind` 的(`isDemoTenant()`),常量只是「不许被改成演示店」的黑名单。我没读 `isDemoTenant` 就下了断语,与 #44 同类。Code 查清再报,记功。

## §一 你的三步,收,第 1 步改一处
**第 1 步不能写成「PROTECTED 从 kind=real 推导」**:清单第 10 项「更多 ▾」里有「转为样板店」这个正门操作 —— 全部 real 都锁死,那个按钮就永远失败。改成:
- `PROTECTED_REAL_TENANTS` 加 `luvia-bj`,保持硬锁(三家真店永远不许转)。
- 「转为样板店」正门加**空店门**:该店 订单 = 0 且 顾客 = 0 且 签署件 = 0 且 老板账号未首登改密 → 才允许;任一非零 → 拒绝并回一句「这家店有真实数据,不能转为样板店」。判据:对 luvia-bj 调 → 硬锁拒;对一家有 1 张单的非保护店调 → 空店门拒;对四家 demo-* 无影响。
- 「重置演示数据」仍只对 `kind='demo'` 放行(现有门不动)。

**第 2 步(普查表先出,我看完再改)**:收,而且 `lucky-luxe.plan_expires_at` 已是 NULL 说明现在就可能有出口把它读成过期或崩 —— 普查表每行:文件:行号 / 读的是哪个字段 / NULL 时现在的行为(过期·永久·崩·未处理)/ 拟改成什么。**表出来之前不改任何一处**。J-112:出口数以 grep `plan_expires_at|expires_at|到期` 三个词的并集为底,回执贴 grep 命令与命中数,漏词漏出口都算漏。

**第 3 步(第二电话 ALTER 单独预批)**:收。预批件格式按 J-113:写死的 SQL、执行前后各一条 COUNT、备份 `VACUUM INTO` 路径、回滚语句。

## §二 顺序
界面 25 项与普查并行 —— 对。普查表到我这里、我回一句「改」之后再动那一处;我不在场时按 J-113:普查表里「NULL ⇒ 永久」这一类改法预批为「全部出口统一改成:NULL 视为永久,且列表格显示『长期』标签」,其余类型(崩/未处理)不预批、进等天亮清单。
