# 大批03s · 小程序批盖章 · 梯子口径 · 下一批(2026-09-03)

## 〇、五处收尾亲核,盖章「小程序批交齐」

提交 `938d8e8`;HEAD 基线 79 套 / 2,804;03q、03r 两份回归日志已 tracked;`.playwright-browsers/` 已 ignore;`run-all-tests.sh` 两道闸在(`wait_port_free` + `assert_db_domain` 用 `lsof` 看进程真开的 `.sqlite`、两边 `pwd -P`);护栏刀尾行与 ①c 同尺并把 NOT_A_DB 差额说完整;03r 日志 79/79、「本轮未跑」0;本机库对 03q 清污前备份逐表 87 张只差那五行(即清理本身),此后零写入;03q 回执那句已划掉改写——**「我没找到」和「它没有」是两句话,你把这条写进了自己的案底,好。**

两处队尾小病顺手收得干净:等级中文名进 `conversation-card.mjs` 唯一出口、网页那份 `qsLabel` 分叉这次真收了;相对时间收成一个出口、边界含恰好 48 小时。

**小程序批到此盖章。** 23 步走查单已在店主手上,她走到哪一步不对会停下截图。

## 一、你停下来问的口径:分级店自定义梯子显示 `key` 还是 `label`

**显示 `label`,永远不显示 `key`。** 键是程序用的,名字是人看的;`gold` 露出来就是这条的反面教材。三条一起落:
1. 自定义梯子**新建/编辑时 `label` 必填**(与金额更正「原因必填」同口径),后端拒空。
2. 存量梯子 `label` 为空的,**条件迁移**:`label` 空则填 `key`(一次性,不覆盖已有 label),并在门店设置页对这些行提示「等级名沿用了键名,建议改成中文名」。
3. 消费方一律读 label;label 取不到(不该发生)走 `conversation-card.mjs` 那句「认不出的等级」兜底,不显示原值。

顺带一处同族:`ai-desk.js:440` `card.quoteLabel || '本会话暂无报价'` 是那句 none 态文案的**第二份**——后端 none 态本来就下发 `quoteLabel`,网页不该再存一份;取不到时不呈现(不可用即不呈现),不自己补句。

## 二、下一批(店主走查期间并行)

1. **走查回执优先**:店主走到哪步停下,那一步的回执先于一切。
2. **D121 · 演示数据标记**(挂账转实做):`bookings`/`settlements`(及造景会写的其余表)加来源标记列,造景写入必带;判据:凡造景写入的行一条查询整批认出;迁移条件更新;`seed-*` 全族接标记,先出清单再动。
3. **「包了事务必须验回滚」9 处**(A 类 1 + B 类 8):逐个造中间步失败→零行落地→再跑真路径;做完三列清单第三列真打勾。
4. §一 梯子三条 + `quoteLabel` 分叉。
5. 以上交齐后,**上线硬门槛批**开单(平台对账走查 · 作品上墙两道闸 · 提审三件 · 真店两账号密码店主重置 · `STAFF_DEMO_PASSWORD` 生产惰性 · 生产核验小婕店两行 · 凭据刀三处收紧 · 小程序版本指纹 · 三端基线对照表),我另发清单。

四库四名照「未动须有证」写,交齐附对照表。

## 三、追记(店主拍板,2026-09-03)

店主定了下一批顺序:**开门必需优先——S1 价目表≠服务项目 → S5 通用设置 → 多项目预约多单串联结算 → S2 卡包+商城**;业绩大屏放后;上线硬门槛批推到功能批之后再开。

**分工**:S1 的图我来出,**出图前你不动价目表/服务项目/结算单目录这几处界面**(诉求总账第五栏的规矩)。你这段先收 §二的 2–4(D121 实做 / 事务验回滚 9 处 / 梯子三条 + quoteLabel 分叉)和店主走查回执;S1 图到手再开工。
