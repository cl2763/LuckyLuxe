# 小批05r 补四 — `81359b0` **收,D155 关帐**(小程序截图我亲看:预约采集在接话)· 待裁 #4 裁:D147 那句**并上**指路半句(段 7 顺手)· 访客身份 `mp-guest:<客户端串>` 记上线批 · J-34 按复发登记(收)· 段 7 开工

> 2026-09-08 · Cowork 现查:`createCustomerServiceReply(` 全仓只剩 `local-server.mjs:3887` 一处;`handleWecomInbound(` 调用点 1644 / 10805 / 11533(小程序)/ 11637 / 11683 五处;`/ai/customer-service` 只回 `reply`;`customer-chat.mjs` 两命名空间;小程序 `aiCustomerService(message)` 不再带 history;`D155截图/luvia-bj_ai-chat.png` 魔数 + 亲看 · 两处各一份

## 一、收

- 出口唯一、五道门在册加条数棘轮、只回 `reply` 不回整份会话(这一条你想到了,我没写——记你一笔)、身份分登录/访客两命名空间、小程序不再带 history——**收**。
- 截图亲看:北京店问「9月8日能约吗?」,答「好的,明天(9月8日,周二) 可以。您想做美甲还是美睫呀?」——这是采集在一句一问,老路走不到;与模拟器逐字相同——收。
- 判据自咬两处(②a 函数体范围、③b 写死 2 行)当场改——收;「没病也会红」的判据是废判据,你自己先砍了。
- **J-34 按复发登记——收,而且这个登记方式是对的**:教训只写回执不装护栏 = 没教训。`knife-backup.sh` + 预检两条才是兑现。总表里 J-34 我改成「复发(首次 08-30c)」。

## 二、待裁 #4 · 裁:并句,段 7 顺手做(≤ 15 分)

D147 那句的承诺现在是真的(消息入库、真待人工),所以用它——对。指路那半句的价值别丢:**并成一句**——「不好意思,我这边暂时没法自动回复,已经转给同事啦,稍后回您~ 想先约的话可以直接在小程序里选项目预约」+(店有电话时)「,或致电 XXX」。判据从「比文案」翻成两条:① 承诺兑得了(消息在库、状态待人工);② 指路在(含「预约」二字;店有电话则含电话,无电话则 0 次出现「致电」)。三入口同句(同一出口自然同句,判据只要对一次)。

## 三、访客身份 · 记上线批,不在本批动

`mp-guest:<clientId>` 的串是**客户端自己生成**的(`api.js:775`,时间戳 + 随机),访客只要猜中/复制别人的串就能接着别人的会话往下聊(接口只回 `reply`,读不到历史,但能**续**别人的预约草稿)。本机/沙箱可以;**上生产前**访客串改成服务端签发(首问时后端发一个签名串,客户端只负责存),登上线批清单。现在写一行判据占位:`mp-guest` 串若来自客户端且未签名 → `/health` 报 `guestIdUnsigned: true`(上线批那条看它变 false)。

## 四、两处小的,进段 7 六通 v4 锚

- 回复「(9月8日,周二) 可以」括号后多一个空格——D148 族,格式化那处补一个 trim;
- 小程序 `pages/ai-chat` 的欢迎语是**客户端写死**的(「你好呀~我是本店的 AI 客服✨…」),它是页面壳不是答案,允许留在客户端;但判据加一条:欢迎语里不许出现本店没有的承诺(现在那句「需要人工的话我也会帮你转接」成立,因为出口真会转)。

## 五、段 7 开工(05q §二 D150–D153 + §四 六通 v4,原文不改)

提醒两点:D151 合并窗落在 `handleWecomInbound` 入口——D155 之后小程序自然吃到,判据里三入口各测一次「三句间隔 1 秒 → 一条回复」;D153 的静态判据你在 D155 里已经立了(出口唯一 + 五道门),段 7 里**引用不重写**。六通 v4 出来我发店主打第二轮分。

## 六、交齐

D150–D153 各判据/造病 · 待裁 #4 并句两判据 · `guestIdUnsigned` 占位 · 六通 v4 文件路径 · 提交号 · 回归/断言增量 · 整跑时长 · 棘轮两数 · 未推 main。
