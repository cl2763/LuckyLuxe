# 网页上线发布验收（2026-09-24）

状态：本地发布候选验证完成，等待 GitHub CI 和生产部署核验。不得把本报告读成已上线。

## 本批范围

优先交付网页：平台配店、服务及顾客导入、AI 授权开关、账号退出；商家排单、定金、改期、开单；未绑定微信顾客凭商家限时链接签字；员工客档、服务小记和工作台。共享后端同时修正小程序入口，但本批不代表微信体验版或审核已经通过。

发布分支从当前线上 1329a95 建立，不包含本机另三条尚在评估的修图提交。

## 已验证

- 完整本地发布回归 153 套，全部通过；四个依赖沙箱的套件各在独立夹具库完整执行。
- 浏览器实际操作 51 项通过，包含平台、商家、员工和网页顾客签署；无未处理异常。截图和逐项结果在 `12t-web-ready-evidence/`。
- 签署与读取权限 55 项，顾客手机号验证 30 项，员工切换账号缓存 4 项；签字不自动冒充微信绑定，只知道单号不能查看单据和签名。
- 断言基线、代码凭据扫描、数据库目标护栏、文件行数、结构迁移均通过。
- 生产两家店已保存更新前业务数量与账目基线；备份已成功：`/app/apps/api/local-data/backups/lucky-luxe-20260924055309-web-ready-20260924.sqlite`。

## 发布核验待完成

1. GitHub CI 全绿。
2. 生产备份已成功，路径见上。
3. 发布后核对三个域名版本、关键网页资源、数据表结构、账本触发器和两家店五项基线。
4. 网页确认可用后，再做小程序真机体验、门店入口和提审检查；真实微信授权、真机操作及微信审核不能用接口替身结果代替。

## 补充发布检查

旧造病夹具留下的异常积分、孤立单据及自由文本分类会污染第二轮检查。账本护栏测试现只在临时数据库副本执行，分类探针用完清理；同库连续跑两轮爽约售后，各 760 项通过。开店灯号测试保持原有基线 396，不把造病空店数量变化误算为覆盖增加。

浅深色对比度实扫 3264 个文字节点：甲档阻断 0；乙档既有配色欠账去重 14 处，164 个渐变面节点本工具不能判定。六页两档样式运行检查 12/12 通过。

跨店归属检查已改用公开接口返回的真实可约时段；周一休息的反例夹具仍要求本店建单成功、跨店明确返回 USER_TENANT_MISMATCH。原有 8 项断言保留。
