# 小批 09k · 🔴🔴 急 · `DATA_DIR` 生产上**没有设置** · 库正跑在回落路径上 · **推停,停到两个路径对上为止**

**日期**:2026-09-16 · **发件**:Cowork · **收件**:Code
**上游令**:09i(`fa3fed3e…`)· 09j(`61f7e1d6…`)· 本令。**按 J-81,回执首行点名三份到手与否。**

---

## 〇 新事实(店主在面板上读的,一句话)

> 🔴 **`DATA_DIR` —— 没有设置。**

你在 09h 问 2 里自己写下的那一行是:
> 「库路径由 **`DATA_DIR`** 决定(`local-server.mjs:128`)」

**两句拼起来** = 🔴 **生产库现在跑在一个「`DATA_DIR` 没设时的回落路径」上,而这个回落路径是什么,没有人报过。**

---

## 一 先把事情说准,不许放大也不许缩小

**这不是新出现的风险,是一直如此。** 店主的数据现在好好的(70 个真人 + 7 行七月压测残留,两个半月没丢),**所以现实只可能是下面两种之一**:

- **甲(大概率)**:回落路径**正好落在持久卷 `api-volume` 的挂载点里**。一直这样,一直正常。**那就什么事都没有,推。**
- 🔴 **乙**:回落路径**在容器的临时盘上**,而这个容器**从 2026-07-03/04 起没有重新部署过**,所以数据才活到今天。
  **若为乙 —— 推 main = 重新部署 = 换容器 = 那个库文件连同全部顾客数据一起消失。**

**甲和乙,靠推理分不开。靠两个事实就能分开。**
🔴 **在这两个事实对上之前,推停。** 这一条**取代 09j 的件 2**,优先级高于 09j 全部四件。

---

## 二 要的两个事实

### 事实 A(你查,本机,不碰生产)· 回落路径到底是什么

**`local-server.mjs:128` 附近那段拼库路径的代码,逐字报**:
1. `DATA_DIR` **不存在**时,拼出来的**绝对路径**是什么?——🔴 **不要读代码推,按你 09h 的丙档标准做:本机隔离实例里 unset 掉它启动,用 `lsof` 报进程真正打开的那个文件的绝对路径。**
2. 是 `||` 兜底、`??` 兜底、还是拼成 `/lucky-luxe.sqlite`(前缀直接空掉)?**给 `文件:行` 原文。**
3. 🔴 **顺带答 09i 问 4**:除了 `DATA_DIR`,还有没有别的名字参与拼这条路径(前缀/子目录/文件名)?**逐个点名 + 扫描面。**
4. **旧代码(`origin/main = 285b20d`)的同一段**,回落路径是不是同一个?**不同 → 单独红报。**(= 09j 件 2 的核心,现在必须连着答。)

### 事实 B(店主读,面板上)· 卷挂在哪

`api-volume` 的 **Mount path**。**这是一个路径,不是密钥,可以报。** 我已去问她。

### 🔴 判据

**事实 A 的绝对路径,是不是落在事实 B 那个挂载点底下?**
- **是** → 甲档成立,**这一条解除,回到 09j 的四件。**
- **否** → 🔴 **乙档,推停死。** 立刻报我,**不要自己想办法,不要改代码「顺手修一下」**,我要先和店主定备份与迁移的顺序。

---

## 三 🔴 这件事把「推前备份」从一个步骤变成了整件事的地基

09j 件 1 是「备份路径要坐实到 `lsof` 那一层」。**现在它的分量变了**:
🔴 **如果是乙档,那份备份就是那家店两个半月全部经营数据的唯一副本。**
**所以件 1 不但不降级,它现在排第一** —— 而且加一条:

> 🔴 **备份做完之后,必须验证这份备份是可读的**:在一个与生产无关的地方用 `sqlite3 <备份文件> "select count(*) from users"` 之类的读一次,**报出行数,并与生产那头的数对上**。
> **一个没被打开过的备份文件,和没有备份是一回事。**(J-65③ 的备份版:**一个不带「我读得开、里面有多少行」的备份,和一个没有依据的备份是一回事。**)

---

## 四 停线(J-77③,写明层级)

- 🔴 **停「推 main」这一件,停到事实 A 与事实 B 对上为止。**
- 🔴 **停「任何会导致生产容器重启/重新部署的动作」** —— 包括但不限于:改 Railway 变量并 Apply、重新部署、重启服务。**Code 本来就不许碰 Railway,这一条是写给台账和店主看的。**
- 🟢 **不停**:09j 件 1 / 件 3 / 件 4 照做(它们都不需要碰生产,而且件 1 现在更要紧);09i 推后清单照旧推后。
- 🟢 **不停这一班**,代码照改。
- 🔴 **不许 push。**

---

## 五 回执

1. 首行按 J-81 点名三份上游令 + md5。
2. **事实 A 四问逐条,每条带现场证据(`lsof` 输出的绝对路径 + `文件:行` 原文)。**
3. 明确写一句:**「甲档」还是「乙档」还是「等事实 B 才能判」。**
4. 09j 件 1 / 件 3 / 件 4 的进度(件 1 加上「备份可读性验证」那一条)。
5. 🔴 **一个 push 都不许执行。**
