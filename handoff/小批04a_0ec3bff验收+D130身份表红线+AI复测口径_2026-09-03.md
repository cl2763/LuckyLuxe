# 小批04a — `0ec3bff` 验收(三件带病收)· 🔴 D130 身份表串味红线(现修)· D131 三十一张表同族(入册报数)· AI 复测 12 场景执行口径

> 2026-09-03 · Cowork 亲核于店主 Mac(命令原文附文末)· 本文两处各一份:桌面交接夹 + 仓内 `handoff/`
> 先说结论:**三件都收,但都带病**;顺手在拆档脚本的同族里咬出一条**红线级**新病(D130),按 03t 口径红线现修,不等结案。

---

## 〇、总裁

| 件 | 裁 | 一句话 |
|---|---|---|
| ① 三列清单改由刀生成 | **带病收** | 刀是对的,但**改动没入库**、清单**在同一提交上已过期**、刀**咬到自己** |
| ② 拆档迁移脚本 | **带病收** | 护栏五件齐;但**只拆了一半**——16 张表带 `user_id`,本次到店的从属记录不跟单走,尺子又只量 `bookings` 所以看不见 |
| ③ AI 复测第一问 | **收** | 现查与你一致;补一条你没说的:`.env` 里同住生产令牌,整份喂沙箱等于把生产令牌塞进沙箱进程 |
| 🔴 D130 | **红线现修** | `user_identities.tenant_id DEFAULT 'lucky-luxe'` + 启动回填不带租户 → 本机库 96 行身份挂错店,别店顾客的 phone/openid 身份能被旗舰店 upsert 改指 |
| 🟡 D131 | **入册,先报数** | 带 `DEFAULT 'lucky-luxe'` 的表不是 2 张,是 **31 张**;D128/D130 那把刀只盯 `users`/`user_identities`,同族 29 张没人盯 |
| AI 复测 | **开工,按 §四 口径** | 只打 4310;密钥走单独最小 env 文件且先进 .gitignore;`AI_REQUIRE_REAL=true` 造病验红先行;12 场景每条交四样 |

顺序:**§一 三处病 + §二 D130 现修(一轮交)** → **§四 AI 复测(第二轮交)** → 结案批交齐 → 主页重画批。§三 D131 只报数不动手,报数随第一轮交。

---

## 一、`0ec3bff` 三件亲核结果

### ① 清单由刀生成 —— 收,三处病

**核到的**:`tools/gen-guard-checklist.mjs` 140 行、`handoff/写库脚本护栏三列清单.md` 121 行、手写版改名作废(`_作废_…手写版_2026-09-02.md`)、`回归日志_2026-09-03z_86套件.log` 3,185 行,都在提交里。剥注释函数保住行号(我在 `local-server.mjs` 18,243→18,243、`ledger-guards.mjs` 151→151 上自守过)。

**病一 · 改动没入库**:`git status` 显示 `M apps/api/test-txn-rollback.mjs`(+18/−20,正是「手写 9 处 → 现扫」那一改)**停在工作区**,`HEAD` 里的 `test-txn-rollback` 还是 9 处手写清单。你报的「`test-txn-rollback` 现在也用同一把尺扫」在提交里**不成立**。问:03z 回归日志跑的是工作区版还是提交版?→ 提交它,**重跑这一把**附输出;日志文件名对应到新提交号。

**病二 · 清单在同一提交上已过期**:清单头写「生成于提交 `15a3a21`」,可 `0ec3bff` 自己新增了两个文件。我用你那把尺(同一正则、同一扫描面)在 `0ec3bff` 上现扫:**14 处含事务,不是 12**——多出的两处:

- `tools/migrate-cross-tenant-bookings.mjs` —— **真写库点、真事务**,清单上没有它。清单是给拆档脚本这种东西看的,它自己不在上面,这就是清单的意义丢了一半。
- `tools/gen-guard-checklist.mjs` —— **刀咬到自己**:生成器源码里写着 `BEGIN IMMEDIATE|BEGIN TRANSACTION` / `COMMIT` / `ROLLBACK` 这几个字面量作尺子,按形态就被当成「含事务的写库点」。刀认形态没错,但生成器不是写库点。

**裁**:
1. 加一条**常驻判据**:`handoff/写库脚本护栏三列清单.md` 必须与**当前提交现扫输出**一致(结构等价即可,生成时间/提交号那两行除外);不一致 = 红,红的话报「清单过期,跑 `gen-guard-checklist --write`」。没有这一条,「由刀生成」只是把手写的时机换了个人,同一提交就过期已经证明了这一点。
2. 生成器与所有 `test-*`/`run-*` 同列「刀本身」排除面,理由写进清单头的「尺子」段(不是悄悄排除)。不许用「把字面量拆开拼」这种躲刀的写法。
3. 重生成,清单里必须出现 `tools/migrate-cross-tenant-bookings.mjs`(A 类 · 直连库 · ✔ requireTarget · ✔ 有事务)。

### ② 拆档迁移脚本 —— 收,一处大病

**核到的护栏**(逐条看过源码):`requireTarget({envName:'MIGRATE_DB_PATH'})` 无默认目标 ✔ · 默认 dry-run、`--apply` 才写 ✔ · `copyFileSync` 先备份且路径打印 ✔ · `BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK` 一个事务 ✔ · 五表前后打数 ✔ · 幂等(零条即退) ✔ · 复用优先于新建(openid→phone→email 三键找已有档案) ✔。

**大病 · 只拆了一半**。本机库现查:**16 张表带 `user_id`**。其中六张同时带 `booking_id`——它们是「这一次到店」的从属记录,不是「他在别店的资产」:

| 表 | 列 | 归属 |
|---|---|---|
| `settlements` | tenant_id, booking_id, user_id | 这单的结算 → **跟单走** |
| `deposit_receipts` / `deposit_disposals` | tenant_id, booking_id, user_id | 这单的定金 → **跟单走** |
| `service_notes` | tenant_id, user_id, booking_id | 这单的小记 → **跟单走** |
| `reminder_tasks` | user_id, booking_id, tenant_id | 这单的提醒 → **跟单走** |
| `booking_drafts` | user_id, booking_id, tenant_id | 这单的草稿 → **跟单走** |
| `member_timecards` / `stored_value_transactions` / `points_transactions` / `coupon_grants` / `timecard_refunds` / `deposit_retains` | tenant_id, user_id(无 booking_id) | 资产 → **不动**(口径不变),但要打数报告 |
| `quote_requests` / `archive_bind_tokens` / `user_identities` | user_id, tenant_id | 见 D130;`quote_requests` 报数 |

脚本只改 `bookings.user_id`,尺子「剩余跨租户单」也只量 `bookings`:拆完以后结算单、定金、小记还指着旗舰店那份档案,**串味换了张桌子坐**,而尺子报 0。你的「储值/结算 0」是行数没变,不是归属对了。

**裁**(改完再彩排,彩排样本要带病):
1. 演练输出对每条候选单**逐表打挂载数**(上表 16 张,数字);
2. `--apply` 在**同一事务**里把六张「跟单走」表中 `booking_id = 该单 AND user_id = 旧档案` 的行一并指到目标档案;资产表不动;
3. 「差值形状」与「剩余跨租户」两把尺**覆盖到六张跟单表**:`SELECT COUNT(*) FROM <表> x JOIN users u ON u.id=x.user_id WHERE x.tenant_id<>u.tenant_id` 逐表必须 0;
4. 再彩排:自造那张单要**带结算 + 小记 + 提醒各一行**,走复用路径与新建路径各一次,逐表行/列对照附回执;
5. **造病验红**:故意把 `service_notes` 从跟单表清单里去掉再跑 → 尺子必须红;
6. 生产那一条:**先只读**报它在 16 张表里的逐表挂载数(只要数字,不要任何字段值),我看后另发批;跑不跑随部署批。

### ③ AI 第一问 —— 收,补一条

现查与你一致:`ai-utils.mjs:1-5`(`AI_API_KEY` 有 → `openai-compatible`,无 → `mock`;`AI_REQUIRE_REAL==='true'` 时无真供应商**抛错**,122–123 行);全仓只有 `启动服务器.command:13` 带 `--env-file-if-exists=apps/api/.env`;`run-all-tests.sh`/`start-sandbox.sh` 无任何 `AI_*`;`.env` 在 `.gitignore`、未跟踪。**结论成立:回归里 21 个沾 AI 的套件从来只打过 mock。**

**你没说的一条**:`apps/api/.env` 里同住 `PROD_OWNER_TOKEN`、`OWNER_DEMO_TOKEN`、`WECHAT_MINI_SECRET`、`WECOM_*`、`COS_SECRET_*`(我只看了键名,没看值)。给沙箱进程整份 `.env`,等于把**生产令牌与三家外部密钥**塞进一个演示/走查用的进程。§四 按最小授权另开文件。

---

## 二、🔴 D130 · 身份表串味(跨租户 · 红线 · 现修)

**病**:`user_identities.tenant_id TEXT NOT NULL DEFAULT 'lucky-luxe'`(与 D126/D128 的 `users` 同一根子),而 `local-server.mjs:17333–17342` 的**启动回填**四条 `INSERT OR IGNORE INTO user_identities (…)` **都不带 `tenant_id`**,每次启动都跑。

**证据(本机库,只读)**:`user_identities` 220 行 / 161 人;**96 行**身份的 `tenant_id` 与其主人 `users.tenant_id` 不同,分布:`lucky-luxe|demo-ai|email 24`、`lucky-luxe|demo-ai|phone 24`、`lucky-luxe|jics-nail|phone 16`、`lucky-luxe|jics-nail|wechat_miniprogram 31`、`lucky-luxe|hoptest-demo2|wechat_miniprogram 1`。全部挂在 `lucky-luxe` 名下,主人全在别店——正是「不带就落默认」的形状。

**危害路径(读码,`local-server.mjs:5567–5584`)**:`upsertUserIdentity` 以 `(provider, provider_user_id, ownerTenant)` 找 `existing`;旗舰店一位顾客用手机号 X 绑定 → `ownerTenant='lucky-luxe'` → 命中的是**小婕店顾客那行被错标成 lucky-luxe 的身份** → `UPDATE … SET user_id = 旗舰店顾客` → **小婕店顾客的手机号身份被改指给别人**,她下次用手机号登小婕店的小程序就找不到自己。另:唯一键含 `tenant_id`,所以正确挂在 `jics-nail` 的那行和错标的这行**并存**(220/161 就是这么来的)。生产上小婕店目前只有 1 位顾客(且已被吞),所以现在伤面小——**上线后每一次启动都会再造这种行**,这是要在部署前修掉的。

**修法**(这一轮交):
1. 回填四条 `SELECT` 带上 `users.tenant_id`(`INSERT OR IGNORE INTO user_identities (…, tenant_id) SELECT …, tenant_id FROM users …`);`import-customers.mjs:72` 已带,不动;`upsertUserIdentity` 逻辑不动。
2. **D128 静态刀扩到 `user_identities`**:全仓每一处 `INSERT INTO user_identities` 必须显式写 `tenant_id`;金丝雀正反各一;`stillDefault` 在案行加这张表。
3. **存量修复 = 条件更新**(本机库 4128、沙箱库 4310;各自先备份、批前批后逐表行/列快照对照):
   - 错标行且**目标租户下不存在同 (provider, provider_user_id)** → `UPDATE user_identities SET tenant_id = (SELECT tenant_id FROM users WHERE id = user_id)`;
   - 错标行且目标租户下**已有正确行** → 删错标行(身份表非账本,允许删;删前后各报数);
   - 修完尺子:`SELECT COUNT(*) … WHERE x.tenant_id<>u.tenant_id` = 0,且 `(tenant_id, provider, provider_user_id)` 无重复。
   - 这是写库:按**写库自报律·手令形态**先报目标库绝对路径,4128 那一份先在沙箱彩排。
4. **行为判据**(沙箱造景):A 店顾客 phone=X、B 店顾客 phone=X 各自 upsert 后,两行 `tenant_id` 各归各、`user_id` 各归各;再以 A 店身份 upsert 一次,B 店那行**一个字段不变**(逐字节对照)。
5. **造病验红**:把回填的 `tenant_id` 再去掉 → 静态刀红;把行为判据里 B 店那行手改成 lucky-luxe → 行为判据红。
6. 生产:**只读报数**——`user_identities` 总行数、错标行数、按 (identity 租户/主人租户/provider) 分布,只要数字。我看后另批。**上线批**清单加一行:部署包含 D130 回填修正,生产存量修复随拆档一并批。

---

## 三、🟡 D131 · 同族 31 张(入册 · 先报数不动手)

本机库 schema 现查:带 `tenant_id … DEFAULT 'lucky-luxe'` 的表 **31 张**(全部 78 张带 `tenant_id`):`admin_accounts, ai_conversation_states, ai_learning_examples, ai_response_feedback, attendance_records, bookings, coupon_grants, coupons, finance_recurring_rules, finance_transactions, membership_packages, points_prizes, points_transactions, quote_requests, recharge_tiers, reminder_tasks, salary_payrolls, salary_plans, service_categories, service_notes, service_prices, services, staff_compensation, staff_nudges, store_wifi, stored_value_transactions, stores, technicians, user_identities, users, wechat_conversations`。

D128 刀只盯 `INSERT INTO users`,D130 加 `user_identities`;剩下 29 张**同一根子、没人盯**。「点名处必扫同族律」:

1. 表清单**由刀从 schema 生成**(`pragma_table_info` 找 `tenant_id` 带 DEFAULT 的),不手写;
2. 刀扩成:对清单里每张表,全仓每处 `INSERT INTO <表>` 必须显式写 `tenant_id`(`INSERT … SELECT` 也算,SELECT 列表里得有 `tenant_id`);
3. **新刀先预判该红几处**:先以 report-only 跑一次,**逐表报命中数**随第一轮交回——**不修、不白名单**,等我看数再裁是逐处修还是分批;
4. 上线批「去 DEFAULT」那一行,从「`users`」改成「31 张表」。

---

## 四、AI 复测 12 场景 · 执行口径(03t §三 的落地细则)

**目标**:店主要亲眼看到**真模型**在多轮、带历史、带打断的对话里,状态机走对没有——不是看夹具绿。

1. **只打 4310**。回执首行自报沙箱库绝对路径;本机库 4128 与生产不参与;`run-all-tests.sh` 与 86 套回归**不带真模型**(仍 mock,基线 2,854 不动,不花钱)。
2. **密钥注入 · 最小授权**:
   - 先改 `.gitignore`:加 `apps/api/.env.*` 与 `!apps/api/.env.example`;`git check-ignore -v apps/api/.env.ai-sandbox` 的输出附回执;`test-credential-scan` 金丝雀加一条(一行 `AI_API_KEY=sk-…假值` 的文件形态必须被咬);
   - 新建 `apps/api/.env.ai-sandbox`,**只五行**:`AI_PROVIDER`、`AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY`、`AI_REQUIRE_REAL=true`——不带 `.env` 里任何别的键;**值由店主本人填**,你不复制、不打印、不写进任何文件;
   - `start-sandbox.sh` 加**可选**参数 `--ai-env <文件>`,不传 = 现状(mock)一个字不变;传了 = `node --env-file-if-exists=<文件> …`。
3. **造病验红先行**:`AI_REQUIRE_REAL=true` 且**不给** key 起沙箱 → 第一条 AI 请求必须响亮失败(`ai-utils.mjs:123` 那条错),**不许悄悄回落 mock**;这一次失败的输出附回执,再放 key 正式跑。
4. **12 场景每条交四样**:①输入序列(每轮原文,含打断/改口/带历史的前情);②模型**原始回复**(原文,不摘);③每轮后的 `quote state` 与横幅/状态字段快照;④机器判据结果——**锚状态字段,不锚文案**(模型措辞会变,状态不该变)。人可读的对话记录合成一份走查文档 `handoff/AI复测_真模型12场景_<日期>.md`,店主要读。
5. **红了先归因**:模型 / 规则 / 夹具三选一写明;**改夹具让它绿不算修**;模型抖动的场景跑 3 次报 n/3;费用报总 token 与估价(店主 03t 已点头花这笔钱)。
6. **模型回显脱敏**:日志与走查文档里若出现 key 片段一律 `sk-****`;交回前 `test-credential-scan` 扫走查文档。
7. 跑完:`apps/api/.env.ai-sandbox` **留在本机不入库**(已 ignore),沙箱按原样(不带 `--ai-env`)拉回。

---

## 五、交齐清单

**第一轮**(§一三病 + §二 D130 + §三报数):提交号;`test-txn-rollback` 入库并重跑输出;清单一致性判据 + 重生成清单(含 migrate 脚本、不含生成器);拆档脚本改法 + 带病彩排逐表对照 + 造病红;D130 回填修正 + 刀扩 + 本机/沙箱存量修复前后逐表行列对照(**未动须有证:批前快照交齐**)+ 行为判据 + 造病红;D131 逐表命中数(report-only);生产两项只读报数(拆档 16 表挂载数、身份表分布)。两个巨型文件棘轮只许降。

**第二轮**(§四):`.gitignore` 与 check-ignore 输出;无 key 造病失败输出;12 场景四样 + 走查文档;费用;沙箱拉回证明。

**不许出现**:任何 key/密码/令牌的值;「未动」不带对照;`head`/`tail` 截断的底数命令。

---

## 附 · Cowork 亲核命令原文(店主 Mac,`/Users/changliu/Documents/Codex/2026-04-29/new-chat`)

```
git log --oneline -8 ; git status --short ; git show --stat=300 --format=%h HEAD
git diff apps/api/test-txn-rollback.mjs
node -e '<与 test-txn-rollback 工作区版逐字同一 bare()/tracked/filter 的现扫,打印命中数与逐文件 COMMIT/ROLLBACK>'   → 14
node -e '<bare() 前后 split("\n").length 对照:local-server 18243/18243 · ledger-guards 151/151 · migrate 124/124>'
sed -n 5538,5590p apps/api/local-server.mjs ; sed -n 17325,17345p apps/api/local-server.mjs
grep -rn "INTO user_identities" apps/api tools --include='*.mjs' | grep -v '/test-'
sqlite3 -readonly apps/api/local-data/lucky-luxe.sqlite  ".schema user_identities"
sqlite3 -readonly … "select x.tenant_id,u.tenant_id,x.provider,count(*) from user_identities x join users u on u.id=x.user_id where x.tenant_id<>u.tenant_id group by 1,2,3"
sqlite3 -readonly … "select m.name,p.dflt_value from sqlite_master m join pragma_table_info(m.name) p where m.type='table' and p.name='tenant_id' and p.dflt_value is not null"   → 31
sqlite3 -readonly … "select m.name,group_concat(p.name) from sqlite_master m join pragma_table_info(m.name) p where … p.name in ('user_id','booking_id','tenant_id','settlement_id') …"
逐表行/列快照:backup-D127-20260902-221038.sqlite vs lucky-luxe.sqlite → 差异仅 bookings −5/+1列、booking_status_history −5、payments −5、settlements +1列、notify_scan_marks +1(前四项对应 03u 清 15 行与 D121 加列;notify_scan_marks +1 请你说明来源——回归还是店主 4128 的调度器)
sed -n 1,6p apps/api/ai-utils.mjs ; grep -rn env-file . --include=*.sh --include=*.command --include=*.mjs ; grep -n AI_ apps/api/run-all-tests.sh apps/api/start-sandbox.sh ; git ls-files | grep .env ; grep -o '^[A-Z_]*=' apps/api/.env   (只取键名)
lsof -nP -iTCP:4128 -sTCP:LISTEN ; lsof -nP -iTCP:4310 -sTCP:LISTEN ; ps -o pid,lstart,command -p …   → 两进程 02:50:58 同秒起,cwd apps/api,均无 AI_API_KEY
```
