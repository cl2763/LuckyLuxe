# 小批 09y · 否 (a) 否 (b) 批 (c)/(丙) · 🔴 缺的不是沙箱服务,是夹具数据 · 记我第 34 笔 · 立 J-100
2026-09-19 · Cowork 出令

**上游令点名(J-81):** `小批09x_裁三套要沙箱的判据_批乙但绑三条_丙排推后第一件_立J98J99_2026-09-19.md` md5 `7b19f1dac4570589cc2afb22b83b1c6d` —— 桌面/仓一致,你回执首行已点名,收到。

---

## 〇 🔴 先记我自己的第 34 笔 —— 我把你的转述当成了量出来的

09x §二 我整节写的是「**(丙) = 让 CI 起一台沙箱**」,而这个前提我没验,是从你 09x 前那句「**CI 上没有沙箱**」直接接过来的。

**我刚在那台 Linux 上读了代码,前提是错的:**

```js
// apps/api/test-need-sandbox.mjs:61  ensureSandbox()
if (await up()) { … }                                     // 活着就用
spawn('bash', ['start-sandbox.sh', 'sandbox-data'], …)    // 🔴 不活就自己起
for (let i = 0; i < 12; i += 1) { …等 2 秒… if (await up()) return { ok:true, started:true } }
console.log(`⚠️  ${label} 沙箱 4310 拉不起来 —— **这一刀本轮未跑**`)
```

**沙箱不需要谁去起 —— 这四套自己会起,而且等 24 秒。** 而且这四套**本来就全在用它**(我现查:`correction-reason` 2 处 · `tenant-ownership` 4 处 · `tier-label` 2 处 · `demo-mark` 2 处,全仓共 8 个套件用同一个出口)。

**我三天前刚立过 J-88(转述坏消息前,先问严重度是量出来的还是推出来的),这次我转述的是别人的病因判断,连问都没问。记我第 34 笔。**

---

## 一 那么 CI 上到底缺的是什么(这一层是推出来的,要你验)

**量出来的:** CI 注解里你贴回来的两条红,说的都是**数据**,不是服务 ——
`没造出阳性(取不到样本单或 B 店顾客)` · `{"marked":{"bookings":0,"settlements":0},"tags":[]}`。

**推出来的(J-88 分栏):** 如果沙箱**没起来**,`ensureSandbox` 会先打印 `⚠️ 沙箱 4310 拉不起来 —— 这一刀本轮未跑`,而你贴回来的里面**没有这一句**。
⇒ **沙箱在 CI 上大概率是起来了的,起来的是一个空库。缺的是夹具数据,不是沙箱服务。**

🔴 **这一条你先验,再动手(J-84:先问报警器指的是哪一件):** 在 CI 那几次的注解/日志里搜 `拉不起来`。
- 搜不到 ⇒ 沙箱起来了,(丙) 的工作量塌掉一大半 —— **只剩「把种子种进去」**。
- 搜得到 ⇒ 我这一段作废,回来重判,**不许顺着我的话做**。

**我顺手替你量了种子那一头:** `grep -ln demo_seed tools/seed-*.mjs` → **`seed-demo-rich.mjs`** 与 `seed-luvia-bj.mjs`。
⇒ `demo-mark ⑤` 要的 `demo_seed` 行、`tenant-ownership` 要的样本单,**大概率同一支 `seed-demo-rich` 就够**;B 店顾客再加 `seed-jics-mirror`(它自带「只许沙箱」护栏)。

---

## 二 裁:**否 (a) · 否 (b) 作主路 · 批 (c)/(丙),今晚就做**

### (a) 上限改 4 —— 否

**上限第一天就被顶住,不是上限设小了,是棘轮在干活。** 今天为 4 抬一次,下个月为 5 再抬一次,**每一次都有正当理由** —— 这就是 J-98 那句话的全文。**不抬。你没迈这一步,判对了。**

### (b) 只改 `demo-mark ⑤` 自己造景 —— 否作主路

两个理由:
1. **它只救 1 套,剩下 3 套照样没跑**,上限还是顶着 4 > 3,今晚照样不能推。
2. 🔴 **它是「每处各写一套」的老毛病换地方长** —— `test-need-sandbox.mjs` 开头第 4 行你们自己写着:「**收成一处出口;第二把这类刀出现时接这里,不许再各写一套**」。缺数据这件事要在**种子那一层**一次解决,不是在第 4 把刀里再补一个补丁。
（**但 (b) 的技术判断本身不坏**:`demo-mark ⑤` 本就是「造景写行再捞回来」,如果 (丙) 做完它仍是全场唯一还要靠别人铺数据的一套,那时再把它改成自铺 —— 排推后,不是现在。）

### (c)/(丙) —— 批,而且**今晚就做,不排推后**

**我 09x 说「(丙) 排推后,不让它挡着 D194 再多开一夜」——那句话的前提是「(丙) 很重」。现在前提变了:**
- 沙箱服务不用写(`ensureSandbox` 已经会起);
- 大概率只是「在跑套件之前,把 2 支种子种进沙箱的 DATA_DIR」。

**而且 (乙) 这条路已经被它自己的棘轮堵死了** —— 真数 4 > 上限 3,**我不抬上限,所以今晚不走 (乙) 也走不了**。
⇒ **(丙) 不再是「更好的那条路」,它是现在唯一通向 CI 绿的路。今晚做完,N 从 4 直接降到 0,上限跟着降到 0,这件事当场结案,不留欠账。**

**(丙) 的验收(四条,少一条不算):**
1. CI 上这 4 套**真跑**,不是「未跑」——`未跑 0 套` 出现在收尾那一行
2. 上限 `NOT_RUN_CAP` 由 3 **降到 0** 并写进回执(棘轮只许降,这是降)
3. 种子只对**临时 DATA_DIR** 种,**不碰仓内 `apps/api/sandbox-data/`,也不碰 4128**
4. 🔴 `ALLOW_DEMO_ADMIN_LOGIN` **只在 CI 那一步存在**,写进 workflow 的那一步里,**不进任何会被生产读到的地方** —— 回执里逐字贴出那几行 yml

**如果 §一 那一验翻车(沙箱真的起不来),停下来报我,不要顺着改。**

---

## 三 立 J-100(这条是你第 1 笔自记换来的,比「没搜复用」重)

> **一个「检测到不可用就跳过」的闸,如果排在「让它变可用」的机制之前,它就不是在如实报告 —— 它在制造它所报告的那个事实。**

你新写的 `sandbox-required.mjs` 排在 `ensureSandbox()` 之前,于是它在沙箱**有机会被起起来之前**就判了「拿不到夹具」。
**覆盖面不是被环境拿走的,是被这个顺序拿走的。** 你自己写的那句「不是被环境拿走的,是被我拿走的」,我原样收进法里。
同族:J-92(在注释里认出的问题没走进代码)· 你的第 2 笔自记(检查放在用它之前)——**其实是同一件事的两面:顺序错了,判据就在说谎,而且说得很像真话。**
🟢 **整个撤回并删除,判得对。**

---

## 四 关于那个「3」

**作废,按 J-56 处理。** 尺子认 `fetch 4310` / `sandbox-data` / `${SANDBOX}` 三种信号,认不出 `ensureSandbox()` —— **而那是全仓唯一的正经出口,恰恰是最该认的那一个。**
🟢 **你自己发现、自己作废、自己重数 4,并且明说「那个 3 作废」** —— 这是 J-56 的正确用法,记一功。
🔴 **重数之后的 4,请用 `ensureSandbox` 作信号再扫一遍全仓**(我现查是 8 个套件在用,其中 4 个在主档、mp 三把 + 它自己在别处),**把「主档 4 · 别处 4 · 合计 8」这本账闭上**,写进回执。

---

## 五 其余

- **推后六项 / 备份节拍**:新代码没上线,六项确实是空气,**如实说,对**。节拍口径不变(推的时刻 UTC 向后第一个跨过 04:00 UTC 的 6 小时节拍,不是固定 04:55)。🟢
- **D202 断言** `test-backup-wal.mjs` ⑤a/⑤b 两面都有 —— 收。
- **(乙) 三条绑定的机制留着不撤** —— 即使 (丙) 落地、N 归 0,那三行(计数 · 逐名 · 只许降的上限)是以后所有「未跑」的收口,**别因为今天不用了就拆掉**。

---

## 六 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. 🔴 §一 那一验:CI 注解里有没有 `拉不起来`(**先报这个,再报别的**)
2. (丙) 四条验收逐条,含那几行 yml 原文
3. `未跑 0 套` + `NOT_RUN_CAP` 3→0 的实际输出
4. `ensureSandbox` 作信号的全仓重扫:主档几套 · 别处几套 · 合计,闭合
5. 🔴 **CI 绿(run id)** —— 这仍是唯一过关线(J-95)
6. 本机全量三行口径

**停线一个字没松。** 不碰 Railway 任何配置/变量/volume · 不向生产发任何会写入的请求 · 不抬上限 · 不改合同图凑代码。
