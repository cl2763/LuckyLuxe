# 已报价状态机 · 交付(2026-09-01,31p 开工令兑现)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。做完不说可以关帐。

## 三点核答照录落地

1. **6h/48h 照准**:默认 6/48,每店可配(新读写口 `GET/PUT /admin/quote-settings`,后端闸 1~72 / 1~336,
   越界 400 —— 可配没有写口就是假可配,所以补了这一条小口,套件⑤钉着);
2. **quote-state 并入会话详情**:零新读路由 —— `getWecomConversation` 序列化直接带 `quoteState`
   (列表/详情/两端同一份);
3. **AI 改口句**:句已立唯一常量(quote-state.mjs `EXPIRED_REPRICE_SENTENCE`)——
   **注入暂缓一批**(红线优先):AI 回复出口在 handleWecomInbound 有多个分支(售后/报价/通用各自
   `assistantReplyText`),单点安全注入需要先做分支盘点,赶在本批末尾塞进去=拿 matrix 66 赌进度。
   注入点分析:候选=assistantReplyText 单收口(风险:碰所有回复)vs 各分支逐个(风险:漏);
   下批与 includes 扫描同行落地,新句断言+matrix 全绿两条件照录。

## 建成件

- **新模块 quote-state.mjs**(127 行):会话键**由 transcript 读推导**(与方案的"+2 列"等价实现,
  报明:transcript 本身就是真相,少一份可腐化状态;要列存回来一句话改);报价行钉 `session_key`
  (ALTER);`quote_price_changes` 留痕表(append-only);四态读推导零状态列;
- **mark-quoted 增强**:落 session_key + expires_at(=now+可配时长);**改价防线** —— 同会话有效价不同
  → 409 `QUOTE_OVERRIDE_NEEDED`,人话句照 31p 追加逐字形:「本会话 N 小时前已由〈技师〉报价 ¥X,
  确认要按新价 ¥Y 重报吗?」(后端出,币随店);confirmOverride=true → 放行 + 留痕行;
- **横幅两端**:网页客服台会话头(qs-banner 三色)+ 小程序会话页顶部,**句零前端拼串**
  (套件断言前端文件零「本次会话已报价」字面);
- 四态句照方案:A「本次会话已报价 ¥X · 技师 · N 小时前 · 有效期至 M月D日 HH:MM」/
  B「(已过期)…需重新确认」/ C「历史报价参考:上次(N 天前)报过 ¥X · 技师 — 本次尚未报价」/ D 无横幅。

## 判据与刀

**test-quote-state 新套 16 项**(run-all 入册,陪审团保持最后):
⑤设置默认与双闸 400 / A 态横幅句 / ③改价 409 人话句+override 留痕恰 1 行(1288→988)/
②过期翻态(直写库拨过去,读时判不等真钟)/ ①切会话(transcript 拨老 7h+新进线 → reference 句)/
④两端渲染链+零前端拼串。
**两刀照方案**:刀A(改价不留痕)→ ③红 `[]` 上招;刀B(无视过期)→ ②红,泄漏横幅连着旧有效期整句上招;
均 cp 逐字节还原回绿。

## 同批杂项

- **方案文档勘误批注**:11 项方案 08-05 的「阶梯没实现」行已加 2026-08-31 勘误框
  (指向 commissionByLadder progressive + 套件 ¥1,280 案例),防下个人被旧行骗;
- D91(工资历史月切换 UI)按裁排进 **includes 扫描批**一并做;31o AI 修图件候材料并行。

## 回报固定六项

1. **改了什么**:quote-state.mjs(新)+ local-server 五针(import/构造/建表/mark-quoted 增强/
   quote-settings 口/序列化带 quoteState)+ 两端横幅 + 三色样式;test-quote-state 16 项入册;方案勘误批注。
2. **在哪一层验**:断言层(行为五形)+ 两刀红-绿;像素=店主走查(工作台横幅,请并入走查单第五新点)。
3. **同类扫尽**:报价写口只有 mark-quoted 一条(grep 钉);会话键推导单函数,状态四态单出口。
4. **用什么数据验**:双单同会话冲价形 / 过期拨表形 / 7h 豁口切段形 / 缺 conversation 的孤儿报价(守空)。
5. **重复稳定性**:套件首跑绿 → 两刀各红 → 还原绿 → 全量回归(见下)。
6. **残留与不确定**:①AI 改口注入=下批首件(分析已附);②quote-settings 的 UI(两数入口)未建 ——
   等裁放哪(建议客服工作台设置角);③会话键推导 vs 加列=实现细节替换已报明;
   ④棘轮:local-server 18,131→**18,164(+33:mark-quoted 增强+quote-settings 口+序列化针,主体在新模块 127 行;随批报,排班域二期抵扣照计)**、admin.js 8,509(±0);⑤员工 mark-quoted 权限沿既有
   assertStaffCanAccessQuote(未改口径)。

### 回归结果(补记)
- **✅ 全部 66 个套件通过,2,540 项断言全绿**(31l 的 2,524 → +16:test-quote-state 新套入册;
  清单 61→62,陪审团 I1-I9 × 90 家保持最后)。日志:handoff/回归日志_2026-09-01p_66套件.log。
  端口已还:4128/4310 双活。
