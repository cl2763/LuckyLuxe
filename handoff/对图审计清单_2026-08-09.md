# 全量对图审计清单(2026-08-09)

> 验收标准:**设计图=合同,做出来必须跟图一模一样**。
> 差异分级:**A 未做** / **B 只做一端**(图标了双端的,网页与小程序都得有)/ **C 布局交互与图不符** / **D 文案与图不符** / **✅ 一致**。
> 审计方式:逐屏读设计图原文 → 读对应实现代码(网页 `apps/web/admin.js`·`admin.html`·`sign.html`,小程序 `miniprogram/pages/merchant/*`,后端 `apps/api/local-server.mjs`)。
> 标「待复核」= 我只从代码判断、还没在沙箱里并排比过的,修的时候会补看。
>
> **本清单只审、不改**。等你过目后再动手;结构性返工那三条先给方案等你点头。

---

## 一、《结算与定金_UI设计图_2026-08-08》7 屏

| 屏 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| 屏 0 结算入口 | 小程序 | **C** | 按状态给按钮做对了,但用的是系统 ActionSheet,不是图上那张自定义面板;且缺「可撤回改单」 |
| 屏 1 结算开单 | 小程序 | **C/D** | 主体齐,但加项目录分组名不是图上的「延长类/补甲类(单指计价)/卸甲类」,CTA 少了单数 |
| 屏 2 签署页 | 小程序+网页 | **C/D** | 网页版已同构;缺「待签 1/2」角标、代付标注位置与图不同 |
| 屏 3 预约页定金 | 小程序 | ✅ | 定金卡+三要点卡+商家原文+按钮文案,与图一致(待复核) |
| 屏 4 定金规则卡 | 网页 | ✅ | 左配置右预览、auto/custom 两态都在 |
| 屏 5 AI 智能包 Tab | 网页 | ✅ | 三状态卡+场景行+变量提示+知识库入口都在 |
| 屏 6 报价试算器 | 小程序 | ✅ | 预估标记/填入报价/复制话术都在 |

### 逐条

**0-1 · 屏 0 · 小程序 · C**
- 现状:`orders/index.js` `orderActions()` 用 `wx.showActionSheet` 弹系统列表。
- 图上要求:图里是一张**自定义底部面板**——顶部一行「小红 · 简单款式3h · 进行中」的单据摘要,下面是按钮,底部「关闭」。
- 修法:换成自建弹层(与选券面板同一套 mask+sheet 样式),顶部带订单摘要行。

**0-2 · 屏 0 · 小程序 · A**
- 现状:「结算单已推送待签」时只有「查看结算单(待签 N 张)」。
- 图上要求:该状态下**可撤回改单**。
- 修法:加「撤回改单」按钮 + 后端 `POST /admin/settlements/:id/void`(只允许 pending_sign;已签一律不可撤,走更正链)。

**1-1 · 屏 1 · 小程序 · D**
- 现状:加项目录按**价目表大类**分组,组名 = 大类名(Jie'Nail 会显示「美甲简单款式/卸甲/…」)。
- 图上要求:组名是**加项类型**——「延长类」「补甲类(单指计价)」「卸甲类」「整单规则」。
- 修法:加项分组改按 `unit`/命名规则归类,或给 `service_categories` 加一个「加项组名」字段由店主配;倾向后者(数据驱动,不写死中文)。

**1-2 · 屏 1 · 小程序 · D**
- 现状:CTA = 「推送给卡主签字」。
- 图上要求:「推送给卡主签字(2 张服务单)」——带张数。
- 修法:按当前被服务者数量拼张数。

**2-1 · 屏 2 · 双端 · D**
- 现状:右上角只有「待签 / 已签署」。
- 图上要求:「待签 1/2」——同组第几张。
- 修法:`GET /settlements/:code` 增 `groupIndex/groupTotal`,前端显示。

**2-2 · 屏 2 · 双端 · C**
- 现状:代付标注是「服务明细」标题后一个 `代付` 小徽章。
- 图上要求:明细区**上方独立一行**「本单由 小美 的卡支付(代付)」。
- 修法:改成独立提示行,带卡主姓名。

---

## 二、《日结与薪资_UI设计图_2026-08-08》v6 13 屏

| 屏 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| 屏 1 日结 | 小程序 | **C(结构性)** | **日结被做成独立页 `daily-close`,图上它是「订单页·今日台面」网格下方的板块** |
| 屏 1 日结 | 网页 | **C(结构性)** | 网页把日结放进了「财务」页签,图与管理页分组图都把它归在**订单管理** |
| 屏 1b 金额更正 | 双端 | ✅ | 上只读存档单+下更正区+三点说明,两端都有 |
| 屏 2 工资试算 | 小程序 | ✅ | 日结业绩模块/黄条/锁定门槛/归属备注已删,与图一致 |
| 屏 3a 薪资方案(网页) | 网页 | **C** | **网页编辑器还是 v1 表单**:没有阶段/阶梯模式、没有首充/续卡、没有自定义行、没有开关族、没有两模式对比 |
| 屏 3b 薪资方案·阶梯 | 小程序 | **C** | 模板名是「纯提成/底薪+阶梯/底薪+固定提成」,图上是**阶段/阶梯/自定义**三段选;缺基础项开关化;缺常驻三模式对比 |
| 屏 3c 薪资方案·自定义 | 小程序 | **C** | 同上;「自定义」态在图上是独立段选,现状是模板卡片 |
| 屏 3b/3c 卡提成 | 双端 | **D(店主已抓)** | **「耗卡提成」「充值提成」两行残留**,图上只有 首充/续卡/自定义行 + 明写「耗卡不设提成」 |
| 屏 4a 排班 | 小程序 | **C(结构性,店主已抓)** | 排班页签里只有一张「计时排班 ›」卡片,**又要点一层**才进周视图;图上周视图直接铺在页签里 |
| 屏 4a-2 时段弹层 | 小程序 | ✅ | 五选一+自定义起止+同步到之后每个周 N,与图一致 |
| 屏 4b 业绩目标板块 | 小程序 | ✅ | 设置/显示两组段选 + 排行卡在上,与 V2 图一致 |
| 屏 4c 薪资/账号板块 | 小程序 | ✅ | 每人一行 + 行内按钮,与图一致 |
| 屏 4d 网页员工管理 | 网页 | ✅ | 五页签无 emoji + 业绩目标双开关,与图一致 |
| 屏 5a 员工端(含分项) | 小程序 | ✅ | 三页签 + 近 6 月柱图 + hero 分项 |
| 屏 5b 员工端(仅总进度) | 小程序 | ✅ | 整页无分项(接口层裁字段),与 v5 修正一致 |

### 逐条(结构性三条 + 残留)

**★ 2-1 · 屏 1 · 小程序 · C · 结构性返工**
- 现状:`pages/merchant/daily-close/` 独立页;管理页「日常经营」里还有一个独立的「日结」入口。
- 图上要求:日结是**今日台面页的下半部分**——顶部 pill 加「待日结 N」,技师时间网格下面接「日结 · 8月8日」板块(待分配单折叠卡 → 技师汇总表 → 异常核查 → 确认按钮)。《管理页分组》图里也写死「订单管理:今日台面 · 全部订单 · **日结**」。
- **方案(等你点头)**:把 `daily-close` 页面的渲染整体搬进 `orders/index` 今日台面模式的底部,今日台面顶部加「待日结 N」胶囊;`daily-close` 页保留但只作为「从工资试算『去日结』跳转」的落地页(或直接改成跳订单页并锚定)。管理页的独立「日结」行删掉,改到「订单管理」行的副标题里。
- 影响面:一个大页面合并,约 250 行 wxml/js 搬迁 + 两处入口调整。

**★ 2-2 · 屏 1 · 网页 · C · 结构性返工**
- 现状:网页日结在「财务 → 日结」页签。
- 图上要求:归订单管理。
- **方案(等你点头)**:网页订单页加「今日台面」视图并把日结板块挂在下面;或退一步——保留财务页签(老板在电脑上确实是在财务区做账),但在订单页顶部加「待日结 N · 去日结」直达条。**我倾向后者**,因为网页端订单页目前是列表不是技师网格,硬搬会做出一个图上没有的第三种形态。请你选 ① 完全照图搬 ② 订单页加直达条 + 财务页保留。

**★ 2-3 · 屏 4a · 小程序 · C · 结构性返工(店主已抓:排班嵌套层)**
- 现状:员工管理 → 排班页签 → 卡片「计时排班 ›」→ 再跳 `schedule-day` 页。
- 图上要求:点进员工管理默认落排班页签,**周视图直接在页签里**(按天行 + 时段胶囊 + 行尾在岗数 + 底部「本周模式应用到未来 4 周」)。图注也写明「和以前点排班一样快」。
- **方案**:把 `schedule-day` 的渲染内嵌进 staff 页 schedule 页签(共用同一份数据与弹层),`schedule-day` 页保留给深链;顺带清理另一个更老的 `pages/merchant/schedule` 页(疑似 v1 周网格,已无入口 —— 待复核后删)。

**2-4 · 屏 3b/3c + 3a · 双端 · D · 店主已抓:耗卡提成残留**
- 现状:
  - 小程序 `salary-plan/index.wxml` 第 60–70 行:「⑤ 卡提成(充值和耗卡分开配)」下有 **充值提成 / 耗卡提成** 两行,底部 hint 还写「+ 卡耗提成」。
  - 网页 `admin.js` `renderSalaryPlanEditor()`:有 **耗卡提成 % / 充值提成 %** 两个输入框。
- 图上要求:卡提成只有 **首充提成 / 续卡提成 / 自定义行(＋加一行)**,并明写「耗卡不设提成——卡耗计入业绩」。
- 修法:两端删掉 充值提成 + 耗卡提成 两行与对应字段(`cardPct`/`rechargePct` 后端已退役但仍被 UI 读写),底部 hint 改成图上那句。

**2-5 · 屏 3b/3c · 小程序 · C**
- 现状:① 选模板 = 三张卡片「纯提成/底薪+阶梯/底薪+固定提成」;阶梯里再有一个二级「阶段|阶梯」段选。
- 图上要求:顶部**一排三段选「阶段 | 阶梯 | 自定义」**,选中即切换下面那张主卡(阶梯表 / 固定提点);基础项(底薪/手工费/加班费)带**开关**;底部常驻「试算 ¥12,000:阶梯 ¥1,280 · 阶段 ¥1,440 · 自定义 ¥960」三模式对比。
- 修法:重排页面结构 —— 段选、主卡、基础项开关、卡提成、常驻试算条(调 `/admin/salary-plans/preview`)。

**2-6 · 屏 3a · 网页 · C**
- 现状:网页薪资编辑器 = v1 表单(模板下拉 + 底薪 + 手工费 + 固定提成 + 耗卡/充值提成 + 加班 + 阶梯表)。
- 图上要求:三模板三状态并列展示(阶段/阶梯/自定义各一张卡,含算例),「与阶段差 ¥160 —— 两模式对比常驻显示」。
- 修法:按 3a 图重写编辑器,与小程序 3b/3c 共用同一套字段(ladderMode / 首充 / 续卡 / 自定义行 / 开关族)。

**2-7 · 屏 1 · 小程序 · D**
- 现状:今日台面图例 =「淡色=未到 · ●进行中 · ✓完成」。
- 图上要求:图例 =「手部 / 美睫 / **已结算(斜杠底纹)**」;已结算的块要有斜杠背景 + ✓。
- 修法:补已结算态样式与图例。

**2-8 · 屏 1 · 小程序 · A**
- 现状:今日台面顶部 pill 是 今日 N 单 / 在做 N 人 / 在岗 N 人 / 空档 N h。
- 图上要求:今日 N 单 / 在岗 N 人 / **待日结 N**。
- 修法:加「待日结 N」胶囊(点它滚到日结板块)。

---

## 三、《技师业绩可视化_UI设计图_2026-08-08》v2 4 屏

| 屏 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| V1 网页技师业绩 | 网页 | ✅ | 大页签 + 排行卡(三维度×本日/本月)+ 目标进度卡,金额在条外、未设不画条 |
| V2 小程序业绩目标 | 小程序 | ✅ | 排行卡插在原设置区上方,原区位置交互不变 |
| V3 员工端 hero 进度条 | 小程序 | ✅ | 进度条 + 「还差 ¥X」,未设不显示,`gapCents` 后端给 |
| V4 网页财务密码卡 | 网页 | ✅ | 两态卡 + 关闭/改密都验旧密码 + 忘记走平台文案 |
| V4 对齐 | **小程序** | **B(店主已抓:财务密码主动索要)** | 网页已按「默认关闭」改造,**小程序财务页仍强制门禁** |

**3-1 · V4 · 小程序 · B**
- 现状:`pages/merchant/finance/index.wxml` 进页先渲染 `.gate`,未配置时显示「**首次设置财务密码**」并要求设两遍,不设进不去。
- 图上要求 / 08-08 口径:**默认关闭、商家自助**;没开启就直接进财务页,开启入口在设置里。
- 修法:开页先读 `/admin/finance/lock-status`,`enabled=false` 直接放行;门禁只在 `enabled=true` 时出现;删掉「首次设置」分支(设密码走「我的 → 财务密码」)。顺带:门禁提示里「可用启动服务器窗口里的 Owner Token 解锁」是开发期文案,商家不该看见,一并删。

---

## 四、《结算单用券_UI设计图_2026-08-09》v3 3 屏 + 规则⓪–⑥

| 屏 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| C1 结算开单券行+选券面板 | 小程序 | ✅ | 定金行下方券行、可用/置灰写原因/不使用,与图一致 |
| C2 签署页选券 | 网页(手机+桌面同页) | ✅ | 签字前可换、店员代选标注、签后锁定、快照加券行、无券不占位 |
| C2 签署页选券 | **小程序** | **B** | 小程序侧**没有独立的顾客签署页**,顾客走 `/sign/<单号>` 网页;图把 mini 与 web 分别画了两框 |
| C3 自定义发放 | 网页 | ✅ | 搜顾客→发放→原因必填→发放记录只追加,员工端 403 |
| C3 自定义发放 | **小程序** | **B** | 图注写「小程序商家端老板版同布局」,未做(上批已报挂 P3) |
| 规则 ⓪–⑥ | 后端 | ✅ | 84 条断言覆盖,含一单一张/核销时序/更正退券/审计禁删 |

**4-1 · C2 · 小程序 · B**
- 现状:顾客签署走网页 `/sign/:code`(手机浏览器打开)。
- 图上要求:图里 C2 左框标的是「顾客签署页(小程序,签字前)」。
- 修法:两条路——① 在小程序里加一个 web-view 包住 `/sign`(工作量小,但要配业务域名);② 用小程序原生页重写签署页(签名板要用 canvas,工作量大)。**建议 ①**,请你定。

---

## 五、《管理页分组调整_前后对比_2026-08-08》

| 项 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| 员工管理提到「日常经营」 | 小程序 | ✅ | 已提上去,副标题「排班 · 业绩目标 · 薪资方案 · 账号」与图一致 |
| 排班独立入口移除 | 小程序 | ✅ | 老板端已移除(员工端保留「查看本周班表」,与图注一致) |
| 「日常经营」组内条目 | 小程序 | **C** | 图上是 订单管理 / 员工管理 / 储值充值 / 客服工作台 四条;现状是 员工管理 / **日结** / 考勤打卡 / 财务 / 工资试算 / 客户库,**多了日结独立行、少了订单管理行** |

**5-1 · 管理页 · 小程序 · C**
- 修法:删「日结」独立行(并入订单管理);补「订单管理」行(副标题「今日台面 · 全部订单 · 日结」);考勤/财务/工资试算/客户库是图外条目 —— 图注说「其余条目一条不动」,所以**保留**,只调这两处。

---

## 六、《财务页升级_趋势与周期对比_设计图》

| 元素 | 端 | 级别 | 一行结论 |
|---|---|---|---|
| 趋势图(收入/支出/净赚) | 网页 | **C** | 做成了「日/周/月/年」粒度切换的柱图,图上是**近6/近12/今年/自定义**周期切换 |
| 月营收目标线 + 达标月标记 | 网页 | **A** | 未做(图:柱上一条深色目标线,达标月柱变色) |
| 净赚折线叠在柱上 | 网页 | **A** | 未做(现状只有柱 + 环比行) |
| 当月「本月至今」浅色 + partial | 网页 | **A** | 未做 —— 图里点名这是「趋势图最容易骗人的地方」 |
| 「本月至今 · 和谁比」相同天数三组 | 网页 | **A** | 未做(上月同期 / 去年同期,后端按相同天数截断) |
| 「钱花在哪了」支出结构横条 | 网页 | **A** | 未做 |
| 「收入构成变化」堆叠柱(近6完整月) | 网页 | **A** | 未做(储值消费单独一类) |
| 每张图「表格视图」切换 | 网页 | **A** | 未做(现状是图下固定明细表,不是切换) |
| 导出 CSV | 网页 | **A** | 未做 |
| 未设目标 B 态引导条 | 网页 | **A** | 未做(图:一行淡引导「就 3 项,一分钟」+ 趋势图不画目标线) |
| AI 智能总结 | 网页 | ✅ | 已有(`/admin/finance/insights`) |
| 手机财务页不加趋势图 | 小程序 | ✅ | 按纪律未加,与图一致 |

**6-1 · 财务趋势 · 网页 · C+A**
- 现状:`GET /admin/finance/trend?granularity=&periods=` + 单张柱图 + 环比 + 八列明细表。
- 图上要求:见上表 —— 这是**一整屏的重做**,不是补几个元素。
- **方案(等你点头)**:后端把 trend 端点扩成图上那一个「一次全给」的接口(每月收支净 + partial + 相同天数三组对比 + 支出按 category + 收入按服务类型/储值消费),前端按图重画四块图 + 表格视图切换 + CSV。工作量约等于一个 P2 子批。请你定**做全套**还是**先做目标线+partial+相同天数对比**这三个最要命的。

---

## 七、店主已抓的 6 条 → 对应编号

| 店主的话 | 本清单编号 | 级别 |
|---|---|---|
| 排班嵌套层 | 2-3 | C 结构性 |
| 日结独立页 | 2-1 / 2-2 / 5-1 | C 结构性 |
| 耗卡提成残留 | 2-4 | D |
| 财务密码主动索要 | 3-1 | B |
| 旧定金开关残留 | 见下 8-1 | C |
| 数据与会话缺失 | 第三步大数据(本清单不含) | — |

**8-1 · 旧定金开关残留 · 小程序 · C**
- 现状:`pages/merchant/store/index.wxml` 还有一个「**线上定金**」开关(开/关两态文案),与新的《定金与取消规则》配置(`deposit_config`,含 enabled/deductible/mode/金额/退改)重复,两处能互相打架。
- 图上要求:屏 4 把定金统一到「定金规则卡」一个入口。
- 修法:删掉门店信息页的「线上定金」开关,改成一行「定金与取消规则 ›」跳到定金规则卡(与网页同一份配置)。

**8-2 · 排班页重复 · 小程序 · C(顺手清)**
- 现状:小程序里同时存在两个排班页 —— 旧的 `pages/merchant/schedule/`(v1 周网格)与新的 `pages/merchant/schedule-day/`(屏 4a 按天行)。
  员工管理页签跳的是新页;但**首页「我的排班」行仍跳旧页**(`home/index.js goSchedule()`),而且那一行还挂着 📅 emoji ——
  P2③ 定的「页签及各处按钮 📅📈📝💰 一并移除」没清干净。
- 图上要求:排班只有一套(屏 4a);无 emoji 规范。
- 修法:首页那行改指 `schedule-day`(员工端只读形态)、去掉 📅;旧 `schedule` 页确认无其他入口后删除并从 `app.json` 摘掉。

---

## 八、汇总

- 审了 **6 份图 / 33 屏**。
- ✅ 一致:**18 屏**
- **A 未做:9 项**(集中在财务趋势 8 项 + 屏 0 撤回改单)
- **B 只做一端:3 项**(小程序签署页、小程序自定义发放、小程序财务密码口径)
- **C 布局交互不符:10 项**(其中 **3 项结构性返工**:小程序日结归位、小程序排班内嵌、网页财务趋势重做;另 网页日结归位待你选方案)
- **D 文案不符:5 项**

### 需要你先拍板的四件事
1. **网页日结**:① 完全照图搬进订单页 ② 订单页加「待日结 N · 去日结」直达条、财务页保留(我倾向 ②)。
2. **财务趋势**:做全套(≈ 一个子批)还是先做 目标线 + 本月至今 partial + 相同天数对比 三项。
3. **小程序顾客签署页**:web-view 包 `/sign`(快)还是原生重写(慢但纯原生体验)。
4. **加项目录分组**:改成图上的「延长类/补甲类/卸甲类」需要给大类加一个「加项组名」配置项 —— 由你在价目表里填,还是我按项目名规则自动归类?

拍板后我按清单逐条修,每屏在沙箱/浏览器里与设计图并排比过再打勾,然后再灌大规模演示数据。
