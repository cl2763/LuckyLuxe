# 上线批清单 —— 逐条现状(店主 05y §四 要的那份;**这一批只出清单,不动生产**)

> 现状是我**现查的**(命令写在每条后面),不是从记忆里抄的。
> 口径:🟢 = 已经就绪 · 🟡 = 要做一件事 · 🔴 = 必须店主本人做 / 必须单独批准

| # | 事 | 现状 | 谁来做 | 现查依据 |
|---|---|---|---|---|
| 1 | **推 main** | 🟡 本地领先 `origin/main` **194 个提交**(全是这条线的活) | Code 推,但**要店主先批**(停线:不推 main) | `git rev-list --count origin/main..HEAD` |
| 2 | 推前**手动备份一次生产库** | 🔴 没做(这一批不碰生产) | 店主批准后 Code 做,路径写进回报 | 生产部署纪律 ②-② |
| 3 | 自托管 **Fraunces woff2** + 域名白名单 | 🟡 小程序端**已经内嵌 base64 子集**(2,748 字节)不依赖域名;`wx.loadFontFace` 那条路仍要 https + 白名单 | 上线时把域名填进 `miniprogram/utils/numfont.js` 的 `FRAUNCES_URL` | 该文件抬头写着三条路的实测结论 |
| 4 | **访客身份串服务端签发** | 🔴 现在仍是 `guestIdUnsigned: true` | 后端改签发 + 校验,单独一批 | `curl -s localhost:4310/health` 里那个字段 |
| 5 | 生产 `ALLOW_DEMO_ADMIN_LOGIN` **必须未设** | 🟡 本机沙箱是 `true`(起沙箱脚本里显式给的);生产要确认未设 | 上线前 `railway variables` 只读核对 | `apps/api/start-sandbox.sh:81` |
| 6 | 两家真店密码**由店主亲手设** | 🔴 必须店主本人 | 店主(走平台后台「重置老板密码」那条唯一合法路径) | 脚本红线① |
| 7 | 31 张表**去 DEFAULT 随部署** | 🟡 未逐表核 | 上线批列表逐张核,`test-schema-consistency` 兜底 | 待办沿用 05y §四 |
| 8 | 生产**会话唯一索引** | 🟡 本机已有 `(tenant_id, provider, external_user_id)`(启动日志 D132 那行);生产要现测 | 上线后只读核验 | `/tmp/ll-sandbox-4310.log` 第 1 行 |
| 9 | 企微每店先填 `open_kfid` | 🔴 每家店一条,要真值 | 店主/运营 | 沿用 05y §四 |
| 10 | 版本指纹灌 **git 提交号** | 🟡 现在前端版本是手写串(这一批是 `20260909b-contrast`) | 上线批改成构建时注入 `git rev-parse --short HEAD` | `apps/web/admin.js:3` |

## 这一批新增、也该进上线批的两条

| # | 事 | 为什么 |
|---|---|---|
| 11 | **对比度乙档 489 处**(AA 欠账) | 多半是合同图那几个品牌色本身(奶白压金按钮 2.32:1 / 品牌金压白底 3.16:1)。改它 = 改合同图,**请店主裁**:改图、还是接受 AA 不达标并写进设计说明 |
| 12 | **小程序 wxss 还有 2,731 处写死色** | 站内两档现在**全站页面都挂上了**(66/67),但页面内部那些写死色在深色档下仍会露白。棘轮只许降;要清零得排一批 |
