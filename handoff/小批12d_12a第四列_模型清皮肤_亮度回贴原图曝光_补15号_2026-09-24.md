# 小批 12d —— 12a 第四条路:模型只管皮肤,曝光交给「亮度回贴」;补 15 号

前置:12c 回执已收、已核。停线不变:不碰 `apps/`、不推 main、不碰生产、不看密钥、`env/ark.env` 钥匙不进日志。
产出夹仍是 `31o_修图验证_2026-09-22/`。

## §零 对 12c 回执的核验(我核的,不是转述)

- 结论 1「色温句 5/5 有效」:我按 round3/ 五张自量 R/B,与你报的一致 —— **认**。
- 结论 2「提亮句不受控」:23 号 88.7→188.0 我自量 mean 188.2 —— **认**。这是本轮最重要的一句:**口令这条路对曝光已经证伪,不再往口令里加提亮相关的字**。
- ⑦-3 废断言自咬、⑦-4 超时改法(不是重试到成功)、⑦-5 15 号不重试第三次 —— 都对,记功。
- ⑤「店主偏好不能推出接受提亮」—— 对,而且我第 55 笔就是这个的另一面。
- 「不用修」命中 44% 我**不改门槛**:5 张探针拟合不出判据,18 张更不该拿数字定,见 §三。

## §一 我做的零成本实验(店主机上,Pillow,不经模型,已在 `round3_lumamatch/`)

把第三轮模型出图的 **Y 通道直方图回贴到原图**(Cb/Cr 不动),意思是:模型只留它擅长的「清皮肤/去反光/去杂」,曝光与明暗分布**逐像素还给原图**。

| 号 | 原图 mean / 死白 / p95 / R:B | 第三轮模型 | 模型 + 亮度回贴 |
|---|---|---|---|
| 23 | 88.7 / 0.19% / 151 / 1.18 | 188.2 / 26.57% / 245 / 1.18 | **87.3 / 0.19% / 151.3 / 1.184** |
| 02 | 77.5 / 0.01% / — / — | 81.9 / 0.10% | **77.5 / 0.01%** |
| 18 | 32.1 / — / — / 1.27 | 73.2 / — / — / 1.273 | 30.1 / — / — / **1.803 ⚠️** |

- 23、02:三项指标**回到原图**,肉眼上就是「原图的光,模型的皮肤」。三联图 `round3_lumamatch/_三联_亮度回贴.jpg`(23/18/02 × 原图|模型|回贴)。
- 18 号副作用:亮度压回去而色度没跟着缩,R:B 跳到 1.80,**过暖**。这是我只回 Y 不回 C 的直接后果,不是模型的错。
- 这一列成本 **¥0**,每张 < 1 秒。它和「不经模型」那列的区别:不经模型没有清皮肤;这一列有。

## §二 做(全部零模型成本,除 15 号)

1. **对比包加第五列「模型 + 亮度回贴」**,对第三轮 5 张探针(+15 号若补成)各出**两个变体**,店主眼判:
   - `LM-Y_*`:只回 Y(我上面的做法,函数见下)。
   - `LM-YC_*`:Y 回贴后,**Cb/Cr 也按原图直方图回贴**(同一函数分别对三个通道跑),看 18 号过暖是否消失、皮肤清理是否还在。
   - 每张 LM 图旁标四个数(mean / 死白 / p95 / R:B)与**原图的差值**;差值本身不是判据,是给她看的。
   - 缩略图点开原文件;人脸手以上裁掉的规矩照旧(输入已是归一化裁片,不会再露)。
2. **15 号补跑一次**(桶 B,¥0.32,已批);仍失败就记 `fetch failed` 收工,**不第三次**。
3. **「不用修」的 18 张**:出一张表(文件名 + mean + 死白 + p95 + R:B),**不改门槛、不删张**;门槛由店主看完表再定。
4. **不动**:三桶口令、`bucket-prompts.mjs`、`apps/`、12b 文档(等她定了再改)。
5. 回执格式:一句一因;先贴磁盘计数 ↔ 日志对账,再贴表;累计花费到分。

回贴函数(与我用的一致,单通道;`LM-YC` 对 Y/Cb/Cr 各调一次):
```python
def match_hist(s_ch, r_ch):  # s_ch: 模型出图某通道 float 数组; r_ch: 原图同尺寸同通道
    s_vals, s_idx, s_cnt = np.unique(s_ch.ravel(), return_inverse=True, return_counts=True)
    r_vals, r_cnt = np.unique(r_ch.ravel(), return_counts=True)
    s_q = np.cumsum(s_cnt) / s_ch.size; r_q = np.cumsum(r_cnt) / r_ch.size
    return np.interp(s_q, r_q, r_vals)[s_idx].reshape(s_ch.shape)
```
(YCbCr 由 Pillow `convert('YCbCr')` 得;原图 `resize` 到出图尺寸再取通道。)

## §三 判据(机器查得了的;全中即已批,J-113)
- 对比包行数 = round3/ 出图数(含 15 号若成),与跑批日志对账,差额报红退出 1(沿用 12c 那把刀)。
- 每张 `LM-Y` 的 Y 通道 p50 与原图 p50 之差 ≤ 2(回贴没回对就红)。
- 每张 `LM-YC` 的 R:B 与原图 R:B 之差 ≤ 0.05。
- `apps/` 改动文件数 = 0;`git status` 只见 `tools/ai-retouch-eval/` 与产出夹。
- 累计花费 ≤ ¥48.32 + 0(15 号一次)。

## §四 店主待定(她看完对比包再答,不由 Code 猜)
1. 桶 A 三张不提亮的样子要不要(12c ⑨-1)。
2. 「不经模型」够不够(12c ⑨-2)—— 现在多了第五列一起比。
3. 「不用修」门槛 44% 是否偏松,看 §二-3 那张表定。
