# 小批05s 补二 — `bc675a2` **收,D160 关帐**、两处补件亲测通过 · 🔴 **D163:回归「还回去」把 `MERGE_WINDOW_MS=0` 漏进了 4128 / 4310**——现在两台活服务的合并窗都是 0,店主亲测通五永远合不到 · 待裁 #10 裁:三件同做、可约只许来自 `/availability` · 然后六通 v5

> 2026-09-08 晚 · Cowork 现查:`bc675a2` 13 文件;108 套 3,647;北京店亲测「最便宜的美甲多少钱」→ 券 + 原价 + 券后 + 时长 + 「其次是猫眼渐变 ¥398」`fixed_price_direct` ✅,尾巴 0 问句 ✅;「猫眼渐变多少钱」→ 「开业礼 · 满 300 减 50」正确命中、精致单色(298)正确**不**命中 ✅。**但**同一访客三句间隔 1 秒 → 出了三条回复;`/health.mergeWindowSeconds = 0`;`ps eww` 见 4310 进程环境 `MERGE_WINDOW_MS=0`,`run-all-tests.sh:43` `export MERGE_WINDOW_MS=0` 后「还回去」的重启继承了它 · 两处各一份

## 一、收

- 「最便宜/最贵 + 大类词 → 在该大类 fixed 项目里挑,quote 不许被挑」——收,亲测对。
- 价格答句后不拼采集问句改「认形状」+ 全半角标点转义写死 + 两条专守——收;「差两个字整条守形同虚设」这种坑,判据里以后**用真模板句当夹具**,不手写。
- D160 三档口径、只答得上一部分不许短路、夹具自造三条 FAQ 再删——**收,关帐**;「验机制不验巧合」这句写进口径。
- 「带 `.pre-k` 时整轮跑不动,造病直接跑那一套」——收进口径。
- 六通 v5 不跑缺件样本——对。

## 二、🔴 D163 · 回归收尾把窗置 0 漏进了活服务(先修,≤ 30 分)

现状:回归脚本 `export MERGE_WINDOW_MS=0`(为了回归不等窗),跑完「还回去」重启 4128/4310 时**这个环境变量跟着进去了**——两台活服务的合并窗现在是 0。后果:店主在模拟器/小程序连发三句会得到三条回复,D151 等于没上线;而且回执「4128/4310 都还回去了」在窗这件事上不成立。

裁:
1. 「还回去」的重启**只带开跑前抓下来的那份环境**(脚本第 88 行已经在抓 `DATA_DIR`,同法抓整份 env 或显式 `unset MERGE_WINDOW_MS`),回归专用变量一个都不许漏;
2. 判据(进回归收尾自证):跑完后 `curl 4128/health` 与 `4310/health` 的 `mergeWindowSeconds` **必须等于 4**(默认值),`mergeWindowCapSeconds` = 12;不等 → 收尾红,回执不许写「还回去了」;
3. 造病:故意漏 `MERGE_WINDOW_MS=0` → 收尾红;
4. 修完把 4128/4310 各重启一次,回执贴两台 `/health` 三个数。
5. 同族扫一遍(点名处必扫同族律):回归里 `export` 的其它变量(`AI_REQUIRE_REAL`、`ALLOW_DEMO_ADMIN_LOGIN`、`DATA_DIR` 之外还有什么)是否也漏进过活服务——列一张表,漏了的同修。

## 三、待裁 #10 · 裁:三件同做,一个中断口

同意「同一个机制一起做」。裁:
1. 报价采集里加**一个**中断口:顾客句被 `classifyTurn` 判为 优惠 / 可约 / 时长 / 价格 四类之一 → 先答 → 再把**待答的那句采集问句原样接回去**(「那我们接着说,本甲还是延长?」);
2. **可约**只许 `getAvailability` 出真时段——没位就说没位并给最近两个有位时段;**一个字都不许编**(05q 原文);优惠接 `discountFacts`;时长接 `services.base_duration_min`;价格已由 fixed 出口先于采集处理;
3. 判据(北京店多轮):采集第二问时插「明天下午三点有位吗」→ 回复含 `/availability` 返回的时段或「没位」+ 两个替代 + 采集问句原样接回;插「有优惠吗」→ 券名 + 采集问句;插「要多久」→ 分钟数 + 采集问句;造病:中断口绕开 `getAvailability` 编时段 → 红;中断答完没接回采集问句 → 红;
4. 那 21 个套件是「绝不破」——改前后各跑一次,断言只增不减;
5. 通三定金句 ≤120:「定金 ¥50(到店付,不抵尾款);提前 24 小时可全退,不足扣一半,爽约不退。要我先帮您留着吗?」细则第二句只在追问时出。

## 四、然后六通 v5

同脚本;通五三句间隔 1 秒(**在窗 = 4 的活服务上跑**,D163 修完);通六 `fixed` 项目问价 + 「最便宜的」各一句;文件 `05q_像人六通_原文_v5_2026-09-08.md` 落仓,新会话,`ranOn`。出来我发店主打第二轮分。

## 五、交齐

D163 三判据 + 造病 + 两台 `/health` 三数 + 同族表 · 中断口三判据 + 两造病 + 21 套件前后 · 通三句 · v5 路径 · 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 未推 main。
