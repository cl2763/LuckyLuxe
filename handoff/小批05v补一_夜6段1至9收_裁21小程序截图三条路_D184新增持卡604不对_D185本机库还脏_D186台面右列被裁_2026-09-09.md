# 小批05v 补一 — 夜 6 段 1–9 **收**(段 10/11 如实没做,顺延)· 裁 **#21**:小程序截图**先试第三条路**再认「交不出来」· 新查出三条:**D184 新增持卡 604 是错的** · **D185 4128 本机库的演示面还是脏的** · **D186 台面分栏后右边那列被裁掉**

> 2026-09-09 04:23 CST 醒来第 1 次 · Cowork 亲查(只读):两台 `/health` 都在(4310→`apps/api/sandbox-data/lucky-luxe.sqlite`,4128→`apps/api/local-data/lucky-luxe.sqlite`,窗 4 封顶 12);提交到 `5d07e6f`;18 张截图在;`tools/mp-layout-proof.mjs`、`tools/db-backup.mjs`、`handoff/night-runs/05t回执_2026-09-09.md` **都在**(J-27 过) · 两处各一份

## 一、收(逐条自己查过,不是照抄回执)

| 段 | 我怎么核的 | 结论 |
|---|---|---|
| 1 D170 | 直接读沙箱库:三家店 `quote_requests PENDING_STAFF` 各 **2**(今天的),夹具名 **0** 位;写库自报写了**绝对路径 + 备份文件名** | **收,关帐**(带 §四 D185) |
| 2 D171 | 开图 `段2_D171_台面_luvia-bj_深色.png` / `段3_J36_luvia-bj_dark.png`:交叠的单真并排了,叠字没了 | **收**(带 §四 D186) |
| 3 D172 / D183 / J-36 | 开图 `段3_J36_lucky-luxe_dark.png`:侧栏、台面、四小牌、右列待办**全深**,金色 Fraunces 大数字在,文字不重叠 | **收,D172 关帐** |
| 8 D177 | 仓里 `ROTATE_MS` / `rotateAt` / `dots(` 在 `apps/web/dashboard-home.js` 各 **0 处** | **收,关帐** |
| 收尾 | 棘轮 `local-server.mjs` **17,708** · `admin.js` **7,928**(自己 `wc -l` 量的,未升);`d166-probe` 仓里 **0 处**;断言基线 `dashboard-home 81→94`、`mp-home-owner 84→122` | **收** |

三件「刀自己错了」和三个「顺手咬出来的真问题」——**收其诚实**,尤其「币符扫描咬到我自己注释里引的店主原话」那条:判据没错,错的是注释,这个处理方式对。

## 二、裁 **#21** · 小程序截图:**先试第三条路,试完再认「交不出来」**

倾向我基本同意,但**不能就此把小程序端的图这一项永久取消**——店主是**用眼睛**看小程序的,`图=合同`这条不能只靠数字兑现。按序三条:

1. **先上纪律(不是验收)**:`mp.screenshot()` 一律包 **20 秒超时**,超时算「本次没拍到」并点名,**不许再吊死整轮**(D167 同族)。判据:人为让 screenshot 卡住 → 该套 20 秒内报「未拍到」、整轮其余套照跑;造病:去掉超时 → 看门狗 300 秒自杀 → 红。
2. **第三条路,这一批必须试一次**:绕开 automator,用 macOS **`screencapture -l <windowid>`** 直接拍**微信开发者工具模拟器那个窗口**(窗口 id 从 `CGWindowListCopyWindowInfo` / `osascript` 取)。拍到就用它交图,文件名照 `06截图` 的规矩落 `handoff/night-runs/07截图/`。
3. **三条都不成**,才准写「小程序截图:这台机器交不出来」,并且回执里要**逐条列出试过哪三条、各自卡在哪**;验收改成 `tools/mp-layout-proof.mjs` 的布局实测 + 画布像素数。**照旧不许拿 DOM 冒充图**(J-32)。

**#20 已自解**,同意留档:`flex` 子项的 `width` 会被当成可压缩的建议值,`flex:none` + `min-width` 是修法——这条写进口径,以后遇到「样式写了量不到」先想它。

## 三、🔴 D184 · 「新增持卡 604」这个数是错的(这一批第一件事)

**证据(我自己查的,两头对不上)**:
- 图:`段3_J36_lucky-luxe_dark.png`,「今日 · 新增持卡 **604**」(旗舰店,当天 9 月 8 日,币 CAD);
- 库:同一个库 `apps/api/sandbox-data/lucky-luxe.sqlite`,拿 `dashboard-pulse.mjs:122 newCardCount()` **原样那段 SQL** 查 `lucky-luxe` / `2026-09-08` → **1**;
- 对照组:北京店同一张图写 **2**,库里查出来也是 **2**——**对得上**。所以不是口径整体错,是**旗舰店这一路**出了岔。

**裁**:口径不动(「当期首次开卡的顾客,按顾客去重」是对的),去查**从 SQL 到屏幕这一路**哪一段把 1 变成了 604——嫌疑顺序:① 绑参(`get(tid, tid, from, to)` 四个占位符有没有错位)② `spark`/`prev` 的值被写进了 `value` ③ 前端把别的数画进了这张牌。

**判据**:同一时刻,`/admin/dashboard/pulse?period=today` 里 `newCard.value` **必须等于**直接跑 `newCardCount()` 那段 SQL 的结果——**三家店 × 四个周期(今日/本周/本月/本年)12 组全等**,不等就红并**打印两个数**。造病:把绑参故意错位一位 → 12 组里至少一组红并点名。
**顺带**:J-35 那条同族——这张牌拿不到数时出「—」不出 0。

## 四、另外两条(都是我今晚新查出来的)

**D185 · 4128 本机库的演示面还是脏的**(店主一开 4128 就会又看见夹具名)
库 `apps/api/local-data/lucky-luxe.sqlite`:`lucky-luxe` 的 `quote_requests PENDING_STAFF` **68 条**、夹具名顾客 **26 位**;`jics-nail` 夹具名 **12 位**、PENDING_STAFF **1 条**。段 1 只清了沙箱库——**这不是失职**,是我立的停线(「写 4128 既有行停」)拦住的,做得对。
**两步走,这一批只做第一步**:
- **只做只读清单**:把这 68 条 + 38 位逐条列进 `handoff/night-runs/D185_4128待清单_2026-09-09.md`——顾客要带**挂着的单数 / 结算笔数 / 储值余额**(用来证明这些真是夹具、不是加拿大店的真顾客),报价请求带日期;同时给出**将要执行的 SQL 原文**和 `tools/db-backup.mjs` 的备份命令。
- **一个字都不许写 4128**。我看完清单再批第二步。批之前谁也不许「顺手清一下」。

**D186 · 台面分栏之后,右边那列被裁掉了**
图 `段3_J36_luvia-bj_dark.png`:棠棠那条泳道并排了 **5 张卡**,把知夏那列挤到屏幕外,右边缘露出半张「12…/罗…」。叠字是治好了,但**看不全**是新病。
**裁**:并排分栏之后,**整块台面不许出现横向被裁的卡**——列宽到下限就让整块台面横向可滚(带看得见的滚动提示),或者把同一泳道折叠成「+N 单」可展开,两种任选,但**结果必须是「要么看得全,要么看得出还有」**。
**判据**:三店 × 浅深,量每张卡的包围盒右边 ≤ 台面容器右边(允许 1px 误差),超出即红并点名是哪张卡;造病:把列宽下限拿掉 → 红。**要图**(J-32):至少 `luvia-bj` 深色一张。

## 五、记我一笔

我上一轮说「北京店新增持卡 **33**」——**错了**。实图是 **2**,库里也是 **2**,对得上。604 只有旗舰店一家。我把**没核实的数字**当成了发现报出去,这跟我要求 Code 的标准是一个标准,记一笔。

## 六、顺序与交齐

**D184(§三)→ #21 三条路(§二)→ D186(§四)→ D185 只读清单(§四)→ 段 10/11(D173 / D174 / D176 三条口径 + 六通 v6 + 三端截图对照表)。**

交齐:D184 判据 12 组 + 造病 + 两个数打印 · #21 超时判据 + 造病 + 第三条路试没试到写清楚 · D186 判据 + 造病 + 至少一张图 · D185 清单路径(**0 处写库**)· 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 写库自报(库绝对路径)· 未推 main。

**停线照旧**:写 4128 既有行停 · 四红线修法不明停 · 沙箱模型 >600 万 token/夜停 · 回归红 30 分钟定位不到停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询。
