# A3 施工令 · 交付(2026-08-25)

**真库一行没写,生产一行没写。** 42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-25_A3施工批.txt`

## 棘轮(wc -l 现测)

```
apps/api/local-server.mjs   18,708 → 18,687   (−21:资产总页出口与 assetsOverviewOf 退役)
apps/web/admin.js            8,968 →  8,968   (不变)
apps/web/customer.js         2,815 →  2,786   (−29:assets/coupons 链条退役)
```
新模块 `apps/api/store-directory.mjs` 58 行(已 git add)。

---

## ❌ 品牌层级:一行没动

已结案,不碰。以后网页新页面照基准图那张六行表落位。

---

## ① 门店列表:排序 + 标记(**不是过滤**)

先读后动:`openStoreSwitcher()` 那套机制(列 `/shops` 全部公开门店、与小程序 shop-select 同源、
`?store=` 整页重载)**一行没动**。

### 【门店列表三个一】落在哪

| 一 | 落点 |
|---|---|
| **一个数据源** | `/shops` 那条 SQL,全部公开门店。路由里**零过滤**(断言扫 `shops.filter`) |
| **一个排序器** | `apps/api/store-directory.mjs` 的 `sortShops()`。以后加"距离近的优先"往这加一个维度 |
| **一处标记** | 同模块 `markJoined()`。前端只渲染 `shop.joined`,不自己判会员关系 |

**为什么放后端**:两端读同一条 `/shops`,排序与标记做在这里两端天然一致,不需要各写一份(四之九)。
**小程序 shop-select 一行没改** —— 它读同一条接口,顺序与标记自动跟上。

> ⚠️ 一个连带效果先说清楚:小程序选店页的**顺序**也会跟着变成"已入会置顶"(它读的是同一条接口)。
> 这与两端口径一致的原则相符,但确实改变了小程序的显示顺序,请知悉;不想要的话在排序器里加个开关即可。

### 断言(真跑取值)

| 要求 | 结果 |
|---|---|
| 已入会的店排在未入会之前 | 真跑排序器:`[a,b,c(会员),d]` → `c,a,b,d` ✅ 组内保持原顺序(稳定排序) |
| 列表长度 == `/shops` 长度 | 真跑:未登录 4 家 / 登录后 4 家,**长度一致**(没有过滤混进来)✅ |
| 🔴 串号 | 见下 |

**沙箱真跑**(同一微信身份 `demo-openid-crossshop-a-zhen`):
```
未登录: [lucky-luxe, demo-lucky-luxe, jics-store, jics-sandbox]
登录后: [demo-lucky-luxe(会员), jics-sandbox(会员), lucky-luxe, jics-store]
```

### 🔴 串号走查(切换后逐项属于新租户)

| | Lucky Luxe(演示) | 小婕的店(演示) |
|---|---|---|
| users.id | `user_mt5lql2e_g4zwug` | `user_mt5lu99a_blu22q` |
| 余额 | 888.00 | 366.00 |
| 积分 | 710 | 1500 |
| 累计消费 | 1110.00 | 1500.00 |
| 卡包(次卡/券) | 1 / 3 | 1 / 3 |
| 订单 | 5 单,**归属全是 demo-lucky-luxe** | 6 单,**归属全是 jics-sandbox** |

一项都没留在上一家。另外加了一条断言:**门店列表本身不泄露跨店资产** ——
`joined` 只回一个布尔(有没有档案),不带任何余额/积分/订单数字。

---

## ② 资产层收敛

网页收成与小程序同构:**卡包(券+次卡)/ 储值 / 积分 / 商城**。

**删干净(照礼品卡退役那次的标准)**:
- `assets` 资产总览聚合页:视图分支 / `renderAssetsWeb` / `renderAssetsWebRetired` / `loadAssets` / `state.assets` / i18n / 合法视图集合
- `coupons` 券独立页:同上
- **后端半条链同批收**:`/my/assets` 与 `assetsOverviewOf()` 退役(小程序在裁定A 那轮就退了这两页,
  网页是最后一处 —— 留着就是半条链);小程序 `getAssets()` 与它的导出一并删。

### 🔴 连带保留:黑卡上的数字仍然能点开

这正是我上次判错的地方,这次先把它写成断言再动手:
`data-me-target` 三格 **pointsMall / cardPack / storedValue 一个都没少**。
并且把网页黑卡第三格的 key 从 `cardPack` 改回 `storedValue`(渲染仍落卡包页的储值行,
网页暂无独立储值页)—— 这样**两端入口 key 数组逐项相同**:

```
web : [pointsMall, cardPack, storedValue]
mini: [goPoints→pointsMall, goCardPack→cardPack, goStored→storedValue]
逐项相同 ✅(两个源文件各自取,再逐项比)
```

---

## 自己抓到的坑(四条)

1. **裁定A 的四条老断言会崩**:它们比的是 `/my/assets` 各行 ≡ 明细页,而那个口这次退役了。
   我没删断言 —— 那条口径("同一个数在多处必须一致")仍然成立,只是没有总页可比了,
   改成**黑卡三格 ≡ 各自明细页**(卡包角标 ≡ 页内张数、储值格 ≡ 卡包储值行、积分格 ≡ 积分页余额)。
2. **门禁断言差点被顺手删掉**:原来那条验的是"资产口未登录 401",口退役后它报 404。
   退役一个口不等于放松门禁 —— 改指仍在的卡包口,并**补一条退役自证**(`/my/assets` 必须 404)。
3. **`couponCount` 那条永久律的锚点没了**(`couponRowCount` 原来住在 `assetsOverviewOf` 里)。
   保留并**扩大**负向那半(后端 + 网页 + 小程序三处都扫恒为 0 的假字段不许复活)。
4. **㋙D 护栏第三次立功**:`store-directory.mjs` 又忘了 `git add`,断言当场点名。
