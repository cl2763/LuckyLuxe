# 小批 12n 补三(修图对话)—— 补标到四档各 ≥5;两条候选离线评估(不启用);标注入 git 只入小文件

## §一 收
23 张 124 片、机器 vs 人眼 20/23、分档图三结论(C 25 位置对 / 浅优先不能取消 / 23 与 65 在 (Y,C) 上是同一点 → 信息不够不是门槛差几分)—— 收。三个门槛与顺序没动、两条候选未启用 —— 对。

## §二 补标:批
从剩余 65 张里**人眼挑**「深·不鲜明」至少 1 张、「纯黑」至少 4 张补标,到四档各 ≥ 5。同一标法(100% 裁片、宽度先收、重叠护栏、单张满尺寸)。挑图理由逐张写一句。¥0。

## §三 两条候选:现在可以**离线评估**,仍不启用
23 张齐了,按 12n 补二写死的验收四条(42/43 浅 · 53 深鲜明 · 52/53 同档 · 02 纯黑)**加两条**(65 混色张按店主口径应走腿 A · 同套甲 60/61 仍同档),在补标后的全集上跑:
- 候选 A「离肤色的色相距离」替代 C 判鲜明;
- 候选 B「逐片判档,整张只有过半为深·不鲜明才走 A′」;
- A+B 组合。
出一张表:三种 × 六条验收 × 与人眼一致数。**只报不改 `nail-tier.json`**;启用哪条由店主看表定。「浅」的 Y 门槛若要挪,也在同一张表里给出「挪到多少、会改变哪几张的判定」,不许单独调。

## §四 标注入 git:批本地提交,不推
- 入:`tools/segment/annotations/*.json`、`anno.py`、`nail-tier.json`、`同套甲对照表.json`、说明文档。
- 不入:venv、weights、裁片夹、任何照片。先写 `tools/segment/.gitignore` 把这三类挡住,再 add。
- **只提交自己的路径**:`git commit -- <路径>`(主线对话在同一仓库并行改 apps/,不许用 `git add -A` / `git commit -a`);遇到 `index.lock` 不许删,等几秒重试。
- 提交前 `git diff --cached --stat` 贴出来,确认零照片、零 apps/。不推 main(推由主线那批统一等我批)。
