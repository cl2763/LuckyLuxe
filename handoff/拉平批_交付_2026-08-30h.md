# 拉平批交付:七准补条 + O-id1 取证 + RFM 接回 + 排班域搬家 + 两清单 + 镜像店造景实拍(2026-08-30h)

> 安全保证:**生产库未动(只读取证除外,railway ssh + readOnly)· 本机库未动(只读现测除外)**。
> 沙箱=造景与实拍(动前快照已留)。未推 main。做完不说可以关帐。
> 回执顺序照令:七准判据补条+账目一行 → 身份域取证 → 拉平批 1→2→3(清单)→4(名单)→5。

---

## 〇 · 账目一行 + 七准补条

**账目**:test-notify-scheduler 最终数=**36 项**(回执早稿「28」是排期估数、总结「34」是当时现跑数,
之后 +2=⑥b 默认面块;下批起回执一律引套件现跑数,不再手写估数)。

**补条判据(⑥b)**:「扫描类默认关」入行为断言 —— 新店塞一个**今天生日**的顾客(最可能露馅形),
tick 后通知域扫描任务必须为 0 且四类读口全 enabled=false。
**挨两刀**:刀 a(默认改开)→ 在①读口层就红;刀 b(**扫描不看开关照跑**,读口仍答「关」)→
①照绿、⑥b 红 scanTasks=1 —— 正是⑥b 独有的盲区(判据律:缺陷存在时旧判据照样绿)。还原逐字节校验回绿。

## 一 · O-id1 身份域取证(只读,只报不改)

全文:核验截图_2026-08-30/O-id1_身份域取证_2026-08-30h.md。要点:
- users 表 7 个写入位逐个查:**真正无租户上下文的只有两条 demo 闸内路由**
  (`/auth/email/register`、`/auth/google/demo`),**生产 403 关死**;
- 计数(同组查询双库跑):旗舰行而在别店有预约 —— 本机库 **1**(demo-cust-01 演示噪音)、
  生产库 **1**(微信客,首登旗舰后在小婕店下单=一人多店合法形,**不属静默回落族**);
- 上线前硬门槛两问已列:演示邮箱登录移除时 registerEmailUser 是否随退;跨店客各域按哪个店读。

## 二 · 拉平批

### 1 RFM 接回网页(店主已裁)

- 名单动作:分层条四芯片可点筛名单(再点取消),沉睡 S 名单行自带 AI 召回;
- 阈值微调:⚙ 编辑器五数(高价值 天/次/元 + 新客天 + 沉睡天),读写**既有** `GET/PUT /admin/segment-rules`
  (与小程序同两口,零新后端);金额框用 text+inputmode(不碰 type=number 两套白名单);
- 死口句「名单动作与阈值微调在小程序客户库」随功能删,**crossend-cta 白名单 6→5(棘轮只减)**;
- 跨端断言入 test-cross-end-effect ④b(9→13 项):PUT→GET 逐字段钉 + 两端渲染链 + 死口句零残留;
- 实拍:镜像店 A11/B17/N12/S5 芯片、沉睡筛出恰 5 人、编辑器展示服务端值。

### 2 排班域搬家(纯迁移 + A3 尸清)

- schedule-week/day 两路由 **141 行逐字搬**进新模块 `schedule-board.mjs`(161 行),原地换 2 行分发;
- **搬前后逐字节对比**(搬前整树起 4602 / 搬后起 4603,同库同分钟):四形全等
  (今天 2 单含到店态 1,269B / 周网格带计数 1,107B / 未来休息日空形 / 缺参形)——
  证据:核验截图_2026-08-30/排班域搬家_字节对比_2026-08-30h.md;
- 连带 A3 尸清:两端网格兜底 `|| '10:00'/'19:00'` 删(死代码:无时段必走墙/休息条),
  套件反向守常驻(today-board 27→28);
- **棘轮:local-server 18,328 → 18,194(−134;+14/+17/+23=54 行欠账全抵,净余 −80)**;
  admin.js 8,484 → **8,482(−2)** —— 本批两大文件**双降**,搬后基线报此两数。

### 3 员工端等同剩余清单(只列,待核对)

handoff/员工端等同剩余清单_2026-08-30h.md —— 14 条 × 两端现状(全部现码机核重数,不抄旧表)×
处置建议,分四组:员工端正主 5(我的业绩/补偿自查+死口并案/工作台三件/打卡豁免/staff-visibility 请裁)、
编辑口归位 3(充值/次卡套餐两端都在调 → 逐屏对编辑动作;次卡设置已归位)、
配置类 3(booking-rules、schedule-settings 分叉债、store-wifi 请裁)、查看类 3。**核对后动工。**

### 4 死口候刀 6 条名单(只报,点头后删)

handoff/死口候刀名单_2026-08-30h.md —— `recharge-tiers`×3(12655 起一块,连带 12490/12499 闸引用)
+ `merchant-leads`×2(13733/13737,平台位面已取代)+ `finance/change-password`(14062,lock-settings 取代);
每条带路由首行原文与取代证据;`stored-value/consume` 墓碑明确**不在刀下**。

### 5 镜像店大走查造景 + 店主视角实拍(当天)

全文:核验截图_2026-08-30/镜像店大走查造景与实拍_2026-08-30h.md。
- 景(两店各一份):今天预约 2 单(一单●到店)/ 错记充值 $100(冲销靶,**冲销那下留给店主**)/
  临期次卡 / 通知开六类 → lucky 28 条记录(回访 23=真店量级长列表)、jics 5 条(真数据实情);
- 实拍四屏(lucky 全程 + jics 台面):台面乙案(六列绿区全从 15:57 此分钟起;jics 未开门店绿区从 09:00 起)/
  RFM 条与筛名单 / 账调四 tab+冲销行 / 通知页开关+记录表;
- 镜像老板号首登改密 + 财务密码经 OWNER_TOKEN 合法口重设,全部记 本地自查账号.txt(裁 4);
- 开发者工具侧准备与**清缓存提醒**写进走查文档(体验版缓传已裁);
- 若店主非当天走查,按律当天重铺重拍。

## 过程收获(J/O 册)

- **O-fin1(新观察,只报不改)**:财务门未解锁时账调「冲销」tab 把门禁态错报成空态
  (「没有流水」而非「请先解锁」),且同面板充值 tab 不受门拦 —— 两半不一致,请裁级别。
- 走查实拍途中当批小修两处排版(通知规则行对齐、RFM 编辑器行内化)—— 均在新文件,零波及。

## 回报固定六项

1. **改了什么**:notify 套件⑥b(34→36)+ 默认面两刀;customer-tags.js 重写(RFM 就地操作)+
   admin.js 三处最小接线;schedule-board.mjs(新)+ local-server 三刀 + 两端网格兜底删;
   cross-end ④b(9→13);crossend-cta 白名单 −1;today-board 反向守(27→28);两份清单文档;镜像造景。
2. **在哪一层验**:断言层 + **搬家=响应字节层** + 走查=店主会看到的渲染层(实拍)+ 生产取证=只读库层。
3. **同类扫尽**:users 写入位=7 处逐个;网格兜底=两端同串反向守;RFM 数字框=零 type=number(两套白名单零扰动)。
4. **用什么数据验**:真店量级镜像(6 技师/74 客/23 条回访);今天生日客(⑥b);字节对比四形;双时区两镜像店。
5. **重复稳定性**:字节对比连跑 3 轮全等;⑥b 两刀红-绿闭环;全量回归(见下)。
6. **残留与不确定**:①批 3 清单与批 4 名单**等店主核对/点头**,未动工;②O-fin1 待裁;
   ③RFM 编辑器与通知行排版已修但**像素终验=店主走查**;④小程序侧像素=开发者工具(清缓存)+ 重传体验版。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,489 项断言全绿**(上批 2,482 → +7:notify-scheduler 34→36、
  today-board 27→28、cross-end-effect 9→13;crossend-cta 白名单 6→5、陪审团运行时全集 89 家)。
  日志:handoff/回归日志_2026-08-30h_65套件.log。
- 端口已还:4128(本机库,adminBuild c221deec)/4310(沙箱,走查景在)双活。
- 巨型文件行数(搬后基线报批):**local-server 18,328 → 18,194(−134)· admin.js 8,484 → 8,482(−2)**。
