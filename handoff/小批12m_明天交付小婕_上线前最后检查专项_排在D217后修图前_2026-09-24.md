# 小批 12m —— 明天(09-25)真交付第一家商户小婕(jics-nail):上线前最后检查专项;排在 D217 第一批之后、修图(12i/12j/12k)之前

店主 09-24 原话:明天要真实给小婕开这个应用并使用;功能要完备;修图往后排。
**范围口径先说清(写进给她的交付说明)**:明天能交付的是 **网页版商家后台**(`app.jingshengyouji.com/admin`)+ **网页顾客端**(她店的租户链接);**小程序还没提审**,顾客用小程序要等审核(11c §二 前置清单店主侧未完成:类目资质 / request 合法域名 / 隐私协议)。

## §一 店主四问,我先答能答的
1. **图片上传入口有**:商家后台 → 服务价目 → 编辑某项 → 「上传图片」(`admin.js:3922 imageFile`,前端压缩后存 data:image);技师头像同一通道(`5245`);小记图片 ≤9 张。**但没有「批量上传」**,28 项要一项一项传 —— 明天够用,批量传立案 D225(排修图后)。
2. **登录慢**:不猜,量(§二-1)。可疑三处:境内域名 → 腾讯云反代 → Railway 海外,每个请求跨境一次;`admin.js`/`styles.css` 各六七千行是否有缓存头与 gzip;登录页首屏发了几个 API 请求。
3. **登录页高光**:`styles.css:7220 .login-role-tab.active{background:var(--ink);color:var(--heroink)}` —— 深色档下两个变量都偏亮,字看不见。改成 `color:var(--paper)`(或对比度工具认的那一对),**两档各截一张图 + 甲档对比度工具跑一次**(不许只改浅色档)。
4. **「注册 Owner」按钮**:后端已挡(`10834`:生产 `DEMO_LOGIN_ALLOWED` 未设 → 403「注册已停用」),**但按钮在生产登录页仍显示**(截图可见)。J-112 第二款:挡了一层不算,前端要按 `/health` 或登录页配置隐藏它,生产上不许出现「注册」字样(裁 #103 精神)。判据:生产登录页 DOM 无 `#ownerRegisterButton` 可见;沙箱 demo 模式仍可见。

## §二 Code 做(¥0,不推 main 除非我明批;生产只读)
1. **量登录慢**:在店主机上 `curl -w` 三组各 5 次取中位:①`https://app.jingshengyouji.com/admin`(HTML)②同域 `admin.js`、`styles.css`(看 `content-encoding`、`cache-control`、大小)③登录 POST 与登录后首屏的每个 API(用浏览器 HAR 或 Playwright 计时);对照 Railway 直连域名同样三组。回执给一张表:每项 DNS/TLS/TTFB/总时长,并指出最大的一段在哪(跨境 / 无压缩 / 无缓存 / 串行请求)。**只量不改**;改法我看表再裁(反代加 gzip+缓存头是配置件,店主亲手改;前端合并/懒加载是代码件)。
2. **高光修**(§一-3)+ **注册按钮隐藏**(§一-4),两处各配判据。
3. **上线批清单重跑**:`tools/release-checklist.mjs` 现查重算(上一版 09-21 HEAD c1b2def 已过期),回执贴新表;每条标「Code 做 / 店主做 / 不挡明天」。
4. **小婕店生产只读核**(SELECT only,J-113 只读预批):jics-nail 的 技师名单、价目表行数与三档价、营业时间、门店信息(地址 `长兴南街华润悦府13号楼1单元2005室(20层)`、电话)、老板账号存在且 `must_change_password=1`、0 单 0 假顾客、AI 包状态、企微 `open_kfid` 是否为空。每项一行「现值 / 该是什么 / 差」。
5. **她明天要走的路,在沙箱按同一版本跑一遍**(Playwright,截图留证):老板首登改密 → 门店信息 → 服务价目上传一张图 → 技师 → 营业时间 → 顾客网页端看到价目与图 → 顾客下一单 → 后台看到这单 → 员工登录。任一步断 = 阻断项,列在回执最上面。
6. **给小婕的一页《开店第一天》**(人话,放资料库 D218 之前先出 md 给店主):登录网址、用户名(密码由店主线下交付,不进文件)、首登改密、上面那条路的每一步截图、「看不到想找的?联系有迹 15698550698」。
7. 推 main:本批代码改动(高光、注册按钮隐藏)+ D217 第一批,**J-117 全量 log 贴出来等我批**;推前备份生产库 `VACUUM INTO`,推后只读核验四项。

## §三 店主做(明天之前)
- 平台后台「重置老板密码」给 jics-nail 设初始密码,线下交给小婕(密码不进任何文件/聊天)。
- 平台后台给 jics-nail 开 AI 包(要不要开由你定)。
- 企微 `open_kfid` 若要用企微客服,今天填;不填明天顾客发企微消息会被拒收(上线批 #9)。
- 小程序三件(类目资质 / request 合法域名 / 隐私协议)—— 不挡明天网页版,但顾客要用小程序得等这三件 + 提审。

## §四 顺序
D217 第一批回执 → 本令 §二 1–6 → 我裁推 main(§二-7)→ 12i/12j/12k 修图线。

## §五 店主追加(09-24 晚)
1. **AI 包:给小婕开**(店主定)。平台后台 → 套餐计费 → 该店 AI 包开通;回执核 jics-nail 的 AI 包状态 = 已开通。
2. **主页首图(顾客首页轮播)上传口在**:商家后台 →「顾客首页轮播」面板(D78,`store-content.js`,上传 / 排序 / 文案 / 启停,上限 `HERO_SLIDE_MAX`)。**§二-5 那条路加一步**:上传一张轮播图 → 顾客端首页看到;《开店第一天》写清这一步在哪点。
3. **小程序**:店主查到审核通常几小时。所以**小程序不往后放,一起准备到「今天能提」**:Code 侧 11c §二 §一 的 1/3/6(`USE_LOCAL_SANDBOX=false` 闸、主包、必跑通的路)今晚全部到位并出「提审版」构建;店主侧三件(类目资质 / request 合法域名 `https://api.jingshengyouji.com` / 隐私协议)—— **隐私协议与用户信息收集说明文案由 Code 出草稿**放资料库,店主复制填;类目按 11c §二 截图给我挑。**request 合法域名一定要填,否则提审版一个请求都发不出。**
4. **企微微信客服:代码链路是全的,配置是空的**。代码:回调验签(`1444`)、`kf/sync_msg` 拉消息(`1603`)、`kf/send_msg` 回消息(`1565`)、按店路由 `open_kfid`(`wecom-routing.mjs`)都在。**空的是环境变量**:`WECOM_CORP_ID / CUSTOMER_SERVICE_SECRET / TOKEN / AES_KEY / OPEN_KFID` 五个(`403–408`),由店主亲手灌 Railway(Code/我不看不印);外部卡点是**可信 IP**(09-03 挂起至今,Railway 出口 IP 不固定;境内反代那台腾讯云有固定 IP,可作为企微回调与出站的落点 —— 这是一条路,要立项 D226 才能定)。**结论:明天小婕若要开企微客服,今天开不了;先给她走「不开企微、AI 客服走小程序/网页」的路。** Code 出一页《商家开通企微客服流程》(商家侧要做什么、平台侧要做什么、卡点在哪),放资料库,店主明天问小婕后照着走。
5. **修图线并行:可以。** 店主给 Code 另开一个对话跑 12i/12i补/12j/12j补/12k,**前提三条**:①两个对话不许同时改 `apps/` —— 修图对话只许动 `tools/ai-retouch-eval/`、`tools/segment/`、产出夹;②修图对话不推 main、不碰生产;③两边回执都发到我这里核,台账仍一本。上线对话优先级高,修图对话预算按 12i 的 ¥12。

## §六 店主追加二:小程序手机号一键验证 · 隐私保护指引
**现状(我读码)**:小程序「我的」页有 `open-type="getPhoneNumber"` 按钮(`phone-auth.js`),后端 `/auth/wechat/mini-phone` 走的是**老路**(`encryptedData+iv` 用 session_key 解密);微信**新路只返回 `code`**,前端遇到只有 code 时现在是「照实说、打不了绿勾、让顾客手动填」。店主后台截图显示两个手机号组件各有 **1000 次资源包**,不用再买。
**D228(本批做,小,≤ 60 行)**:`mini-phone.mjs` 加新路 —— 收到 `code` 时用小程序 access_token 调 `business/getuserphonenumber`,取 `phone_info.purePhoneNumber` 绑定;老路保留;前端 `phone-auth.js` 「只有 code」分支改为送后端,成功才绿勾(静默失败器族律照旧)。判据:①沙箱用 stub 返回 `{phone_info:{purePhoneNumber:'13800000000'}}` → 绑定成功;②stub 返回 `errcode:40001` → 不打绿勾、提示重试;③**真机验收由店主在体验版点一次**(需生产 `WECHAT_MINI_APPID/SECRET` 已灌 —— 上线批 #4b 那件,店主报「设了/没设」)。
**隐私保护指引(店主问:等 Code 出文案再复制 —— 对)**:Code 出两份:①《用户隐私保护指引》正文(按微信模板结构:收集哪些信息、用途、存储、第三方、联系方式 15698550698);②**后台勾选清单**:必须勾「手机号」(不勾则 `errno 112`,一键验证直接被微信挡)、「微信昵称头像」(若登录取)、「相册/摄像头」(顾客端签署上传/小记传图用到才勾,Code 按代码现查列)、「位置」(门店导航若用 wx.getLocation 才勾)。**勾多了会被审核问,勾少了功能被挡** —— 清单以代码现查为准,每项注明对应哪个页面哪个 API。
**提审备注文案**一并出:顾客端微信登录无需账号;商家端登录仅签约商户;提供审核员一条可走的顾客路径(选店 → 看价目 → 下单)。
