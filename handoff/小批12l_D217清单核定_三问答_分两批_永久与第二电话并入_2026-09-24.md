# 小批 12l —— D217 交付清单 42 项核定;三问答;拆两批;D215/D216 并入第一批

## §零 三问答
- **40 门店数**:11u §〇.2 为准,**列保留**;图上「去掉门店数」那句作废(图是 09-23 画的,拍板在图之后)。
- **41 电话**:**15698550698 为准**。图上写 18536823102 是**我画图时写错**(记我第 61 笔),设计图已由我改正(现在图里只有 15698550698)。D218 本批只记不做。
- **42 到期**:按合并「套餐 · 到期」一列做;到期日与永久标签都在这一格。

## §一 清单核定
42 项**全部收**,四处补:
- **9 样板店「重置演示数据」**:走 `demo-reset`,加两条判据 —— 真店行 DOM 里不得出现此按钮;API 对非样板店拒绝(`PROTECTED_REAL_TENANTS` + `kind==='demo'` 那道门照旧)。**luvia-bj 也是真店**,不在 PROTECTED 常量里 —— 这一批把「真店 = 非样板店」的判定改成读 tenants 上的样板店标记,不再靠写死常量(常量保留作第二道锁)。
- **11/23 永久(D215)**:数据层 = `tenants.subscription_expires_at` 为 NULL 且 `plan_kind='perpetual'`(或现有等价字段,Code 读代码定,回执贴字段);**J-112:凡读到期日的出口(平台列表、商家端提示、API 校验、定时任务)全部数一遍、每个都验 NULL ⇒ 永久**,漏一个视为未交。生产上给 luvia-bj 点「设为永久」由店主亲点。
- **22/32 第二电话(D216)**:`stores.phone2` 新列;顾客端与 AI 客服口径:主号显示,第二号在「联系我们」里并列;需要**生产迁移一列** —— 走两步:本地/沙箱先迁 + 备份 `VACUUM INTO`,生产迁移列为独立预批(写死 SQL、判据 = 迁移前后行数一致且 phone2 全 NULL)。
- **39 可见性判据文字长度下限**:定 ≥ 4 个汉字或 ≥ 8 个字符,写进断言。

## §二 拆两批
- **第一批(本批)**:图一全部(1–25)+ 判据 34–39 + D215/D216 数据层。
- **第二批**:图二(26–33)。价目表导入 UI 接 11o 三步、企微客服号接 `wecom-kfid`。第一批回执通过再开。

## §三 交付
- L1 截图三张 1920 宽;**并排比对图**:设计图截图 | 实现截图,同尺寸同位置(合同式交付制度)。
- 本批 `apps/web/platform.html` + `apps/api/*` 改动文件清单;`git log origin/main..HEAD` 全量;不推 main(除非明批)。
- 生产不动;D216 生产迁移列单独预批。
