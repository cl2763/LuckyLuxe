# C 清理执行 + D73 交付(2026-08-24)

## 一、C 清理:真库已执行(店主 08-24 说「执行」)

执行前停了 4128(避免带锁写库),跑完还回去。完整输出:`handoff/C清理执行证据_真库_2026-08-24.txt`

| 项 | 值 |
|---|---|
| **备份绝对路径** | `/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/backups/lucky-luxe-20260824154302-清理测试租户前.sqlite`(11,759,616 字节,与执行前主库同大小) |
| **实删行数** | **3,115**(70 张带 tenant_id 的表 2,691 + bookings 关联 206 + technician_services 138 + tenants 80) |
| **对账五项** | lucky-luxe / jics-nail / demo-ai / demo-basic / hoptest-demo2 的 收入·账本行·单·顾客·结算单 **逐项零差异** |
| **删后全库收入** | **¥490,185.00** = ¥491,785 − ¥1,600 ✅ |
| **重跑幂等** | 目标 0 个、待删 0 行,一分不动 |
| EXIT | **0** |

删后全库自证:租户 85 → **5**(real 2 / demo 3)、预约 1,642 → 1,436、账本 1,311 → 1,300;
**孤儿单 0 条、孤儿账本行 0 条**;两家真店 ¥61,401 / ¥81,360 分文未动。

回滚办法(万一要):停 4128 → `cp` 上面那份备份覆盖回 `apps/api/local-data/lucky-luxe.sqlite`。

## 二、D73:判据搬了家,判据的输入没搬 —— 已修

店主指出的两个生产后果,逐条堵:

### ① 建店归属不许再看名字
```
- const tenantKind = DATA_SCOPE === 'test' ? 'test' : (id.startsWith('demo-') ? 'demo' : 'real')
+ const tenantKind = DATA_SCOPE === 'test' ? 'test' : (body.isDemo === true ? 'demo' : 'real')
```
平台后台「新建商家」加了一个**显式勾选**:「这是演示店(账本可重置,不受只追加律保护)」,
勾了还要二次确认(确认文案里直接写明"对外不能拿它说账本不可篡改")。
顺手把 `/shops` 的演示店过滤也从 `id NOT LIKE 'demo-%'` 改成读 `kind`。

### ② 回填改一次性迁移,生产禁跑
`backfillTenantKind`(每次启动扫全表)→ `backfillTenantKindOnce`:
- 跑过一次在 `tenant_settings.__system__.tenant_kind_backfill_v1` 记一笔,以后启动直接跳过;
- **生产(IS_PRODUCTION)一律不跑** —— 生产库的归属只能由平台后台显式设置。
- 前缀表只服务于那一次迁移(店主逐行核过的 85 行归属),之后永久退役。

**实证**:真库重启一次 → 记账行时间戳落 `2026-08-24T15:46:57`,归属稳定在 real 2 / demo 3,
收入 ¥490,185 不动。

### ③ 会红的断言(㋜ 组 5 条)
④⑤ 两条是**真调用**(内存库跑 `backfillTenantKindOnce`):
- 生产上跑 → `skipped='production'`,`p12-realshop` 这种"真商户 id 撞上套件前缀"的**一行不动**;
- 非生产跑两次 → 第二次 `already-done`。
缺陷存在时这两条必红,不是"文件里有没有某几个字"那种废判据。

## 三、小件两条(店主提,已改)

- **先看有没有目标,再备份**:0 目标时打印「无事可做(没有备份,没有事务)」直接退出,不再白拷 11MB。实测过。
- **同秒重跑撞文件名**:原来直接 throw = 裸栈。现在自动加序号(最多 20 次),再撞就给人话提示 + exit 1。

## 四、口径更正(店主指出,报告跟着改)

「十二条统一判据」的准确说法是 **11/12**:`settlements_signed_no_update` 有意**不带** kind 判据 ——
演示/测试租户的已签单同样不许偷改,否则演示数据自己会自相矛盾。模块注释里本来就写了这一条,
是我在回报里说成了"十二条统一",口径以 11/12 为准。

## 五、我自己的判据踩坑第三次(结构性改法)

㋛②、㋛⑤、㋜① 三次都栽在同一件事:**负向判据扫全文,被我自己写的注释命中**
(注释里引用旧写法 → 判据以为旧写法还在)。
㋜① 改成**只取那一行赋值语句**再判 —— 注释怎么写都影响不到它。
这条记进判据律的操作细则:**负向判据不许扫全文,要么取代码行/DDL/真调用,要么别写。**

## 六、待办与顺序

- **演示店生产种数据(选项A)按店主顺序令排在 D73 之后** —— D73 已完成,可以开工了。
- demo 租户豁免保留(重播种需要),但重置要走单一入口 + 留痕:**尚未实现**,需要 Cowork 出规则
  (谁能点、点了写什么留痕、要不要二次确认)。在此之前,演示店账本的可重置性只靠 kind 一层。
- 对外话术红线已记档:**不许拿演示店说"账本不可篡改"** —— 那家店恰好是唯一可改的。

42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-24_D73批.txt`。
**巨型文件行数:local-server.mjs 18,771 → 18,779(+8);admin.js 9,598 → 9,598。**
+8 全在建店归属那一处(显式勾选的注释 6 行 + 一次性迁移的调用块)。按棘轮律我该自己还上,
但这批动的是账本律与平台后台,没有能干净搬走的同域代码 —— **挂账 +8 报店主裁**:
认这 8 行,还是下批用 `serializeQuoteRequest` 那一族(约 40 行,AI 报价域)搬出来一次还清。
