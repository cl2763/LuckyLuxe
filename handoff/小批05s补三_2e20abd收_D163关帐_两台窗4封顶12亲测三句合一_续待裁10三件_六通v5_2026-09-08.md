# 小批05s 补三 — `2e20abd` **收,D163 关帐**(两台 `/health` 窗 4 / 封顶 12,进程环境无回归变量,北京店三句间隔 1 秒亲测合成一条、三件都答到)· 一句口径 · 续待裁 #10 三件 → 六通 v5

> 2026-09-08 夜 · Cowork 现查:4128 `win 4 cap 12`、4310 `win 4 cap 12`;`ps eww` 两进程环境只剩 `PORT`(4310 另有显式 `ALLOW_DEMO_ADMIN_LOGIN=true`,是重启命令自己写的,对),`MERGE_WINDOW_MS` / `TEST_DB_PATH` 0 处;同一访客「几点关门 / 顺便问下定金 / 还有停车」间隔 1 秒 → 流水里顾客行一条(三句并一)、回答一条「周二至周日 20:00 关门,周一休息。预约定金 ¥50(不抵尾款,提前 24h 取消可退)。万达 6 号楼有停车场…」 · 两处各一份

## 一、收

- 回归专用变量登记成表、还回去逐个 `env -u`、真要带的显式写——收。
- 收尾自证「窗与封顶等于默认值,不等就红并明说这一轮不许说『都还回去了』」——**收,这句从此由脚本打印**;锚「等于默认值」不锚 4——对。
- 自证第一版在污染环境里取默认值把对的判成错的、一跑露馅——收其诚实;「取默认值先脱环境」进口径。
- 同族表七个 export(`TEST_DB_PATH` 81 处最险)——收;这张表随脚本走,新增 export 必须先登记(静态判据一条:脚本里 `export X=` 的 X 不在表里 → 预检红)。
- `PRE_REGRESSION=skip` 改脚本自己的刀——收进口径。

## 二、一句口径(不返工)

新会话第一句合并回复前有一条欢迎语(「您好欢迎来到…我是您的预约助手」)是单独一行——通五 v5 的「只出一条回复」判据把欢迎语**排除在外**数(它不是回答),回执里写明;之前的三条回复是三条**回答**,别混。

## 三、续

待裁 #10 三件(一个中断口:可约 `getAvailability` / 优惠 `discountFacts` / 时长 `base_duration_min`,答完把采集问句原样接回;21 套件改前后各跑)→ 通三定金句 ≤120 → **六通 v5**(窗 = 4 的活服务;通五三句间隔 1 秒;通六 fixed 问价 + 「最便宜的」)→ 落仓 → 我发店主打分。
