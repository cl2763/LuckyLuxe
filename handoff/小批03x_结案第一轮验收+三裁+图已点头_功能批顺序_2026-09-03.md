# 小批03x · 结案第一轮验收 · 三裁 · 图已点头,功能批顺序(2026-09-03)

## 〇、亲核

`6a2d217`;D104 全仓原生对话框残留 0(我用带 `window.` 前缀的尺子重扫,唯一命中是 `pages/bind` 里一个叫 `confirm` 的方法名,误报);`ui-dialog.js` 两端都挂;`admin.js` 8,550 / `local-server` 18,231 不动;6139 那个 IIFE 还是 `(() => {`,没被改坏;`showCredentialsOnce` 两处调用都 `await` 了;`TIER_LABEL_REQUIRED` 在后端;两把新刀 7/7、8/8;基线 84 套 2,839。**收。**

那个剥注释的 `\s*//` 吃掉换行、让你连报三次错数还改坏一个 IIFE——你自己抓的、钉成了断言,记名。收编一句:**给刀用的预处理不许改行号;凡剥注释/剥字符串的辅助函数,自守一条「剥前剥后行数相等」。**

## 一、三裁

1. **¥330 挂账(欠图#12)销账**:两库 +49,500/−49,500 净 0、账本 0 行,清单划掉。附带那个测试档案 `isMember=True`(储值 0)是小婕店 `any_recharge` + 默认 `keep` 的配置结果,**不动**——那是她店的规则不是病;只在配置说明里补一句「退光了也保留资格」是这个开关的含义,免得下次再被当挂账。
2. **拆档迁移彩排脚本现在就写好待命**:`tools/migrate-cross-tenant-bookings.mjs`,接 `requireTarget`/`resolveDbPath`(无默认目标)、**默认 dry-run**、`--apply` 才写、先备份、逐表打数、幂等可重跑;做法照 03w §一(在单所属店建档、复制身份不复制别店余额会员、单指过去)。彩排:沙箱用本机库副本 + **自造一条**同形跨租户单,dry-run 报一行→apply→再跑报零→打数差值=闭包。生产那次随部署走,不在本批。
3. **#6 结论收**:小婕店 27 行 `/assets` 由渲染层修复兜住,部署即好,不清库;入上线批「部署含 D126/D127/占位回落」。

## 二、图已点头,功能批顺序(店主拍板 09-03)

《商家端主页重画》v3.1 店主点头,**即合同**。但顺序不乱:**先把结案批剩下的做完**——D121 演示标记实做 → 事务验回滚 9 处 → D109/D112/D113 只答 → AI 复测 12 场景(03t §三)。结案批交齐并核过,直接开功能批:

1. **主页重画批**(按图 v3.1):口径两问已在图 §四答了 → 三接口 `pulse`(六项、四维度、逐日拼)/ `now` / `todo` → 小程序老板视角 → 员工打卡门两态 → 网页首页 → 全屏态;每段交齐附截图 + DOM 证据;十一条判据落常驻刀。
2. S1 两处缺口(D128/D129 收一套 + 存量「其他」大类)。
3. S5 通用设置页(**等我出图**)。
4. 多单串联三处(**等我流程图**)。
5. S2 商城缺口(等图)。

## 三、顺序

D121 → 事务回滚 9 处 → 三条只答 → 拆档迁移脚本+彩排 → AI 复测 → 结案交齐 → 主页重画批。
