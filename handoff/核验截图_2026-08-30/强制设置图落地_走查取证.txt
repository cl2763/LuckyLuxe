# 《营业时间强制设置图 v1.0》落地 · 走查取证(2026-08-30,沙箱 4310)
店:时闸走查店(hgate-walk,专为取证新建的未设置店;老板密码走平台唯一合法重置口,原因已落运维日志)

## A1 拦截(网页,老板号真登录真点)
- 登录 boss-hgate-walk → 首登强制改密弹层**先行**(账户安全在前;图未画此次序,已入假设清单)
- 改密进入 → 整屏「设置营业时间」墙(像素截图=图屏①:标题+副句「开始使用前,先告诉客人你什么时候营业」、
  周一~周日七行开关、7 处「选择时间」占位、灰保存钮「保存并开始使用」+ 橙注「至少选择一天营业」)
- **零预填**:页面不含任何 10:00 之类默认(noPrefill=true)
- **管理页不可达**:实点「订单管理」→ 墙仍在(navTest=墙仍在:true);无 ×、无「稍后再说」、无侧栏出口

## A5 设完立即生效(不退出重进)
- 勾 周一 10:00-19:00 + 周六 11:00-18:00 → 保存钮由灰转实 → 点保存 → 墙拆除 → 台面直接渲染
- 台面显「本日休息」——今天=周日,店只勾了周一/周六:**这一次「休息」是真话**
  (对照 D84 原病:未设置店谎称休息;现在只有真设了且当天真休才显休息 —— A6 合同五活证据)

## A2/A3/A4/A6 断言层(test-hours-gate,25 项全绿;两态夹具=未设置店+已设置店)
- A2:七天全关 PUT → 400 HOURS_ALL_CLOSED「至少选择一天营业」;合并态判定(部分提交 3 天关=200,
  补关剩余 4 天=400)—— 后端是最终闸
- A3:全库「|| 'HH:MM'」回落白名单机械扫 —— 处死 5 处读口回落(可约时段/排班边界/台面/周历/直排),
  余 1 条打卡域成文口径 + 1 条停用件,逐条理由+钉数;行为面:未设置店可约时段=0、
  全天班排班→400 HOURS_UNSET(以前会凭空长出 10:00-20:00 一整天)
- A4:员工墙两端零表单元素(网页 vm 渲染断言 + 小程序 wxml 分支断言 + 员工号 auth/me 旗标行为断言)
- A6:未设置店 hoursUnset=true 且 isClosed=false;**七天全关(老库遗留形态)同判未设置**(统一判定)

## 五刀变异(照图右列「变异刀」,每刀:造→重启→红且点名→还原→回绿)
- 刀A1-web(删 render 咽喉 gate 行)→ 红「render() 咽喉第一行就是 HoursSetup.gate」
- 刀A1-mini(guardMerchant 摘闸)→ **前两刀没红,咬出判据自身两连病**(①数全文出现次数把函数定义行也数进去
  ②切片边界吃掉 async function 邻居)→ 判据改「逐守卫体内点名」后第三刀红且点名 guardMerchant —— J 族+2
- 刀A2(删后端拒)→ 红(前端灰兜不住,如图预言)
- 刀A3(注入 || '10:00')→ 红「local-server.mjs:2 处 > 钉数 1」
- 刀A4(员工墙塞 <input>)→ 红
- 刀A6(判定退回 !hours=休息 的 D84 原病)→ 红 {u:false,c:true}

## 小程序侧(像素给不到:本机无微信开发者工具,明说)
- 新页 pages/merchant/hours-setup(四件+app.json 注册,页面完整性双向断言天生覆盖)
- 闸=api.js 单一咽喉(guardMerchant/guardOwner 都过 enforceHoursGate;redirectTo 无返回键=不可跳过);
  A1 断言:全部 pages/merchant/** 页 onShow 走守卫(0 漏)
- 表单同图:七 switch + picker(time)+「选择时间」占位 + 灰钮注同句;员工分支零表单元素(A4 断言)
- 文案全部后端 hoursGateText(adminMe 缓存),两端同句
