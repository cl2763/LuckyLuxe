# 排班域搬家 · 搬前后接口响应逐字节对比(08-30h 拉平批②)

**搬法**:`/admin/schedule-week` + `/admin/schedule-day` 两段路由体(141 行)从 local-server.mjs
**逐字照搬**进新模块 `apps/api/schedule-board.mjs`(依赖注入,原地换 2 行分发,匹配顺序不变);
连带 A3 尸清:today-board.js 与小程序 orders 的网格兜底 `|| '10:00' / '19:00'` 删除
(死代码:无时段必走未设置墙/休息条,网格不画;套件反向守常驻)。

**对比法**:搬前整棵 apps/api 拷树起 4602,搬后代码起 4603,**同一份临时库、同一分钟**逐端点 curl,
`cmp` 逐字节比。夹具按反例数据律铺:今天 2 单(一单已到店 active)+ 周日特设营业 + 周六休 + 缺参形。

| 端点形 | 字节数 | 结果 |
|---|---|---|
| schedule-day?date=今天(2 单,pending+active,技师 1) | 1,269 | **逐字节相等** |
| schedule-week?from=今天(周六休、周日 09:30-23:30、bookingCounts 带数) | 1,107 | **逐字节相等** |
| schedule-day?date=2026-09-05(未来休息日,空台面) | 321 | **逐字节相等** |
| schedule-week(缺参=本周) | 1,107 | **逐字节相等** |

空台面形(首轮夹具建单被 REST_DAY 拦下,只比到 321 字节空形)按反例数据律重铺后才算数 —— 四形都过。
巨型文件行数:local-server 18,328 → **18,194**(−134;+14/+17/+23 共 54 行欠账全抵,还余 −80)。
