# 乙案落地 · 台面绿区从「门店现在这一分钟」起(08-30g 件一)

**裁定**:店主看图后拍板乙案 —— 绿区起点=门店现在,不等下一个半点(随点随排);两端同一刀。

## 同店同分钟双端对拍(30f 同手法;沙箱 N-5 走查店,2026-08-30)

- 仓码两端裁剪式**逐字同串**(各恰 1 处,套件钉着):
  `date === r.storeToday && r.storeNow ? toMin(r.storeNow) : -1`
  旧半点式 `Math.ceil(toMin(r.storeNow) / 30) * 30` 两端**零残留**(反向守断言常驻,改回即红)。
- 门店 **15:10** 那一分钟,同一份 schedule-day 响应喂两端裁剪式:
  - 网页绿区 = 小程序绿区 = `{走查技师: [15:10~19:00]}`,**逐项相等**;
  - 裁剪起点 = **15:10 = storeNow,零对齐**(半点对齐时代这里会是 15:30);
  - 徽章「空档 **3.8 h**」= (19:00−15:10)/60 取一位 —— freeTotal 在同一裁剪循环累加,**同源同改**。
- 网页实拍(老板号登录,订单管理今天台面,build vc4b016ab):
  10:00~12:00 预约块 → **12:00~15:10 过去段灰、不可点** → 绿块「+ 直接排单」**15:11 起**
  (截屏那一分钟 storeNow 已走到 15:11,DOM `data-time="15:11"` 现测)→ 19:00 关店止。
  **随点随排**:点绿块,直排面板带起的时间就是这一分钟(`data-time` 直接进面板)。

## 三项要求逐条

1. **徽章同源同改**:freeTotal 累加式在裁剪循环内(`freeTotal += (s - from)`,from=裁后起点),
   两端同式;「徽章数 ≡ 绿区总长」断言未松(test-today-board 套件里)。
2. **立即插单起点=现在这一分钟**:两端绿块携带的 startTime = 裁后起点 = storeNow;
   时长按服务算(直排面板既有逻辑);**后端 409 逻辑未动,D88 三样不回退**
   (409 两句说人话 / 双击双 POST 拦 / 顾客可约滤过去 —— 各自断言常驻)。
3. **变异刀**:改回半点对齐 → 逐字同串断言必红(见交付回执刀口记录)。

另注:店主真机要见乙案手感,仍需**重传体验版**(现 1.2.0 = 08-23 码)。
