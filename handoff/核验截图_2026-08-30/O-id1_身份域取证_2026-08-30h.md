# O-id1 · 顾客注册无租户上下文 → 落旗舰租户 · 只读取证(2026-08-30h,只报不改)

> 边界:本机库/生产库均**只读**(生产走 railway ssh + sqlite readOnly);一行未动。

## 1 逐入口列(users 表全部 7 个写入位,机械枚举)

| 入口 | 位置 | 租户上下文 | 判定 |
|---|---|---|---|
| `POST /auth/email/register` → registerEmailUser | local-server.mjs:5823 | **无**(INSERT 不带 tenant_id → DEFAULT 'lucky-luxe') | **本族正主**。但整条路由被 `DEMO_LOGIN_ALLOWED` 闸住:**生产 403 关死**,只在本地/沙箱开(CI 夹具踩的就是它) |
| `POST /auth/google/demo` → registerGoogleDemoUser | local-server.mjs:6107 | **无**(同上) | 同族,demo 专用路由,生产同样不在用户路径上 |
| `POST /auth/wechat/mini-login` | local-server.mjs:6066 | **有**(loginTenantEarly 显式写入) | 干净;但 user 行钉在**首次登录**的店,之后跨店下单合法 —— 见 §2 生产那 1 例 |
| 平台导入 import-customers.mjs:69 | — | 有(显式 tenant) | 干净 |
| 商家建客 local-server.mjs:15168 | — | 有(显式 tid) | 干净 |
| 演示铺客 local-server.mjs:14444 | — | 无(旗舰演示客,设计如此) | demo-seed 专用 |
| users_rebuild 迁移拷贝 :17707 | — | 原样搬 | 中性 |

## 2 计数(同一组查询双库跑)

| 指标 | 本机库 | 生产库 |
|---|---|---|
| 旗舰租户 users 总数 | 74 | 37 |
| 邮箱注册签名(有 email、无微信、无手机) | 24 | 25 |
| **旗舰行、却在别店有预约** | **1** | **1** |
| 旗舰行、在别店有储值流水 | 0 | 0 |

那两例的最小指纹(不带个资):
- 本机库:`demo-cust-01`(演示客,曾在 jics-nail 有单)—— 演示噪音;
- 生产库:`user_mt1iifi2_8970pu`(**微信客**,lucky-luxe 与 jics-nail 都有预约)——
  它不是无租户回落:微信登录写了显式租户(首登=lucky-luxe),之后在小婕店下单 ——
  **一人多店是身份模型既定形**(identity_links 域),不属静默回落族。

## 3 结论(只报不改)

- 真正「无租户上下文注册」的入口只有两个 demo 闸内路由,**生产关死**;
  生产库不存在由它产生的行(生产 25 条邮箱签名行为早期迁移/演示遗留形,零跨店活动)。
- 生产唯一跨店例=微信一人多店合法形。
- **上线前硬门槛审视时要过的问题**:①上线前移除演示邮箱登录(CLAUDE.md 既有承诺)后,
  registerEmailUser 整函数是否随之退役;②「首登定店」的微信客跨店下单时,
  RFM/通知/画像各域按哪个店读它(通知域现按预约店,I7 已按此钉)。
