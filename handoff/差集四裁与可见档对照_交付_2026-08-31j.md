# 差集四裁 + 可见档对照 · 交付(2026-08-31j)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。做完不说可以关帐。
> 回执顺序照令:可见档对照 → entry-config 逐屏对 → 白名单两行落墨 → 营销三件登欠账表。

## 一 · 可见档对照(只报不建;全文=handoff/可见档对照表_2026-08-31j.md)

- **店主记忆无误,开关找到了**:业绩目标域的「显示:仅总进度/含分项」段选
  (`perf_targets.display_mode`,按技师×按月,**两端都有 UI**)—— 管员工业绩页看不看得到
  卡耗金额/单量目标等分项(含每日流水行)。
- **对照结论:两回事,不收敛** —— display_mode 裁**粒度**(每人每月),staff_visibility 裁**板块**
  (全店一刀:业绩/工资两块有无);同一页被两把刀各裁一维,重叠只在作用面不在语义。
- 建 UI 建议(等小合同):staff_visibility 的三选一就放**业绩目标面板同屏**(店主记忆已把两者归一处),
  两把刀摆一起各标一句管什么。
- **「非法值回落默认」复查**:写口 PUT 非法 400=终闸,API 进不来坏值;读口回落只在**库被直写坏值**时
  命中,现状确属静默吞。判定:不该 400(读口炸=业绩页整页打死),**该留痕** —— 两选等裁:
  ① 命中 console.warn;② 店群陪审团加一条「staff_visibility ∈ 三枚」库层判据(与 I7 状态机同型,推荐)。

## 二 · entry-config 逐屏对:**等价,非缺陷**

同一份 `manualEntryConfig()`(cash-notes.mjs 唯一出口)走两条运输线:小程序打
`GET /admin/finance/entry-config`,网页搭 `GET /admin/finance/transactions` 的车(响应带 entryConfig 字段,
finance-entry-form.js:10 就读它)。选项与「记一笔/现金手记分工句」同源同句;
**断言早已常驻**(test-cash-notes ⑨c:viaGet 同形 + 网页实发资源改读 entryConfig 不用常量)。

## 三 · 白名单两行落墨 ✅

test-cross-end-effect ⑤ roster 9→**11**(钉数随批改):
- 「深财务+payroll确认=网页」(既有裁定延伸;发工资是坐下来干的事;七条路由点名);
- 「客服 AI 深功能=网页」(conversation 轻版=设计,不是欠账)。

## 四 · 营销三件登欠账表 ✅

双端欠账总表追加 08-31j 节:social-copy/recall-copy/daily-brief 补网页营销域(同三口),
**非急件排小件队列**;同节顺带记差集表另三行的裁定归档。

## 回报固定六项

1. **改了什么**:代码零动;套件一处(⑤ roster +2 行,钉数 9→11);三份文档(对照表/欠账表追加/本回执)。
2. **在哪一层验**:现码机核(两端 display_mode UI 逐处引行号;staffVisibilityOf 全调用面)+ 既有断言引注(⑨c)。
3. **同类扫尽**:可见类开关全扫(salary-plan 编辑器、员工管理四板块确认无第三把刀)。
4. **用什么数据验**:—(排查批,无夹具)。
5. **重复稳定性**:全量回归(见下)。
6. **残留与不确定**:①staff_visibility UI 与「回落留痕两选」等裁;②营销三件在队列未动工;
   ③走查候令不变(店主开走当天:景重铺+她视角重拍 →「景=今日,实拍=今日」→ 她开始;$100 冲销靶留她点)。
   巨型文件行数:local-server 18,122(±0)· admin.js 8,505(±0)。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,496 项断言全绿**(与 30i 持平;cross-end 17 项含 roster 11 行钉数)。
  日志:handoff/回归日志_2026-08-31j_65套件.log。端口已还:4128/4310 双活。
