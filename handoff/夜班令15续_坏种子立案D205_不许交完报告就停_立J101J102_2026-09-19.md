# 夜班令 15-续 · 坏种子立案 D205 · 🔴 交完报告不许停 · 立 J-101 J-102
2026-09-19 夜 · Cowork 出令 · **店主还醒着,但这份令不需要她再回话**

**上游令点名(J-81):** `夜班令15_十小时_先验一件再做丙_CI绿才推_兜底队列管够_2026-09-19.md` md5 `df6d35c74df5a20c215d15c86fd912b6`。你的 §一 回执收到。

---

## 〇 🔴 先说最要紧的一条:**你又停了**

你自己写着「**下一步(继续做,不等回话)**」,然后**停在那里等回话**。

**从现在起写死:**

> **交出一份报告 ≠ 这一轮结束。写完文件,立刻回到队列的下一件。**

**只有这三种情况允许停下等人:**
1. 要碰**生产**或 **Railway 的任何配置/变量/volume**
2. 要在**网页顾客端**加「注册 / 创建账号 / 新建顾客」
3. 撞上本令与夜班令 15 都没覆盖、而且**做错了不可逆**的事

**其余一律自己判断继续。** 拿不准就选「可逆的那一边」,做完记账,天亮我来裁。
**一个卡住的夜班,比一个做错了可逆动作的夜班贵得多。**

---

## 一 你那条证据,判得漂亮 —— 立 J-101

你没有用「我搜了但没搜到」来结案,而是给了**正例**:

```
test-demo-mark.mjs:106   if (!sb.ok) { …跳过… } else { …行为层… }
CI 那条红:  not ok 9 - ⑤ …  ← ⑤ 在 else 里
⇒ sb.ok 为真 ⇒ ensureSandbox() 成功 ⇒ 沙箱起来了
```

**而且你主动写清了扫描面(J-65③):注解里只有「红行 + 末 25 行」,不是全日志,这个 0 只能说「那一段里没有」。** 🟢 **这正是我要的样子。**

### 立 J-101
> **要证明一件事发生过,先找它的下游有没有跑 —— 下游跑了,上游就成立。**
> **「我搜了没搜到」是弱证据,它同时可能是「没发生」和「我没看见」;「它的下游执行了」是强证据,只有一种解释。**
同族 J-96(空结果有两种来源)。**J-96 说的是别把空当成不存在;J-101 说的是那时候该去找什么。**

🟢 顺手在 `ci.yml` 失败分支加 `grep -c '拉不起来'`,下次有字面答案 —— **对,这是把一次性的推理固化成常设判据。**

---

## 二 坏种子:立案 **D205**,但它今晚大概率不挡路

```
✗ seed-jics-nail: PUT /admin/kb/facts → 400 UNKNOWN_KB_KEY: currency
```

**定性:这是真缺陷,立 D205。** 币种早已收归「门店设置 → 币种」唯一真相(币种红线),而这支种子还在往 `kb/facts` 塞 `currency`。

🔴 **裁死一条:不许动币种红线去迁就种子。** 要改的是种子 —— 要么走「门店设置 → 币种」那条唯一真相,要么**这支种子根本不写币种**(它的活是造顾客,不是定币种)。**J-55②:不许改产出去凑判据,这里是不许改法去凑脚本。**

**但先别一头扎进去修它(J-82 判别式:修好它,今晚的夹具才齐吗?):**
- B 店顾客你要的是 **`seed-jics-mirror`**(它自带「只许沙箱」护栏),**不一定非得 `seed-jics-nail`**;
- 样本单 + `demo_seed` 看 `seed-demo-rich`。

⇒ **先把 `mirror` + `demo-rich` 逐支实跑,看三样夹具能不能齐。齐了,D205 排推后,今晚不修。** 不齐,再回头修 D205,**修法按上面那条裁死的走**。

🟢 你那个「把临时目录命名成 `…/sandbox-data`,同时满足我 §二.3 与种子自带的『只许沙箱』护栏,不用改任何护栏」—— **便宜、干净、两边护栏一个没动,记一功。**

---

## 三 立 J-102(你那句话值一条法)

你写:「**一支种子脚本对着现在的代码已经跑不通了,而它不在任何回归里,所以没人知道。**」

### 立 J-102
> **凡是「要用的时候才跑」的脚本(种子 / 迁移 / 还原 / 造病 / 备份),必须有一条最便宜的冒烟判据进回归。**
> **没进回归的脚本,不是「还没验」,是「已经坏了,只是还没人发现」** —— 你要用它的那一天,正是你最没空修它的那一天。
同族 J-95(CI 24 天没跑过)。**这两条是同一件事的大号和小号:没人跑的东西,坏了不会有人告诉你。**

**今晚只立案不落地**:12 支 `seed-*.mjs` 逐支冒烟,排**推后**,写进欠账。**今晚别为它花时间。**

---

## 四 今晚剩下的路,顺序不变

1. `seed-jics-mirror` / `seed-demo-rich`(必要时 `seed-demo-twin` / `seed-bigdemo`)逐支实跑,**每支两面验:种之前那条断言红 → 种之后绿**
2. 三样夹具齐 → (丙) 四条验收 → `未跑 0` · `NOT_RUN_CAP` 3→0
3. **CI 绿(run id)** → 🔴 **重做一次备份**(`VACUUM INTO` · 拉回本机 · 打开验可读 + 五表行数 · 两头 sha256)→ 推
4. 推完六项核验 + **D194 是否从线上消失单独报一行**
5. 还有时间就走夜班令 15 §六 兜底队列,**里面那件「合同图 v1 四屏店主看不到」别忘了**

**三样夹具怎么试都造不齐** ⇒ 写清卡在哪、试过哪几支、各自报什么错,**然后直接去做兜底队列,不要空等。**

---

## 五 回执(J-81)

首行点名本令文件名 + md5。**下一份报告写完立刻继续干活,不要停。**
早报仍按夜班令 15 §七 的顺序,**要我裁的写最后,每条注明挡不挡上线。**

停线一个字没松。
