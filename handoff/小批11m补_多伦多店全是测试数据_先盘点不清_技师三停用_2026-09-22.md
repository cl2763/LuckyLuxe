# 小批 11m 补 —— 店主明说:多伦多店全是测试数据 · 今晚只盘点不清 · 三位技师停用

**出令时刻**:2026-09-22 夜 · **接 `小批11m`(md5 `9a79c758e8d4f5ca92dfef9cd475f6df`),不覆盖它,§一 照跑**

---

## §〇 店主原话(2026-09-22,逐字)

> 「avalin 是假技师。我说的就是我全职测试用的这些数据。我就是想告诉你,我的店铺里面还有多伦多的店铺里面这些全都是假的。
> 只有比如说我帮小婕配置的是这些名字是我告诉你你写进去、以及当时价格表是我告诉你你写进去,这些才是真的。
> 所有由系统生成的这种名字全都是假的。」

**这句话的边界,我这样读(J-109):**
- **真的**:小婕店的**技师名字 + 价目表**(店主口述、我写进去的)。门店资料(地址等)也是她给的,算真。
- **假的**:两家店里**所有系统生成的人和单** —— 顾客、订单、账本、储值、积分、券、多伦多那三位技师。
- **她没说要清什么、怎么清。** 所以今晚**不清**,只把要清的东西**量全**;清法明天我看着盘点设计。

---

## §一 为什么今晚不能一刀清:门是锁着的,而且锁是店主自己 08-25 上的

我读了 `apps/api/demo-reset.mjs`。系统**有**一个正门 `POST /platform/tenants/:id/demo-reset`,清 30 张经营表、自动备份、写 ops-log、要手打店名。**这就是清一家店该走的门。**

但它有两道锁:① `kind='demo'` 才放行;② **`PROTECTED_REAL_TENANTS = ['lucky-luxe', 'jics-nail']` 硬编码 —— 这两家永远改不成演示店、永远不能重置。**
**这把锁是店主 08-25 亲自裁的,目的就是防「真店被当演示店清掉」。**

现在店主说这两家店里的数据是假的 —— **锁没错,变的是店主对数据的认定。**
🔴 **所以不许绕:不许改黑名单、不许临时改 kind、不许手写 30 张表的 DELETE(J-115)。**
正确做法是**明天开一扇新的、更窄的门**,而开门的设计要先有盘点。

另外 `RESET_TABLES` 按设计**不含** `users` / `user_identities` / `technicians`(演示店重置保留人,只清经营数据),
而店主要的是**连假顾客和假技师一起清** —— 这是范围扩展,更不能今晚顺手做。

---

## §二 今晚做:全景只读盘点(两家店)

生产只读。**80 张有 `tenant_id` 的表逐张数**,两家店各一列:

| 表 | lucky-luxe 行数 | jics-nail 行数 | 在 `RESET_TABLES` 里吗 | 涉钱吗 | 备注 |
|---|---|---|---|---|---|

**必须单独量清的几格**:
1. `users`:每家店几个;**其中绑了微信的几个,是哪几个 openid 的后四位**(不贴全)
2. `technicians`:每家店几个;**jics-nail 那几位是店主口述的真名(11c 核过),逐个列名**
3. `services` / 价目:jics-nail 的**一行不动**,但要列出来数一遍,作为「真的那部分」的对照
4. `finance_transactions` 1160 笔:**全在 lucky-luxe 吗**;有没有 `provider != 'MOCK'` 的支付(有一笔真钱,整件事就不一样)
5. 储值 / 积分 / 券 三张表:余额非 0 的有几个人
6. 🔴 **不在 `RESET_TABLES` 里、却有这两家店数据的表**:逐张列 —— 明天开门时这些是要单独裁的

产出 `handoff/night-runs/两家店全景盘点_只读_2026-09-22.md`。**一个数不带「哪台机器·哪一刻·对谁」不许写(J-108)。**

---

## §三 今晚做:三位技师停用(不删)

11m §一 那批单删完之后(**先单后人**,顺序不许反):

- `technicians` 三行 Mia Chen / Ava Lin / Lina Zhou:`is_active = 0`,`WHERE id IN (三个字面量)`,影响行数 = 3
- `admin_accounts` 里 `avalin`:先读代码停用是哪列哪值(不猜),同法停用,影响 = 1;**`boss` 不动**
- 顾客端只读实访:技师列表 / 作品页不再出这三个名字
- **可逆,所以预批;不删,因为明天的门可能一并处理**

---

## §四 明天我来做的

拿着 §二 那张表,设计「真店测试数据清空」这扇门:比 demo-reset 更窄 —— 手打店名 + 手打固定确认语 + 原因 + 备份 + ops-log,**范围逐表白名单**,用完不留成常驻按钮。店主 08-25 那把锁**不动**。

——Cowork,2026-09-22
