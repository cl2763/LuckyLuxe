# 本批交付清单 · 店主两项改判批(积分全量追溯+测试店保留)2026-08-12

> 判定口径:改判①落地+店主末验(换店往返+积分页三行「累计获得=累计消费」对上)→ Cowork 宣布全清。本批不宣布。

## 一、改判①:积分历史全量追溯(《财务总逻辑》恒等式区四行已先读)

- **实现**:pointsEarnRows 只剩一条查询=Σ已签结算单档位小计(与累计消费 customerSignedSubtotalCents 同形状同基数;LEFT JOIN 保住无 bookingId 的「朋友单」);切换时点常量 `POINTS_POLICY_B_SWITCH_ISO` 与旧口径分支**拆除,L2 机械扫 0 残留**;留痕升 v2(points_policy=subtotal_full_retro)。
- **三行**:/my/points-mall 下发 earnedTotal/redeemedTotal/balance;积分页 hero 三行上屏(累计获得/已兑换/余额)。
- **硬守恒(三次补拍)**:运行时断言——余额>累计获得 = 500 `POINTS_INVARIANT_VIOLATION` 拒绝出账(与四条金额恒等式同待遇);CI ㉔ 全档案扫(每个有动账/签署的档案 0≤余额≤获得)+抛错路径探针(凭空+100→500→对冲−100→恢复,账本只追加不删毒行)。
- **钳位迁移**(幂等:钳后余额=0,重跑扫不到负数):本地库钳位 5 档案——lucky-luxe/demo-cust-01(林小雅)+236,demo-ai×2、demo-basic×2;台账留痕行「口径重算钳位(2026-08-12 改判①)」。生产库随部署同扫。

## 二、回报数字(店主点名要的)

| 档案 | 累计获得 | 已兑换 | 余额 | 说明 |
|---|---|---|---|---|
| **林小雅**(lucky) | **564** | **800** | **0** | 混合口径钳位档案:明细里可见 +236 钳位留痕行;564=98+198+268(Σ已签档位小计)≡我的页累计消费 ✓ |
| 测试-统一口径-01 | 300 | 0 | 300 | 充值 1000(不计积分)→ 签署消费 300(储值抵扣照常积分) |
| 测试-统一口径-02 | 900 | 800 | 100 | 签署消费 900 → 正门兑换 800 分奖品 |
| 测试-统一口径-03 | 268 | 0 | 268 | 签署消费 268 |

三户全走新口径正门(排单→开单→签署→兑换),命名清晰,**留库不撤场**(对账与末验标准样本);jics 演示顾客积分 368→198(追溯后=Σ已签档位小计),d39 换店走查断言同步。

## 三、改判②:测试店保留(零动作)

- 上批「/shops 测试残渣店」上报之裁决=**撤回停用建议**,留作多店测试场;数据零动作。
- 发版清单已加:「正式发版前议测试店在顾客选店页的可见性(隐藏/停用二选一,届时店主拍板)」。

## 四、验证与回归

- CI 111 断言全绿(㉔ 重写:三行恒等/全档案守恒扫/抛错路径;㉖ 累计消费与口径②保持);42 套件全量回归(后台跑毕见回报);收工闸门双租户 jics 13/13+lucky 10/10。
- 渲染层 L5:积分页三行 5/5(564/800/0,获得≡我的页累计消费,钳位行在明细可见)+储值页 5/5(864 不变)+换店往返 5/5+等级双租户 5/5。截图 r2-d36-points(三行版)。
- 工具痕迹:devtools ws 走神一次,整只重启根治(老配方,本批第 2 次)。

## 假设清单

1. 「已兑换」口径=台账负行绝对值合计(兑换);冲正/钳位是正行,不算兑换——三行在干净档案严格恒等,钳位档案由 0≤余额≤获得 兜底。
2. 02 户兑换用与服务端同构签名的 mini token(密钥=正在跑的服务进程实际值,候选探测)走 /my/points-mall/redeem **正门**(库存/限兑/余额闸全过),仅跳过登录握手;绑定=直连库写 openid(CI ⑮ 同法)。
3. 运行时守恒只断上界(余额≤获得);负余额由钳位迁移兜底,若未来出现新负数(如签后撤单)由 CI 全档案扫抓,不在读路径硬钳(避免明细与余额对不上)。
