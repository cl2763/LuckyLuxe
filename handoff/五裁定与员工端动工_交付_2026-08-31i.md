# 五裁定 + 员工端动工 · 交付(2026-08-31,30i)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。做完不说可以关帐。
> 回执顺序照令:裁定 1→2 → 3 语义回报 → 4 → 5 员工端(清单序)→ 走查日重铺确认。

---

## 裁定1 · 死口六条全删 ✅

- 三块整删:`recharge-tiers` 一块 34 行(三变体同块)+ `merchant-leads` 两条 15 行 + `finance/change-password` 14 行;
  连带清三处闸引用(12490 前缀 / 12499 staffMayRead / 14082 豁免子句);各留一行墓碑注释记族谱。
- 公开入驻表单 `POST /merchant-leads`(无 /admin 前缀)与 `stored-value/consume` 410 墓碑**原样保留**。
- **防复活断言**(test-membership-config 原「档位 CRUD」块原地改):①静态 —— 剥注释后 local-server 零
  这三族路由串;②行为 —— 五形直打全部 404。**刀 B**:塞回一条 GET 桩 → 静态扫当场红点名 → 还原逐字节校验回绿。

## 裁定2 · D90 当批修 ✅(O-fin1 转正)

- **requireRefundRight 摘掉财务钥匙半句** → 账调四 tab(充值/赠送/退卡/冲销)统一=仅老板,与充值同权;
- **储值域出门**:财务钥匙闸从 `startsWith('/admin/stored-value')` 收窄到**总览一条**(财务页负债块);
- **「该顾客流水」账调域放行**:`GET /admin/finance/transactions` 新增 `userId` 参(按该客预约归属圈行),
  带 userId 的客户视角形不挂门;**全量账本读与财务页整体门禁原样**;冲销口点名豁免;
- 两端账调读口都带上 userId;小程序「先去财务页解锁」弹跳拆除;
- **刀 A**:把钥匙半句塞回 → card-refund 新判据红点名 FINANCE_LOCKED → 还原逐字节校验回绿;
  反向守:`finance/summary` 没钥匙仍必须被拦(财务页门未松,断言常驻)。
- **附款(空态律)全端扫**:门内路由(finance/* + 储值总览)的 UI 读方逐个过 ——

| 读方 | 门禁真话 |
|---|---|
| 网页财务页(admin.js) | ✅ 整页解锁屏 |
| 网页首页「本月收入/财务待办」卡 | ✅ 🔒 图标 |
| 网页记一笔(finance-entry-form) | ✅ 住在财务页门后 |
| 小程序 财务 / 记一笔 / 流水 / 工资月表 / 我的(密码设置) | ✅ 各有门话或解锁流 |
| **账调两端(旧 O-fin1)** | 曾把门装成空态 —— **D90 连根治**(不挂门了) |

结论:装空态的只此一处,已随 D90 处死;其余门内面全数说真话。

## 裁定3 · staff-visibility 语义回报(只报,不建 UI)

- **存储**:`tenant_settings key='staff_visibility'`;非法值一律回落默认 `perf_and_salary`。
- **取值三枚**:`perf_only`(只业绩)/ `perf_and_salary`(默认)/ `salary_only`(只工资)。
- **路由**:GET `/admin/staff-visibility` → `{visibility, modes}`(管理员可读);PUT 仅老板,非法值 400。
- **作用面恰两处**(机械扫 staffVisibilityOf 全部调用):
  1. `staffPerformanceView`(员工「我的业绩」双端同吃):`salary_only` → hero/daily/monthly/yearly/trend
     **整块清空** + 后端句「本店设置为只向员工展示工资,不展示业绩明细。」;
  2. `/admin/salary/my-estimate`(员工薪资口,清单#2 收敛后的唯一口):`perf_only` → 403
     「本店设置为只向员工展示业绩,不展示工资。」
- **现状**:零 UI 写口 → 全部店都在默认档;test-daily-close 对三态早有行为断言(接口层裁,不只裁页面)。
- 建 UI 素材:入口建议=网页员工管理页(与业绩目标同域),控件=三选一;等你的小合同,我不动。

## 裁定4 · store-wifi 网页补编辑 ✅

新模块 `attendance-wifi.js` 挂考勤区:名单列表 + 删除 + 手填 BSSID(格式前端提示、后端闸原样)+ SSID 选填;
指路句半删半留 —— 「打卡在员工小程序端(需连店内 WiFi 验证,真机才读得到 WiFi)」豁免句只剩打卡本身,
「请在小程序…操作」死口尾巴删除(crossend-cta 白名单 marker 原句头仍在,条目不动)。
跨端断言:cross-end ④c(POST 名单 → 同口读回)。

## 裁定5 · 员工端动工(清单序 1→2→3→9→10→6/7→12→13→14)

| # | 交付 |
|---|---|
| 1+3 | **新模块 `staff-workbench.js`**:员工登录网页侧栏新「工作台」页,四件一屏 —— 我的业绩(/admin/my-performance 原句原裁)· 薪资估算(v2 口)· 我的排班(schedule-week 只渲染自己列)· **报价试算**(与小程序 quote-calc 同引擎同 body 形 /admin/settlements/preview,零金额运算,复制话术走剪贴板)。#3a 我的排班逐屏对:小程序=全店台面(排班团队可见),网页员工=自己周表 —— 数据同两口,形状按端裁,结论=等价 |
| 2 | **同义双口收敛定向 v2**:留 `/admin/salary/my-estimate`(computeSalaryEstimate=薪资方案引擎+可见性闸+工资表状态),退役 `/admin/my-compensation-estimate`(走 legacy staff_compensation=第二套薪资真相,分叉债清偿);网页 4 处读方切新口(noPlan 诚实态),**小程序「我的业绩」页新增薪资估算卡**(同一口;perf_only 的 403 后端句原样落卡);死透扫 +1 口 +404 探针 |
| 4 | 打卡豁免维持(硬件边界) —— 零动作 ✅ |
| 5 | staff-visibility=裁定3 语义回报,等小合同 |
| 9 | **`booking-rules.js`**:门店设置补「线上预约收定金」开关(与小程序 store 页同口);cross-end ④c 效果级断言 |
| 10 | **分叉债现查勘正**:两端写口**本就同为** `/admin/schedule-batch`(欠账表推测的「两套写路由」不成立);真缺口=网页没有「上下午分界」编辑面 → 排班页补「上下午分界」钮(prompt 式,读写与小程序同条 `/admin/schedule-settings`);④c 断言 |
| 6/7 | 逐屏对结论:`recharge/timecard-packages` 两端**全部 GET 选档**,零编辑动作走这两条;套餐编辑走 `/admin/packages`,两端都有编辑=批次七已裁「创建类留会员与营销」的例外面 → **齐,零收口** |
| 12 | 逐屏对结论:web aiGallery(bookings 全集含审核态=审图工作台)与 mini published-works(approved 快照=展示清单)**同表同字段同源**(gallery_status/approved_work_images),形状按用途分 → 等价,写「合理两形」入册,不补 |
| 13 | **`recall-digest.js`**:老板首页召回横条(同口 /admin/recall-digest,懒生成后端既有;名单+一人一句话术+复制) |
| 14 | **差集表**(只报,待裁):

| 差集 | 端 | 建议 |
|---|---|---|
| finance/summary·trend·verify·recurring·compensation·coupon-discounts·payroll(+confirm) | 仅网页 | 已有裁定「趋势/深财务=网页,手机=指标」→ 合理单端;payroll 确认=网页(发工资是坐下来干的事),请确认 |
| ai/booking-summary·customer-insight·customer-service/feedback·logic-notes | 仅网页 | 客服工作台/客户档案 AI 深功能 → 建议合理单端(小程序 conversation 是轻版),请裁 |
| finance/entry-config | 仅小程序? | 网页记一笔表单要对一遍是否同吃该配置 —— 逐屏对后回报 |
| ai/social-copy·recall-copy·daily-brief | 仅小程序 | 营销文案三件在小程序营销页;网页营销页是否该补,请裁 |

## 走查日重铺(候令)

店主开走那天:①景重铺到当天(预约日期/扫描标记/通知记录全按当日重造)②我以她视角重拍全程
③回执一行「**景=今日,实拍=今日**」她再开始。**D90 已修完随批带上** —— 她第 4 步进冲销 tab
不会再被财务门拦成空态($100 冲销靶保留给她亲手点)。体验版缓传照裁:小程序像素在开发者工具验,先清缓存。

## 回报固定六项

1. **改了什么**:死口三块删+防复活;D90 三处闸改+两端读口带 userId+小程序弹跳拆;
   attendance-wifi/booking-rules/recall-digest/staff-workbench 四个新前端模块 + admin.js 接线;
   小程序我的业绩薪资卡;my-compensation-estimate 退役;套件:membership-config 死透块、
   card-refund D90 三断言、staff-portal 切 v2 口、cross-end ④c(13→17)、mini-account-adjust ①改。
2. **在哪一层验**:断言层 + 刀 A/B 红-绿 + 门禁附款=逐读方过(表附上)。
3. **同类扫尽**:staffVisibilityOf 全调用面 3 处逐个;门内路由 UI 读方全扫;死口三族连闸引用。
4. **用什么数据验**:锁开不带钥匙的退卡/流水读(D90 正形)+ summary 反向守;死口五形直打;
   膜拜过的既有三态可见性断言(daily-close)未动照跑。
5. **重复稳定性**:刀 A/B 各红一次还原绿;全量回归(见下)。
6. **残留与不确定**:①#14 差集表四行待裁;②staff-visibility 等小合同;③工作台/新开关像素终验=
   店主走查(网页)+ 开发者工具(小程序);④棘轮:local-server 18,194 → **18,122(−72:死口 −63、
   收敛 −19、D90 +10)**;admin.js 8,482 → **8,505(+23,报批:员工工作台/四个挂载位/上下午分界钮的
   接线必须住在 admin.js 骨架里;可用下一次排班域二期搬家抵扣)**。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,496 项断言全绿**(上批 2,489 → +7:cross-end 13→17、card-refund +1、
  noshow ⑭b +1、membership-config 30→31;引现跑数:noshow 753、card-refund 127、cross-end 17、
  membership 31、notify 36、陪审团 89 家)。日志:handoff/回归日志_2026-08-31i_65套件.log。
- **全量回归四轮才绿 —— 三口都是护栏在咬批文外的边**,各按批文处置:
  ①auth-surface 路由条数下限 153→**150**(批文内七路退役,注释载明「无批文不许再降」);
  ②noshow ⑭ 技师读档位 → 改为读**取代口** recharge-packages 200 + 旧口 404(⑭b 新增);
  ③noshow ⑭ 技师写档位 403 → 改打**取代写口** /admin/packages 403(读写分明原意保留)。
- L1 实拍(沙箱):老板视角 —— 首页召回横条(本周 5 位真名单)/ 门店设置预约规则开关 /
  员工管理·技师业绩页打卡 WiFi 名单(**带出镜像真店一条 TP-LINK 记录**)/ 上下午分界钮;
  员工视角(shiyi)—— 侧栏「工作台」四卡整页(业绩 CAD $0 诚实空 / noPlan 诚实句 / 周排班 7 天 /
  报价试算勾一项实算 **预估 CAD $198**)。
- 端口已还:4128(adminBuild 2694a161)/ 4310 双活。生产库未动 · 本机库未动。
