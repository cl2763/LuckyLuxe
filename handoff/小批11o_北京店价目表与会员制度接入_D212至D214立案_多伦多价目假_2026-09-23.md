# 小批 11o —— 北京店价目表与会员制度接入 · D212 / D213 / D214 立案 · 多伦多价目表是假的

**出令时刻**:2026-09-23 · **接 `小批11n`(`ec010bb03eef58aab73f79cb22e77b8a`),不覆盖**

---

## §〇 店主两句(2026-09-23,逐字要点)

1. 「**多伦多是假的,小婕是真的。**」⇒ 11n 等天亮那一问答了:`lucky-luxe` 的 `services`/价目 **进清空白名单**;`jics-nail` 的**留**。11n 那扇门两家都可以跑了。
2. 「北京店也出了价目表和会员权益,**你直接帮我配置好**;**以后这种操作我要能在商家配置后台自己做**。」

我把两份印刷稿(`output/LUVIA_价目表_延长类新增_印刷定稿/价目表.html` · `output/LUVIA_会员权益_A4印刷定稿/会员权益.html`,**从源文件抄的,不是从 PDF 认的**)逐项抄成了两份稿,放在夹子 `luvia-bj/` 下:
- `北京旗舰店_价目表_导入模板_2026-09-23.csv` —— 28 行,按 `import-services.mjs` 的模板列 + **三列现在模板不认的**(分享价/会员价/疗程价)
- `北京旗舰店_会员制度_配置稿_2026-09-23.md` —— 系统能配的 + 五条缺口

---

## §一 我读代码读到的三条缺口 —— 立案,不许绕

| 缺陷 | 在哪 | 是什么 |
|---|---|---|
| **D212** | `apps/api/import-services.mjs` `HEAD` | 导入模板只认**一个**「价格」;而项目本身四档价(`:6838` list/share/member/course)早就有。**价目表上每项三个价,模板吃不下。**裁:模板加 `分享价/会员价/疗程价/疗程次数/加做项` 五列,**都可空**;空的就只写 list;`加做项=是` ⇒ `addon`(加项永不见客,规则①)。**试跑报告要能一眼看出哪几行三档价倒挂**(会员价 > 分享价 > 原价 ⇒ 标红退回)。 |
| **D213** | `membership-config.mjs` `tiers` | 等级只有 `key/label/minSpendCents/depositWaived`,**没有折扣率**;会员价靠每项一个 `memberPriceCents` = 银卡价。**金/铂/黑三档无处落;累计充值 OR 累计消费双轨也表达不了;卸甲/3次卡「不叠加折扣」没有开关。**裁:**只立案,今晚不做** —— 这是钱的口径(J-105/涉钱),要图先行。我出图。 |
| **D214** | `admin.js:1196` | 商家后台唯一的「上传导入」是**知识库 FAQ 那条**,价目表导入是**平台代导**(`/platform/tenants/:id/import/services`)。店主要的「以后自己在商家后台做」现在做不到。裁:**商家后台加「价目表 · 上传导入」入口,复用同一个 `serviceImportApi`(试跑 → 报告 → 执行),口子挂 `isOwner`,不另写解析。**图我出,本批先做后端口 + 判据,界面等图。 |

---

## §二 本批做的(顺序)

1. **11n 白名单更新**:`lucky-luxe` 的 `services` / `service_prices` / 分类映射 **进清**;`jics-nail` 的**留**。判据里加一条反向守:跑完 jics-nail 后其 `services` 行数不变。
2. **D212**(纯代码,无批点):模板五列 + 倒挂报红 + 阳性对照(塞一行会员价 > 原价 ⇒ 整批退回)。
3. **D214 后端口**:`POST /admin/services/import`,`isOwner`,同一个 `serviceImportApi`;判据:员工令牌 403 · 试跑不落库 · 执行落库行数 = 报告行数。
4. **北京店价目表试跑**(**不执行**):店还没建(等 D210 推上去、店主点)。**先拿那 28 行对 `jics-store` 沙箱镜像跑一次 dryRun**,把报告贴回执 —— 哪些行缺时长、哪些行大类认不出来、三档价有没有倒挂。**店主看完报告再定**。
5. 🔴 **时长我没编**:单色/延长/补卸/美睫/护理的时长印刷稿没写,CSV 里留空。**等天亮问店主**:这几类各多久(或答「先按 90 分钟」)。
6. 四家样板店标签文案(11n §五)顺手。

---

## §三 店主要账号密码 —— 这一条我答给她,不是给你

写在回复里,不进令。**你不许把任何口令、初始密码、平台钥匙写进回执。**

——Cowork,2026-09-23
