# 小批05p 补三 — 读版 v3 收(`b5aeb15`)· 🔴 到底率三跑明细**又没归档**(J-30)· 跑机默认输出改为落仓 · 尺子星期几口径收

> 2026-09-08 · Cowork 现查:`handoff/ai-eval-results/` 里 **没有任何 05p 到底率 JSON**(最新仍是 05n 六份);`b5aeb15` 只改了 `assertion-baseline.json`;`05p_像人六通_原文_v3` 也未入仓 · 两处各一份

## 一、收

五个数齐:10/12 · 100/160 · 0/40 · 0 破(真跑 28 条)· 72%(18 条)。「到底率这条曲线一直被星期几左右」——**收其诚实**,裁:老尺不动作历史锚;新尺加「那周六呢」成立(真顾客会说);**每份明细 JSON 顶部必须记 `ranOn`(日期 + 星期几)与两家夹具店的休息日**,读版脚注写「跑于周日」。北京店 4/6 单列不混分母——对。D146 后台勾选 + `quote` 项**整个 price 字段不下发**——对,这才是「没数据不许出数字」的正解。

## 二、🔴 J-30 · 三跑明细第二次没落仓(05l 那次裁过「不许再写 /tmp」)

现查 `ai-eval-results/` 无 05p 到底率文件,回执里的 6/6/7、10/10/10、4/6×3 **无明细可核**。裁:
1. `tools/ai-eval/booking-rate.mjs`、`fact-hit.mjs`、`human-five.mjs`、`run-200`、`run-edge-80` 的输出路径**默认就是** `handoff/ai-eval-results/<批>_<名>_<尺>_第n轮_<日期>.json`,`BR_OUT` 之类只许覆盖不许留空;写 `/tmp` 的分支删掉;
2. 判据(进预检刀):回执里引用的每个评测数字,`ai-eval-results/` 必须有同批文件——预检脚本扫回执 `05p_` 前缀的文件名与目录对得上,对不上红;
3. 本批立刻**重跑三跑两尺 + 北京店**归档(约 10 分钟),数字若与回执不同以文件为准,读版 v3 随之改;
4. `05p_像人六通_原文_v3` 与这些 JSON 随下一提交入仓。

## 三、其余照 05p 原序:段 4 小件 → 主页重画段 5、段 6。六通打分到了我再补第二轮锚。
