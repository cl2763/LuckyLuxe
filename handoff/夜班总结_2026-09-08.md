# 夜班总结 — 2026-09-08(夜班令 3)

## ① 做到哪一段

**段 0 → 段 3(D145)做完**。段 4 小件、段 5/6 主页重画**未开工**(按补二的裁定改到日班)。

## ② 各段提交号

| 段 | 内容 | 提交 |
|---|---|---|
| 0 | 收 Cowork 待审页热修 + 三条行为判据 | `0591e45` |
| 1 | 回归提速三刀(预检 / 计时 / 批内子集) | `bb015cf` |
| 2 | 新店 `luvia-bj` 两库建店 + 逐行指纹 + 三店常驻判据 | `74807ad` |
| 3 | **D145 采集态分类**(`turn-classify.mjs` + 常驻套件 + 造病) | `05o-3` |

## ③ 停在哪

停在**段 3 做完、全量全绿、日志入仓**。没往段 4/5/6 推 —— 补二把它们移到日班了。
段 3 里**没做完的部分**(D145 七处只治了 4 处):

| 通 | 那一句 | 现在 | 还差什么 |
|---|---|---|---|
| 一 | 「好的谢谢你啦」 | ✅ 收尾不问 | — |
| 五 | 「谢谢」 | ✅ 收尾不问 | — |
| 补 | 「我再想想」 | ✅ 让开一轮 | — |
| 四 | 「改成美睫」「那周日行吗」 | ✅ 复述变化(「好的,改成美睫了。」) | 只在预约采集接了,报价采集那一路还没接 |
| 一 | 「大概要多久」 | ⬜ 分类认得出是 `question`,**但还没接上「先答时长再问」** | 要接价目 `baseDurationMin` / 知识库 |
| 二 | 「预算不多,能推荐吗」 | ⬜ 分类认得出是 `budget`,**还没接上「报最便宜的 1–2 项」** | 要接价目并走事实闸 |
| 三 | 定金政策整段说第二遍 | ⬜ 没做 | 「政策文本一会话一次」还要在预约路上落 |
| 二 | 「会不会很快就掉」被转人工 | ⬜ 没做 | 体验类不转人工那条,分类已认作 `question`,出句没接 |

**为什么停在这儿**:`question` / `budget` 两档的正解是「**先答、再至多一问**」,
而「答」要从价目与知识库取数,必须走事实闸(不许自己编数字)——
那是接数据链的活,不是接一个出句。今晚剩的时间不够把它做到能挨刀,
**做一半比不做更坏**(判据会绿在「分类对了」上,而顾客那一侧照旧答非所问)。

## ④ token 增量

沙箱真模型今晚**没跑评测**(五通 v3、到底率三跑、200 句都按补二移到日班),
只有段 3 行为判据打了 5 次 mock-chat,`/health.aiUsage` 现测四项全 0 —— **远低于 600 万的停线**。

## ⑤ 全量跑了几次、各多少分钟

| 次 | 场景 | 结果 |
|---|---|---|
| 1 | 段 2 首跑 | 红 · `observe-fixes` 夹具日撞店休 |
| 2–5 | 段 2 逐处修(店名白名单 / 护栏按解析点判 / 日志命名 / 夹具抢日子) | 逐次红,每次约 2 分钟 |
| 6 | 段 2 收工 | 98 套,3 把小程序刀未跑,`assertion-baseline` 红 2 |
| 7 | 段 3 收工 | **✅ 99/99 全绿 · 2 分 09 秒**(套件 1 分 56 · 夹具起停 13 秒) |

计时刀第一次量出来的那句话仍然成立:**回归本身两分钟**;段 2 那四小时是**逐处定位红**,不是跑得慢。
日志入仓:`handoff/regression-logs/回归日志_2026-09-08_05o-3_99套件.log`。
断言 **3199 → 3243(+44)**。棘轮 `local-server.mjs` 17,751 · `admin.js` 8,550,**两数今晚一行没往里加**。

**三把小程序刀今晚跑起来了** —— 按补二的裁定起了开发者工具的 automation 会话
(`cli auto --project miniprogram --auto-port 9420`,只起会话、`miniprogram/` 一个字没改),
`miniprogram-automator` 装在会话临时目录(仓外)。
⚠️ 那个会话是我起的,**店主早上如果关掉开发者工具,下次全量这三把又会「本轮未跑」** ——
要长期绿,得把「起会话」写进跑回归的固定动作。

## ⑥ 明早店主要看的三样,各怎么打开

### 一、待审页(段 0 的热修,店主亲测那两句)
浏览器开 `http://localhost:4310/admin` → 用 `boss` 登录 → 左栏「AI 客服 → 待审」。
点「改一句」框会亮出来但**不发请求**;写上字再保存才记;卡片会写「已记上:「改一句」—— 改后的话已进样本」。

### 二、北京新店(段 2)
两个库都建好了:`http://localhost:4310/admin`(沙箱)与 `http://localhost:4128/admin`(本机)。
右上角换店选「**LUVIA 半径美甲美睫 · 北京旗舰店**」,或用 `boss-luvia-bj` 登录。
🔑 **那是一次性密码,第一次登会要求改密** —— 改成您那个本地走查密码即可
(密码在 `handoff/本地自查账号.txt`,该文件在 `.gitignore` 第 43 行,不进 git)。
值得看两眼的:金额全是 **¥**、「今天」按北京算、未来两周 30 张 + 历史 20 张预约、周一那一列是休息、定金规则页。

### 三、D145 那四句(段 3)
在待审页那个模拟对话框里,对**沙箱**说这几句(每句之间不用等):

| 您说 | 现在应该 | 修之前是 |
|---|---|---|
| 「我想做美甲」→「想问问价格」→「**好的谢谢你啦**」 | 一句「好的呀,随时找我就行」,**没有问号** | 「请问是否有断甲或需要修补?」 |
| 「我想做美睫」→「**我再想想**」 | 「不着急的,您慢慢看」,**不再追问** | 继续追下一格 |
| 预约路:「我想预约」→「做美甲」→「**还是改成美睫吧**」 | 先说「好的,改成美睫了。」再问下一格 | 直接问下一格,您不知道它听没听见 |
| 「大概要多久」 | ⬜ **还没修好**(见 §三),它现在会让开,由原流程答 | 「请问是否需要卸甲?」 |

**五通 v3 与到底率三跑今晚没跑**(补二移到日班),所以还没有可打分的新一版对照。

---

## 附:今晚顺手咬出来的缺陷(都不是这两批引入的)

| # | 件 | 处置 |
|---|---|---|
| 1 | `test-observe-fixes` 夹具日写死「今天−13天」,逢周一整条钱链被跳过 | 已修(改成搜营业日) |
| 2 | `test-booking-intake` 8 处说「明天」,默认店周一休 → **每逢周日 13 条集体红** | 已修(逢店休下特殊日,跑完删回去) |
| 3 | 两个夹具抢同一天 → `store-jury` I10 红 | 已修(错开找日子) |
| 4 | `db-snapshot.mjs` 看不见 UPDATE(**J-29**,Cowork 已登记) | 新刀 `tenant-fingerprint.mjs` 补上;两把都绿才许打印「未动」这条**待做**(段 4 小件) |
| 5 | `/health` 不下发 `dataFile`,写库自报律的「路径」那格从没真报过 | 已绕开(`SEED_DB_PATH` 必给);根治要动棘轮文件,**待批** |
| 6 | `classifyTurn` 首版漏了「会不会 / 行不行」这类反复问句 | **判据自己咬出来的**(锚第 9 句),已改成通用式 `(.)不\1` |
| 7 | 我把 05o-1 的回归日志起名带 `_带计时` 后缀,冲掉判据要的结尾 | 改文件名,不改判据 |

**待办**(补二已登记,列在这儿免得掉):D146 `services.price_mode` · 三店那三条新判据补刀 ·
J-29 的 `receipt-db-proof.sh` 两把都绿 · D145 剩下的 `question`/`budget`/政策一次 三处。
