# 批次五六七(UI 收敛)+ D88 + 三裁定落位 · 交付(2026-08-30c)

> 安全保证:**生产库未动 · 本机库未动**(施工=仓内代码 + 4599 临时库;沙箱只在实拍时读写走查店)。
> 未推 main。做完不说可以关帐 —— 走查单草稿已备,店主按单走查。
> 回执顺序照开工令:三裁定落位 → 五 → 六 → 七(含 D88)→ 走查单草稿+当天实拍。

---

## 〇 三裁定落位

**裁定1(改密次序追认)**:落注完成 —— admin.js render 咽喉行注释改为「图 v1.1 已追认为合同」。

**裁定2(表单分叉债收敛)**:营业时间表单=一份组件两处挂载,旧编辑器两端死净。
- 小程序:新组件 `components/hours-form`(七行开关+picker+save 事件,文案=后端 hoursGateText);
  强制页与门店设置页同挂此组件。门店设置旧自绘编辑器删除 —— 连带处死它的**前端编数**
  (缺行预填「周一休+10:00-19:00」,与 A3 同族的前端回落)。
- 网页:hours-setup.js 抽出 `daysHtml()` 唯一渲染出口 + `mountSettings()`;门店设置周网格模板、
  data-hours-closed 联动、saveBusinessHoursSettings(内含 `|| '10:00'` 前端回落 ×2)、保存按钮全删。
- hours-gate 套件补「同一组件」锚点 5 条;**刀3 挨刀时咬出判据子串病**(`includes('<hours-form')` 连
  `<hours-formX` 都算命中)→ 改标签边界匹配后第二刀红、还原绿。J 族 +1。

**裁定3(supabase 尸清)**:已删。清单+接活方+零残留判据=死口总表_批次五六七 第一节(4 条:
3,066 行主尸 + package.json 两脚本 + noshow 套件双后端断言收单 + **A3 白名单随尸减一(2→1,棘轮只减)**)。

## 五 · 小程序账户调整输入框

- 全部输入框/选择器整行铺满:`.in/.pk { width:100%; box-sizing:border-box; display:block }`,
  规格一条 CSS 管所有框 —— 任意真机宽度下(rpx 随屏缩放)一行字完整,不横滚。
- 四个参考数改 **2×2**(四格挤一行时「顾客实付累计」六字标签在窄机必截 —— 反例数据律选的排布)。
- ⚠️ 真机逐框截图给不到(本机无微信开发者工具)—— 布局规格+文件级断言已附,像素终验=店主真机
  (与前两批同口径,走查单第 1-2 步)。

## 六 · 页签/按键配色统一(两端)

- 规格照店主原文:**选中=黑底白字(#2d2826/--black),未选中=白底黑字描边**。
- 小程序收编 7 处白 pill 页签组(member/gallery/content/work-detail/finance/finance-entry + finance 储值签)
  与**点名件**客户档案 2×2(hbtn.ghost→与 hbtn 同黑,两黑两白死);记一笔「收/支」选中红绿语义并入
  统一黑白(金额红绿语义仍在列表数字上)—— 走查单第 3 步如不认可请裁回。
- 网页收编唯一破例:账调弹层 aa-seg 选中暗红 → --black。
- 常驻判据 `test-tab-colors.mjs`(18 项,白名单钉 12+5 选择器+点名件):**挨刀两连咬出判据自身两病**
  ——①只查两色都在(白底黑字同样含这两色,色序盲)②网页版 `||var(--ink)` 兜底恒真。
  改「黑必须在 background、白必须在 color」属性归位判后刀红、还原绿。J 族 +2。

## 七 · 入口总收敛(+D88)

**收敛结果**:凡针对特定顾客改余额的动作(充值/赠送/退卡/冲销),唯一 UI 入口=客户档案→账户调整,两端同此。
- 删除 5 处入口/表单(网页 3 + 小程序 2),清单/原位行号/接活方=死口总表_批次五六七 第三节;
  设置类/创建类(会员与营销的套餐/券/体系)原样保留。
- 两端账调页四 tab 全内嵌:充值/赠送=POST /admin/stored-value/recharge(套餐快捷自动填充 X 赠 Y、
  付款方式收窄四项、经手技师、未绑定提示+后端 UNBOUND 终闸);冲销=本月与该顾客订单关联的账本流水
  列表 + 既有唯一冲销口 POST /admin/finance/transactions/:id/reverse(红字反向、原始保留、已冲销标记、月切换)。
- **余额类写路由集合前后证明**:机械抽取 HEAD vs 工作区逐条相同(死口总表第四节)—— 零增零改零删。
- 登记待裁:**储值充值错记**(stored_value 行)无既有冲销路由(类型标签 'reversal' 在 stored-value.mjs
  里是死字,零写方)—— 本批不加路由,冲销 tab 只覆盖账本(finance)流水;要不要为储值行开冲销口请裁。

**D88(并批修,三层)**:
- 后端:schedule-day 下发 storeNow/storeToday(台面时刻唯一源);409 分因两句(过去=「这个时段已经
  过去了(门店现在 HH:MM)」/占用=「和已有预约重叠(所选服务需 N 分钟)」);可约时段滤过去
  (**同族扩杀**:以前晚上查今天照样列出上午可约 —— 顾客端同刀)。
- 两端台面:过去空档不出「+直接排单」,跨现在的空档从下一个半点起画(截齐式两端逐字同串,断言钉恰 1 处);
- 双击双 POST 拦:网页 f.busy / 小程序 _directBusy(老板补录过去时段仍放行 —— 合法补录不误伤)。

## 判据与突变(本批 6 刀 + 判据自伤 3 案)

| 刀 | 结果 |
|---|---|
| 刀1 指路复活(账调页塞回 goElsewhere) | 红点名 → 还原绿(第一轮还原误用 git checkout 把重造页滚回旧版 —— **自伤事故,已重建并复核**,教训入回执残留) |
| 刀3 门店设置摘组件 | 第一刀没红=判据子串病 → 修标签边界匹配 → 第二刀红 → 绿 |
| 刀4 白 pill 回潮 | 前两刀没红=判据色序盲+恒真兜底 → 修属性归位判 → 第三刀红点名 member → 绿 |
| 刀5 双端同刀断裂(30→60) | 红(恰 1 处断言咬住) → 绿 |
| 刀6 409 分因退化(删过去分支) | 红(报出错句原文) → 绿 |

**套件面**:test-tab-colors 新入册(18);test-today-board 13→26(D88 七条);test-mini-account-adjust
21→25(收敛翻面+网页同批断言);test-hours-gate 25→30(裁定2 锚点);test-card-refund 钱框名单换代
(mSv→aaRv);test-noshow D62 四搜索口收三口+反向守。

## 回报固定六项
1. **改了什么**:上文四节;网页 admin.js/account-adjust.js/hours-setup.js/today-board.js/styles.css/admin.html;
   小程序 account-adjust(重造 268 行)/member/store/orders/customer/7 页 wxss + 新组件 hours-form;
   后端 local-server D88 三刀;套件 6 只;文档 3 份(死口总表/走查单草稿/本回执)。
2. **在哪一层验**:断言层(全量回归)+ 网页实拍(走查店当天 DOM 现测,见附件)+ 小程序文件层
   (像素=店主真机,走查单已标)。
3. **同类扫尽**:死口=总表 11 条逐条接活/豁免;配色=白名单 17 选择器+点名件;路由集=机械抽取双向对比;
   钱框=manifest 面扫描(名单换代不缩水)。
4. **用什么数据验**:走查店(绑定客/未绑定客/有包卡/有流水);反例=过去时段撞位、未来撞位、
   金额 0、白 pill 回潮、组件摘除。
5. **重复稳定性**:六刀全部红-绿闭环;新账调页交互链 L5 实拍轮补(网页端);小程序端待真机。
6. **残留与不确定**:①**自伤事故**:刀1 还原误用 git checkout 滚掉重造页,当场发现重建+全套件复核 ——
   教训:变异一律 cp 备份还原,git checkout 禁入变异流程(已在本批后半执行);②小程序像素待真机;
   ③记一笔收/支选中色语义并入黑白,待店主裁回与否;④储值行冲销无路由,待裁;
   ⑤local-server +14 行**报批**(D88 三刀都在既有巨函数内,无小件可拆;下批动排班域时随手搬
   schedule-day/week 抵扣);⑥网页 today-board/mini orders 的 `r.openTime || '10:00'` 网格兜底
   仍在(hoursUnset/isClosed 分支先行,值不消费 —— 已登记,随排班域搬家批一并清)。

**巨型文件行数**:local-server.mjs 18,258 → **18,272(+14,报批,理由见残留⑤)**;
admin.js 基线 8,582 → **8,482(−100:旧营业时间编辑器+充值表单+给TA充值全删)**。

### 回归结果(补记)
- **✅ 全部 63 个套件通过,2,412 项断言全绿**(上批 62/2,376 → +1 套件 +36 断言)。
  日志:handoff/回归日志_2026-08-30c_63套件.log。途中两红两修:
  ① D62 四搜索口断言 —— 会员页代充搜索口随死口一并删,判据收三口+反向守;
  ② 钱框名单 mSvAmount/mSvBonus 已死 —— 名单换代 aaRvAmount/aaRvBonus(总数不缩水)。
- 端口已还:4128/4310 双活;adminBuild=20260830d-ui567 指纹。

### 当天实拍索引(补记)
- 核验截图_2026-08-30/批次五六七_当天实拍.txt —— 入口收敛全链(充值 500→1050 真值/冲销红字+已冲销)、
  裁定2 组件七行、D88 空档起点 13:30 vs 门店现在 13:16、弹层 L5 五连 5/5。
- 走查单草稿:handoff/走查单草稿_批次五六七_2026-08-30c.md(每步已注实拍对应行)。
