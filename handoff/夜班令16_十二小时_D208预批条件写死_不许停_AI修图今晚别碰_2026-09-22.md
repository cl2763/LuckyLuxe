# 夜班令 16 · 十二小时 · D208 **预批(条件写死)** · 不许停 · 🔴 AI 修图今晚别碰
2026-09-22 · Cowork 出令 · **店主已睡,本令自带全部裁决,一件都不要等人**

**上游令点名(J-81):** `小批10i…` md5 `b40ff893785fe263a53d925f3cd8ea81` · `小批10h…` `651d3d608bb96d635ecad70a14dbf567`。

---

## 〇 🔴 不许停(15-续 那三条,今晚继续生效,原文抄一遍)

> **交出一份报告 ≠ 这一轮结束。写完文件,立刻回到队列的下一件。**

**只有这三种情况允许停下等人:**
1. 要碰**生产**或 **Railway 的任何配置/变量/volume**
2. 要在**网页顾客端**加「注册 / 创建账号 / 新建顾客」
3. 撞上本令没覆盖、而且**做错了不可逆**的事

**其余一律自己判断继续。拿不准选「可逆的那一边」,做完记账,天亮我来裁。**
**一个卡住的夜班,比一个做错了可逆动作的夜班贵得多。**

---

## 一 🔴 AI 修图:**今晚一个字都别碰**

**店主已经把 AI 修图那件开在另一个对话(「AI 美甲修图拍照方案」)里推进,明天出结果后我们再会合。**
**⇒ 今晚不许动 31o、不许动 AI 钥匙、不许找样片、不许写任何 AI 修图相关代码或设计。**
**碰了就是重复劳动,而且会跟那边的产出打架。**

---

## 二 D208:**我预先批了,条件写死,不要等我**

10i 写的是「方案先报我」。**店主睡了,所以我把那个决策点换成决策规则 —— 照规则做,不用等。**

### 批准范围(只许做这些)

**(甲) 进分包的,只许是这两类:**
1. **顾客根本走不到的**:`merchant` / `merchant-login` / `merchant-apply` / `merchant-forgot` / `merchant-change-password` / `sandbox-identity`
2. **顾客低频才走的**:`mall` / `portfolio` / `points` / `member-benefits` / `about` / `messages`

**(乙) 🔴 一律留主包,一个都不许挪:**
`entry` / `home` / `shop-select` / `services` / `service-detail` / `booking` / `booking-done` / `cart` / `checkout` / `orders` / `order-detail` / `me` / `card-pack` / `stored-value` / `sign` / `bind` / `store-location`
**判别式:顾客第一次用、或每次都用的那条路,一律留主包。** 分包会让第一次点进去多等一下 —— **那一下不许花在顾客的主路上。**

**(丙) 图片压缩:** 那 4 张 jpg 可以压(质量别掉到能看出来),**不许删图、不许换图。**

**(丁) 🔴 不许为了瘦身删任何功能、不许动任何判据。**

### 验收(四条,少一条不算做完)
1. **主包 ≤ 1900KB**(不是 2048 —— 留 150KB 余量,因为它会一直长)
2. 🔴 **真的走一次「上传」把那道闸走通** —— **只要过闸,不许真的提交审核 / 不许发布**
3. **体积棘轮进回归**:主包 ≤ 当前实测值,只许降;>1900KB 黄、>2048KB 红;**两面验(塞大文件必红 / 拿掉回绿)**
4. **顾客主路手点一遍**:首页 → 服务 → 预约 → 我的,四步都在;**分包那几页点得开**

### 🔴 如果按白名单做完仍 > 1900KB
**停下来,不许自作主张挪顾客常走的页。** 写清:现在多少 KB · 还差多少 · 下一个候选是谁 · 为什么 —— **然后直接去做队列里的下一件,不要空等。**

---

## 三 今晚队列(做完一件立刻下一件)

1. **D208**(§二)
2. **小程序 L5 补跑**(纯 CSS 那次之后那一跑)
3. **乙档第二批 ≤10 处** —— 先修会落进对账 / 结算 / 储值的;**`i18n.js` 那两个零调用方死键一起删**(删前按 J-96 把「零调用方」再量一次)
4. **推一次**:前三件做完 → 🔴 **推前先拍当期备份**(`VACUUM INTO` · 拉回本机 · 打开验 · 表数 · 五表行数 · 两头 sha256)→ 推 → **CI 绿才算**(J-95)→ 部署 → 轻量六项 → 写进早报
5. **乙档第三批 ≤10 处**
6. **兜底队列**(不许闲着,按顺序):
   - `ai_response_feedback` / `ai_learning_examples` 那把尺子看不见的事(给一条能对的判据,或在尺子里明写「不在量程内」——**两条路都行,不许沉默**)
   - **`users` 没有 `created_at`** —— 立案 + 写清影响面(**只立案,今晚不改表**)
   - **40 个涉钱口破坏测试**,剩 30,能推几个是几个(**造得出阳性才算**)
   - **11 条追不到口的断言**,剩 4
   - **D199** · **J-60 那 23 处**
   - `tools/knife-ratchet-attr.sh` 四处 `sed -i ''`(macOS 语法)改成两边都能跑

🔴 **D207 那 51 行 · D207-b 那 1 行:整夜冻结,一个字不许写。清理仍等我批 `SELECT` 原文 —— 本令没批。**

---

## 四 跑满十二小时的口径

- **任何可能超过 5 分钟的动作,一律后台跑 + 轮询**(夜 14 查实:唯一一次停是工具 5 分钟超时打断 push)
- **心跳每 30 分钟写一份** `夜16_心跳.md`(覆盖同一份),四行:`当前在做哪一件` · `已完成几件` · `卡在哪` · `下一步`
- **收工时那份心跳的时间戳必须在最后半小时内** —— 这是「跑了多久」唯一算数的证据

---

## 五 停线(一个字没松)

- 不碰 Railway **任何**配置 / 变量 / volume;只读进容器可以,**写文件不行,即使 `/tmp`**
- 生产只许 `SELECT`;**不向生产发任何会写入的请求**(J-72)
- **不抬棘轮 · 不松判据 · 不换尺子 · 不 `--accept` · 不改合同图凑代码**
- **抽取必须行为等价**(J-107);**一条法只授权它明写的那件事**
- 🔴 **密码 / 令牌一律不进聊天、不进令、不进台账、不进回执**;不看 `.env*` 与 `handoff/本地自查账号.txt`
- 🔴 **顾客端加「注册 / 创建账号 / 新建顾客」一律停下问,没有例外**
- 一个 0 不带「扫了几个文件」不许写进报告;批量替换前先量全(J-78)
- **量出来的数必须带三样:哪台机器 · 哪一刻 · 对谁**(J-108)

---

## 六 早报(店主醒来第一眼)

写 `夜16_早报.md`,**顺序写死**:

1. **一句话:小程序现在发得出去了吗**(主包多少 KB · 上传闸过没过)
2. **生产动没动**(没动就明写「生产一个字节没动」)
3. **推了没有**:commit · CI run id · 轻量六项
4. **队列做了几件,分别是哪几件**
5. **新发现**(如果有)—— **严重度分「量出来的」与「推出来的」两栏(J-88)**
6. **要店主裁的** —— 🔴 **写在最后,每一条注明「挡不挡上线」**(J-82)

**回执三行口径不许省:**
```
本机全量:129/129 · N 条断言 · not ok 0 · 未跑 0(哪台机器)
GitHub CI:绿 / 红(run id)
GitHub CI 未跑:N 套(逐名)
```

**首行点名本令文件名 + md5。**
