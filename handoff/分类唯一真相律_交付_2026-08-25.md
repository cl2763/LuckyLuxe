# 分类唯一真相律 · 落地交付(2026-08-25)

**真库一行没写,生产一行没写。** 42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-25_分类唯一真相律.txt`

## 棘轮(wc -l 现测)

```
apps/api/local-server.mjs   18,675 → 18,670   (−5)
apps/web/admin.js            8,945 →  8,869   (−76)
```
本批新功能先让两个文件涨了(local +79 / admin +8),**同批还清**:
`seed-services.mjs`(14)/ `pricing-serialize.mjs`(100)/ `import-services.mjs`(130,新功能新模块)/
`service-catalog.js`(90)。四个都已 git add。

---

## 结构:两级,不是两套

- **上层 = 大类**(店内 `service_categories`)。平台三类「美甲 / 美睫 / 护理·其他」是**默认起点** ——
  建店时自动落好;商家可细分(小婕那六类照样放得下)。
- **下层 = 款式** = 项目本身,挂在大类下,**不再自成一组**。

## 律怎么落的(四条逐条)

| 条 | 落点 |
|---|---|
| ① `services.category` 退役为**派生字段** | 读:`categoryNameOf()` 唯一出口,由 `category_id` join 字典;拿不到返空串(不回落、不编)。写:`servicePayload` / `pricingItemShape` **不再接受** `body.category`,该列一律写空串 |
| ② 商家编辑项目**只选大类** | 自由文本「分类」输入框已删,换成大类下拉;前端只送 `categoryId` |
| ③ 建店落三大类 + **不挂大类不准上架** | `seedDefaults()` 建店即铺(幂等);`assertCategoryOk()` 是**唯一校验口**,商家线/平台线共用,不挂即 400 `CATEGORY_REQUIRED` |
| ④ 导入口同受管 | 新模块 `import-services.mjs`:模板必须有大类列;缺大类的行**标红退回并点名到行号**;**一行有问题整批不进库** |

**另外堵了两个我发现的入口**(不堵就是"配了一半的店"的来源):
- **平台代建项目**(`/platform/tenants/:id/services` POST):没传大类就落**本店同类型的默认大类**,落不到才拒
  —— 平台代配是批量活,不能因为少填一列整批停,但绝不允许留空。
- **演示种子**:它原来往 `category` 里写「法式系列 / 日式款」这类**款式名** ——
  顾客端「8 个分组各 1 项」就是这么来的。现在种子只写 `category_id`。
  那些款式名不丢:它们本来就是款式,不是分类。

## 会红的断言(㋦ 组 9 条,全绿)

| # | 断言 | 结果 |
|---|---|---|
| ① | 全库 `services.category` 与大类字典**零不一致** | ✅ |
| ② | 商家建项目不挂大类 = **400 `CATEGORY_REQUIRED`** | ✅ |
| ③ | 目录建主项目要上架却不挂大类 = 400 | ✅ |
| ④ | 顾客端 `/services` 分组名 **≡ 大类字典名**(真跑取值,零野名字) | ✅ |
| ⑤ | 导入试跑:缺大类的行标红退回,**点名到行号** | ✅ |
| ⑥ | 导入执行:一行有问题**整批不进库**(前后行数相等) | ✅ |
| ⑦ | 导入正例:全挂大类才真进库,且落的是 `category_id` | ✅ |
| ⑧ | 建店即落平台三大类 | ✅ |
| ⑨ | 商家端自由文本分类输入框**零残留** | ✅ |

**全新库启动实测**:大类 = 护理·其他/美睫/美甲;不一致行 **0**;没挂大类的上架项目 **0**。

**导入报告长这样**(真跑):
```
结论:退回:3 行有问题,一行都不会进库(缺大类 1 行)
  第 3 行 | 加钻     | 🔴 缺大类 —— 不挂大类不许进库
  第 4 行 | 睫毛加密 | 🔴 大类「美睫」不在本店大类字典里(现有:美甲)
  第 5 行 | 神秘项目 | 🔴 大类「不存在的大类」不在本店大类字典里(现有:美甲)
```

**存量按令没碰**(Cowork 已对齐 4 店 22 项),迁移只保证以后不再分叉。

---

## 顺带两件

1. **《商家入驻 SOP(N-1)》** 已写:`handoff/商家入驻SOP_N-1_2026-08-25.md`。
   里面把「**大类配一次就够,不存在第二个地方**」写在第 1 步,并写明顺序 **先配大类再配项目**(反过来会卡住)。
2. **沙箱 boss 密码已重置**并实测登录成功(role=owner);新密码只写进
   `handoff/本地自查账号.txt`(该文件已 gitignore),**不进聊天不进仓**。
   顺手在那行下面写清楚:08-24 那条为什么失效(沙箱库后来重建过,哈希跟着没了)。
   **真库与生产的密码一律没碰。**

---

## 自己抓到的坑(四条)

1. **建店铺三大类会撞掉 11 个套件的夹具** —— 它们都 POST 一个 key='nail' 的大类,现在必然 409。
   没有为了让测试过就把 409 放松,而是把夹具改成「有就用,没有才建」(判据/夹具跟着口径走)。
2. **差点把"不挂大类不许上架"做成"不挂大类不许编辑"** —— 那会让存量未归类项目连改价都改不了。
   现在只拦**上架**这一步;真正该堵的是创建入口,所以补堵了平台代建与种子两处。
3. **导入落库连撞两次表结构**:`services` 没有 `created_at` 列、`description_zh` 是 NOT NULL。
   凭印象写字段就是这个下场 —— 两次都是真跑报错才发现,不是看代码看出来的。
4. **搬域时依赖漏注两次**(`servicePriceMap` / `startingPriceCentsOf`),都是真跑当场崩。
   这类"抠出来真跑"的搬法,漏了会立刻炸,比默默绿好。
