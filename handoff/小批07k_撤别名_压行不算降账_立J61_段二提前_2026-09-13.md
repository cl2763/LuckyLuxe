# 小批 07k · 撤掉那个别名 · 压行不算降账 · 立 J-61 · **段二提到前面**

**下达时刻**:2026-09-13
**Code 第一件事**:落进桌面夹 + 仓里 `handoff/`,报两处**全 32 位** md5。

---

## 〇 先记你两功

1. **「7 套」那个数,是你自己把它推翻的。**
   > 「我当时那个『7』是按『文件里出现这个串』数的,不是按『真的调它』数的 —— 口径本身就松。」

   这句话你可以不说。上一轮我按「7」下的裁,你照做到「点名第 7 套」就算交差了 —— **你没交差,你回去查了那个数怎么来的。** 见 §三,我从这里立了一条律。

2. **J-60 那 32 处你没改,而且理由是对的。**
   32 处散在 14 个文件,每处都要起服务真跑才知道转对没转对,而你今天光 `stored-value` 一套就栽了三跤。
   **你没有为了交差盲改 32 处,而是把这笔账立成了机械的、只许降的棘轮。** 改坏 32 处比慢还账贵得多。**这个判断我接受,不用再解释。**

**数我核过**:1 个提交 → 299+1 = **300** ✅ · 主档 115→**116**(新套件 `test-fixture-front-door`)✅ · 门关档 绿 8 + 红 8 = 16 ✅ · 那 8 条红点名 = 4 顾客 + 3 员工 + card-refund ✅ · J-60 三个数 41 = 32 + 9 ✅。

---

## 一 🔴 裁 #88 · **那个别名撤掉** —— 你拿产品去迁就夹具了

> 「顺手把 `newCustomerPhone` 也认了 —— 传错名字不该变成『悄悄没有手机号』。」

**前半句我不认。** 这句话说的是病,治病的是你前面那一件(缺号落 `tags_json=["无手机号"]`),**不是这一件。**

有了标记之后,传错名字**已经不会**变成「悄悄没有手机号」了 —— 它会变成**一条后台看得见的「无手机号」档案**。那正是我们要的结果。

**所以这个别名什么都没多治,只多了一样东西:同一个字段的第二个拼法。**

三条理由:

1. **一件事两处真相。** 这个仓一整个月都在拔这个 —— `--accent`/`--brand` 归一、启动参数收一处、直连腾讯只留一处。**这里反手加了一个。**
2. **它把发现病的那个信号关掉了。** 下一个人写 `customerPhone` 或 `phoneNumber`,照样静默;而我们刚刚教会了自己「传错名字就加个别名」。**别名不是治法,是把一个病扩散成一族。**
3. **最要紧的一条:传错名字的是你的夹具,不是顾客。** 你改的是**产品**,去迁就**夹具**。方向反了 —— **夹具错了改夹具。**

### 要做的三件

1. **撤掉 `newCustomerPhone` 这个别名。** 那个口只认 `phone`。
2. **夹具那边把名字改对**(你已经知道是哪一个)。
3. 🔴 **治真病根 —— 你自己只报不改的那一条**:

   > 「这个口对不认识的字段**一声不吭地忽略** —— 我这次就是这么栽的。」

   **这才是让你栽跤的东西。** 治法:
   - **ci/sandbox 库域:收到未知字段直接拒绝**,点名是哪个字段。
   - **生产/本机库域:忽略,但记一条**(能被后台/日志看见)。
   - 造病:传一个不存在的字段 → ci 档必须红并点名。

   为什么两档不一样:生产上真拒绝可能会把老客户端弄崩;但**开发期一声不吭地忽略,等于把每一个拼写错误都变成一次静默的数据丢失**。你今天就是被这个咬的。

   **同类扫尽**:同族的写入口(建档、改档、下单、充值)对未知字段各是什么行为?逐个列,**能上 ci 档拒绝的都上**。

---

## 二 🔴 裁 #89 · **压行不算降账** —— 把那一行前后贴给我看

> 「棘轮涨了 6 行 —— 不行。把说明压进同一行,净增 0。」

**先说清楚:你不想顶破棘轮,这个反应是对的。但压行可能是在骗尺子。**

那个 17,707 的行数棘轮,它要的不是「行数别涨」。**它要的是:别再往这个巨型文件里倒东西。** 你自己就照这个意思做过两次对的事 —— `party.js`、`secret-gate.mjs`,都是**摘出去**,不是压进去。

**把六行压成一行,行数达标了,但那六行的内容一行没少,而且更难读了。** 判据满意了,判据要防的事照样发生。

### 要做的

1. **把那一行的前后原样贴给我看。**
   - 如果只是一句注释并进了已有的行,读起来照样清楚 → **没事,这一条到此为止。**
   - 如果是把逻辑塞成了一行长表达式 → **摘出去**(照 `secret-gate.mjs` 那个形状),或者**报「我要加 6 行,理由 X」让我裁**。
2. **立口径**:**为了过行数棘轮而压行,不算降账。**
   棘轮顶破了,正确的三个反应是 —— **摘出去 / 报上来请裁 / 说明为什么这六行非加不可**;**压行不在里面。**

---

## 三 🔴 **立 J-61 · 数「出现」不等于数「发生」**(两款)

### 第一款 · 判据要认行为,不认文本

你这次自己撞出来了:「7 套」是**提及数**,真调用是 **5 套**;其中 `login-entries` 把路由名当**数据**写在映射表里(那正是它的工作),另两套是**你自己的注释**。

这个病在这个仓里已经是第四次了:

- 英文指路句扫不出来(按「小程序」三个中文字认句子)
- `painted`/`seen` 去重把节点数藏掉(594→66)
- 声称判据认措辞不认标记(J-49)
- **这次:按字符串数出「7 套走旧路」**

**款文**:判据要认**行为**(调用、执行、写入、渲染),不认**文本**(提及、注释、映射表里的名字、字符串常量)。
凡是按字符串数出来的数,**报的时候必须写明「这是提及数」**,不许当成发生数用;要当发生数用,先证明它认的是调用。

**并且**:上一轮我基于那个「7」下了裁 #85。按 J-55,结论要回去看 —— **我看了:裁 #85 的结论仍然成立,而且更成立。** 因为它现在多证明了一件事:**连「有几套走旧路」这个数,当时都是提及数。**「不靠红发现旧路」这条改得对,**只是当时我以为要抓的是 7 个,其实是 5 个 + 2 条注释 + 1 张映射表。**

### 第二款 · **判据默认排除自己,不许每次靠人发现**

**三批,三次**:
- 07i:替身那套自己碰登录态,落进「该跑」名单;
- 07j:判据扫到自己的探针字符串;
- 本批:又扫到 `①h` 的探针字符串,还扫到你自己的注释。

**同一件事三次靠人当场发现,就该做成规矩,不该再靠人。**

**款文**:所有扫全仓的刀,**默认排除「判据自身 + 判据的夹具」**;这条排除要**具名**(不是内容模式,J-51),并且要有**造病**:把自身放回扫描面 → 判据必须**只红在真目标上**,不许红在自己身上。

---

## 四 J-60 那 32 处:**27 处是同一个形状,你已经有样板了**

你报的三个数里,32 处 = **绑微信 27 + 建档案 5**。

**那 27 处「绑微信」正是 D190 那条路** —— 而你刚刚在 `stored-value` 上把它转完了一遍,**转法是现成的**:商家建轻档案 → 同号从正门登录让绑定真发生一次。

所以那不是「27 次独立设计」,是**一个样板复制 27 次**。这和「散在 14 个文件、每处都要真跑」不矛盾 —— 真跑还是要真跑,但**设计只做一次**。

**本批要的**:

1. **把 `stored-value` 那一套抽成共用出口**(如果还没抽),然后**先转 5 处试试水**,报转完之后两档的数。
2. **9 条具名冻结逐条给理由**,不许共用一句「队列收尾/播种」。每条要答:**正门为什么产生不了这个状态?**
   —— 凡是答不出来的,**那一条不是冻结,是还没想清楚**。
3. 棘轮 32 **只许降**,并且**每一批至少降 5** —— 不许无限期挂着。

---

## 五 裁 #86 还欠着,排前面 —— 它便宜而且它是身份路

演示白名单那条回落路(`:10869-10871`)**现在还有谁在用?**

- 没人用 → **删。**
- 有人用 → 具名冻结 + 造病(生产库域必须抛)。

**这条排在段 A 之前** —— 它只要一次 grep 加一个删除,而它是**第二条员工身份路**。身份不许有回落。

---

## 六 🔴 **段二提到前面** —— 我改一次顺序,理由说清楚

段二(小程序四步)已经被挤了四轮了。我重新排:

**门关档那 8 条红是「判据的健康」;段二是「产品能不能认得出人」。两件都重要,但它们不是一回事,而且后者有个时间成本前者没有:**

> **顾客档案里没有手机号,店主在后台就认不出人。而且存量越多,以后补采越痛。**
> **每晚一天上线,就多一批「以后要回头补手机号」的顾客。**

判据的健康没有这个滚雪球 —— 那 8 条红今天修和下周修,代价一样。

**所以**:段二排在 J-60 那 27 处之前、排在 card-refund 三条硬条件之前。
J-60 挂着只许降就行,**它是笔慢账,不是急账。**

**段二的提醒(上次给过,再放一遍,省得你卡住)**:
手机号解密要 `session_key`,而它正是 `jscode2session` 给的 —— **替身给的是假的**。替身**也要给出一把确定的 `session_key`,并用它按真算法加密一份测试载荷**,服务端照真算法解密。**解密失败必须拒绝,不许回落成空号。**
`getPhoneNumber` 真授权那一下如果没真凭据跑不了,**照实写「未做,原因 X」,不许造一个假的当它做了。**

---

## 七 两个小的

1. **断言数补报**:3,936(夜 10)→ 3,951(本批起点)中间那 **+15**,发生在 07i 和日 2 两批,**那两批都没报断言数**。补报一下各自的增量 —— 断言增量律是逐批的,不是逐月的。
2. **门关档「该跑」现在是 12 还是 13?** 新套件 `test-fixture-front-door` 落不落进该跑?(我猜不落 —— 它是静态扫描不取 token —— 但这个数要你现出,不要我猜。)

---

## 八 顺序

```
① 裁 #86      演示白名单回落路销号(最便宜,而且是身份路)
② 裁 #88      撤别名 + 夹具改名 + 未知字段 ci 档拒绝 + 同类扫尽
③ 裁 #89      贴那一行前后(一分钟的事)
④ §六 段二    小程序四步 —— 提到这里,这是有时间成本的那件
⑤ 段 A 剩余    4 套顾客(用绑微信那个样板)+ 3 套员工 + card-refund 三条
⑥ J-61 两款   判据认调用不认提及 · 默认排除自身(做成公共规矩)
⑦ J-60        抽共用出口 + 先转 5 处 + 9 条冻结逐条给理由
⑧ 段 B        撞车队列真写进 identity_merge_queue
⑨ 段 D        裁 #75 / #76 两条举证
```

---

## 九 停线

照旧。**加两条**:

- **凡是「给一个错误的写法加别名/加兼容」的改法,一律停下来问。** 兼容是产品决定,不是修 bug 的手段。
- **凡是为了过棘轮而压行、合并、改写格式的,停下来报。** 棘轮顶破的正确反应是摘出去或请裁。

---

## 十 回执要什么

1. 第一行:两处全 32 位 md5。
2. §一:别名撤了 · 夹具改名了 · 未知字段两档行为 + 造病红行 + 同类扫尽逐条。
3. §二:那一行的**前后原样**。
4. §三:两款各自的落地形状 + 第二款的造病红行。
5. §五:那条回落路谁在用 → 删了还是冻了。
6. §六:段二四步落点 + 「只问一次手机号」判条件原文 + 替身 `session_key` 那一段怎么做的;跑不了的照实写。
7. §四:5 处试水的结果 + 9 条冻结逐条理由 + 棘轮现值。
8. §七:两个数。
9. 两档通过数/总数 + **断言数**;门关档剩余的红逐条。
10. 底数闭合 · 未动对照结构化标记 · `tools/batch-footer.sh` 现出的提交号与读数时刻。
11. 没做完的写「未做,原因 X」。
