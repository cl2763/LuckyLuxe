# 01v 交付:补录小合同建成 + 两小裁 + 一条调查勘正(2026-09-01)

**安全保证:生产库未动 · 本机库未动。** 未推 main。全程只碰沙箱库与回归临时库。

## 〇 · 先勘正:我 01u 的调查有漏,现测咬出来了

01u 我报「老板从台面之外**没有**入口建过去时间的单」。**这句不准确。**
本批建合同前按「归因先于动刀」翻了一次台面日期导航,现测:
**过去日的空档块一直都在,而且写着「+ 直接排单」** —— 入口早就有,只是:
①伪装成"直接排单",店主看不出这是事后补记;②**没有归属日判定**,直排到已日结的日子会污染已确认的账(R1 只能事后标 stale 逼重开)。

我 01u 只看了"今天"的台面(绿区从此刻起画)就下了结论,**凭代码推断没翻页现测**。
正是你刚立的「登记缺陷前先现测」要防的事。**本批第一件事就是复核,不是修。**
→ 所以合同一的实际工作是**改造现有块**(直接排单 → 补录 + 归属判定),不是从零开新入口。

## 一 · 补录小合同六条:全部落地

全文对照表见 `handoff/补录小合同_2026-09-01v.md`。要点:

- **合同一 入口**:过去日块文案「**+ 补录**」、面板标题「补录」、提示「事后补记:这一天的单当时没开,现在补上」、CTA「补录到 HH:MM」、图例同步;**同一条路由** `POST /admin/bookings/direct`(加 `backfill:true`),零新路由;**双端同批**。今天台面**不下发**这块 → 绿区照旧从此刻起画,时间诚实不破。
- **合同二 归属**:`backfillPlanFor()` 唯一出口。未日结 → 落原日;已日结 → **落今天** + 单上「服务发生于 X 月 X 日」,原服务日存 `backfill_service_date`(新列走 ALTER,老库跟上)。**归属日仍只由 `appointment_start` 一个字段决定**,下游(台面/日结/业绩/账本)零改动,不必到处判"这是不是补录单"。
- **合同三 提交前告知**:`schedule-day` 过去日下发 `backfill{serviceDate,targetDate,closed,note,label,hint}`,两端**只渲染不判断**。实拍两句:
  「这单会记到 8 月 30 日(当天还没日结),单、钱、业绩都算那一天。」
  「这单会记到 9 月 1 日(8 月 8 日 已日结,已确认的账不回改),单上会注明服务发生于 8 月 8 日。」
- **合同四 两闸不动**:休息日 `REST_DAY` / 撞位 409 原样吃(`createBooking` 那两处一行未改);补录口另加一闸:**只收过去日**(未来日 400)。
- **合同五 判据**:`test-observe-fixes` 补录 12 项(两形行为 + 三闸 + 双端链 + 前端零判断);`test-store-jury` **新增 I10**(事实级:任一已确认日不得有 backfill 单把预约日也落在该日)。
- **合同六**:撞出一处没写到的,**没动刀**,登记候裁(见下)。

### 变异刀(合同五指定的那一把)

`targetDate = serviceDate`(已日结也写回原日,你最怕的那件事)→ **红**,
且**预览句与落库同时错** —— 恰好证明"提交前那句话"和"实际落哪天"同源(一处真相)。
字节还原(`cmp` 一致)→ 回绿。陪审团 **I1–I10 × 23 家**全过。

## 二 · 两小裁:照裁写进规范表

- **可点的内容卡**:「整张卡装着图文内容的,按容器算,不按按钮算;二形法管的是控件,不是内容卡」——原话写进规范表交叉态一节,维持圆角矩形。
- **展示型徽标**:不是控件、不归二形法;形制归队尾 UI 总审。
两条都从「候裁」栏移到「已定」,标注 ✅,免得下个人再撞。

## 三 · 按〇律自走查(老板端两形实点闭环)

| 走的链 | 结果 |
|---|---|
| 今天台面 → 块=「+ 直接排单」、无补录块 | ✅ 时间诚实不破 |
| 翻到 8月30日(未日结)→ 块=「+ 补录」→ 面板三句齐 → 实点提交 | ✅ 落 **2026-08-30**、无注明句、跟去也生效 |
| 直达 8月8日(已 confirmed)→ 确认句先告知落今天 → 实点提交 | ✅ 落 **2026-09-01**、单上「**服务发生于 8 月 8 日**」、原服务日留痕 |
| 撞位/休息日/未来日三闸 | ✅ 各报各的 |

**自走查又咬出我自己一个漏**:网页 `assemble()` 组装 `dv` 时**漏接** `backfill`(渲染读 `dv.backfill` 却永远是 undefined)——
第一次翻到过去日,块还是「+ 直接排单」。小程序那侧我加了、网页忘了。**已修,并把"组装真的带了"钉成判据**
(静默失败器族:渲染读不到就什么都不显示,不报错)。

自走查数据(补录验客/跟去验客/待写验客/UI轮测客 共 11 单 11 客)已清,走查景干净。

## 回报六项

①**改了什么**:后端 6 处(新列 / `dailyCloseStateOf` / `backfillPlanFor` / 直排接补录 / `serializeBooking` 出句 / `schedule-day` 下发块)+ 双端前端各 5 处 + 判据 13 项(observe-fixes 12 + jury I10)+ 规范表两小裁 + 候裁登记。
②**在哪层验**:渲染层实点(面板三句/块文案/CTA)+ 行为层两形落库 + 三闸现测 + 变异刀归因 + 陪审团逐店。
③**同类扫尽**:前端零处出现日结判断(判据钉死);两端同句同源(后端唯一出口)。
④**反例数据**:未日结日 / 已 confirmed 日 / 休息日 / 未来日 / 撞位 —— 五种都拿真数据打过。
⑤**重复稳定性**:刀红→归因→字节还原→回绿;两形各实点一轮;全量 68 套件。
⑥**残留与不确定**:
  - **补录口撞位措辞**候裁(合同六,未动刀)——详见下节
  - **"已日结日补录到今天、今天再日结"完整下游链**没端到端跑过(归属日就是 `appointment_start`、与普通单同路,但没专门验),下批或走查时补
  - 小程序补录两句的**像素**要你在开发者工具亲看

## 候裁一条(合同六:没写到的回来问)

**补录口的撞位措辞**:补录往过去排是本意,撞位时若走 UNIQUE 兜底分支会报
「这个时段已经过去了…选一个之后的时段」—— 补录语境里这是废建议。裁① 刚终裁过"过去优先",我不擅自翻。
**建议**:仅当 `backfill=true` 时改报「这个时段已经有单了…」,普通排单照裁① 不动。
(附带现测:多数撞位走 `assertBookable` 显式检查、报的**就是**「重叠」;「已过去」只在 UNIQUE 兜底那条路出现,面比原以为的小。)

## 棘轮(请批)

`local-server.mjs` **18,186 → 18,239(+53:新列 ALTER 5 + 归属判定与人话句 26 + 直排接补录 16 + 出句 6)**;
`admin.js` **8,521 → 8,521(0)**;`schedule-board.mjs` 219 → 227(+8,限内)。

**全量回归:68 套件全绿,绿行 2,634 → 2,647**(日志 `handoff/回归日志_2026-09-01v_68套件.log`)。

**下一步**:你说开走那天重铺景+当日重拍回「景=今日,实拍=今日」→ 三轮观看
(重点:小程序侧 78 处胶囊观感,尤其 `.seg`/`.tab` 窄控件;外加本批补录两句)。做完了,等你验。
