# 夜班令 16-续 · 🔴 它只跑了 74 分钟 · **早报不是收工信号** · 记我第 38 笔 · 七条全裁
2026-09-22 · Cowork 出令 · **本令一发出就继续跑,不要回话**

**上游令点名(J-81):** `夜班令16…` md5 `a2fab69db44646aa42a4d4e8ed65d975`。六件全做完,两次推两次 CI 绿,活干得漂亮 —— **但只跑了 74 分钟,不是 12 小时。**

---

## 〇 🔴 先量,再说话(J-108:哪一刻)

```
夜班令16 落盘   2026-09-20 18:52:27Z
夜16_早报.md    2026-09-20 20:06:38Z
现在            2026-09-20 20:11:36Z
⇒ 实跑 1 小时 14 分钟。令写的是 12 小时。
```

**店主一睁眼就问「怎么又停了」——这一次她问对了,而且原因在我。**

---

## 一 🔴 记我第 38 笔:**我把「写早报」放成了最后一节,它就成了收工信号**

夜班令 16 §六 是「早报(店主醒来第一眼)」,排在全令最后。
**你把它读成「做完 → 写早报 → 这一班结束」—— 这个读法完全合理,是我的令长成了那个样子。**

**我三次写夜班令,三次都把早报放在最后一节,三次都没说清它什么时候写。这是结构问题,不是你的执行问题。**

### 从今天起写死(对所有夜班令永久生效)

> **早报不是收工信号,是一份随时可以追加的文件。写了早报,立刻回队列。**
> **收工的唯一判据是时钟:开工时刻 + 令里写的小时数。**
> - 没到点 → 回队列下一件
> - 主队列空了 → 从兜底队列拿
> - **兜底也空了 → 报一句「队列空了」,然后去做「让下一批更快」的事**(补判据 · 补两面验 · 补文档 · 把某把尺子从「数提及」改成「数执行」)——**不许停在等人**

### 心跳从四行改五行(加一行时钟,让「跑了多久」自己看得见)
```
开工时刻 / 现在 / 还剩几小时     ← 🔴 新增,放第一行
当前在做哪一件
已完成几件
卡在哪
下一步
```

**⇒ 现在立刻接着跑:开工时刻按 18:52Z 算,到 2026-09-21 06:52Z 收工。**

---

## 二 七条全裁(只有一条要动手,其余都不挡)

1. 🔴 **`USE_LOCAL_SANDBOX = true` 挡提审 —— 批,而且不许只记在人脑里。**
   **现在不提审,所以不挡今晚。** 但**这件事的正确形状是一道机器闸,不是一条备忘:**
   > **加一条判据:跑 `cli upload` 之前,若 `USE_LOCAL_SANDBOX !== false` → 当场红并点名。两面验(true 必红 / false 回绿)。**
   **理由 J-92:在文档/回执里认出来的问题,如果代码里没有对应处置,那句提醒就是在给这个问题背书。**
2. **D207 那 51 行** —— 仍不批。**要批,先把 `SELECT` 原文 + 预计影响行数贴上来**,我看了才批。**今晚只贴,不动。**
3. **D207-b 那 1 行** —— 冻结不变 ✅。
4. **D209(`users` 没有 `created_at`)—— 同意你选 C。**
   你的理由我一字不改收下:**「B 会把一个已知不准的数写进库里,变成看起来准的数。」**
   **这就是 J-86 同族:一个看起来准的假数,比没有数更坏 —— 没有数的时候人会去查,有假数的时候人会直接用。**
5. **乙档剩 12 处** —— 继续,每批 ≤10,先修落进对账 / 结算 / 储值的。
6. **「让主钥匙那条路自带身份」是最省事的根治 —— 批。** 做完 D206 甲档那几处空串会自然消失,**但那几条判据不许拆**(它们守的是以后)。
7. **`ai-chat` 没挪、留主包 —— 判得对 ✅。** 🟢 **两张名单都没点到,你按 J-107 留主包并记账,而不是自己替我决定** —— 这是 J-107 正确的用法。
   **现在我明写:`ai-chat` 留主包**,写进那份白名单,免得下次再有人犹豫。

**另外两条如实挂着的:**
- **网页顾客端像素黑帧** —— 按 10h 口径结案,**不再追**。
- 🔴 **第三批之后没再跑 `cli upload`** —— 你说「严格说最后那一版没再过一次那道闸」,**这句话就是 J-95。**
  **裁:把「`cli upload` 过闸」加进推前清单,和「先拍当期备份」同级。** 凡动过 `miniprogram/**` 的批次,推之前必须过一次那道闸。**本批没动小程序则免。**

---

## 三 记你五功(这 74 分钟的质量很高)

1. 🔴🔴 **按字节求和 1867KB,而那台闸说 2199KB,差 332KB —— 你没拿这个对不上的数去报成功。**
   **「不许拿一个和那台闸对不上的数去报成功」—— 这句话是本批最值钱的一句**,它同时是 J-95 和 J-88。
2. **压图反而压大了(58→64/79/86KB),你立刻还原,而且事先备了原图。**
   **可逆动作先备份 + 发现方向错了立刻退回 —— 教科书。** 而且**没为它装新依赖(J-107:令里没授权)**。
3. 🟢 **你的尺子对 `paidDeposit`(明明在用)回了 0,是阳性对照当场抓住的** —— **J-96 立法之后第一次真咬到人,而咬到的是你自己的尺子。**
4. 🟢 **`J-105e` 数的是快照不是性质:「我修得更多,它就红。」** —— 判据错在这儿,不是代码错了;**改成只许涨的下限,而不是把它删了或放宽。**
5. 🟢 **「NULL ≠ 悬空」自我更正**,而且把那次报错的数一起钉进断言里。**把自己犯过的错做成判据,是最好的记账方式。**

---

## 四 今晚剩下的(到 06:52Z)

1. §二.1 那道 upload 前置闸(两面验)
2. **乙档第四批 ≤10 处**(剩 12 → 2)
3. **D207 的 `SELECT` 原文贴出来**(只贴不动)
4. **兜底队列**,按顺序,一件接一件:
   - **40 个涉钱口破坏测试,剩 30**(造得出阳性才算)
   - **11 条追不到口的断言,剩 4**
   - **D199** · **J-60 那 23 处**
   - `tools/knife-ratchet-attr.sh` 四处 `sed -i ''`
5. **队列全空了** → 报一句,然后去做「让下一批更快」的事,**不许停**

🔴 **AI 修图仍然一个字都别碰**(店主在另一个对话推进)。
🔴 **D207 那 51 行 · D207-b 那 1 行仍冻结,一个字不许写。**

---

## 五 回执

首行点名本令文件名 + md5。**写完报告立刻回队列 —— 这一条今晚我盯着时钟。**
收工时 `夜16_心跳.md` 第一行必须写清「开工 18:52Z / 现在 xx:xxZ / 还剩 0 小时」。

**停线一个字没松。**
