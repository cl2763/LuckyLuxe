# 夜班令 6 — 八小时(2026-09-09 夜)· **店主今晚亲看小程序,所以小程序按图 + 交互排最前** · 段与段之间不停、不问

> 店主原话:「小程序虽然说是颜色对了,但**字体不对**,可能在**某些 UI 展示方面也不太一样**,总之就是**小程序也没有完全按照图做**;这个版面的**动态轮播效果也不是很自然**,感觉要**等很久**才自动展示下一个数据,而且**让人看不出来这个版面是有自动轮播的**,你需要去**增加一些交互**。」
> 合同 = `handoff/商家端主页重画_两端设计图_2026-09-03.html`(下称「图」)· 规矩同夜班令 5,新增见 §一 · 两处各一份

---

## 〇、段序与预算(做完一段立刻下一段;**段 1–8 是今晚的正题**,段 9–11 够得着就做)

| 段 | 内容 | 预算 |
|---|---|---|
| 1 | **D170 演示面清干净** —— 她小程序截图「今日预约」第一条就是「运营字段测试-mrm0lewr」 | 45 分 |
| 2 | **D171 今日台面文字重叠** —— 她两次都拍到了,灌满数据后每天都会看见 | 60 分 |
| 3 | **D172 深色一次走完 + D183 明暗双模式可选** —— 店主今晚新加的一条 | 90 分 |
| 4 | **D178 小程序按图重排**(五小数一行 / 大数字三段式 / 顶部安全区)| 75 分 |
| 5 | **D179 图 §六「动作 → 结果」整张表落地**(两端,7 条,一条没做)| 75 分 |
| 6 | **D180 轮播手感**(间隔 / 进度可见 / 手势)| 45 分 |
| 7 | **D181 小程序字体:三条路都试过再下结论** | 45 分 |
| 8 | **D177 网页去轮播**(图 §三 无 dots,已裁)| 30 分 |

段 1–8 合 465 分 ≈ 今晚的正题。**段 9–11 够得着就做**:D182 小程序折线改 canvas 按图 · D173/D174/D176(v5 三条口径,见 05u §三)· 六通 v6 + 三端截图 + 对照表。

## 一、规矩(同夜班令 5,记这几条)

1. 每段结束:提交 + 短回执 + `handoff/night-runs/进度_2026-09-09.md` 追加一行(HH:MM 段 N / 提交号 / 全量 N 分 / 断言 ±);**直接下一段,不停不问**;
2. 要裁的写 `待裁_2026-09-09.md`(**#20 起**),跳下一段;
3. 停线照旧:写 4128 既有行停 · 四红线修法不明停 · 回归红 30 分钟定位不到停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询 · 单段超预算一倍停那段跳下一段;
4. 造病还原只走 `knife-backup.sh`(J-34);带图的段**必交截图落仓**(J-32),魔数判据;
5. 回归看门狗 5 分钟照旧;收尾自证两台 `/health` 窗 4 / 封顶 12;
6. 🆕 **J-36 双模式验收律**:凡带界面的段,**浅色 + 深色各一张截图**才算交;两种模式下「功能显示不受影响」的含义是可验的四条 —— **文字不重叠 · 正文对比度 ≥ 4.5:1 · 字体与浅色态同一套 · 数字不折行**;四条任一不过,那一段不算完;
7. 全部完成或全部停住 → 写 `夜班总结_2026-09-09.md`(每段一行 + 待裁几条 + 六通 v6 在哪)→ 退出。

---

## 二、段 1 · D170 演示面清干净(见 05u §四,补一条)

她小程序首页「今日预约 · 此刻之后的前 3 条」第一条就是 **「运营字段测试-mrm0lewr · 经典奶油法式 · Ava Lin」** —— 这条最刺眼,先清。做法照 05u §四(历史评测报价请求置终态、夹具用户的今天与未来预约移出演示三店、J-33 收尾律扩到报价请求与夹具用户),**判据加一条:三店的网页首页 / 小程序首页 / 今日台面,`测试 / 演示 / 闸测 / mrm / 随机段` 字样 0 处**。

## 三、段 2 · 🔴 D171 · 今日台面文字重叠(她两次都拍到了)

**现查(她 09-09 那张北京店深色截图,逐处)**:「10:00–13:00」自己叠着另一个时间;顾客名「颐无涵 3」是两串字叠在一起;`11:30–13:30 箫宴` 压在 `12:00–15:00 罗清和` 上;`14:00–16:00 林书禾` 压在 `14:30–16:30 许听澜` 上;「+ 直接排单」的虚线框与卡片相交。**同一技师、时段交叠的单,全都画在同一列同一层。**

裁:①同一技师列里时段交叠的单,**并排分栏**(两单各占半列、三单各占三分之一),列宽不够时收字号并省略号,**不许互相压**;②「+ 直接排单」的空档框只画在**真空档**上,与任何单不相交;③文字层级:时间 > 顾客名 > 项目,溢出一律 `text-overflow: ellipsis` 单行,不换行不折字;④判据:造一天里同一技师三单两两交叠 → 逐对卡片**包围盒不相交**(DOM 现测 `getBoundingClientRect`),空档框与所有卡片不相交;造病:去掉分栏 → 红;⑤**三店 × 浅色/深色 各一张台面截图**(J-36)。

## 四、段 3 · 🔴 D172 深色一次走完 + 🆕 D183 明暗双模式可选

**店主原话**:「可以选择晚上以及白天两种模式,但**前提都是要不能影响正常功能的显示**,比如字体、比如颜色等等,你看可能还出现了这样的重叠效果之类的。」

1. **D172 深色走完**(照 05u 补一 §二):左侧栏 / 顶栏按钮 / 今日台面 / 待办卡 / 所有写死色 → 令牌;静态判据:`styles.css` 新增写死色(`#fff` / `#fbf8f5` 这类)→ 预检红。**她那张深色截图里白侧栏 + 浅色台面卡,就是这条。**
2. **D183 明暗双模式**:通用设置里加一个三档开关 **浅色 / 深色 / 跟随系统**,默认跟随系统;选择存本地并即时生效(`[data-theme]` 已有的两套令牌就是为它准备的,合同图 line 15–19 写好了);**网页与小程序两端都要**(小程序用 `wx.setStorageSync` + 页面级 class)。
3. **「不影响正常功能显示」= 四条可验**(J-36):两种模式下 ①文字不重叠 ②正文对比度 ≥ 4.5:1(取大数字、正文、次要文字、待办数字四处实测)③字体与浅色态同一套(`font-family` 全等)④数字不折行。判据逐条;造病:把 `--ink` 在深色下换成浅色态的值 → 对比度判据红。
4. 交齐:三店 × 浅色/深色 各一张**整页**截图(侧栏、台面、待办卡都要在画面里)+ 一张对比度实测表(四处 × 两模式)。

## 五、段 4 · 🔴 D178 · 小程序按图重排(逐条对图,证据在下面)

| 图上是什么 | 她截图里是什么 | 图的出处 |
|---|---|---|
| 五个小数 **一行五列**(`grid-template-columns:repeat(5,1fr)`),轮播把谁放大谁就从这行**空出(位置保留、数字淡出)**,其余四个照显 —— **五个数任何时刻都在屏上** | 排成 **两行(3+2)**,且「现金业绩 CAD / $5,760.00」**折行**成两行 | 图 line 69 · 规矩 line 213 |
| 大数字 **三段**:`CAD`(1.2rem 金色 `--herogold`)+ `1,318`(3.3rem Fraunces)+ `.00`(1.6rem,opacity .75) | `CAD $1,318.00` **整串一样大**,与网页端也不一致 | 图 line 51–52 · §一 line 167 |
| 顶行「LUVIA 半径 · 店主,早上好 / 9月9日 周三」完整可读 | 左边被状态栏/胶囊切掉(「LUVIA」的 L 缺一块) | 安全区 |

裁:①五小数**严格一行五列**,数字用 `tabular-nums`,长金额缩字号不折行(`.62rem` 标签 + `1.05rem` 数字,同图 line 71–72);②轮播放大谁,那一格**位置保留、数字淡出**(不是整格消失、不是重排);③大数字三段式,与网页端**同一套写法**(金额出口 `moneyParts` 已有);④顶部加安全区内边距(`env(safe-area-inset-top)` / `wx.getSystemInfo` 胶囊高度),三种机型宽度各截一张。判据:mini5 子节点 5 个且在同一行(`offsetTop` 相等)· 大数字含三个子节点且字号 3:1 递减 · 顶行首字符不被裁(截图 + 左边距 ≥ 胶囊左沿)。造病各一。

## 六、段 5 · 🔴 D179 · 图 §六「动作 → 结果」整张表 —— **一条都没做**

图里明写七行,现仓 `miniprogram/pages/merchant/home/index.js` grep 不到任何对应绑定:

| 动作 | 图上写的结果 |
|---|---|
| 进入首页 / 下拉 | 取 pulse + now + todo 各一次;首页**不轮询** |
| 切维度 | 重取 pulse;大数字**滚动**到新值;折线单位随维度 |
| **点大数字 / 左右滑** | **暂停轮播并切指标;再点继续** |
| **长按大数字 / 点小牌** | 营业收入、现金 → 财务页当期;卡耗 → 储值流水;新增持卡 → 客户库筛「新持卡」;到店人次 → 今日台面 |
| **点今日预约格 / 网页此刻行** | 跳今日台面 |
| 员工点圆形打卡钮 | 走现有考勤接口;成功 → 钮收起、大屏原地浮现;失败 → 钮不收起、钮下说明原因 |
| **点今日要处理的行** | 客服待人工 → 客服工作台;待报价 → 报价试算;待写小记 → 小记列表;调休 → 排班审批;日结 → 日结页 |

裁:**七行逐条落地,两端都要**(网页端已有的照旧,缺的补)。这正是店主说的「你需要去增加一些交互」—— 她要的东西图上早就写了。判据:每行一条(点/滑/长按后的落点页面或状态),七条齐;造病:去掉「点大数字暂停轮播」→ 红。**做不完的行不许悄悄跳过**,写进待裁并在回执列明哪几行没做。

## 七、段 6 · 🔴 D180 · 轮播手感(她三句话都在说这个)

现状:`index.js:200` 固定 **6000ms**,dots 只是五个点。图 §一 **没有写间隔**(6 秒只写在 §五 全屏态 line 377);所以现在这个 6 秒是自己定的。裁:
1. **间隔改 4 秒**(手机上第一屏,4 秒一换既看得清又能感到它在动);全屏态维持 6 秒不动;
2. **让人看得出来它在转**:当前 dot 是金色胶囊(图 line 59 已有),**在它上面叠一条 4 秒走满的进度**(`transition: width 4s linear`,`prefers-reduced-motion` 时不画进度只换点);
3. **首次进入给一次提示**:第一次轮播换指标时,大数字旁淡入一枚极小的「‹ ›」或「自动轮播中」灰字,**2 秒后消失,每台设备只出一次**(存 storage);
4. 手势:左右滑 / 点大数字 → 暂停并切指标,再点继续(与段 3 那一行同一处实现,不许两套);**暂停时 dots 变灰**,让「已暂停」看得见;
5. 判据:间隔 4 秒 ±0.3 · 进度条宽度在 2 秒时约半 · 点一下后 8 秒内 slot 不变(暂停生效)且 dots 变灰 · 再点恢复 · reduce-motion 下无进度条但仍换指标;造病:去掉暂停 → 红。

## 八、段 7 · 🔴 D181 · 小程序字体:**三条路都试过再下结论**

现状 `utils/numfont.js` 抬头写「`wx.loadFontFace` 只吃 https + 域名白名单,本地给不出」→ 判定做不到。**这个结论下早了,还有两条路没试**:
1. **base64 内嵌**:Fraunces 只取**拉丁数字 + 逗号 + 句点**子集(`pyftsubset` 或在线子集化),转 woff2 base64,写进 `wxss` 的 `@font-face { src: url(data:font/woff2;base64,...) }` —— 小程序 wxss 历史上支持 data URI 字体,**必须真跑一次开发者工具验证**,不许凭印象判断;
2. **局域网 https / 开发者工具「不校验域名」**:开发工具里勾掉域名校验,用本机地址 `wx.loadFontFace` 试一次 —— 至少让**开发环境**看得到真字体(生产走上线批的自托管);
3. 都不成 → 维持系统字体,**但要在回执里写清楚试了哪两条、各自失败在哪一步**(不是「不能引外部样式表」这一句)。
判据:①成了 → 大数字 `font-family` 含 Fraunces + 截图;②没成 → `numfont.js` 抬头写明三条路各自的失败点,`/health` 或页面上**不许**出现任何"已加载 Fraunces"的假话。**中文字体接受系统字体**(05u 已裁),这段只谈数字。

## 九、段 8 · D177 网页去轮播(照 05u 补一 §一,六条判据 + 造病 + 同族越界表)


## 十、段 9(够得着)· 🔴 D182 · 小程序折线按图(canvas)

05u 我裁过「柱形可、canvas 单独一批」——**店主今晚说小程序没完全按图,这条改裁:今晚就做**。图 §一 是一条**带渐变填充的折线 + 末点圆环**(line 168–172:`linearGradient` .35 → 0、`stroke-width 2`、末点 `circle r=3.5` 描边 `--hero`)。小程序用 `<canvas type="2d">`:按 `pixelRatio` 缩放、离屏一次画好、随维度重画、全 0 不画(图 §六)。判据:canvas 存在且非空白(取像素采样金色点数 > 0)· 全 0 时不画 · 维度切换后重画;造病:去掉渐变 → 红(采样中间行透明度)。做不成 → 写待裁,**柱形保底不许拆**。

## 十一、段 10–11(够得着)

D173/D174/D176(见 05u §三)· 六通 v6 + 三端截图 + 对照表。

## 十二、交齐

每段:判据 + 造病 + **浅色/深色两张截图**(J-36)· 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 未推 main · 4128 未动 · 两台 `/health` 窗 4/封顶 12。
