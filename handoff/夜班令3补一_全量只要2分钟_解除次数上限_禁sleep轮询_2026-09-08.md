# 夜班令 3 补一 — 段 0/1 收(`0591e45` / `bb015cf`);计时刀量出 **整跑 1 分 59 秒**,我那句「每次约 15 分钟」错了;停线里「全量最多 2 次」**解除**,改成「不许 sleep 轮询」

> 2026-09-08 凌晨 · Cowork(店主在睡)· 两处各一份

## 一、更正(记台账)

Cowork 05o §三 写「五次全量每次约 15 分钟」——那是按日志时间戳倒推的,**错了**。计时刀第一次量:整跑 1 分 59 秒(套件 1 分 47 秒 · 夹具起停 12 秒),最慢三把是小程序刀(41/17/16 秒),其余 ≤5 秒。两个半小时的大头是 Code 起完全量后 `sleep 600~1150` 再去看——**等得慢,不是跑得慢**。计时刀就是为了让这类话有数可依,这条更正照原文进台账。

## 二、停线改一条(其余不变)

- ~~全量回归今晚最多 2 次~~ → **解除**。全量 2 分钟,该跑就跑,每段提交前跑一次也行;
- **新增**:起回归后**前台等它跑完**(`wait` / 同步执行 + 合理超时,预检 1 分钟、全量 5 分钟到点即视为挂起去查),**不许 `sleep N` 轮询**;任何 `sleep` 超过 30 秒都要在回执里写原因;
- 真模型评测(到底率三跑、五通、200 句)一轮一两分钟,也是前台等;
- 预检刀先跑仍然成立(5.7 秒拦可预判的红)。

## 三、段 0 两处收

热修收下并补三条行为判据(POST 0 次 / 「已记上」/ 造 500 「没记上」)——收。`db-snapshot.json` 仓根那个还在(`?? db-snapshot.json`),段 4 别忘。段 2 起照夜班令 3 原表,时间表按新的耗时自然会宽裕,**省下的时间不新开段**,用在段 5/6 的判据与截图上。
