# 《双端欠账总表》(2026-08-28)· 只查不改

> 店主点名的头号件。**这张表只查不改,先交店主排序。**
> 立表的律:**双端同批** —— 凡新做或改做一个功能,若它在另一端也存在、或按已拍口径该存在,
> **必须同一批做完两端**。「网页先做、小程序后补」= 永远不补。

---

## 一、怎么数的(**反着数,不靠列举**)

Cowork 点了七行,但按《白名单判据律》**列举=必漏**。所以被测集合不是功能名,是**机械可枚举的两样东西**:

1. **后端 `/admin/*` 路由全集 155 条**(从 `local-server.mjs` + 全部 `*-routes.mjs` 抠出来),
   逐条查「网页端有没有调」「小程序有没有调」→ 一端调另一端不调 = **欠账候选**;
2. **两端页面/视图清单**(小程序 `app.json` 63 页 vs 网页 `admin.html` 12 个 page + `customer.js` 17 个 view)。

**机核结果**:

| | 条数 |
|---|---|
| 两端都在用 | **84** |
| **只有网页在用** | **35** ← 小程序欠账候选 |
| **只有小程序在用** | **21** ← 网页欠账候选 |
| **两端都没人调**(建了没入口) | **15** ← 第三种欠账,以前没人数过 |

---

## 二、🔴 A 组 · 店主已经裁过要双端、现在还没齐(优先级最高)

| # | 功能 | 店主裁定(台账) | 网页 | 小程序 | 机核证据 | 缺哪一端 |
|---|---|---|---|---|---|---|
| A1 | **客户档案「账户调整」**(充值/赠送/退卡/冲销) | 08-27「账户调整=双端」 | ✅ | ❌ | `/admin/account-adjust/facts` 网页 1 文件 / 小程序 **0** | **小程序** |
| A2 | **退卡口** | 同上(账户调整四段之一);08-25「退卡=财务动作,仅老板+财务密码」 | ✅ | ❌ | `/admin/stored-value/refund` 网页 1 / 小程序 **0** | **小程序**(但门禁口径要一起裁:老板在小程序上算不算够格) |
| A3 | **员工自视业绩「我的业绩」** | 08-27(二)「员工端双端等同」,推翻 08-14 表一行 | ❌ | ✅ | `/admin/my-performance` 网页 **0** / 小程序 1 | **网页** |
| A4 | **员工工作台整体**(工作台/打卡/报价器/我的排班) | 同上 | ❌ | ✅ | 小程序 `merchant/workbench`·`attendance`·`quote-calc` 三页;网页 `admin.html` **零对应 page** | **网页** |
| A5 | **员工补偿自查** | 同上 | ✅(`/admin/my-compensation-estimate`) | ❌ | 网页 1 / 小程序 0;另有 `/admin/salary/my-estimate` **两端都没人调** | **小程序** + 一条死口 |
| A6 | **服务小记 · 图片入口** | 08-27(五)「查明=两边都没有 → 排员工端双端等同批」 | ❌ | ❌ | 两端都只有文本框 | **两端都缺**(新功能) |
| A7 | **开单 / 结算单预览** | 08-27「08-14 表『开单双端保留』那行是错的,事实上只在小程序」 | ❌ | ✅ | `/admin/bookings/direct`·`/admin/settlements/preview` 网页 **0** | **网页**(店主 08-19 已撞过:境外商家没小程序=开不了单) |
| A8 | **会员套餐 / 次卡套餐编辑** | 08-27「会员与营销**编辑**=网页」 | ❌ | ✅ | `/admin/recharge-packages`·`/admin/timecard-packages` 网页 **0** / 小程序 1 | **网页**(现在编辑口在小程序,与裁定相反) |
| A9 | **日结「现金手记」** | 08-28(D79) | ✅ | ✅ | 两端都做了(本批) | ✅ 齐 |
| A10 | **顾客首页轮播自管** | 08-28(D78) | ✅ | ❌ | `/admin/hero-slides` 网页 1 / 小程序 0 | **待裁**:配置类按 08-14 原则①「网页=总和」可只在网页;但按新立的**双端同批**律要一起做 —— 请店主裁 |

---

## 三、B 组 · 只有网页在用的另 25 条(小程序侧未接入)

**判定分三类**:🔴=按已拍口径该双端 · ⚪=合理单端(网页=总和/重规则在网页) · ❓=待店主裁

| 路由 | 是什么 | 判定 |
|---|---|---|
| `/admin/ai/booking-summary`·`/admin/ai/customer-insight` | AI 预约摘要 / 顾客洞察 | ❓ 小程序有 `conversation` 页但不读这两条 |
| `/admin/ai/customer-service/feedback`·`/logic-notes` | 客服工作台反馈与逻辑备注 | ❓ 同上 |
| `/admin/kb`·`/kb/entries`·`/kb/facts`·`/kb/import` | AI 知识库(店规/FAQ) | ⚪ 重规则编辑在网页 |
| `/admin/pricing/rules`·`/pricing/preview`·`/pricing/categories/*` | 价目规则与大类 | ⚪ 同上 |
| `/admin/finance/summary`·`/trend`·`/verify`·`/recurring`·`/compensation`·`/coupon-discounts` | 财务汇总/趋势/核对/固定支出/补偿/券折 | ❓ 小程序 `finance` 页只读了一部分,差哪几块要逐屏对 |
| `/admin/finance/payroll`·`/payroll/confirm` | 工资发放与确认 | ❓ 小程序有 `salary-month`/`salary-plan`,发放确认在网页 |
| `/admin/message-templates`(2 条) | 消息模板(P3 前置) | ⚪ 配置类;P3 上线时再裁 |
| `/admin/reminder-tasks/*` | 提醒任务 | ❓ P3 相关 |
| `/admin/timecard-settings` | 次卡设置 | 🔴 与 A8 同族(编辑该在网页,设置却也只在网页而小程序在卖) |
| `/admin/mall/self-purchase` | 商城自购 | ❓ |
| `/admin/booking-drafts` | 预约草稿 | ❓ |
| `/admin/auth/register` | 注册老板号 | ⚪ 合理单端 |
| `/admin/demo/full-seed`·`/finance-seed` | 演示数据按钮 | ⚪ 合理单端 |
| `/admin/wechat/status`·`/mock-message`·`/mock-chat-message` | 微信模拟器 | ⚪ 网页专属工具 |

---

## 四、C 组 · 只有小程序在用的另 13 条(网页侧未接入)

| 路由 | 是什么 | 判定 |
|---|---|---|
| `/admin/booking-rules` | 预约规则(提前多久/可约范围) | 🔴 **配置类,按「网页=总和」该在网页** |
| `/admin/schedule-settings`·`/schedule-day` | 排班设置 / 按日排班 | 🔴 网页有排班页但走的是别的口,**同一件事两套路由**,要并 |
| `/admin/store-wifi` | 打卡 WiFi 设置 | 🔴 配置类,网页缺 |
| `/admin/segment-rules` | 客群规则 | 🔴 配置类,网页缺 |
| `/admin/membership/members` | 会员名册 | ❓ 网页 `membershipPage` 走别的口,要逐屏对 |
| `/admin/stored-value/txns` | 储值流水 | ❓ 网页顾客档案里有没有等价视图,要逐屏对 |
| `/admin/customers/lookup` | 顾客快查 | ⚪ 到店快捷 |
| `/admin/coupons/redeem` | 核销券 | ⚪ 到店动作 |
| `/admin/attendance/clock` | 打卡 | ⚪ 到店动作 |
| `/admin/published-works` | 已发布作品 | ❓ 网页 `aiGalleryPage` 是否等价,要对 |
| `/admin/recall-digest`·`/staff-nudges/mine` | 召回摘要 / 员工提醒 | ❓ |
| `/admin/tenant/entitlements` | 套餐权益 | ❓ 网页读的是 `/admin/tenant/plan` 之类,要对 |
| `/admin/store-clock` | 门店时钟(今天是哪天) | ⚪ 网页用前端 `storeToday()` |

---

## 五、D 组 · 🔴 两端都没有入口的 15 条(第三种欠账:建了没人用)

**这一类以前没人数过。** 后端有口、两端都不调 —— 要么是死口该退役,要么是功能做了一半没接。

| 路由 | 判定建议 |
|---|---|
| `/admin/recharge-tiers`·`/recharge-tiers*`·`/recharge-tiers/*`(3 个变体) | 充值档位。**三个变体同时存在本身可疑**,先查是不是被 `recharge-packages` 取代了 |
| `/admin/touch-rules` | 触达规则(P3 相关?) |
| `/admin/staff-visibility` | 员工可见范围 —— **与员工端双端等同直接相关** |
| `/admin/salary/my-estimate` | 员工自查工资(与 A5 重叠,两条口做同一件事?) |
| `/admin/identity-merge-queue` | 身份合并队列 —— 有后台逻辑没入口,合并冲突现在没人看得见 |
| `/admin/merchant-leads`(2 条) | 入驻线索(平台侧有 `page-leads`,商家侧这两条没人用) |
| `/admin/finance/deposit-conservation`(+`/repair`) | 定金守恒自检与修复 —— **是个对账工具,没入口=没人跑过** |
| `/admin/finance/change-password` | 财务密码修改 —— 网页有财务设置页但不调它? |
| `/admin/ops/import-db` | 运维导库 | ⚪ 运维专用 |
| `/admin/stored-value/consume` | 手动耗卡 —— **已永久关闭(410)**,可退役 |
| `/admin/users/*` | 顾客明细 | ❓ |

---

## 六、E 组 · 顾客端两端页面差(与 08-14 拍板②「顾客端全功能对齐」对表)

| 页面 | 网页顾客端 | 小程序顾客端 | 状态 |
|---|---|---|---|
| AI 客服 | ❌ | ✅ `ai-chat` | 08-25 已裁「网页 AI 客服排上线后」—— **有裁定,不算欠账** |
| 消息中心 | ❌ | ✅ `messages` | A1 清单 a6 登记为「功能补齐,单独排期」—— **待排** |
| 积分页 | ✅ `pointsMall` | ✅ `points` + `mall` | 结构不同,要逐屏对 |
| 门店位置 | ✅ `store` | ✅ `store-location` | 齐 |
| 其余 15 屏 | — | — | 齐(A1 清单 08-25 已逐屏对过) |

---

## 七、给店主的排序建议(**你排,不是我排**)

按「会不会让她或商家当场卡住」排:

1. **A7 网页开单**(境外商家没有小程序 = 根本开不了单;08-19 她已经撞过一次)
2. **A1 + A2 小程序账户调整与退卡**(她 08-27 裁过,钱的动作只有一端能做)
3. **A3 + A4 + A5 员工端双端等同**(她 08-27 裁过,整块)
4. **A8 + C 组配置四条**(编辑口在小程序,与「网页=总和」的口径相反)
5. **D 组 15 条死口**(先查再退役,便宜且能缩小攻击面)
6. **A10 轮播 / E 组顾客端**(体验类)

**A6(服务小记图片)是新功能不是欠账**,建议单独排。

---

## 八、这张表本身的判据(免得它也变成靠列举)

- 被测集合来自**后端路由表 + app.json + admin.html**,三个都是机械可枚举的;
- 数字可复验:`node` 扫描脚本逻辑写在本文件第一节,任何人重跑应得到 **155 / 84 / 35 / 21 / 15**;
- **建议常驻一条断言**:两端调用面的差集**只许缩不许涨** —— 涨了说明又出现了新的单端功能而没报批。
  这条要等店主排完序、知道哪些是「合理单端」之后才能设基线,**本轮先不设**。

---

## 追加(2026-08-30,店主已裁)

- **RFM 名单动作与阈值微调 → 接回网页,排进拉平批,不插队**(死口清剿总表 #7 的裁定落位)。
  现状:网页客户档案页只有分层统计条,名单动作(按层圈人发券/发提醒)与阈值微调只在小程序客户库。
  拉平批落地时:网页补齐两件 + 删掉指路句(admin.js:4917)。

## 追加(2026-08-31j,店主已裁)

- **营销文案三件(ai/social-copy · recall-copy · daily-brief)→ 补网页**(店主总裁定「两端一模一样」,
  网页营销域接同三口)。**非急件:排在 31j 批之后的小件队列**,登记于此防丢。
  现状:仅小程序营销页在调;网页营销面待接。
- 差集表其余三行 08-31 已裁:深财务+payroll 确认=网页 ✅、客服 AI 深功能=网页 ✅(两行已落
  test-cross-end-effect ⑤ 白名单,理由随行);finance/entry-config 逐屏对=**等价**
  (同一 manualEntryConfig() 出口,网页搭 transactions 车/小程序打 entry-config,⑨c 断言常驻)。

## 勘正(2026-08-31n):营销三件=零欠账,结案

31j 登记的「social-copy/recall-copy/daily-brief 仅小程序」**数错了** —— 差集表那轮机核的输出
`sort | uniq -c | head -16` 被 head 截断,网页侧 ai/ 三行掉在截断线下。现码机核+行为现测勘正:
- 网页**三口早已在用**:daily-brief=老板首页简报面板(admin.html #aiBriefPanel;admin.js:5483)、
  recall-copy=客户档案 S 层「✦ AI 召回」钮(admin.js:5069)、social-copy=AI 图库平台发帖文案
  (admin.js:5533,带 aiCopyHistory 避重);
- 沙箱镜像行为现测:daily-brief 200 回简报句、recall-copy 200 回一人一句(mock 降级形)。
**队列里的「营销三件」撤单**;教训与 #10(排班双写路由推测不成立)同族:清单按现码勘正,
且**机核管道不许 head 截断出报表**(数数的命令里 head=静默失败器,入 J 族)。
