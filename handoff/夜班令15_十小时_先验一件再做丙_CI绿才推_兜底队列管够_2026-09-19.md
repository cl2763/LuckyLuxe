# 夜班令 15 · 十小时 · 先验一件 → 做 (丙) → CI 绿才推 · 兜底队列管够
2026-09-19 夜 · Cowork 出令 · **店主已睡,本令自带全部裁决,不要等人**

---

## 〇 本令合并 09y,店主没单独发过它

`小批09y_否a否b批丙_缺的是夹具数据不是沙箱服务_记我第34笔_立J100_2026-09-19.md`
md5 `98a7cc7ccf0a9a88e8995f8e1b93f8c3`(7,833 字节)—— **在桌面夹里,自己去读全文。**
**本令是它的执行版:凡两者有出入,以本令为准。** 09y 里的裁决(否 a · 否 b · 批 c · J-100 · 我的第 34 笔)**全部有效,不再重复论证。**

---

## 一 🔴 开工第一件事:验一句话,别的都往后排

**在 CI 那几次的注解/日志里搜 `拉不起来`。**

- **搜不到** ⇒ 沙箱在 CI 上起来了,起来的是**空库**;缺的是**夹具数据**,不是沙箱服务 ⇒ 按 §二 做。
- **搜得到** ⇒ **我 09y §一 那一整段作废**,停下来把现场写进 `夜15_第一件的结果.md`,**然后按 §六 兜底队列干别的,不许顺着我的话硬做 (丙)。**

**这一步是 J-84(先问报警器指的是哪一件)。十分钟之内要有结论,写进心跳。**

---

## 二 (丙):让那 4 套在 CI 上真跑

**目标:`未跑 0 套` · `NOT_RUN_CAP` 3 → 0 · CI 绿。**

四条验收,少一条不算做完:
1. CI 上 `correction-reason` · `tenant-ownership` · `tier-label` · `demo-mark` **四套真跑**,收尾那行是 `未跑 0 套`
2. `NOT_RUN_CAP` 由 3 **降到 0**(棘轮只许降,这是降)
3. 种子只对**临时 DATA_DIR** 种 —— **不碰仓内 `apps/api/sandbox-data/`,不碰 4128**
4. 🔴 `ALLOW_DEMO_ADMIN_LOGIN` **只在 CI 那一步存在**,回执里**逐字贴出那几行 yml**

**已替你量过的(不用再查):** `ensureSandbox()` 会自己 `spawn start-sandbox.sh` 并等 24 秒 · `seed-demo-rich.mjs` 写 `demo_seed`(`seed-luvia-bj.mjs` 也写)· B 店顾客看 `seed-jics-mirror.mjs`(自带「只许沙箱」护栏)。
**哪支种子真能造出那两样,要实跑定,不许按名字猜(J-83)。**

**每一支种子都要两面验:种之前那条断言红 → 种之后绿。** 只验绿的那一面不算(J-91)。

---

## 三 推:**条件性,CI 绿才推**

**店主 09-18 夜那句「那就推」仍然有效,今晚不用再问。** 但门槛只有一条:

> **GitHub 那台机器上绿(run id 写进回执)。CI 不绿,一次都不许推。**(J-95)

🔴 **推之前必须重做一次备份** —— 09-18 那份是两天前的,这两天生产还在被顾客用。
备份口径不变:`VACUUM INTO`(**`cp`/`copyFileSync` 无效**)· 拉回本机 · **打开验可读 + 五表行数** · 两头 sha256 对 · 路径与字节数写进回执。

推完:六项核验 · 推后第一次自动备份(**推的时刻 UTC 向后第一个跨过 04:00 UTC 的 6 小时节拍**,不是固定 04:55)· **D194 是否从线上消失,单独报一行。**

---

## 四 🔴 跑一整夜:上一次「跑两三个小时就停」的真因已经量出来了

**夜 14 查实:唯一一次停是工具 5 分钟超时打断了 push(455 提交 · 826 MiB · 实际 34 分钟)。**
⇒ **今晚任何可能超过 5 分钟的动作,一律后台跑 + 轮询,不要撞工具超时。** push / 全量回归 / 长种子都算。

**心跳:每 30 分钟写一份** `夜15_心跳.md`(覆盖同一份即可),四行:
`当前在做哪一件` · `已完成几件` · `卡在哪` · `下一步`
**十小时结束时这份文件的时间戳必须是最后半小时内的** —— 这是「它到底跑了多久」唯一算数的证据。

---

## 五 停线(一个字没松,抄一遍免得记岔)

- 不碰 Railway **任何**配置 / 变量 / volume;`railway` CLI 与面板同权;只读进容器可以,**写文件不行,即使 `/tmp`**
- 不向生产发任何**会写入**的请求(J-72);生产只许 `SELECT`,查询原文先贴等批
- **不抬上限 · 不松判据 · 不换尺子 · 不 `--accept` · 不改合同图凑代码**
- 不看密钥文件;`WECHAT_MINI_TOKEN_SECRET` 不查看不打印不拷贝;口令一律不进聊天/队列/文档
- 不许 sleep 轮询;凡落刀必经造病台;**不许把「本地已删」写成「已修复」**
- **D194 从线上消失之前,不许接通境内代理、不许把备案域名指到后端**
- 🔴 **凡是要在网页顾客端加「注册 / 创建账号 / 新建顾客」的,一律停下来问,不管理由多充分 —— 这一条没有例外**
- 一个 0 不带「扫了几个文件」不许写进报告;批量替换前先量全(J-78)

---

## 六 兜底队列(做完上面还有时间,按顺序往下,不许闲着)

1. **签署文件留档** —— 出**实现计划**(不是动手):前端怎么拆才不破棘轮 · 后端那 +1 行走抽取还是批额度 · 类型机器名。🔴 **外带一件我答应店主自己查的:合同图 v1 的四屏她说看不到,查清是图没出、还是出了没送到,写一行结论给我。**
2. **40 个涉钱接口破坏测试** —— 做完 10,**剩 30**,今晚能推几个是几个,每个都要「造得出阳性」才算数。
3. **11 条追不到口的断言** —— 已 11→4,**把剩下 4 条追到口或说清为什么追不到**。
4. **D199**(门关档那条红)· **J-60 那 23 处**。
5. `tools/knife-ratchet-attr.sh` 四处 `sed -i ''`(macOS 语法,不在 CI 路径上)—— 改成两边都能跑。
6. **预检棘轮跟着 D204 删码降一次**(−8 那笔)。

**队列的意义:不许出现「没活干了就停」。** 夜 14 这一档是 0,今晚也要是 0。

---

## 七 早报(店主醒来第一眼看的那份)

写 `夜15_早报.md`,**顺序写死,不许倒过来写**:

1. **一句话:上线了没有。** 上了写 run id + 线上提交号;没上写**卡在哪一件**
2. **生产动没动。** 没动就明写「生产一个字节没动」
3. **§一 那一件的结果**(有没有「拉不起来」)
4. **(丙) 四条验收逐条**
5. **兜底队列做了几件,分别是哪几件**
6. **你自己新发现的问题**(如果有)—— **严重度必须分「量出来的」与「推出来的」两栏(J-88)**
7. **要店主裁的事**:🔴 **写在最后,而且每一条都要写「这件挡不挡上线」**(J-82)

**回执三行口径(09x 立的,不许省):**
```
本机全量:128/128 · N 条断言 · not ok 0(哪台机器)
GitHub CI:绿 / 红(run id)
GitHub CI 未跑:N 套(逐名)
```

**首行点名本令文件名 + md5。**
