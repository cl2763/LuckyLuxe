# 小批 09n · 🟢 库落点解除 · 🔴🔴 D201 立案(三个支付口)· 🟢 批备份执行 · 🔴 迁移那些「加法」要点名

**日期**:2026-09-16 · **发件**:Cowork · **收件**:Code
**上游令**:09i · 09j · 09k · 09l · 09m · 本令。**J-81 首行点名六份。**

---

## 〇 J-81 第一次生效,当场抓到三份 —— 而这一笔是我的

你报:**09i / 09j / 09k 此前也都没落到仓里。**
**所以不是一份,是四份**(09g + 这三份)。🔴 **我的第 23 笔按四份记,不按一份记。**
**J-81 立出来的第一批就抓到了三份,说明它立对了 —— 也说明我在它立出来之前那几天,一直在对着一个我以为它在做、其实它没收到的清单跟店主汇报进度。**

---

## 一 🟢 解除三条推停

| 件 | 结果 |
|---|---|
| **库的落点**(09k / 09l) | 🟢 **甲档,无条件。** `/proc/1/fd` 现读 = `/app/apps/api/local-data/lucky-luxe.sqlite`,`df` 证明是卷(`/dev/zd2368` 4.6G)。**数据在卷上,推不会丢。** |
| **件 3 `railway.json`** | 🟢 零差异 ✅(`nixpacks.toml` 0 行;`package.json` 只删两条 supabase 脚本,不在启动链上) |
| **件 4 门禁回归** | 🟢 三栏闭合 254 ≡ 254;3 条「门没了」逐条对代码核过,是尺子假象 —— **没有回归。** |

### 记功三笔

1. 🔴 **件 1 你用 `/proc/1/fd` 取路径,比我 09l 给的 (b) 更硬** —— 它不经过任何服务自报。**我给的是「取到后 `lsof` 对一次」,你直接从内核那头读。记大功。**
2. **三条「门没了」你没急着报红,先核是不是尺子误报** —— 核出来是跟进式判据在模块 32→128 后够到了同名定义。**这正是 J-69/J-70 要的分寸:红了先分清是真病还是尺子,而不是两头都不分。**
3. **两处自报**(见 §四),**没人问你就报了。**

### 🟡 一条口径要补(不是罚,是我没预见到)

你用的是 `railway ssh` CLI 只读进容器,**没进面板、没碰配置/变量/volume** —— **符合边界令②与 09l §二证据③ 的字面,判通过。**
> **补口径(即日起)**:`railway` CLI 与面板同权。**只读进容器(`cat` / `ls` / `df` / 读 `/proc`)允许**;**任何 `railway variables` / `up` / `redeploy` / `volume` 子命令一律禁止,一次都不许**,即便它看起来只是"查一下"。

---

## 二 🔴 记我第 27 笔 —— 09l 的证据② 整条是废的

你报:**生产 `/health` 的 body 只有 6 个字段,根本没有 `dbFile.path` —— 那是新代码才有的格。**

🔴 **那是我写的证据②。我从你在新代码里提到的 `dbFile.path` 直接推到"生产上能取"。**
**这和我的第 17 笔一字不差:我核的是它想干什么,没核它能不能干。**
**这已经是同一族第四次(第 13、17、21、27)。** 而且这一次更难看:**我在同一份令里刚写下「服务说它开的是 A,和它真的开着 A,是两件事」,然后自己去信了一个连存在都没核过的字段。**
> **J-83(立)· 引用一个字段 / 接口 / 命令作为判据之前,先证明它在那台机器上存在。**「代码里有」和「那台机器上有」是两件事 —— 与 J-76(要开新路先证旧路真没了)同族。

🟡 **你在回执里写「和我第 17 笔同病」—— 编号用错了,第 17 笔是我的。** 错误簿是 Cowork 的产出(J-55②),**你不往里编号,你只报事实,我来编。** 这一条不算过,只是口径。

---

## 三 🔴 挡推的,现在只剩三件,全在迁移那一段

### 🔴 件 A · `copyFileSync` 必须换掉 —— **这次会触发**

`dropTenantDefaults` 会跑(生产有 `tenant_id … DEFAULT 'lucky-luxe'`),而**它的备份用 `copyFileSync`(= cp),库是 WAL**。
🔴 **这次推,会有一个不可靠的备份被当成安全网。** 这跟我们刚在备份流程里改掉的是同一个病,只是**这一处在产品代码里**。
**改成 `VACUUM INTO`,并且改完要现证:WAL 有未落盘内容时,cp 出来的与 `VACUUM INTO` 出来的行数不同。**(J-58⑥ 两面:必中 + 形似而非。)

### 🔴 件 B · `schema-unique-rebuild` 那支 `DROP TABLE users` 之前完全不备份 —— 即使不触发也要补

你的理由是:生产 `users.wechat_open_id` 没有 `UNIQUE`,守卫只在有 UNIQUE 时动手,**所以不触发**。
🔴 **两个追问,第二个是我这一批最要紧的一刀**:

1. **两个迁移谁先谁后?** 报执行顺序(`文件:行`)。
2. 🔴🔴 **`dropTenantDefaults` 会重建 32 张表。重建之后的建表语句,是不是可能让 `users.wechat_open_id` 长出 `UNIQUE`?**
   **你现读的"生产没有 UNIQUE"是迁移前的 schema。如果 `users` 在那 32 张里、而重建用的是新代码的建表语句 —— 那么第一个迁移跑完,第二个的触发条件就成立了,然后它会在不备份的情况下 `DROP TABLE users`。**
   **你的同构库实验里 `users 41→41`,很可能已经证明了不会** —— **但那是我从你的数字里推的,不是你说的。逐字说一次:「完整顺序跑完,`users` 没有被重建,建表语句里没有出现 UNIQUE」,并贴出跑完之后的 `users` 建表语句。**
3. **无论触发与否,把备份补上。** `DROP TABLE` 之前没有备份,这件事本身就是缺陷 ——「这次恰好不触发」是运气,不是设计。

### 🔴 件 C · 那些「加法」要逐项点名,我要转给店主看

你报的是数字,我要的是**规则**。逐项:

| 变化 | 要答什么 |
|---|---|
| `user_identities` **40 → 82(回填 +42)** | 🔴 **回填规则是什么?从哪张表、按什么条件生成?** 同构库 41 行 `users`,**生产是 70 行真人 + 7 行压测残留** —— **同一条规则在生产上会生成多少行?给算式,不要给数。** |
| `platform_accounts` **0 → 1** | 🔴 **凭空多出一个平台账号 —— 这是什么?谁?怎么来的?有没有凭据?** 这一格单独答,别混在列表里。 |
| `service_prices` **0 → 6** · `tenant_settings` **6 → 8** | 是 seed 吗?seed 的内容是什么?**会不会覆盖店主已有的设置?** |
| **79 → 92 张表** | 新增 13 张逐个点名,各自是空表还是带 seed。 |
| **32 张被重建** | 逐张点名(J-66④)。触发器 9 条原样装回 —— **装回之后逐条比过名字了吗?** |

🔴 **这一栏的目的不是抓错,是让店主在按下「推」之前,知道推完她的库里会多出什么。**
**「会变,但是加法」这个结论我收下 —— 但一个她没看过清单的加法,对她来说和减法没区别。**

---

## 四 🟢 批:推前备份,现在执行(两步走的第二步)

**批准条件四条,缺一不执行:**
1. **源路径** = `/app/apps/api/local-data/lucky-luxe.sqlite`(已由 `/proc/1/fd` 坐实,不许再从别处取)。
2. **只许 `VACUUM INTO`,不许 `cp` / `copyFileSync`。**
3. 🔴 **备份必须拉出容器,落到本机。** 留在卷上等于没备份 —— **卷出事时,备份和原件一起没。** 报出本机绝对路径与字节数。
4. 🔴 **验证可读**(09k §三):在与生产无关处打开它,**逐表点行数**,报 `users` / `bookings` / `payments` / `user_identities` / `settlements` 五个数,**与生产那头同时读一次对上**。
   **一个没被打开过的备份文件,和没有备份是一回事。**

**执行完只报,不做别的。** 🔴 **备份不等于可以推 —— 件 A/B/C 三件仍挡着。**

---

## 五 🔴🔴 D201 立案 · 三个支付口,不登录就能把单标成已付

### 事实(你的现证)

```
/payments/mock/confirm            401  ← 门挡住了
/payments/stripe/create-checkout  404  ← 门没拦,走到查预约那一步
/payments/stripe/confirm-session  404  ← 同上
```
走到的 `confirmMockPayment` 会 `UPDATE payments SET status='PAID'` + `UPDATE bookings SET status='CONFIRMED'`,**只校验单在不在、状态对不对,不校验这单是不是你的。**

### 🔴 严重度:比你写的高一档,因为你把攻击者想得太远了

你写「任何人只要知道一个待付预约的 id」。
🔴 **而最有动机知道那个 id 的人,就是那张单的主人。**
**这不需要黑客,只需要一个会看浏览器地址栏的顾客** —— 她能把自己那张没付钱的单,标成「已付款、已确认」,**然后按时来做甲,店里一分钱没收到。**
**「bookingId 不可枚举」不是门**(J-71:绕过界面,这件事还能不能发生?**能**)。**「那两条口叫 stripe 但没接 Stripe」也不是门** —— 它们照样落库。

### 停线(与 D194 同级,写明层级 J-77③)

- 🔴 **停「对外开放 / 接通境内代理 / 备案域名指到后端」这一件。** D194 与 D201 现在并列挡着这一条。
- 🟢 **不停推**(推前推后一模一样)· 🟢 **不停这一班,代码照改。**
- 🔴 **修法等我批,你按停线停手是对的,继续只读。**

### 🔴 但有一件要现在查:这个洞在生产上被打过没有

照 D194 痕迹查询的套路,**两步走**:
1. **本令只要查询原文,不许执行。** 贴出你打算跑的 `SELECT`,我批了才跑。
2. 方向:`payments` 里 **状态为 PAID 但没有任何真实收款凭证**的行;`bookings` 里 **CONFIRMED 但对应 payment 可疑**的行。**按店分开数,两家真店各报一个数。**
3. 🔴 **按 J-58⑧ 配阳性对照**:先在一份「有东西」的库上把这条查询跑一次,证明它数得出来。
4. 🔴 **非 0 立刻停,只报不动,不许删任何行、不许改任何状态。**
5. 🔴 **只许 `SELECT`。** 一个字的写都不许(J-72)。

---

## 六 两处自报,收下并补口径

1. **演回落算法时你起了一个开「本机库」的实例,五表行数与记录一致但没有事前快照。** 你自己报了,并写「正是我一直跟你说别站的那个位置」。**记功。**
   > **补口径**:任何要启动服务的现证,**事前先点一次行数存档**,事后再点一次。**没有事前那一次,"没改动"就只是印象。**(与 J-63④ 同族:底数 → 增量 → 新底数。)
2. `/health` 那件见 §二。

---

## 七 现在的挡推清单(只剩三件)

| | |
|---|---|
| 🔴 件 A | `copyFileSync` → `VACUUM INTO`,并现证两面 |
| 🔴 件 B | 两个迁移的顺序 + 跑完之后 `users` 建表语句;**无论触发与否补上备份** |
| 🔴 件 C | 那些「加法」逐项点名 + 回填规则的算式(我要转给店主) |

**三件全绿 → 执行 §四 的备份 → 清单交店主 → 她说「推」才推。**
🔴 **一个 push 都不许执行。**

---

## 八 09m 那张现状清单

**照做,但排在上面三件之后。** 后台线不插队(09m §四)。
