# 小批03z · 事务九处 + 三条只答验收(2026-09-03)

## 〇、亲核

`15a3a21`;`ledger-guards.mjs` 98–103 已 `BEGIN IMMEDIATE / COMMIT / ROLLBACK` 包住 12 组 DROP+CREATE;`test-txn-rollback` 4/4;`test-untouched-proof` 7/7 含新加的 ②c(只改列数也必红);基线 86 套 2,854(+5 = 4+1)。**收。**

两处记名:①**清单说「包了事务」的地方,现测第一件事就发现根本没有事务**——`ledger-guards` 一句 `db.exec` 跑 24 条 DDL,第 7 句失败前 6 条触发器已经没了,恰恰是它自己注释里警告的样子;这是「三列清单第三列不许草草打勾」的正面案例,清单是人写的、刀是机器咬的,以刀为准。②反向守抓出你把列名写成 `kind`——没有反向守,第二步会靠「插不进去」假绿。

## 一、一处小收尾:三列清单由刀生成,不再手写

`handoff/写库脚本护栏三列清单_2026-09-02.md` 还停在 09-02 的手写状态(第二列 10 处 ⬜ 早已被 D124 接上护栏、第三列的九处也已验回滚,清单没跟着动)。**清单改成由 `test-db-target-guard` + `test-txn-rollback` 的输出生成**(跑一次刀就重写这份文件),手写的那份作废。判据自述与文档自述都得跟行为一致,文档也不例外。

## 二、三条只答:收,处置如下

- **D109**:后端 6365/6375 拦得住,前端徽章只显示——结论收;「点进去日历置灰」归主页重画批(它动的正是那一区)。
- **D112**:两句写死英文在 `home/index.wxml:102/121`——**等主页批一并做**,不现在摘(入册不现修;那一区整块要重画,改两行再推倒是白做)。
- **D113**:自定义 tab bar 改得动——形制随主页图;图上没画底部导航的形制,主页批交齐后单独补一小步,我到时给一行规格。

## 三、顺序

拆档迁移脚本 + 沙箱彩排 → AI 复测 12 场景(03t §三)→ 结案批交齐 → **主页重画批**开工(图 v3.1 已是合同)。
