# 小批 06e — **配色两档都拍板了**(店主:深色「可以」· 浅色「浅色也可以」)· **合同图已改到 v3.4**,`design-token-diff` 现在报 **7 块红 —— 那是对的** · 你的活就是**把图搬进两份代码**(网页 3 块 + **小程序 4 块**,站内浅色那块最容易漏)· 搬完两档全站重扫,交**新旧欠账对照**

> 2026-09-10 · Cowork 现做:`handoff/商家端主页重画_两端设计图_2026-09-03.html` 已改(备份 `*.before-palette-20260910-143944`);三段 `:root` 各 30 条、名字一个不增不减;`node tools/design-token-diff.mjs` 现输出 **共 21 条 · 红 7 条** —— 逐条点名「图=X 仓=Y」。**这份输出就是工单,不用我再抄一遍值。** · 两处各一份

## 〇、先记我一笔

我在深色档提案里写「**只改 13 个值**」,而表里逐行数下来是 **14 个**(`--paper --ink --sub --line --card --wall --hero2 --herosub --goodbg --badbg --flat --flatbg --done --next`)。**数错的是我,不是表。**
**以表为准:深色 14 条 · 浅色 6 条 · 合计 20 个值。** 令牌名 30 条一个不增不减。
(顺带自查:提案里那句「其余 17 个不动」也应是 **16 个**。)

## 一、这一批要做什么

**把图搬进代码,不是照我的话改代码。** 刀已经把每一条差异点名了,按它的输出搬:

| 文件 | 几块 | 提醒 |
|---|---|---|
| `apps/web/design-tokens.css` | **3 块**(light · system-dark · theme-dark) | 深色的值在 2 块里各出现一次 |
| `miniprogram/styles/tokens.wxss` | **4 块**(`page,.theme-root` · `@media…page,.theme-root` · `.theme-dark` · `.theme-light`) | 🔴 **浅色的值出现两次**(第 18 行和第 38 行)—— 店主专门提醒过「小程序也要引用这两套色系」,**站内选浅色那一块最容易漏** |

搬完 `node tools/design-token-diff.mjs` 必须 **21 条全绿**。
**不许反过来改图去凑代码** —— 图是合同,店主刚拍的板。

## 二、顺带把这三件一起落(它们和配色是同一件事)

1. **主按钮**(接 `06c §二`,店主已认建议):金底不动,**字改深** —— 深色档 `--brand #d3b483` 底 + `#221c17` 字(**8.52**);浅色档 `--brand #b09468` 底 + `--hero #2a2521` 字(**5.26**)。再把按钮字号/字重推到大字档(≥18.66px 且 700),门槛降到 3.0,稳过。
2. **用法规矩甲**:**浅色档 `--brand` 不许当正文字色**(压白 2.88),当字要用 `--brandd #8a6f45`(4.73)。判据:浅色档下 `color:var(--brand)` 的**正文级**用法 0 处;造病:把「查看全部 ›」改回 `--brand` → 红并打印比值。
3. **用法规矩乙**:**`--done` / `--next` 只当色块,不当字色**(浅色 `--done` 压白 1.81)。判据同族。

## 三、搬完要交的:两档全站重扫 + **新旧欠账对照**

用你已经写好的 `tools/contrast-sweep.mjs`,**浅深两档 × 后台 13 页 × 顾客端网页 4 页 × 小程序顾客端 6 页**,再跑一遍:

- **甲档「看不见」必须仍是 0**(换了底色之后不许冒出新的);
- **乙档「AA 欠账」交新旧对照**:上一轮是 **106 处(去重)**,这一轮是多少?**逐条列出哪些因为改色消失了、哪些还在**。我预计会大幅缩水,因为欠账里最常出现的就是 `--sub` `--flat` `--good` `--brand` 这几个 —— **但预计不算数,跑出来的数才算**。
- 🔴 **照 J-39**:两份红榜抬头都要印**刀的提交号 + 被测代码提交号 + 界面文件内容指纹**,而且**新旧两份必须同尺**(同一版 `contrast-sweep.mjs`)。跨尺比较这条我上一轮已经咬过一次了。
- 🔴 **照 J-38**:交付图带拍摄时刻;**图必须拍在改完之后**。

**要图**:小程序商家首页 + 「我的/管理」菜单页 + 今日考勤页,**每页浅深两张**(店主就是在这三页上看出问题的),用你已跑通的 `screencapture -l <windowid>` 那条路。

## 四、还欠着的(顺序排在配色之后)

`小批06c §三` 的 **D188 四条**(令牌引用统一入口 + 静态判据 + 运行判据 + 全仓 html 扫一遍)——**这一批之后第一件**。
理由:D188 治的正是「令牌文件没被引进来」,而这一批刚好大改令牌值 —— **先把值搬对,再把「引不引得到」焊死**,顺序是对的。

`小批06d` 那两条(上线批清单合成一份带读数时刻 · `pkill` 白名单判据)照旧排在后面。

## 五、交齐

`design-token-diff` **21 条全绿**的输出原文 · 两份代码各改了几块几条 · 主按钮两档判据 + 造病 · 用法规矩甲乙各判据 + 造病 · 两档全站红榜(**新旧同尺 + 带刀提交号**)· **乙档新旧逐条对照** · 三页 × 两档截图(带拍摄时刻)· 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 写库自报(三库分开)· **未推 main**。

**停线照旧**:写 4128 既有行停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询 · **不许松判据/抬棘轮/换尺子** · **不许改合同图来凑代码**。
