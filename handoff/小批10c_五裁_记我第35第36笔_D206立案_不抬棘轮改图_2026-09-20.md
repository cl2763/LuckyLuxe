# 小批 10c · 五件全裁 · 🔴 记我第 35、36 笔 · D206 立案 · 棘轮不抬,改图
2026-09-20 · Cowork 出令

**上游令点名(J-81):** `小批10a…` md5 `76e8d67fad0d849d0f1b2c97c38a1925` · `小批10b…` md5 `f4ccff4b0da4e890345a23b66a10dff1` —— 回执收到,两份已落仓。

---

## 〇 🔴 先记我自己的两笔

### 第 35 笔 —— 我批的那条豁免,理由是假的,而我没去看那一行

你 09o 报「那处 `copyFileSync` 跑在接口线程里、导入事务之前」,我 09x **照着这句话批了豁免**,还替它写了漂亮的理由。
**你这次现测:它在第 137 行、模块顶层、开机时跑,而库连接第 142 行才建 —— 142 行之前一个连接都没有。**

**更难看的是丙档:delete 模式 + 握着写事务,`VACUUM INTO` 照样 2ms 通过。**
⇒ **「会打架」这个前提从来没成立过。** 所以 10a §二 我写的 J-104(条件变了豁免到期)**这次根本用不上** —— 真相不是「它过期了」,是**它一开始就不该被批**。

**我的错在于:我审的是「这个理由讲不讲得通」,而不是「这个理由挂的那个事实对不对」。**

### J-83 补一款(不另立新法)
> **J-83②:一个关于「这段代码跑在什么位置 / 什么时机」的说法,必须去看那一行本身。位置是可以一眼查证的事实,不许听转述。**
> 原 J-83 管「这个字段/命令在不在那台机器上」,②管「这段代码在不在那个位置上」。**同一件事:引用之前先看。**

### 第 36 笔 —— 我在同一份令里自己打自己

10b §〇 我一边写「**以图 v2 为准**」,一边在表里写了**护理同意书** —— 而图上三处都是**价格表确认书**。
**我抄了你计划里的字,没跟我自己画的图对一遍。** 这是 J-79/J-80 同族(同一份产出里自相矛盾)。
🟢 **你没有照我的字做,而是照图做,并且把差异摆到台面上 —— 判得对,记一功。**

---

## 一 五件裁决

### ① `pricelist` vs `consent` —— **`pricelist`,你判对了**

**机器名 `pricelist`,中文「价格表确认书」,三处以图为准。**
你那句「**一个叫 `consent` 的字段装价格表,是给下一个人埋雷**」—— 收进台账,这是 J-94 同族(名字要对得上里面装的东西)。
**图不用改,改的是我 10b §〇 那张表。此条已更正。**

### ② 金底无字棘轮 16 → 17 —— 🔴 **不抬。改图。**

**你按判据自己写的机制走(抬一格 + 具名 + 摆进要裁清单),程序上完全正确,记一功。但我不走那一支,因为有更便宜的一条:**

**那个金色选中点是我画的,而图的作者是我 —— 所以要改的是图,不是棘轮。**

> **图 v2.1:`.opt.on .k` 选中点由品牌金 `--brandd` 改为主色墨绿 `--accd` 实心 + `--card` 内环。**

三个理由:
1. **两档都稳** —— 墨绿是双档主色,不靠金色在深色档上撑;
2. **棘轮不动**(16 = 16),**判据一个字不改**;
3. **系统上也更对** —— 金色在这套里是装饰色,「选中」是状态,状态该用主色。

🔴 **而你报的那句话,是这一批里最值钱的一句:**
> **「我在浏览器里看过那个金色按钮 —— 浅色档下白字金底,看上去完全正确,而 `test-color-usage` 才知道深色档下它会塌。」**
**这就是判据存在的全部理由:它管的是你看不见的那一档。** 记一功。

**如果你实测墨绿在那一格视觉上不对,回来说,我再改图 —— 但不抬棘轮这一条不变。**

### ③ `cosDeleteObject` 全仓零调用方 —— **删**

理由三条:①死码不白占棘轮 ②**少一个将来会被误用的删除口** —— 而这一整件的主题就是「不许删」 ③J-102:没人跑的东西坏了也没人知道,一个零调用方的删除函数正是那种东西。
**要用的那天从 git 历史取回来,一条命令的事。**

### ④ 35 份日备全是 `cp` 出来的 —— 🔴 **先量,不要删,也不要假设它们能用**

**这是今天最有价值的一条,而且它让我们手上的「备份」这件事变得不确定。**

**要做的(容器内只读,内联 `node -e`,不写文件):**
- **抽 3 份**(最新 / 中间 / 最旧)逐份:能不能打开 · `PRAGMA integrity_check` · 表数 · 五表行数
- **三份全绿** ⇒ 写一行「**侥幸,不可依赖**」:WAL 上 `cp` 能出好文件只是恰好 checkpoint 干净,不是它安全
- **任一份坏** ⇒ 立刻在台账里写死一句:**「09-21 04:55 之前,我们手上唯一可靠的生产备份,是 09-20 补拍那一份。」**

🔴 **不管哪种结果:在第一份 `VACUUM INTO` 日备落下来(09-21 04:55 UTC)之前,不许拿任何一份旧日备做还原。** 这一条挡还原,不挡开发。

### ⑤ 屏1 整块像素图没拿到 —— **判得对,不挡第二步,但第一步收尾前补一次**

🟢 **「按 L3 挂着,不写成已核验」—— 这正是我要的:拿不到就说拿不到,不许用逐字核对冒充像素核对。**
补法换一个:缩窗 / 把区块滚到视口顶 / 或像你验弹层那样找一个 `position:fixed` 的容器取像素。
**两次都拿不到,就在交付清单里写死一句「这一屏只有逐字核对,没有整块像素图」,不许含糊过去。**

---

## 二 🔴 D206 立案:主钥匙那条路不带身份,留痕会是空的 —— 这不止这一件

你查实:`OWNER_TOKEN` 那条路 `requireAdmin` **一个身份字段都不带**,所以 `voided_by` 会是空串。你在这一件里改成「要么说得出是谁,要么报错」—— **对**。

**但这是平台级的:凡是用主钥匙做的写操作,只要要记 `*_by` / `*_by_name`,留痕都会是空的。**

**立 D206,排第二段:扫全仓,列出所有会写 `*_by`/`created_by`/`updated_by`/`voided_by` 的写路径,逐条标「主钥匙走这条路时留痕是空的吗」。**
**先只要清单,不要改 —— 我要先看见面有多大。**

---

## 三 两件要你在下一份回执里补清楚

1. **`user_identities` 105 → 181(+76)** —— 这是**开机迁移在生产库上写了 76 行**。
   🔴 **问题只有一个:这 76 行,在推前那份「推完会多出什么」清单里预告过吗?**
   - 预告过 ⇒ 一行确认即可
   - **没预告** ⇒ 那就是「推完多出来的东西没在清单上」,要单独记一笔,并说清回填逻辑与数据来源(`users` 仍 77,人没多,那多出来的身份行是从哪来的)。
2. **CI 未推** —— 同意你先攒着。**但攒到第一步收尾就推一次,不要攒到第二步做完。**
   口径:**小批上线** —— 每次上线的改动越小,出事时越容易看出是哪一件。
   顺序:第一步收尾 → 推 → CI 绿 → 部署 → **轻量六项**(`/health` commit · 两家真店零差异 · 新表在且为空 · 触发器数 · 备份件指纹 · 版本指纹)→ 报我 → **我批了才开第二步**。

---

## 四 记功:这一批你交出的东西,质量是这一整案里最高的

- **十几个缺陷,全是判据抓的,没有一个是我或店主发现的** —— 这就是「判据是主体」该有的样子
- 🔴 **`voided_by` 空串**:主钥匙那条路留痕丢了「谁」——**一个记不住是谁干的作废,在纠纷里等于没作废**
- 🔴 **`window.owner` 是 undefined,那条「用户切走就丢弃」的守卫一次都没生效** —— 静默失败器教科书
- 🔴 **抽取漏了调用方(`/admin/store-clock` 500)**,而且你**当场给它装了血缘护栏**:抽出去的名字,调用方一个都不许掉队 —— **这比修好那一个 500 值钱得多**
- **「数提及不数执行」你自己认了三次,第三次不再打补丁,抽出 `codeOnly()` 统一走** —— 🟢 **第三次才是对的做法,而你自己说了「如实记」**
- **`tools/db-backup.mjs` 在生产容器里崩,而且 `VACUUM` 其实做完了、命令却报失败** —— J-102 立法当天就兑现一次,**而最坏那一面(做成了却说失败)你点出来了**
- **④c 判据标签写「≤1」代码写 `<=2`,说谎的方向恰好是放松那一边** —— **判据自己在说谎**,这一条我收进台账当例子

---

## 五 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. `pricelist` 三处改完(图/码/判据口径一致)
2. 选中点改墨绿后:棘轮仍 16 · `test-color-usage` 绿 · **深色档实拍一张**
3. `cosDeleteObject` 已删 + 棘轮数
4. **三份旧日备的体检结果**(能不能开 / integrity / 表数),以及台账那一句写成哪一版
5. 屏1 像素图补到没有;没补到就按 §一⑤ 写死那句话
6. **D206 清单**(只列不改)
7. `user_identities` +76 有没有预告
8. 第一步收尾 → 推 → 轻量六项 → **报我,等批,再开第二步**

**停线一个字没松。** 顾客端第二步没我批准不许开工;顾客端加建号口一律停下问,没有例外。
