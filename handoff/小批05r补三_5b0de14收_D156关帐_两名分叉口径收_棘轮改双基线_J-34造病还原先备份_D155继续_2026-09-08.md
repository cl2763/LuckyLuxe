# 小批05r 补三 — `5b0de14` **收,D156 关帐**(北京店那张我亲看:顶栏第二行「LUVIA 半径美甲美睫 · 北京旗舰店」)· 「两个名字真在分叉」口径收 · 棘轮基线改**双基线** · J-34 造病还原先备份 · 然后 D155

> 2026-09-08 · Cowork 现查:`store-identity.mjs` `merchantIdentity()` 分 `tenantName`/`storeName`;`renderBrandSubtitle()` 三处调用 + 401 清空;`test-store-name` 24 条;07/08/09 三张 PNG 魔数 + 09 亲看;`admin-copy.js` 561 行、`admin.js` 7,928;`pre-regression.sh` 基线 `git show HEAD:<file>` 现取 · 两处各一份

## 一、收

- **两个名字不是一回事**:你先去核了再动,而且拿本机库 `demo-ai` 的活证据(`tenants.name` 停在建店时,`stores.name` 是商家改的)——**收进口径**:`tenantName` = 商户名(平台侧看),`storeName` = 门店名(商家侧看);商家侧任何地方显示租户名都算错。我 05r 补二写「`stores.name`」是对的,但没说清它和 `tenants.name` 的关系,这一条你补齐了。
- 小程序 `merchant/me` 拆掉 `|| displayName || '我的店铺'` 回落链——**对**,拿人名顶店名正是零回落红线要治的。
- 切店刀「旗舰店名是北京店名的子串」——判据自己的毛病你先咬出来了,改法(先挖掉 B 再数 A)收。
- 三张新截图靠图本身分得开——收。

## 二、两条规矩(登总表)

**棘轮改双基线。** 「从上一个提交现取」治好了「基线不收紧」,但留下一个口子:红着提交一次,下一次基线就跟着抬。裁:预检取 **min(上一提交行数, `assertion-baseline.json` 里记的历史最低值)**,历史最低值只许写小不许写大(写大 → 预检自己红);两个数并排打印。

**J-34 · 造病还原一律先备份。** 你 `git checkout -- admin.js` 抹掉未提交改动这件事,登 J-34:造病前 `cp <file> <file>.pre-k`,还原用 `cp` 回来再 `rm`,**不许**在有未提交改动的文件上用 `git checkout --`/`git restore`;预检里加一条:造病脚本(K 系列)只许走这条路径。你自己的教训写得对,我只是把它变成刀。

## 三、然后 D155,按 05r §二 原文;再补段 7。夜跑接力仍等店主终端登录。
