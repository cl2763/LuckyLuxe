# 小批05b — 大批05 ⓪ 对话全录验收(`e931f1d`/`d141619`:收,补一步「脱敏正门」)· 放行 ①门 + 三档

> 2026-09-03 夜 · Cowork 亲核于店主 Mac · 两处各一份

## 〇、亲核
- 本机库 `conversation_messages`:12 列,`tenant_id/role/source/content NOT NULL`;唯一索引 `(tenant_id, channel_msg_id) WHERE channel_msg_id IS NOT NULL`;触发器 `conv_msg_no_delete` + `conv_msg_no_update`(锁 content/role/source/conversation_id/tenant_id/created_at)。
- 迁移:表 1,293 行 = `transcript_json` 逐通条数之和 1,293(assistant 718+26 / customer 515 / staff 34),来源栏 `migrate:*` 可辨。
- 五条路:`origin 5` 落 `role=staff`(1571–1579);顾客非文本六种占位 + 附件引用(1603–1616);第二漏斗 `recordWecomConversation` 收尾补落(2146);全仓漏斗恰两条(3441 注)。
- 新套件 `test-conversation-log` ①–⑨(追加锁行为、逐条一致、两店同号隔离、幂等、origin 5 与非文本静态);`conversation-log.mjs` 150 行;92 套;未推 main 96。

## 一、裁
**⓪ 收**,但图 §〇 第 4 条「顾客要求删除时脱敏不删行」现在**做不到**:`content` 被 `no_update` 锁死,触发器提示写着「redact identity fields instead」,可没有一条路能 redact。补 **⓪b 脱敏正门**(小,同 D122 事由必填一族):
1. 唯一入口 `POST /admin/conversations/:id/redact`(老板权限,事由必填 `REASON_REQUIRED`);把该会话在 `conversation_messages` 与 `users` 身份字段里的顾客侧内容替换为固定标记 `[已脱敏 YYYY-MM-DD]`,**行数不变**;写一行操作留痕(谁/何时/事由/影响行数)。
2. 触发器加一条例外:仅当 `NEW.content = '[已脱敏 …]'` 且 `NEW.redacted_at IS NOT NULL` 时允许 UPDATE content;其余一律 ABORT(加列 `redacted_at`)。
3. 判据:脱敏后行数不变、内容全为标记、`transcript_json` 同步为标记、留痕 +1;绕过正门直接 UPDATE 成别的文案 → 仍拒;造病:去掉例外条件 → 任意 UPDATE 都能过 → 红。
4. `intent`/`staff_name`/`attachments_json` 可更新这一点写进口径(学习标注要用),其余列锁死不变。

## 二、放行
⓪b 交齐随 ① 一起回执即可,**① 门 + 三档现在开工**:先按图 §五 造 200 句 + 80 条评测集,跑「关键词门」记基线,再实现模型门,四数并排达标才换门;D133 反面判据、只有 `human_active` 静默;关键词表降级为快速通道。红了先归因;只打 4310;`AI_REQUIRE_REAL=true`。
