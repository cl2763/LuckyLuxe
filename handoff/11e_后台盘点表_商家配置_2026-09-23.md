# 11e · 平台后台「商家配置」盘点表(**只列不做**)

> **量的是什么**:`apps/web/platform.html`(6 个标签页)+ `apps/api/local-server.mjs` 的对应口
> **哪一刻**:2026-09-23 · **对着的图**:《有迹新店入驻向导》五步表(09b §一,合同图 `3CyNmBJ9BDuSNbvVQUg7vJ`)
> 🔴 **不是** 8 月那份《平台后台_全站UI原型》(補令 §二 点名换图)
> **状态只许三种**:🟢 能做 · 🟡 能做但要人绕一下 · 🔴 没有

---

## 〇 一句话

**图上五步 19 格,代码里 🟢 8 格 · 🟡 5 格 · 🔴 6 格。**
**而「向导」这个壳本身 —— 五步、进度、每步状态灯 —— 一格都没有(`platform.html` 里「向导/进度/wizard」0 命中)。**
现在能看出「这家店配到哪了」的,只有概览上一格「待配置商家 · 缺价目表」,**只看一件事**。

---

## 一、① 建店

| 能做的事(人话) | 状态 | 在哪做 / 还差什么 |
|---|---|---|
| 新建租户、填店名、id、套餐、首期 | 🟢 能做 | 商家管理 → 新建商家 |
| 填币种、时区 | 🟢 能做 | 同上(**必选,无默认** —— 11l D210 本月才补上) |
| 填地址、门店电话 | 🟢 能做 | 同上(11l D210 补) |
| **店名英文(图:中英两栏都必填)** | 🔴 **没有** | 表单里没有这个框。后端 `body.nameEn` 收得下(`local-server.mjs` 建店口写 `stores.name_en`),**但界面不发** ⇒ 所有新店 `name_en = null`(`luvia-bj` 现测就是 null) |
| **Logo** | 🔴 **没有** | 全页 3 处 `logo` 全是平台自己的图标,**没有给商家传 Logo 的入口**;库里也没有这一列 |
| 营业时间 | 🟢 能做 | 商家配置 → 营业时间标签 |

## 二、② 经营规则

| 能做的事 | 状态 | 在哪做 / 还差什么 |
|---|---|---|
| 价目表:平台代导(CSV) | 🟢 能做 | 商家配置 → 服务价目表 |
| 价目表:**商家自己导** | 🟢 能做 | **后端口本月刚做**(11o D214,`POST /admin/services/import`)—— 🟡 **但商家后台还没有界面入口**,只有口 |
| 逐项手工加服务 | 🟢 能做 | 同上那一屏 |
| 定金规则 | 🟡 **要绕** | 平台配置页**没有**定金那一块;定金在**商家后台**自己配。平台代配要用主钥匙进商家后台 |
| **顾客可见服务 vs 结算目录两套分开** | 🔴 **没有** | `platform.html` 里 `storefront` **0 命中**。后端 `services.storefront` 列在,**平台界面不给设** |

## 三、③ 老板账号

| 能做的事 | 状态 | 在哪做 / 还差什么 |
|---|---|---|
| 建店即生成老板账号 + 初始密码 | 🟢 能做 | 建店返回里带,`credBox` 显示一次 |
| 重置老板密码(手打店名 + 原因 + 吊销会话) | 🟢 能做 | 租户列表 → 重置老板密码 |
| 首登强制改密 | 🟢 能做 | `must_change_password=1` |
| 留痕(谁 / 何时 / 哪家店) | 🟢 能做 | `platform_ops_log` |
| **只在生成那一刻显示一次,刷新就没了** | 🟡 **半个** | 密码写在 `credBox.innerHTML` 里,**刷新确实就没了**(没存 localStorage)。但**同一页不刷新的话它一直在那儿**,图上写的是「只显示一次」—— 严格说要点掉就消失 |
| **初始密码 7 天不用自动作废** | 🔴 **没有** | 全仓 0 处。图上是硬规矩 |
| **12 位去易混字符(0/O、1/l/I)** | 🟡 **要核** | `randomPassword()` 的字符集没按这条裁剪过(本盘点只列,没改) |

## 四、④ 企微接入

| 能做的事 | 状态 | 在哪做 / 还差什么 |
|---|---|---|
| 填 `open_kfid` | 🟡 **要绕** | 🔴 **平台界面上没有这个框**(`platform.html` 里 `kfid` **0 命中**)。后端口在(`PUT /platform/tenants/:id/wecom-kfid`),**只能拿 curl 打** |
| 两张码(客服码 / 人设号码) | 🔴 **没有** | 建店返回里只有 `shopEntry.scene` 一串文本;**没有生成、没有下载**(11i §三 已量:缺的是接微信那个生成接口) |
| 欢迎语文案 | 🔴 **没有** | 0 命中。11i §四 已量:成员好友那条通道**一行代码都没有** |
| **状态灯:kfid 已填 / 回调通 / 可信 IP / 最近一条进线** | 🔴 **没有** | 0 命中。图上这四盏是硬要求 |

## 五、⑤ 开店卡

| 能做的事 | 状态 | 在哪做 / 还差什么 |
|---|---|---|
| **两张码 + 后台网址 + 初始密码 + 一句说明,可打印一页导出** | 🔴 **没有** | 0 命中。图上第⑤步整步没有 |

## 六、向导壳本身

| 能做的事 | 状态 | 还差什么 |
|---|---|---|
| **五步向导** | 🔴 **没有** | `platform.html` 里「向导 / wizard / 进度」**0 命中** |
| **每步状态灯(绿/灰/红)** | 🔴 **没有** | 同上 |
| **一页看出「这家店配到哪了」** | 🟡 **半格** | 只有概览一格「待配置商家 · 缺价目表」—— **只看价目表一件事**,其余八件看不出来 |

---

## 七、🔴 「图上有、代码里没有」单列(補令点名要的一节)

**六件,按「店主看得见的程度」排:**

| # | 图上写的 | 代码现状 | 影响 |
|---|---|---|---|
| 1 | **五步向导 + 每步状态灯** | 0 处 | 🔴 **店主原话:「我看不出商家配置到哪一环」** —— 就是这一条 |
| 2 | **⑤ 开店卡(可打印一页)** | 0 处 | 每开一家店要手工拼:网址 + 账号 + 密码 + 两张码 |
| 3 | **④ 四盏状态灯**(kfid / 回调 / IP / 最近进线) | 0 处 | 企微接没接通,平台侧看不出来 |
| 4 | **④ 欢迎语 + 两张码** | 0 处 | 11i 已量过,缺的是接微信/企微那两个生成接口 |
| 5 | **① 店名英文、Logo** | 界面 0 处 | 新店 `name_en` 恒为 null;商家没法有自己的 Logo |
| 6 | **③ 初始密码 7 天作废** | 0 处 | 图上的硬规矩,现在密码永久有效 |

**另有两件是「代码有、界面没有」**(比上面那类好治,只差一个框):
- **`open_kfid`** —— 后端口在,平台界面没框 ⇒ 现在只能 curl
- **商家自导价目表** —— 后端口本月刚做(11o D214),商家后台没入口

---

## 八、我对下一步的建议(仅供参考,不代替裁决)

補令 §二.3 让先做「D197 第一段:配置进度五步灯」——
按这张表看,**那正好是第 1 条**,也是店主唯一亲口说的那一条。
五盏灯要判的五件事,**代码里都已经有数据源**:

| 灯 | 判据数据源 | 现在取得到吗 |
|---|---|---|
| 建店 | `stores` 的 name/address/phone/timezone/currency 齐不齐 | ✅ |
| 经营规则 | `services` ≥ 1 且无占位值 | ✅ |
| 老板账号 | `admin_accounts.must_change_password = 0`(改过密) | ✅ |
| 企微接入 | `tenant_settings.wecom_open_kfid` 非空 | ✅ |
| 上线检查 | 提审前置清单那几项 | 🟡 清单在文档里,**没有机器可读的来源** |

🔴 **最后那盏灯没有数据源** —— 要么先只做前四盏,要么把提审清单那几项变成可查的。**这一条请你定。**
