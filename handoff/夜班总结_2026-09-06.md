# 夜班总结 2026-09-06

| | |
|---|---|
| **做到哪一步** | 夜班令三步**全部做完**:05i → ③ 预约采集 → ④ 审样本页。⑤ 正式评测**没开**(照令) |
| **三个提交号** | `a89db1d` 05i · `ccff478` ③ · `b50cc8b` ④(另两个收尾提交 `af9f49a` / `dfa281b`) |
| **停在哪** | **没碰到停线**。四条红线无红;回归没有 30 分钟定位不到的;没写 4128 本机库的**数据** |
| **token 增量** | 沙箱真模型 **约 24 万 / 600 万**(不到停线的 5%) |
| **未推 main** | **113** 个提交 |

## 三份回执
- [回执05i](回执05i_J-23刀+D140币种一处真相+废弃键过滤_2026-09-05.md)
- [回执05j ③](回执05j_③预约采集_一句一问+查真可约+草稿只建一次_2026-09-05.md)
- [回执05k ④](回执05k_④审样本页_待审与回流_2026-09-06.md)

## ③ 那三个数(照令单列,与基线并排)

| | 05h 基线 | 本批(同尺) | 本批(加一句确认) |
|---|---:|---:|---:|
| 到底 | 0 / 12 | 0 / 12 | **10 / 12** |
| 丢 7 项表 | 10 / 12 | **0 / 12** | **0 / 12** |
| 进人工 | 7 / 12 | **0 / 12** | **0 / 12** |

「加一句确认」的原因写在 ③ 回执里:基线那 6 组**没有一句是确认**,而图 §二 写死「确认才建草稿」——
旧尺子下「建出草稿=0」**不可证伪**。两把尺子的账都摆着。

## 早上请您裁的五件

1. **③:并发没验** —— 两人同时确认同一时段,`createBookingDraft` 没加锁。不敢说它是对的。
2. **④:像素层挂 ⬜** —— 行为层在浏览器里点到底了,但「登录进去之后」的截图没有:
   真登录要老板密码(脚本红线①),手动藏登录闸截出来的是我自己摆的样子,不算证据。
3. **④:页面写「本周模型放行」,其实是全部历史** —— 接口留了时间窗参数没接前端。
   接上时间窗,还是改文案?
4. **我动了一条既有判据**(`test-ai-gate ①d`,锚在字面量上)—— 改成守语义且更严,
   改后造病验红再还原。不是为凑绿,但动判据该您点头。
5. **`MP_AUTOMATOR` 我第三次忘带** —— 建议把「检测不到可 require 的 automator 就直接红」
   写进 `run-all-tests.sh`;现在这条路是「没跑也算过」,全靠最后一套棘轮兜底。

## 顺手登记的两条判据缺陷(不在三步范围内,没动)

- **`observe-fixes` 的断言数会随星期几飘**(昨天 57、今天 58):
  有断言被 `if (status === 201)` 包着,而夹具用「今天 ±N 天」,撞上门店休息日就**静默跳过** ——
  正是《断言增量律》点名禁止的写法。
- **`test-ai-fact-gate ③` 的租户隔离可能是空转的**:它用 `x-tenant-id` 换店,
  而那个头**不是**后台路由的换店开关(闸门取 `admin.tenantId`)。
  ③④ 两批都被这个坑绊过 —— 那条判据大概率在「自己跟自己比」。**建议下一批复核。**

## 本机库口径(照 J-23,结论由刀打印)

- 05i / ③:**未动**(只有服务心跳表 `notify_scan_marks` 长行)
- ④:**有动 —— 表结构两处**(新建 `ai_review_marks`;`conversation_messages` 加 2 列),
  **行数持平、一条数据没写**。原因:加列走开机迁移,回归结束把 4128 拉起来时就跑了。
- **生产库:三步全程没连过。**
