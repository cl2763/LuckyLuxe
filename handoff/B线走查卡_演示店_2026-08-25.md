# B 线第一段 · 演示店走查卡(2026-08-25)

**跑在沙箱(4310 / `apps/api/sandbox-data`),真库与生产一行没写。**
命令:`SEED_BASE_URL=http://127.0.0.1:4310 SEED_DB_PATH=apps/api/sandbox-data/lucky-luxe.sqlite node tools/seed-demo-twin.mjs`

## 一、判据改了什么(①)

- 目标判据:租户名白名单 → **`tenants.kind = 'demo'`**,与 `demo-reset.mjs` 用**同一个 `isDemoTenant()`**
  (唯一出口:判据只写一处)。原来那张表现在只提供"铺什么数据"(币种/时区/孪生档案数字),**不再决定能不能写**。
- 非 demo:**拒绝并说明是被拦了**,不是"找不到"——
  `拒绝写入 X:它的归属是 kind=real,不是演示店。这是**被拦住了**,不是找不到。`
- 真店黑名单(`REAL_TENANTS`)保留作**第二道锁**,两道都过才写。
- 生产硬边界(只跑 127.0.0.1/localhost)**一行没改**(断言 ㋡④ 锁死)。

**判据当场拦了一次(真事)**:`jics-sandbox` 名字叫「小婕的店(演示)」,归属却是 `kind=real` ——
名字说演示、数据说真店。新判据按数据拦住了它(fail-closed 正确)。

## 二、撞到的洞:归属只能在建店那一刻定,已建的店没有入口改

D73 把归属改成"建店时显式勾选",但**没给已建的店留修改入口** ——
于是我写在错误提示里那句"演示归属只能在平台后台显式设置"当时是句空话。
本批补上(实现在 `demo-reset.mjs`,与判据同域):

- `PATCH /platform/tenants/:id/kind`,isPlatform 闸门;**只在 real ↔ demo 之间**
  (`test` 由测试库建店自动产生,不许人工设,撞上直接 403);
- **必填原因**,每次写 `platform_ops_log`(`tenant_kind_change`),detail 里写明后果:
  转 demo = 账本从此不受只追加律保护;转 real = 从此受保护;
- 平台后台商家列表加「归属」列(真实商户 / 演示店 / 测试)+ 切换按钮,二次确认文案直写后果。

沙箱上用这条真接口把 `jics-sandbox` 补正为 demo(`from: real → to: demo`,ops-log 已落一行)。

## 三、走查卡(②)· 数字全部取自**顾客端公开接口**,不是查库

| 店 | 顾客 | 等级 · 成长值 | 累计消费 | 到店 | 储值 | 积分 | 卡包 | 历史订单 | 会员码 |
|---|---|---|---:|---:|---:|---:|---|---|---|
| **Lucky Luxe(演示)** `demo-lucky-luxe` | 演示·跨店阿珍 | 会员 · 1110 | 1110 | 3 次 | 888 | 710 | 次卡 1 张(剩 4)· 券 3 张 | 5 单,**已签署 5 张**,首单「已结清 CAD $798」 | LL-2EG4ZWUG |
| 同上 | 演示·本店常客 | 会员 · 198 | 198 | 1 次 | 120 | 198 | 券 1 张 | 1 单(已签署 1)「已结清 CAD $198」 | LL-A7I76IIP |
| **小婕的店(演示)** `jics-sandbox` | 演示·跨店阿珍 | 会员 · 1500 | 1500 | 3 次 | 366 | 1500 | 次卡 1 张(剩 2)· 券 3 张 | 6 单,**已签署 5 张**,首单「已结清 ¥228」 | LL-9ABLU22Q |
| 同上 | 演示·本店常客 | 会员 · 228 | 228 | 1 次 | 120 | 228 | 券 1 张 | 1 单(已签署 1)「已结清 ¥228」 | LL-ASGAS6X7 |

**会员信息全可见** ✅:等级/成长值/累计消费/到店次数/储值/次卡/券/积分/历史订单(含已签署单)逐项有真值。

### 跨店同一微信身份(串号走查用)

同一个 `openid = demo-openid-crossshop-a-zhen`,两店各一行 users,**数字一眼可分辨**:

| | Lucky Luxe(演示) | 小婕的店(演示) |
|---|---|---|
| users.id | `user_mt5lql2e_g4zwug` | `user_mt5lu99a_blu22q` |
| 储值 | **888**(CAD) | **366**(CNY) |
| 累计消费 | **1110** | **1500** |
| 次卡剩 | **4 次** | **2 次** |
| 币种 | CAD $ | ¥ |

任何一处两店数字相同 = 串号嫌疑,一眼能看出来。

### 演示租户显示名(店主 08-23 定的「演示」口径)

`Lucky Luxe(演示)` / `小婕的店(演示)` —— 两家都带「(演示)」,✅ 合口径。

## 四、幂等(③)

重跑输出:`租户已有…kind=demo` / `服务已有 2 项,跳过` / `技师已有 2 位,跳过` / `→ 演示·XXX 就绪`。
**没有重复造数**(重跑前后订单数、券数、储值数不变)。

## 五、我自己抓到的坑

**走查卡第一版把「卡包」报成 0 张** —— 我的取数脚本读了 `j.timecards`,而接口返回的是
`{ cardPack: { timecards, coupons } }`,层级读错了。查库发现数据在(3 张券 + 1 张次卡),
**先查了库再下结论,没把自己的脚本错误报成产品缺陷**。修完取数层级后数字正常。

## 六、第二段(写生产)按令**没有开始**

流程照店主已定的走:平台后台显式勾选 isDemo 建店 → Cowork 核 dry-run → 自动备份 → 跑一次
→ 出对账(lucky-luxe / jics-nail 五项零差异)。本批一步没提前动。
