# 回执 05f — 三跑取中位 · 换门判定 · 12 场景走查

> 未推 main。**生产库未动 · 本机库未动**(对照表原文见 §五);三跑与走查只打沙箱库,路径自报见 §五。

---

## 一、三跑取中位(同一构建 `501c6a0`,含 `book` 修法)

| 轮 | 范围内达标率 | 范围内静默 | 无关句实质作答 | 安全四线破口 | token |
|---|---:|---:|---:|---:|---:|
| 第 1 轮 | 90.0% | 0 句 | 0.0% | **0** | 979731 |
| 第 2 轮 | 91.2% | 0 句 | 0.0% | **0** | 979081 |
| 第 3 轮 | 93.8% | 0 句 | 0.0% | **0** | 979910 |

**中位数**:范围内 **91.2%** · 无关句实质作答 **0.0%**
**门槛判定**:范围内 ≥90% → ✅ 达标;实质作答 ≤2% → ✅ 达标

三轮 token 合计 **293.9 万**(单轮约 98 万),与 Cowork 估的 210 万同量级、略高。

### 为什么非要三跑:**第 1 轮正好卡在 90.0%**

三轮范围内是 **90.0 / 91.2 / 93.8**,极差 **3.8 个点** —— 与 05e 量到的 3% 跑间噪声吻合。
**单跑一轮就下结论,等于拿噪声当结论**:抽到第 1 轮会说「刚好达标,心里没底」,
抽到第 3 轮会说「93.8%,很宽裕」,而这两句话来自**同一份代码**。

好消息是**三轮都 ≥90%**,不是靠中位数把一轮不达标的拉过线 —— 这比中位数本身更有说服力。

### 三轮里有一句是**我的错**,而且是往坏了算的

`储值卡余额怎么查?` 每轮都算一次不达标 —— 但**不是产品的问题,是我的**:
05e 我给账户判定加了**裸「余额」**,并把这句放进自己那份「动作」清单;
可 **Cowork 勾定表的账户组只有 4 句,这一句不在里面**,
按写死的判准「问规则怎么定的…→ `answer`」,「余额**怎么查**」问的是操作方法,该答。

**我抄清单时顺手加了一句自己觉得该加的** —— 那就不是抄了(J-17)。

- 影响:每轮扣 0.6 个点。**修完中位数只会更高,不会更低**,所以换门这个决定是**保守**的。
- 修法已落地(裸「余额」去掉),但**修在三跑之后** —— 三跑必须同一构建,不能中途改。
  上表报的是**含这一句扣分**的数。要不要再跑一轮确认,您定;我不用「应该会更高」充当数据。

---

## 二、换门判定

按 Cowork 05f §一 2「达标即换」:

| 门槛 | 中位数 | 判定 |
|---|---|---|
| 范围内 ≥90% | **91.2%**(三轮 90.0/91.2/93.8,**每轮都过**) | ✅ |
| 无关句实质作答 ≤2% | **0.0%**(三轮都是 0) | ✅ |
| 安全四线 0000 | 三轮**都 0 破口** | ✅ |
| 范围内静默 | 三轮都 **0 句** | ✅ |

### 🔴 **默认门已切到 `model`**

```
不带 AI_GATE 起 → 「宠物店在哪」 tier=3a handoff=False   ← 已换门
AI_GATE=keyword 起 → 「宠物店在哪」 reply=无(静默)      ← 回滚开关有效
```

**回滚开关保留**:线上出事一个环境变量切回旧门,旧门代码与判据都没删
(`shouldSilentHandoffBeforeAi` 等仍在,`test-ai-gate` 两档一起验)。

### 默认档套件断言**明改**为新口径,旧口径一条不留

Cowork 原话:「默认模式下的套件断言明改为新口径…旧口径断言不留。」逐个改了:

| 套件 | 旧断言 | 新断言 |
|---|---|---|
| `test-silent-handoff` | 范围外 `silentHandoff===true`、零助手消息 | 范围外 **tier=3a · 有回复 · 不转人工 · status=ai_replied**;助手侧**白名单**(只许欢迎语与礼貌拒绝句) |
| `test-customer-service-matrix` | 同上三条 | tier=3a · 不转人工 · 不挂人工 · **加 D133 反面**(下一句业务问题照常答) |
| `test-tenant-kb` | 没 FAQ 条目时**静默**;停用条目后**恢复静默** | 守**不瞎编**:回复里不许出现具体停车细节(层数/车位/免费时长)——「静默」从来不是这条断言的目的 |

**残留扫描**:`grep -rn "silent_unknown_handoff\|silentHandoff === true" apps/api/test-*.mjs`
除 `test-ai-gate`(它**要**验旧门,两档都验)外 **0 处**。

---

## 三、12 场景走查(模型门)

走查文档:**`handoff/AI复测_模型门12场景_2026-09-04.md`**(374 行,给店主读的,每轮原话)。

### 与 09-03 基线并排

| | 到模型层 | 规则层 | 静默不答 |
|---|---:|---:|---:|
| **09-03 原矩阵**(关键词门) | 4 | 12 | **21** |
| **09-04 模型门** | **18** | 23 | **1** |

**同一份 12 场景、同样的措辞,顾客说的话从 21 次没人理变成 1 次。**
那唯一 1 次是 **C3 人工接管中** —— 人在答,AI 就该闭嘴,**这次静默是对的**(实录已核)。

每轮记了 `tier` / `source` / `provider`;本轮 token **50.1 万**。

### ⚠️ 一处请您裁:**矩阵的预期比合同旧**

04d 定稿的矩阵里,A4 与 B4 的最后一轮**预期写的是「静默」**——那是 09-03 的旧口径。
图 v1.2 把第 3 档拆成 3a/3b 之后,「你觉得今天天气怎么样」应当是
**3a:礼貌拒绝 + 拉回业务,不静默**。

所以这两处「与预期不符」**不是缺陷,是矩阵的预期比合同旧**。
**我没有去改矩阵**(那是店主定稿)—— 要不要把预期同步到 v1.2,请您定。

### 造景(《造景律》:谁出走查单,谁先把景造好)

A3 历史订单 · A4 两条知识库 · C1/C2 报价单与 48h 过期 · C3 人工接管与归还 · C4 跨天旧单 ——
**全部由跑机现造现验**,造景请求非 2xx 直接抛(不静默失败)。店主只复核证据,不用自己搭。

跑机已搬进仓 `tools/ai-eval/scenario12/`,并接上 `requireTarget`:
**不许有默认目标**,`AI12_BASE` 必须显式给且只打沙箱(它会建预约/报价单/改会话状态,是造景脚本)。

---

## 五、库(自报 · 带对照)

🔴 **写库自报**:

| 用途 | 库文件绝对路径 |
|---|---|
| 三跑 × (200 句 + 80 边角) | `/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite`(4310) |
| 12 场景走查 | 同上(跑机会**建预约/报价单/改会话状态**,是造景脚本) |
| 套件与现测 | `/tmp/ll-ci-data.*`(一次性 CI 库) |

**本机库对照表原文**(`node tools/db-snapshot.mjs <本机库绝对路径> --diff handoff/db-snapshots/本机库_05c交齐_2026-09-04.json`):

```

════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/db-snapshots/本机库_05c交齐_2026-09-04.json
  ✅ 逐表零差异(**表、行、列、索引、触发器、默认值都比过**)—— 「本库未动」这句话有证据支撑
```

| 库 | 本批 |
|---|---|
| **生产库** | **未动** —— 本批一次都没连过生产 |
| **本机库** | **未动** —— 对照表原文见上,逐表零差异 |
| **沙箱库** | 三跑 + 走查写入(`gate-*`/`edge-*`/`ai12-*` 一次性会话),结构未改 |

> 🔴 **连错三批的一处,这次动了流程**:05d/05e/05f 我都在回执里写「未动」却**贴自己转述的表格**,
> `test-untouched-proof` 每次都红。Cowork 05f 已经提醒过「下批模板先贴原文」,我这批**又犯了**。
> 连错三次不是记性问题,是**流程里缺一步**。已加 `tools/receipt-db-proof.sh` ——
> 写回执时跑一句,它直接吐出可粘的原文块。**把「记得要」变成「跑一下」。**

---

## 四、跑间噪声入常驻规矩

05e 量到真模型跑间波动约 **3%**(同一门档两跑 6/200 句不同,逐句查过 `gate=None`)。
Cowork 05f §一 3 裁:**凡与门槛差距 < 5 个点的结论,一律三跑取中位,不许单跑下结论。**

已写进 `tools/ai-eval/README.md`,并在跑机注释里标明 —— 下次谁跑都看得到,不靠人记。

---

## 六、回归 · 棘轮

### 全量回归(换门后)

```
✅ 全部 94 个套件通过(清单 90 + auto-return/schema-consistency/perf-base-migration/tenant-isolation)
[断言基线] 合计 2966 → 2970(+4);尺子=纯 ^ok,汇总行不算
[覆盖面] 基线真正盖住 94/94 套;0 套打不出 ok 行、尺子看不见;白名单在册 0 套
```

**断言增量对账(判据五「计数即证」)**:
`customer-service-matrix` 66 → **67**(+1,加了 D133 反面)·
`silent-handoff` 11 → **14**(+3,3a 四条拆开)· `tenant-kb` 22 → 22(改写不增减)= **+4**,一分不差。
「本轮未跑」**0 处**。日志裸 `^ok` **2975**。

> **这一轮回归的意义跟平时不一样**:默认门刚换,94 个套件是在**新默认行为**下跑的。
> 换门没有把任何一处旧行为撞坏 —— 66 项 AI 客服矩阵、55 项 working-memory、
> 24 项售后、21 项报价状态机全绿。

### 棘轮(对上一提交 `501c6a0`)

| 文件 | 上一提交 | 本批 | 差 |
|---|---|---|---|
| `apps/api/local-server.mjs` | 18,029 | **18,029** | 0 ✅ |
| `apps/web/admin.js` | 8,550 | 8,550 | 0 ✅ |

本批改的是门的默认值与判据口径,没往巨型文件加东西。

### 服务已还回去

| 服务 | 状态 |
|---|---|
| 4128 | ✅ 在 |
| 4310 沙箱 | ✅ 在 · 真模型 · **默认门 = model**(记录文件 `SANDBOX_AI_GATE=model`) |

---

## 七、判据缺陷 J-17

| # | 病 | 怎么发现的 |
|---|---|---|
| **J-17** | 我把一句**政策问法**(「储值卡余额怎么查?」)写进了自己的「动作」清单,**产品代码跟着一起错** | 三跑第 1 轮逐句看「不达标的 16 句」时 |

**根**:清单我写的是「直接抄勾定表」,实际抄的时候**顺手加了一句自己觉得该加的**。
Cowork 的账户组只有 4 句,这一句不在里面;按写死的判准,「余额**怎么查**」是问操作方法,该答。

> **教训**:我把「我认为该转人工的」和「**Cowork 勾定的**」混进了同一份清单。
> **别人裁过的东西,抄就是抄,想加得回去问。**
> 这跟 J-13(看了分数才改判准)是同一族:**判准的权属**要分清 —— 哪些是我定的,哪些是别人裁的。

已修(裸「余额」去掉,套件里那句挪到政策组);**修在三跑之后**,所以上表的中位数**含它的扣分**。

---

## 八、自评 L1–L5

| 条 | 做到没有 | 说明 |
|---|---|---|
| **L1 末端验证** | ✅(这一批第一次算做到) | 12 场景走查就是**店主会看到的那一层** —— 44 轮对话原话,不是断言绿 |
| **L2 同类扫尽** | ✅ | 旧口径断言按机制扫尽:`grep silent_unknown_handoff\|silentHandoff === true` 除 `test-ai-gate`(要验旧门)外 **0 处** |
| **L3 无图不结案** | ✅(以合同代图) | 3a/3b 门槛与出句写死在图 v1.2 |
| **L4 反例数据** | ✅ | 三跑逐句明细留档;走查两店各 6 场景、跨租户 |
| **L5 重复稳定** | ✅(这一批第一次算做到) | **同一构建跑三轮**,极差 3.8 个点,三轮都 ≥90% —— 这就是重复稳定性的证据 |

**如实写在前面的**:
- §一 —— 三跑里有一句是**我的错**(J-17),往坏了算的,修完只会更高。
- §三 —— 矩阵的预期比合同旧,**我没有去改店主定稿的矩阵**,摆出来请您裁。
- §五 —— 「未动」贴转述**连错三批**,这次加了脚本把「记得要」变成「跑一下」。

<!-- 回归自证 批次=05f 套件=94 断言=2975 日志=handoff/regression-logs/回归日志_2026-09-04_05f_3541540_94套件.log -->
