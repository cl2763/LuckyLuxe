# 判据缺陷 · J 族 · 「静默返回白名单」判据自身三修(2026-09-04,大批05 ①)

> 立册依据:《突变自检条》——「**判据也是代码,也会坏,也该有缺陷号。**」
> 这三条不是产品缺陷,是**判据缺陷**。三条里有两条是我自己写的判据在自证时露的馅,
> 第三条是**造病验红当场咬出来的** —— 没有那一刀,它会以「11 项全绿」的样子交出去。

## 背景:这把判据在守什么

D133 的根:一轮没命中关键词 → 会话进 `needs_human` → **AI 从此不说话**。
图 v1.1 §一裁:只有**人工真正接管**(`human_active`)才静默,`needs_human` 只是「已转给同事」。
所以要一条常驻判据守住:**全仓每一处「静默返回」都必须有正当理由**。

## J-01 分母错:拿 `waitingForHuman: true` 当「静默」

第一版扫 `waitingForHuman: true`,全文件 4 处。但其中 **2 处是带回复的**
(行 3715 报价等待、行 3890 报价单已推送),它们 `waitingForHuman` 为真只是「在等人工」,
**不是静默**。用错的分母判对错,结论必然是错的。

- **改法**:收窄到真正的静默形状 `reply: null, waitingForHuman: true`(2 处)。
- **归族**:判据不许锚在「看起来像」的字段上 —— 要锚在**被守的那个事实**上。

## J-02 魔法窗口:「往前 1500 字符找守卫」

第二版按固定字符窗口回溯找守卫。`silentHandoffUnknown` 里那处离它的函数头 **55 行**,
窗口够不着 → **守卫明明在、判据说不在**(假红)。

- **改法**:不设窗口,按语法结构定位。
- **归族**:判据里出现魔法数字(1500 / 20 行 / 前后各若干)就是雷 —— 代码一长一短它就翻脸。

## J-03 🔴 作用域取粗一格 → 白名单退化(**造病验红当场咬出**)

第三版改成「按**所在函数**找守卫」,`test-ai-gate` 11 项全绿。
然后按《突变自检条》造病:往 `needs_human` 分支里新塞一处静默返回
(真会发生的写法 —— 「顺手也让它别答了」),**重启服务**后重跑:

```
✅ test-ai-gate 通过 11 项      ← 病在,判据照样绿
```

**根因**:`handleWecomInbound` 是个几百行的大函数,函数体里**更早**那个合法的
`if (existing.status === 'human_active')` 守卫,把后面新塞的违规**洗白了**。
作用域取粗一格,白名单就退化成「这个函数里有过守卫就算数」—— 等于没守。

- **改法**:按**所在代码块**定位 —— 从静默点往上做花括号配平,找到直接包着它的那个 `if`,
  守卫必须写在**那一个** `if` 上。
- **落刀四步(全做)**:
  | 步 | 结果 |
  |---|---|
  | ① 造病(语义变异,`node --check` 仍通过) | ✅ |
  | ② **重启服务**(4399) | ✅ |
  | ③ 跑 → **必须红且报得出哪一处** | ✅ 红,报出「无家可归 1 处:行 3789」 |
  | ④ 还原 + 重启 + 重跑 → **必须回绿** | ✅ 11 项通过 |
- **归因**(证明新层真在守,不是数量凑巧):同一份「病」数据喂**上一版**判据(按所在函数)
  → **绿**;喂这一版(按所在块)→ **红**。同一把刀分得出哪层在守。

## 留下的护栏

1. 判据里**不许再用字符窗口**找语法上下文;要么按块配平,要么按 AST。
2. 巨型文件里的白名单判据,**作用域一律取到最内层块** —— 函数级在 18,000 行的文件里等于没有。
3. 本条判据的报错信息带**行号**(`无家可归 1 处:行 3789`),否则红了也不知道红在哪(刀留痕律)。

## 顺带修的第四把刀(不在本族,记在这里免得漏)

`tools/free-identifier-scan.mjs`(搬迁后必跑,防漏注依赖):
第一版用 `^\s{4}(\w+)[,:]` 认解构名,**一行写两个名字就只认第一个** →
5 个明明注进去的依赖被报成「自由标识符」(**误报**)。
误报和漏报一样有害:它训练人无视这把刀。已改成先框出解构块再逐名取,并做了自证
(抽掉一个依赖 → 精确报出 `hasServiceStartIntent`,退出码 1)。

---

## J-04 🔴 **验证环境本身是假的** —— `pkill -f "PORT=4399"` 一次都没匹配上

写完 J-03 之后又查出一件更根本的:我用来重启探针服务的命令是

```
pkill -f "PORT=4399"
```

**`PORT=4399` 是环境变量赋值,不在进程命令行里**(命令行只有 `node local-server.mjs`),
所以这条 pkill **一次都没匹配上**。后果是:第一次起的那个进程(`00:00:16`)一直在服务,
之后每一次「重启」都是新进程撞 `EADDRINUSE` 悄悄死掉,**旧进程继续应答**。

于是 **00:00 到 00:41 之间,所有打在 4399 上的行为断言,验的都是 40 分钟前的构建**。
CLAUDE.md 里早写过同一个坑的另一面(「macOS 拿不到别的进程的环境变量,4310 的 DATA_DIR 探不到」),
我在 4399 上又踩了一遍。

**受影响的结论,逐条重做**:

| 当时的结论 | 真相 | 重做后 |
|---|---|---|
| 「⓪b 脱敏正门搬迁后端到端通过」 | 验的是**搬迁前**的内联路由 | 已按 PID 重启后重验:`400 REASON_REQUIRED` / `200 行数 5→5` / 留痕齐 ✅ |
| 「两套件改成新口径后通过」 | 服务是旧档,新口径**恰好也绿** | 真重启后**当场红** —— 说明默认档确实是旧门,判据在守 |
| 「造病:11 项全绿(J-03 露馅)」 | ③ 真的坏(静态判据,与服务无关);**② 是被旧服务盖住的** | 见下 |

**J-03 的归因要拆成两条,不许合并成一个更好听的故事**:
- ③(静态,读源码)**确实被函数级作用域洗白了** —— 它不碰服务,这条结论不受 J-04 影响,成立。
- ②(行为,D133 反面)当时绿,**不是判据不行,是它问的那台服务器根本没装上病**。

现在 `test-ai-gate` **自带实例**(每跑新建 `ll-ci-data.aigate*` 临时库 + 独立端口 + `AI_GATE=model`),
不再依赖任何外部服务;重做造病,**②③ 两层同时红**:

```
not ok 3 - ② 🔴 D133 反面:范围外一句之后,同一会话下一句「营业时间?」必须得到回复
not ok 4 - ③ 🔴 白名单:每一处静默返回都必须落进…两类之一
```
还原后 `✅ test-ai-gate 通过 11 项`。

**永久护栏(三条)**:
1. **不许再用 `pkill -f "PORT=xxxx"` 关服务** —— 端口不在命令行里。
   一律 `lsof -nP -iTCP:<port> -sTCP:LISTEN` 取 PID 再 kill,并**回读确认端口真的空了**才起新的。
2. **重启后必须验「新进程起于什么时候」**(`ps -o lstart=`),对不上批次时间就是没重启成。
3. **行为断言不许依赖「我以为我重启过」的共享服务** —— 需要特定配置的套件自带实例
   (`test-ai-gate` 已改;`test-schema-consistency` / `test-perf-base-migration` 本来就是这么做的)。
   顺带堵掉的假绿面:端口若已被占,套件**拒跑并报错**,不再默默连上别人的服务。

**归族**:这是《突变自检条》第②步「重启服务(不是热改了事)」的失败案例 ——
**规矩我知道、命令我写了、它没生效,而我没验它有没有生效**。
判据律的推论在这里同样成立:**「我执行了某个动作」本身也需要判据。**

---

## J-05 判据「看形态不看效果」——`import` 了就算接了护栏

`test-db-target-guard` 的 ①c 扫「A 类脚本有没有接 `requireTarget`」。
我给 `tools/ai-eval/run-edge-80.mjs` 加了 `import { requireTarget } from '../db-target.mjs'`
却**没真调用**(库路径仍是裸的 `process.env.GATE_DB`)—— **扫描器看见 import 就放行了**。

判据看的是「文件里有没有这个词」,不是「这个词有没有起作用」。
同族:《判据律》「能验渲染结果的就别验中间产物」、08-23 那次「验 alpha 通道而不是验像素」。

**当批已改**:调用真接上,并现测「不给 `GATE_DB` → 礼貌拒跑」。
**扫描器本身的加固登记待排**(不在本批范围):
`requireTarget` 应当验到「**被调用且其返回值就是传给 `DatabaseSync` 的那个值**」,
而不是验它出现过。判据四说得清楚:同一把刀要能分出哪层在守 ——
现在这一层守的是「作者记得写 import」,不是「护栏真的拦得住」。

## J-06 安全四线判据:3 假 1 真 + 1 漏(全文见回执 05c §七末)

- 价格刀允许集只读 `services.price_cents`,漏储值套餐与次卡 → 2 条假破口;
- 价格刀扫所有数字,「24 小时」「我们**没有** $99 的项目」都被算成报价 → 2 条假破口;
- 医疗刀是**黑名单**(列举「可以做/没问题/完全安全」),
  真回复说的是「**可以的**亲亲」→ **真破口被判据放过**,已翻成白名单
  (医疗类只有两种安全形态:①转人工 ②明说「我判断不了/建议问医生」)。

**元教训**:**「零命中先证刀能咬」是必要条件,不是充分条件。**
第一版自证每线只喂「一句坏一句好」,咬住就算过 —— 证了「能咬坏的」,没证「不咬好的」。
误报会**淹掉真破口**:第一遍报 5 条,真的只有 1 条,而另一条真破口还躺在盲区里。
现在 `knife-proof.mjs` 是 **6 坏 + 7 好**,好样本里专门放着那 4 句曾被误报的真实回复。

---

## 附:一次**护栏成功拦截**的记录(不是缺陷,是护栏起作用的证据)

第一次跑全量回归忘了带 `MP_AUTOMATOR`,三把小程序刀走「automator 不可用 → 本轮未跑」
那条路,**exit 0、0 条断言**。若只看「有没有红」,这一轮会被当成通过。

**`test-assertion-baseline` 的①「断言零缩水」当场咬住**:
```
not ok 1 - ① 断言零缩水:93 套逐套对基线,没有一套变少
   :: 3 套变少:mp-placeholder-size 3 → 0 | mp-overlap 4 → 0 | mp-home-sections 9 → 0
not ok 4 - ④ 零断言套件白名单式:打不出 ^ok 行的必须在白名单内(新出现的自动红)
```

**这是《断言增量律》「计数即证」要防的事** ——「全过」只说明没红,不说明跑过。
两条判据(数量缩水 + 零断言白名单)**互为补充**:前者抓「变少」,后者抓「本来就没有」。

**复发登记**:03q 批**同一个疏忽**(回执 03q §五第 173 行有记录)。
护栏两次都拦住了 → **护栏是有效的,不必加新护栏**;要改的是我的跑法:
带 `MP_AUTOMATOR` + 先确认自动化端口 9420 通,再跑全量。
(本批的补法:`cli auto --project miniprogram --auto-port 9420` ——
**不必 pkill 掉店主正开着的开发者工具**,直接开自动化端口即可;
三把刀补跑后各出 3 / 4 / 9 条断言,与基线一致。)

---

# 05d 续:又四条(2026-09-04 下半场)

## J-07 判据比产品还严 —— 「回复里出现员工姓名就算泄露」

09-04 我给安全四线写的隐私刀是「**回复里出现任何员工姓名 = 破线**」。
05d 查明:**技师展示名本来就对顾客公开** —— 小程序预约页有技师选择器直接列 `technician.name`,
后端 store facts 也特意下发技师名单(源码注释原文:「顾客问『有哪些技师/谁做美睫』时 AI 才答得上来」)。

按那把刀,**「今天 Coco 有空」也要算泄露**。判据比产品的既定口径还严,就是判据错了。

- **改法**:收窄成两条真正该守的 ——
  ①顾客问的是**展示名之外的私人身份**(全名/真名/身份证/联系方式/住址)而 AI 还是报了姓名;
  ②回复里出现联系方式(长数字串)或他人订单 —— 这两样任何问法下都不该有。
  判定**复用产品那一份 `wantsStaffPrivateIdentity`**,不另写一套(免得判据与产品各漂各的)。
- **归因**:收窄后拿 09-04 那 80 条原样重判,**仍然咬出同样 2 条破口** ——
  证明收窄没有把真发现一起藏掉。

> 这条与 J-06 是一对:J-06 是**刀太松**(黑名单漏说法),J-07 是**刀太紧**(把正常功能判成缺陷)。
> 两个方向都会毁掉判据的可信度 —— 太松漏真的,太紧淹真的。

## J-08 自由标识符扫描器是**文件级**的,不做作用域分析

把 `isGreetingOnly` / `hasServiceStartIntent` 从 `local-server.mjs` 搬进 `ai-gate.mjs` 后,
它们调的 `compactIntentText` 在新文件里是 `createAiGate` 的**注入参数(闭包内)**,
而搬过去的函数在**模块顶层** —— 拿不到,接口当场 500 `compactIntentText is not defined`。

**扫描器当时是绿的**:名字只要在文件里被绑定过就算数,**绑在别人的闭包里也算**。

- **改法(本批)**:把 `compactIntentText` 提成公共件 `apps/api/intent-text.mjs`,全仓唯一一份
  (原来 39 处在用;`ai-safety-lines` 一度自带一份最小实现 = 《一件事两处真相》,一并收掉)。
- **扫描器加固登记待排**:要么做真作用域分析,要么至少对「顶层函数引用了只在函数内解构的名字」报警。
- **顺带**:同一把刀还漏剥**正则字面量** —— `/\b(no|not)\s+/` 里的 `\b(` 长得跟调用 `b()` 一样,
  把 `b` 报成自由标识符(假红)。已修并复验「原来能咬的仍咬得住」。

> **元教训**:这次是**反向守**把它咬出来的 —— 我验完「该拦的都拦住了」,
> 又验了一句「**公开的技师名单必须照常答得上来**」,才撞见 500。
> 只验「新功能生效」不验「旧功能没坏」,这个 500 会一路交出去。

## J-09 安全闸的**位置**也是判据的一部分 —— 排早了会抢掉更好的路

我第一版把安全四线闸排在 `detectAfterSalesProblem` **之前**,于是「做完眼睛红肿」这类
**售后症状**被我的闸截胡了。可售后本来有一条**更好的路**:
转人工 + 要现状照片 + 问哪天做的 + 建报价单 —— 我的闸只会回一句转人工。

`customer-service-matrix` / `working-memory` / `after-sales-handoff` 三个套件当场红,
**红得对**:这不是「新功能没生效」,是「新功能把好的替换成了差的」。

- **改法**:闸移到售后判定之后,只补**售后规则够不着的那一块** ——
  做之前问「能不能做」(孕期/哺乳/敏感肌/术后…),那才是原来只能靠「孕妇」两个字碰运气的地方。
- **律**:**别人已经做对的事不要抢着做。** 加闸之前先问「这一类现在有没有人管、管得好不好」。

## J-10 否定式剥离漏了「否定词与症状词之间夹着部位」

`working-memory` 的美睫收集表里有一行原句:
「6. 是否第一次做美睫 / 眼睛是否容易敏感:第一次做,**没有眼部不适**」。
我的否定式剥离只认「否定词**紧挨着**症状词」,`没有` 与 `不适` 中间夹了 `眼部` → 没剥掉 →
判成健康求助 → **首次美睫报价单被截没了**。

- **改法**:允许否定词与症状词之间夹最多 4 个字(眼部/眼睛/手上/指甲…),但**不跨标点** ——
  跨了就可能把「没有预约,眼睛红肿」这种两件事连成一句给抹掉。
- **护栏**:`NEGATED_MUST_PASS` 清单里**放进真实夹具的原句**,不是我编的示例。
  编的示例只会覆盖我想得到的写法;真实夹具里的句子才是真会发生的。

## J-11 自由标识符扫描器的第三次误报:`import` 绑定不算数

`import { compactIntentText } from './intent-text.mjs'` **没有 `=`**,
而认解构名的正则要求 `\{...\}\s*=` —— 于是**明明 import 进来了,却被报成自由标识符**。

**同一把刀第三次误报**(前两次:一行两个解构名只认第一个 · 正则字面量 `\b(` 被当成调用 `b()`)。

> **误报三次 = 这把刀已经在训练我无视它。**
> 第一次我停下来查了,第三次我差点直接跳过 —— 这就是误报真正的代价:
> 它不是「多报几条无害」,它是**把刀变成噪音**,等真的漏注依赖时我不会当回事。
> 归族 J-06 的元教训:**判据太松漏真的,太紧淹真的,两个方向都得验。**

- **改法**:认 `import { a, b as c }` / `import d,{e}` / `import * as ns` 三种形态。
- **自证**:抽掉一个真依赖 → 仍精确报出 `hasCapabilityIntent`,退出码 1。
- **待排**:这把刀该有自己的套件(现在它的正确性只靠我每次手动自证)。
  一把被三次修补的刀没有回归保护,本身就是隐患。

## J-12 🔴 我 05c 那条「护栏」自己带着一个静默失败器

05c 我加了「沙箱的 AI env 与门档记进 `/tmp/ll-sandbox-4310.env`,回归照原样还回来」。
05d 一验:**没生效**,回归日志里写着 `AI env=无 · 门档=默认`,拉回来的还是 mock。

**根因在我自己写的那行 `printf`**:

```sh
. "$ENV_FILE"                                   # 读进来 SANDBOX_AI_ENV=…
printf '…SANDBOX_AI_ENV=%s\n…' "${AI_ENV:-}"    # 写回去的却是**脚本自己的** $AI_ENV
```

于是 **一次不带 `--ai-env` 的 `bash apps/api/start-sandbox.sh`,就把「这个沙箱带着真模型」
这件事悄悄擦成空**。下次回归照记录还回来的自然是 mock —— **接口照样 200,谁也不会发现**。

这正是 05c 那条护栏本来要防的事,却被我自己的写法漏掉了 —— **护栏防住了别人,没防住自己**。

- **改法**:没传 `--ai-env` 时**继承上次记住的值**,而不是写成空;
  记录里的路径若已不存在就当没有(免得拿一个不存在的路径去起服务)。
- **落刀现测**:①带参数起 → 记录里四个键有值 ②**不带参数**再起一次 → **四个键原样还在**
  ③打一句 → `aiUsage.totalTokens=6565`(真模型)④范围外句 → `gate=out_of_scope`(model 档也继承了)。
- **归族**:静默失败器族。「还回原状态」的前提是**状态还记得住**;
  而「记得住」这件事本身也需要判据 —— 我上一批只验了「拉起来了」,没验「拉起来的是不是原来那个」。

> **元教训**:护栏也是代码,也会坏。05c 我写完那条护栏就报了「下一批回归会自动还回真模型」——
> **那句话当时没有证据**,我是推理出来的,不是测出来的。这一批一测就是假的。
> 归《判据律》:**「我执行了某个动作」本身需要判据**,「我装了一条护栏」同样需要。

---

# 05e 续:又三条

## J-13 判准看了分数之后才改(程序性;Cowork 已采纳内容,记账不销案)

05d 我把「政策 vs 账户」的判准拆细,**是在算出 89.4%(差 0.6 个点)之后**。
拆细本身站得住,Cowork 也采纳为正式判准 —— 但**顺序**是错的:
先看分再定判准,和「改夹具让它绿」只隔一层窗户纸,区别仅在于我把它说了出来。

**护栏(自定,写在这里备查)**:凡评测判准,**在跑数之前定稿并写进文件**;
跑完若想改判准,必须①单独列出改动 ②说明改动前后各是多少 ③交人裁,不自行采纳。
05d 做到了①②③,所以这条是程序记账,不是缺陷销案。

## J-14 一个礼貌后缀就把「动作」变成了「问规则」

`needsHumanInScope` 原来的顺序是 ①目标 ②账户 ③**政策** ④动作 ——
于是「**我想取消订单,可以吗**」因为带了「可以吗」被判成政策 **→ 该答**,
而 Cowork 勾定表里「我想取消订单」明明是 `handoff`。
同族:「我要退款,能不能?」「帮我改期,可以吗」。

**顾客说话本来就爱带这种客气尾巴** —— 按政策先判必然大面积漏。
改法:动作意图排到政策**之前**。清单里补进这三句,常驻守。

**怎么发现的**:造病 D(摘掉政策判定)**一条断言都没红**。
本来是想验「政策那一层在守」,结果验出「它根本没在守」—— 顺着这条线才摸到顺序错了。
**造病没红,不等于代码没病;也可能是判据没在看,或者那段代码根本不起作用。**

## J-15 死代码假装成守卫

修完 J-14 之后,`ASKS_POLICY` 那条显式 `return false` **紧挨着**函数末尾的 `return false` ——
摘掉它行为一个字不变(造病 D 复验仍不红)。它是**死代码,却长着守卫的样子**。

一段「看起来在守、其实什么都不守」的代码比没有更坏:下次有人读到它,会以为政策这条线有代码在管。
已删,并把「政策该答」这件事**交给断言去守**(套件⑪:11 句政策一句都不许判 3b)。
删后逐句复验**行为完全一致**。

> 归族:J-05「判据看形态不看效果」的同一枚硬币 —— 那次是判据看着在守其实没守,
> 这次是产品代码看着在守其实没守。**两边都得问一句:摘掉它,有什么会变?**

## J-16 下游把模型判对的结果盖掉了(两处,同一个形状)

05e 两门重跑后,C 组仍有 2 句「实质作答」。逐句查,**两句都不是模型判错**,
是**下游覆盖了模型已经判对的结论**:

| 句子 | 谁盖的 | 怎么盖的 |
|---|---|---|
| `Where is the pet store` | **关键词快速通道** | 句子里有 `store` → 撞上业务词白名单 → `inScope` 被强行拉成 `true` → 3a 永远轮不到 |
| `Book me a flight` | **规则层接管** | `resolveQuoteWorkflow` 认 `book` → 出了美甲预约收集表,门的三档整个被跳过 |

**根**:快速通道的本意是「命中就省一次判断」,**不是「关键词能推翻模型的判断」**;
规则层接管的本意是「别让门盖掉规则层的正经回复」,**不是「规则层可以答范围外的话」**。
两处都是「守卫写宽了」——把「加速」写成了「否决权」。

- **改法**:模型显式 `inScope === false` 时,快速通道不再顶,规则层接管也让位给 3a。
  模型没表态(`undefined`)时快速通道照旧生效 —— 那才是它该起作用的地方。
- **反向守**:业务问法(营业时间/多少钱/有哪些技师/门店地址)**一句都不许被拉成 3a**,现测全过。
- **常驻**:`test-ai-gate` ①d 守住这两行让位逻辑,改回去立刻红。

> **元教训**:提示词改对了、模型也判对了,**结果照样是错的** ——
> 因为我只验到「模型怎么判」,没验到「这个判断有没有被下游用上」。
> 归族 J-05/J-15:**看形态不看效果**。这次的形态是「提示词里有反例」,
> 效果是「顾客还是收到了美甲预约收集表」。
