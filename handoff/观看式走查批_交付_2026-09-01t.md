# 01t 交付:观看式走查缺陷单全修 + UI 规范立法 + 店群 UI 轮测(2026-09-01)

**安全保证:生产库未动 · 本机库未动。** 未推 main。全程只碰沙箱库与回归临时库。
〇观看式走查律已按新标准执行:本批自检=三角色实登真点真提交(不是截图),自走查还当场咬出两条我自己刚写的雷(详见「自检自咬」)。

## 一 · 归因四条(先答)

| 缺陷 | 归因 |
|---|---|
| **D95** | 小程序直排面板=大类→小类两级(D24 结算目录同源)+ 服务定时长 + **±30 微调/标准** + **预计结束显示** + 定金已收/未收;网页面板此前只有平铺服务 chips+起始时间,四件全缺。**不是"未来日"特有,今天也缺** —— 她在未来日撞见而已 |
| **D96** | **写进库了**(直排 API 落单无恙),是**读口两层盖**:①直排成功只刷台面,`owner.bookings` 全局缓存(启动抓取)不回灌 → 全部预约读旧缓存;②列表日期筛默认今天,未来单还要再翻日期。属读口缺陷,不是写缺陷 |
| **D97** | 后端**全在**:`/admin/service-notes/pending` 按单判待写、小记表带 booking_id、写口收 bookingId(08-27 越权闸就是围它建的)。断的是**前端三处接线**:结算完成环节没挂、台面/工作台无待写提示、网页订单卡无补写口(小程序订单面板的「写/补服务小记」倒一直在,带单) |
| **D101①** | **横幅挂了**(conversation 页 js:90/wxml:2,与网页同一读口同一 quoteState 字段)。她没看到的最可能面=打开的会话本无报价态(三态之「不该有=不出现」)+ **列表无报价态标识**,没法找到有报价历史的会话 —— 可用性缺陷,②的顾客卡「报价状态」行连带解决;工具缓存为次要变量。夹具已补:bigdemo:lucky-luxe:0=今日已报价+已绑 Cathy(会员),供三轮直接看 |

**归因即答不改两条照录**:9/1 上午不可排=时间诚实设计(过去时段不给排,翻 9/2 全天可排即对照);老板补录过去单是否要入口,候裁。

## 二 · 缺陷单逐条修(全部常驻断言)

- **D94**:根=`this.load` 不存在(方法叫 `loadDayView`)→ 点击即炸;一行修;chips 改 999rpx 胶囊+选中黑白(台面规格)+ 呼吸(24rpx 行距+虚线分隔,照图例区);15 条控制台警告根=`snapViewer` 无初值(组件收 undefined)——初值收进唯一真相 `dailyCloseData`(mixin),finance/order-detail 各自补,关闭 handler 不再回 null(L2 零残留);**归因勘正:orders/daily-close 的快照链不是死链,handler 一直住在 mixin 里 —— 我起先差点并排写第二份,公约④刹住撤回**。
- **D95**:网页面板补齐四件(两级选择器/时长自动+微调±30+标准/预计结束/定金勾),提交带 durationMin+depositPaid;实点验证:切大类小类联动、+30 → 预计结束 12:00→12:30。
- **D96**:直排成功回灌 `owner.bookings`(deps.refreshBookings);末端验证:排「自走查D95客」→ 切全部预约当场可见。
- **D97**:①网页开单成功→弹写小记(可跳过,弹层写明「跳过进待写清单」);②小程序全签完→showModal 去写/跳过→回台面;③台面待写 pill 双端(网页点开清单面板逐行「写」;小程序 actionSheet 选单跳写页;员工=后端只给本人单);④网页员工工作台待写卡(近 7 天);⑤网页订单卡「写服务小记」钮(导航类,与「详情」同类,D70 状态机合同不涉——假设已记);⑥弹层挂单落库。行为闭环常驻:完成无小记→入清单→写(挂单)→消行(observe-fixes 实测)。
- **D98**:值日行=行内说明小字+行右 `.ui-sw` 标准开关(=营业时间那颗,同一条 CSS 规则别名,开关单一真相);实点开↔关两向 toast 各响。值日通知候裁未做(照令)。
- **D99**:定金规则摘要行照本页 .row 规格(20rpx 22rpx 呼吸,标题不顶边)。
- **D100**:并入规范批 —— chips 三处统一 999px;规范扫描器首跑就咬出 `.sw-cta`(10px→8px)与 **`.tb-duty-chip`(=D94 网页同病**,14px 青选中→999px 黑白,双端同病检查律自动兑现)。
- **D101②**:会话侧顾客卡六件双端(姓名/会员/会员码/储值/次卡/报价状态),读口全既有(customers 行+timecards 口+quoteState);次卡懒取、fail 显「—」零回落。实测:Cathy 卡六件齐、报价状态「本会话已报价」、次卡「无」=真值(那张走查次卡在别人名下)。
- **阈值编辑器**:横排一行(裁定照改)。

## 三 · UI 规范(《UI组件规范表_2026-09-01t》立为合同,待你终裁)

现状:按钮圆角网页 8 种/小程序 12 种。标准=每类一种(主导形):主按钮 8px/16rpx、次按钮与 chips 999 胶囊、选中=黑底白字、开关=`.ui-sw` 唯一、输入框 8px/12rpx、行距照门店设置行。
**test-ui-spec 常驻**(白名单判据+存量挂账棘轮 24 条只减不增,新面用规范外形制即红)——首跑即咬出两处真违规并已修(见 D100)。

## 四 · 店群 UI 轮测(10 店 × 5 族,真点)

| 店 | ①三端同步 | ②商家隔离 | ③会员隔离 | ④财务链 | ⑤排班链 |
|---|---|---|---|---|---|
| lucky-luxe 镜像 | ✓UI=API(报价设置+台面单数) | ✓ | ✓ | ✓**全链真点**(绑定客充$1→冲销红字) | ✓直排落单+值日 |
| jics-nail 镜像 | ✓ | ✓ | ✓ | ✓绑定闸拦(未绑客不许充=闸对) | ✓直排+值日 |
| zj-open-cad | ✓ | ✓ | ✓ | ✓绑定闸 | ✓直排+值日 |
| jics-sandbox | ✓ | ✓ | ✓ | ✓**全链真点** | ✓直排+值日 |
| jics-store | ✓ | ✓ | ✓ | ✓绑定闸 | ✓直排+值日 |
| demo-lucky-luxe | ✓**强制墙标本**(真点开周二保存墙撤)+撤后 UI=API | ✓ | ✓ | ✓**全链真点** | ✓直排+值日 |
| demo-rehearsal-0825 | ✓墙拦=对 | ✓ | ✓ | ✓墙拦 | ✓值日落库(API 层) |
| demo-prod-rehearsal2 | ✓墙拦=对 | ✓ | ✓ | ✓墙拦 | ✓值日落库(API 层) |
| demo-prod-rh6 | ✓墙拦=对 | ✓ | ✓ | ✓墙拦 | ✓值日落库(API 层) |
| zj-open-cny | ✓墙拦=对 | ✓ | ✓ | ✓墙拦 | ✓值日落库(API 层) |

**零红格。** 口径注记:②③=同一会话摸 lucky 资产(档案/单/会员码)必须 null——十店逐一过;墙店 UI 面被强制设置墙管(设计行为),墙流程本身在 demo-lucky 真点过关;小程序层=同读口同数据(既有口径),像素=开发者工具侧。执行器与逐店原始输出归档 handoff/attachments/UI轮测_2026-09-01t/。轮测记号残留:各店 $1 充值+冲销对、「UI轮测客」直排单(沙箱可重置店)。8 家陪审店老板密码经平台正门重置(理由入运维日志),现密码=一次性轮测密码,记 本地自查账号.txt。

## 自检自咬(〇的标准立竿见影)

1. **D97 挂单恒 null**:弹层 open() 先赋 `st.bookingId` 再整对象重建 —— 赋值被冲掉,小记落库 booking_id=null。**我的套件行为断言绿着**(它走 API 路径不走弹层)——判据盲区一并补(钉在重建体内);修后实点二发:booking_id 落库 ✓。静默失败器族+判据律双案。
2. **D96 初验假红**:reload 后 1.5s 就探,启动抓取未完 —— 缓存空是我抢跑,不是产品;干净会话重验通过。写进回执当「执行器时序教训」。

## 回报六项

①**改了什么**:上表 D94–D101+阈值横排+规范三件;新套件 test-ui-spec(6)+test-observe-fixes(22);今日板/工作台/账调白名单 5→7;ADMIN_BUILD 20260901t-obs01。
②**在哪层验**:界面层实登真点(老板=lucky/jics 全流程、员工=avalin 工作台、10 店轮测);行为层(observe-fixes 完成→待写→写→消行);静态链常驻。小程序像素=工具侧(结构+套件常驻,与前批同口径)。
③**同类扫尽**:规范扫描白名单式全 CSS;viewer:null 零残留;chips 三处+同病两处一次改齐。
④**反例数据**:无 hours 店(墙)、未绑定客(充值闸)、跨店资产探针、次卡他人名下(零回落「无」)。
⑤**重复稳定性**:D98 开关两向;直排 10 店各真点一发;值日 API 开关来回。
⑥**残留与不确定**:小程序像素(D94 chips/D99 行距/D97 弹窗)待你三轮工具亲验;值日推通知候裁;D96 日期筛默认今天=设计现状(未来单要翻日期,若要"排完自动跳到该日"候裁);轮测⑤在墙店只到 API 层。

## 棘轮(请批)

local-server.mjs **18,184 → 18,184(0,本批后端零行)**;admin.js **8,510 → 8,519(+9:D96 回灌钩 4 + D97 订单卡钮 2 + 委托分支 6,抵扣既有 −3)——请批 8,519**;
today-board.js 379 / staff-workbench.js 177 / 新套件 observe-fixes 80 + ui-spec 41(均限内)。

## 全量回归 & 过程再咬二雷

**68 套件全绿**(66+ui-spec+observe-fixes;日志 handoff/回归日志_2026-09-01t_68套件.log)。首跑全量另咬出两雷,均已修:
1. **日界夹具雷(只在每月 1 号引爆)**:daily-close 归属⑧(跨月补签)在「上月最后一天 21:00」直排,与归属⑤的 techA 昨天 20:30–21:30 —— 每月 1 号「昨天=上月末」两夹具撞位 UNIQUE 409;D88 措辞选择器还把撞位说成「时段已过」(过去日期的撞位命中它第一分支)。夹具挪 22:30 错开;教训入册:**日期推导的两个夹具必须错位,别赌日历**。D88 措辞分支的误导面登记待裁(小:撞位在过去日期时报错句答非所因)。
2. **元护栏咬新套件**:noshow ㋙C①「每个打接口的套件必须装测试护栏」抓住 test-observe-fixes 没装 —— 装上(assertTestTarget)。元护栏的覆盖面判据在工作,好事。

**下一步**:你三轮观看(走查单 17 步+本批新修处;夹具已备:bigdemo:lucky-luxe:0=今日报价+绑 Cathy)。做完了,等你验。
