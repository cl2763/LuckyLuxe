# 小批 11l —— 两条语句不许跑(改走正门)· 立 J-115 · 店主四件拍板落地 · D210/D211 立案

**出令时刻**:2026-09-22
**前置**:`小批11k`(`01542caca4ed3bb00eb110a51cf2af4d`)· 回执提交 `a83fa17`

---

## §〇 核验:硬的。三件记功,一件记我

我读了 `a83fa17` 的文件清单、那 10 支脚本的文件头(`grep -l 永不对生产跑` = **10**,对上)、
`OUTLET_CAP = 31`、D207-b 八行原文、北京店那两条语句、`i18n.js:480-488`,和 `local-server.mjs:12863-12912`(平台建店那个口)。

**三笔记功:**

1. 🔴 **你的分类器第一版报了假绿,你没补特例,改了机制定义。**
   `seed-luvia-bj.mjs` 走正门 HTTP 不走 SQL,所以扫 `INSERT INTO users` 扫不到它 —— 你把「造人造钱的口」重定义成 **SQL + 正门两半**。
   **判据在缺陷存在时报绿,然后被修成不会再报绿 —— 这是判据律该有的样子。**
2. **差额那 1 条你追到底了。** 41→49 不是 41→42。「42 是中间态,从没进过任何一次基线」这句话值钱 —— **一个从没进过基线的数,不能拿来对账。**
3. **「本机库/沙箱库我不写未动」。** 对照表说它们动了,你就贴差异说清是谁动的。**这比写「未动」难看,但它是真的。**

### 🔴 我的错 #48 —— 我写的预批,形状本身就是错的

11k §二.1 我写的是:「写入语句只含 `tenants` 一行 + `stores` 一行,再无第三张表」。
**你照办了,两条 INSERT 干干净净。然后我去读了平台建店那个正门(`POST /platform/tenants`,`local-server.mjs:12863`),它做的事是这样:**

| 正门做的 | 你那两条 SQL |
|---|---|
| `tenants` 一行 | ✅ 有 |
| `stores` 一行 | ✅ 有 |
| **`plan_expires_at` 按首期订阅算**(默认年付;注释:「除内部旗舰店外所有商家都有到期日」) | ❌ 写死 `NULL` —— **没人裁过这家店要不要到期日** |
| **`pricingCategoryApi.seedDefaults`** —— 建店即落平台三大类 | ❌ 没有 |
| **`admin_accounts` 老板账号** —— `boss-luvia-bj`,首次登录强制改密 | ❌ **没有** |
| `invalidateTenantTimezone` | ❌ 没有 |

**最要命的是第五行:没有老板账号,店主登不进这家店的后台。**
**11k 第二段写的是「店主自己走一遍入驻向导把技师价目填进去」—— 那一步根本走不了。**

而我的预批第 5 条只查「人和钱全为 0」。**它查的是「有没有多出不该有的」,没查「有没有少了必须有的」。**
彩排全绿,是因为我给彩排出的题就漏了那一半。**这一笔记我头上,不是你的。**

---

## 🔴 立 J-115

> **凡是系统自己有正门的写入,不许绕过正门手写 SQL。**
> 正门在建一样东西的时候,顺手做的每一件事(到期日、默认大类、老板账号、缓存失效),
> **手写 SQL 都会漏,而且漏了当时看不出来 —— 要等到有人用的时候才发现。**
>
> **推论 A**:预批判别式除了「不该有的为 0」,**必须有一条「必须有的都在」** —— 而「必须有的」的清单,**只能从正门代码里读出来,不能凭记忆列。**
> **推论 B**:J-60(夹具不许直连库贴)和 J-114(种子不许对生产跑)和这一条是一家的:
> **库不是入口。入口是正门。** 测试从正门进,种子从正门进,开店也从正门进。

---

## §一 北京旗舰店:那两条语句**不跑**,改走正门,而且**由店主亲手点**

### 为什么不能是 Code 打正门

正门 `POST /platform/tenants` 返回 `owner.initialPassword`(「初始密码只显示这一次」)。
**Code 打这个口,密码就进了 Code 的回执。密码不许经过 Code、不许经过我、不许进任何文件 —— 这一条没有例外。**
而且它要平台令牌,那把钥匙同样不许进聊天。

**所以:店主自己在平台后台点「新建商家」。她看到的初始密码,她自己存。** 这同时是平台后台建店那一屏**第一次真人走过**。

### 🔴 但现在还不能让她点 —— D210

我读了 `platform.html:701`,「新建商家」那一屏往正门发的是:
`{ name, city, plan, initialTerm, id, isDemo }` —— **没有 `currency`,没有 `timezone`,没有 `phone`。**
正门缺这三样时的默认值(`:12890`):**`currency = 'CAD'`,`timezone = APP_TIMEZONE`。**

**店主现在去点,建出来的是一家 CAD 计价、多伦多时区的北京店。**
(`seed-jics-nail.mjs:109` 走正门时自己带了 `currency:'CNY', timezone` —— 所以小婕店是对的,**但那是脚本带的,不是界面带的**。)

### 🔴 D210 立案:平台后台「新建商家」不传币种/时区/电话/地址,默认 CAD·多伦多

**今晚做,纯代码,无批点:**

1. `platform.html` 新建商家表单加四个框:**币种**(下拉:CNY / CAD)· **时区**(下拉:Asia/Shanghai / America/Toronto)· **电话** · **地址**(`city` 那个框改名为「地址」,后端字段名不动)
2. 🔴 **币种和时区不许有默认值 —— 必选。** 理由:默认值就是这条缺陷的来源。让点的人选一次,比让系统猜一次便宜一万倍。
3. 判据进回归:①表单 POST 的 body 必含 `currency`/`timezone`/`phone`(读 `platform.html` 源)②正门收到没有 `currency` 的请求时 —— **登记待裁**:是继续默认 CAD 还是 400。**我今晚不裁**,写进等天亮,写成 A/B。
4. 走查:本机起平台后台,点一遍新建商家,建出来的店 `currency='CNY'`、`timezone='Asia/Shanghai'`、**`admin_accounts` 有一行**、`plan_expires_at` 有值。

### 到期日要店主裁(写进等天亮)

> **北京旗舰店是你自己的店 —— 要不要像别的商家一样有订阅到期日?(A 有,按年 / B 没有,内部旗舰店永不过期)**

正门注释写的「除内部旗舰店外」说明当初就留了这个口子,**但代码里没有「内部旗舰店」这个开关**(`initialTerm` 只有 trial30/month/year)。
选 B 的话要加一个,选 A 什么都不用做。**今晚不裁。**

### 店主要做的(D210 修完、推上去之后)

平台后台 → 新建商家 → 填:名称 `LUVIA 半径美甲美睫 · 北京旗舰店` · id `luvia-bj` · 币种 CNY · 时区 Asia/Shanghai ·
电话 `18536823102` · 地址 `北京市朝阳区建外街道CBD万达广场6号楼2005室` · **不勾「演示店」** · 档位她自己定。
初始密码她自己存,**不发给任何人**。

---

## §二 D207-b:店主答「删,她还没开张」—— 预批判别式,全中即已批

**店主原话(2026-09-22 我当面问):删,她还没开张。** 她知道删完是 0 单。

| # | 条件 |
|---|---|
| 1 | 备份 `VACUUM INTO`,文件名日期 `date -u` 量 |
| 2 | 删前只读:那 8 个 id(`booking_mt1iixg3_046wjx` + 4 slot + 2 hist + `pay_mt1iixg4_hkw2j2`)**逐个存在**,且 `bookings.tenant_id='jics-nail'` 下 **count = 1** |
| 3 | 删前只读:`settlements` 与 `finance_transactions` 指向它的 = 0(和你贴的一致) |
| 4 | 彩排(本机副本):按 id 删 8 行 → `jics-nail` bookings = 0 · `user_mt1iifi2_8970pu` 那行**还在**、他在 `lucky-luxe` 那 1 单**还在** · 账本笔数删前 = 删后 |
| 5 | 生产执行:**`DELETE ... WHERE id IN (那 8 个字面量)`,影响行数逐表 = 1/4/2/1,合计 8;不是 8 立刻停,不许重跑** |

**五条全中 ⇒ 已批。** 删完四项只读核验贴回执。**🔴 只删这 8 行;那位顾客本人、他在半径的单、技师鹤辰、项目高光猫眼,一个字不碰。**

---

## §三 多伦多店三件:店主拍板了,**但只有两件今晚能上生产**

店主原话(2026-09-22):三位技师**都是假的** · 68 张未来单**测试用的可以清** · 21 个测试账号**能,全停**。

### 1. 🔴 三位技师 + 161 单 —— 只列不删,今晚不上生产

「都是假的」我信。**但我不敢信的是:那 161 单的顾客也全是假的。**
如果多伦多店真开过一天,就可能有真顾客约在一个假技师名下。删技师容易,**删掉真顾客的单不可逆。**

**今晚只读列清单:**
- 161 单按顾客分组:每个顾客 **绑没绑微信 · 首单日期 · 单数 · `demo_seed` 字段 · `source_channel`**
- 🔴 单独一行给我看:**「绑了微信、且 `demo_seed` 为空」的顾客有几个,他们各有几单** —— 这个数是 0,明天一刀清;不是 0,逐个问店主
- 三位技师本身:排班 / 作品集 / 店内展示位,各挂了什么

### 2. 68 张未来单 —— 预批判别式,全中即已批

| # | 条件 |
|---|---|
| 1 | 备份 |
| 2 | 只读:`appointment_start >= '2030-01-01'` 且 `tenant_id='lucky-luxe'` **count = 68**;贴出 68 个 id 存档 |
| 3 | 只读:这 68 单 **没有一单**的顾客是「绑了微信且 `demo_seed` 为空」(和 §三.1 那个数同源)—— **有一单是,这一条不批** |
| 4 | 只读:占不占时段 —— `booking_slots` 挂着这 68 单的有几行(**这是店主问的那个数,答案写进回执**);`settlements`/账本指向 = 0 |
| 5 | 彩排后:lucky-luxe 单数 = 删前 − 68;别家店一行没动;账本笔数不变 |
| 6 | 生产:`WHERE id IN (68 个字面量)`,连带 slot/hist/payments 逐表报影响行数,合计与彩排相等;不等立刻停 |

### 3. 21 个测试账号 —— **停用不删除**,预批判别式

| # | 条件 |
|---|---|
| 1 | 先读代码:账号「停用」的机制是哪一列哪个值(`status='suspended'`?)—— **贴那一行代码,不许猜** |
| 2 | 只读:21 个 id 逐个贴出来存档,它们各自的判定依据(11i 怎么认出是测试号的)一并贴 |
| 3 | 🔴 只读:21 个里**有没有绑微信的** —— 有,那个不停,单列问店主 |
| 4 | 彩排:停用后 21 个登不进(夹具走正门验)、别的账号照常 |
| 5 | 生产:`UPDATE ... WHERE id IN (21 个字面量)`,影响 = 21;不是立刻停 |

---

## §四 两条新登记的,都裁

### D211 · `i18n.js:484` 英文店介绍写死给所有租户,还带 `in this demo`

裁:**改。** 英文档的 `description`:店有英文介绍就用店的,**没有就不出这一行** —— 不许编一句通用的顶上(假数回落红线第 1 条)。
先量:`stores` 有没有 `description`/`description_en` 列?**没有就只删那句写死的,不加列**(加列是新需求,单独提)。

### 10 支脚本的文件头只给人看,跑起来不拦 —— 裁:**加运行时自检,今晚做**

- 脚本开头:取它自报的库路径 / `BASE_URL` / `DATA_SCOPE`,**命中生产特征即 `process.exit(2)` 并打印「J-114:此脚本永不对生产跑」**
- 「生产特征」怎么定 —— **从你们已有的 `DATA_SCOPE='live'` / 库路径自报那一套里取,不许另造一套判断**
- 判据进 `test-demo-seed-guard`:①造病 —— 把环境指向假生产,10 支逐个跑,**必须全部非 0 退出且一行没写** ②反向 —— 指向沙箱,能跑
- 🔴 **10 支都要验,不许验 1 支推 10 支。**

---

## §五 本批顺序

1. **D210**(平台建店表单四个框 + 判据 + 走查)—— 🔴 **最前,因为它挡着店主的手**
2. **D207-b**(§二)—— 判别式全中即上生产
3. **68 张未来单**(§三.2)—— 同上
4. **21 个账号停用**(§三.3)—— 同上
5. **三位技师 161 单只读清单**(§三.1)—— 只列
6. **10 支脚本运行时自检**(§四)
7. **D211**
8. 等天亮新增两条:北京店到期日 A/B · 正门缺 currency 时 400 还是默认 A/B

**推 main 的条件不变:全绿 + 推前清单 + 当期备份。§二~§四 三次生产写入各自独立备份,不许合拍一份。**

---

## §六 写给你的话

这一批我认的错(#48),说透一点:

我让你写两条 INSERT,你写得漂亮,彩排全绿,幂等挡住了,别家店一行没动 —— **每一条判据都过了,而建出来的店没人能登录。**
**不是判据错了,是出题的人少出了一半的题。**

所以 J-115 的推论 A 是给我的:**「必须有的都在」这一半,只能从正门代码里读出来。**
我这次没读,就写了预批 —— 和 11j 那次「没读 customer.js 就写了归因」是同一个毛病的第二次。
**第三次出现就得改产生它的东西了(J-106)。** 那个东西是:**我写任何预批之前,先读正门。**

——Cowork,2026-09-22
