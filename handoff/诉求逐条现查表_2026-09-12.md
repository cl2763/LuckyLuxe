# 诉求逐条现查表 · 2026-09-12

> 店主问「我之前提过的一些细小功能是不是还没做」。
> **这一份是现查,不是凭记忆** —— 每条都去代码里看过,证据写在后面。
> 来源:诉求总账(商家回访 11 项 · 待办总方案 P0–P8 · 开检反馈 32 条与 S 系列 · 欠图清单 · 历次口述)。

---

## 〇、结论三句

1. **没有丢。** 你提过的都在总账上,今天我逐条现查了一遍,**状态是准的**。
2. **确实有一批没做** —— 下面 §二 那 11 条。它们不是被忘了,是**被排在后面**。
3. **这 11 条里,一条都不挡开门。** 挡开门的只有上线清单那六件(见 §四)。

---

## 一、我现查确认「真的还没做」的(证据在括号里)

| # | 你提的 | 现查 | 证据 |
|---|---|---|---|
| 1 | **线下协议书拍照上传**(你 09-03 提) | **一行代码都没有** | 全仓搜「协议书」:`apps` / `miniprogram` **0 个文件命中** |
| 2 | **公司宣传网页(landing page)** | **没建** | `apps/web/` 只有 6 个页面:admin / index / platform / share / sign / wechat-simulator |
| 3 | **AI 快速修图** | **没做**(只调研过) | 全仓搜 `qwen-image` / `image-edit`:**0 处** |
| 4 | **S2 顾客线上自助充值 / 买卡** | **两端都没有** | 小程序储值页那两个按钮是:`去充值 ›` → 跳商城、`确认到账` → `confirmRecharge`(**到店/线下充值的确认流**);服务端的「购卡 / 随单充值」(B3)是**店里开单时**用的,不是顾客自己付 |
| 5 | **网页顾客端三块**(AI 客服 / 门店导航 / 消息中心) | **网页侧欠,小程序侧有** | `miniprogram/pages/` 里 `ai-chat` `messages` `store-location` **三页都在** |

---

## 二、总账上写着「未动 / 半成」,今天仍然成立的 11 条

**上面五条,加上这六条:**

| # | 你提的 | 卡在哪 |
|---|---|---|
| 6 | 券临期提醒 / 到店提醒**真发到顾客手机** | 队列、文案、记录全活,**只差发出去那一下** —— 等小程序上架 + 订阅消息 |
| 7 | 多项目预约 → 多单串联结算 | ≈9 成;缺**卡耗按项** · **小记按项号** · **到店组合并推送**三处 |
| 8 | S1 价目表 ≠ 服务项目 | ≈9 成;缺**旧服务口不能改上架位** · **存量 6 项挂橱窗无大类** |
| 9 | S5 网页「通用设置」页 | 半成;剩**员工可见 + 身份裁字段** · **头像两端** · **币种单一出处** · **短信自助重置** |
| 10 | 旧系统数据迁移 | 导入工具在,**缺的是从旧软件把数据弄出来** —— 等你给两家软件名 |
| 11 | 网页端处置 / 售后弹层视觉同源 | 未动 |

**另外欠你的图**:小程序顾客端首页 · 网页顾客端首页 · S2 卡包与商城 · B-4 身份架构 ·
平台工具箱整体设计 · 《上线落地方案》· AI 修图设计图。

---

## 三、🔴 真正会丢的那一类 —— 需要你帮我核一次

上面那些**不会丢,因为它们落了账**。
会丢的是**你在聊天里说过、而当时没写进台账的那一句** —— 那种东西我这边没有任何记录能找回来。

**所以请你做一件事(五分钟):**
打开「有迹诉求总账」那一页,**只看第二、三、五节**,回我一句:
**「有没有你记得提过、但上面找不到的?」**

哪怕只是「我好像还说过 XX」,你说出来我就当场入账、排进队列。
**你记得的那一句,比我的清单值钱** —— 这几个月里好几件事(网页自动轮播、
深色下看不清的字、主页为什么变了样)都是你一句话逼出来的,不是任何判据扫出来的。

---

## 四、这 11 条挡不挡开门 —— 一条都不挡

**挡开门的只有这六件**(上线清单现查):

| 谁做 | 事 | 状态 |
|---|---|---|
| **Code** | 签名密钥去默认值 + fail closed(07c §一) | **在做** |
| **店主** | 两串密钥生成并存好 | ✅ **今天做完了** |
| **店主** | 批准推 main(领先 265+) | 等 Code 做完密钥那件 |
| **Code** | 推前手动备份一次生产库 | 批准后 |
| **店主** | 两家真店老板密码亲手设 | 上线当天 |
| **Code** | 生产只读核两件(`ALLOW_DEMO_ADMIN_LOGIN` 未设 · 会话唯一索引在) | 推上去之后 |

**§二 那 11 条,全部可以开门之后慢慢补。**
其中 **#6(提醒真发出去)** 本来就要等小程序上架,**它不是我们欠的,是流程欠的**。

---

## 五、我建议的补齐顺序(开门之后)

1. **#6 提醒真发送** —— 上架当天就能通,顾客感知最强。
2. **#4 顾客自助充值/买卡** —— 微信支付你已经申请下来了,这条的外部件齐了。
3. **#7 / #8 那两个 ≈9 成的缺口** —— 各就三处、两处,收口快。
4. **#9 S5 通用设置四件**。
5. **#1 协议书拍照上传**(轻,随手件)· **#11 弹层同源**(轻)。
6. **#2 宣传页** · **#3 AI 修图** · **#10 旧系统迁移** —— 这三件要你先给料
   (文案与调性 / 样片与指令 / 两家软件名),给了才动得了。

---

**这一份我落了两处(桌面交接夹 + 仓里 `handoff/`),以后每次有新诉求就往总账上加一条。**
