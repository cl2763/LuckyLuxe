# 小批05r 补二 — `18901e1` **收**(六张 PNG 我逐个验了魔数、旗舰店/北京店两张亲看)· 待裁 #2 裁:网页「点一下重试」收,图记 v3.2 修订 · 待裁 #3 裁:**现在就做**——顶栏显当前店名(20 分)· 图 v3.2 第二处修订:AI 一句通栏放英雄区下方 · 然后 D155

> 2026-09-08 · Cowork 现查:`handoff/night-runs/D154截图/` 六个文件头都是 `89504e47`;`01_旗舰店_本月` 与 `03_北京店_本月` 亲看;`money-format.js` 千分位只加整数位;`assertion-baseline` observe-fixes 60;`local-server.mjs` 17,745 · 两处各一份

## 一、收

- 六张截图真在仓里,判据 ⑩ 数文件读魔数、目录多一张也红——**收**,这是「回执引用的文件必须存在」的图片版。
- 大数「CAD $ 25,885」币种一次、千分位有;四小牌走 `money()` 也带千分位——**收**。`money-format.js` 是新出口,币种红线仍只此一处,对。
- 待裁 #1 落码,`test-observe-fixes` 两条回到验、基线 60——**收**。
- `points_policy` 开机留痕幂等 → 本机库两把刀第一次同时绿——**收**,这条正是 J-29 的价值:以前「未动」永远证不了,不是库真的动了。
- 沙箱「有动」如实报(调度器 +4/+8、小程序套件改一行 `membership_config`)——**收其如实**;调度器在跑机期间写库是它的本职,不算病。
- `miniprogram-automator` 装在仓外 `/Users/changliu/mp-automator`、三把小程序刀真跑——收;这个路径写进 `本地自查账号.txt` 旁边的「本地环境备忘」一段(不含任何密钥)。

亲看两张图后的两句:旗舰店那张与图 §三 的骨架对上了(周期条 / 大数 + 折线 / 四小牌 2×2 / 台面嵌入 / 要处理);北京店本月 0 是真数(50 单全在 9 月 8 日起、无日结、无账本),不是取数失败,对。

## 二、待裁 #2 · 裁:**网页端「点一下重试 · 或稍后再看」收**;图记 v3.2 修订

图 §六 那句「下拉重试」是照小程序写的,网页没有下拉,你改得对、报得也对(这不是悄悄发挥,是摆上来了)。裁:网页端用「点一下重试」;小程序端段 9–11 保留「下拉重试」;判据 ⑫「网页首页不许出现『下拉』」留着。我把这条写进图 v3.2 修订记录(Cowork 改图,Code 不动图)。

## 三、待裁 #3 · 裁:**现在就做**——顶栏显当前店名(D156,≤ 20 分)

网页后台看不出自己在哪家店,这在多店/平台代运维时是改错店的真风险;而且你截三家店的图分不出来,这件事本身就说明该显示。图 §三 顶栏没画店名,但左上「有迹 · 商家后台 / 店主控制台」那行第二行是空位:**「店主控制台」改成当前店名**(`stores.name`,从 `/admin/auth/me` 带回,前端不拼不猜),没取到显示「—」不显示旧店名。判据:`[data-tenant-name]` 的 `textContent` === 该店 `stores.name`(三店各一条);切店后旧店名 0 次出现(造景:先登旗舰店再登北京店);造病:前端写死店名 → 红。补三张今日截图入同一目录(文件名同规)——这次三张图靠店名就能分出来。图 v3.2 第三处修订我来记。

## 四、图 v3.2 修订(Cowork 记,三处)

1. 网页失败态按钮文案「点一下重试」(小程序仍「下拉重试」);
2. **AI 今日一句**在网页端放**英雄区下方通栏**(现实现如此,读起来比塞在右栏小牌里顺——我接受,合同随之改);
3. 顶栏第二行显当前店名(D156)。

## 五、然后 D155,按 05r §二 原文;再补段 7。

## 六、交齐

D156 三店判据 + 造景/造病 + 三张新截图 · 提交号 · 回归/断言增量 · 整跑时长 · 棘轮两数 · 未推 main。
