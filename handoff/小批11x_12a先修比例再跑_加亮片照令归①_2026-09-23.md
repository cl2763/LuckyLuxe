# 小批 11x —— 12a:先修比例再跑第一轮 · 「加亮片」照令归 ①

## §〇 记功
试 1 张再试 2 张,没有盲跑 82 张;第一张翻车就停,两件都摆给我裁而不是自己定。**2 元换来的三条事实**(base64 可用免 COS · 单张 23 秒 · 输出不是原图比例)都比 23 元的盲跑值钱。

## §一 两件都按你倾向裁
1. **先修比例,再跑。** 预算:探比例这一步 **≤ 5 元**(每个 `size` 取值最多 2 张)。试的顺序:传原图宽高(`WxH`)→ `adaptive` / `auto`(以方舟文档字面为准,不猜参数名)→ 若都不行,试 `size` 给最接近原图比例的档。**三种都不行 ⇒ 停,报我**:那意味着「同一构图」要靠贴回而不是模型,12a §1.3 的甲面贴回 PoC 从「3 张」升级成主线,不再是可选项。
2. **「加亮片」照令归 ①「改了不该改的」**,不另开类。但在翻车记录里 **① 拆两级**:①a 改甲面(图案/形状/颜色/钻)—— 最重,一票否决;①b 改构图/手型/背景。**对美甲店,①a 一张都不能有;第一轮结束按 ①a 单独算一个率。**

## §二 比例修好后的第一轮
- 照 11w:41 张 × combo × 两模型,30 元先停报数,60 上限
- 对比包每格加**输出尺寸与原图尺寸比**;「构图没变」那列由此可判
- 回执单独一份

## §三 顺带记一条给 12b
12b 3.1 写「端侧压缩长边 ≤ 2048」—— **如果模型输出天然 ≤ 2560,那「清晰度不低于原图」这条对 4032 的原图永远不成立**。12a 回执里给一句:线上该按什么尺寸送模型、什么尺寸存,让 12b 改配置不改代码。
