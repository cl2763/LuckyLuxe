# 写库脚本护栏 · 三列清单(店主 03b 裁定二 + 03c 裁定二,2026-09-02)

## 尺子(一把量到底,写明是什么)
```
扫描面   tools/*.mjs + apps/api/*.mjs(排除 test-* / run-*)
机制定义 写库 = 直连 sqlite 写(DatabaseSync / .prepare().run / db.exec)
         或 走 HTTP 打后台写口(method: POST|PUT|PATCH|DELETE)
分类     A 独立可跑脚本(人直接 node 跑)= 护栏的对象
         B 服务内模块(被 local-server import,不能独立跑)= 自报由服务启动那一行统一做
现测     写库文件 111 个(含测试 65)· 非测试 46 个 · A 类 24 · B 类 22
```

## A 类 · 24 个(逐个三列)

| 脚本 | 类型 | 去默认目标 | 自报库路径 | 事务 |
|---|---|---|---|---|
| `apps/api/e2e-stress-test.mjs` | HTTP | —(无目标库参数) | ⬜ 待补 | — |
| `apps/api/local-server.mjs` | 直连库+HTTP | —(无目标库参数) | ✔ | 有事务 |
| `apps/api/other-tenant-names.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | — |
| `apps/api/probe-intents.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `apps/api/sandbox-mirror-jury.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | 有事务 |
| `apps/api/smoke-qwen.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `apps/api/store-matrix.mjs` | HTTP | —(无目标库参数) | ⬜ 待补 | — |
| `tools/audit-test-tenants.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | — |
| `tools/clean-test-tenants.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | 有事务 |
| `tools/configure-jienail.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/ops-cos-delete.mjs` | HTTP | —(无目标库参数) | ⬜ 待补 | — |
| `tools/ops-rename-tenant-display.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | — |
| `tools/ops-reset-finance-lock.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/prepare-migration.mjs` | 直连库 | —(无目标库参数) | ⬜ 待补 | — |
| `tools/seed-addon-groups.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-bigdemo.mjs` | 直连库+HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-customer-doc-scene.mjs` | 直连库+HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-demo-coupons.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-demo-today.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-demo-twin.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-jics-nail.mjs` | HTTP | ✔ requireTarget | ✔ | — |
| `tools/seed-selfcheck-data.mjs` | 直连库+HTTP | ✔ requireTarget | ✔ | — |
| `tools/smoke-cos-upload.mjs` | HTTP | —(无目标库参数) | ⬜ 待补 | — |
| `tools/verify-jics-kb.mjs` | HTTP | ✔ requireTarget | ✔ | — |

## B 类 · 22 个(不改,写理由)

这些是**被 `local-server.mjs` import 的服务内模块**,不能独立运行 ——
它们写的库就是服务开的那个库,**自报由服务启动时那一行「数据库: <绝对路径>」统一做**,
每个模块再自报一次是噪声。清单:

```
  apps/api/account-refund-schema.mjs
  apps/api/account-refund.mjs
  apps/api/admin-auth.mjs
  apps/api/ai-utils.mjs
  apps/api/cash-notes.mjs
  apps/api/demo-facts.mjs
  apps/api/demo-reset.mjs
  apps/api/deposit-audit.mjs
  apps/api/hero-slides.mjs
  apps/api/import-customers.mjs
  apps/api/import-services.mjs
  apps/api/ledger-guards.mjs
  apps/api/legacy-demo-retire.mjs
  apps/api/notify-scheduler.mjs
  apps/api/platform-ops.mjs
  apps/api/platform-session.mjs
  apps/api/pricing-categories.mjs
  apps/api/quote-state.mjs
  apps/api/schedule-board.mjs
  apps/api/store-identity.mjs
  apps/api/stored-value-reversal.mjs
  apps/api/tenant-visibility.mjs
```

**唯一例外已查**:`apps/api/ai-utils.mjs` 里的 `AI_BASE_URL || 'https://api.openai.com/v1'`
是 **AI 模型网关地址,不是数据库**,不在本律辖内。

## 事务那一列 · 「包了事务必须验回滚」(店主 03c 立)

A 类里含事务的只有 `tools/clean-test-tenants.mjs` 一个;
B 类里 8 个(account-refund / demo-reset / hero-slides / import-customers / import-services /
ledger-guards / sandbox-mirror-jury / stored-value-reversal)。
**本批只完成「去默认目标 + 自报路径」两列;第三列(逐个造病验回滚)条目多、且要各造各的失败点,
我不在这一批里草草打勾** —— 单独立项,清单在此,逐个做完再标。

## 案由(为什么有这张表)

1. **02x**:我以为 `DATA_DIR` 能改 `seed-bigdemo` 的目标,**它走 HTTP、`SEED_BASE_URL` 有默认值 4128** ——
   打错了不报错,148 行演示数据写进本机库。
2. **03d**:手敲 curl 又把 4128 敲进去,冲销了一条**真实收入**。

病根不是「没白名单」,是**「有默认值,所以打错了不报错」**;
端口会骗人,**只有库文件绝对路径不会**。
