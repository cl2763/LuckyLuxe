# 回执 04d-2 — AI 复测 · 真模型 12 场景(跑完,走查文档已交)

> 只打沙箱 4310。**生产库未动 · 本机库未动**(本机库对账器原样输出:
> `✅ 逐表零差异(**表、行、列、索引都比过**)—— 「本库未动」这句话有证据支撑`)。
> 走查文档:[`handoff/AI复测_真模型12场景_2026-09-03.md`](AI复测_真模型12场景_2026-09-03.md)(375 行,含 source 地图 + 矩阵 + 逐场景对话原文)。
> **提交号 `28877df`**。未推 main。回归 **89/89**,断言 **2,897 持平**(本段只跑走查、没加判据)。

---

## 〇、先说最要紧的一条

**多轮对话里只要有一轮用词没命中关键词白名单,这通对话的 AI 就不再说话了**(直到人工介入)。

一句话之差的对照实验(旗舰店沙箱,真模型):

| # | 做什么 | 结果 |
|---|---|---|
| ① | 干净会话说「你们**几点关门**?」 | **静默不答**,`status → needs_human` |
| ② | **同一会话**接着说「你们**营业**时间几点到几点?」(命中白名单) | **仍然不答** |
| ③ | 干净会话首轮就说「你们**营业**时间几点到几点?」 | **模型答** |
| ④ | 干净会话命中 → 第二句「周日开吗?」 | **从此哑掉** |

「周日开吗?」是顾客最自然的第二句。这不是模型答错 —— **是它根本没被问到**。
Cowork 04d §二 已经把「关键词白名单」写成产品事实并说「要不要放宽应答面是店主决定」;
**我这一轮量出来的是它的第二层:一次没命中就永久静音。** 这一条我不动,请您裁。

---

## 一、两跑并排(原矩阵不动,对照组并列)

您的矩阵是定稿,我按它原样跑了一遍;44 轮里只有 **4 轮**到模型层。
为了让每个场景都看得到模型的真实表现,又跑了一遍**对照组**:**只改措辞让它命中白名单,
轮次结构/预期/断言一字未动**。两份结果并排保留,原矩阵那份没被替换。

| | 到模型层 | 规则层 | 静默不答 |
|---|---|---|---|
| **原矩阵**(您定稿的措辞) | **4** | 12 | **21** |
| **对照组**(只改措辞) | **14** | 18 | 5 |

| 场景 | 原矩阵 | 对照组 | | 场景 | 原矩阵 | 对照组 |
|---|---|---|---|---|---|---|
| A1 旗舰 | `静静静静` | `模模模规` | | B3 镜像 | `静静` | `规静` |
| A2 镜像 | `静静静静` | `模模模规` | | B4 旗舰 | `静静` | `模静` |
| A3 镜像 | `模规模` | `模规模` | | C1 镜像 | `规规规` | `规规规` |
| A4 旗舰 | `静静静` | `规规静`※ | | C2 旗舰 | `规规规` | `规规规` |
| B1 镜像 | `静静静` | `规规规` | | C3 镜像 | `模静静` | `模静模` |
| B2 旗舰 | `规模静` | `规模静` | | C4 旗舰 | `规规规规` | `规规规规` |

※ A4 跑了三版,归因见 §二。

---

## 二、归因:红的六处,**四处是我的,两处是规则**

按「红了先归因」,一条一条查到底:

| 现象 | 归因 | 查到的事实 |
|---|---|---|
| 21 轮静默 | **规则**(既定设计) | 关键词白名单 + 一次没命中就永久静音(见 §〇) |
| A2「英文问、中文答」 | **我的跑机** | 取字段只取了 `answerZh`。模型两栏都出了 —— 改成按 `lang` 取之后**英文问英文答**,连报价采集模板都出英文版 |
| A4「模型不用知识库」(第一版) | **我的跑机** | 知识库口写成 `/admin/tenant-kb/facts`(真口 `/admin/kb/facts`),**404 了跑机一声不吭** |
| A4(第二版仍不用) | **产品的一个静默失败器** | `/admin/kb/facts` 的 `allowed` 只有 5 个键,别的键**静默丢弃还回 200** —— 从响应上分不出「存了」和「没存」。自由条目要走 `/admin/kb/entries` |
| B1 想测模型意图却走了采集 | **规则** | 句子里只要有「美甲」,报价采集就抢在模型前面(优先级,不是错) |
| C3 归还 AI 后不答 | **规则(措辞)** | 对照组同一场景归还后模型正常答 —— 归还链路是通的 |

**三条我自己的教训**(都写进跑机了):
1. **造景不校验返回码 = 静默失败器** —— 已给跑机加 `mustOk`,任何造景请求非 2xx 直接抛;
2. **取字段取错 ≠ 产品有病** —— 报「模型答错了」之前先确认自己取的是哪一栏;
3. **造景律又栽一次** —— 第一版 source 地图在空店上量、A4 的 KB 根本没造上。

---

## 三、🟡 顺带咬出一条产品的静默失败器(入册请裁)

`PUT /admin/kb/facts` 的 `allowed = ['brandName','assistantName','storeAddress','depositAmount','currency']`,
**不在这五个里的键静默丢弃、照样回 200**,响应体里也看不出来。
商家(或将来的平台代填)写了个没在白名单里的键,界面上会显示「保存成功」而实际没存。
归族**静默失败器族**。改法两选一,请您裁:①不认识的键回 400 并点名;②照收但在响应里回
`ignoredKeys: [...]`,前端提示。**本批没动**(改的是接口口径)。

---

## 四、几处断言的实测结论

✔ A2 回复语言=en · ✔ B1 报价单落小婕店 · ✔ A4 答不了不瞎编、跑题静默 ·
✔ C1 quoted 段不重新采集 · ✔ C3 人工中不答/归还后能答 · ✔ B4 status 不回退
⬜ **C2「报价过 48h 不复用旧价」** —— 走的是 `quote_draft`,**需要您看回复原文裁**是否算「不复用」(原文在走查文档 C2 一节)。

---

## 五、费用

真模型 **116 次调用 · prompt 536,036 · completion 31,256 · 合计 567,292 token**。
计数是从模型响应的 `usage` 累计的,**不是估** —— 本批给 `ai-utils` 加了计数,出口 `/health.aiUsage`
(mock 时全 0,可当「这一跑到底打没打真模型」的判据用)。

---

## 六、沙箱拉回 · 库 · 回归

- 沙箱已按原样拉回(**不带** `--ai-env` = mock),`/health.aiUsage` 归零 = 新进程没带 key;两端口 200。
- `apps/api/.env.ai-sandbox` 留在本机、未入库(`.gitignore:18`);**内容我没看、没打印、没复制**。
- **生产库未动 · 本机库未动**(逐表零差异);沙箱库逐表对照与逐条来源见走查文档 §五 ——
  **58 通走查会话留在沙箱里没删**,您可以在工作台逐通点开看。
- 回归 **89/89**,断言 **2,897 持平**;巨型文件 `local-server.mjs` **18,145** / `admin.js` **8,550**。

<!-- 回归自证 批次=04d2 套件=89 断言=2902 日志=handoff/regression-logs/回归日志_2026-09-03_04d2_28877df_89套件.log -->
