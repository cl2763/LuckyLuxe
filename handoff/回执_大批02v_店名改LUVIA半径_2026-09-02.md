# 回执 · 大批 02v —— 店名改「LUVIA 半径」+ 判据改锚 + 外线搬走

## 一 · 搬家(裁定六)+ 同族又扫出一片

`luckyluxe-source`(171M/131 文件)与 `luckyluxe-remotion-tour`(984M)已 `mv` 到桌面各自单独文件夹,
原样不改名、未用 `rm`。两边 `ls` 回显自证;`git status` 空(本就不在库)。

**但同族不止两个。** 搬完顺手现扫仓库根,还有一整族外线目录:

| 目录 | 体量 | git 跟踪 | 主仓引用 |
|---|---|---|---|
| `luckyluxe-output/` | 133M / 50 项 | 0 | 0 |
| `output/` | 145M / 7 项 | 0 | 3 |
| `LuckyLuxe-ClaudeDesign-confirmed-v5 / v6` | 27M + 29M | 0 | 0 |
| `radius-luvia-phase1 / -v2 / -v3` | 16M+18M+17M | 0 | 0 |
| `radius-luvia-logo-proof-v4` | 472K | 0 | 0 |
| `LuckyNail-127sqm-concepts-v1 / -corrected-v2 / -layouts-v3` | 6.0M+5.6M+9.0M | 0 | 0 |
| `lucky-nail-brief` | 1.6M | 0 | 0 |
| `tmp/` | 47M | 0 | 14 |

**只报不动**(您只裁了两个)。`luckyluxe-output` 里是 floorplan / reception / vip-nail-room 渲染图,
与 `luckyluxe-source` 明显同一条线;`radius-luvia-*` 看着是 LUVIA 品牌那条线。请裁。

## 二 · 店名改名 —— 落刀先红,红清单在此

### 🔴 我上一批的清单**漏了一整族**:无空格 `LuckyLuxe`

刀落地时命中 **104 处**,而我预判 41(A 类)。查刀不查病,对账严丝合缝:

```
带空格 'Lucky Luxe'  85 行
无空格 'LuckyLuxe'   16 行   ← 上一批清单整族漏掉
刀自身注释            3 行
合计 104 ✔
```
**那 16 处里有 9 处是顾客可见的小红书话术标签**(`#LuckyLuxe` / `#LuckyLuxeAtelier`)——
属 A 类,是**直接发给顾客的字**,多租户下等于让别家店替本店打广告。
上一批我用 `grep "Lucky Luxe"` 带空格搜,漏了同族;本刀用**形状正则**才咬全 —— 又一次点名处必扫同族。

### 改完:80 处逐个落白名单,A 类字面量归零

| 类 | 处置 |
|---|---|
| **A 类(已改)** | 26 处拔回落/改读字段:`brandName` 回落、营业时间答复两处、系统提示词、分享文案 6 句 + 9 个标签、share.html/js、index.html、admin.html、模拟器、`admin-auth` 显示名、三个 seed 工具、种子 KB 四处、`local-server` 五处 |
| **B 类(不动)** | 启动日志 / 包描述 / CI 注释 / 开发者 console 前缀 `[LuckyLuxe][privacy]` / 员工演示密码常量 |
| **旧栈(不动 + 留痕)** | `supabase/001_init.sql`、`prisma/seed.ts`:零引用、回归跑不到。**白名单理由里写明「若将来复活须同步改名」** |
| **D 类(保留)** | 记录当年踩坑的注释;文档面 299 处不改(改了就是篡改历史) |

**⬜ 一处请您裁**:`miniprogram/pages/merchant-forgot/index.wxml` 里的
**平台客服微信号 `LuckyLuxe-Support`** —— 这是**真实微信账号**,改文案会让商家加不到人。我按 B 类留着。

### 数据层:四处真相归一 + 英文名甲案

```
本店 tenants.name  = LUVIA 半径
本店 stores.name   = LUVIA 半径      name_en = LUVIA     ← Ontario 已去掉(您裁)
本店 brandName     = LUVIA 半径
本店 assistantName = LUVIA 预约助手
镜像演示店         = LUVIA 半径(演示)
```
`name_en` 走**甲案新列**,`try/catch ALTER TABLE ADD COLUMN`(公约⑧:只写 CREATE TABLE 等于只对全新库生效)。
**渲染链也接了** —— `api.js` 透传 `storeNameEn`、首页按 `lang` 选;
`name_en` 为空的租户**英文态退回中文店名**(空态说真话,不空白不回落)。

### 迁移(裁定三):三行都带租户限定,**且只在现值仍是旧名时才改**

您要的两条我都做了,还多加一条自己的:
- 三行都带 `tenant_id='lucky-luxe'`;
- **只在现值是旧名时才 UPDATE** —— 无条件 UPDATE 会把您日后在门店设置里自助改的名字
  **在下次重启时抹回去**(与 `perf_base_cents`「派生字段两个写入者」同一个坑);
- **生产库这批没碰。**

**隔离守的运行时版(迁移前后各读一次)**:
```
迁移前  本机库 jics = Jie's Nail 小婕        沙箱库 jics = 小婕的店(演示)/(沙箱)/Jie's Nail 小婕
迁移后  本机库 jics = Jie's Nail 小婕        沙箱库 jics = 小婕的店(演示)/(沙箱)/Jie's Nail 小婕
        ↑ 一个字没变
```
**两店对照(同一接口只换租户头,4310 现取)**:
```
lucky-luxe       中文='LUVIA 半径'      英文='LUVIA'
jics-nail        中文="Jie's Nail 小婕"  英文=None
demo-lucky-luxe  中文='LUVIA 半径(演示)' 英文=None
```

### 判据落库:`test-store-name.mjs` 八条

① 店名零硬编码(白名单式,**32 个文件逐条写理由 + 处数**;处数增加即红)· ①b 条目棘轮 ≤40 ·
①c 扫描面下限 200 · ② 三处真相逐字相同 · ②b assistantName 口径 · ②c `name_en` 落地 ·
③ 隔离守(**现取别家店名**,不写死小婕店叫什么)· ③b 被测集合非空。

**白名单不用行号做 key** —— 行号一改白名单就散,那是判据自己会腐烂;改用「文件 + 处数」。

**造病验红**:把本店 `stores.name` 与 `brandName` 清空 →
```
顾客端 /stores 现取:storeName=None,整份响应零回落(不含 Lucky Luxe / 小婕 / Jie's / 星野)
刀:❌ ② 四处真相归一 :: {"tenants":"LUVIA 半径","stores":"", ...}
还原(沙箱库 cmp 字节一致)→ ✅ 八条全绿
```

## 三 · 裁定五 · 四条判据改锚(这一条比改名本身重要)

新增共用现取件 `apps/api/other-tenant-names.mjs`:**从数据现取「当前租户之外的所有店名」**
(stores + tenants + brandName 三处并集,剔掉本店同名与过短的),再断言结果里一个都不含。
**店名再改多少次刀都还在守;新开一家店自动纳入被测集合。**

| 刀 | 原来 | 现在 |
|---|---|---|
| `test-tenant-hygiene` | 判「blob 里不许出现 Lucky Luxe」 | 现取别家店名逐个查 + **反向守集合非空** |
| `test-hero-slides` | 名字说「不含 Lucky Luxe 的图源」 | 名字改准(判的本是图源文件名)+ 加两条现取判据 |
| `test-noshow-aftersales ㉟` | 只数 `Lucky Luxe` | 改数**店名形状**(旧名+新名都算) |
| `tools/verify-jics-kb` | 检测词只有旧名 | 旧名 + 新名都留;判据名去掉具体店名 |

**产品代码里也有同一处病**:`shouldSendNewCustomerWelcome` 靠「欢迎来到 Lucky Luxe」这个店名串
判断"欢迎语发过没有" —— **店名一改就再也匹配不上,会对老顾客重发**。改锚在**结构词**(助手自称)上。

## 四 · 我这批自己踩的两处(都被机器抓住,不是我看出来的)

1. **误创游离空库**:起沙箱时我传了相对路径参数,脚本按 `$PWD` 解析,
   在**仓库根**造出一个 `sandbox-data/lucky-luxe.sqlite`(users=1/bookings=0,而真沙箱是 273/464/45),
   **而且它不在 `.gitignore` 里**。是 `start-sandbox.sh` 打印的「库路径」把我抓住的。
   两份游离库已 `mv` 出仓库(不 `rm`,留证在 scratchpad);`/tmp/ll-sandbox-4310.env` 里那条错路径也已改正。
   **CLAUDE.md 早写了「沙箱一律不带参数起」——我没照做。**
2. **棘轮刀咬住我自己**:`local-server.mjs` 18243 → **18272(+29)**。
   按公约「两个巨型文件只许搬出、不许新增」,**我没申请抬棘轮**,而是把这批新增的搬出去:
   新建 `apps/api/store-identity.mjs`(31 行,迁移 + 欢迎语措辞),再压掉几处注释 →
   **回到 18243/18243,零上涨**。
3. **交付完整性刀**(仓库原有)咬出新模块 `store-identity.mjs` 没进 git ——「漏一个=克隆出来起不来」。
   已入库并 `git ls-files --error-unmatch` 逐个回显自证。

## 五 · 回报固定六项

1. **改了什么**:店名四处真相归一 + `name_en` 新列与渲染链 + A 类 26 处拔回落/改读字段 +
   四条判据改锚 + 新刀 `test-store-name`(8 条) + 新件 `other-tenant-names` / `store-identity`。
2. **在哪一层验的**:数据层(两库 sqlite 现读)+ 接口层(4310 三租户现取)+ 小程序真页(截图)+ 断言层。
3. **同类扫尽**:无空格 `LuckyLuxe` 整族(16 处,上批漏);「判据锚在业务值上」这一类全仓扫出 4 条判据 + 1 处产品代码,全改。
4. **用什么数据验的**:反例(店名清空 → 零回落 + 刀红)、三租户对照、迁移前后各读一次小婕店。
5. **重复稳定性**:全量回归本批跑 4 轮(2 轮被自家刀拦下:棘轮上涨 / 新模块未入库),末轮 73/73 绿。
6. **残留与不确定**:
   - **英文态截图取到的是镜像演示店**(它没配 `name_en`),所以图上英文显示中文名 —— 那正是空态规则,
     真店的英文名以**接口层三租户对照**为证(`lucky-luxe` 英文=`LUVIA`)。
   - `LuckyLuxe-Support` 客服微信号未动,**待您裁**。
   - 仓库根还有一族外线目录(见第一节),**只报不动**。
   - **走查单 artifact 上还写着「Lucky Luxe(演示)」** —— 按您 02u 第八条,**提醒您改**。

**自评 L1–L5**:L1 末端(真页 + 接口 + 库三层)· L2 同类扫尽(无空格族 + 锚在业务值族)·
L3 有书面标准(您 02v 的四裁字面)· L4 反例(店名清空)· L5 重复稳定(4 轮)。**全过。**

---

**棘轮**:`local-server.mjs` 18,243/18,243(**先涨 +29 后搬回,净持平**)· `admin.js` 8,552/8,552(持平)·
`customer.js` 2,811 · `me` 638 · `settlement` 929 · `orders` 829(持平)。
新增:日志下限 40→41 · 自证块 4→5 · 店名白名单 32 条/80 处(只减不增)。

**断言**:基线口径 2,752 → **2,763(+11**,= 店名刀 8 + 改锚新增 3);全日志纯 `^ok` **2,768**。
**全量回归**:✅ **73/73 套件通过**。
**生产库未动 · 本机库未动(只跑了改名迁移,已逐条列出)· 生产库这批不碰 · 未推 main**。

<!-- 回归自证 批次=02v 套件=73 断言=2768 日志=handoff/regression-logs/回归日志_2026-09-02v_73套件.log -->
