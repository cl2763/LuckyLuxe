# 待办挂账清单(店主/Cowork 裁决后的悬账,销一条划一条)

> 规则:每条写清「怎么来的 / 为什么现在清不掉 / 什么条件下清 / 清账动作」。
> 新增挂账必须同时出现在当批结果报告里;销账时在本文件划掉并注明销账 commit/凭据。

## 挂-001 · 轻档案「自动化验证-勿动」¥495 测试储值余额

- **怎么来的**:2026-08-11 结算单服务分组批 L5 支付菜单路 10 次(¥33×10=330)
  + 2026-08-12 技师代充放行批 L5 技师路 5 次(¥33×5=165),共 15 笔,
  user_msojbzxv_h59nc8,note 均带「结算单内代充」标记。组合矩阵未再入账(面板只开选关)。
- **为什么清不掉**:储值只有入没有出——耗卡=确认收入,不能拿来冲测试;现无「储值红字调整」路由。
- **清账条件**:S2 储值红字调整路由上线(店主裁决 2026-08-12:留,S2 后清)。
- **清账动作**:红字调整 −¥330,note 引用本条挂账号;同时把该轻档案会员资格复核
  (any_recharge 口径下它因测试充值成了会员)。

## 挂-002 · 测试租户 nsas-a-msok023f(已停用,不物理删)

- **怎么来的**:2026-08-11 standalone 跑 test-noshow-aftersales 没起 scratch 服务器,
  套件在 4128 真库建了这个测试店(数据全部圈在该租户内)。
- **店主裁决(2026-08-12)**:停用即隔离,不做物理删库 —— **本条按裁决即视为已处置,留档备查**。
- **护栏(已采纳)**:standalone 跑任何套件一律
  `DATA_DIR=/tmp/xxx PORT=43xx node local-server.mjs` 起 scratch 服务器 + `TEST_BASE_URL` 指过去;
  套件文件头已写用法注释。

## 挂-003 · 「店主验签」档案 +¥5 测试储值(D25 放行路)

- **怎么来的**:2026-08-12 终验二轮批,D25「绑定后放行」L5 ×5,每次手输 ¥1 真实入账
  (店主验签 user_mslzgvy0_ijmrps 是库里现成的已绑定测试档案)。
- **清账条件**:与挂-001 同批,S2 储值红字调整路由上线后一并清(合计红字 −¥500 与 −¥49,500 分)。

## F1 · 分叉债:storeToday 双端两份实现(登记 2026-08-12,扫雷批丁之二)

- 小程序 utils/storeclock.js 与网页 admin.js 各有一份 storeToday(语义同源、代码两份)。
  按四之九:日界类计算逻辑双实现=底层分叉缺陷。**S1 收敛裁决**(候选:后端下发/共享模块)。

## F2 · 分叉债:裸 wx 导航 106 处(登记 2026-08-12,扫雷批裁决②)

- navigateTo 77 / redirectTo 12 / switchTab 10 / reLaunch 7,页面栈满时静默 fail(理论风险未烧过)。
- 已建 utils/nav.js 统一封装+CI ㉑ 基线拦增量(新增代码裸导航=红);
  存量随 S 组各页改造迁移,S 组收官时清零(总控台跟踪)。

## F3 · 分叉债:会员等级体系双实现(登记 2026-08-12,抽验批小件6 顺带发现)

- 后端有两套等级事实源:①全局硬编码 `MEMBER_TIERS` 梯子(serializeUser 的
  memberLevel/growthValue/memberTiers 由它按累计消费算,**不分租户**);
  ②租户级 `membership_config.tiers`(平台管,tiersEnabled 开关吃这套)。
- 现状行为:渲染开关(分级/不分级)吃租户配置;但称谓文本与成长值吃全局梯子——
  lucky-luxe 未配置 membership_config(默认不分级),顾客 memberLevel 仍下发「Platinum Member」。
- 小件6 已按店主口径落地:tiersEnabled=false 的店,顾客端三减法(成长条不渲染/
  称谓只写「会员」/权益卡留空),全局梯子标签在这类店不再露出;分级店仍显全局梯子标签。
- 按四之九:同一业务口径双实现=分叉债。
- **✅ 已收敛(2026-08-12 复核二轮批,店主拍板②)**:租户 membership_config.tiers 上位为唯一事实源;
  四份实现删三留一(server 全局梯子/server 客户列表内联阈值/网页 customer.js/小程序 api.js 全删,
  前端只吃后端 user.memberTiers);lucky-luxe 以原全局梯子为初始配置开分级(迁移幂等,决策标记防重放),
  jics 不分级三减法不变;CI ㉕ 双租户常驻;L5 双租户实拍各 5/5。等级内容日后店主在 S9 配置界面自改。


## 挂-004 · 林小雅(demo-cust-01)演示种子「充1000送50」赠送行缺失(D36 定性,登记 2026-08-12)

- **事实**:2026-03-15 演示种子充值行 note 写「充1000送50」,但账上只有 +1000,赠送 ¥50 从未入账
  ——店主复核二轮报的「50 元差值」即此。全套对账见《D36对账表_林小雅_2026-08-12.md》,没有算错的钱。
- **为什么不当场补**:lucky-luxe 是真实租户,台账只追加(触发器禁改 note),补一笔 +50 会把假充值
  混进八月真实冲卡统计;系统现行充值路径也没有「送金」机制(属 S2 规划)。
- **清账条件**:S2 储值红字/调整路由上线,或演示档案整体清理时,一并处置(与挂-001/挂-003 同批)。

- [挂账 2026-08-21] 体验版重传(店主降级为不紧急):手机体验版仍是 08-20 旧包(不含 B3-1/B3-3/4/弹窗修);批③首日随手重传一次——上传前双闸自查(USE_LOCAL_SANDBOX=false+API 指生产),报版本号提醒店主扫码。
