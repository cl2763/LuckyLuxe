# 批次三 · 〇两票 + 一网页开单(2026-08-29)

> 照你的交付顺序:〇 → 一,一批报一次。二.1(账户调整上小程序)下一批。

---

## 〇、两票落地

### ① `deposit-conservation` → 定时自检(新模块 `deposit-audit.mjs` + 套件 10 项)

- **「定时」的落法**:每日**日结确认那一刻**自动跑一次(日结每天确认一次=每日一次;不起常驻计时器 ——
  `--watch` 重载下会攒幽灵任务,而且没人守它挂没挂)。结果**落库**(`daily_closes.deposit_audit_json`):
  哪天审的、审出什么,和日结记录钉在一起,可回看。
- **显示照你原话**:平 = 什么都不显示(响应里**连字段都没有**,不是空字符串);
  不平 = 日结页红字「**定金对不上,差 $X(N 笔)**」,句子后端出,两端条件渲染。
- **判据两向**(`test-deposit-audit.mjs`):
  ①平的日子:确认后响应零新增字段,**但库里有审计记录**(平≠没审);
  ②造一笔故意不平的($77 的已兑现回执、不给收入行)→ 红字出现且**金额正好 $77、笔数 1**;
  ③修复口补上收入行 → 重确认 → 红字**整个消失**(不是留一条「差 $0」);
  ④运行时取证:两端红字锚点都是条件渲染。
- **变异**:摘掉调度(确认后不跑)→ 重启 → **当场红**(「审计确实跑了并落了库」那条抓的);还原回绿。

### ② 判定表三项已标注

轮播自管 / 商城自购 / 预约草稿 =「**合理只在网页 · 店主 08-29 拍**」,小程序侧只读展示照旧。
判定表相应行已改,**无悬项**。

---

## 一、网页开单(上线前必办)

### 做了什么

| 合同条 | 落法 |
|---|---|
| 同一套后端路由,不许新造接口 | 网页只打 `preview` / `POST settlements` / `GET settlements` / `pricing` / `technicians` / `membership` / `timecards` —— **零新增业务接口**(唯一新增的是 `settleAction` 展示字段,见下) |
| 入口:订单管理 → 该单操作 →「去结算」 | 按钮**后端出**(D70 律):`settleAction` 由 `booking-state.mjs` 下发 —— 未完成的单才有;有待签单时文案自动变「继续结算(待签 N 张)」。**前端零 if**,渲染只认这个字段 |
| 页面块与小程序一一对应 | 新模块 `apps/web/settlement-web.js`(399 行):价格体系 → 服务项目(单选自动替换,大类切换,**次卡核销**)→ 加项 → 自选填写行 → 本单技师(1–2 位,主/副)→ **被服务者(占位「留空=本人;朋友填称呼」)** → 定金抵扣 / 券 / 储值抵扣 → 合计(后端试算)→ 送签 |
| 入账唯一路径=签署 | 网页只到「生成待签结算单」为止;开完显示待签单号,签署仍走顾客侧现有签署流 |

**写法上的两条硬底**:
- **body 构造是纯函数**(`buildSheets`/`buildBody`),与小程序 `groupSheets`/`formBody` **逐字段同形** —— 测试直接加载网页模块喂同一状态,深比对两端产出;
- **金额零算术**:渲染只拿后端 preview 的 cents 做货币格式化(与小程序 `m()` 同口径),模块源码里**没有一处金额加减乘除**(有断言守着)。

### 判据(你给的五条,`test-web-settlement.mjs` 16 项)

| # | 判据 | 结果 |
|---|---|---|
| ① | 两端构造的 body **深比对相等**;各开一单 → **落库行 33 列逐字段相等**(除单号/时间戳身份列) | ✅ |
| ② | 被服务人姓名**能输空格**:「张 三」原样落库;输入 handler 原样进 state 不回写不 trim | ✅ |
| ③ | 钱输入框老规矩:`type=text + inputmode=decimal + data-money`(走全局 MoneyInput) | ✅ |
| ④ | 组数=2:sheets=2 且组级合计=Σ各组(后端同一口);网页模块**零金额算术**(源码断言) | ✅ |
| ⑤ | **12 个块**全部在服务端实发资源上找到锚点;模块带内容指纹挂载 | ✅ |
| + | 入口按钮后端出(有待签单时「继续结算(待签 1 张)」实测)· 开出的单 `pending_sign` · **签字之前账本一分未记** | ✅ |

### 像素走查(浏览器实走全链,L1)

登录走查店(平台口重置老板密码,留痕带原因)→ 订单管理:卡片上「**去结算**」按钮(后端出)→
点进「结算开单」页 → 勾技师 Lina(主)→ 被服务者输「**王 姐**」(空格)→ 后端试算:
「1 精品单色 CAD $198.00 · 到店支付 CAD $198.00 · 合计 CAD $198.00」→ 点「生成待签结算单」→
页面出「该预约已有待签单 1 张 —— 顾客侧签署中」+ 单号 `WP-20260828-NJJN` →
**库里现测**:该单 `pending_sign`、`served_person_name = "王 姐"`(空格在)、`total_cents = 19800`。

### 变异自检

把 `buildSheets` 的 servedPersonName 改成吞空格(`replace(/\s+/g,'')`)→ 重启 → 判据①**当场红**
(深比对把两端差异原样打出来);还原回绿。加上〇的一刀,本批两刀都红。

### 假设清单(图上没画到的,按最合理做并记录 —— 不是悄悄发挥)

1. **随签充值(rvDraft)与现场购卡(purchasePackageId)第一版没做 UI**:指令块清单没列它们;
   body 形状已兼容(字段在,恒 undefined),补 UI 不动接口。**登记待排。**
2. **双技师数字位分配(itemNos)第一版没做 UI**:提交空数组(=不预分配,店长日结核定,与小程序单技师时同口径)。**登记待排。**
3. 块顺序照**小程序屏序**(价格档→项目→…),你指令里的枚举顺序与小程序屏序不同,按「一一对应」优先取小程序屏序。
4. 券区:预览响应里有券选项才渲染下拉(小程序是弹层,网页用下拉 —— 排版随端,口径同源)。

---

## 二、回报固定六项

**①改了什么** 定金定时自检(新模块 + 确认钩子 + 两端红字)· 网页开单整链(新模块 `settlement-web.js` +
`settleAction` 后端出 + 入口按钮)· 判定表三项标注。
结构:开单三条路由(preview/建单/列表)搬进新模块 `settlement-routes.mjs`(**只搬不改**,107 行);
`settleAction` 住 `booking-state.mjs`。

**②在哪一层验的** 接口层(真打)· 数据库层(落库 33 列逐字段比对 + 直插反例)· 源码/vm 层(纯函数深比对)·
运行时前端(实发资源锚点)· **浏览器像素层(全链实走)** · 全量回归(第五轮,数字见下)。

**③同类扫尽** 类「按钮前端手写 if」:去结算按钮从第一天就走后端 `settleAction`,没欠;
类「路由赖在巨型文件」:开单三条随批搬出;类「输入过程改写内容」:被服务者与金额框按族规写,断言常驻。

**④用什么数据验的** 「  张 三  」(前后空格+中间空格)· 两组两技师 · 自选行 1500 分 · $77 故意不平的回执 ·
待签单在场时的续办态。

**⑤重复稳定性** 两刀变异(摘调度 / 吞空格)都重启服务后当场红、还原回绿、零残留(机核)。

**⑥残留与不确定**
1. 随签充值 / 现场购卡 / 双技师数字位 三处 UI 缺口**登记待排**(body 兼容,不动接口);
2. **小程序端零改动**(本批网页补齐,小程序流程原样)—— 双端同批律无欠账;
3. `settleAction` 只在商家序列化下发,顾客端位面没有它(位面验过);
4. **小程序像素走查依旧做不了**(无微信开发者工具);
5. **生产库未动 · 本机库未动 · 沙箱库未动 · 未推 main。**

**自评 L1–L5**:L1 网页全链实走到库(截图+库现测);L2 给了机械证据;L3 开单页无设计图 ——
**结构照小程序屏序一一对应(那就是「对的样子」的书面依据),视觉细节挂 ⬜ 等走查**;
L4 反例驱动;L5 全链实走 1 次,未跑 5 次(如实报)。

---

## 三、棘轮账(现测 `wc -l`)

| 文件 | 上批 | 本批 | 增减 |
|---|---|---|---|
| `apps/api/local-server.mjs` | 18,331 | **18,260** | **−71** ✅ |
| `apps/web/admin.js` | 8,598 | **8,598** | **0** ✅ |

新增在模块:`settlement-web.js` 399 · `settlement-routes.mjs` 107(搬出)· `deposit-audit.mjs` 39。

**57 套件 EXIT=0 / 2,269 项断言**(日志 `handoff/回归日志_2026-08-29b_57套件.log`);
4128 与 4310 已拉回并核验在线;变异探针零残留。
