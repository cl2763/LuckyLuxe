# 小批 11n —— 生产假数据走一扇门清 · 零碎删除全部作废 · 12b 接 11m 的壳

**出令时刻**:2026-09-23
**前置**:`小批11m` + `11m补` · 回执提交 `23d2d12` · 盘点 `两家店全景盘点_只读_2026-09-22.md` · 店主三句拍板(见 §〇)

---

## §〇 店主三句拍板(2026-09-22 夜,逐字要点)

1. 「**生产版上面所有的假数据都要清掉**,但这些数据在沙箱里或本地都可以留着 —— 生产版是要上线的,不许有假数据。」
2. 「两家店里**所有由系统生成的名字全是假的**;只有小婕店我口述你写进去的**技师名字和价目表**才是真的。」
3. 「那四家演示店**留着当样板**,后台标注好就行。」

**边界**:只清**生产库**;沙箱库、本机库一行不动。只清 `lucky-luxe` 与 `jics-nail` 两家;四家 demo 店不碰。

---

## §一 核验 `23d2d12`:硬的,四笔记功一笔自认

- **条件④ 挡住 161 单**(账本有 2 笔指向)—— 令写「涉钱不预批」,你就停,**没有去凑「这两笔也是假的」**。对。
- **三位技师没停用** —— 前置「先单后人」不成立就不做。对。
- **全景盘点** 82 张表逐张、33 张有数据、**只有 7 张在 `RESET_TABLES`** —— 这一句直接改变了明天的设计(见 §二)。
- **L1 截图**:三张真截到,而且自己抓了两个假绿(`Page.reload()` 洗掉注入 · 「读得到 ≠ 看得见」),加了可见性判据当场拦下 2 张假图。**欠三批的账,这批清了。**
- **抽取**净减 12 行,踩到 `json()` 返回 `undefined` ⇒ 二次应答打死进程 —— 这种「GET 看着对、后面全连不上」的病记进注释是对的。
- 自认:商家后台那处要老板密码没截 —— **对,不硬凑。**

---

## §二 🔴 裁:零碎删除全部作废,改走一扇门

11m §一(161+68 单)、11m补 §三(技师停用)、等天亮里「两笔账怎么办 / ¥2,744 怎么处理」—— **全部作废,不再单独裁。**

**理由**:店主已把范围说到最大 —— 生产上两家店**所有**系统生成的数据都清。再一批一批删,每批都要一次备份、一次彩排、一次判别式,**而且每批都会撞上下一个「涉钱」**(这次是 2 笔账,下次是 7 笔储值)。盘点已经证明 **33 张有数据的表里 26 张不在任何现成的门里**。**零碎删是在用 33 次小手术做一次该做的大手术。**

### 立 J-116

> **当店主把清理范围说到「全部」时,不许再按「一批一批」执行。**
> 分批删的每一批都会各自撞上一条判据然后各自停下,**结果是「一直在删、永远删不完、每次都停」**。
> **正确形状是:一扇门,一次备份,一张白名单,一次确认,一次执行,一次核验。**

---

## §三 那扇门:`POST /platform/tenants/:id/prelaunch-wipe`(上线前清空)

**不是改 `demo-reset`,不碰 `PROTECTED_REAL_TENANTS`,新模块 `apps/api/prelaunch-wipe.mjs`(公约①),`local-server.mjs` 只留一行分发。**

### 闸(全部机器判,任一不过 ⇒ 403/409,一个字节不删)

| # | 闸 | 为什么 |
|---|---|---|
| 1 | `isPlatform()` | 平台钥匙 |
| 2 | `tenants.kind = 'real'` **才放行**(和 demo-reset 正好相反) | 这扇门是给真店上线前用的 |
| 3 | 🔴 **该店 `payments` 里 `provider != 'MOCK'` 的 = 0** | 一分真钱都没收过才许清;盘点已量全库 = 0,但闸要**执行那一刻再量** |
| 4 | 🔴 **该店 `tenants.prelaunch_wiped_at IS NULL`** —— 用过一次就永久拒 | **一次性的门**,不留成常驻按钮 |
| 5 | 请求体 `confirmName` **逐字等于店名** + `confirmPhrase` **逐字等于**「我确认这家店里没有任何真实顾客和真实收入」+ `reason` 非空 | 手打,不许勾选框(店主 08-25 ③ 同款) |
| 6 | `VACUUM INTO` 备份成功才继续;备份失败 ⇒ 中止 | 08-25 ④ 同款 |
| 7 | 写 `platform_ops_log`:操作者、时刻、店 id、**逐表删前行数快照**、原因、备份路径 | 08-25 ⑤ 同款 |

### 白名单(按盘点 33 张,逐张定「清 / 留」)

**留(一行不动)**:
- 店本身:`tenants` · `stores` · `business_hours` · `tenant_settings` · 知识库事实 · 平台分类/大类
- 账号:`admin_accounts` 里 **`boss`**(其余账号见下)
- 🔴 **`ai_response_feedback` 97 · `ai_learning_examples` 68** —— 店主批过的话术,**不是假数据**(你盘点里拎出来的那条,对)
- **`jics-nail` 的 `technicians` / `services` / 价目** —— 店主口述的真配置
- `technician_schedules` 若属 jics-nail 真技师 —— 留

**清(该租户下整表)**:
- 人:`users` · `user_identities`(含店主自己那 4 个测试微信绑定 —— 她自己的号,清了下次打开就是新档案)· `lucky-luxe` 的 `technicians` 三行 · `admin_accounts` 里 `avalin`
- 单与钱:`RESET_TABLES` 全部 30 张 + 盘点 §二 那 26 张里凡是「经营数据」的(单/支付/账本/储值/积分/券/结算/日结/工资/考勤/小记/AI 会话记录/通知记录……)
- 🔴 **`finance_transactions` 那 2 笔与储值 7 笔 ¥2,744**:随表整清。**这是唯一一次允许删真店账本行的地方**,理由写死在模块头:「上线前,店主逐字确认无真实收入,全库非 MOCK 支付 = 0」。**过了这扇门,只追加律恢复,永不再开。**

🔴 **白名单以盘点 33 张为底,逐张写进模块,不许 `SELECT name FROM sqlite_master` 动态取** —— 表是明写的,新表要人来加。
🔴 **`lucky-luxe` 的 `services` / 价目 清不清:等天亮问店主**(她只说了小婕的价目是真的,没说自己店的)。**问完才能跑 lucky-luxe;jics-nail 可以先跑。**

### 判据(进主档 + 门关档 + 推前清单)
- 闸 2/3/4/5 各一条阳性对照(demo 店打 ⇒ 403 · 塞一笔非 MOCK 支付 ⇒ 409 · 跑两次第二次 ⇒ 409 · 店名差一个字 ⇒ 400)
- **留的表逐张 count 删前 = 删后**;清的表逐张 = 0
- 四家 demo 店 + 另一家真店逐表行数不变(反向守)
- ops-log 落一行,含逐表快照
- 🔴 **沙箱库彩排一次并贴逐表前后**,但沙箱库本身**不清**(店主说沙箱留着)—— 彩排用沙箱**副本**

### 执行顺序
1. 模块 + 判据 + 平台后台一个按钮(**红色,二次弹窗要手打两句话**)
2. 沙箱副本彩排,贴逐表前后
3. 🔴 **生产那一下由店主自己在平台后台点** —— 手打店名和确认语是她的手,不是 Code 的 curl。先 `jics-nail`(0 顾客,最干净),再 `lucky-luxe`(等价目那一问答完)
4. 执行后:两端顾客那一层只读实访 + 逐表核验 + `prelaunch_wiped_at` 已写

---

## §四 12b(姿势卡 + AI 修图功能)接 11m 的壳

店主说夹子里的 `小批12b_姿势卡+AI修图功能开工_2026-09-22.md` 已按两入口两口径改过,**以夹子里那份为准,你直接读它**。只加三句对接:

1. `ai_retouch_jobs` 表由 12b 建;11m 那个 `ai_retouch` 三态开关**不动**
2. 🔴 **12b 做完之前,`on` 那档仍然 409**;12b 交付时**一并**把 409 换成「流程就绪判据过了才许拨 on」,并给这条判据一个阳性对照
3. 12b 的界面部分**按 L1 截图收口同款**交:每一屏一张真截图,可见性判据过

---

## §五 等天亮(接着往下跑,不等)
- `lucky-luxe` 的服务项目/价目是真是假(A 真,留 / B 假,清)
- 四家样板店标签文案改「样板店 · 展示/测试用」—— 顺手,本批做

——Cowork,2026-09-23
