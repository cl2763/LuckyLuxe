# 夜班令 — 05i → ③ 预约采集 → ④ 审样本页,按序连做,不等 Cowork 核;⑤ 不开

> 2026-09-05 夜 · 店主要睡了,Cowork 代拟 · 两处各一份:桌面交接夹 + 仓内 `handoff/`
> 一句话:今晚不用等我。05i(J-23 + D140 + 过滤)做完 → 直接 ③ → ③ 做完直接 ④。每一步各自提交、各自回执,早上我三份一起核。**⑤ 正式评测不开**(它要 ③④ 都核过,还要店主亲自读五通对话打分)。

---

## 一、顺序与「能不能不等」

| 步 | 合同 | 等不等我 | 与前一步的关系 |
|---|---|---|---|
| 05i(J-23 刀 · D140 币种 · 废弃键过滤 · D141 入册行) | 小批05i | **不等** | — |
| ③ 预约采集实现 | 05h §三 + 图 v1.2 §二/§六 | **不等** | 只读 05i 改过的币种口(出钱数走 `tenantCurrencyCode` 新口径),别的不相交 |
| ④ 审样本页 | 图 v1.2 §四 + §六「审样本」 | **不等** | 只依赖 ①(`gate=model` 打标)与 ⓪(全录),不依赖 ③;若 ③ 卡住,**先做 ④**,回执写明为什么跳 |
| ⑤ 正式评测(六组 80 条 + 像人五通) | 图 §五 | **等**——不开 | 要 ③④ 核过;像人五通要店主打分 |

## 二、④ 审样本页要什么(照图,不复述图)

- 打标:模型放行的每一轮已记 `gate=model`(① 做了),这里**只读**;
- 网页后台「AI 客服 → 待审」:按店列出 顾客原话 / AI 答的 / intent / confidence;老板一键 **「对」「改一句」「不该答」**;
- 回流:「对」→ `ai_learning_examples`(现有);「改一句」→ 改后版本进样本;「不该答」→ 反例集,同句再来**先反问**;**回流按租户隔离**(A 店样本不进 B 店提示词);
- 页顶三个数:本周模型放行 / 老板认可率 / 反问率(认可率 = 店主问的「准确率」);
- 代码落点:新文件 `apps/web/ai-review.js`;`admin.js` **不许长**(棘轮 8,550,只许搬出);后端路由进独立模块,`local-server.mjs` 不越 18,243;
- 判据(图 §六):模型放行一轮 → 待审 +1;「对」→ examples +1;「不该答」→ 同句再来先反问;三数与库里数一致(计数即证);两店各跑;造病:回流不带租户 → A 店样本出现在 B 店 → 红;
- 写的库:一次性 CI 库 + 沙箱;**4128 不写**。

## 三、今晚的停线(碰到就停,写进回执,等早上)

1. 要写 4128 本机库 → 停(写库自报律);
2. 碰到账本 / 凭据 / 生产 / 跨租户四条红线之一而修法不明 → 停,只报;
3. 沙箱真模型用量:`/health.aiUsage.totalTokens` **今晚增量超过 600 万** → 停模型跑,剩下的先做不需要模型的部分;
4. 回归红且 30 分钟内定位不到 → 停,不改判据凑绿;
5. 不推 main、不碰生产、不看 `.env.ai-sandbox`(照旧)。

## 四、早上交什么

三份回执(05i / ③ / ④)各自照模板;另加一页 **`夜班总结_2026-09-06.md`**,最上面五行:做到哪一步 · 三个提交号 · 停在哪(若停)· token 增量 · 未推 main 数。③ 回执单列 到底 n/12 · 丢 7 项表 n/12 · 进人工 n/12,与基线 0/10/7 并排。
