# 12i / 12j / 12k 回执(第一批)——
# 12j补 入库 88 张 ✅ · 12k 可点反馈 ✅ · 🔴 **12i 按它自己的判据停工,等你裁定**

多伦多时间 2026-09-23 夜。本批 **模型 0 次调用 · 花费 ¥0**,累计仍是 12a 关帐那个 ≈¥58.6。

---

## 〇 三条围栏的自证

| 围栏 | 证据 |
|---|---|
| 只动 tools/ai-retouch-eval/、tools/segment/、产出夹 | 本批写过的文件全列在 §五;`apps/` 我一个字节没动 |
| 不推 main、不碰生产 | 无 `git push`、无 `railway`、无生产库连接;`run-all-tests.sh` 没跑(它会 pkill 你的 4128/4310) |
| 花钱停线 ¥12 / 累计 ¥72 | **本批 ¥0**,方舟一次没调 |

🔴 **但「apps/ 工作树干净」这条判据我用坏了,必须说清楚**:
我开工时存了 `apps/` 全部 445 个文件的哈希,收工再比 —— 结果
`apps/api/local-server.mjs`(23:46 改)与 `apps/api/platform-ops.mjs`(23:44 改)**变了**。
查下来不是我:**同一个仓库里 Cowork/Codex 的进程正在同时改 apps/**(`ps` 里有
`codex-primary-runtime` 在跑 local-server.mjs),另外 `sandbox-data/*.sqlite` 是 4310 沙箱在写。
**教训(归族《白名单判据》)**:「apps/ 现在是不是原样」在并发下根本不是我的判据 ——
它证的是「全世界都没动 apps/」。正确的判据是**「我写过的文件清单里有没有 apps/」**,
这条我照样能给(§五),而且并发也打不坏。这次是我判据选错,不是围栏破了。

---

## 一 12j 补 §一 入库:88 张齐了 ✅

- **去重不靠文件名推理,靠像素**(判据律:能按像素验就别验元数据)。
  把桌面夹 `AI修图样片/` 全部 85 个文件**用同一支 `normalize.mjs`、同一组参数**跑了一遍,
  再与 `_input/` 逐张比 256×256 的平均绝对差(MAD):
  - **41 张 MAD = 0.000** —— 第一批本尊(含那 10 个文件名带「 2」的,清单的核定**被像素证实了**)
  - **44 张 MAD ≥ 25.8** —— 真新增
  - 分界干净到不需要挑门槛:0.000 和 25.8 之间什么都没有。
- 新增 44 张按 **45–88** 入 `_input/`,清单写在 `_input/_归一化清单_第二批.json`。
- **判据 §三-1 对上**:`_input/` = 41 + 3 + 44 = **88 张** ✅

## 二 无人脸核验:抓到一张 🔴,已按老规矩处理

判据不是「我裁过了」那句话,是像素。全部 44 张顶部 400px 拼成一张图逐格看
(`_归一化抽查_第二批_顶部400px_2026-09-24.jpg`,44 张**全检**不是抽检):

| 号 | 情况 | 处理 |
|---|---|---|
| **50_IMG_1276** | 🔴 **左上角有嘴唇和下巴** —— 顶部 18% 那一刀没裁够 | 按老规矩**再裁顶部 340px**;再核已无脸,**五片指甲一片没少**(三档裁量 260/340/420 并排看过) |
| **54_IMG_1523** | 无脸,但有头发 + 脖子 + 锁骨 + 衣领 | **没动,等你一句话**:令写的是「不许出现脸」,这张没脸;但它比别的片子露得多。你说裁我就裁 —— 只是它的指甲从 y=0 就开始,再裁会切掉指甲。 |
| 76_IMG_8653 | 画面右上有「Lucky nail」手写体水印 | 不是隐私问题,但**送模型时水印可能被改掉或糊掉**,先记一笔 |
| 其余 41 张 | 只有手 | — |

🔴 **自动检测器这一层没跑成,不许记成「0 张有脸」**:本机 `cv2` 是 headless 构建,
不带 `data/haarcascade_*.xml`,`CascadeClassifier` 读不到文件时**不报错、只是 empty()** ——
拿它跑出来的「0 张有脸」是假绿(**静默失败器族**又一案)。
我在 `face-check.py` 里显式判了 empty,取不到就明说「检测器不可用」,
**绝不让「没检出」和「没检测」长成同一个样子**。所以这一条的证据是**全量顶部拼图 + 我逐格看过**,
写清楚了,你复核那张拼图即可。

---

## 三 🔴 12i 停工 —— 按 12i §五 自己的判据,不许开跑

12i §五 写死:**「三档各 ≥ 5 张才开跑,不足的档报出来先停」**。88 张跑完的分布是:

| 甲色档 | 张数 | 判据要求 |
|---|---|---|
| 浅(Y ≥ 90) | **32** | ≥5 ✅ |
| 深·鲜明(Y<90 且 C≥25) | **0** | ≥5 🔴 |
| 深·不鲜明(Y<90 且 C<25) | **56** | ≥5 ✅ |
| 甲面区取不到(默认腿 A) | **0** | — |

**所以停,不跑那 30 次调用。** 但真正的问题比「某一档人少」严重得多:

### 3.1 不是数据里没有鲜明甲,是**这把尺量的根本不是甲面**

88 张的甲面色度 C:**最大 26.0、中位 6.4**,门槛定在 25 等于永远不触发。逐张看:

| 号 | 实际是什么甲 | 机器量到的 C | 机器判成 |
|---|---|---|---|
| 14 | **大红甲** | 5.2 | 深·不鲜明 |
| 53 | **荧光蓝 + 玫红** | 17.7 | 深·不鲜明 |
| 28 | 墨绿/咖啡 | 8.5 | 深·不鲜明 |
| 02 | 黑甲 | 5.4 | 深·不鲜明 |

把掩膜**画出来看**(`_分档overlay_2026-09-24/`,青色 = 机器认的甲面)就一眼明白:
- **14 号(红甲)**:整只手被判成皮肤 —— 红甲的色度落在肤色窗口里,青色只剩边角碎块。
  C 量的是那几块碎屑,不是红甲。
- **53 号(蓝粉甲)**:最鲜艳的那几片几乎整片没标出来。
- **49/44 号**:掩膜捡的是白衣服和背景。
- **02 号(黑甲)**:掩膜其实**认得很准** —— 但黑色的色度天生接近 0,
  落进「深·不鲜明」。这正是 12i §二 自己预告的冲突(你说黑要 A,机器判成不鲜明)。

🔴 **而这一切在数字上看不出来**:88 张全部落进两档、「甲面区取不到」**恒为 0**,
所以 12i §二 那条兜底(「取不到就默认 A」)**从来没触发过** —— 它不是没用上,是**没资格用上**:
掩膜不是「取不到」,是「取到了错的东西」。
**判据在缺陷存在时照样绿** —— 判据律那条刀口,这次是砍在判据自己身上。

### 3.2 我顺手试了一把替代尺(¥0),结论是:**绕不过去**

想法:甲油通常是手部区域里最饱和的东西,那就别用「非肤色连通块的均值」,
改取手部区域内色度的 99 分位(C99)。跑完 88 张排序,头部确实对了 ——
49(红甲长)71.8 · 53(蓝粉)65.0 · 10 号 64.8 · 76 号 58.6。
**但它同样立不住**:皮肤本身的色度就有 35–40,
14 号大红甲 47.4 和 47 号没颜色的甲 40.4 挨得太近,分不开;
而「深·不鲜明」那一档(墨绿、裸色、黑)本来就比皮肤还低。
**没有真的甲面掩膜,鲜明度这把尺没有地基。**

### 3.3 三个走法,请你挑一个

| | 走法 | 代价 | 我的看法 |
|---|---|---|---|
| **A** | **先做 12j 标注(¥0),标出真甲面 → 用真掩膜重算 Y/C → 门槛按 88 张分布给你看 → 再跑 15 张** | 扩样往后挪一档 | **推荐**。这条路你 09-24 已经批过(12j + D222),而且 12i补 §一 自己写着「三件事全部以『找到指甲』为前提 → D222 是闸门,不是可选项」。现在是这句话应验了。 |
| **B** | 门槛从 C≥25 往下调到能凑出五张 | ¥0,立刻能跑 | **不建议**,而且 12i §三 明令「不许自己调了再跑」。尺子指的不是甲面,调门槛只是把错误分得更细。 |
| **C** | 不分档,15 张一律两版(A / A′)都跑,让你的眼睛直接判 | ¥9.60,立刻能跑;拿不到「机器能不能自动选」的答案 | 如果你现在就想看图、分档的事以后再说,这条最快。**三档规律成不成立这个问题会留着没答案。** |

---

## 四 12k 可点反馈 ✅ 全做完了(¥0)

### 4.1 做了什么
- `tools/ai-retouch-eval/feedback-widget.js`(210 行,单文件、零外链、不上传)——
  每格 **过 / 不过 / 有问题**(点第二下取消),选「有问题」展开文本框;
  每行最左「这行我最喜欢哪版」下拉;页顶「已填 x / 共 y 格」+ **导出反馈** + 清空;
  键盘 1/2/3(鼠标停在哪格就点哪格),页顶 30 秒说明写了。
- `feedback-embed.mjs` —— **唯一嵌入口**,五个 `build-report*.mjs` 出页时统一调它,
  不许每个报告各写一套;它同时是回炉工具(幂等:嵌过的再跑不会嵌第二份)。
- `ingest-feedback.mjs` —— `_反馈/*.json` → `偏好数据集.jsonl`,只追加。
- **五份已存在的对比包全部回炉嵌了一次**(第一轮到第五轮),你想回头点哪一轮都行。

### 4.2 判据(12k §二-3 四条,逐条给证据)
| # | 判据 | 结果 |
|---|---|---|
| ① | 导出 json 条数 = 页面已填格数 | 页顶「已填 3 / 共 39」,导出 json `已填:3`、`条目` 3 条 ✅ |
| ② | ingest 后 jsonl 行数增量 = json 条数 | `0 → 3(增量 3)` ✅;**再喂一次** `3 → 3(增量 0)`,幂等 ✅ |
| ③ | Safari 与 Chrome 都能点、都能导出 | **Chromium 实点全过**(见下);**Safari 没实点成 —— 见 §六 残留** |
| ④ | 落一刀:改坏本地存储键名 → 刷新后丢失 → 红 | **落了,红了,还原回绿** ✅(见下) |

**Chromium 实点(弹层实点律,全是真点不是模拟事件)**:
点「过」/「不过」/「有问题」各一格 → 第三格弹出文本框 → 真敲进「钻的高光糊了」→
选「这行我最喜欢哪版」= `02_模型只去瑕疵+亮度回贴全+皮肤层冷白轻+磨皮` →
**刷新** → 页顶回到「已填 3 / 共 39 格」、下拉还选着那一版 ✅
→ 点导出,拦下真实下载内容核对:文件名 `反馈_对比包_第五轮_2026-09-23.json` / `.md`,
**每条的「版本名」就是 12e §六 的人话文件名**,和图名逐字一致 ✅
**L5 连续 5 次进出页面**:5/5 都是「已填 3 / 共 39 格」,组件每次都挂上 ✅
五份对比包各自的格数:第一轮 121 · 第二轮 118 · 第三轮 27 · 第四轮 63 · 第五轮 39,
**没有一份渲染出「没找到可反馈的图格」那条告警** ✅

**🔪 刀(12k §二-3④),按刀留痕律留了凭据**:
```
[刀已落] 23:45:40 注入点:对比包_第五轮.html 内联组件;命中:var KEY 那一行;llfb: → llfb-BROKEN:
刷新后 → 页顶「已填 0 / 共 39 格」(她点的 3 条不见了)= 🔴 红,且指得出是哪一处
还原 + 刷新 → 「已填 3 / 共 39 格」= 回绿;夹具收尾,判据保持幂等
```

### 4.3 做的过程中修掉的一个规格违背
12k §一 原文是「没填完导出时**提示但不拦**」。我第一版用 `window.confirm` 实现「提示」——
**那是拦**(点取消就不导出),把规格写反了。更糟的是它**静默**:
实测内嵌浏览器里 `confirm` 根本不弹、1 毫秒直接返回 `false`,
表现就是「点了导出什么也没发生」。已改成页内黄条提示,**永不拦**
(现在导出会顶上一行「还有 36 格没填 —— 已按现在填的这 3 条导出」)。
归族:**静默失败器**。

---

## 五 本批写过的文件(围栏①的正确判据)

**仓库内(全在 tools/ 下)**
- 新增 `tools/ai-retouch-eval/feedback-widget.js` · `feedback-embed.mjs` · `ingest-feedback.mjs` · `face-check.py` · `偏好数据集.jsonl`(+ `.偏好数据集.已摄入.json`)
- 改 `tools/ai-retouch-eval/region-metrics.mjs`(加 甲面色度C / 甲色档 / 口令腿 / 甲面区取不到 四个字段 + 分布打印)
- 改 `tools/ai-retouch-eval/build-report{,2,3,4,5}.mjs`(末尾统一嵌反馈组件,每份 +6 行)

**产出夹内**(`31o_修图验证_2026-09-22/`)
- `_input/` +44 张(45–88)、50 号重裁;`_input/_归一化清单_第二批.json`
- `_分档表_88张_2026-09-24.json` · `_分档overlay_2026-09-24/`(88 张掩膜叠图)
- `_归一化抽查_第二批_顶部400px_2026-09-24.jpg` · `_人脸检测_第二批.json`
- `_反馈/反馈_对比包_第五轮_2026-09-23.json`(我自测点的那 3 条,你可以删)
- 五份 `对比包*.html` 各自末尾 +1 段内联组件

**`apps/` 出现次数:0。**

---

## 六 残留与不确定(不挂着,写明谁来收)

1. **Safari 没实点成** —— 本机工具链里浏览器只给到「看得见、点不了」这一档,
   我驱动不了 Safari。代码层我逐个核过用到的 API(`closest` / `Blob` / `a.download` /
   `Intl.DateTimeFormat timeZone` / `localStorage`)都是 Safari 14+ 支持的,
   **但这是元数据判据,不是渲染结果判据,按判据律不算验过**。
   **请你在 Safari 里点一次**:随便开一份对比包,点两下「过/不过」,刷新看还在不在,再点「导出反馈」看有没有下载。
   一句话回我就行,不行我立刻改。
2. **本机内嵌浏览器不落盘下载** —— 它只留了个临时文件(已清掉),
   所以「文件真的存到硬盘」这一步我是**拦下 Blob 内容逐字核对**的,不是看到文件。
   Safari 那一次点就把这一格补上了。
3. **12j 标注还没开工** —— 因为标哪 23 张取决于 12i 挑样,而 12i 停在你那儿。
   如果你选 §3.3 的 **A**,我就直接按 12j 补 §二 的难类顺序开标(红甲/裸甲/纯黑/暗光/立体钻优先),不用再等。
4. **54 号露颈胸、76 号带店招水印** —— 各等你一句话。

---

## 七 回报六项 + L1–L5 自评

① **改了什么**:入库 44 张(→88)· 给分档加了 C 和三档判 · 反馈组件全套 + 五份对比包回炉 · 50 号重裁。
② **在哪一层验的**:像素(去重 MAD / 重裁前后尺寸)· 渲染(顶部拼图逐格看、掩膜叠图逐张看)·
   真实浏览器实点(不是模拟事件)· 导出的字节(拦 Blob 逐字核)。
③ **同类扫尽**:反馈组件不是只嵌第五轮 —— **五份全嵌**,且出口只有一个(`feedback-embed.mjs`);
   无人脸不是抽 4 张 —— **44 张全检**;去重不是只查那 10 个疑似 —— **85 × 44 全比**。
④ **用什么数据验的(反例数据律)**:专挑会露馅的 —— 红甲 14、蓝粉 53、黑甲 02、墨绿 28、
   背景最花的 49/44;正是它们把「甲色三档」这把尺捅穿的。
⑤ **重复稳定性**:L5 连续 5 次进出页面 5/5 绿;ingest 重跑幂等(增量 0);刀「造→红→还原→回绿」走完。
⑥ **残留与不确定**:见 §六 四条。

**L1–L5 自评**
- **L1 末端验证** ✅ —— 都在你会看到的那一层验的(拼图、叠图、真页面实点)。
- **L2 同类扫尽** ✅ —— 见 ③。
- **L3 无图不结案** ✅ —— 12k 有 §一 的书面「对的样子」,逐条对过。
- **L4 反例数据** ✅ —— 见 ④。**而且正是 L4 把 12i 的判据咬出来的**:
  只拿浅甲片验,32 张全落「浅」,分档表会好看得很。
- **L5 重复稳定** ✅ 5/5。
- 🔴 **我自己做砸的一条**:§〇 那个「apps/ 哈希对比」判据选错了
  —— 在并发仓库里它证的不是「我没动」。已换成「我写过的文件清单」。

---

## 八 收工核对

- **巨型文件行数**:`local-server.mjs` 17,518 · `admin.js` 7,893。
  **本批我没碰这两个文件**(见 §五),所以棘轮上这一批不由我记账;
  这两个数是收工当时的读数,`local-server.mjs` 23:46 被另一个进程改过(§〇)。
- **本机服务还回去了**:4128 `/health` 200 · 4310 `/health` 200 —— 两个都活着。
  本批没跑 `run-all-tests.sh`,所以它俩本就不该受影响,还是探了一次。
- 为了在浏览器里实点,我在 `127.0.0.1:4399` 临时起过一个静态文件服务(只绑本机),
  **验完已关**(探活 000)。对比包是本地单文件 html,你直接双击打开就能点,不需要任何服务。
- **生产库未动 · 本机库未动 · 沙箱库未动**。本批模型调用 0 次,花费 ¥0,累计仍 ≈¥58.6。
