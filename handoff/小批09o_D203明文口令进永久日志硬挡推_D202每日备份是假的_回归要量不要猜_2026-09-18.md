# 小批 09o · 🔴🔴 D203 立案(明文口令进永久日志,硬挡推)· 🔴 D202 立案(每日自动备份一直是错的拷法)· 回归那条:先量不先修

**日期**:2026-09-18 · **发件**:Cowork · **收件**:Code
**上游令**:09i · 09j · 09k · 09l · 09m · 09n · 本令。**J-81 首行点名七份。**

---

## 〇 先记功,五笔,这一批你做得很硬

1. 🔴 **件 A 你把范围扩大了,而扩大的那一处比我点的更重。** 我只点了 `dropTenantDefaults` 那一处,**你全仓扫出三处,其中 `apps/api/db-backup.mjs` 是按需快照 + 生产每日自动备份的唯一出口,一直在用 `cp`。** 我没点到它。**记大功。**
2. 🔴 **三个现测数把"拷法是错的"钉死了**:本机 WAL 此刻 **4,144,752 字节**(cp 那一刻这 4.1 MB 一个字节不进备份)· **全仓零 `wal_checkpoint`** · **两面证**(WAL 压着 5,000 行时 cp 那份连表都读不到,VACUUM 那份 5,000 行)。**这是 J-58⑥ 的教科书写法。**
3. 🔴 **"能不能还原我没验过(验它要动生产库),只说拷法是错的"** —— **这个分寸正是我要的。** 你没有把"拷法错"夸张成"备份一定坏",也没有为了把话说满去动生产。**记功。**
4. 🔴 **件 B 里那个自探**:`backupFirst` 落在事务中间,你自己去探了时序,并且验了**"备份里看不到未提交的影子表"**。**我没要求这一条,你想到了。** 一个在事务中间拍的快照,如果拍进了影子表,那它就不是"重建前"那一份 —— **你验的正是这个。记大功。**
5. 🔴 **看门狗 300 秒自杀,你没抬它,理由是"那是判据"。** **J-70 的正确执行:报警器响是它在工作,不是它坏了。** 而且你**把排除掉的两条写进回执**(触发器比对段很快;mp 档 9420 会话是活的 426),**免得我以为你没查** —— 这一句是对的做法,记功。
6. **断言总数 4,104 你标明"是算出来的不是量出来的"。** ✅ J-65③ 的精神,收下。

### 一笔自纠要点名

你先把看门狗归因到"夜13 那个自找 `MP_AUTOMATOR` 带来的回归",**后来自己发现看门狗指的是 `tenant-isolation`,不是 mp 档,并改了口。**
**第一次归因错了,第二次纠正了 —— 纠正记功。** 但立一条:
> **J-84(立)· 定位一个红,先问"报警器指的是哪一件",再问"是什么引起的"。** 归因跑在定位前面,最容易把自己最近改过的东西当成答案 —— **因为那是脑子里最亮的那个,不是证据最强的那个。**

---

## 一 🔴🔴 D203 立案 · 开机把 platform-admin 的一次性口令明文打进启动日志 —— **硬挡推**

你报:**开机会把 `platform-admin` 的一次性口令明文打进日志,而 Railway 部署日志是留存的、事后删不掉。与 D195 同族。你没自己改,归我裁。** —— **你判对了,交上来是对的。**

### 裁:这一条硬挡推,理由三条

1. 🔴 **不可逆。** 写进去就删不掉。我们这一路守的所有"两步走""先批后写",守的都是这一类。
2. 🔴 **它是平台级凭据,不是某一家店的。** 一把钥匙,所有店。
3. **它是这次推**夹带**进来的** —— 生产现在 `platform_accounts` **是 0 行**,也就是说**今天的后台根本不靠它登**。
   **这次推的目的是关掉 D194,不是给平台凭空生一把新钥匙。**

### 立刻做的一件(不等别的)

**把那行打印去掉。** 改成只输出「已创建 platform-admin,口令未输出」之类不含值的一句。
🔴 **报错、警告、调试分支里也要一并扫** —— **门守住了却在拒绝的时候把钥匙念一遍**,这一族你上次记过大功,这次别在自己这儿栽。**报扫描面**(扫了几个文件,按什么定义)。

### 然后是一道二选一,你答完 09m 问 3 再定

**口令不打印,就没人知道它是什么。于是只有两种可能,必居其一:**

- **(甲) 这个账号现在没用** —— 没人需要它的口令,后台走的是别的登录方式。
  → 🔴 **那就别在这次推里建它。** 把这个迁移整段推迟到后台线,那时候连着 D149 密码登录一起设计。
- **(乙) 这个账号有用** —— 推完得靠它登后台。
  → 🔴 **那必须有一条不经过日志的取得口令的路。** 照既有口径:**从环境变量读,店主亲手生成与灌入,代码不生成、不打印、不拷贝**(与 `WECHAT_MINI_TOKEN_SECRET` 同族);读不到就**拒绝启动**,不许回落成空(J-53②)。

**要你答的**:**09m 问 3(平台后台的登录与鉴权现在走哪一条,`文件:行`)** —— 🔴 **这一问从"后台线的清单"升级成"挡推的前置",现在就答,不排队。**
**并连着 D200 答**:`requireOwnerToken` 本该守的是不是正是 `/platform/*` 这批口。

---

## 二 🔴 D202 立案 · 生产每日自动备份一直是 cp 一个带 WAL 的库

**与推 main 无关,已经发生很久了 —— 但严重度不低,而且店主必须知道。**
🔴 **她以为自己有每日备份。而按你测出来的三个数,那些备份很可能还原不出来 —— 也就是说,她这段时间一直站在一张自己不知道是空的安全网上。**

**要你做的(都不碰生产)**:
1. **这批自动备份的文件现在落在哪、有多少份、最早一份什么时候**(只读列目录,不打开、不还原)。
2. 🔴 **挑最新的一份,在与生产无关处试着打开并逐表点行数。** ——**只读那份备份文件,不碰生产库。** 打得开 → 报五表行数;打不开 → **报原文错误,这就是"安全网是空的"的直接证据。**
3. **改法**:`db-backup.mjs` 全线 `VACUUM INTO`(件 A 已改),**并补一条常驻判据:这个仓里不许再出现 `copyFileSync`/`cp` 拷 sqlite 的写法**,上限只许变短。
4. 🔴 **旧的那些备份不许删、不许覆盖。** 它们即使是坏的,也是证据。

---

## 三 🔴 挡推的另一条:全量回归。**先量,不先修**

你按 30 分钟停线停了 —— **停得对。** 但这一件我给你解除,换一个更硬的边界:

> 🟢 **为"定位 `tenant-isolation` 为什么跑不完"这一件,解除 30 分钟停线,上限一次 2 小时。**
> 🔴 **边界:只量不改。** 一行产品代码都不许动,判据不许松,看门狗不许抬(**它是判据,你上次守住了,这次继续守**)。

### 要量的四样,一样都不许跳

1. 🔴 **`tenant-isolation` 这一档里有多少个用例,逐用例计时。** 报出最慢的三个。**"这一档慢"不是一个能修的东西;"这一档里第 7 个用例吃掉 800 秒"才是。**
2. 🔴🔴 **拿 `285b20d`(生产那份)跑同一档,逐用例计时,做推前/推后对照。**
   **现在连"是不是回归"都还没定。** 有了这一对数才知道:是这 442 个提交带来的,还是它本来就慢、只是从前没跑到超时线。
   （J-76 同族:要说它是新病,先证明旧代码上没这个病。）
3. 🔴 **变慢的不止你报的两档 —— 全量列一遍。** 你报了 `tenant-isolation` 超时、`test-user-write-auth` 26s→41s。
   **报一个变慢,不等于知道有几个变慢**(J-63④ 的时间版)。**逐档列出推前/推后耗时,标出所有变慢超过 50% 的。**
   **两处都变慢,很可能是同一个共同原因** —— 而共同原因只有在全量那张表上才看得见。
4. **这一档在哪一步卡住**:是某个查询、某次启动、某个等待?**给现场证据,不要给猜测。**

### 判别式

> 🔴 **超时不是断言失败。** `tenant-isolation` 现在是"跑不完",不是"结论错了"。
> **这两件的处置完全不同**:结论错了要修产品;跑不完要先知道时间花在哪。
> **在没量出来之前,任何"顺手优化一下"都是盲改**(J-78 同族)。

🔴 **2 小时到了还定不到 → 停手、报我,不许继续。** 到时候我们谈第二条路(比如:在这一档上先拿一份逐用例耗时表交店主,由她决定要不要为这一件再花时间)。

---

## 四 🟢 三件裁定

### ① 棘轮 +2 行 —— 批

`local-server.mjs` **17,621 → 17,623**,两处调用各加一个 `dbPath` 参数,**不传就没有备份。**
🟢 **批。新上限 17,623。**
**理由**:这 2 行换的是「`DROP TABLE` 之前有备份」。**棘轮是为了防止这个文件继续膨胀,不是为了阻止安全修复。**
> **J-85(立)· 棘轮遇到安全修复让路,但让路要记账。** 抬上限时必须写明:抬了几行、换来的是哪一件安全修复、新上限是多少。
> 🔴 **而且不许拿这次抬出来的额度去装别的东西** —— 下一次这个文件到 17,623 还想再加,照旧要单独批。

### ② 件 A / 件 B / 件 C —— 收,解除这三条推停

- **件 A** ✅ 全仓三处改 `VACUUM INTO`,两面现证。
- **件 B** ✅ 顺序 `rebuildTenantScopedUnique`(:17035)在前、`dropTenantDefaults`(:17529)在后;跑完 `users` 建表语句**逐字贴出,含 UNIQUE:没有**;两处 `DROP TABLE` 各补备份,时序自探过。
  🟡 **一句更正我自己**:我 09n 担心的是"dropDefaults 重建后让 unique-rebuild 触发"—— **顺序反了,unique-rebuild 在前。我的担心方向错了,但你答的那一条(跑完之后建表语句里没有 UNIQUE)把两个方向都盖住了。** 记我一次方向判错,不记你。
- **件 C** ✅ 回填给算式不给估数,**`email`/`phone` 两个全库数没量过就不拿估数填** —— 🔴 **这个分寸记功。一个编出来的数比一个缺口更难查。**

### ③ 触发器比对改成逐名 ✅

原来只数条数没比名字,已改成逐名比、对不上就抛(整批回滚),现证「逐名比对通过 9 条」。
🟡 **顺带一句**:此前所有"触发器 9 条原样装回"的结论,都是在只数条数的尺子上得出的。**现在逐名比过了,结论没变** —— 但这正是 J-68② 那条(换尺子旧数作废)该在的位置,**记在案:那些旧结论从此以新尺子那次为准。**

---

## 五 D201 痕迹查询 —— 🔴 不接受"指纹分不开",再找一层

你先说清:`confirmMockPayment` 与有门的 `/payments/mock/confirm` 写的是同一个 `provider='MOCK'`,**指纹分不开,查出来只能是「多少行处在这个形态」,不是「被打了多少次」。**
🔴 **这一句主动说出来,记大功** —— 正是 J-58⑨(指纹要选到只有这条路会留下的那一层)。**你没有把一个分不开的数当成答案交上来。**

**但结论不是"就这样了"。追问两条:**

1. 🔴 **有门的那条路,会不会留下别的东西?** 会话、`user_id`、操作日志、`updated_by`、时间戳的密集程度、来源 IP……
   **只要有任何一个字段是"走有门那条路才会被写上的",那就是指纹。** 逐个找,报找过哪些、各自为什么不行。
   **J-58⑨:指纹要选到「只有这条路会留下」的那一层,使用前单独证唯一。**
2. **如果确实找不到第二层** —— 那就**改查询的形状**:不要只数行数,**逐行列出**(店 · booking id · 金额 · 创建时间 · 对应 booking 的状态)。
   🔴 **数分不开,人眼分得开** —— 两家真店的量级都很小,逐行看得完。

**两条都做完,把查询原文重新交上来等批。** 🔴 **在我批之前一个字不许跑;只许 `SELECT`;非 0 立刻停,只报不动,不许删任何行、不许改任何状态。**

---

## 六 现在的挡推清单

| | |
|---|---|
| 🔴🔴 **D203** | 开机打印 platform-admin 明文口令。**先去掉打印(现在就做)**;账号建不建,连 09m 问 3 一起定 |
| 🔴 **全量回归** | 拿不出一次绿。**先量不先修**,四样数据,2 小时上限 |
| ✅ 解除 | 件 A · 件 B · 件 C · 库落点 · `railway.json` · 门禁回归 |
| 🟡 不挡推 | D201(推前推后一样)· D202(已发生很久)· 09m 后台清单 |

**两件全绿 → 执行 09n §四 的备份(四条不变)→ 清单交店主 → 她说「推」才推。**
🔴 **一个 push 都不许执行。**
