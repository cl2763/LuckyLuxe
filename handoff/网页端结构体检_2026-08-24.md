# 网页端(admin.js)结构体检 · 2026-08-24(只查不改)

后端体检上批已交(local-server.mjs)。本篇补网页端,按公约⑤ 月度结构健康报告的格式。

## 一、量

| 文件 | 行数 | 说明 |
|---|---:|---|
| apps/web/admin.js | 9,600 | 老板端+员工端同一套(按角色渲染),**超红线 1500**,只许瘦 |
| apps/web/customer.js | 2,721 | 网页顾客端,超红线 |
| apps/web/wechat-simulator.js | 669 | 开发用模拟器 |
| apps/web/share.js | 214 | |
| apps/web/order-state.js | 82 | **本批新增**:订单状态与动作表达域 |
| apps/web/snapshot-viewer.js | 57 | 快照浮层(已是共用件) |

admin.js:216 个函数,其中 **90 个是 render\***(占 42%)—— 这个比例说明它本质是「一个巨型渲染层」,取数、状态、表达三件事没分家。

## 二、结构问题(按严重度)

1. **一个文件装了 8 个业务域**。粗分:AI/客服 30 个函数、客户/会员 18、订单/预约 12、门店设置 12、服务价目 11、员工排班 10、财务日结 2,另有 49 个通用件与 72 个难以归类的(多为一次性弹层与局部渲染)。改任何一个域都要在 9,600 行里定位。
2. ~~**超长函数**:`updateWechatMock` 892 行、`readStoredAuth` 781 行…~~
   🔴 **2026-08-25 更正(这条是我量错的)**:那份数字是用「到下一个 `function` 声明的距离」量的 ——
   函数后面跟一大段非函数代码时,这个距离把那段全算进去了。按 `}` 收在第 0 列重量:
   `updateWechatMock` **实测 8 行**(写覆盖 → 存本地 → 重渲染),`readStoredAuth` 同理不是 781 行。
   **真实的超长函数榜**(admin.js,搬出 AI 域之前):
   `renderSalaryPlanEditor` 213 / `renderFinancePayroll` 162 / `applyLanguage` 149 /
   `renderFinanceTrend` 144 / `renderDailyClose` 116 / `renderDashboard` 99。
   量法脚本已固化,以后体检一律用它,不再用 awk 估距离。
3. **classic script 全局作用域**:没有模块系统,函数全挂全局。好处是拆文件成本极低(新建 `/web/xxx.js` + admin.html 加一行,本批 `order-state.js` 就是这么拆的);坏处是没有边界,谁都能调谁。
4. **表达与口径混住**:金额句、徽标句已陆续收到后端唯一出口,但仍有前端自算的残留(如 `bookingSource()` 拿不到真来源就哈希编一个,见 D71)。

## 三、拆法建议(按公约②「边改边拆」,不设专门重构批)

按域切,一次一个,每次都跟着当批要改的功能走:

| 优先 | 域 | 大致内容 | 预计搬出 |
|---|---|---|---|
| 1 | AI/客服 | 22 个函数(实测 631 行) | **已完成 2026-08-25:搬出 630 行到 `ai-desk.js`** |
| 2 | 财务/日结 | 日结、工资、账本视图 | ~800 行 |
| 3 | 客户/会员 | 档案、券、积分 | ~700 行 |
| 4 | 服务与价目 | `renderPricingRules` 等 | ~500 行 |
| 5 | 弹层基础件 | `openFormModal` 335 行 → 共用组件 | ~400 行 |

按这个顺序走完,admin.js 会落到 ~5,500 行;要到 1,500 红线以下还需要把 render 层继续按页拆。**不建议一次性重构**(测试覆盖不到渲染层,大改必翻车),按棘轮律每批削一点。

## 四、本批已做

- 新建 `order-state.js`(82 行):`statusLabel` / `activeStatuses` / `isAfterSalesOpen` / `hasAfterSalesTrack` / `matchesStatusFilter` / `renderAfterSalesReadonly` / `bookingActionButtons` / `bookingSource`。
- 删掉一份无人可达的重复实现(`data-convert-aftersales` 转售后弹层)。
- 棘轮:admin.js 9,617 → 9,600(**−17**)。
