# 网页开单 UI 重做 · 两端对照走查单(2026-08-29)

> **小程序结算屏 = 设计合同**(店主原话:「可以把它做成跟小程序一样的吗?包括 UI 设计。
> 现在每一个环节都需要有一个下拉菜单才能点开,这个很麻烦。」)
> 本单给店主按块对照走查:左列=小程序屏上那块(wxml 行号),右列=网页对应块。
> ⚠️ 小程序侧像素给不到(本机没有微信开发者工具)—— 左列以 wxml 结构为准,店主手机上开一次小程序即见。

## 一、逐块对照(块序照小程序屏序)

| # | 小程序屏上的块(settlement/index.wxml) | 控件形态 | 网页对应 | 网页控件形态 | 动作数 |
|---|---|---|---|---|---|
| 0 | 头部:标题+顾客行(姓名+脱敏手机号+「新客·未绑定」徽标,后端出句)(L2-10) | 文本行 | ✅ 同位 | 同形(徽标同句) | 0 |
| 1 | 价格体系:**chips 单选**(原价/分享价/会员价/疗程价)+「默认按会员判定/已改档将留痕」(L35-38) | chips | ✅ | **chips**(原来是下拉,已删) | 点 1 下=改档(两端同) |
| 2 | 服务项目:大类**横向 chips** + 项目**列表行**(✓勾框+名称+划线原价+现价)+「本组主项目:X(点其他大类里的项目=替换)」(L42-72) | chips+列表行 | ✅ | 同形;**宽屏项目行两栏**(只用横向空间,交互不变) | 选项目点 1 下(原来:点开下拉→找→点=2-3 下) |
| 3 | 次卡:卡列表行(过期灰不可点)→核销项目 chips(L46-62) | 列表行+chips | ✅ | 同形 | 同 |
| 4 | 加项目录:列表行勾选;按指计价=**stepper(− n指 ＋)**(L74-97) | 列表行+stepper | ✅ | 同形(stepper 原来没有,已补) | 勾 1 下 / 步进 1 下 |
| 5 | 自选填写行:名称+金额+「＋添加」;已加行点击移除(L99-108) | inputline | ✅ | 同形(金额框 data-money 族规) | 同 |
| 6 | 本单技师:**chips**(1–2 位,主/副)(L110-116) | chips | ✅ | **chips**(原来没有这形态) | 点 1 下 |
| 7 | 双技师编号分配 minichips(L118-127) | minichips | ✅(08-30 死口清剿批接回) | **minichips 同形**(数字清单+每技师排,itemNos 原序进提交体) | 点 1 下=选/取消(两端同) |
| 8 | 被服务者:input full,占位「留空=本人;朋友填称呼」(L129-131) | input | ✅ | 同形(原样输入不重画) | 0 |
| 9 | ＋添加第二个服务项目(L135) | 虚线按钮 | ✅ | 同形 | 1 |
| 10 | 定金:**radio 两态**(已付抵扣 −X / 未付定金);无记录=empty 句(L140-150) | radio | ✅ | **radio 同形同句**(原来是 checkbox) | 1 |
| 11 | 优惠券:**入口行 →弹层选择**(L153-166 + 面板 L246-266) | 入口行+sheet | ✅ | 同形(入口行+居中弹层) | 2 下(开+选,两端同) |
| 12 | 整单规则:足部美甲 / 甲片重复利用 两个**开关**(L169-179) | switch | ✅ | **switch 同形同句**(原来整块没有,已补——开关会传给后端,店里没启用该规则时后端不加价,两端同口径) | 各 1 下 |
| 13 | 支付构成·菜单式:储值卡抵扣✓行 / 到店收·差额自动(**被动行→**)(L182-210) | 勾行+被动行 | ✅ | 同形同句(差额金额后端算) | 勾 1 下 |
| 14 | 随单充值行 + 挂充面板(L199-206 + L288-303) | ✓行+sheet | ✅(08-30 死口清剿批接回) | **✓行+居中面板同形同句**(套餐后端 label/手输无赠/D55 不联动/未绑定灰链同句拦) | 同小程序(开面板+选+挂) |
| 15 | 分组明细+合计:组标题、行(名称×qty+划线原价+金额)、**原价合计/档位小计/共优惠/次卡抵扣/储值抵扣/定金抵扣/到店应收(大)**(L213-243) | 收据式 | ✅ | 同形逐行(原来只有 4 行,已补全) | 0 |
| 16 | CTA 大按钮 + foot「金额全部由后端计价引擎算出,本页不做任何金额运算」(L268-270) | 按钮 | ✅ | 同形同句 | 1 |
| 17 | 出码/绑定码层(L305-326) | sheet | ⚠️ 网页=显示待签单号,签署走顾客侧既有链 | 文字条 | —(签署路径本来两端不同,原第一版口径) |

**动作数结论**:主流程(选档→选项目→加项→技师→被服务者→提交)两端**逐块点击数相同**;
~~网页比小程序少两步~~ → **08-30 死口清剿批已把双技师分配、随单充值、现场购卡全部接回,两端逐块等同,少零步多零步**。
**下拉从 6 个减到 0 个**(常驻断言:开单模块渲染输出零 `<select>`,塞回一个当场红)。

## 二、网页截图(4128 · 小婕店 198 单长列表,夹具规模律)

上半屏(实拍):顾客行「阿禾 138****0003 新客·未绑定」→ 价格体系 chips「**原价(默认)** 分享价 会员价 疗程价」→
大类 chips「**美甲单色** 美甲简单款式 美甲复杂款式 美睫 护理 卸甲」→ 项目行「✓ 精品单色 ¥368 | 高光猫眼 ¥468」(两栏)→
加项行(免收标 + 纤维补甲「− 0 指 ＋」stepper)。
下半屏(DOM 现测):定金 empty 句 / 券入口行「无可用券」/ 整单规则两开关 / 支付构成「储值卡抵扣 + → 到店收·差额自动 ¥368」/
收据式合计「原价合计 ¥368 · 档位小计 ¥368 · **到店应收 ¥368**」。

## 三、安全网与变异

- **19 项既有判据原样全绿**(body 纯函数深比对 / 落库 33 列逐字段 / 空格 / 零算术 / 12 锚点 / 真点)+ 新增 2 条:
  **零 `<select>`** · 小程序同形控件七件在场(chips/勾框/stepper/radio/switch/券入口行/应收大行);
- 变异:塞回一个下拉 → 重启 → **零 select 判据当场红**,还原回绿,零残留。
