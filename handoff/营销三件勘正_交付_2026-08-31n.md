# 营销三件 · 勘正结案(2026-08-31n)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。零代码改动批(勘正+登记)。

## 结论:零欠账,撤单

31j 差集表「social-copy/recall-copy/daily-brief 仅小程序」是**我数错的** ——
那轮机核 `sort | uniq -c | head -16` 被 head 截断,网页 ai/ 三行掉线下。现测勘正:

| 口 | 网页面(早已在) | 小程序面 | 行为现测(沙箱镜像) |
|---|---|---|---|
| daily-brief | 老板首页「AI 简报」面板(#aiBriefPanel,admin.js:5483) | 老板首页简报卡 | 200,回今日头条句(mock 形) |
| recall-copy | 客户档案 S 层「✦ AI 召回」钮(admin.js:5069)—— RFM 接回批还在用它 | 客户库圈人批量 | 200,一人一句(Cathy 句现渲) |
| social-copy | AI 图库平台发帖文案(admin.js:5533,aiCopyHistory 避重) | 作品详情/内容页 | 同一路由(/admin/ai/social-copy) |

## 收获(J 族入册)

**机核管道里的 head=静默失败器**:`…| head -16` 出的"全集"少了三行而无任何提示,
直接写进裁定用差集表 —— 与 #10(排班双写路由推测)同族=清单未按现码勘正。
规矩(与「判据匹配必带界定」并列记):**数数出报表的命令不许带 head/截断;要截断必须先打总数**。

## 队列状态

营销三件撤单 → 下一件=**31l 批**(值日表小合同+工资族对表+状态机方案+迁移预研,按其指令文件开工)
→ includes 子串盲全仓扫描 → 店主走查候令(通知页/直排改时间/写小记三新走查点届时并入)
→ 上线前硬门槛审视批(生产 Qwen 冒烟首位)。

## 两小裁定回执

1. 记录页「已撤」灰点:**图 v1.1 已准** —— 现实现(灰点+撤销原因可见、留痕不删行)与更新后的图一致,零改动;
2. includes 子串盲全仓扫描:**已排队列**(第三件),届时立「判据匹配必带界定」编写规矩(本批新判据已先行遵守)。
