# 12n 补四 回执 —— 门槛 58 **逐张对上你的预期**;154 片已更正;标注已入库(未推)

md5 核过:`b91df715fdd89a473780eb178dddb466`,桌面夹与 handoff/ 两份一致。¥0,未推 main。

---

## §一 纯黑门槛 35 → 58 ✅ **与你预期逐张一致**

`nail-tier.json` 只动了 `纯黑_Y上限` 一个值:

```
Y门槛 90 · C门槛 25 · 纯黑_C上限 10   ← 一个字没改
纯黑_Y上限 35 → 58                    ← 只有这一行
```

重跑:

```
机器给出的纯黑: ['02', '18', '38', '39', '46', '74']
你的预期:       ['02', '18', '38', '39', '46', '74']
一致
```

**18 号那条分歧已写进 `nail-tier.json` 的 `已知接受的分歧`**:人眼「深·不鲜明」/ 机器「纯黑」,
Y 39.5 / C 3.6 与 46 号亮面黑(39.0 / 3.6)在 (Y,C) 上不可分,**尺子量程不够,不是门槛定错**;
店主 2026-09-24 接受,**人眼标签不改**(18.json 里仍记「深·不鲜明」)。

**与人眼一致数,两个口径**:
- 18 计为不一致:**24 / 28**
- 18 按「已接受分歧」计为一致:**25 / 28**
- (把 18 整张剔除:24 / 27)

剩下的三处不一致还是老面孔:**23**(人眼浅 / 机器深·不鲜明)· **53**(人眼深·鲜明 / 机器浅)· **65**(人眼混 / 机器深·不鲜明)。

### 🔴 但「四档各 ≥ 5」还是不过 —— 这次卡在**深·不鲜明**

| 档 | 改门槛前 | 改门槛后 |
|---|---|---|
| 浅 | 12 | 12 ✅ |
| 深·鲜明 | 6 | 6 ✅ |
| **深·不鲜明** | 8 | **4** 🔴 |
| 纯黑 | 2 | **6** ✅ |

**这不是新错,是搬家的必然结果**:18 / 38 / 46 / 74 四张原来就坐在「深·不鲜明」里,
门槛一抬整批搬进「纯黑」,那一档就从 8 掉到 4。现在的深·不鲜明只剩 **23 · 28 · 65 · 71**。

**按令「对不上就停下报,不许再挪门槛去凑」—— 我停在这里。** 两条路,你挑:
1. **再补标 1–2 张「深而不鲜明」**(墨绿 / 暗紫 / 咖啡猫眼这一类,¥0,不碰门槛);
   候选里现成就有:**05_IMG_1662**(深灰长甲)、**17_IMG_4226**(灰米哑光,粗估 C 3.2)——
   正是我上一轮因为「那一档已经够了」而撤掉的两张。
2. 认为四档各 ≥5 这条判据在 28 张这个规模上本来就苛刻,**改判据**(那要先改文档)。

---

## §二 片数 151 → **154**,差的 3 片查清了

**差在哪**:23 张那次的 124 片是脚本数出来的;这一次我**没重跑,手写了一个 151** ——
新增五张按 `71(6)+74(6)+39(6)+46(4)+38(5)=27` 加了上去,
而 **46 号实际标了 7 片**(5 枚黑 + 2 枚银箔,我记成只标了黑的那几枚)。`124+30=154` 才对。

逐张机器数:02·7 14·8 18·6 23·6 28·7 38·5 39·6 42·3 43·4 44·6 46·7 49·4 51·5 52·6 53·5
57·7 60·8 61·4 65·6 69·5 70·5 71·6 72·5 74·6 78·4 85·5 87·3 88·5 = **154**。
28 个 json 的「多边形」与「多边形_全图像素」条数逐个相等,没有一片因小于 50 像素被跳过。

**护栏已装**:`nail-tone-true.py` 每次出表打一行 `规模:28 张 · 154 片` ——
**片数从此由机器报,我不许再手写。** 回执已改。

---

## §三 两条候选的离线评估表 —— 本回合未做,下一回合出

要加的两列已经记下:①逐片 Y/C(46 片6/片8 银箔 168/181 · 38 片6 蝴蝶结 119/C 27 · 74 片5 108 · 39 片5 73)
②23 号单列。门槛以本回合改后的 nail-tier.json 为准,**只报不改**。

---

## §四 标注入 git ✅ 按你说的改的根 .gitignore 第 73 行

**你诊断得对。** 时间线是:

```
01:27  我写 12n交付,说「annotations 不是被 ignore,是没加过」—— 当时这句是真的
01:38  提交 dea38f9,**那一次把 tools/segment/annotations/ 加进了 .gitignore**
01:50  我写 12n补三 回执,计划「在 annotations 里加一个 .gitignore 放行」—— 这时已经晚了
```

### 🔴 二次更正:dea38f9 **确实是主线提交的** —— 我最初的判断对,中间那次「更正」改错了

时间顺序得说清楚:
1. 我第一版写「主线对话提交 dea38f9」—— **结论对,但当时我没有证据**,是凭印象推的。
2. 店主查下来觉得没依据,让我改成「是修图线自己的提交」—— 我照改了。
3. 主线对话随后发来旁证,我**自己独立核了一遍**,证实第一版的结论是对的。

**我独立核出来的(不靠主线的说法)**:

| 查什么 | 结果 |
|---|---|
| 三笔提交的时刻 | dea38f9 `01:38:37` → de2cec2 `01:38:54` → 36b14ce `01:39:08`,**31 秒内连发三笔** |
| 三笔各属哪条线 | 12d–12n 修图线 / D217 第一批 / 12m —— **三条不同的工作线** |
| 再往前一笔 | 5bf3250「12c」,**09-23 15:00** —— HEAD 在那儿停了 **十个半小时** |
| 我在本对话里的 git 记录 | **4c382a6 之前我一次 commit 都没跑过** |

**三条不同工作线的提交在 31 秒内连发,而我那段时间没做过任何提交动作** ——
只有「一个人攒了很久一次补交」讲得通。**提交标题写「12d–12n 修图线」说的是内容,不是谁提交的。**

**主线补的旁证**(与上面互相印证):`.git/index.lock` 从 9/23 21:28 起是一把僵尸锁,
12d 之后主线每次提交都失败而没察觉,HEAD 一直停在 12c;摘锁后按三条线分三笔补上。
`tools/segment/{venv,venv312,weights,annotations}` 那几条忽略也是主线在 dea38f9 里加的。

**教训分两层,别混**:
- **结论层**:我第一版说对了 —— 那条更正是错的,现已撤回。
- **过程层**:我第一版**确实没有证据**,只是碰巧蒙对。「没有证据不许把事算到别人头上」这条仍然成立,
  错的是我当时拿不出证据,不是结论。
- **新学到的判法**(主线给的,收下):判一笔提交是谁做的,**看提交时间有没有和别的提交挤在一起、看回执里谁说是自己做的**,
  别只看提交标题 —— 标题写的是内容。

**改法照令**,第 73 行换成四行:
```
tools/segment/annotations/*
!tools/segment/annotations/[0-9]*.json
!tools/segment/annotations/_标注集清单.md
!tools/segment/annotations/_补标挑图理由_*.md
```

**复核(用不带 `-v` 的干净判法,只打印被忽略的路径)**:
```
$ git check-ignore annotations/02.json  _标注集清单.md  _补标挑图理由_2026-09-24.md
(空)                                  ← 三类都放行 ✅
$ git check-ignore annotations/裁片  annotations/自核图
tools/segment/annotations/裁片
tools/segment/annotations/自核图        ← 仍被挡 ✅
```
(带 `-v` 时会打印命中的那条 `!` 规则,不是「没放行」—— 这一点顺带说明,免得你看 `-v` 输出以为没成。)

**暂存与提交**:`git add` 逐路径,**没用 `-A`、没用 `-f`**;`git commit -- <路径>`。

```
git diff --cached --stat → 32 files changed, 34793 insertions(+), 4 deletions(-)
图片 / venv / weights / 裁片 / 自核图 / apps:一个都没有(机械过滤查过)
提交:4c382a6  「12n补四:纯黑门槛 35→58(店主拍板)· 28 张 154 片标注入库」
未推 main。
```
`anno.py` / `nail-tier.json` / `同套甲对照表.json` 确认**早已在库**(01:38 主线那次带进去的),
本次只提交 `nail-tier.json` 的改动。

---

## §五 46 号倾角
收。改后逐片 Y 22/27/30/39/46 与黑甲一致,修正成立。
「从自核图读两端点再算角度」已写进标法,不再凭印象给角度。

---

## 另:handoff/ 里有一份我还没读的新令
`小批12i补二_先出图_按人眼档挑15张跑两腿加回贴_不等机器分类_第七轮对比包_2026-09-24.md`
—— 看标题是「不等机器分类、先按人眼档挑 15 张跑两腿 + 回贴」。
**这与我手上「四档各≥5 才开跑」的停线可能冲突**(它像是绕开分档直接出图)。
**我没有动它**,等你说先跑哪一条。
