# 小批05n 补三 — 🔴 D144:「待审」页面 401——`ai-review.js` 读的是 `lucky-owner-token`,后台登录态存的是 `lucky-owner-auth`(JSON,`auth.accessToken`)。店主登录后第一眼就看见了。**排在 05n 最前面修。**

> 2026-09-06 · 店主截图:`localhost:4310/admin` 已登录(客服工作台 62 条会话可见),「待审 · 模型放行的回复」区显示「待审列表没取到(HTTP 401)—— 刷新看看」
> Cowork 现查:`apps/web/ai-review.js:28` `localStorage.getItem('lucky-owner-token')`;`apps/web/admin.js:20/904/1226` 登录态存在 `lucky-owner-auth`,令牌在 `owner.auth.accessToken`。用真登录令牌 curl `GET /admin/ai/review/pending?since=7d` → **200,列表非空**(第一条就是「算了不约了」那轮);不带令牌 → 401。**后端是好的,页面拿错了钥匙。**

## 一、为什么 24 条判据全绿却挂在第一眼

- `test-ai-review ⑧` 只验「未登录 → 401」,正向路径用的是脚本令牌直打接口——**没有一条判据经过页面那把钥匙**;
- 05k 回执自述「在浏览器里三个按钮都真点到底了」——点的时候用的是什么令牌?`ai-review.js:26` 的注释写着「头一版猜了 `ll_admin_token` 当场 401,改成 `lucky-owner-token`」——**第二次还是猜的**(admin.js 里根本没有这个键),而回执把它写成了「admin.js 就存在这个键上」。这句是不实陈述,归 J-23 那一族(结论与证据反着写),登记 **J-27**。
- 这就是 ⬜ 像素层挂着的意义:断言绿不等于老板打开是好的。店主一眼看出来了。

## 二、修法(小)

1. `ai-review.js` **不再自己读 localStorage**:复用 admin.js 已有的取令牌出口(`:904` 那个 `owner.auth?.accessToken || owner.token`),没有可复用的导出就把它导出成一个函数,两处共用——**钥匙只许有一把**;
2. 顺带扫同族(点名处必扫同族律):`apps/web/*.js` 里所有 `localStorage.getItem('lucky-owner-*')` 列出来,凡不是 `lucky-owner-auth` 的都是猜的,一并归一;
3. 判据(浏览器层,真登录):无头浏览器打开 `/admin` → 走**真实登录表单**(账号密码从本地 gitignored `handoff/本地自查账号.txt` 读,**不许写进代码、判据、回执**)→ 进「待审」→ 断言列表请求 200 且条目 ≥1、点「对」后认可率数字变化;`test-ai-review` 加一条「页面钥匙 = 登录态钥匙」的静态断言(`ai-review.js` 里 `getItem('lucky-owner-token')` 0 命中,白名单式);
4. 造病:把键改回 `lucky-owner-token` → 浏览器判据红 + 静态红;
5. 回执贴**真登录后**的待审页截图(这次账号是店主为走查专门定的,可以用;生产账号永远不行)。

## 三、店主侧

不是你操作的问题,是页面拿错了钥匙。修好前「待审」这块看不到东西;模拟器里聊的那些轮次后台已经记着(我查到列表里已有条目),修好一刷新就出来。
