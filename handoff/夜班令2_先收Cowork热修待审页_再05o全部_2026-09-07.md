# 【已被夜班令 3 替代(2026-09-07 夜),§〇 热修部分仍有效】
# 夜班令 2 — 先收 Cowork 对待审页的热修(未提交,在工作区)→ 再按 05o 全部做 → 停线同前;⑤ 的店主打分随后补入

> 2026-09-07 夜 · 店主要睡了,Cowork 代拟 · 两处各一份:桌面交接夹 + 仓内 `handoff/`

## 〇、先说工作区里有两个未提交改动是 Cowork 干的(不是你)

店主亲测待审页:「按钮点了没反应;改一句改完像没保存」。她要睡前能用,我直接热修了两个网页文件,**没提交**:

| 文件 | 改了什么 |
|---|---|
| `apps/web/ai-review.js`(135→181 行) | 「改一句」改成 亮框 → 填字 → **「保存这句」** 才提交,加「取消」;每次提交在卡片上当场写「已记上:对/改一句/不该答」或失败原因(HTTP 码 + 后端 message);fetch 抛错也显示(原来静默);提交中按钮禁用;成功 0.9 秒后卡片收走再 `load()`;两个 fetch 加 `cache:'no-store'` |
| `apps/web/admin.html` | `ai-review.js?v=` 改成 `20260907-cw-hotfix1`(后来查到服务端会按内容指纹重写 v,这一改无害,留着) |

顺带交代:我为了验后端,用店主老板号真令牌在**沙箱 4310** 对待审第一条点了一次「对」(`judged` 2→3)。沙箱库,不是 4128;写进本批对照。

**你第一件事**:读这两处 diff,认可就作为 `05o-0` 提交(留我的注释),并补三条判据:① 「改一句」不点保存不发请求(空框/未保存 0 次 POST);② 成功后卡片内出现「已记上」字样;③ 失败(造 500)时卡片内出现「没记上(HTTP 500」。不认可就说理由,别静默改回。`admin.js` 仍 8,550。

## 一、然后按 `小批05o_…_2026-09-07.md` 全部做,顺序

1. 05o §三 提速三刀**先做**(预检脚本、批内相关套件、计时)——今晚后面每一步都靠它省时间;
2. 05o §一 新店 `luvia-bj`(「LUVIA 半径美甲美睫 · 北京旗舰店」)两库建店 + mock + 三店并行;**4128 只 INSERT**,写前 `tools/db-backup.mjs`;
3. 05o §二 D145 `classifyTurn` + 五通常驻套件 + 造病;
4. 五通 v3 + 到底率三跑(两尺)+ 店主读版 v3(三店);
5. 05o 其余小件(作废三份抬头、删仓根 `db-snapshot.json`、备份刀指向统一)。

## 二、店主打分

店主睡前会把五通 v2 的分数和评语交给 Cowork,Cowork 会写成 `店主打分_像人五通v2_2026-09-07.md` 放进两处交接夹。**你做 D145 前先读它**:她打低分的那几句就是判据的锚。文件不在就先做别的,做到 D145 时再看一眼。

## 三、停线(同夜班令 1)

写 4128 既有行停;四红线修法不明停;沙箱模型今晚增量 >600 万 token 停;回归红 30 分钟定位不到停、不改判据凑绿;不推 main、不碰生产、不看密钥文件。**新增一条**:全量回归今晚最多跑 **2 次**(预检不算),第三次要跑就停下写原因。

## 四、早上交

回执 05o(可合并)+ `夜班总结_2026-09-08.md` 五行(做到哪 · 提交号 · 停在哪 · token 增量 · 全量跑了几次各多少分钟)。
