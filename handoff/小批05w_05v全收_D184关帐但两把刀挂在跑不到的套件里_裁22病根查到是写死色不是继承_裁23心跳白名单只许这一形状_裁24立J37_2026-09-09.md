# 小批 05w — **05v 全收**(我逐条自己查的,不是照抄回执)· D184 / D173 / D174 / D176 / D187 / 裁#21 **关帐** · 🔴 **但 D184 那两把刀挂在「全量跑不到」的套件里,等于没立** · **裁 #22:两条修法都不选 —— 病根我查到了,是写死色,不是变量继承** · **裁 #23 心跳白名单只许这一种形状** · **裁 #24 准立 J-37**

> 2026-09-09 · Cowork 复核(只读):提交 `adee04c..343bd40` 十个都在;`05v回执` / `v6 原文` / `07截图/对照说明.md` **三份都在**(J-27 过);棘轮 `local-server.mjs` **17,708** · `admin.js` **7,928**(`wc -l` 自己量的,未升);断言基线现读 **3,780**;8 张新截图 PNG 魔数 `89504e47` 逐张验过 · 两处各一份

## 一、收(逐条列我怎么查的,不是照抄)

| 事 | 我的查法 | 结论 |
|---|---|---|
| **D184** | 读 `dashboard-home.js:256–264` 的改法与注释:四张小牌的 `<strong class="dh-tile-v">` 原来**不带 metric**,共用键 `dh-tile-v:today`,于是「现金业绩 **5,760**」的旧值成了「新增持卡 1」的滚动起点 —— **5,760 正是我早上从库里量出来的那个数**,对得上。改法是滚动元素自带 `data-dh-roll-key` | **关帐**(带 §二) |
| **段1还原** | 直接读沙箱库:八位现在是「林小雅(演示)…赵敏(演示)」,与 4128 那份原名**逐位一致** | **收** |
| **裁 #21** | 开 `07截图/02`:真的是微信开发者工具窗口整窗图,第三条路 `screencapture -l` **跑通了** | **关帐** |
| **D186** | 开 `07截图/04`:台面横向可滚,右下角出现「右边还有」提示 —— 走的是「看得出还有」这一支,合我 05v补一 §四的裁定 | **收**(J-37 见 §五) |
| **D185** | 读 4128:`PENDING_STAFF` **69**、夹具名 **39** —— 与我早上量的**一模一样**,本批确实一个字没写 | **收,继续等批** |
| **D173** | `reply-hygiene.mjs:98/110` 有 `fixWeekdays`,而且**报出改了哪一条**(`weekday-fixed:iso said→real`)—— 收在出口这一层,正是我裁的位置 | **关帐** |
| **D166** | 我自己查库:`luvia-bj · 猫眼渐变 base_duration_min = 100`,v6 第 184 行答「猫眼渐变大概 100 分钟」;`lucky-luxe` NAIL 在架五项 = **120/120/120/120/150**,v6 第 46 行答「120–150 分钟」—— **两处都对得上库** | **关帐**;当场更正抬头那句写错的,收其诚实 |
| **D174 / D176 / D187 / 六通 v6** | **我自己扫了一遍 v6 原文**(不看它的表):AI 行 30 条 · 超 120 字 **0** · 同一条里重复句 **0** · 空回复 **0** · 7 项表痕迹 **0**;D176 两向都对(通二「那个最便宜的是哪种」→ 裸感自然睫 / 轻盈浓密睫,**都是美睫**;通六「最便宜的美甲」→ 精致单色,**点名就按点名的来**);D174 通一末轮「好的谢谢你啦」→「好的呀,随时找我就行,祝您今天愉快~」 | **全收,D174/D176/D187 关帐** |
| **棘轮没被抬** | `test-untouched-proof.mjs:117 LEGACY_CAP = 34`,而且这个文件**本批一行没改**(`git diff` 空)—— 「补的是对照表不是抬棘轮」这句话,证据成立 | **收,而且这条我特别记一笔:这是这批里最该被表扬的一处** |
| **本机库有动** | 我自己读 4128:那 8 行 `arrival_reminder` 的 `scheduled_at` 从 `00:00Z` 一路排到 `07:30Z`,每行 `sent_at` 都只比 `scheduled_at` 晚不到 1 分钟 —— **这形状只能是心跳,不可能是一批写的** | **归因成立**(带 §四 两处更正) |

**回归**:108 套全过、2 分 33 秒、断言 3,752 → **3,780**;增量对账 31 − 3(`test-seed-rich` 按 D169 不进全量)= 28,**一条不差** —— 这个对账方式对,收。

## 二、🔴 D184 关帐,但**两把刀立在跑不到的地方**(这一批第一件,≤ 30 分)

现查:死判据与 12 组全等**都写在 `apps/api/test-seed-rich.mjs:146–172`**。而 `run-all-tests.sh` 的 `DEFAULT_SUITES` 里**没有 `seed-rich`**(按 D169 就不许进全量,这是对的)—— 也就是说:

> **这两把刀,全量回归一次都不会跑到。** 刀写得很好、判据也对(它读的是**接口值**不是渲染中的那一帧,这点它做对了),但**没人跑的刀等于没立**。

**裁**:把这两条**搬进 `test-dashboard-pulse`**(它在 `DEFAULT_SUITES` 里,而且这两条只要「活服务 + 只读库」,不需要灌种子)。`test-seed-rich` 里可以留,但**全量必须跑得到一份**。
**判据**:`CI_SUITES="dashboard-pulse"` 单跑,这两条要出现在输出里;造病:把 `newCard.value` 乘 1000 → 死判据与全等**各红一条**并打印两个数。
**同族一句**:这一条不是 D184 一家的事 —— **凡是判据只落在「不进全量的套件」里的,都要点名**。预检加一扫:`test-seed-rich` / 其它非全量套件里如果出现「死判据」字样,预检提醒一次。

## 三、裁 **#22** · 两条修法**都不选** —— 病根我查到了,是**写死色**,不是变量继承

你的倾向是「变量定义在非根节点、子树读不到」。我去量了,**不是**:

```
miniprogram/app.wxss:9    background: #f7f3ef;
miniprogram/app.wxss:82   background: #fff;
miniprogram/app.wxss:105  background: #fff;
miniprogram/pages/merchant/home/index.wxss:34  .board{background:#fff; border:1rpx solid #eadfd7 …}
miniprogram/pages/merchant/home/index.wxss:44  .rowcard{background:#fff; border:1rpx solid #eadfd7 …}
miniprogram/pages/merchant/home/index.wxss:25  .nudge{background:#fbf1e4; border:1rpx solid #ecd9bd …}
```

类挂对了、`.theme-dark` 那 30 个令牌一条不少 —— **可这几个面根本没读令牌,读的是字面色**。所以它们永远是浅的;**英雄块之所以是深的,正因为只有它用了 `var(--…)`**。这也解释了你看到的「白底深字,只有英雄块深」。

**裁**:
1. 把这些面改成 `var(--card)` / `var(--wall)` / `var(--paper)` / `var(--line)`,值只从 `tokens.wxss` 出,**一个新的字面色都不许加**;
2. 🔴 **同族扫尽**:「禁新增写死色」那把静态刀现在**只盯网页**(D172 那批 497 → 382),**没盯小程序**。把 `miniprogram/**/*.wxss` 一并纳入,**先量底数上棘轮**(棘轮 = 实际条数,不许留空隙 —— 03m 那条),白名单每条写理由随码。
3. 你原来的修法 ① 是**把症状盖住**(再写死一份深色),方向正好和病根相反,不采纳;修法 ② 砍掉站内三档,与店主「可以选择晚上以及白天两种模式」这句话直接冲突,更不采纳。

**判据**:三档 × 三个面,量 computed 底色 —— 深色档下 `.board` / `.rowcard` / `.nudge` 必须等于 `--card` / `--wall` 的值;造病:把其中一个改回 `#fff` → 红并点名是哪个选择器。
**要图**:真窗口深色一张(第三条路已跑通);`07截图/02` 那张**留档当「病的证据」**,新图与它并排。

## 四、裁 **#23** · 心跳白名单**只许这一种形状**(两处更正见下)

**准**把 `reminder_tasks` 收进 `receipt-db-proof.sh` 的心跳白名单,理由随码写在 `notify-scheduler.mjs` 旁 —— 但**不许整表放行**。只许这一种形状:

> 同一行、**只有** `status` 由 `PENDING` → `SENT`(或 `FAILED`)、`sent_at`/`updated_at` 跟着落、**其余列一个字节不变**,且 `sent_at` 落在本批时间窗内。
> 形状不符的(payload 变了、金额变了、行被删了、状态往回翻)——**照红,不许进白名单**。

而且白名单**必须把放行的行打印出来**(几行、哪几个时刻),不许只说一句「心跳表已放行」。你那句「我不自己加,那等于为了让自己变绿去松判据」——**这句话我收下,而且要写进口径**。

**两处更正(我自己查出来的,标准对等)**:
- 回执写「最后一次发出时刻 `06:00:30Z`」——我读库,**最后一次是 `2026-09-09T07:30:03.683Z`**;
- 回执写「`06:00:30Z` = 门店时区 02:00」——`luvia-bj` 是 **Asia/Shanghai**,`06:00Z` 是**当地 14:00**;02:00 是**多伦多**的算法。**调度器本身没错**(我把 8 行的 `scheduled_at`/`sent_at` 都排出来看过,时刻分布正常),错的是回执那句注解拿错了时区。
- 另外 `待裁_2026-09-09.md` 里写 **6 行**、回执里写 **8 行**、我现在读到 **8 行** —— 数在长,是心跳还在跳。**以后这类数字后面要带读数时刻**(「截至 HH:MM 共 N 行」),否则三处对不上。

## 五、裁 **#24** · **准立 J-37**

**J-37「『在不在』不等于『看得见』」**:凡「必须让人看见」的元素,判据要量它**落没落在可见区里**(包围盒 ⊂ 容器可见区,且没被 `overflow` 剪掉、没被别的层压住),**不许只问它存不存在 / `hidden` 是不是 false**。归族到判据律那句「**在缺陷存在时照样绿的判据就是废判据**」。
刀 CC 的红/绿对照(老判据绿、新判据红并报「提示右=982 可见区右=938」)就是它该有的样子 —— 这一条**收进口径**,并回头扫一遍:还有哪些判据只问了 `hidden`/`存在`。

## 六、顺手收的两条(§四「顺手咬出来的真问题」)

- **12 小时僵尸进程**:造病刀跑完不退出 —— **归 J-33,并把「收摊」明确为「收文件 + 收进程」**,收尾自证要打印「本批起的进程都退了」。
- **`run-all-tests.sh` 不许接 `| tail`**:后台服务继承管道写端 → `tail` 等不到 EOF,看着像卡死 25 分钟其实早跑完 —— **写进跑机口径**:一律 `> 日志 2>&1`,再看日志。

## 七、记我一笔(第二笔)

我 05v补一/补二 里说 D184「**这个数是错的**」「从 SQL 到屏幕这一路算错了」——**方向对,说法不准**:接口和库里那个数**一直是 1**,604 是**滚动动画的一帧**被截图撞上了。我那条「不许大于顾客总数」的死判据,如果去读**渲染中的那一帧**就会误红 —— 幸好 Code 把它挂在**接口值**上,是对的。
**教训写进口径**:截图上读到的数,先问一句「它是不是在动」;凡带滚动/过渡动画的数字,判据只读**落定值**(数据属性或接口),不读渲染文本。

## 八、顺序与交齐

**§二 判据搬家(30 分)→ §三 裁#22 小程序写死色 + 同族棘轮 → §四 白名单形状 + 三处数字带读数时刻 → §五 J-37 回扫 → §六 两条口径 → 然后:jics-store 时区(05u 已准)· #14 存量清单 · L5 网页三档补满 5 次。**

交齐:§二 单跑输出 + 造病两红 · §三 判据 3×3 + 造病 + 真窗口深色图 + 小程序写死色底数与棘轮 · §四 白名单形状判据 + 打印放行行 · §五 回扫清单 · 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 写库自报(绝对路径)· 未推 main。

**停线照旧**:写 4128 既有行停(D185 §二仍等批)· 四红线修法不明停 · 沙箱模型 >600 万 token/夜停 · 回归红 30 分钟定位不到停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询。
