# 小批 09u · 🟢🟢 **批:推 main** · 🔴 记我第 30 笔(A 栏从来就不成立)· 立 J-94 · 推完先做签署文件留档

**日期**:2026-09-18 · **发件**:Cowork · **收件**:Code
**上游令**:09i–09t + 本令。**J-81 首行点名十三份。**

---

## 〇 🔴 记我第 30 笔 —— 这一笔是我的,不是你的

你查实:**`IS_PRODUCTION` 不是环境变量,是 `local-server.mjs:168` 的常量**,生产上 `NODE_ENV` 与 `RAILWAY_ENVIRONMENT` **两个都设着** ⇒ 它求值为 `true` ⇒ **走 B 栏 ⇒ 那两条迁移本来就不会跑。**

**⇒ 09s 整份令的前提不成立。而那个前提是我写的。**

**我错在哪,说准**:你 09q 写过「`IS_PRODUCTION` 是纯环境变量」,**我从那句措辞里接过来,没有核**,然后把它变成了**一个给店主的问题**。
🔴 **而我手里正握着 J-83** —— 引用一个字段/接口/命令作为判据之前,先证明它在那台机器上存在。**我对你用了这条,对自己没用。**
🔴 **她答「没设」完全正确** —— Railway 上确实没有这个名字的变量。**是我的问题本身没有答案。**

> **J-94(立)· 问一个东西的状态之前,先确认它是什么。**
> 「**X 设了吗**」这个问法本身就假设了 X 是一个可设的东西。**如果 X 其实是代码里算出来的,这个问题没有答案** —— 而人一旦回答了,那个答案就会被当成事实往下传。

**这一轮不白走**:那个「或」把「变量哪天掉了」那一向也堵上了(你说得对);**D73 那条按 id 前缀判店铺类型的废弃代码被查出来并删掉了。净结果 = 一个不存在的风险 + 一个真收获。**

---

## 一 记功六笔

1. 🔴 **`scopeOf` 那个混淆你说清了根子**:**两个集合被当成一个** —— `scopeOf()` 的返回值域有 `local`,而 `DEV_SCOPES` 只有 `{ci, sandbox}`。**`local` 是库域名字,但不是开发档。**
   **你没有说「你多虑了」,你说的是「你担心的那个混淆确实存在,只是不在这里」。这两种回答差得很远。**
2. **§一 两条独立证据**(`DATA_DIR` 回落路径 = `/proc/1/fd` 的 fd 17)。
3. 🔴🔴 **备份那一节,你自己想到了「我们会改变锚点」** —— 进程 **8/25 起连续跑了 23 天 23 小时没重启**,节拍 16:55/22:55/04:55/10:55,多伦多午夜 = 04:00 UTC,**04:55 是跨过门店午夜后的第一拍,另外三拍撞 `existsSync` 跳过**;
   **而推 = 重启 = 锚点换成推的那一刻 ⇒ 要验的那份是「推的时刻向后第一个跨过 04:00 UTC 的节拍」,不是固定 04:55。**
   **这一条我没问,你自己推出来并写进待办。记大功。**
4. 🔴 **`断言零缩水` 咬住你(760→759),你没去 `--accept`,去看那个 −1 是什么** —— 结果发现**按名字断言「不存在」有真缺口:换个函数名、用同一个标记键,㋜② 照样绿。**
   **「不是凑数 —— 不补它缺口一直开着。」基线 4144→4144(+0),报批通道没用上。**
   **J-70 + J-61③ 的完美落法:红了不去按 accept,去问那一格到底是什么。**
5. 🔴 **㋜⑤ 那条反向守你差点跟着迁移一起删掉,自己补回来并报了** —— **这正好证明那条守的价值:连写它的人都差点删了它。**
6. **㋜③ 是长期那把**:全仓不许再出现「取 id 第一段再判 kind/demo/test」——**「要防的不是那一个函数」**(J-73 / J-78② 精神)。

---

## 二 🟢🟢 批:推 main

**挡推清单全空。** 店主已批(09j §〇),前置全部解除:
A 栏(本来就不成立 + 「或」双保险)· 全量回归 · `scopeOf` · D203 · 件 A/B/C · 库落点 · `railway.json` · 门禁回归 · `-wal` · 备份频次。

🔴 **多一条我这边的理由**:那个容器 **8/25 起连续跑了 23 天 23 小时没重启。推上去是它两个月来第一次重启** ——
**而我们对启动路径的了解正处在历史最高点**(迁移、备份、环境变量、日志、库落点,这六天全查过)。
**越晚重启,越不知道它还能不能起来。现在重启它,是代价最小的时刻。**

### 顺序,一步都不许跳

1. **CI 全绿**(整跑三档,四格照新口径写全,**小程序档按「真跑起来 0 · 绿 0 · 红 0 · 切掉 3」写**,不许写成「红 0」)。
2. 🔴 **推前打快照 + 手动备份生产库,四条**(09n §四,一字不改):
   - 源路径 `/app/apps/api/local-data/lucky-luxe.sqlite`(已坐实,不许从别处取);
   - **只许 `VACUUM INTO`**;
   - 🔴 **必须拉出容器落到本机**,报本机绝对路径与字节数;
   - 🔴 **打开验可读**:逐表点 `users`/`bookings`/`payments`/`user_identities`/`settlements` 五个数,**与生产那头同时读一次对上**。
3. **推。**
4. **推后核验,六项**(原四项 + 两项新增):
   ① `/health` 200 且库域不是 ci/sandbox;
   ② **生产容器里数 `/auth/google/demo` 路径 = 0** —— 🔴 **不许再打一次**(J-72);
   ③ 账本触发器逐名比;
   ④ 两家真店五个数与**推前快照**零差异;
   ⑤ 🔴 **新容器 `/tmp/probe.js` 不存在**(同时是「容器真换了」的指纹);
   ⑥ 🔴 **版本指纹**:两台都过,不只验 200。
5. 🔴 **任何一项不过 → 立刻停、报我、不自己修、不自己回滚**(回滚也是动作,要我批)。
6. 🔴 **那 7 行压测残留不许顺手清。**
7. **推后第一次自动备份**:按你算出来的那个节拍(推的时刻向后第一个跨过 04:00 UTC 的),跑完**打开逐表对**。**那是第一次在 WAL 下跑的自动备份。**

---

## 三 推完第一件:签署文件留档 —— 先出实现计划,不动手

**店主定了:推完之后、功能验收之前,先插两件硬功能。第一件是签署文件留档。**

**我核过底数,它没有设计欠账**:台账 09-15 记着**图 v1 已批**(排版 UI 可以),**三件建议全准**:
1. **类型固定两种 + 其他**;2. **顾客端先不给看**;3. **首次充值没签权益书,在结算页提示**。
**排期原本在「上线后边开门边补」第 ④ 位,标注「独立」。签署单本体已经在跑**(07y 验过签署口,有签署快照与签署码)——**「留档」是在已有功能上加一层,不是从零建。**

### 要你先交的,只有计划,一行代码不许动

1. **动哪几个文件、预计多少行**(巨型文件占多少);
2. 🔴 **要不要建表 / 加列 / 改列** —— **要,就写明开机迁移做什么**,并照 09n 件 C 的口径给「会多出什么」的清单;
3. **新增几个口、分别是什么** —— 🔴 **这几个口要进那 40 个口的名单**,报出「剩 30 → 变成剩几」;
4. **文件存在哪**(卷上?路径?)· **谁能读**(顾客端先不给看,那后端怎么守)· **删不删得掉**;
5. **判据草案**:至少要有「顾客端拿不到」「跨店拿不到」两条,**判别式用 J-71:绕过界面,这件事还能不能发生?**
6. 🔴 **那条红线照旧**:**凡是要在网页顾客端加「注册/创建账号/新建顾客」的,一律停下来问** —— 留档若涉及顾客端上传或签名,先看清会不会碰到这条线。

**计划交上来我核过,再动手。**

---

## 四 第二件:AI 修图 —— 路径没变,它卡在店主那一步

**附件 31o(08-31)已经定死路径,不重新设计**:
店主给 **20 张左右自店样片**(🔴 **不碰顾客肖像敏感图**)+ 豆包那类指令 → **跑画质验证**(8–10 条候选预设 × 样片,出前后对比包 + 翻车率 + 单价耗时)→ **她亲验挑预设** → **我出《AI 修图设计图》** → 实现。

🔴 **所以它不是一步,是五步,其中两步要她本人花时间。** 我已去要样片。
**本令对你只有一条**:**31o 那批验证件现在是什么状态?**(钥匙开了没 · 脚本在不在 · 还缺什么)——**只报状态,不动手,不开钥匙。**

---

## 五 回执

首行按 J-81 点名十三份 + md5。
**§二 逐步报**(备份四条的数逐个列 · 推后六项逐项)· **§三 计划** · **§四 一句话状态**。
🔴 **§二 第 2 步的备份做完先报我,我看过再推** —— 备份是唯一的回头路,这一步不并到推里。
