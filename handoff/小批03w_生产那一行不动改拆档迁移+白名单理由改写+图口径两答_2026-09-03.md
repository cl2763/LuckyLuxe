# 小批03w · 三裁(2026-09-03)

先说做法:生产四次查询全 `readOnly`、脚本 base64 走 `node -e` 不落文件、令牌只比哈希不出内存、`demo_staff_accounts=4` 量错对象自己纠回——这一批的手法是对的。收。

## 一、生产那一行:不动,不删,改「拆档迁移」随部署走

那张单四个特征都像真单(小程序来源、CONFIRMED、08-20、备注空),而且它把根子演示得很清楚:**小婕店 0 个顾客档案、1 张预约,唯一那位顾客被 `DEFAULT 'lucky-luxe'` 吞进了旗舰店。** 这不是脏数据,是被吞的人。删掉是把受害者当垃圾清。

裁:**现在生产不动。** 正确处置是一次条件迁移——「跨租户单拆档」:凡 `bookings.tenant_id ≠ users.tenant_id` 的单,在单所属的店里为该顾客建一份档案(复制身份字段,不复制别店的余额/会员),把单指过去;幂等、可重跑、先备份、删前删后打数。这条迁移**与 D126/D127 的部署同一批上生产**(上线硬门槛批),先在沙箱用本机库副本彩排(造一条同形的单验它)。旗舰店那 21 位「零预约档案」不猜归属,不动。

## 二、凭据刀白名单那条理由:改写,并给它一个判据

`LuckyluxeStaff0312` 那条理由从「⚠ 暂放行,待只读核生产」改成事实句:「`STAFF_DEMO_PASSWORD` 的默认值;该口令闸整个包在 `if (!DEMO_LOGIN_ALLOWED) throw 403`(行号)之内,生产 `ALLOW_DEMO_ADMIN_LOGIN` 未设,请求在比对口令前即被挡;惰性成立的前提=生产永不设该 env」。前提要有判据守:上线硬门槛批的「生产 env 核验清单」里加一条 `ALLOW_DEMO_ADMIN_LOGIN` 必须未设(与「生产不许有演示白名单」同一条律),核验脚本只读、只报设/未设。

## 三、图的两处口径(已写进图 v2.1 §四)

1. 跨已日结/未日结的周月年:**逐日拼**——已日结的日子读 `daily_closes` 快照,未日结的日子(通常只有今天)实时算,两段相加;冲销按 N-5 当期冲减,记在哪天落哪天,所以拼出来的月与日结月报天然相等,这就是判据 1 的月度形态。
2. 员工身份不含店级 metrics = **后端按身份裁字段**,前端隐藏不算。

图仍是草案,店主点头再开工。

## 四、顺序

回 03t §二 逐条结案(D121 / 事务回滚 9 处 / 梯子三条+quoteLabel / D104 10 处 / 三条只答 / ¥330 只查报批 / 走查回执)→ §三 AI 复测 12 场景。拆档迁移与 env 核验清单写进上线硬门槛批,我另发那张单。
