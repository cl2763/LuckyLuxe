# 回执 · 小批 02p —— 两处退回 + 造病验红 + 沙箱清单层面

## 一、退回①:条件写死 `true`(恒真兜底,J 族复发)

抽检刀那条断言名字说"两态都只留一张卡且 image 确实换了",两件事上面**确实算了**,
但没喂给断言,条件是字面量 `true`。这是「判据自述须与判据行为一致」的**第二案**
(第一案:名字叫"不挂账"、条件写 `<= 14`)。

**修**:`measure()` 把 `{cards, imageMatched, rendered}` 推进 `proof` 数组,断言改判
`proof.length === 2 && every(cards===1 && imageMatched && rendered>=1)`。

**自守补第二支**(店主令):任何断言条件是字面量 `true` 一律红。
落在 `test-image-placeholder.mjs`,扫全仓 `test-*.mjs`;名字明写「跳过」的除外(那是有声跳过,不是兜底)。

**它一上线就咬出 6 处不是我这一处的同族**(白名单判据 > 黑名单判据,这就是价值):

| 文件 | 断言 | 原来判什么 | 改成判什么 |
|---|---|---|---|
| test-mp-placeholder-size | 抽检①② 造态自证 | `true` | proof 两态各 1 卡 + image 换了 + 真渲染 |
| test-admin-accounts:40 | 老板凭据文件已写 | `true` | 文件非空 + 含账号 + 含「初始密码: xxx」 |
| test-settle-stress | ① 恒等式 N 轮零破例 | `true` | `identityRounds === 1200`(实验了几轮) |
| test-settle-stress | ② 入参没变金额没变 | `true` | `sameSetRepeats > 0`(321 次) |
| test-settle-stress | ② 逐个减项目合计变小 | `true` | `decRounds === mains.length-1 && > 0`(实减 3 次) |
| test-store-jury | ④ 矩阵店特征逐家现测 | `true` | `featChecked === built.length && > 0`(22 家) |
| test-store-jury | ⑤ 强制页链五连 | `true` | `chainRuns === 5` |

七处**同一个形状**:循环里 throw 得挺凶,断言本身什么都不判 —— 循环跑 0 次照样全绿。
改法统一成「计数即证」(store-jury 的 ③ 本来就是这么写的,是文件里现成的正例)。

**我这一轮自己又踩一次**:noshow ⑰ 我第一版写成 `dbx.prepare(...member_timecards...)` ——
句柄名不存在、表也不是夹具插的那张(夹具插的是 `stored_value_transactions`)。
`❌ dbx is not defined` 当场炸出来。现改为新开只读句柄查 `sv_fx17_*` 那一行且 `amount_cents === -700`,
**判据名字同步改准**("储值消耗 fixture 真落账"),不叫"耗卡"。

## 二、退回②:宿主高只判"两态相等",没锚定 97

您 02m 的原文是「相等**且等于 97**」,我只落了一半。
您指出的漏洞完全成立:**尺寸类被删掉或改成 auto,两态会一起塌成同一个值,这刀照样全绿** ——
而那正是 D120 本身的病。

**修**:锚不写死 97,按屏宽现算 `170rpx → px`,容差 2px:
```js
const EXPECT_H = Number((170 * (await mp.evaluate(() => wx.getSystemInfoSync().windowWidth / 750))).toFixed(1))
// 97.5px
… && Math.abs(withImg.host[0] - EXPECT_H) <= 2
```

## 三、刀留痕律第二形态 —— 造病验红(三步全贴)

```
【第1步 造病】miniprogram/pages/home/index.wxss
             .recommend-visual  height: 170rpx → height: auto     ← 这就是 D120 的病
【第2步 跑刀】❌ Error: 🔴 D120 两态宿主高相等**且锚定 170rpx(97.5px)**
                       有图 宿主[0]/ph-img[0]/ph-box[不存在]
             [判别] 甲 .recommend-card 命中1 [140]  乙 img-placeholder 命中1 [50]
             ↑ 卡片从 187 塌到 140、宿主从 97 塌到 0 —— 病造回去,刀红了,且红在锚上
【第3步 恢复】cp 还原,cmp 字节一致 → ✅ ok1/ok2/ok3 全绿
             [判别] 甲 .recommend-card 命中1 [187]  乙 img-placeholder 命中1 [97]
```

**结论**:它不是"相等检查器"。第2步两态**确实一起塌成同一个值**(都塌了),
若只判相等这一步会绿 —— 锚定 97.5 才是真正拦住它的那半条。您的退回意见在这次实证里直接兑现。

## 四、追问的答案:沙箱是清单层面的事,我不再各打补丁

您说得对,我上一版是「给一把刀打一个补丁」。现扫清单:

**全仓依赖沙箱 4310 的套件只有 1 个** —— `mp-placeholder-size`(它要真环境跑小程序)。
其余套件全用回归自建的临时库(`/tmp/ll-ci-data.XXXXXX`),`pkill` 打不到它们。
所以这次不存在"还有几套受同一影响"的清单;但**第二把这类刀迟早会有**。

**处置**(按您的话:统一前置,不许各写一套):抽出共用件 `apps/api/test-need-sandbox.mjs`,
导出 `ensureSandbox()` —— 探活 → 不活就 `start-sandbox.sh` 拉起 → 12×2s 轮询 → 拉不起来**明说"这一刀本轮未跑"**
(不静默跳过,跳过不得连任)。抽检刀已改成接这个出口,私有实现删掉。
第二把刀接这里,不许并排再写。

全量回归里的现场证据(证明前置真在工作、刀真跑了、没被跳过):
```
[前置] 沙箱 4310 存活=false;首页推荐位=0 条        ← 回归确实把它打死了
[前置] 自拉沙箱后存活=true(共用前置件)
ok 1 / ok 2 / ok 3   [mp-placeholder-size] all 3 checks passed
```

## 五、回报固定六项

1. **改了什么**:7 处恒真兜底改成计数即证 · 宿主高锚定 170rpx · 自守②(字面量 true 一律红)·
   沙箱前置抽成共用件 · noshow ⑰ 判据改准(含名字)
2. **在哪一层验的**:小程序**真机运行层**(automator 连真实例、reLaunch、setData、量渲染盒)+ 断言层
3. **同类扫尽**:自守②扫全仓 `test-*.mjs`,咬出 7 处(我 1 + 存量 6),现 0 残留;
   沙箱依赖现扫全仓 = 1 个套件,已收进唯一出口
4. **用什么数据验的**:两态造态(有图 `https://…` / 无图 `''`),读回 image 字段自证 + `.recommend-card`
   命中数自证;**外加造病态**(height:auto)证明刀会红
5. **重复稳定性**:抽检刀本批连跑 4 次(病态 1 次红 + 健康态 3 次绿),结果一致
6. **残留与不确定**:
   - 抽检只覆盖 `SPOTS[1]`(首页人气美甲卡)一处;另外 18 处由**静态刀**(②d)全覆盖,
     运行时只抽 1 处 —— 这是有意的分工,不是漏
   - 容差 2px:rpx 折算有小数,`97.5` 与实测 `97` 差 0.5,故留 2px。收得更紧会因机型误报

**自评 L1–L5**:L1 末端(真机量渲染盒,过)· L2 同类扫尽(过,且咬出 6 处存量)·
L3 无图不结案(D120 有您 02k/02m 定的数值标准,过)· L4 反例数据(**本批最强的一次**:
不只用反例数据,是直接造病)· L5 重复稳定(4 次一致,过)。

**巨型文件行数**:local-server.mjs 18,929 → **18,243**;admin.js 9,617 → **8,552**(均只降不升)。
**断言基线**:本批全量 ok 条数 **2,635**。
**全量回归**:✅ 69/69 套件通过。
**生产库未动 · 本机库未动**。**未推 main**(本地提交)。
