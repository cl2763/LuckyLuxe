# 小批 07e · 排班五色一次定完 · 渐变卡抬级 · OWNER_TOKEN 切测试

**开单:2026-09-13** · 上游:07d(`c8191f7` → `7120440`,四个,领先 main **287**)· 裁 #61–#65

> 🔴 **先说排期**:**D190(生产上网页顾客端登不进去)是上线阻断,比这一批里任何一件都急。**
> 这一单是「等 D190 的图定下来之前,顺手能收的」——**D190 的令一到,先做 D190。**

---

## 〇、先认我一笔,再说这一批

**裁 #58② 的病因是我判错的。** 卡住回归的是你 07c 装的 `ext-deps-check --strict`,
**不是「两台库名」那条** —— 那一行在我自己的日志里写着 **✅**,而真正的红就印在它上面十一行。
**我引用了一行 ✅ 的输出,说它是红的。** 已立 **J-55**(结论被后来的证据推翻,要回去改产出它的那份文件),
`小批07d` §一② 已加更正块。详见台账 09-13。

**你这一批自查出的那两跤,比结果值钱** —— 尤其第二跤:
**收敛完又分叉了,而分叉藏在参数顺序里**(`--bg` 把赋值放在 `env -u` 之前被抹掉)。
你立的 ④m2「验两支逐字同序」是对的落点。**这一件是这批最值钱的。**

---

## 一、裁 #61 —— 护理块取 `--good`:**准,而且你的理由比我的指示好**

我给的是「`--acc` 或 `--good`,取过 AA 的那个」。你两个都量了、都过,然后按**另外两条**选了 `--good`:
色相不配(青绿压浅绿),以及 —— **仓里没有 `--accbg` 与它成对**。

**第二条比对比度硬。** 没有成对的底色,就得临时挑一个底,**那又是一处写死** ——
等于为了接一个令牌,新添一处写死色。**你选对了,不推翻。**

> 顺手立个口径:**挑令牌时先看它有没有成对的底**。没有成对的,除非那一处的底本来就是令牌,否则不选它。

---

## 二、裁 #62 —— 排班另外四个状态色:**不许你自己猜,把料给我,我一次定完五个**

你量出来的是真问题:**五个状态色全是写死 hex 压在随档变的底上**,`working` 深档只有 **2.96**。
你只改了我准过的那一个 —— **守得对**。

但剩下四个**我不会隔空定**,因为它们是**状态**(不是品类),语义各不相同,而语义决定令牌(J-54)。

**你交这张表,我一次裁完:**

| 状态(代码里的名字) | 中文显示 | 现在的 hex | 压在什么底上 | 浅档比值 | 深档比值 |
|---|---|---|---|---|---|

**五行都要填满**,底色那一栏写清是令牌还是写死值。

**我的预判(交表后可能改)**:在岗 → `--good/--goodbg`(已做)· 休息 / 未排 → `--flat/--flatbg`(中性,
与沉睡 S 同一条理由:**不是错误,只是没动静**)· 请假 → `--tagw/--tagwbg`(提示)。
**调休**要看你们业务上它更接近「休息」还是「另一种在岗」—— 这一格你写一句你的理解。

---

## 三、裁 #63 —— 渐变卡三张:**从「欠账」抬成上线后第一批**

你这句话把它抬级了:

> 同一行四张卡,深色下**一屏里两种行为**。

`.tcard.a` 随档走、`.tcard.b/n/s` 两档逐字节相同 —— **四张并排,深色下三张不对劲**。
这不是「令牌化欠账」那种慢慢还的债,**这是店主和顾客一眼看得见的病**。

**裁**:仍然不在这一批改(它要动渐变,属改图范围,J-41),
但**从「小程序 wxss 令牌化欠账」里拎出来,排进上线后第一批**,与「站内消息记录页接真数据」同批。
到时候拿浅深两张并排图给店主看一次 —— **四张卡放一起,一眼就知道该怎么办。**

---

## 四、裁 #64 —— `OWNER_TOKEN` 停在「闸写好、测试没切」:**准,这正是我让你停的地方**

`secret-gate.mjs` 我读了,**是通用形状**(抬头明写「仓里会有不止一把需要 fail closed 的钥匙」,带 `clashLabel`)——
**形状真复用了,没有另写一套。**

**下一批把测试切过去**,按 07d §三 第二层:
ci/sandbox 每次启动**随机生成**、落到那一轮 `DATA_DIR` 下的文件、**测试接一个共用 helper 去拿**。
🔴 **切之前先把那个 helper 写好并让一套测试先用上**,证明形状可行,再批量切 ——
**不许九十个文件一次改完再跑**,那样红起来分不清是谁的。

---

## 五、裁 #65 —— 领先数也由脚本产出

你报「领先 main 286」,我现测 **287**。小事,但既然提交清单已经由脚本现出了,
**领先数就不该还是手写的** —— 一起由脚本出,和提交清单同一行打印。

---

## 六、顺序

1. 🔴 **等 D190 的令**(Cowork 正在出图)—— **它一到,先做它**,这一单往后排
2. §二 排班那张表(**只交表,不改码**,轻,可以立刻做)
3. §四 `OWNER_TOKEN`:先写 helper + 一套测试试点
4. §三 渐变卡:只登记抬级,不动码

**停线一条不松**:不推 main(领先 **287**)· 不碰生产 · 写 4128 既有行仍停 · **不看密钥文件** ·
不许 sleep 轮询 · 不许松判据 / 抬棘轮 / 换尺子 · 不许改合同图来凑代码。

**收尾照旧**:回执落盘后补跑 `test-untouched-proof` · 提交清单与领先数由脚本产出 · J-43 对账。

---

**这一单的由来**:你抓到我把病因判错了,而且证据在我自己的日志里 ——
**我读了一行 ✅,报了它的反面,还写进了单子。** 已立 J-55:
**结论被后来的证据推翻时,要回去改产出它的那份文件,不是只在心里改。**
