# 五线并行批 · 交付(2026-08-25)

**真库一行没动,生产没写。** 42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-25_五线并行批.txt`

## 棘轮(wc -l 现测,不凭记忆)

```
apps/api/local-server.mjs   18,784 → 18,741   (−43,目标 ≤18,771 ✅)
apps/web/admin.js            9,598 →  8,968   (−630)
```
新模块:`ai-desk.js` 672 / `demo-reset.mjs` 109 / `quote-serialize.mjs` 49 / `static-serve.mjs` 48。
四个都已 `git add`(㋙D 护栏当场报了这三个漏 add,见下)。

---

## 甲线|拆 admin.js 第一刀:AI/客服域 → `apps/web/ai-desk.js`

搬出 **22 个函数 / 630 行**,按店主要求分三段:**取数(5)/ 状态(4)/ 渲染(13)**。
`admin.html` 里 `ai-desk.js` 排在 `admin.js` **之前**加载(断言锁死顺序)。

**只搬不改行为 —— 证据比"逐像素"更硬**:同一路由、同一数据,把整屏渲染后的 HTML 存下来,
换回 HEAD 旧代码再渲染一次,**逐字符比对 103,858 个字符**:

```
旧长度 103,856 / 新长度 103,858
首个差异位置 4024:  v20260824g-d71  →  v20260825a-5line
```
**全篇唯一差异就是侧栏版本号**(那是本批有意 bump 的)。

### 🔴 我得先认一个错:`updateWechatMock 892 行` 是我量错的

你这条指令(「必须同时拆开,按取数/状态/渲染三段分」)是照着我 08-24 那份体检报告下的,
而那个数字是**假的**:我当时用 awk 量「到下一个 `function` 声明的距离」——
函数后面跟一大段非函数代码时,那段全被算进去了。

按 `}` 收在第 0 列重量:**`updateWechatMock` 实测 8 行**(写覆盖 → 存本地 → 重渲染,
本来就是取数/状态/渲染各一步)。同一份报告里 `readStoredAuth 781 行` 也是这么来的。
真实的超长函数榜是:`renderSalaryPlanEditor` 213 / `renderFinancePayroll` 162 / `applyLanguage` 149。

处置:①体检报告已就地更正并划掉旧数字;②量法固化成 `tools/fn-length.mjs`,以后体检一律用它;
③三段分**照做了**,只是落在**文件分区**上(取数/状态/渲染三段),而不是把一个 8 行函数硬拆成三个。
如果你要的是"连 8 行也拆",说一声,我改。

---

## 乙线|D74:平台后台「会员制度」面板

依据【设置唯一编辑处律】:规则谁定,编辑处就在谁那儿。
`platform.html` → 商家配置 → **新增第 6 个 tab「会员制度」**(结构/样式照抄现有 tab,没另起一套)。
出:会员资格(4 种模式,选项**来自后端下发的 qualifyModes**)、消费门槛、有效期、是否分等级、等级档位(增删改)。
**没新建后端路由** —— 用的就是既有那条 `GET/PUT /platform/tenants/:id/membership-config`。

**端到端实证**(不是各查各的):平台面板改成「累计消费满 888 / 有效期 365 天」并点保存 →
立刻查**商家端只读接口** `/admin/membership/config`:

```
怎么算会员 = 累计消费满 CAD $888 成为会员
会员有效期 = 365 天
分不分级   = 分级(4 级)
```
验完已改回原值(any_recharge / 0 / 长期 / 4 级)。非平台 token 打这条路由 **401**(实测)。

断言三条:①GET/PUT 各一处真实调用 ②选项来自后端、前端不维护第二份清单 ③后端仍是那一条路由。

---

## 丙线|主线

### 1. 批④ 关帐核验 —— **不给关,有一处残留**

| 件 | 状态 |
|---|---|
| S8 四块顺序(预约信息 → 签署单 → 服务留档 → 售后) | ✅ 双端都已是这个顺序 |
| S7 定金规则摘要 7 项 + 去哪改引导 | ✅ 商家小程序逐行显示,全部读后端 summaryRows |
| S7 `storeDepositAmount()` 拿不到返 0 | ✅ 已改 null(0 在界面上等于"免定金") |
| S9 商家端只读展示 + 人话说明 | ✅ 在场 |
| **S8 「去售后」入口位置** | 🔴 **双端分叉,D70 带出来的** |

残留说清楚:D70 那刀把小程序的「去售后」按钮移进了**底部动作条**(由状态机 `customerActions` 渲染),
而网页顾客端的「去售后」还在**第四块里**。四块顺序双端一致了,但那颗按钮**一个在块里、一个在条上**。
S8 的图说的是"第四块=去售后",所以严格讲两端都还没完全对上图。

**建议**(等你裁,我没自作主张):要么网页也做底部动作条(与小程序同构、也接 `customerActions`),
要么小程序把按钮放回第四块。前者更彻底(顺带把网页顾客端也纳入状态机动作层),后者改动小。
**在此之前批④ 不关帐。**

### 2. 微信业务域名校验文件 —— 通道已做好,只等文件

三件一起做的(缺一件都会让人查半天):
- 根路径 `.txt` 放行(以前根下的 .txt 掉进 API 404);
- `.txt` → `text/plain; charset=utf-8`(以前不在 mime 表里,会当二进制下载);
- **文件不存在时真 404**,不回落 `index.html`(回落会返回一段 HTML,微信比对内容失败,
  但 HTTP 200 会让人以为"路由通了")。

实测:放一个测试文件进去 → `200 text/plain` 且原文返回;删掉 → `404`。
**你拿到文件后:直接把公众平台下载的 `MP_verify_xxx.txt` 放进 `apps/web/` 根下,推一次即可,代码不用再动。**

### 3. 剩下两件 —— 你在公众平台照着点

**A. 小程序「服务器域名」(request / downloadFile 合法域名)**
1. 打开 mp.weixin.qq.com,用**小程序**账号登录(不是公众号那个)
2. 左侧 → 「开发管理」
3. 顶部 → 「开发设置」
4. 往下找到「服务器域名」→ 点右边「修改」
5. `request 合法域名` 填:`https://www.luckyluxeatelier.com`
6. `downloadFile 合法域名` 填:同一个 `https://www.luckyluxeatelier.com`
7. 点「保存并提交」(每月只有 5 次修改机会,一次填全)
8. 回到微信开发者工具 → 「详情 → 本地设置」→ **取消勾选**「不校验合法域名」,再跑一遍确认不报错

**B. 剪贴板权限写进隐私政策**
1. 同一个后台 → 左侧「设置」→ 顶部「基本设置」
2. 找到「用户隐私保护指引」→ 点「更新」
3. 在「收集的用户信息」里勾上 **「剪切板」**(小程序里"复制会员码/复制文案"用到)
4. 用途一栏填:**「用于复制会员码、优惠信息与文案,便于用户粘贴分享」**
5. 提交(审核一般 1–2 个工作日)
6. 审核通过前,小程序里那几个复制按钮在真机上可能弹权限提示;通过后自然消失

---

## 丁线|还棘轮 + 演示店重置口

**第一步·搬**:`serializeQuoteRequest` + `getQuoteRequestById` → `apps/api/quote-serialize.mjs`(49 行,行为一字未改)。
顺手把**静态文件服务**(`contentType` / `serveFile`)也搬出去 → `apps/api/static-serve.mjs`(48 行)——
因为丙线动的就是那一域(公约②),而且第一次搬完只到 18,778、离 18,771 还差 7 行,
按你「搬得不够就多搬,不许挂账」多搬了这一域。**最终 18,741,比目标还低 30 行。**

**第二步·加**:`apps/api/demo-reset.mjs`(109 行),`local-server` 只留 import + 一段分发。
六条逐条落地,**七条会红的断言全绿**:

| 店主六条 | 断言 | 结果 |
|---|---|---|
| ①唯一入口 + isPlatform 闸门 | 非平台 token 打 = 401 | ✅ |
| ②kind='demo' 才放行,非 demo **403** | 拿 `lucky-luxe` 当探针 | ✅ 403 `NOT_DEMO_TENANT` |
| ③手打完整店名 | 名字不对 = 400 `CONFIRM_NAME_MISMATCH` | ✅ |
| ④先备份,备份不成中止 | 回报里带 `backupPath` | ✅ |
| ⑤ops-log 一行含快照/原因/备份路径 | 真跑后查库 | ✅ 一行,三项都在 |
| ⑥demo 店真跑一次 | 账本 1 行 → 0 行 | ✅ |
| (附)原因必填 | 空原因 = 400 `REASON_REQUIRED` | ✅ |

重置只清**经营数据**(账本/单/结算/券/日结/薪资…共 30 张表),不动店本身、账号、门店资料、服务价目 —— 与重播种脚本对齐。

---

## 自己抓到的坑(四条)

1. **`updateWechatMock 892 行` 是我量错的**(见甲线),你的指令是照着我的错数字下的。已更正 + 量法固化成工具。
2. **㋙D 护栏第二次实战立功**:`quote-serialize.mjs` / `demo-reset.mjs` / `ai-desk.js` 三个新模块我都忘了 `git add`,
   断言直接点名报出来。上批立的护栏,这批又兜住一次。
3. **第一次搬完没够线**(18,778 > 18,771),按你的规矩多搬一域,没挂账。
4. **判据又差点扫全文**:㋝甲① 一开始想写「admin.js 里不含 wechatMockSessions」——
   那会被注释骗。按新细则改成**取函数声明行**(`^function xxx\b` 多行匹配)。丙① 同样在搬完 contentType 后跟着改指到新文件。

## 待你裁

- **S8「去售后」入口位置的双端分叉**(见丙线 1):网页也做动作条,还是小程序放回块里?**批④ 暂不关帐。**
- **甲线三段分的粒度**:8 行的 `updateWechatMock` 我按文件分区归到三段里,没硬拆成三个函数。要硬拆说一声。
