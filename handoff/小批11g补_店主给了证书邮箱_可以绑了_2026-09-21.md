# 小批 11g-补 · 店主给了证书提醒邮箱 · 可以绑了
2026-09-21 · Cowork 出令

**上游令点名(J-81):** `小批11g…` md5 `05c1095f21149656b39d3ccf68ccbca1`。本补件只答 §五 那一件。

---

## 〇 店主拍板:**用她自己那个 Gmail**

```
nini3131254931@gmail.com
```

**这是她亲口给的,不是我猜的(令里写死「邮箱由店主给,不许猜」——现在她给了)。**

---

## 一 去做

在那台北京机上:
```
sudo certbot update_account -m nini3131254931@gmail.com --agree-tos
```

**做完验三件:**
1. `sudo certbot show_account`(或等价命令)能看到这个邮箱 —— **看到了才算绑上**
2. 🔴 **官网仍是 200** —— 改账户不该影响站点,但**照规矩量一次**
3. `sudo certbot certificates` 三张证书(官网 / www / api)**状态与到期日不变**

---

## 二 口径(写进台账)

- **这个邮箱只用于这台服务器的 Let's Encrypt 账户,只用于证书到期提醒。**
- 🔴 **不许把它写进任何别的配置、不许用它注册任何别的服务、不许出现在代码里。**
- **它不是密钥,可以出现在回执和台账里;但仅限这一件事的记录。**

---

## 三 顺带:告警那条不因为绑了邮箱就松

**邮箱是「万一自动续期坏了,有人被通知」;你做的那条告警是「我们自己也在看」。两条都要,不许删其中一条。**
**尤其那一条「取不到证书也必须报」—— 它治的是 J-86,留着。**

---

## 四 回执

一句话:邮箱绑上没有、官网还是不是 200、三张证书到期日有没有变。
