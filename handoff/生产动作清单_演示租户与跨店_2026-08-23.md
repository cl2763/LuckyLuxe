# 生产动作清单 · 演示租户与跨店串号(报 Cowork 批,**未执行**)

> 边界令①:生产写入前先报清单。以下每一条**都还没做**,等你/Cowork 批了我再执行。
> 沙箱(4310)已全部做完并验证,证据见本批回报与 `handoff/核验截图_2026-08-23/`。

## A. 必须先批的**结构性迁移**(涉及生产库表结构)

| # | 动作 | 为什么必须做 | 风险与兜底 |
|---|---|---|---|
| A1 | `users` 表重建:去掉 `wechat_open_id` / `google_id` 的**全局** UNIQUE,改为 `(tenant_id, wechat_open_id)` / `(tenant_id, google_id)` 复合唯一 | **会员=用户×店的地基坏了**:同一个微信在第二家店根本插不进第二行,拿到的是第一家店那一行 —— B 店老板在客户档案里看不到她、给她开单会被「卡主不在本店档案里」挡下,而「我的」页读的又是 B 店口径,半串半不串 | SQLite 不能改列约束,只能重建表(建新表→复制→改名)。**幂等**:只有检测到旧约束才重建,重跑一分不动。执行前**先备份库**(生产已有每日自动备份,我会再手动打一份) |
| A2 | `user_identities` 同上:`UNIQUE(provider, provider_user_id)` → `UNIQUE(tenant_id, provider, provider_user_id)` | 同 A1(身份表不带租户,同一 openid 只能挂一个 user) | 同 A1 |
| A3 | 建租户维度索引:`idx_users_tenant_openid` / `idx_users_tenant_googleid` / `idx_identities_tenant_provider` / `idx_users_tenant` | 保证新唯一性 + 按店查顾客的性能 | 纯加索引,可回滚 |

**生产影响面自查**:现有生产库里**没有**同一 openid 跨租户多行(全局唯一约束不允许),所以重建不会撞唯一冲突;重建只搬数据不改值,金额/账本零触碰。代码侧登录路径已同步改为按店查/按店建(见本批提交)。

## B. 演示租户(**新建租户 + 新建账号**,属敏感动作)

| # | 动作 | 说明 |
|---|---|---|
| B1 | 生产执行改名:`jics-sandbox` → 显示名「小婕的店(演示)」 | **你已拍板**。走 `tools/ops-rename-tenant-display.mjs`,只改 `tenants.name` / `stores.name`,业务数据零触碰。⚠️ 需要你确认:生产上是否存在 `jics-sandbox`?我只在沙箱见过它;生产 `/shops` 目前只有 `jics-nail`(小婕真店)与 `lucky-luxe` |
| B2 | 新建演示孪生租户 `demo-lucky-luxe`「Lucky Luxe(演示)」 | 会连带创建**门店**与**老板账号**(平台接口自动生成初始密码)。演示店的老板账号是否需要交付给谁?不需要的话我建完即记录、不外发 |
| B3 | 新建演示孪生租户 `demo-jics`「小婕的店(演示)」 | **仅当生产没有 jics-sandbox 时才需要**——若生产已有,B1 改名即可,不必新建 |
| B4 | 给两个演示店铺演示目录与顾客档案(服务/技师/套餐/券/顾客/储值/次卡/券/已签署单) | 走 `tools/seed-demo-twin.mjs`。⚠️ 该脚本**当前只允许连本机**(硬拦 127.0.0.1),要在生产跑必须我先加一道「显式生产开关 + 只写演示租户白名单」的二次确认,不会默默放开 |
| B5 | 演示店对普通顾客的可见性 | 现状:选店页默认隐藏 `demo-` 前缀的店,店主连点标题 5 次开「演示模式」才看得到。`jics-sandbox` 不带 `demo-` 前缀 → 顾客看得到。**建议**:给 `tenants` 加 `is_demo` 列,可见性与 id 前缀解耦(改 id 要迁移全库,不划算)。这条要你拍 |

## C. 真店零触碰(边界令①的延伸,已按你说的执行)

- 演示顾客只进演示租户;**`lucky-luxe` 与 `jics-nail` 一行不写**。
- 本批沙箱铺设脚本内置真店黑名单:写这三个 id(`lucky-luxe` / `jics-nail` / `jics-store`)直接抛错拒绝。
- 若你日后要在真店放「演示-」前缀档案,另行拍板再做。

## D. 我建议的执行顺序(批了之后)

1. 手动备份生产库 → 2. A1–A3 迁移(随一次正常部署自动跑,启动日志会打印重建结果)→ 3. 验证 `/health` 与顾客端登录 →
4. B1 改名 → 5. B2/B3 建演示店 → 6. B4 铺演示档案(需先加生产开关)→ 7. 一次性双端走查。

## E. 待你回答的三个问题

1. 生产上有没有 `jics-sandbox` 这个租户?(决定 B1 改名 vs B3 新建)
2. 演示店的老板账号要不要交付给人?(不要的话我建完只记录不外发)
3. B5 的 `is_demo` 列要不要做?(做了才能让不带 `demo-` 前缀的演示店也对顾客隐藏)
