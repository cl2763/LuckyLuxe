# 本批交付清单 · S2批②(开单/核销/随单充值)+首日四小件 · 2026-08-20

> 合同:S2 图 v1.2《S2充值次卡券三线_UI设计图_2026-08-17.html》第四节(开单与核销)+ 队首指令(2026-08-18)。
> 纪律:一项一行;图上没画到的按最合理做并记假设;逐屏并排核验;「代码写了」≠「做完了」。

## A 组 · 首日四小件+存疑三处(随批,先行)

### A1 currency-scan 盲区补课(Cowork 护栏补课令)
- ☑ A1-1 盲区定性:现行「文案中 $数字」规则 scope='mini' 只扫小程序——apps/web 的 .html 静态文案(admin.html:680 那类)整层不在扫描面
- ☑ A1-2 新规则:apps/web/*.html 静态文案 `$数字` 一律红(scope=webhtml;注释行既有豁免机制保留;replace/RegExp 反向引用豁免保留)
- ☑ A1-3 反证:把「$1消费=1积分」临时塞回 admin.html 跑扫描必须红(实测红:admin.html:684 被咬住);还原后全绿
- ☑ A1-4 存量清零:apps/web 全部 .html 现存命中=0(sign.html:126 为注释行,豁免机制覆盖)

### A2 「会员与营销」四页签改五页签(店主拍板 08-18,图 v1.2)
- ☑ A2-1 页签栏:① 充值套餐 ② 次卡 ③ 优惠券 ④ 积分商城 ⑤ 会员体系(S9)(L1 实拍)
- ☑ A2-2 积分商城卡整体移入第④签,ID 与事件零改动(L1 实点④签可见)
- ☑ A2-3 优惠券页签剩券模板+自定义发放;③签 DOM 不再含积分商城(couponContainsMall=false)
- ☑ A2-4 五签逐个实点:互斥全过(L5 五轮 tab-mall/coupon/times/tiers/recharge 全 ok);默认落①
- ☑ A2-5 CI:无既有「四页签」断言需升级;新增 ㊷ 五页签断言(mall 页签+容器+切换逻辑)

### A3 D51 网页订单管理接入售后状态(四之九双端同病;修完=店主借它实点售后弹层=D50-b 终验)
- ☑ A3-1 后端:/admin/bookings 随单下发 listBadgeText/listBadgeKind(复用 customerOrderBadges(),后端唯一持有)
- ☑ A3-2 筛选加「售后中」「售后完成」(L1 实测:售后中筛出 2 张开着的;售后完成恰 1 张已解决,resolved 不混入售后中)
- ☑ A3-3 「需关注」含售后中(L1 实测两张售后中入需关注列表)
- ☑ A3-4 状态 chip 修正:AFTER_SALES 不再显「需关注」,取后端细分文案(L1:售后中蓝 chip/售后已解决)
- ☑ A3-5 列表卡徽标三态(L1:已完成筛出 19 徽标=已签署×17 绿+已更正×2 暖橙;配色同小程序)
- ☑ A3-6 血缘同查(全表核过;另抓一处顺手修:售后中卡需关注句原为通用文案,改「售后处理完成前需要关注。」自证来源)(网页渲染订单状态的全部位面,机械扫 statusLabel( 调用点):
  | 位面 | 行号(改前) | 处置 |
  |---|---|---|
  | statusLabel() 本体 | admin.js:951 | AFTER_SALES 映射修正 |
  | 订单列表卡 renderBookingCard | admin.js:5085 | chip 修正+徽标+需关注含售后 |
  | 订单详情 renderBookingDetail | admin.js:5125 | chip 修正 |
  | 台面 today 视图 | 同 filteredBookings | 随筛选逻辑一致 |
  | 日历格 renderCalendarCell | admin.js:5215 | 匹配逻辑统一+AFTER_SALES 样式补 |
  | Dashboard mini 卡 renderBookingMini | admin.js:4919 | chip 修正 |
  | 员工端我的今天 renderStaffTodayTimeline | admin.js:5073 | chip 修正 |
  | 客户档案消费记录 renderCustomerRecord | admin.js:5967 | chip 修正+徽标 |
  | 状态筛选器 options | admin.js:1098 | 两项新增 |
  | 日结页 renderDailyClose | admin.js:2477 | 查明:结算单维度,无订单状态渲染位面,零改动(如实报) |
- ☑ A3-7 CI 行为断言 ㊷:售后开→徽标售后中/aftersales;解决→售后已解决;待支付单徽标空串(186 断言全绿)
- ☑ A3-8 L1:本地实点筛售后中出单、蓝徽标在场;L4 反例=jics 三态同库(待处理/处理中/已解决)+已签/已更正同屏

### A4 「上架商城」勾选框与文字排同一行
- ☑ A4-1 根因:.kb-facts-grid label{flex-direction:column} 盖过弹层内联 display:flex(缺 row)
- ☑ A4-2 修:.fm-check 类+站内样式(实测 accent=rgb(185,141,95)/flex row/同行 sameLine=true)
- ☑ A4-3 同类扫尽:组件级修,openFormModal 全部 checkbox 字段同源生效

### A5 存疑三处查明(admin.js 9171/9173/9188,原编号 9121/9123/9138)
- ☑ A5-1 定性:门店设置页「充值档位」块(msAddTier/tier-toggle/tier-delete),走 recharge_tiers 旧表
- ☑ A5-2 活性判定:**非死代码**——活跃读方=商家小程序结算面板代充选档(merchant/settlement/index.js:445)
- ☑ A5-3 分叉登记:recharge_tiers(门店设置+小程序结算面板)vs membership_packages(批① 充值套餐+网页收编充值)两模型并存
- ☑ A5-4 处置定案:随批② ⑤ 收敛选档源到 membership_packages 后删门店设置块(基线 27→24);首日不删避免「界面删了旧表还被读」中间态
- ☑ A5-5 兑现(店主终段令义务①**迁移贴数**):本地库 recharge_tiers=**0 行**、生产库=**0 行**(railway ssh 只读直查,两店旧档位从未配置)——**无数据可迁、零静默丢**;充值套餐现值:本地 lucky×2(充1000送50/充2000送300)+jics×1(充1000送50),生产 lucky×1(500 赠10)、jics 0 个(jics 要用随单充值选档需先在套餐页建,手输金额随时可用)
- ☑ A5-6 兑现(义务②**零读方 CI 断言**):网页两端+签署页+小程序全目录机械扫 `/admin/recharge-tiers` 调用=0(类定义按机制:读方=打接口的调用,退役注释不算),防复活常驻;门店设置充值档位块已删(msAddTier/tier-toggle/tier-delete 三处原生弹窗随删,㊶ 基线 27→24)+指路句「充值套餐在会员与营销 → 套餐维护」

## B 组 · 批② 主体(图=合同第四节;三端共用同一后端核销引擎)

### 屏1 开单·选服务(新增「次卡」大类)
- ☑ B1-1 次卡大类入口(L1 已拍:b31-settle-01b 亮起+角标1)
- ☑ B1-2 剩 0 次的卡不出现(接口层单源过滤,㊺ 断言)
- ☑ B1-3 过期卡置灰+「已过期」标+点击拦(L1 实点:点了不选中,timecardId 保持空;b31-settle-01b)
- ☑ B1-4 卡片行文案后端唯一持有(label 直渲,零前端拼)
- ☑ B1-5 选卡→组内 chips→preview 金额面(L1:折算 180/纯核销到店 0;b31-settle-01b/05)
- ☑ B1-6 「+现场购卡」入口常驻次卡大类(选套餐 label 后端句→组内选项目→分行由 preview 呈现;三态互斥 主项目/已有卡/购卡;员工可读套餐口=⑭先例;B1/B1-6 wiring 断言;L1 随 B5)
- ☑ B1-7 修正后同构:开单只在小程序结算页(商家/员工同页角色差异)=引擎唯一入口;网页开单缺席已裁(a)案归 S12+批④前瞻标记

### 屏2 次卡核销单(三件套)
- ☑ B2-1 后端:核销行留痕「第 n/N 次」(㊻ 断言;前端选卡行随三端入口)——原文:选卡行:「本次核销第 n/N 次」
- ☑ B2-2 金额=折算单价(末次吃余数)+times_card 腿,计入 subtotal=积分/业绩自然按折算(㊻)——原文:金额栏=「次卡抵扣」(不收现金);折算单价(卡价/总次数)计积分/业绩
- ☑ B2-3 组内选项目:project_group 按二级分类名精确匹配,空组=不限(假设⑤);出组=400(㊻)
- ⬜ B2-4 附加服务:复用自定义价目条,照常收钱、照常计积分/业绩
- ◐ B2-5 后端:核销单必有技师闸(㊻);数字点选 UI 随三端入口
- ☑ B2-6 签字时刻乐观锁扣次(WHERE used_times=nth-1)+条目留痕(㊻)
- ☑ B2-7 四闸全断言:组内一卡一次 400/剩0建单 400/过期建单+签署双拦/必有技师 400(㊻+㊺)
- ☑ B2-8 售后返还+业绩扣回(a 案三裁全兑现):releaseTimecard 更正=次数−1+积分回冲(台账负 adjust 行)+收入红字(reversalOf 链);防重放 409/非核销单 400(㊽×7)。**业绩扣回已落**:perfReleaseRowsOn 唯一实现(归属日=返还日案X/双技师真实比例摊/未分配暂缓=假设⑥);五族继承(日结净额+快照线→排行/工资/我的业绩/目标零改动);显式行三位面渲染(网页日结/小程序日结/小程序我的业绩)+R1 业绩对账位+人话句;㊾×17+㊾补×6 断言;L1 三屏实拍(网页日结 boss/小程序日结 boss/小程序我的业绩**员工小美身份**)——净额 200=180+200−180 逐屏自证;《财务总逻辑》v1.7 §十-7 三句已写(末次吃余数/售后业绩扣回/跨月扣当月)
- ☑ B2-9 次卡覆盖不进现金/储值腿;确认收入单列「服务收入-次卡核销」payChannel=times_card(㊻)

### 屏3 随单充值/现场购卡·签署页
- ☑ B3-1 随单充值分行(挂单随签,㋀×21):面板只挂草稿,**签字那一刻**充值行才入账(未签退出=什么都没发生,㋀ 撤回断言);恒等式再扩 total=subtotal−定金−券+购卡+**充值实收**;充值实收摘出储值可抵(充值不能拿储值付)、充+赠计入本单可抵范围(储值烧到「充进来的钱」,签署链按充后口径校验);三行分行「本次充值实收/充值赠送/本单储值抵扣/充后余额」=serializeSettlement 后端句唯一,四渲染面(商家结算页合计区/签署页 sign.html=web-view 双端同构/快照 SVG/单据预览)只贴现成行;**充后余额签字瞬间冻结**进 recharge_json(快照不随余额漂);赠额一律后端按套餐算(防前端造赠);经手技师=技师会话强制本人/老板可空(不猜归属);财务红线:充值不写收入行+不进 perfBase+日结冲卡列 Δ=+30000 断言(bonus 天然排除)
- ☑ B3-2 现场购卡全链(引擎㊿×10+入口 UI B1-6+日结单列裁①):恒等式扩项 total=subtotal−定金−券+购卡/分行腿(times_card 折算+现金购卡实收)/签字建卡 used=1 快照齐溯源单号/核销确认收入 18000/购卡=预收负债不写收入行(充值同构)/新卡回流持卡接口剩 2/3;结算页入口 UI 随 B1-6 落
- ☑ B3-3 单独代充(不开单)即时到账+顾客端回执卡(金额/渠道/时间戳;余额=页顶大数)带「确认到账」钮(㋁×15;「推送」以顾客端储值页站内回执卡呈现=假设⑦,真微信订阅消息随批⑤支付基建)
- ☑ B3-4 未确认不阻塞(到账/消费/结算全不受影响);商家流水标「顾客未确认」(网页+商家小程序同源字段 customerConfirmed);确认口幂等+越权 401/悬空 404/赠送行 400;**随单充值=顾客亲签,入账即确认**(不进回执);台账 append-only 触发器开唯一豁免=确认列 NULL→值单向一次(账目列逐列相等强制,改账目照旧 ABORT+二改时间戳 ABORT 两条复发护栏断言常驻)
- ☑ B3-5 随单充值选档源=membership_packages 充值套餐(新员工可读口 /admin/recharge-packages,⑭ 先例 label 后端句「充X赠Y」);面板+手输双路;旧 recharge_tiers 收敛(接 A5-4,见 A5-5/6)

### 规则与口径(全断言化)
- ☑ B4-1 三口径断言⑦全兑现:售卡不计积分/业绩(perfBase=18000 断言+总额面设计零特判)/核销折算确认收入计积分业绩(㊻㊿)/日结次卡单列双行(裁①×2:售卡+54000 预收/核销 n 次折算)
- ☑ B4-2 财务红线:购卡不进 perf 链(perfBase=subtotal 不含购卡=结构保证+㊿ 断言);业绩四位面读快照线=同一来源零旁路
- ☑ B4-3 引擎唯一⑧:机械扫=前端全仓 POST /admin/settlements 调用 1 处(商家小程序结算页,员工/老板同页)+后端 createSettlementGroup/computeSettlement 唯一实现;网页端零调用(网页开单=批④ 前瞻)
- ☑ B4-4 并发形断言:剩1两张待签单,后签=409 TIMECARD_RACE 不超扣;幂等分立断言(重签 ALREADY_SIGNED 零副作用)——Cowork 预嘱②兑现(㊻)

### 收工(指令⑨)
- ☑ B5-1 走查完成(9 张 L1 实拍+签署页 Browser 并排+L5 5/5×2+生产 commit 1a2fe46 验证+两店只读贴数);墙内屏生产亲验=Cowork 位面(收工总报第六节清单)
- ☑ B5-2 兑现:42 套件 EXIT=0(尾句原文「✅ 全部 42 个套件通过」)+312 断言(㋀×21/㋁×15/wiring×7)+组合矩阵(引擎位+页面位)+总六项回报——全文见 handoff/收工总报_S2批②_2026-08-21.md

## 假设清单(随施工补)
1. A3 徽标只挂「订单列表卡+客户档案消费记录」两处列表位面;Dashboard mini/员工今天时间线为紧凑视图,只修状态 chip 文案不加第二徽标(图上未画,按最合理做)。
2. A3 「售后完成」筛选=resolved+closed 合并一项(图未画筛选器细分;顾客端徽标区分已解决/已关闭,列表卡徽标文案照顾客端细分)。
3. A5 删除动作押后到 ⑤ 同批:避免「配置界面已删、旧表仍被小程序读」的中间态。
7. B3-3「推送回执」以顾客端储值页**站内回执卡**呈现(置顶待确认卡+流水待确认标):现阶段无微信订阅消息基建,真推送随批⑤微信支付一并接;图上未画推送形态,按最合理做,记册待裁。
8. B3-1 随单充值渠道:面板未设渠道选择,签字入账渠道默认 cash(到店收的钱与线下腿同性质);老板端单独代充仍可传渠道。图未画渠道位,记册待裁。
9. B3-1 赠送金额烧掉时计入「服务收入-耗卡」(负债兑现=收入,现金差额即营销让利内含)——沿批① 规则④ 既有口径零特判,未新开口径。

## corner case 覆盖计划(六类+财务红线)
边界值(剩1次核销/最后一次/折算末次吃余数)/空态(无卡顾客次卡大类不亮)/并发(双单抢同一张剩1次的卡)/越权(员工代充回执确认口/跨店卡)/幂等(核销签署重放)/异常输入(过期卡强开 400/悬空卡 id)+大额购卡不进业绩四位面。

## C 组 · 店主体验版走查三发现(队首 08-20)
- ☑ C1 质询①答:「有疑问,去售后」**从未实现**——顾客端(小程序+网页)全仓 0 命中,顾客侧发起售后的后端路由不存在;顾客端售后 UI 全为只读(徽标三态+D3 进度卡)。今天唯一真实发起入口=**商家小程序 → 订单 → 点一张「已完成」的单 → 操作面板「转售后」**(merchant/orders/index.js:159);前置条件=单状态已完成,**不要求已签结算单**(后端 PATCH 无结算单校验)。网页管理端无「转售后」钮=商家侧发起入口网页缺席(与顾客网页售后线同入 S12 审计)。本地同 build 实拍:核验截图_2026-08-20/zx1-merchant-orders-zhuanshouhou.png(面板三项可见)。复发登记:操作卡第③步照抄队列文本未核入口在码上是否存在(与「先核生产对象在场」同族,发生在 UI 层);护栏=操作卡里每个点击对象发卡前 grep 实证在场。
- ☑ C2 D52 订单排序:双端同查结论=**商家小程序本来就是日期组倒序**(index.js:128)+组内时间升序——病只在网页端(升序)。修=renderBookings 日期组倒序对齐小程序;L1 实测网页 11 组严格降序(首组=今天 2026-08-20)+小程序端 data 取证降序 true;CI ㊸×2(网页倒序在场+小程序倒序锚,防任一端回退)。台面today/日历=单日/日历面,无此位面。
- ☑ C3 S1 三件(服务Tab/起价/护理修复)店主体验版预验完毕——《小程序发版清单》已记录,正式发版时机店主定。

## C 组补 · 批② 收尾随批两小件(售后线图 v1.1 §四+拍板③,队首 08-20)
- ☑ C4-1 网页管理端「转售后」钮:订单卡操作区,仅「已完成且已签署结算单」显示(判据=listBadgeKind∈{signed,amended},后端徽标唯一持有)
- ☑ C4-2 点击=openFormModal 弹层:问题描述(必填)+确认;空描述不放行;禁原生弹窗
- ☑ C4-3 转后即刷:卡片挂蓝「售后中」徽标,按钮变「处理售后」(现有弹层)
- ☑ C4-4 后端:PATCH status=AFTER_SALES 落 booking_status_history(note=问题描述)——发起原因进 afterSalesProgress 同一读链(后端唯一持有)
- ☑ C5-1 商家小程序「转售后」加前置:已签署结算单才显示(sheets 含 signed/amended;拍板③双端同刀)
- ☑ C5-2 老数据红线:已存在的无签署单售后原样保留只读可处理(只收紧新发起,不回溯)
- ☑ C6 CI ㊹:双端前置在场+PATCH 转售后 note 落史;L1 双端正反例实点(有签署=现钮/无签署=无钮)

## B 组预嘱两条(Cowork 08-20,随 B② 施工)
- ☑ B6-1 出路断言:被拦单撤回→改支付构成重开→签成(㊻ 实测)——原文:拦下之后要有出路:剩次不足/并发拦截的待签单,提示后必须能改支付腿再签(或撤回重开),不许死胡同废单;收工附实测
- ☑ B6-2 幂等两道闸:路由 ALREADY_SIGNED+事务内串行化复读;与乐观锁分立断言(㊻)——原文:签署接口幂等:双击/网络重试只扣一次——幂等(同单重签不重扣)与乐观锁(两单抢末次)是两道闸,corner case 分开断言

## 假设清单补
4. payIntent 只认 sheet 级(createSettlementGroup 只展开 sheet,body 级被忽略)——既有体位,批② 三端前端一律 sheet 级传;记档不改(改=动既有调用面)。
5. project_group 组内匹配=服务二级分类名精确相等;空组=不限。【已裁:表单改下拉禁自由文本,㊼ 落地】
6. 双技师核销单**未分配即返还**(出现场景:双技师单签署后、店长尚未在日结页分配分成,当天就发生售后返还——极小窗口):扣回**暂缓**,不用默认比例猜;分配落定后扣回行按真实比例自动出现(perfReleaseRowsOn 读时现算);日结确认闸(UNALLOCATED blocker)保证冻结快照永远是分配后的正确版。断言已盖(㊾补×6:暂缓不出行→重开→分配→70/30=12600+5400 末位吃余数)。

## D53(B② 施工中扫出,已修+回归形断言)
签署路径「服务收入-耗卡」入账漏传 tenantId——签署页=公开路由,不传=记到旗舰店账(跨租户记账)。潜伏未炸:生产 0 签署单+套件未覆盖此位面。修=显式带租户(次卡核销入账同刀);㊻ 回归形断言=非旗舰租户签储值单,耗卡收入必落本租户。类=「签署路径写账不带租户」,机械扫签署链 3 写账点:定金(原本就带+警示注释)/耗卡(修)/次卡(新写即带)。

## B1 施工查明两条(2026-08-21,动刀前)
- **查明①(报 Cowork):网页端没有开单 UI**——admin.js 零调 POST /admin/settlements,开单/结算从来只在小程序结算页(商家/员工同页角色差异)。图 §四写「三端」,网页开单整面缺席=界面没图不自由发挥;两案请裁:(a) B1 落小程序双角色端(真实唯一开单处),网页开单缺席归 S12 网页对齐审计出图后补;(b) 本批补建网页开单=新大面(需出图)。倾向 (a)。
- **查明②(注入方案定型)**:结算页=组级机器(金额红线:前端零运算,preview/正式同一 computeSettlement)。次卡大类注入=伪类 __timecard:卡主选定后拉 /admin/customers/:uid/timecards;有可用卡才亮+角标=redeemable 数;组切次卡类=卡列表(label 后端句/过期灰)→选卡→组内项目 chips(project_group→同名二级分类 items,空组=全部主项目)→技师数字点选(既有)→附加照常;sheet 带 timecardId/timecardServiceId(payIntent sheet 级);金额面全由 preview 回显(times_card 腿)。mainId 必填校验对次卡组豁免。B1-6「+现场购卡」入口与 B3 购卡通道同批落(不放半成品按钮)。
