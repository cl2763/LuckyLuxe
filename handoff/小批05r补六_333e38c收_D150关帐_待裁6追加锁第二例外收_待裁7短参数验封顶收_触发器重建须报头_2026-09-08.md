# 小批05r 补六(夜班 Cowork 代审 #3,05:58 CST)— `333e38c` **收,D150 关帐**、合并窗参数 + 看门狗关帐 · 待裁 #6 裁:追加锁第二例外**收** · 待裁 #7 裁:短参数验封顶**收** · 一句提醒:触发器重建属开机迁移,回执头要写 · 继续 7b → 7d → 段 8

> 2026-09-08 夜 · Cowork 现查:`handleWecomInbound` 已是薄壳(`guardedHandle(…, handleWecomInboundCore, …)`,`:4031`),Core 14 个 return 未动;`repeat-guard.mjs` 164 行 + `test-repeat-guard` 135 行;`conv_msg_no_update` 触发器第二形状现读(`conversation-log.mjs:38`,沙箱库 `sqlite_master` 已是新版);`merge-window.mjs` `DEFAULT_S=4 / CAP_S=12 / MIN 2 / MAX 10`;`run-all-tests.sh` 看门狗 300 秒;105 套 3,499;`local-server.mjs` 17,711 · 两处各一份

## 一、收

- **D150 关帐**:壳在出口、Core 不动、第二句换答法、第三句转人工、报价早 return 那条现在真被守住——收。
- 合并窗 4 / 12 / 2–10 三个数 + `/health` 三个数 + 看门狗——收。
- 小程序「正在看你的消息…」(`ai-chat/index.wxml`)——收。

## 二、待裁 #6 · 裁:**收,而且这才是对的形状**

追加锁的第二个例外不是「放行 UPDATE」,是「**只放行能自证的改写**」:`role='assistant'`、未脱敏、`meta.rewrittenFrom = OLD.content`、`meta.rewrittenBy='repeat-guard'`、其余列一字不动——触发器自己对账,改得掉「顾客现在看到哪句」,改不掉「当时说过哪句」。收。补一条判据(若还没有):`rewrittenFrom` 与 `OLD.content` 差一个字 → 触发器 ABORT(造病验红,一次即可)。

## 三、待裁 #7 · 裁:**收**

封顶是「窗被一直刷新时的上限」这个机制;机制拿短参数(窗 1 秒 / 封顶 3 秒)验,数字由静态判据守 `DEFAULT_S=4` / `CAP_S=12`——两层合起来才完整,收。回执里把这句话原样写给店主,免得她看到「3 秒」以为封顶没按 12 秒做。附加一条:短参数**只许**经测试实例的环境变量传入,生产/沙箱启动路径不许出现 `MERGE_WINDOW_MS`(静态判据一条)。

## 四、一句提醒(不扣分,登口径)

`conv_msg_no_update` 触发器是**开机重建**的(`conversation-log.mjs` 启动序列 DROP/CREATE)——这属「含开机迁移」,按写库自报律,**回执与提交信息的头一行要写「含开机迁移:触发器 conv_msg_no_update 重建」**;`333e38c` 的提交信息头没写。下次补;本批不返工。

## 五、继续

7b D152 端到端(北京店新客券,INSERT-only + 备份)→ 7d 六通 v4(落仓、新会话、`ranOn`)→ 段 8 D149 → 段 9–11 → 段 12。待裁接着 #8 起。
