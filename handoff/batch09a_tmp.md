# 小批 09a · 夜13 核 · 🔴 生产那 7 行 · 🔴 停线解释你判对了(记我一笔第 19)· D196 立案 · 立 J-77③ / J-65③

日期:2026-09-16
上游:夜班令 13 回执(段一–段七走完 + 兜底队列,进度行 25 行)
性质:**一处最重 · 一处我认账 · 一缺陷立案 · 两条新款 · 三处 J-55 勘正 · 九件记功 · 一张执行单**

---

## 〇 · 对账

md5 两处一致 `40229a34…` · 账本 **326 + 79 = 405** ✅(昨 364,本夜 **+41**)· 主档 **124/124** · 门关档 **25 套红 1**(昨 3)· **mp 档 3 套红 0**(昨 3)· `customer.js` **2,755 → 2,458(−297)且棘轮跟着降** ✅(裁 #105 执行到位,没留余量)· **J-60 28 → 23(降 5)** ✅ · 进度行 **25 行**,段一至段七走完后按兜底队列继续 —— **J-77 和兜底队列当晚就兑现了,这一夜没有空转。**

---

## 一 · 🔴 停线解释 —— **你判对了。而账记我头上(第 19 笔)**

你写在待裁第一条的那句:

> 「§五.2 触发了,我判成『停这一件 + 从此不再碰生产』,不是停全班。**这是我做的解释,不是你给的** —— 如果你的意思是停全班,**本夜段二之后的活都该作废重议。**」

**先说结论:你解释对了,本夜所有工作有效。**

**依据是我自己写下的文字**,逐条对:
- **§五.1**(第二个 D194 级洞)我写了「停手,写完整报告,**转只读盘点,不继续改任何代码**」——**范围写清了**;
- **§五.3**(主档回归红)我写了「**这一条不停全班**」——**范围写清了**;
- **§五.4**(模型超量)我写了「**其余照常**」——**范围写清了**;
- **§五.5**(环境坏了)我写了「**修不好则停全班**」——**范围写清了**;
- 🔴 **§五.2(痕迹非 0)我只写了动作**(「立刻停,只报不动,一行都不许删」)**,没写范围。**

**而 J-77 第一款的默认值是「停那一件,不停那一班」。所以按我写下的东西,你的解释是唯一正确的解释。**

### 🔴 记我一笔第 19

**五条停线里,三条写清了停到哪一层,两条没写,而触发的正好是没写的那一条。**

**你没有钻空子** —— 你在我留下的空白处做了一个**保守**的解释(「从此不再碰生产」比「只是这一次不动」更严),**而且主动报备、并且说清了「如果我解释错了,我这一夜后面的活都该作废」。**

**能在交一夜成果的时候,主动把那一夜的成果放到可能作废的位置上,这一件我给最高分。**

> **J-77 第三款(立)· 每条停线必须写明「停到哪一层」。**
> 三件缺一不可:**①停哪一件 ②还能不能改代码 ③还能不能继续这一班。**
> **没写范围的停线,执行的人只能猜 —— 而猜对了也是运气。**

**本批要做**:把现行全部停线过一遍,**逐条补上这三件**,做成一份 `handoff/停线总表.md`(**一处真相**,以后只改这一份)。

---

## 二 · 🔴🔴 生产库那 7 行 —— 收下,但闭合还差一步

### 收下的部分

**不是攻击,是我们自己的 e2e 压测**,证据链我逐条核过,**成立**:
- `display_name` 全是 `Google E2E`,**该字符串全仓只在 `e2e-stress-test.mjs:57` 一处**(**指纹唯一性先证,再谈数字 —— J-58① 的正确形态**);
- 时间对上 2026-07-03/04 云部署验证那两天;
- **零消费零预约零结算零储值零券**;
- **一行都没动** ✅。

**而你那句归纳比「是不是攻击」更重要,我原样收进台账**:

> **「那条无门禁的路在生产上真的被走通过。」**

**这把 D194 从「代码里有一个洞」升级为「一个被验证过可用的洞」** —— 而且它**至少通了两个半月**。修法不变(删路),**但「推 main 排在一切之前」这条裁定,现在有了现测证据,不再只是推理。**

### 🔴 还差一步:第二个指纹没报

那条路写两样东西:**`users.google_id`**,以及**一行 `user_identities`**(07z 现测)。
所以**有两个指纹**,而 08a §四 我批的四条里两个都在:
1. `google_id` 非空 → **你报了:7 行**;
2. **有 `user_identities` 行但没有 openid** → **这一条的结果你没报。**

> 🔴 **两个指纹应该指向同一批行。** 第二条查出来**不等于 7**,差额就是说不清的 —— 按 J-66,**在两个数对上之前,这一格不算闭合。**

**本批补报**:第二条指纹的行数 + **与那 7 行的 id 逐一对**(对得上几行 / 对不上几行)。

### 🔴 还差一步:这 7 行落在哪个租户

07z 现测过:**那条路的租户取自 body,而且会回落到默认店(= `lucky-luxe`)。**

**所以这 7 行很可能全在 `lucky-luxe`。** 这直接改一串数字:

| 数 | 我一直在说的 | 若 7 行都在 lucky-luxe |
|---|---|---|
| 全库 users | 77 | **真顾客 70** |
| `lucky-luxe` users | **37** | **30** |
| `lucky-luxe` 没手机号 | 28 | **要重算** |

**本批补报**:那 7 行的 `tenant_id` 分布。**报完我按 J-55 回去把台账、总表、诉求总账里所有基于 77 / 37 / 28 的说法原地更正。**

🔴 **这一条对我是第三次了**:第 11 笔(「1,432 条」凭空)· 第 12 笔(把 37 行当成真顾客)· 这一次(**77 里有 7 行不是人**)。**同一族:一个数,和它被安上的那个意思。**

---

## 三 · 🔴 D196 立案 · 三个涉钱口「跑满了还全绿」

段四十个口,六格全闭合,**红的是该红的那条:1/10**。

- 🟢 **`/admin/settlements/:id/void`(作废)守住了** —— 红的就是该红的那条,因果链通。**十个里唯一一个。**
- 🔴 **三个是「跑满了还全绿」,而且三个都有 J-58④ 的证刀**(证明刀确实咬到了):

| 口 | 造病 | 结果 |
|---|---|---|
| **建单** | `INSERT INTO settlement_groups` **整条注掉** | **759 条断言一条没红** |
| **扫码认领** | — | **53 条零红** |
| **发券** | — | **86 条零红** |

**你那句归纳是这一夜最好的一句话,原样进台账**:

> **「三个都在顾客那条线上:这单钱从哪来 · 这个人是谁 · 这张券发没发出去。」**

**三件都是顾客会当面发现的事,而且两件涉钱。**

**立 D196 · 三个涉钱口没有判据守着**(三条子项)。**根因我怀疑是同一个**:判据在断言**返回体或中间状态**,没有**从库那头读回来**(**J-62 第二款**)。

### 🔴 本批要量的一个数(这是裁 #106 那把读写集尺子的第一次大规模应用)

> **那 759 条断言里,有几条读了 `settlement_groups`?**

- 如果是 **0** —— 那 759 条**全部在验别的东西**,而「建单成功了没有」这件事**从来没有任何一条判据守过**;
- 如果不是 0 —— 那几条为什么没红,逐条给因果链。

**同样的量法用在 53 条和 86 条上**(各自该读的那张表)。**三个数一起报。**

### 上线排序的裁定

**这三条不挡上线,但补判据排在推 main 之前。** 理由说清楚:
- **D194 是「产品确实是坏的」→ 挡**;
- **这三条是「产品可能是好的,但我们无法证明」→ 不挡**;
- **但「给这三条各补一条从库那头读回来的判据」很便宜**,而它们是钱和身份;
- 🔴 **补完如果发现产品真的坏了,那时才挡** —— 到时再裁。

**其余六口 fail-fast 半路断,判成「没验成」不是「没守住」——判对了**,按 J-57 去 fail-fast 后重跑,列进下一批。

---

## 四 · 立 J-65 第三款 · 扫描面必须报「这次扫了几个文件」

**补靶子时咬出的三个真缺陷里,第一个最严重**:

> 🔴 **裁 #103 那把刀的扫描面里有一个不存在的文件,`try/catch` 静默跳过** ——**那个「0 处」是在只扫 1 个文件的面上得出的。**

**这不是刀瞎,是刀根本没看。而它报的是 0。**

**而裁 #103 的判据是我最看重的那一条**（「网页注册入口全站 0 处」）。**也就是说:那句话之前一直是在一个几乎空的扫描面上得出来的。**

> **J-65 第三款(立)· 兜底把「文件不存在 / 读不了」吞掉,等于把扫描面悄悄缩小。**
> ①扫描器读文件失败**必须出数并红**,不许 `try/catch` 跳过;
> ②🔴 **每次扫描必须同时报「这次扫了几个文件」**,与**预期文件数**对;**对不上就红。**
> ③**这个数要和每一个 0 一起出现** —— **一个不带扫描面的 0,和一个没有依据的 0 是一回事。**

**按 J-56 连坐**:`test-web-not-signup` **历史上报过的所有 0 全部作废**,补完扫描面重跑,**有差按 J-50 归因**。

**另两件收下**:
- `success-state-trace` **两头都没锚**(`/my/cou` 也报「碰过了」)——**假阴方向,而那把刀自己的注释就写着假阴更危险**。🔴 **注释是对的,实现没跟上** —— 这一条要记:**注释写对了不算守住,注释不是判据。**
- `customer-paths-scan` **漏了 24 个调用**(`pub()` 正是打 `/bind-tokens/…/confirm` 的那个)→ **路径总数 40 → 62**。

---

## 五 · 路径 40 → 62:我那个追问答完了,但要连坐

**我在 07v / 08b 问过两次「40 条已知偏少」,现在答完了:真值 62。**

🔴 **但 40 这个分母被用过**:07m 报过「**路径表 40 条,8 条没走过,含两条支付**」。
**分母从 40 变成 62,那个「8 条没走过」必须重算** —— 新增的 22 条里有几条从没走过?**按 J-56,基于「40 条」做过的所有结论一律标存疑,本批重出一次表。**

---

## 六 · mp 档 3 套红:挂了十几批,真因是一个环境变量

> 「模块在、工具在、9420 会话是活的(426)—— **只差 `MP_AUTOMATOR` 没设**。**我之前报的『仓外模块没装』是错的**,那是**照预检提示语抄的**。」

**自己认了,而且认到了根上。** 这是 **J-63 第①问的又一例**,而且是新的一种形态:

> **以前认的「抄」是抄一个数。这一次抄的是一句「提示语」——而提示语也是「别人说的」。**
> **口径:工具告诉你的原因,和你量出来的原因,是两件事。**

治法「变量没设就按清单路径自找,真不在才红」✅ **正确形态**(把「说不清」变成「先找,找不到才红」)。**红 3 → 红 0,收。**

---

## 七 · 记功九件

1. 🔴🔴 **停线解释主动报备,并且把一夜的成果放到可能作废的位置上。**(见 §一)
2. 🔴 **痕迹查询非 0 之后,立刻停、只报不动、一行没删,并按 08a 预案补三样只读事实。** ——**在最想动手的那一刻没动手。**
3. **指纹唯一性先证再谈数字**(`Google E2E` 全仓一处)——J-58① 的正确形态。
4. 🔴 **「那条无门禁的路在生产上真的被走通过」** —— 这句话比「是不是攻击」重要,**而它是你自己从事实里提出来的,不是我问的。**
5. **三个真缺陷是补靶子时顺手咬出来的**,其中「扫描面里有个不存在的文件被静默跳过」**直接推翻了我最看重的那条判据的历史结论**。
6. **mp 那句「仓外模块没装」自己认错,并认到「照提示语抄的」这一层。**
7. **靶子生成器三改不对就停,并且换路子**(改走「读代码定靶 + 造病台验靶」),**不是第四次试** —— **知道什么时候该换方法,而不是再试一次。**
8. **`noshow-aftersales` 三改不对 → 原样还原,用脚本反向改回,没用 `git checkout`** —— J-34 守住了,**主档不留红这条底线也守住了。**
9. **文案表摘取时先查出 `moneyY(...)` 这类函数调用会白屏,改成工厂式** —— **摘之前先查依赖。而白屏是顾客端,顾客当场就会看到。**

---

## 八 · 执行单(唯一真相)

| 序 | 事 | 交什么 |
|---|---|---|
| **1** | 🔴 **7 行的两件补报** | ①**第二个指纹**(有 `user_identities` 无 openid)的行数 + 与那 7 行 **id 逐一对** ②那 7 行的 **`tenant_id` 分布**。**仍然只读,一行不许动** |
| **2** | 🔴 **D196 那三个数** | 759 / 53 / 86 条断言里,**各有几条读了该读的那张表**(`settlement_groups` 等)。不是 0 的,逐条给「为什么没红」的因果链 |
| **3** | **D196 补判据** | 三个口各补**一条从库那头读回来**的判据(J-62②),造病现证会红(J-37⑤ 五步齐)。**排在推 main 之前** |
| **4** | **停线总表** | `handoff/停线总表.md` 一处真相,**每条补齐「停哪一件 / 还能不能改代码 / 还能不能继续这一班」三件**(J-77③) |
| **5** | **裁 #103 那把刀** | 补扫描面 + **报「扫了几个文件」与预期数** + 历史 0 按 J-56 作废重跑,有差按 J-50 归因 |
| **6** | **路径表重出** | 62 条的全表 + **「几条没走过」重算**(原 8/40 作废);基于 40 的结论逐条标存疑 |
| **7** | **其余六口去 fail-fast 重跑** | 按六格报,红逐条点名 |
| **8** | **40 个口继续**(已做 10) | 涉钱剩余优先;**11 条追不到口的断言归口结果**(上一夜没报) |
| **9** | 门关档**红 1** · J-60 **23 → 至少 18** · 9 条冻结理由按 J-70 复核 | — |

**挂起不变**:推 main(等店主批)· D195 实现(**图我今天读,读完批**)· 境内代理(**等 D194 从线上消失**)。

---

## 九 · 停线(新增两条,其余照旧)

- 🔴 **生产从此只读,而且非必要不连** —— 你那条解释我采纳为正式口径:**痕迹既然查清,生产侧的事到此为止,下一次连生产必须有单独的批。**
- 🔴 **一个 0 不带「扫了几个文件」,不许写进任何报告**(J-65③)。

**重申**:不推 main · 不许向生产发写请求 · 不许把「本地已删」写成「已修复」 · **D194 从线上消失之前不许接通境内代理** · 分类数加不上底数就停 · 红 N 没有名字不算交 · 凡落刀必经造病台 · 🔴 **凡要在网页顾客端加「注册/创建账号/新建顾客」的一律停下来问,这一条没有例外。**
