# 本批交付清单 · 横幅批+大类改造批(图 v1.4,店主 08-16 点头三层结构)

> 三层口径(定稿):大类(平台字典·可扩展)→ 项目(商家自定义)→ 加项(项目附属,只在开单选中项目后出现,顾客端永不显示)。
> 搁置摘除(店主 08-16):店主账号改密、lucky 地址电话两件不再跟踪。

## 横幅批(上批判词欠账)
- ☑ H1 首页 hero 主标题/logo alt/轮播 aria 随店(brandName() 单源;stores 未到首帧回落平台名,加载即刷)
- ☑ H2 品牌文案字面量全仓再扫:渲染层 12 处全部收敛到 brandName()(hero 标题/服务页标题/AI 面板 eyebrow/作品集 eyebrow+alt×3/登录页 eyebrow/store-cover alt/轮播图签去品牌名);注册标题「创建 Lucky Luxe 账号」→「创建账号」(假设①)
- ☑ H3 CI ㉟:剥注释后全文件 'Lucky Luxe' 字面量=1(即回落值本身),多一处即红
- ☑ H4 两店首页终拍(生产,见收工报告)

## 大类改造批(v1.4 复核-3)
- ☑ C1 平台字典表 `platform_categories`(key/name_zh/name_en/types_json/sort):种子三行 美甲[NAIL]/美睫[LASH]/护理·其他[CARE,OTHER];商家零写入口(只读);**加行业=平台 INSERT 一行,前后端零代码**(types_json 挂新 type)
- ☑ C2 必挂大类:services.type 本就必填 → platformCategoryOf() 全量映射(OTHER 归护理·其他);「待归类」态结构上不存在(type 无 NULL,断言兜底脏值)
- ☑ C3 顾客网页服务页:左栏=固定大类(字典驱动,**空大类不显示** visibleCategories);顶部 nail/lash 分段撤除;原二级分类并入项目卡 eyebrow(v1.4「或并入项目名」路线);**顺带修暗礁:此前网页只拉 nail+lash 两类,护理类服务网页顾客端根本看不到**——改一次拉全量
- ☑ C4 小程序顾客端:现三 Tab(美甲/美睫/护理·其他)结构上已=三大类;字典驱动标签随发版(接口已兼容,本批不动小程序,记发版清单)
- ☑ C5 加项归属:既有 addon_scope 机制即 v1.4 语义(scope 空=通用全组显示;开单选项目后按域出现)——本批零改动,断言背书(加项 0 出现在顾客大类列表)
- ☑ C6 断言:CI ㊱×4(字典下发三行起步/全条目∈某大类/加项次卡 0 出现/空类过滤在场)+㉟,套件 154 全绿
- ☑ C7 模块①编辑器:type 选择改平台大类标签(美甲/美睫/护理/其他(归护理·其他类)),原是 NAIL/LASH 英文双枚举(CARE/OTHER 存量在编辑器里根本选不到——顺带修)
- ☑ C8 生产逐屏两店实拍(2c4564c:平台头/切店弹层/hero 店名/服务页大类+起,两店)

## 假设清单
1. 注册页标题去品牌硬绑改通用「创建账号」(登录页 eyebrow 已随店名;copy 静态字面量无法运行时插店名,改措辞最稳)。
2. hero 轮播三张照片是平台通用素材(stores 无图片字段),图签去品牌名改通用(「店内氛围」);hero 副题「Nail & Lash Atelier」保留为行业描述线。
3. 「护理」大类命名沿用顾客端既有聚合口径「护理·其他」(OTHER 类有归属,不产生「待归类」孤儿);纯「护理」叫法如店主坚持,字典改一行数据即可。
4. 网页服务页默认进「美甲」类(原默认 nail 保持);首页「立即预约」按钮仍落美甲类。

---

# 追批 · v1.4 三件批(店主 08-16 关单判词携带;D43-D46 已关)

## ⓪ 平台头修正(店主 mid-batch 反馈,当批落地)
- ☑ 网页左上角=平台品牌「有迹/YOUJI」(与小程序同款 youji-logo.png,资产同源零搬运);店名撤出页头
- ☑ hero 横幅精简:去 logo/去「Nail & Lash Atelier」小字/去描述句——只剩店名+两颗功能按钮;标签页标题仍随店

## ① 网页切店入口(口径翻案:不能只靠每店专属链接)
- ☑ 页头「切换门店」钮(页头现为平台名,入口挂按钮区=假设⑤)→ 弹层列表(与小程序 shop-select 同一 /shops 数据源,演示店同口径隐藏)
- ☑ 选中即 ?store= 整页进店=D45 直达链接同一机制,整页加载=天然全清场(三跳零残留那套直接复用);当前店高亮「当前门店」
- ☑ 每店专属链接保留;实点切店 lucky→jics 端到端验证(hero/tenant 记忆随切)
- 注:本地沙盘列表 76 家含测试渣店=与小程序同口径(生产干净;发版前批量停用已挂账,本批不动)

## ② 小程序服务 Tab 按图 v1.4 重构(店主实拍判词五条逐条)
- ☑ 判①顶部段选撤除(type-switch DOM+逻辑零残留,CI ㊲ 断言)
- ☑ 判②左栏=平台大类(getServiceCatalog 字典驱动,空类不显示,与网页同构)
- ☑ 判③右侧=项目卡,二级分类并入卡片眉标(service-eyebrow)
- ☑ 判④「加项服务」分类灭绝:静态分类数组(NAIL_CATS/LASH_CATS 含「加项服务」)整体删除——**上批「三 Tab 已同构只差发版」说错了,认**:实况左栏还是旧二级+加项服务类,本批照图重构
- ☑ 判⑤「¥xxx 起」(priceFromLabel,后端算好)
- ☑ devtools 两店实拍:jics 美甲9/美睫5/护理3、lucky 美甲5/美睫2/护理1,逐大类实点全带起+眉标(核验截图_2026-08-16/)

## ③ jics 目录归类核对(店主口述清单,本地+生产双库)
**结论:jics 归类与店主口述完全吻合,主项目零调整。**对照表:
| 大类 | 二级(库内 category) | 条目 | 状态 |
|---|---|---|---|
| 美甲 | 美甲单色 | 精品单色/高光猫眼 | ✓ 吻合 |
| 美甲 | 美甲简单款式 | 简单款式 2/3/3.5 小时 | ✓ |
| 美甲 | 美甲复杂款式 | 复杂款式 4/5/6/8 小时 | ✓ |
| 美睫 | 美睫 | 单根嫁接/自然款/款式美睫/下睫毛/卸除睫毛 | ✓(店主口述「一对一单根嫁接」=库内「单根嫁接」,名称简称差异,商家自定义名未动) |
| 护理 | 护理 | 手部精修前置/富勒烯手护/焕颜足护 | ✓ |
| (加项) | 延长类/补甲类/卸甲类(/卸睫类) | 浅贴甲片/甲膜/水晶/纤维补甲/水晶甲矫正/日本UP足部矫正/卸本甲/足部卸甲/甲片卸甲/水晶卸甲 | ✓ 全部 item_kind=addon,顾客分类 0 出现 |

**唯一数据调整(生产)**:生产 jics 加项 addon_group 全空(本地有分组)→ 同名同步 10 条(空→延长类×3/补甲类×3/卸甲类×4),开单面板分组恢复;本地多出的「本店制作免卸甲/免卸睫毛」两条生产不存在,未强建。
**lucky 自查**:结构正常;1 疑点报店主拍板——「手部基础护理」(type=NAIL/二级=基础护理)挂在美甲大类,语义上亦可归护理大类,两库一致,未动;「测试服务」(已停用)=测试渣不见客,随发版清单口径。

## ④ 组合矩阵欠账补跑(大类×购物车)
- ☑ 抓获真缺陷:购物车 localStorage 单 key 不分店——切店带着上家店的商品走(D39 同族)。修=按店分仓 `lucky-web-cart:<租户>`(6 处读写全换,CI ㊲ 断言)
- ☑ 实测:lucky 种 2 条→切 jics 徽章 0→切回 lucky 徽章 2(双向零残留);小程序大类×切换=走查逐类实点
- 注:老 key 购物车暂存数据随分仓作废(购物车为暂存态,无钱无单据)

## 验证
- ☑ CI ㊲×6(段选撤/字典驱动/加项类灭/起在场/切店在场/cart 分仓)累计 160 断言全绿
- ☑ 42 套件 EXIT=0(补跑真全绿;首轮虚报已复发登记)→ 生产 2c4564c → 两店实拍全屏集

---

# 追批二 · D48+三闭环+窄屏自查(店主终验三条,2026-08-16)

## D48 小程序护理板块「服务不存在」
- **根因一句+行号**([miniprogram/utils/api.js:518](../miniprogram/utils/api.js) 原实现):`getService(id)` 靠**猜 id 前缀**定类——`id.indexOf('lash')===0 ? 'lash' : 'nail'`,护理类 id(`care-service-*`)被猜成 nail,去美甲列表里找永远 null →「服务不存在」。**护理详情从来就打不开**,v1.4 让护理类真正可点后才暴露。
- 修:全量目录直查(与网页同构),不猜类;**血缘=detail+booking 两调用方**(预约护理类此前同炸,一次同修);L2 同类扫「猜前缀定类」全仓 0 残留。
- 验证:devtools 两店护理详情实拍可开(jics「手部精修前置」/lucky「手足深层SPA护理」,截图入核验档)+美甲详情「精品单色」可开(walk 首轮);**网页同路径对照复验**:护理→详情「手部精修前置 固定价 ¥158」正常——网页此路径一直健康,病只在小程序(四之九对照结论);CI ㊳×2(猜前缀禁令+CARE 条目全量可命中),套件 162 全绿。
- 覆盖面如实:devtools 实拍=美甲+护理×2 店;美睫类同一代码路径(全量 find),由 CI 与接口层覆盖,未单独实拍(devtools 自动化桥本轮多次劣化,已按最小脚本策略拿到关键屏)。

## 三闭环保证(商家自助编辑,平台不代改)
在本地 jics 沙盘用「三闭环测试项/测试加项」实测(用完即删,清场断言过):
- 闭环③ 新建项目挂大类(CARE)→ 顾客端接口立即出现(platformCategory=care)✓
- 闭环① 编辑改大类 CARE→LASH → 顾客端随动(care 消失/lash 出现)✓;编辑器「所属大类」控件=上批 C7 四类中文标签,已在
- 闭环② 加项增→开单面板唯一数据源(/admin/pricing/items)出现+**不泄漏顾客端**;删→消失 ✓
- 验证层级如实:闭环断言在「顾客端/开单面板的唯一数据源」接口层;渲染层随动由两端多轮同源实拍背书(左栏=同一接口字典渲染)。
- **「手部基础护理」分毫未动**——留店主亲手在后台改挂,操作成功即闭环①正式验收。操作路径:网页后台 → 服务与价目 → 模块① 找到「手部基础护理」→ 修改 → 「平台大类(必挂)」选「护理」→ 保存 → 顾客端(网页/小程序)护理类下立即可见。
- 明确边界:改「所属大类」在**模块①编辑器**(全字段);模块②「编辑价档」入口只管价格档位。

## 网页切店按钮窄屏自查
- 375px 宽实拍:「切换门店」按钮**在场、可见、可点**(按钮组折行两行但不藏不挤,页面无横向溢出)——窄屏无缺陷,实拍留证。
- 店主端「没有」最可能=强刷前旧版(切店钮 20260816b 才上线);若强刷后仍无,等她截图单独立案。

---

# 追批三 · D49 详情页价格双矛盾(Cowork 亲验抓获,2026-08-16)

## 根因与修法(D46 同族第五出口)
- 病①:详情页价格用旧 `priceLabelZh`(基础价/固定价+标价)——列表「¥58 起」vs 详情「固定价 ¥158」两个矛盾口径;此出口漏在 D46 的 L2 扫描外(当时只扫「写死店铺事实」,没扫「价格出口不同源」)。
- 病②:价格说明二分支(nail/else)——护理项吃美睫套话。
- 修:后端单源 `priceDetailLabelZh/En` —— **多档=「¥xxx 起(普通 ¥158 / 分享 ¥88 / 会员 ¥58)」;单档才许 基础价/固定价**(「固定价」对多档=禁语,CI ㊴ 行为断言);价格说明三分支(美甲=人工报价/美睫=固定报价/护理·其他=中性「按所选价格档位计价…」);两端详情页同批换源(小程序详情取数刚重写过,顺手同修,四之九)。

## 验证
- CI ㊴×5(多档禁语/单档语义/护理说明非美睫/两端出口引用同源),套件 167 全绿
- 网页实测:列表「¥58 起」=详情「¥58 起(普通 ¥158/分享 ¥88/会员 ¥58)」,说明中性
- 小程序 devtools 两店实拍:jics 多档「¥58 起(…)」/lucky 单档「固定价 CAD $98」(单档=口径正确),截图 d49-care-detail-*.png
- 42 套件回归(日志原文行入收工报告)

## 价格出口 L2 补扫(四处)
| 出口 | 数据源 | 状态 |
|---|---|---|
| 列表(两端) | priceFromLabel(起价) | ✓ 同源(S1 批) |
| 详情(两端) | priceDetailLabel(起+档说明/单档固定价) | ✓ 本批修 |
| 购物车(两端) | service 快照 price(普通档标价)+已选加项预估 | ⚠ 口径待裁决:显示普通档预估价(结算按实际档)是既有金额口径;改成「起」=改业务口径,按纪律先报——中间桥=详情页档说明已能自证 158 从哪来。两端一致(网页=标价+加项合计;小程序=标价),不动 |
| 预约页 | service 快照(标价体系)+加项固定价 | 同上,未动 |

---

# 🏁 S1+S3 线收官落款(2026-08-16,Cowork 宣言)

- D43-D49 七缺陷全清全关;图 v1~v1.4 四版合同全兑现。终验=店主亲手在后台把一个手护项目改挂美甲成功(自助编辑闭环活验收);「手部基础护理」保持原样,店主想挂哪儿随时自己挂。
- **S13 入册(店主收官时提出,归 S4/S5 网页后台波次,出图时一并画)**:
  ① 模块①列表按大类分组显示(现平铺太乱);
  ② 平台侧「大类字典管理」界面(商家只读口径不变;平台加/改大类走界面不走 DB)。
- 发版清单待办:小程序代码包发版(服务 Tab 新结构+「起」+D48/D49 详情修复),店主择时。
- 常驻资产:platform_categories 字典/brandName()/店铺单源渲染件(currentStore 系)/购物车分仓/priceDetailLabel 价格出口体系;常驻纪律:四之十生产终验律/Cowork 亲验关/验尺独立/回归结论粘日志原文行。
- Code 状态:待命等 S2 图(充值/次卡/券+卡包+商城+境外登录)。
