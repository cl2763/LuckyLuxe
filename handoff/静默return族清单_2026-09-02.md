# 静默 return 族 · 全端清单(D105 配套,店主 02x 令:先清单逐处标改/不改+理由,再动刀)

## 尺子(一把量到底,写明是什么)
```
扫描面   apps/web/**.js + miniprogram/**.js(排除 node_modules)
判据形状 函数声明的**下一行**就是 `if (...) return`,且该 return 行内**没有**任何提示动作
         (showToast/showModal/setData/console/throw/toast/tip/hint/alert/note/msg/showLoading)
现测     215 处 / 71 个文件
```

## 按**机制**分四类(不按长相;店主判据律:先写类定义再给机械证据)

| 类 | 机制定义 | 处数 | 改/不改 |
|---|---|---|---|
| **C2 状态不允许却还能点** | 守的是「当前状态不允许这个操作」,而控件照样在、点了没反应、不说为什么 | **12** | **逐处判**(见下) |
| C1 重入锁 | 守的是「正在进行中」旗标(`redeeming`/`clocking`/`paying`…) | 7 | **不改** —— 用户连点两下,第二下无声正是对的 |
| A 防抖/锁 | `loading`/`submitting`/`busy` 等 | 22 | **不改**,同上 |
| B 数据未就绪 | `if (!this.data.list) return` 之类,发生在渲染/刷新助手里,**不是用户动作** | 49 | **不改** —— 没有用户在等反馈 |
| D 工具/内部 | utils 与内部辅助函数的入参守卫 | 125 | **不改** |

## C2 逐处(唯一需要判的那一类)

| 位置 | 守什么 | 改/不改 | 理由 |
|---|---|---|---|
| `miniprogram/pages/store-location/index.js:45` | `if (!this.data.store.address) return` | **改** | 地址为空时点「复制」无反应 —— **正是 D108 那一族**:值没有就不该出现这个动作。随 D108 一并修(整块不出现) |
| `miniprogram/pages/booking/index.js:244` | `if (!this.data.referenceDataImages.length || t` | **不改** | `analyzeReference` 守的是「没传参考图 或 正在分析」;没传图时按钮本身不出现(wx:if 已控),这是二道保险 |
| `miniprogram/pages/booking/index.js:320` | `if (!this.validate()) return` | **不改** | `validate()` 内部已 showToast 指出缺哪一项 —— **不是不吭声**,声在 validate 里 |
| `miniprogram/pages/booking/index.js:332` | `if (!this.validate()) return` | **不改** | 同上,validate() 已出声 |
| `miniprogram/pages/me/index.js:606` | `if (!this.requireLogin()) return` | **不改** | `requireLogin()` 内部会拉起登录流程 —— 有动作,不是静默 |
| `miniprogram/pages/merchant/finance-txns/index.js:54` | `if (this.data.isCurrent) return` | **改** | 已是本月时点「下一月」无反应。改:到边界时按钮置不可用并给一句「已经是最近一个月了」 |
| `miniprogram/pages/merchant/settlement/index.js:884` | `if (!this.data.qr) return` | **改** | 二维码还没生成时点「打开」无反应。改:未生成时不出现该动作(与 D108 同解法) |
| `miniprogram/pages/merchant/settlement/index.js:889` | `if (!this.data.bindQr) return` | **改** | 同上(绑定码) |
| `miniprogram/pages/merchant/settlement/index.js:893` | `if (!this.data.bindQr) return` | **改** | 同上(复制绑定链接) |
| `miniprogram/pages/merchant/salary-plan/index.js:209` | `if (this.data.isDefault) return` | **改** | 已是默认方案时点「恢复默认」无反应。改:置不可用 + 一句「当前已是默认方案」 |
| `miniprogram/pages/merchant/schedule-day/index.js:126` | `if (this.data.readOnly) return` | **改** | 只读态点「保存」无反应 —— **与 D105 值日同一个病**:状态不允许却不说为什么。改:后端出句,两端同句 |
| `miniprogram/pages/merchant/schedule-day/index.js:147` | `if (this.data.readOnly) return` | **改** | 同上(拆分班次) |

**要改 8 处 / 不改 4 处。**

**归族**:C2 这一类的病根只有一个 —— **状态不允许操作时,控件还在**。
D105(值日 `canEdit=false` 点不动不吭声)与 D108(地址空还挂着「导航·复制」)是它的两个已命名成员。
**解法也只有一个**:要么把动作**整块撤掉**(值没有时,如 D108),要么**留着但说明白为什么**(权限/日期不对时,如 D105)。

**棘轮**:C2 条数上限 12,只减不增;新出现的 C2 自动红(判据落在 `test-silent-guard.mjs`)。
