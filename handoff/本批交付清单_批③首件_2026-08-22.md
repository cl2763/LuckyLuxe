# 本批交付清单 · 批③首件:顾客双端订单详情重做+售后发起线 · 2026-08-22

> 合同:《订单详情与售后线_UI设计图_2026-08-20.html》v1.1 定稿(§二/§三;拍板三问已答:①文字版 ②可撤回留痕 ③未签署不能发起·双端统一)。
> 状态:**清单先交,未开工**——店主/Cowork 核过清单再动代码(合同式交付第 1 条)。
> 纪律:一项一行;图上没画到的按最合理做并记假设;逐屏并排核验;新增代码禁原生弹窗(㊶ 基线只减不增)。

## A 组 · 顾客小程序订单详情重做(§二,S8 兑现)

### 屏A 已完成订单详情(重做后)
- ☑ A1 预约信息卡:服务名+状态徽标(后端三态同源)/时间(起–止)/技师/门店/单号
- ☑ A2(卡内=后端 flow 直贴,与签署页同源;web-view 不能内嵌卡=技术差异记录,原件入口=pages/sign)「服务签署单」卡=**签署单快照直出**(pages/sign web-view 单一实现,只读;金额行/签署时间/已签署 ✍ 全在快照内,前端零拼装)
- ☑ A3 未签署的已完成单:签署单卡显示「本单未产生结算单」——绑真数据或明示空态,**不放例句**(D17 家族)
- ☑ A4 「服务留档」卡:技师小记/照片,有则显示、无则整卡不出现(空态=不渲染,不占位)
- ☑ A5(双钮=「查看服务确认单原件」+售后钮后端句)底部双钮:「查看服务确认单」(=A2 同一张页)+「有疑问,去售后」
- ☑ A6 旧页「实付 ¥X」行随重做消亡(480 表达错误在此销案;paidCents 全仓唯一读方即此页,拆除后字段留后端不害)
- ☑ A7(其余状态保持现状,记假设①)非「已完成」状态的详情页:保持现状功能(待服务/已取消等),仅统一头部信息卡结构——图未画的状态不发挥(L3,拿不准报 Cowork)

### 屏B 售后发起(同屏展开,拍板①③)
- ☑ B1 「有疑问,去售后」仅在**已完成且已签署结算单**的单出现(拍板③双端统一;未签署单不出按钮)
- ☑ B2 点击=同屏展开发起表单:问题描述**必填**+提交;不跳原生弹窗、不嵌套弹层(四之七)
- ☑ B3 第一版仅文字描述(拍板①;照片随 S 期后补,入口不预留假控件)
- ☑ B4 提交→售后单(待处理)→本页出现 D3 三步进度卡(发起→跟进中→结果;句子后端唯一)
- ☑ B5(㋈:进行中 409/结案后重开 201)已有进行中的售后:按钮变「查看售后进度」——**一单同时只一条进行中**;结案后可再次发起
- ☑ B6(㋈:撤=resolved+「顾客撤回」;商家发起顾客撤=403;结案后撤=409)顾客可撤回自己发起的售后(拍板②):撤回=转「已解决」+自动备注「顾客撤回」,留痕不删;仅能撤**自己发起且未结案**的
- ☑ B7(POST /my/bookings/:id/after-sales(+/withdraw);㋈×17)后端:顾客侧发起口(新增路由,**不加新状态语义**——走同一状态机+status_history 留痕,actor=顾客);前置校验=本人的单+已完成+已签署;越权/重复发起 4xx 带原因句
- ☑ B8(㋈:带 amountCents 零账目行)涉钱零新径:售后单只记过程与结论;退款/补差一律走既有更正链;原单快照不动(B② 规则原文照守,断言常驻)
- ☑ B9(后端闸+商家口重开同构;⑦ 夹具随新口径改)商家双端「转售后」收紧兜底:后端 PATCH 加闸(无已签署结算单的单转售后=4xx;此前只藏了按钮,接口无闸=C1 查明遗留);双端按钮前置已有(㊹),断言补接口面

## C 组 · 网页顾客端同构同批(§三,S12 审计续行)
- ☑ C1(徽标后端句直渲;「已结清」两案=C5 待拍)订单列表:徽标三态同源(customerOrderBadges 家族句直渲,与小程序同句)
- ☑ C2(flow 卡+原件链接/未签空态句/留档无则不出)订单详情=§二同结构:预约信息/签署单本体(iframe /sign 同一实现)/留档/售后按钮
- ☑ C3(同屏表单+D3 进度+撤回,三端零分叉)发起表单同屏展开+D3 进度展示同口径;句子后端唯一,**三端零分叉**(四之九:金额/状态机类禁两端各写一份)
- ☑ C4(待签列表「到店支付」头条句)网页顾客端待签单列表(D57 小程序侧已落,网页顾客端同构补齐——与 C1 列表同批)
- ☑ C5(店主拍定 08-22:「已结清 ¥X」=实付现金,与单据头条同源=Σoffline 腿;双端同刀:小程序列表句换源、网页列表金额行接同句;售后行「总价 <合计>」同族销案,㋉ 断言常驻)「已结清 ¥X」语义两案(随 C1 列表重做,店主拍)

## E 组 · D61 连签流(店主拍板 08-22,随首件)
- ☑ E1(签署完成态「本组还有 n 张待签+继续签下一张 →」实拍)同客多张待签单:签完一张**自动接续下一张**(签署页直接跳,或明示「还有 n 张待签」一键继续)
- ☑ E2(全签完 groupPendingCount=0 才出完成态;中途退出=待签列表兜底)全部签完才回台面;中途退出=剩余单留在待签列表
- ☑ E3(D57 入口原样保留)日结未签入口降级为**兜底**(中途退出才用),入口保留(D57 已落的可点行不动)
- ☑ E4(商家出码 sign-state nextPendingId 自动接续;㋈ 断言)双端同构:商家出码流(1/N 顺序既有)与顾客端「待你签字确认」卡均接续;签署页「待签 1/N」计数已有,续签钮接它

## D 组 · 三个既定小尾(店主指定随首件)
- ☑ D1(店主拍案一 08-22:「首项目 等N项」——句后端唯一 listTitleText,项=签署单项目行+现场购卡,单项目回空串沿用服务名;双端直渲+映射层透传,㋉ 断言:等2项/单项目空串/购卡计入 N)列表页多项目标题样式(三案已拍,案一落地)
- ⬜ D2 L3① 行内小注:结算页储值行「可用余额 X → 本单抵 Y(Y>X)」时补一句行内小注(如「含本单随签充值」)——文案报 Cowork 定稿后落
- ☑ D3 「线下收款」行改**非勾选样式**「到店收 · 差额自动」(D55 裁:金额口径不动,仅样式去勾选控件感)
- ☑ D4 组卡 dueLabel「组合计应收(N 张)」→「**组到店支付(N 张)**」(08-22 06:10 裁;单张「到店应收」→「本单到店支付」同句族)
- ☑ D5(店主拍案二 08-22:双技师=进「未分配」,店长日结分配业绩那一下一并核定——allocate 口默认归份额最高者/可显式点名(悬空=400);台账补记走触发器豁免②=technician_id 空→值单向一次,金额列永锁;recharge_json 同步补写;㋉ 断言×6)D59 v2 冲卡归属(三层瀑布+双技师日结核定,全落)
- ☑ D6(v1.1.0 已传:批②全量+批③首件;双闸自查后上传,传完沙箱指向已还原)挂账件:体验版重传(批③首日,上传前双闸自查 USE_LOCAL_SANDBOX=false+API 指生产,传完还原沙箱指向)

## 假设清单(开工前已知,施工中续补)
1. A7:图只画「已完成」详情;其余状态仅统一头部结构不重做(记假设,店主可裁)。
2. B7 路由形:POST /my/bookings/:id/after-sales(描述必填)+POST …/withdraw(撤回);命名以实际实现为准,语义=既有状态机零新增。
3. C4 网页待签列表纳入本批(D57 双端收口),若 Cowork 认为该归 S12 独立批,拆出即可。

## corner case 覆盖计划(六类+财务红线)
边界值(描述超长截断/单号悬空)/空态(无签署单的完成单=A3 句;无留档=A4 不渲染)/并发(同单双端同时发起=一单一条 409)/越权(发起他人单/撤回他人售后/未签单强打发起口=4xx)/幂等(重复提交同描述=一条)/异常输入(空描述 400)。+财务红线:发起/撤回/结案全链零金额字段落账(带 amountCents 的请求不产生账目行,沿 B② 断言同构)。
