# 【给 Code 的指令】新对话 —— 跑 12i / 12j / 12k(整段可直接粘进新对话)

店主 09-24:另开一个对话跑 12i / 12j / 12k。**围栏三条,违反即停回报,不许自行放宽。**

---

## 零、三条围栏(最高优先级,盖过下面所有内容)

1. **只许动这三处**:`tools/ai-retouch-eval/`、`tools/segment/`、产出夹
   `~/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/31o_修图验证_2026-09-22/`(含新建子夹)。
   **`apps/` 一个字节不许改**(前端、后端、小程序全在内)。收工回报必须附
   `git status --porcelain apps/` 输出为证,非空即红。
2. **不推 main、不碰生产**。不 `git push`、不 `railway *`、不连生产库、不跑
   `run-all-tests.sh`(它会 `pkill local-server.mjs`,打死店主的 4128 与沙箱 4310)。
   本地提交可以,推送要单独批准。
3. **钱的停线**:模型 B `doubao-seedream-5-0-pro-260628`,¥0.32/次。
   12a 关帐累计 ≈ **¥58.6**;本批(12i 全部,含两支探针)上限 **¥12**,
   **累计上限 ¥72**。每次调用前先算「这一发打完累计多少」,要越线就**停下回报**,不许先跑后说。
   12j / 12j补 / 12k 是 **¥0 批**,一次模型都不许调。

> ⚠️ 第三条围栏店主那句话被截断了,以上「钱的停线」是按 12i 单里写死的口径填的。
> 若店主本意是别的第三条,以她后补的为准,先停再改。

---

## 一、开工前必须确认的环境(别在 worktree 里跑)

- 仓库根:`/Users/changliu/Documents/Codex/2026-04-29/new-chat`
- 🔴 **必须在主仓工作树里跑,不许开 git worktree**:`tools/segment/` **没有入 git**
  (`git ls-files tools/segment` 为空),`venv` / `venv312` / `weights/mobile_sam.pt` /
  `weights/hand_landmarker.task` 只存在于当前工作树。开 worktree = 分割那条链整个不存在。
- 产出夹:`~/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/31o_修图验证_2026-09-22/`
  - 第一批归一化件:`_input/`(45 个文件)
  - 第二批原始样片:`~/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/AI修图样片/`(85 个文件)
  - 第二批文件清单:`_第二批44张_文件清单_2026-09-24.txt`
- 钥匙:`~/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/env/ark.env`(`ARK_API_KEY=`)。
  **不许打印到日志、不许写进任何文件、不许入库。** 跑批脚本一律按现有写法把 env 文件路径
  当第一个 argv 传进去(见 `tools/ai-retouch-eval/run-round5.mjs`)。
- Python:`/opt/anaconda3/bin/python3`(现有脚本里写死的那个)。

## 二、任务单在哪(三份 + 两份补,全部按原文执行,不自由发挥)

| 批 | 文件 | 预算 |
|---|---|---|
| 12i | `handoff/小批12i_店主看图判_口令按甲色三档选_扩样15张验规律_2026-09-24.md` | 含在 ¥12 内 |
| 12i 补 | `handoff/小批12i补_店主目标是别家P图软件给不出的甲面质感_两支探针_2026-09-24.md` | 含在 ¥12 内 |
| 12j | `handoff/小批12j_44张样片可用作D222训练验收集_标注开工_2026-09-24.md` | ¥0 |
| 12j 补 | `handoff/小批12j补_第二批44张样片到位_归一化编号45起_优先难类_2026-09-24.md` | ¥0 |
| 12k | `handoff/小批12k_对比包一律可点反馈_本地HTML_过不过加备注_导出反馈文件_2026-09-24.md` | ¥0 |

(桌面夹 `~/Desktop/LuckyLuxe_Claude_Handoff_2026-07-03/` 下有同名镜像,以仓库 `handoff/` 为准。)

## 三、执行顺序(有依赖,不许乱序)

1. **12j 补 §一 入库(¥0)** —— 第二批去重 → 同工具同参数归一化 → `_input/` 编号 45 起 →
   `metrics.mjs` 四数 + 12i §二 甲色三档机器判 → **41→88 张合并分档表**。
   顶部 400 px 抽 4 张验无人脸(批量抽验,验一次不算)。
   **这一步不出分档表,12i 的 15 张就没法挑 —— 它是 12i 的前置。**
2. **12k(¥0)** —— `feedback-widget.js` + `ingest-feedback.mjs`,`build-report*.mjs` 统一嵌入,
   **第五轮对比包回炉嵌一次**。Safari + Chrome 各验一遍并截图。
   **第七轮对比包必须带反馈按钮才算交付**(12k §三 已定为硬规)。
3. **12i §三 扩样(≈¥9.60)** —— 三档各挑 5 张共 15 张,优先第二批(难类在那)；
   每张两版(腿 A / 腿 A′)+ 各出「亮度回贴全」一版(¥0)。
   ⚠️ 门槛 Y=90 / C=25 是起手值,**跑完按分布给店主看,不许自己调了再跑**。
   ⚠️ 三档任一档不足 5 张 → **停下来报**,不许凑。
4. **12i 补 §二 两支探针(P-A ≤¥1.6 / P-B ≤¥3)** —— 先查方舟 B 支不支持参考图;
   不支持就一句「不支持」收工,不要硬试。P-B 裁片精修按原文。
5. **12j 标注(¥0,可与 3/4 并行)** —— 先标 12i 挑出的 15 张 + 已有角例 8 张 = 23 张;
   第二批的红甲 / 裸甲 / 纯黑 / 暗光 / 立体钻优先。每 10 张报一次耗时。
   **标注耗时就是 D222 成本区间的第一个实测数**,回写 `D222_…md` 成本节。
6. **第七轮对比包** `对比包_第七轮.html` —— 按 12i §四 的五列 + 三档分组 + 黑甲 ⚠️ 标签,
   **不标 ✅、不下结论**,附空表给店主填。

## 四、必须遵守的既有纪律(别重犯)

- **不许自己下结论**:对比包只摆图不判优劣,店主的眼睛是唯一判据(12i §四)。
- **命名一律人话**(12e §六):`44_模型只去瑕疵+亮度回贴全+皮肤层冷白轻+磨皮.jpg` 这种,
  不许代号。反馈文件里的名字必须与图名逐字一致,否则她说「哪版」对不上。
- **每格带 [全图]/[手框] 范围标签**(12d 补三)。
- **三方对账**:调用次数 ↔ 磁盘出图数 ↔ 花费,三个数必须对得上,写进回执。
  版权拒稿记「拒稿」不补跑、不计费。
- **判据律**:写断言前先问一句「这条判据在缺陷存在时会不会照样绿?」会,就是废判据。
- **刀留痕律**:12k §二-3④ 那一刀(改坏本地存储键名 → 刷新后丢失 → 必须红)要留落刀凭据;
  日志里没有「刀已落」那行,按「刀没落」处理,不算验过。
- **造景律**:要店主验的每一条「该看到 X」,先自己造好景、自己测、附证据,不把造环境留给她。
- **真客照禁令**:D222 只许用这 88 张样片,**真客照一张不进**;不上传任何第三方标注平台。

## 五、回执

写 `handoff/12i_12j_12k回执_*.md`(仓库 handoff/,并镜像桌面夹),按**固定六项结构**:
①改了什么 ②在哪一层验的 ③同类扫尽 ④用什么数据验的 ⑤重复稳定性 ⑥残留与不确定;
末尾自评 L1–L5 哪条没做到、为什么。**另附三条围栏的自证**:
`git status --porcelain apps/` 为空 · 无 push / 无生产动作 · 本批花费与累计(≤¥12 / ≤¥72)。
