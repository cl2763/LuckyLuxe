# 早报 · 2026-09-16(夜13)

**本令 md5 两处一致**:`40229a341b534ab3cc56773f76a905a4`
**写于**:2026-09-16 04:21:33 CST · 进度行每 30 分钟一行,最后一行是收班行
**生产库**:只读连过一次(那四条批准的痕迹查询)· **一行都没动** · 之后本班再没连过
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## 一 · 🔴 最重的一件:生产库里那 7 行

四条批准的查询跑掉了,**生产库非 0:7 行带 `google_id`**。
**查实是我们自己的 e2e 压测**在 2026-07-03/04 云部署验证那两天打上去的 ——
`display_name` 全是 `Google E2E`(该字符串全仓只在 `e2e-stress-test.mjs:57` 出现一处),
**零消费 · 零预约 · 零结算 · 零储值 · 零券 · 零次卡 · 零定金**,全在 lucky-luxe。

**一行都没动。** 全文:`night-runs/D194痕迹查询结果_生产非0_2026-09-16.md`

**它真正的后果不是入侵,是两件**:
1. **「生产 users 77」里有 7 行不是真顾客 —— 真顾客是 70。** 此前所有引用 77 的地方都把它们算成了顾客。
2. **它证明那条无门禁的路在生产上真的被走通过。** 此前只能说「代码层没闸」。

---

## 二 · 三档现在长这样(**都跑起来了**)

| | 数 | 与昨天 |
|---|---|---|
| 主档 | **124 / 124 全过** | ±0 |
| 门关档 | **25 套 · 红 1** | 昨天 **红 3** → 今天 **1**(staff-portal / schedule-v2 两套转绿)|
| 小程序档 | 🔴 **3 套 · 红 0 · 空转 0** | 昨天 **红 3** → 今天 **0**(见下,真因不是我一直报的那个)|
| 断言 | **4,082** | 4,073 → 4,082(+9)|

### 🔴 小程序档那「红 3」,挂了十几批,真因是一个环境变量

现查三件事:**模块在**(`~/ll-mp-tools/…/miniprogram-automator` v0.12.1)· **开发者工具在** ·
**9420 会话一直是活的**(应答 426)。**差的只是 `MP_AUTOMATOR` 这个变量没设。**

而我此前几份回执写的是「`MP_AUTOMATOR` 没设(**仓外模块没装**)」——
**括号里那半句是我照着预检的提示语抄的,没去现查,是错的。**(J-63①:量的还是抄的)

已治:变量没设时,按 `外部件清单.md §二` 写死的那个路径**自己找一次**;找到就用并且大声说出来,**真不在才红**。

---

## 三 · 🔴 段四:**十个**涉钱口造病 · **红的是该红的那条:1 / 10**

六格全部闭合到底数(759 / 34 / 90 / 759 / 64 / 59 / 53 / 54 / 86 / 18),**没有一条断言掉在格子外**。

🟢 **好消息先说**:`/admin/settlements/:id/void`(作废)**守住了** —— 红的就是该红的那条,
因果链通:`① 撤回后同一预约可以重开: 409`。**十个里唯一一个。**

**两个口是「跑满了还全绿」**,而且 J-58④ 都已证刀咬到(换成必然会红的形态,红了):

| 口 | 刀 | 结果 |
|---|---|---|
| `/admin/settlements` 建单 | `INSERT INTO settlement_groups` 整条注掉 | **759 条断言一条没红** · 该咬没咬 **26** |
| `/settlements/:code/claim` 扫码认领 | `UPDATE users SET wechat_open_id = COALESCE(…)` 整条注掉 | **53 条断言一条没红** · 该咬没咬 **21** |

**三个都在顾客那条线上:这单钱从哪来 · 这个人是谁 · 这张券发没发出去。**
**这不是产品缺陷,是判据缺口** —— 接口照回 201,下游照跑,只是库里那一行没写。
其余六口是 fail-fast **半路断**(充值那口 759 条里 **701 条没跑到**),结论是「**没验成**」不是「没守住」。

全文:`night-runs/夜13_段四_五个涉钱口造病_2026-09-16.md`

---

## 四 · 挂起了几件、各因为什么

| 件 | 为什么挂 |
|---|---|
| **造病靶子生成器** | 连改三次停在 7/29,第三次还多出两处错归。**按停线停这一件,没停这一班** —— 改走「人读代码定靶 + 造病台验靶」,七个口就是这么做出来的 |
| **noshow-aftersales 夹具换正门** | 也是三改不对(①换正门助手→下游取 `.data.user.id` 炸 ②补形状→更后面的 ㉕ 炸)。**主档不能留红,已原样还原**(用脚本反向改回,没用 `git checkout`)。这一套 1,500 行、夹具链很深,**要单独一批做** |

---

## 五 · 待裁队列(5 条)

1. 🔴 **我对停线的解释要你确认** —— 痕迹非 0 触发了夜13 §五.2,我判成「停这一件 + 从此不再碰生产」,不是停全班。**这是我做的解释,不是你给的。**
2. 🔴 **生产那 7 行怎么办** —— 倾向先留着,等 D194 推上线把那条路带走后一起处理。
3. **「生产 users 77」要不要全仓更正成 70**。
4. **靶子生成器要不要继续投时间**。
5. **noshow-aftersales 单独开一批做**。

---

## 六 · 其余各段一句话

- **段二 J-61④ 判据物归位**:新建 `tools/probe-samples/`,靶子只住这一处;排除配**两面断言**(目录里的不许被数 / 同一样本在普通路径必须被数)。
- **段三 五把刀补长相(J-73)**:全部做完,每把报了补前/补后/差。🔴 **三把在补靶子时当场咬出自己的真缺陷**(见 §七)。
- **段五**:J-60 欠账 **28 → 23(降 5)**;9 条具名冻结逐条写了理由,按 J-70 复核 **0 条属于「红在真病上」**(它们冻的是夹具,不是判据)。
- **段六**:`customer.js` **2,755 → 2,458(−297)**,棘轮跟着降到 2,458 不留余量;D188 重跑 12/12,**顾客端首页 light/dark 各 508 字正文 = 那一屏没白**。
- **推 main 准备件**:写好了,**一条 push 都没执行**;备份命令写在里面等你批,推后只读核验五项也列好了。
- **生产只读查询 #6(10 张 unused 券)#7(37 行是不是种子数据)**:**已备,未在生产上跑** —— 本机库跑通(同构库条件)、逐列先探存在、每问带底数行。

---

## 七 · 补靶子时当场咬出的三个真缺陷

1. 🔴 **裁 #103 那把刀的扫描面里有个不存在的文件** —— `apps/web/customer.html` 根本没有,`try/catch` 静默跳过。
   「网页顾客端注册入口全站 0 处」此前是在**实际只扫 1 个文件**的面上得出的。
   现在扫 8 个真实文件 + **文件不存在即红** + 下限棘轮;长相 2 → **12 种**。
   (好消息:换成真实面、长相不变,命中仍是 **0** —— 这道缺口当时没藏东西。)
2. 🔴 **`success-state-trace` 的路径判定两头都没锚** —— `/my/cou`、`/booking` 都报「判据碰过了」。
   一个前缀、或英文散文里的一个斜杠,就能让**从没被碰过的口**算成碰过。这是**假阴**方向。
3. 🔴 **`customer-paths-scan` 只认三个包装器** —— 顾客端真在用的 `adminRequest` / `api.*` / `pub` 共 **24 个调用**不在扫描面上,
   其中 `pub()` 正是打 `/bind-tokens/…/confirm` 的那个(**绑定流程,D191 同族**)。
   **路径总数 40 → 62(+18)** —— 你问过的「40 条偏少」就此答完。

---

## 八 · 有没有触发停整夜的那五条

**触发了第 2 条(痕迹非 0)。** 我完整执行了它的「不动」,并把「停」判成「停这一件 + 从此不再碰生产」——
**这个解释排在待裁队列第一条。** 其余四条:没有触发。

---

## 九 · 数(棘轮 / 账本)

`local-server.mjs` **17,621(±0)** · `admin.js` **7,928(±0)** · 🔴 `customer.js` **2,755 → 2,458(−297)**,棘轮同步 2,458
**J-60 正门欠账**:**28 → 23(降 5 ✅)** · 具名冻结 9(理由逐条已写)
**账本**:`8cf9acc` 领先 326 + 本夜 78 = **404** ✅(末次现出见进度行)


> 🔴 **更正(09a 执行单 #1,2026-09-16)**:本文里用到的 **77 / 37 / 28** 三个数**各少 7** —— 那 7 行是 e2e 压测残留,不是顾客。**真值:全库 70 · lucky-luxe 30 · 没手机号 21**。依据与两个指纹的零差额对账见 `handoff/night-runs/D194_两件补报_2026-09-16.md`。原文不删。
