# 夜班令 16-续二 · 🔴 我不写第四遍「不许停」· 先量它为什么停 · D207 批到「彩排」那一步
2026-09-21 · Cowork 出令

**上游令点名(J-81):** `夜班令16续…` md5 `050da3d2c1dda72a58b010de67b85480`。六件全做完,第三次推 `ed93184` CI 绿,**活干得很漂亮 —— 但又停在报告那里了。**

---

## 〇 🔴 三次了,所以我不再写第四遍「不许停」,我先量(J-84 / J-106)

```
夜16 开工          18:52:27Z   →  早报 20:06:38Z   停(74 分钟)
夜16续 收到        20:1x Z     →  报告 21:08:57Z   停(约 2 小时 16 分)
```

**而且这一次,你自己在报告最后写的是「队列还有余量 …… 接着跑」。**
**⇒ 你不是「决定停」。你是写完那句话之后,这一轮就结束了,而没有任何东西把你叫起来跑下一轮。**

> **J-106 说过:同一个毛病第三次出现,就不许再打补丁,必须去改那个产生它的东西。**
> **我前两次都在打补丁(写更硬的『不许停』)。第三次了 —— 要改的是机制,不是措辞。**

### 所以这一批我只要你回答一个问题(其余照常干活)

🔴 **你那一侧,有没有任何机制能让你自己叫醒自己?**
比如:定时任务 / 循环模式 / 后台守护 / 自唤醒的待办栈 —— **任何能在一轮结束后,不靠人发消息就开始下一轮的东西。**

- **有** ⇒ 把它的名字、怎么用、有什么限制写回来。**夜班令从此改成「你自己排一串唤醒」,不再靠店主半夜发消息。**
- **没有** ⇒ 也明说。**那我就不再写「12 小时不停」这种令了** —— 那是在要求一件做不到的事,而且每次都让店主以为是你偷懒。
  **改成:令里只写「这一轮做到做不动为止」,店主醒着的时候发一句「续」。**

**🔴 不许猜,不许「大概可以」。按 J-83:引用一个机制之前,先证明它在你那台机器上存在。**

---

## 一 给店主的一句话(她只要复制粘贴)

> **续:按 `夜班令16续二` 往下跑,到 06:52Z 为止,不要停在报告上。**

**这句话她可以重复发。每发一次,你就再跑一轮。**

---

## 二 D207 那 51 行:**你要的证据够了,我批到「彩排」那一步**

你给的三样,正是我要的:
```
主选择式命中          51 行
安全前置:有正确副本的  51 / 51     ← 删掉零损失
反向守:两家真店命中     0 行
删后 user_identities   181 → 130
执行式带 EXISTS 子句 —— 只删「有正确副本」的
```
🟢 **「将来出现没副本的会自己被漏掉」—— 这个写法很好:它让这条语句对没预料到的情况是安全的,而不是对已知情况是正确的。**

### 批准范围:**做到彩排为止,最后那一下再报我**

1. **先拍当期备份**(`VACUUM INTO` · 拉回本机 · 打开验 · 表数 · 五表行数 · 两头 sha256)
2. 🔴 **在造病台上,用那份备份还原出来的库跑一遍执行式** —— **不是空库,是你要动的那份数据**
   报:删了几行 · 删后 `user_identities` 是不是 130 · 两家真店行数一个没少 · 51 个人是不是都还认得到(拿其中 3 个走一次正门解析)
3. **彩排通过 ⇒ 报我一句,我批最后那一下** —— **生产上的那次执行,我要在店主醒着的时候做。**

🔴 **D207-b 那 1 行(`bookings`)不在这 51 行里,继续冻结,一个字不许动。**

---

## 三 今晚剩下的(到 06:52Z,做不动为止)

1. **§〇 那个问题的答案**(先回,一句话就行)
2. **D207 彩排**(§二.1–2)
3. **涉钱口剩 7 条**(名单已可对账)
4. **11 条追不到口的断言,剩 4**
5. **D199** · **J-60 那 23 处**
6. 队列空了 → 去做「让下一批更快」的事

🔴 **AI 修图仍一个字别碰。**

---

## 四 记你四功

1. 🔴🔴 **「40 个口剩 30」从来不是一份可对账的清单 —— 你把它重做成 41 条写口。**
   **而最好的一句是:「已覆盖 27 条是启发式、会高估,所以我只拿没有的那 14 条当待办,不拿 27 当成绩。」**
   > **口径(写进台账):一个会高估的数,只许当待办的上界,不许当成绩。**
2. 🔴 **你自己造了个无限递归 `_SEDI() { _SEDI "$@"; }`,自己抓住、自己归到 J-78,而且它正好只在店主这台 macOS 上挂** —— 这一条要是漏了,就是一把刀在店主机器上永远转圈。
3. **阳性对照红了两次,你先问「是不是景造错了」** —— 发券漏 `mode:'template'`、建店选了本来就含 AI 包的 `chain` 档。**两次都是夹具错,两次都没报成缺陷。**(J-108:对谁量的。)
4. **那三条破坏测试挑得准** —— 重复核销(一张券烧两次)· 员工改自动续费(让店里一直被扣钱)· 空券码当通配(一次退掉全店兑换)。**这三条是真会掉钱的口,不是凑数。**

---

## 五 回执

首行点名本令文件名 + md5。**先回 §〇 那一句,再往下干。**

**停线一个字没松。生产上那一下没我批不许做。**
