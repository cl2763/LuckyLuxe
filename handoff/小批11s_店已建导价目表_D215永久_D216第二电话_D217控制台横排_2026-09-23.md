# 小批 11s —— 北京店已建:导价目表 · D215 永久 · D216 第二电话 · D217 控制台横排

**出令时刻**:2026-09-23 · 接 `11r`(`d0ae780d20e41a9c91af88dfaca1a245`)

## §〇 店主三句
1. 北京店**定名「LUVIA 半径 (北京国贸店)」**,不再叫旗舰店。**全仓凡写「北京旗舰店」的文案/常量/手册,改成国贸店**(`seed-luvia-bj.mjs` 那个常量也改,它虽永不对生产跑但别留错名)。
2. 电话两个都可以 ⇒ **D216**。
3. 「首期」加永久 ⇒ **D215**;控制台右边大片空白、按钮叠着 ⇒ **D217**。

## §一 导价目表(店已建,11r §二 条件成立)
只读确认 `luvia-bj` 存在、`currency=CNY`、`timezone=Asia/Shanghai`、`stores.name` 是店主填的那个 → 走 D214 正门执行导入(28 行)→ 读回逐行比 CSV 三档价 + 时长 → 顾客端 `?store=luvia-bj` 只读实访价目页 → **截图一张**(L1 同款)。
🔴 店主填的地址在截图里被截断了,只读贴出 `stores.address` 全文让她认。

## §二 D215 · 永久
- 建店表单「首期」下拉加第四项「**永久(买断 / 内部店)**」⇒ 正门 `initialTerm='forever'` ⇒ `plan_expires_at = NULL`
- 租户列表每行加「设为永久」按钮(已是永久的显示「永久」标签不显按钮)⇒ 走现成 PATCH `planExpiresAt=''` 那条路(`local-server.mjs:12701`),**不另造**
- 判据:建店选永久 ⇒ `plan_expires_at IS NULL` 且 `daysLeft null`;按钮 ⇒ 同;阳性对照:选年付 ⇒ 非 NULL
- 🔴 **`luvia-bj` 现在是年付**,按钮上了店主自己点

## §三 D216 · 第二联系电话
- `stores` 加列 `phone2 TEXT`(幂等 ALTER,老库兜底);建店表单 + 商家后台门店设置 + 平台配置页三处各加一个「第二电话(可空)」框
- 顾客可见出口:门店信息页两个都显示;「一键拨打」默认 `phone`;**11j 那张出口普查表 31 处 → 按机制重扫,上限跟着改,`phone2` 也过占位词表**
- 判据:空 `phone2` 不渲染;两个都有时两行都在;占位值 `phone2` 被挡

## §四 D217 · 控制台横排 —— 🔴 图先出,我今晚出;你先做不需要图的那一半
**不需要图的**:`.main{max-width:1040px}` 放宽到 `min(100%, 1600px)`;租户表操作列 `white-space:nowrap`、按钮一行横排;三张 `max-width:600px` 的配置卡放宽到 900。**改完 L1 截图一张**(1920 宽)。
**需要图的**(等我):按钮分「常用 / 危险」两组、危险的(停用 / 转演示店 / 重置密码)收进一个「更多」;新建商家表单两行改成一行网格。**图没到不动。**

## §五 顺序
§一 导价目表(店主等着看)→ §二 → §三 → §四 不需要图那一半 → 11n 那扇门继续

## §六 追加(店主同日):「没看到 AI 包的开关」
它**在「套餐计费」页**(`platform.html:512`,每店:开试用 / 顺延 / 加量 / 关闭),商家管理页只有只读的「AI 包:已开通」标签 —— 店主在商家管理页找,自然找不到。
**并入 D217**:商家管理那一行的 AI 包标签改成可点 → 弹同一组四个按钮,**复用 `setAi()`,不另写第二份**。这一条不需要图,和 §四「不需要图的那一半」一起做。
