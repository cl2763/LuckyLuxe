# 小批 12j —— 店主批:44 张样片可当 D222 训练/验收集;标注开工(¥0)

店主 09-24 原话:「可以用」。范围**仅限这 44 张样片**(`31o_修图验证_2026-09-22/_input/` 01–44);**真客照仍按 D222 法务先决,一张不进**。

## §一 做(¥0,与 12i 探针并行,不抢模型预算)
1. 建 `tools/segment/annotations/`:每张一份 `序号.json`(逐片指甲多边形,坐标基于 `_input/` 归一化件的像素;字段:序号、片号、多边形点列、甲色档(浅/深鲜明/深不鲜明,与 12i §二 机器判并存,人眼判一列机器判一列)、备注(钻/蕾丝/猫眼/裸甲/红甲)。
2. 标法沿用已证成立的那条:**100% 裁片逐片手描 → 画回原图自核 → 自核图落 `annotations/自核图/序号.jpg`**。网格目测那条不许再用。
3. 顺序:先标 12i 挑出的 15 张 + 已有角例 8 张(共 23 张,与探针同源,数据立刻能用);其余 21 张排后。
4. 每 10 张报一次:片数合计、每张耗时、遇到的难标类型(裸甲边缘、红甲、透明甲)。**标注耗时就是 D222 成本区间的第一个实测数**,写回 `D222_…md` 的成本节,替掉「1.5–5 分/张」那个假设。
5. 数据只在店主机与仓库 `tools/segment/annotations/`;不上传任何第三方标注平台(D222 三条禁止照旧)。

## §二 判据
- 每张 json 片数 = 自核图上人眼数出的片数;不一致的标「待复核」不入集。
- 自核图 44 张齐才算标完;先交 23 张那批。
- `apps/` 改动 0;本批 ¥0。
