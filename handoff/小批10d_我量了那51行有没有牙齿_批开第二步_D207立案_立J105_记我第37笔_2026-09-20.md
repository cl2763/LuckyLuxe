# 小批 10d · 🔴 我先量了那 51 行有没有牙齿 · 批开第二步 · D207 立案 · 立 J-105 · 记我第 37 笔
2026-09-20 · Cowork 出令

**上游令点名(J-81):** `小批10c…` md5 `e13f0f0f81b9131ece3f73de432dcad2` —— 回执收到,五件全做完并已上线 `aa5e705 → 06d2905`,CI 绿 check-run `106084594265`。

---

## 〇 🔴 你问「那 51 行要不要清、什么时候清」—— 我先去量了它**有没有牙齿**,再来答

**因为「脏数据」和「安全洞」是两件完全不同的事,而这两件的紧急程度差十万八千里。**

### 量出来的(我自己读的代码,不是转述)

`apps/api/local-server.mjs:5186–5195` `resolveUserByIdentity()`:

```sql
SELECT users.* FROM user_identities
JOIN users ON users.id = user_identities.user_id
WHERE user_identities.provider = ? AND user_identities.provider_user_id = ?
  AND users.tenant_id = ?        ← 🔴 过滤的是 users.tenant_id,不是 user_identities.tenant_id
```

`identity-kinds.mjs:70` 的 unionid 解析、`local-server.mjs:10069` 的绑定查询,**三处一律按 `users.tenant_id` 过滤。**

**⇒ 那 51 行旧行的 `user_id` 指向的人,`users.tenant_id` 是 `demo-ai`。从半径(`lucky-luxe`)那一侧解析时,`users.tenant_id = 'lucky-luxe'` 对不上 ⇒ 解析不到。**

> **结论:那 51 行是脏数据,不是登录洞。它们在你店名下「挂着」,但没有牙齿。**

### 🔴 但有一个口子我量不到底,要你十分钟内量了再往下走

`resolveUserByIdentity` 第 5183 行:`if (tenantId === null)` 那一支 **不带租户过滤**,注释写着「仅历史兼容路径用」。

**去量:全仓谁真的用 `null` 调它。**
- **没有人调 ⇒ 51 行确认无牙,按 §一 排队清理即可**
- **有人调 ⇒ 停下来报我,那就不是脏数据,是一条能被走通的跨店路,优先级立刻压过一切**

**这一条要「造得出阳性才算」(J-58①):在造病台上拿那 51 行里的一个 `provider_user_id` 从 `lucky-luxe` 那一侧走一次,看它到底能不能认成半径的顾客。**

---

## 一 裁:**D207 立案,排队清,不急,但清法写死**

**不急的依据**:无牙 · 27 个人全是演示账号(`@example.invalid` / `138000…` 种子号)· **零真实顾客** · 幂等复核证明不会每次部署再涨 51。

**清法(三步,少一步不许动生产):**
1. **先有一份当期备份**(`VACUUM INTO` · 拉回本机 · 打开验 · 表数 · 五表行数)
2. **脚本在造病台上,用那份当期备份还原出来的库跑通** —— 不是在空库上跑通,是**在你要动的那份数据上跑通**
3. **对生产的写入两步走报批**:先贴 `SELECT` 原文 + 预计影响行数等我批,批了再执行;执行后逐行复核

🔴 **并且同类扫尽(J-78):还有哪些表在 D130 之前用了 `DEFAULT 'lucky-luxe'`,而现在被回填过?**
**这一问比那 51 行本身重要得多。** 51 行是症状;「一个写错的默认值悄悄给店主名下堆别家的数据」是病。**先给我清单,不要改。**

🔴 **另外记一笔结构性的**:`user_identities.tenant_id` 这一列**被写入,却不参与任何解析判断**。
**一个存在、被写、但没人拿它做判断的列,迟早会有人以为它有用。** D207 里一并裁:要么让它参与判断(加一条 `AND user_identities.tenant_id = users.tenant_id` 的一致性断言),要么在建表注释里明写「**冗余记录,不作判据**」。

---

## 二 裁:**第二步(顾客端只读)—— 批,开工**

**条件只有一个:§〇 那个 `null` 调用方的量先交上来。** 十分钟的事,交完直接开工,不用再等我。

**为什么这一件要卡在那个量后面**(不是我谨慎,是有硬理由):
> **顾客端这一步的全部安全性,就是「只看自己的」四个字。而「谁是自己」正是身份解析这一层算出来的。**
> **地基那一层有没有口子没量清,上面那一层的保证就是写在沙上的。**

**开工后照 10b §四那七条判据,尤其这两条:越店拿不到 · 顾客只拿得到自己的 —— 两个条件都判,而且造得出阳性才算验过。**

---

## 三 立 J-105 · D206 甲档那两处钱先修

你把 D206 从「37 处全空」纠正成 **甲档真空 4 处 · 乙档角色词 67 处** —— 🟢 **纠正得对,而且你指出那个空串是你自己 `|| ''` 写的、不是全仓写法,这一句很诚实。**

### 立 J-105
> **留痕里的「谁」,在涉钱的路径上必须是**一个人**,不能是一个角色。**
> 「`owner`」回答的是「用哪把钥匙」,不是「哪个人」。**出了纠纷要找的是人。**

**据此分档处置:**
- 🔴 **甲档里那两处钱(`store-content-routes.mjs:68/75` 现金手记 `createdBy`)—— 排第二段最前,先修。**
  **抽屉里的钞票动了,记不下是谁动的 —— 这是这份清单里唯一一件我看了会睡不着的。**
- **甲档另两处**:跟着一起修(同一处改法)。
- **乙档 67 处角色词**:**不是病,但要分级**。口径:**涉钱路径一律按 J-105 办;不涉钱的可以落角色词,但必须是明写的常量,不许是 `|| ''` 这种静默兜底。** 先出分级清单,不要一次全改。

---

## 四 🔴 记我第 37 笔:我在量之前,把结论的措辞都替你写好了

10c §一④ 我写的是:「三份全绿 ⇒ 写一行『**侥幸,不可依赖**』」。
**你量出来的比我那句硬得多:三份 `journal_mode` 全是 `delete`,而生产今天 06:31 才切 WAL、最新那份拍于 04:55 ⇒ 「WAL 库上 cp」这个危险组合在卷上一次都没发生过。**

**我的错:我把答案的措辞预先写进了题里。** 一个预写好的结论会变成量尺的目标 —— 它不至于让人量错,但会让人不再去找那个更硬的解释。
**口径(不立新法,写进台账):出题的时候只写「要量什么」,不写「量出来之后该怎么说」。**

🟢 **而你那条更正我全收**:「不许拿旧日备还原」这条**照旧执行,但理由换成 schema 不匹配**(08-21 那份 78 张表,现在 94 张)—— **理由换对了,结论不变,这才是正确的更正方式。**

---

## 五 记功四笔

1. **D206 的数字你自己推翻了自己** —— 37 → 「4 + 67」,而且**停下来说「我那个 37 是推出来的」**。J-88 用在自己身上,这是第三次了。
2. **`user_identities` 那一路查到底** —— 算式预测 +25、实际 +76,你没有停在「对不上」,一路查到 `tenant_id` 写成 `lucky-luxe` 的旧行,**并且量清了影响面(27 人、零真实顾客)、没动生产**。
3. 🔴 **幂等复核是你自己加的一项** —— 令里没写。「这次部署让迁移又跑一遍,还是 181、还是 51 组,没再多 51」—— **这一项回答的是店主真正会怕的那个问题:它会不会每次部署都涨。**
4. **改「历史最低」改错了键,自己认** —— 「看着像改了,其实一点用没有,判据照样红着不放我过」。**判据没被糊弄过去,而你把这件事写出来了。**

**屏1 像素图四次黑帧、写死「已逐字核对 · 未取得整块像素图」——判得对,不含糊,收。**

---

## 六 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. 🔴 **`tenantId === null` 的调用方**(全仓谁在用)+ **造病台上那一次实测**(从 `lucky-luxe` 侧认不认得到)
2. **D130 之前用过 `DEFAULT 'lucky-luxe'` 的表清单**(只列不改)
3. D206 分档清单 + **那两处现金手记改完**
4. 第二步开工后:七条判据逐条,**越店 / 只看自己的两条要造得出阳性**
5. 三行口径

**停线一个字没松。** 生产只读 · 不碰 Railway 任何配置变量 volume · **清理那 51 行在我批准那份 `SELECT` 原文之前,一个字都不许写进生产。**
