# 小批05n 补二 — 店主拍板:旗舰店(`lucky-luxe`)币种改 **CNY**、时区改 **Asia/Shanghai**(与补一地址一起做,先报影响再写)

> 🚫 **已作废(店主 2026-09-07 改为新开 `luvia-bj`)** —— 旗舰店 `lucky-luxe` 保留加拿大口径(CAD / America/Toronto / 现地址 / 历史 1,432 张预约)**一行不动**;北京口径改由新店「LUVIA 半径美甲美睫 · 北京旗舰店」承担,见 `handoff/小批05o_…_2026-09-07.md` §一。本件**不删**,留作当时的分析与影响面存档。


> 2026-09-06 · 店主原话:「币种一起改吧,然后时区也变成北京的」· 与补一同一次手令、同一次备份

## 一、改什么(一处真相)

| 库 | 表.列(lucky-luxe) | 现值 | 改为 |
|---|---|---|---|
| 4128 本机 / 4310 沙箱 | `stores.currency` | CAD | **CNY** |
| 同上 | `stores.timezone` | America/Toronto | **Asia/Shanghai** |
| 同上 | `tenant_kb_facts.currency`(废弃键,D140) | CAD | **不改**(读口已断;上线批清) |

## 二、先报影响,后写(这两列牵一发动全身)

写之前回执先交一张「影响表」,我看过再动 4128;沙箱可先预演:

1. **币种只是标签,不换算数字**:`deposit_config`(fallback 5000 分 → 显示 ¥50)、价目表、加项、会员卡面额、历史订单金额——**数字原样、单位从 CAD $ 变 ¥**。这些数字对不对是店主的事(§三 问她),Code 不许自作主张换算。判据:改前后 `deposit_config`/价目所有 `*_cents` 逐字节不变,只有货币符号变;顾客侧 `/store/deposit-policy` 与 AI 答的定金都是「¥50」不再出现「CAD」。
2. **时区**:先报 `bookings`/`booking_slots`/`booking_drafts`/`reminder_tasks`/`business_hours` 里时间是**本地串还是 UTC**;每张表列出「换时区后这行的含义变不变」。已有预约若按本地串存,换时区后含义不变(那天那点);若按 UTC 存,显示会平移 12 小时——有这种表就**先停下报我**,不许改。提醒任务(`reminder-tasks`/`notify-scheduler`)按新时区重算 next_run,前后对照贴。`tenantTimezoneCache` 要清(重启即清,写进回执)。
3. **判据**:`GET /store` 顾客侧、AI 答营业时间/可约时段、后台排班 三处同一时区;造一条「明天 15:00」预约 → `/availability` 与 AI 报的都是北京 15:00;「多伦多 / Toronto / CAD」在旗舰店对外文案 0 命中(与补一同一把白名单刀,加两个词)。
4. **程序**同补一:快照 + 库文件备份 `pre-address-currency-tz-<时间戳>` + 沙箱预演 + 前后对照,只许 `stores` 1 行(3 列)+ `tenant_kb_facts` 1 行 差异,提醒任务表允许 next_run 重算(逐行列出)。

## 三、Cowork 问店主的两句(答了再写 4128;沙箱可先预演)

1. 价目、定金现在库里的数字是加币口径(定金 50、各项目价格)——**换成人民币后数字照旧(¥50)还是你要重新填一遍价目?**
2. 小婕店(`jics-nail`)本来就是 CNY / 北京时区吗?现库 `stores.timezone` 我没查——Code 顺手报一下,不动。
