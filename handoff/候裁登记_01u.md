# 候裁登记(01u;做完不动刀,等你裁)

## 一 · 小裁3「老板补录过去时段」现状报告(先查后裁,一刀未动)

**答:后端能力在,前端没有入口 —— 从台面之外老板建不了过去时间的单。**

| 面 | 现状 | 证据 |
|---|---|---|
| **后端 `/admin/bookings/direct`** | ✅ **允许过去时段**(老板直排 `adminDirect` 跳过边界校验) | 现测:沙箱 lucky-luxe 排 2026-08-25(上周二,过去营业日)14:00 → **201 成功**;探针已清。另:日结套件「跨月补签」夹具本就在上月最后一天建单 |
| **同一后端仍拦的两件** | 休息日(D34 `REST_DAY`)、时段被占(409) | 现测:昨天(该店排班休息)→ `REST_DAY` |
| **网页台面** | ❌ 绿区从 `storeNow` 起画(乙案/时间诚实),过去时段不出「+直接排单」块 | today-board.js:74 `minStart = 后端 storeNow` |
| **网页 全部预约 / 日历** | ❌ **没有任何「新建预约」表单入口**(日历点日期=筛列表,不是建单) | 全仓搜 `新建预约/newBooking/createBooking` 零命中;`/admin/bookings/direct` 全网页只有 today-board 一个调用方 |
| **小程序** | ❌ 同样只有台面直排面板一条路 | orders/index 的 `tapFree` 是唯一入口 |

**结论**:**不能** → 按你的令列清单候裁,要不要开这个口。

**若要开(方案清单,等你勾)**:
1. **最小改法**:台面日期导航翻到过去日 → 台面当天的过去时段出「补录」块(与「+直接排单」同形制、同一条后端路由),块上写明「补录(事后记录)」,提交后单直接落 COMPLETED 待开单 —— 改动面=today-board 一处渲染判断 + 一句文案,双端同批。
2. **保守改法**:只在「今天」台面上,过去时段出一句同端提示条「要补录今天更早的单?点这里」→ 展开同一直排面板(同端捷径不算指路),不碰历史日期。
3. **不开**:维持时间诚实,补录走「现在建一张即时单」的既有心智。

**未决点(要你一并裁)**:补录的单该不该计入当日业绩/日结?(若该日日结已确认,补录会动已确认的账 —— 这条关账本口径,我不自己定。)

## 二 · 值日推通知:暂不推(照裁),登记候裁

- **裁定照录**:通道未通,现在推也只落记录;台面/员工端同位可见已够。
- **候裁条件**:微信/企微通道通了再议。
- **通道通了要接的面**(先记着,免得到时漏):`notify-scheduler` 的 `NOTIFY_TYPES` 加一类 `duty_assigned`;
  规则页第九张卡(默认关);模板变量白名单 `{技师}{日期}`;记录页三色照旧;
  跨端断言(《一份数据两端渲染律》)——网页改模板 → 小程序记录页跟着变。

## 三 · 上批遗留候裁(仍未裁,不催)

- 《UI 组件规范表》二形法本批已终裁 ✅(此条销)
- **D96 日期筛默认今天**:本批裁④「排完自动跟去」已收口 ✅(此条销)
- **可点的内容卡算哪侧**(自走查撞出):顾客端 `.recommend-card`/`.quick-item` 整张卡是 button 但装图文价。
  本批按「装内容=8px 圆角矩形」处理未动;要另裁一行改。
- **徽标算不算「能点的」**:`.member-code-chip` 一类展示型徽标,二形法没写到 —— 队尾 UI 总审批时一并裁。
