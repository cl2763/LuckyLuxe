# 小批03y · D121 验收(2026-09-03)

## 〇、亲核

`b4ef86d`;`demo-mark.mjs` + `test-demo-mark` 8/8;基线 85 套 2,849(+10 = 新刀 8 + 结构一致性刀补 2);`local-server` 18,242 ≤ 18,243。本机库 4128:`bookings`/`settlements` 两列 `demo_seed` 都在、**标记行 0**(fail-closed 朝真实),行数 1,432 / 269 与批前一致;沙箱 97 条 `seed-bigdemo` 一条查询整批认出、378 条存量未标(存量不追溯,靠租户 kind + 时间戳,合理)。**D121 收。**

三处现测才跑通的教训都对,尤其第二处:**「先 ALTER 后建表」在老库上看不出来、全新库上才炸**——回归的临时库替你抓的,这就是为什么全量回归每批必跑。两把被波及的判据修法也对:锚「键真传了」不锚字面量尾巴;判据不许只认自己见过的写法。

## 一、一处补:「未动须有证」的对照表加列数

这批本机库**行数零差异,但结构变了**(启动迁移在 4128 加了两列)——这是产品正常路径,不是事故;但回执写「87 张表逐表零差异」只说了行,没说列。快照脚本每表再记一个列数,对照表出「行 / 列」两栏;结构变了就写「+2 列(启动迁移,D121)」。以后谁改了 schema,交齐时自然露出来。

## 二、顺序不变

事务验回滚 9 处 → D109/D112/D113 只答 → 拆档迁移脚本 + 沙箱彩排 → AI 复测 12 场景 → 结案交齐 → 主页重画批。我这边同时画 S5 通用设置页与多单串联流程图。
