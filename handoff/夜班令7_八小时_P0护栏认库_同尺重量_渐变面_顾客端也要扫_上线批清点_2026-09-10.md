# 夜班令 7 · 八小时(2026-09-10 夜)

> **接着 06a 往下做,不重开。** 06a 还在跑就先跑完它,回执照 05z 那份的结构写。
> 每段做完**当场提交**(一段一个提交),段与段之间**不停**;待裁写文件跳下一段。
> Cowork(我)每小时醒来核一次:核库、核图、核仓,不照抄回执;有待裁就裁并写小批到两处。

## 〇、段序(按重要性排,前面的先做完再往下)

| 段 | 事 | 交齐 |
|---|---|---|
| **0** | **把 06a 做完**(同尺重量 + J-39 + 乙档 +52 分解 + 渐变面棘轮 + 乙档棘轮 + `tenant_settings` 查清) | 同尺两个数(**带刀提交号**)· +52 分解 · J-39 造病 · 两条棘轮底数 · `tenant_settings` 写入点 |
| **1** | 🔴 **P0:护栏认库不认名** | 见 §一 |
| **2** | 🔴 **顾客端也要扫**(这一片从来没扫过) | 见 §二 |
| **3** | 渐变面取像素:**先把「屏在图上哪一块」做对** | 见 §三 |
| **4** | 乙档合同图配色表 → 交我裁 | 一张表:选择器 · 前景 · 背景 · 比值 · 用在哪几页 · 属于图里哪个令牌 |
| **5** | **上线批清点**(只列不动生产,**不推 main**) | 见 §四 |
| **6** | 收尾:全量回归 · 夜班总结 · 待裁 · 三库写库自报 | 照旧 |

## 一、段 1 · P0:护栏认库不认名(这一段做不完就不许往下走)

现查(Cowork 亲查):**4310 与 4128 的 `/health.dataScope` 都是 `live`** —— 4128 那份库装着**加拿大店的本机镜像**,所以「只许写 test 域」这道护栏对**最不能碰的那个库**同样失效。

1. **`dataScope` 分开**:4128 = `local`、4310 = `sandbox`(名字可以另定,但**两台不许同名、都不许叫 `live`**);预检加一条:两台的值必须分别等于约定值,不等就红并**把两台的值一起打印**。
2. **护栏改成认库的绝对路径**:允许写的白名单里只有沙箱库与临时库;判断**不许只看 `dataScope` 这个字符串**。
3. **造病**:把某个套件的目标库指到 `local-data/lucky-luxe.sqlite` → 护栏必须**拦住并点名那个库的绝对路径**;去掉护栏 → 红。
4. **写库自报**照旧三库分开写。
> 这一段落地之前,「**写 4128 既有行停**」这条停线不解除(D185 §二 继续等批)。

## 二、段 2 · 顾客端也要扫(全站不等于全站)

05z 那把对比度刀扫的是**商家后台 13 页**。**顾客端一次都没扫过** —— 而顾客端是**外人**看的,比后台更不能出「白字压浅底」。

1. 把 `tools/contrast-sweep.mjs` **对准顾客端**:网页顾客端所有页 + 小程序顾客端所有页(小程序那侧用你已经会的「组件里量出来的值」那条路,量不到的**如实报,不猜**);
2. 两档、浅深两模式,**照 J-39 打印刀提交号**;
3. **甲档必须清零**;乙档上棘轮;
4. 出**红榜全文**,并在抬头写清楚「这一轮扫的是哪些页、哪些页没扫到、为什么」。

## 三、段 3 · 渐变面 723 个字:先把定位做对

上一轮「整机 PNG 取像素」没做成,是因为**找不准手机屏在图上是哪一块**(自证宽高比 0.02 / 2.85,真值约 2.2),刀**拒绝取样** —— 这个处置是对的,保留。

1. **先解决定位**:不要靠猜边界。用**已知的锚**去定位(例如先截一张纯色背景的对照图做差、或用模拟器窗口的已知尺寸换算),**自证必须过**(宽高比落在真值 ±5% 内)才允许取样;
2. 定位过了,再做渐变面取像素:对渐变面上的文字,取它那一小块的**实际像素底色中位数**再算对比度;
3. **自证不过就照旧拒绝出结论** —— 这条不许改。

## 四、段 5 · 上线批清点(只列不动生产)

逐条写**现查依据**(不是凭记忆),每条给「现在是什么状态 / 上线前要做什么 / 谁做」:
推 main(约 170 个提交,**这一夜不推**)· 自托管 Fraunces woff2 + 域名白名单 · 访客身份串服务端签发(`/health.guestIdUnsigned` 现值)· 生产 `ALLOW_DEMO_ADMIN_LOGIN` 必须未设 · 两家真店密码**由店主亲手设**(不许经过你或我)· 31 张表去 DEFAULT 随部署 · 生产会话唯一索引 · 企微每店先填 `open_kfid` · 版本指纹灌 git 提交号 · 拆档迁移与身份表存量修复同批上 · 固定出口 IP · ICP / 微信支付。

## 五、停线(碰到就停,写进待裁,跳下一段)

- **写 4128 既有行停**(段 1 落地前不解除)· **不推 main** · **不碰生产** · **不看密钥文件**
- 四红线修法不明停 · 沙箱模型 **>600 万 token/夜** 停 · 回归红 **30 分钟**定位不到停 · **不许 sleep 轮询**
- **不许为了让自己变绿去松判据、抬棘轮、换尺子**(J-39);**不许拿修前的图给修后的结论作证**(J-38)

## 六、回执与自评(每段都要,收尾汇总)

六项结构(改了什么 / 在哪一层验的 / 同类扫尽 / 用什么数据验的 / 重复稳定性 / 残留与不确定)+ L1–L5 自评 + **没做的如实列**。
**所有带数的报告抬头打印产出它的刀的提交号**(J-39);**所有交付图带拍摄时刻**(J-38)。

## 七、给店主的早报(收尾时写)

写进 `handoff/早报_2026-09-10_*.md`,两处各一份,**用她的话说**:哪几件她看得见的变好了、哪几件还没好、明早她只需要做哪一件事。
