# 小批 12h —— 店主拍板:A 先上、C 立项、B 不做;12a/12g 关帐;12b 一期范围定稿

店主 09-24 原话:「我同意」(对「A 先上、C 立项,B 不值」)。停线不变,本批 ¥0,模型 B 一次不调。

## §一 关帐件(本批交)
1. `对比包_第五轮.html`(含四张掩膜叠图、无效件打标)+ `round6_…/` 分割叠图(三色,标「人给点/人给框」)。
2. **《12a_结论书_2026-09-24.md》** 桌面夹根目录,一页人话,五段:花了多少(按磁盘出图数 × 单价对账,≈¥58.6)· 模型能做/不能做(定律四条)· 分割试验三段结论 · 一期范围(§二)· C 立项要什么(§三)。
3. 分割环境 `tools/segment/venv/`(1.1 GB)**留在店主机不删**,C 立项要用;`README.md` 写清怎么激活、许可证在哪、为什么不用 ultralytics。

## §二 12b 一期范围(定稿,只写不做;开工另令)
**做**:
- 模型 B 口令 = 腿 A′ 四块(【绝对不改】【甲面 深/浅】【手部皮肤·只去瑕疵】【曝光 A/B】),纯文本无 `*`(D220 关),自检:矛盾对 + 黑名单 + 顺序。
- 分桶:手框均亮(曝光维)× 甲面色调(深/浅,由甲面区均亮定,一期用「手 bbox 内非肤色块」粗估,不准就默认浅甲并记日志)。
- 亮度回贴:默认 **100%**(LM-YC:原图的光和色 + 模型细节),商家可选 50 / 0。
- 比例:原图 / 3:4 / 9:16,非生成式裁切(`crop-ratio.mjs`)。
- 贴回 PoC 里那套「差异最大区」一律不用。
- 版权拒稿 400:失败不计张、不重试、提示商家「照片含品牌/卡通元素,请换一张」。
**不做(挂二期,等 C)**:手部冷白/磨皮档位 · 甲面贴回 · 换背景 · 1:1 / 16:9。
**产品文案不许承诺**:暗片不提亮 · 磨皮 · 美白 · 换背景主体不变。
**开工前置**:D217 AI 包开关(平台后台三态)· 生产服务器纯 CPU 单张耗时(模型 B 是 API 调用,本地只跑回贴与裁切,应 < 1 s,要实测)· AI 包计费口径(每张按 B 单价 ¥0.32 + 加成,店主定)。

## §三 C 立项:专用指甲分割模型(D222,只立不做)
- 需要:标注数据(店主的 44 张 + 后续真客照,每张逐片指甲多边形;训练集数量以 D222 立项文档为准(800–3,000);验收集三档各 ≥ 20)· 训练环境(GPU,店主机 mps 或租)· 许可证可商用的底模。
- 先决:店主同意用真客照做训练数据的合规口径(顾客授权条款 → 法务件,先问再动);**在此之前一张真客照不许进训练集**。
- 交付判据:红甲/裸甲/浅甲三档各 ≥ 20 张测试集,逐片 IoU ≥ 0.8 占比 ≥ 90%。
- 成本估算由 Code 在结论书 §五 给区间(标注工时 / GPU 小时 / 时间),不许给单点数。

## §四 队列(12a 关帐后回到主线)
D217 商家管理横排(按 09-23 设计图)→ 12b 一期开工(另令)→ D218 资料库 → D219 顾客端会员价 → D215 永久 → D216 第二电话 → 11n prelaunch-wipe(店主亲点)。
