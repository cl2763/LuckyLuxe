# 回执 · 小批 02s —— 四套 AI 客服现身(覆盖面 70/70)+ D103③ 文字重叠复量

## 裁定一 · 那四套:只改一个助手,断言逻辑与 matrix 一行不动

您说的第三条路成立 —— 而且比我想的还轻。四套的断言全部走各自的 `assert(condition, message)`
助手(`working-memory` 56 处 / `silent-handoff` 12 / `human-handoff` 13 / `after-sales-handoff` 25),
所以**只改这一个函数**就够了:

```js
let asserted = 0
function assert(condition, message) {
  if (!condition) throw new Error(message)
  asserted += 1
  console.log(`ok ${asserted} - ${message}`)     // ← 只多这两行
}
```

**106 个断言点一行没动,matrix 66 项一行没动。** 收尾行按您说的带上数字:
```
[working-memory] all regression checks passed(55 项断言)
[silent-handoff]  all regression checks passed(11 项断言)   ← 它原本连 all-passed 收尾行都没有,补了同形制的一句
[human-handoff]   all regression checks passed(12 项断言)
[after-sales]     all regression checks passed(24 项断言)
```

**数字对得上**:55/11/12/24 = **102 条**,与 `grep -c 'assert('` 的 56/12/13/25 各差 1 ——
差的正是 `function assert(` 定义那一行。**这是预判与实跑的对账,不是"跑出来多少算多少"。**

**覆盖面 66/70 → 70/70**,白名单随之从 4 收到 **0**:
```
[断言增长] working-memory 0 → 55(+55) | silent-handoff 0 → 11(+11) | human-handoff 0 → 12(+12) | after-sales-handoff 0 → 24(+24)
[断言基线] 合计 2641 → 2743(+102);[基线已更新] 涨的部分自动收进基线
[覆盖面] 基线真正盖住 70/70 套;0 套打不出 ok 行、尺子看不见;白名单在册 0 套
```

### 这一改顺手咬出我自己判据的两处病

1. **⑤ 的名字过期了**。02r 那版叫「零断言白名单棘轮 ≤ 4(**这四套的断言基线盖不住,待改造成 ok 行**)」——
   改造完这句就不成立了,**名字不改就是判据在说谎**。改成「现为空;只减不增,再进新成员要店主点头」。
2. **[覆盖面] 数的是白名单成员,不是事实**。白名单清空后它会永远打「N/N 全覆盖」——
   造病时 `working-memory` 明明瞎着,它照样报 **5/5**。这是「判据看着在守其实没守」同族第三案。
   改成数**真的 0 条的套件**,再造病:
   ```
   not ok 3 - ④ 零断言套件白名单式 :: working-memory
   [覆盖面] 基线真正盖住 4/5 套;1 套打不出 ok 行、尺子看不见(working-memory);白名单在册 0 套
   ```
   **这一次它如实说了。**

### 造病验红(④,三步)
```
第1步 造病  把 test-working-memory.mjs 的 assert 助手里那行 console.log 注掉(等于退回改造前)
第2步 跑刀  ❌ not ok 3 - ④ 零断言套件白名单式:…打不出 ^ok 行的必须在白名单内 :: working-memory
第3步 恢复  cp 还原,cmp 字节一致 → ✅ 回绿
```

## 裁定二 · LUVIA 那 12 个文件:**一律未动**

不入库、不删除、不移动、不改名,原样留在工作区等您处理。本批提交是逐路径 `git add` 的,没有 `-A`。

## 二 · D103③ 文字重叠 —— 复量结论:**D120 带走了,销账**

**量法**:不看截图靠眼,逐盒取 `offset`/`size`,算**相邻元素的垂直间隙**,负数=重叠。
两态各量一次(有图 / 占位),沙箱 4310 真页。

| | 有图态 | 占位态 |
|---|---|---|
| 卡片 | top 349 / 高 187 / bottom 536 | top 349 / 高 187 / bottom 536 |
| 图·占位宿主 | top 358 / 高 **97** / bottom 455 | top 358 / 高 **97** / bottom 455 |
| 项目名 | top 465 / 高 22 | top 465 / 高 22 |
| 价格行 | top 492 / 高 35 | top 492 / 高 35 |
| **宿主 → 项目名** | **+10** | **+10** |
| **项目名 → 价格行** | **+5** | **+5** |
| **卡片 → 下一节标题** | **+16** | **+16** |
| **价格行 → 卡片底** | **+9**(不溢出) | **+9** |

**判定:两态零重叠(所有相邻间隙 ≥ 0)。**

### 两处必须交代的取证细节

1. **造态自证第一轮就把我拦住了**:`.recommend-card` 命中 **2** —— 因为**美睫那一节也有卡**。
   与 02m 那次「`.ph-box` 量到另一张卡」同形状。加界定(同时清空 `recommendedLash`,全页只留 1 张)后才量。
2. **两态每个数完全相同 → 按您的口诀先查测量**。虽然"全等"正是 D120 修好的预期,
   但也可能是 setData 根本没作用到渲染层。加**互斥自证**:
   ```
   [互斥自证] 有图态: .ph-img 命中 1 · .ph-box 命中 0
   [互斥自证] 占位态: .ph-img 命中 0 · .ph-box 命中 1
   渲染确实两样: 是
   ```
   **渲染确实换了,布局却全等** —— 这才是"D120 带走了文字重叠"的证据,不是"数字碰巧一样"。

### 真截图两张(附件)
`handoff/attachments/D103三_文字重叠复量_有图态_2026-09-02.png`
`handoff/attachments/D103三_文字重叠复量_占位态_2026-09-02.png`
占位态图上可见:占位框(相机图标 +「还没有图片」)→「演示·经典单色」→「CAD $168」→「90分钟」→
下一节「人气美睫」,**层层不压**。

**结论:D103③ 文字重叠随 D120 闭合,销账。** D103 剩余的「服务卡排版」我尚未复量,下一步做。

### 顺带的现场观察(不动,只记)
这两张图同时照见了**已在册、排在后面的几条**:D108 两枚死口(「门店地址待补充」旁仍挂「导航·复制」、
「门店电话待补充」旁仍挂「一键拨打」)、D110(「技师作品」卡的字母 P 灰圆头像位)、
D112(中文态仍挂 `Popular nail` / `Popular lash`)。**按序处理,本批不动。**

## 回报固定六项

1. **改了什么**:四套 `assert` 助手各加两行;`silent-handoff` 补收尾行;基线刀白名单清零 +
   ⑤名字改准 + 覆盖面改数事实。**产品代码零改动。**
2. **在哪一层验的**:真回归运行层(全量 70 套)+ 小程序**真页盒模型测量层**(offset/size)。
3. **同类扫尽**:「判据自述过期」这一类,本批扫了基线刀与棘轮刀全部 11 条判据名,只有⑤过期,已改;
   「覆盖面数白名单不数事实」这一类,两把刀各查一遍,棘轮刀数的是实扫文件数(没这个病)。
4. **用什么数据验的**:反例(注掉 ok 行 → ④必红)+ 两态互斥自证 + 界定后的单卡现场。
5. **重复稳定性**:复量脚本连跑 3 次(界定前 1 次被自证拦下、界定后 2 次),数字一致。
6. **残留与不确定**:
   - D103 的「服务卡排版」那一症**尚未复量**,下一步做。
   - 复量脚本目前在 scratchpad,**没有做成常驻刀** —— 要不要把"两态零重叠"钉成判据,请您裁
     (我倾向要:它是 D120 的行为层后果,尺寸刀守的是 97,重叠刀守的是间隙 ≥ 0,两层不同)。
   - LUVIA 12 个文件未动,等您处理。

**自评 L1–L5**:L1 末端(真页盒模型 + 真截图)· L2 同类扫尽(判据名过期一类全扫)·
L3 无图不结案(有您 02j/02k 定的"占位必须长成那张图的形状"作标准)· L4 反例数据(注掉 ok 行造病)·
L5 重复稳定(3 次一致)。**全过。**

---

**棘轮数字**:点名钉死 `local-server.mjs 18,243/18,243` · `admin.js 8,552/8,552`(均持平,本批未动产品代码);
现状冻结候拆 `customer.js 2,811` · `me 638` · `settlement 929` · `orders 829`(均持平)。

**断言数**:基线口径 2,641 → **2,743(+102)**;新尺(全日志纯 `^ok`)2,646 → **2,748**;
旧尺(绿行)2,689 → **2,791**。增量全部来自那四套现身,**零新增断言逻辑**。

**全量回归**:✅ **70/70 套件通过**(日志 `handoff/回归日志_2026-09-02s_70套件.log`)。
**清场留痕**:见下。**生产库未动 · 本机库未动**。**未推 main**。
