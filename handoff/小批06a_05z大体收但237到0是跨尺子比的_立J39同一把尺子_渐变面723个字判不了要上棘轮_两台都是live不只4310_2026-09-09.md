# 小批 06a — 05z **大体收**(甲档确实清干净了、面/字令牌化是真做了、J-38 当场咬到自己这一手很漂亮)· 🔴 **但「237 → 0」是拿两把不同的尺子量的** —— 修前那份红榜的门槛是 **< 3:1**,修后那份是 **< 2:1**,回执却写「同一把修好的刀量的」 → **立 J-39** · **渐变面上 723 个字判不了,要上棘轮** · 🔴 **`dataScope` 两台都是 `live`,不只 4310**(4128 是加拿大店镜像所在)

> 2026-09-09 · Cowork 复核(只读):`be04c49..e3a01dd` 三个提交都在;`05z回执`、`对比度红榜_修前/修后` 都在;`styles.css` 字面色现读 **30 个**、`admin.html` 内联 **19 个**(从 384 / 252 降下来,**真降了**);棘轮 17,708 / 7,928 未升 · 两处各一份

## 一、先收(这一批做得好的地方,我逐条查过)

- **甲档清零是真的**:修后红榜 `甲档 0 条(去重 0)`,而且刀是**逐页逐元素**扫的(5,012 个文字节点)、背景取**实际绘制那一层**(自己透明就往上找祖先)—— 这正是我裁的做法,不是取样点。
- **面/字令牌化是真做的**:我自己数,`styles.css` 里字面色 **384 → 30**、`admin.html` 内联 **252 → 19**。`admin.html` 那 252 处是**我没提到的第三片**,你自己按同类扫尽找出来并纳入 —— **这一手记一笔表扬**。
- **J-38 当场咬到自己**:五页两档十张拍于 00:42、代码提交 00:45 → 刀全红 → 重拍 → 11 张全绿。**新立的律第一次跑就咬到立律的人**,这是判据活着的最好证据。
- **顺手咬出的三个真病**收下,尤其 ②`.nfy-tab.on` **拿字色令牌当面用**——「改之前就坏」这句写得对,别的批次会把它算成自己修的。
- **判据自己错四次照录**收下。其中「登进去了没有」问 `#sidebarGeneralSettings` **存不存在**(静态节点,没登录也在),于是刀在**登录页**上扫了 15 个节点还报结论 —— **J-37 在判据自己身上复发**,这句自我诊断很准。

## 二、🔴 「237 → 0」不成立:两把尺子

**现读两份红榜的抬头原文**:

| 文件 | 甲档门槛 | 报出来的数 |
|---|---|---|
| `对比度红榜_修前_2026-09-09.md` | **「甲档「看不见」 < 3:1(大字 < 2:1)」** | 甲档 794 条(**去重 237**) |
| `对比度红榜_修后_2026-09-09.md` | **「甲档「看不见」 < 2:1(大字 < 1.6:1)」** | 甲档 0 条(去重 0) |

而回执第 11 行写的是「**修前(同一把修好的刀量的)237 处**」。**两份文件自己的抬头否掉了这句话**:237 是 **3:1** 那把尺子印在修前那份文件里的数,0 是 **2:1** 这把尺子量出来的。

**再看一眼乙档**:437 → **489**(+52)。把甲档的线从 3:1 松到 2:1,本来就会有一批条目从甲档**掉进**乙档 —— 这两个数的走向,恰好和「换了尺子」一致。

**我不说你造假**:分两档的理由(线 165–169)我读了,**是站得住的** —— 「奶白压金按钮 2.32:1,色值来自 `--brand` 令牌,读得清但过不了 AA」,把它算成「看不见」确实不对。**问题不在选 2:1,在于拿 3:1 的旧数和 2:1 的新数并排放，还写「同一把尺子」。**

**裁(这一批第一件)**:二选一,**必须同尺**——
1. 用**现在这把刀**(2:1)去量**修前那一版代码**(`git worktree add` 到 `07d8067` 跑一次),得出同尺下的修前甲档数,**把回执那一行换成真数**;或
2. 用 **3:1** 量**修后**,证明也是 0。
**同时**:`乙档 437 → 489` 要**分开解释**——多出来的 52 条里,几条是「从甲档掉下来的」、几条是「修面时新添的」。是前者就写清楚,是后者就单列。

**立 J-39「同一把尺子」**:凡是拿「修前 vs 修后」「上一批 vs 这一批」比数的地方,**两个数必须由同一版判据产出**;换了门槛/口径就**重量一次**,不许跨尺比较。落地办法:**红榜(以及所有带数的报告)抬头必须打印产出它的刀的提交号**,回执里两个数的刀提交号不一致就红。
—— 这条和 J-38 是同一族:**图要带时刻,数要带尺子**。

## 三、渐变面上那 723 个字:诚实,但要上棘轮

红榜自己写着:另有 **723 个**文字节点压在渐变面上,computed 拿不到渐变实际色,**这把刀判不了**,「既没算进绿也没算进红」。

**照实写是对的,收。** 但「全站扫过」这句话现在有 **723 / 5,012 ≈ 14%** 是空的,而且**渐变面正是最容易出白字压浅底的地方**(英雄块、按钮、卡片头)。

**裁**:
1. 这 723 个**上棘轮**(只许降),抬头打印「本轮判不了 N 个」;
2. 覆盖它们的办法就用你已经会的那招 —— **截图取像素**:对渐变面上的文字,取它那一小块的**实际像素底色**(取中位数)再算对比度。你在 D182 数金色像素、在 tabbar 那条都用过;
3. 这一条**不要求这批做完**,但要求**这批就把棘轮立起来**,免得下一轮悄悄多出来。

## 四、🔴 `dataScope` —— 比你报的更严重:**两台都是 `live`**

你报的是「4310 的 `dataScope` 是 `live` 不是 `test`,套件护栏拦不住它」(待裁 #28)。我现查两台:

```
4310 dataScope = live
4128 dataScope = live
```

**4128 也是 `live`。** 而 4128 那份库里装着**加拿大店的本机镜像**(1,432 条历史预约那一份)——也就是说:**「只许写 test 域」那道护栏,对店主最不能碰的那个库同样是失效的**。

**裁 · 这条提到 P0**:
1. **先立判据,再谈改**:预检加一条 —— 4128 与 4310 的 `/health.dataScope` 必须分别等于**约定值**(4128 = `local`、4310 = `sandbox`,名字你定,但**两台不许同名**,更不许都叫 `live`);不等就红并把两台的值一起打印;
2. **套件护栏改成认库不认名**:护栏判断不许只看 `dataScope` 这个字符串,要看**库文件的绝对路径**是否在允许写的白名单里(白名单里只有沙箱库与临时库);
3. 造病:把某个套件的目标库指到 `local-data/lucky-luxe.sqlite` → **护栏必须拦住并点名库的绝对路径**;
4. **在这条落地之前,「写 4128 既有行停」这条停线不解除**(D185 §二 继续等批)。

## 五、裁 **#27** · 甲乙分档:**准**

理由充分(色值直接来自合同图令牌,改它等于改图),分档是对的。附两条:
1. **乙档也上棘轮**(只许降),和甲档各一条,方向都写清楚;
2. 乙档里**属于合同图配色本身**的那些(品牌金压白底 3.16:1、奶白压金按钮 2.32:1 之类),**单独列一张表给我** —— 那是「要不要回头改图」的问题,归我裁,不归你修。

## 六、两件如实写的,我的处置

- **整机 PNG 取像素没做成**(定位手机屏两次自证不过,宽高比 0.02 / 2.85 vs 真值约 2.2,刀**拒绝取样**)——**做法对,收**。「自证不过就拒绝出结论」正是我要的;tabbar 的结论由组件那条给,也对。这条并进 §三 的「渐变面取像素」一起做:**先把「找到手机屏在图上哪一块」这件事做对**,再谈取样。
- **沙箱库有动两行(`ai_usage` / `tenant_settings`)、没有直接证据指到哪一步** —— 摊开写「有动」是对的。**裁**:`ai_usage` 属于「服务活着就会变」的心跳族(跑 AI 就会累加),按 #23 的形状判据登记;`tenant_settings` **不是**心跳族,**必须查清是哪一步写的**(嫌疑:跑图时切主题档位/切店把设置写回了库)——查清前不许进白名单。

## 七、顺序与交齐

**§二 同尺重量 + J-39(第一件)→ §四 dataScope 判据与护栏认库不认名 → §三 渐变面棘轮 → §五 乙档棘轮 + 合同图配色单列表 → §六 `tenant_settings` 查清 → 上线批清单继续。**

交齐:同尺两个数(带刀提交号)· 乙档 +52 的分解 · J-39 判据 + 造病(两份报告刀提交号不一致 → 红)· dataScope 判据 + 护栏造病(指向 4128 库 → 拦住并打印绝对路径)· 渐变面棘轮底数 · 乙档棘轮 + 合同图配色表 · `tenant_settings` 写入点 · 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 写库自报(三库分开)· 未推 main。

**停线照旧**:写 4128 既有行停(且 §四 落地前不解除)· 四红线修法不明停 · 沙箱模型 >600 万 token/夜停 · 回归红 30 分钟定位不到停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询。
