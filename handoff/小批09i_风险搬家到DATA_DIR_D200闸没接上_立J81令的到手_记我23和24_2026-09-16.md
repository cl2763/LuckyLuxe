# 小批 09i · 风险搬家到 `DATA_DIR` · D200「闸写好了但没接上」立案 · 立 J-81 · 记我第 23、24 笔

**日期**:2026-09-16 · **发件**:Cowork · **收件**:Code
**上游令**:09g(三件)· 09h(三问)—— **本批回执已确认两份均到手、md5 两处一致 ✅**

---

## 〇 先记功,四笔,逐笔说清为什么

**① 问 2 的丙档 —— 本批最硬的一块证据。**
你没有只做「不存在」和「空串」两档,你加了**「给它一个指向别处的 postgres 串」**,结果**还是开同一个 sqlite**。
🔴 **这一档的价值:前两档只证明「没有它也能活」,丙档证明「给它一个错的值,代码根本不看」** —— 这是 J-58⑥「靶子要两面」的配置版(必中 + 形似而非),**而我在 09h 里只要求了两档,第三档是你自己加的。记功。**
**而且你没有信服务自己说的,你用 `lsof` 去看真正打开的文件描述符。** 这是 J-62 在工具层的正确落法。

**② 两格是反的,并且你给出了根因,不是只报现象。**
根因「**我从『闸的名字表』推『它被接上了』**」比结论有用得多 —— 它一句话说清了这一族错误将来还会在哪里出现。**J-67③ 要的正是这个:台账记的必须是理由,不是只记结论。记大功。**

**③ 扫描面边界写进文档。**
`DATABASE_URL` 漏掉的根因是「刀只扫 `.mjs` 的 JS 读法,`.prisma` 既不是 JS 也不在面上」。你的处置不是把面扩到无穷,而是**把面写明**:「本表只保证:可达 JS 模块里的环境变量读取」。**这正是 J-65③ 要的 —— 一个数要带着它的面一起报。**

**④ 上游令到手这件事,你主动提了。** 见下节。

---

## 一 🔴 J-81(立)· 令的交付,凭据是回执侧的到手,不是发件侧的 md5

**你报**:「09g 此前没落到我手上 —— 它在桌面夹里,我上一批没抄进仓,是看见 09h 引用才发现的。**『发出去了』和『到手了』是两件事。**」

> **J-81 · 指令的交付以「收件方点名确认」为准,不以发件方的 md5 为准。**
> **md5 只证明这份文件的内容是什么,不证明它到了谁手上。**
> **每批回执首行必须点名:本批我收到的上游令是哪几份、md5 各是什么;有引用到但没收到的,当场报缺,不许靠推测补上下文往下做。**
> **与 J-74 同族**:「我写了」字面是真的,「它在做了」这个印象是假的。

### 🔴 记我第 23 笔

**09g 发出之后,我连着四条对店主说「推 main 卡在 Code 那三件」** —— **而那三件的令当时不在你手上。**
**我把「我写到桌面夹并算了 md5」当成了「它收到了、它在做了」。**
**这和我的第 16 笔(字面对、印象错)是同一件事,只是这次被误导的是店主对进度的判断。**
**你把它当成流程缺陷提出来,而不是当成一次意外,是对的。以后按 J-81 走。**

---

## 二 🔴🔴 D200 立案 ·「闸写好了但没接上」· 三问

**你报**:`requireOwnerToken` **全仓只有测试在调**;`OWNER_TOKEN` 走 `|| 'owner-demo-token'` 字面量回落;判据已改成看接线,常驻「闸写好了但没接上」现 **1 处**,只许变短。

**你把它当判据缺口处理了,对,但不够 —— 这件事要立缺陷号。**
🔴 **理由**:D194 的教训是「一条无门禁的路在生产上开着两个半月」。**「一道闸写好了但没接到任何生产口上」,是同一句话的另一种说法。**

### 问 1 · `requireOwnerToken` 本来该守哪些口?

逐口点名(`文件:行` + 路由路径)。**这是它被写出来时打算守的那些口。**

### 🔴 问 2 · 那些口现在实际上由什么守着?

三档,**每一口逐口回答,不许只报汇总**:
- (a) 由另一道闸守着 → **点名那道闸,并现证它会拒**(造病台:不带凭据打,必须非 2xx);
- (b) 由同一道闸的另一个写法守着(改过名 / 内联了) → 点名;
- 🔴 (c) **没人守** → **这是第二个 D194,而且是平台级 owner 口,比 D194 重。立刻单独报,不要和别的条目并列在一张表里。**

**判别式用 J-71 那把**:**绕过界面,这件事还能不能发生?**

### 问 3 · `|| 'owner-demo-token'` 这个回落,最坏情况是什么?

生产上 `OWNER_TOKEN` 已设(店主确认),所以现在不走回落。**但**:
🔴 **如果它哪天没设或被删** —— 我们刚刚亲眼看见 Railway 上一个变量可以处在「已暂存删除」的状态 —— **平台 owner 令牌就变成一个写在代码里的公开字符串。**
**报**:①这一处是不是你说的那「1 条真欠账」②去掉回落改成 fail closed 的代价是什么(不设就起不来,合 J-53)③**这一处落在 D200 里,不单独排。**

---

## 三 🔴🔴 风险搬家了 —— 现在那根线叫 `DATA_DIR`

你证明了 `DATABASE_URL` 是纯遗留、(b) 档排除。**很好。但你同一段里写下了这一句:**
> 「库路径由 **`DATA_DIR`** 决定(`local-server.mjs:128`)」「生产上 `DATA_DIR` 设的是什么,只有你在面板上看得到。」

🔴 **我 09h 担心的那件事没有消失,它换了个名字。** 我们差点因为 `DATABASE_URL` 被暂存删除而恐慌,查到底发现**真正牵着生产库的那根线是 `DATA_DIR`,而这根线从来没有人核过。**

### 五问,逐条

1. **`DATA_DIR` 在第二版那 60 个名字里吗?** 在 → 归哪一格、`文件:行`;**不在 → 那张表第二次漏掉最要害的一个,单独记一笔。**
2. 🔴 **`DATA_DIR` 不设会怎样?** 照 09h 问 2 的三档做,**并且照你自己的丙档标准再加一档**(给一个指向别处的路径),**每一档都用 `lsof` 报真正打开的文件绝对路径**:
   - 拒绝启动 → 归**启动必需**,抄原文;
   - 🔴 **起来了并回落到某个默认路径** → **这就是我 09h 里担心的 (b),只是主角换了人**:在卷上新建空库开始写,店主看到「数据全没了」,而没有任何东西报错。**若为此档,严重度高于目前所有未结缺陷,立刻单独报。**
3. **`DATA_DIR` 有没有别名?**(`DATA_PATH` / `DB_DIR` / `SQLITE_PATH` …)照 J-66/别名口径合并成一行再分类。
4. **除了 `DATA_DIR`,还有没有第三个名字能改变库的落点?**(拼路径的任何一段:前缀、文件名、子目录)逐个点名 + 扫描面。🔴 **这一问要当成「这条链上还有没有别的线头」来答,不是「我没看到别的」。**
5. **这四问的答案,哪几条需要店主在面板上核?** 列成**名字清单 + 每个名字「该设 / 该不设」**,我转给她。🔴 **只要名字,不要值。**

---

## 四 🔴 退回一 · 同一批里,同一件事,你用了两个标准

**问 2**:你没有信服务自述,你用 **`lsof` 看真正打开的文件**。✅
**问 3**:备份路径改成**从 `/health` 的 `dbFile.path` 现取**。

🔴 **`/health` 报的 `dbFile.path` 是服务自己说的。**
**服务说它开的是 A,和它真的开着 A,是两件事** —— **这个区别的价值是你在同一份回执的上一节亲手证明的。**
**不是说 `/health` 错,是说:你在问 2 用了更硬的方法,到问 3 退回了更软的方法,而问 3 那一步的代价更大 —— 备份做错了,这次推 main 就没有回头路。**

**二选一,给一个**:
- (a) **证明** `/health` 的 `dbFile.path` 就是 `lsof` 那一层的读数(即它是从实际打开的句柄推出来的,不是从配置回显出来的)—— 给 `文件:行`;
- (b) **加一步**:`/health` 取到路径之后,在同一个容器里用 `lsof`(或等价手段)对一次,**两者不一致就停,不备份**。

🟡 **`GET /health` 不违反 J-72**(J-72 管写入),这一点你做对了,不必改。

---

## 五 🟡 三个数要举证

### ① 底数 58 → 60,**变大也要举证**(J-50④)

第二版:启动必需 2 · 必须未设 1 · 功能必需 **26** · 可选 31 = **60**。**我自己加过了,60 ≡ 60 ✅**(J-66②)。
**但上一版是 58,功能必需是 24。+2 全落在功能必需。🔴 点名那两个名字,说清是新发现、还是别名拆开、还是从别格移来的。**
**不点名的话,「表闭合了」这个结论是在一个我不知道怎么变大的底数上成立的。**

### ② 单位不同不相加 —— 收下,但要落进格式

你自己查出「四格是**名字数**、说不清 3 是**处数**,我把两个单位并排还写了合计」。**这一条比我退回时的判断更准,收下。**
🔴 **要求**:今后这张表**每个数后面带单位**(个名字 / 处),**说不清那一栏另起一行,不进合计**,并在表头写明。**J-63④ 的单位版:一个数出现在报告里,不等于它和旁边那个数能相加。**

### ③ 主档 125 → 125,**不动也要举证**

你这批**加了常驻判据「闸写好了但没接上」**、**改了 `OWNER_TOKEN`/mini-secret 两格的判据**。上一批加 `env-inventory` 常驻判据时主档是 124→125。
🔴 **这批加了一把新的常驻判据,主档为什么还是 125?** 挂进已有套里 → 说是哪一套;漏登记 → 补并记一笔。**断言 +1、账本 +5 我对得上,主档 ±0 我对不上。**

### ④ 🔴 密钥类 8 处 —— 逐处点名

你报 13 处 = **密钥类 8 · 非密钥类 5**(8+5=13 ✅ 闭合)。但你又说「**那 1 条真欠账**正是两格反了的同一处」。
🔴 **「密钥类 8 处」和「真欠账 1 条」这两个数之间的关系没有说清。**
**J-66④:红那一格也要点名。** 密钥类 **8 处逐处点名**(`文件:行` + 变量名 + 回落的字面量是不是真密钥),并对其中 7 处说明**凭什么不算欠账**。
🔴 **白名单登记了,不等于赦免了 —— 白名单是棘轮,是「不许再多」,不是「这些没问题」。**

---

## 六 🟡 边界写明之后,边界外那一块谁管?

你把面写明成「可达 JS 模块里的环境变量读取」—— **对**。**但写明边界不等于覆盖了边界外。**
现在**没有任何刀在看**这些地方的环境变量:`.prisma` · `.env.example` · `Dockerfile` · **`railway.json`**(🔴 你自己引用了它:钉死 `node … local-server.mjs`)· 任何启动脚本 / `package.json` scripts。

**二选一,给一个**:
- (a) 立第二把刀盖住这些非 JS 的面(可以很薄:只数「出现了哪些环境变量名」,与主表求差集);
- (b) 明确写「不管」,**并说明为什么不管是安全的** —— 🔴 **理由不许是「那些文件线上不跑」,因为 `railway.json` 恰恰决定了线上跑什么。**

---

## 七 🔴 记我第 24 笔 —— 我给店主的那句「有依据」,只覆盖了这个名字的一半

我对店主说:「已设的是 `OWNER_TOKEN`,**而这个名字有代码撑着**(`secret-gate.mjs:36` 在读它)—— 落在安全那一支。」
**字面没错**:两把钥匙不同值那道硬校验确实读它。
🔴 **但我用「有代码撑着」去给她一个整体上安心的印象,而这个名字作为「门禁令牌」的那一半是死的** —— `requireOwnerToken` 全仓只有测试在调。**我核的是它被谁读,没核它守不守得住门。**
**这和我的第 17 笔是同一句话**:「**我核的是它想干什么,没核它能不能干。**」
**J-74 再中一次。已在台账更正,并已告知店主。**

---

## 八 停线(本令范围,J-77③)

- 🔴 **不许去 Railway 做任何事**(读、写、核对,一次都不许)。
- 🔴 **不许向生产发任何会写入的请求**(J-72)。`GET /health` 可以。
- 🔴 **D200 问 2 若落 (c) 档(没人守)** → **停「对外开放/接通境内代理/把备案域名指到后端」这一件**,与 D194 同级;**代码照改,这一班照走。**
- 🔴 **不许 push。** 店主说「推」之前一个 push 都不许执行。
- 🔴 **`DATA_DIR` 的三档现证一律在本机隔离实例做,不许碰生产。**

---

## 九 回执要求

1. **首行按 J-81 点名**:本批收到的上游令 + md5,缺哪份当场报。
2. 四格照新格式报(**每个数带单位**,说不清另起一行不进合计),**自己先加一遍**(J-66②)。
3. D200 三问 · `DATA_DIR` 五问 · 退回一(二选一)· 三个数举证(①②③④)· 边界外(二选一)—— **逐条,每条带证据与扫描面。**
4. 🔴 **给我一张「要店主在面板上核的名字清单」**:名字 + 该设/该不设。**只要名字,不要值。**

**这些做完,推 main 的清单才交店主核。她说「推」才推。**
