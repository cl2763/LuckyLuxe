# 小批03v · 红线三件验收 · 生产那一行两步走 · railway 通了顺带三查(2026-09-03)

## 〇、亲核

提交 `bc013cf`;写口闸 6742 `CUSTOMER_NOT_IN_TENANT`(比的是这张单要写进去的租户,对)、direct 口 15037 原有闸;读口按 `users.tenant_id` 连;`test-tenant-ownership` 5/5;基线 81 套 2,819;`local-server` 18,231 < 18,243;本机库对 `backup-D127-20260902-221038.sqlite` 逐表只差 bookings/payments/status_history 各 −5 = 闭包 15 行,账本零动,备份里跨租户行 5 → 现库 0;储值页「购买」按钮已无(剩的是注释),示例块 `wx:if="{{isDemo}}"`。**红线三件与死按钮,收。**

你更正自己 02b⑤ 的那段是对的,而且顺藤扒到 `users.tenant_id DEFAULT 'lucky-luxe'`——每个邮箱注册的顾客静默落进旗舰店——这比那五行要紧得多,改产品不改夹具,对。三道护栏各咬一次都是护栏对,记下。

## 一、生产那一行 —— 两步走,这一步仍只读

第一步(现在做,只读):把那一行的**非敏感字段**取出来报我——`bookings.id`、`bookings.tenant_id`、该顾客的 `users.tenant_id`、`created_at`、`created_by/source`、`status`、`notes`;以及它的闭包计数(payments 几行、provider 是不是 MOCK、金额;settlements 几行;账本几行;小记几行)。**不取姓名电话邮箱 openid**。
第二步(我看了第一步再批):若是走查夹具且账本零行,我另发批文按 03b 程序清(生产先备份文件再动);若像真单,不动,另议。**这一步之前生产一个字不写。**

## 二、railway 通了,顺带三件只读(各 5 分钟,回执只写结论)

1. D123:生产 `admin_sessions` 是否含那枚已作废的 `sess_…` 令牌——只写「含 / 不含」。
2. `STAFF_DEMO_PASSWORD`:生产是否显式设了该 env;生产是否存在会吃默认值的演示员工账号——只写「设/未设」「有/无」。两者皆惰性才把它归演示值,否则 D123 第二枚。
3. 生产 `users` 里 `tenant_id` 分布(按租户计数)——看 `DEFAULT 'lucky-luxe'` 在生产上吞了多少别家顾客;只写数字。

## 三、`DEFAULT 'lucky-luxe'` 的处置

默认租户与默认目标库同族:有默认值,打错了不报错。现在不重建表(SQLite 改列定义要重建,风险不值);**落一把静态刀**:全仓 `INSERT INTO users` 必须显式写 `tenant_id`,白名单式,例外写理由;去掉 DEFAULT 本身入上线硬门槛批。生产部署含 D126/D127 的版本也入上线硬门槛(生产现在跑的还是有洞的老代码,店没开门所以不急,但开门前必须到位)。

## 四、图的位置

`商家端主页重画_两端设计图_2026-09-03.html` 在桌面交接夹与仓内 `handoff/` 各一份(我刚复制)。**只读,不开工**——店主点头前它是草案。

## 五、顺序

§一第一步 + §二三件(只读,一起交)→ §三 静态刀 → 回 03t §二 逐条结案 → §三 AI 复测。
