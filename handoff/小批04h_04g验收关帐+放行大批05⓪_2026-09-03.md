# 小批04h — 04g 验收(`8392a74`/`ec5bbd5`:收,D137 关帐)· 放行大批05 ⓪ 对话全录

> 2026-09-03 夜 · Cowork 亲核于店主 Mac · 两处各一份

## 〇、亲核
- 本机库 / 沙箱库:`_tenant_fill` 触发器含 `lucky-luxe`/`COALESCE` **0 条**,含 `RAISE(ABORT)` **7 条**;`finance_targets.tenant_id notnull=1`;源码里开机「NULL 归旗舰店」七条 **0 处**;`/health.tenantNullRows` = `{}`、`tenantFallback` 0/0。
- 备份刀真跑了:两库各有 `pre-dropdefault-…`(21:13,开机路径,`finance_targets` 收紧触发)与 `post-dropdefault-…` 现状备份(21:12),字节与源一致(14,065,664 / 18,718,720)。
- 新套件 `test-tenant-fill-trigger` 判据 ①–⑤b(常量兜底白名单式 / 反向守 / 开机只报数 / 行为拒落行 / 正向守 / NOT NULL / 备份 + 日志);造病 W 记了「外键先拦」那一层并补关外键现测——归因干净。
- `local-server.mjs` 18,010;套件 91;未推 main **94**。

## 一、裁
- **D137 关帐**;`finance_targets` 收紧关帐;「重建型迁移必先备份」入常驻。
- 上线批加三数(生产随部署翻面):触发器 7 条仍含常量兜底 · `finance_targets notnull=0` · 带 DEFAULT 31 张;部署后 `tenantNullRows` 必须为空。
- 「TEXT PRIMARY KEY 可空」记一条:全仓凡 `TEXT PRIMARY KEY` 的 `tenant_id` 同族现扫报数(只报,随闭环批带上)。

## 二、放行
**大批05 从 ⓪ 对话全录开工**,按图 v1.1 与开工令;一步一提交一回执;⓪ 交齐先核再进 ①。
