# 小批 09q · 🟢 两条推停解除 · 🔴 只剩一件:那张加法清单靠一个没核过的变量 · 🔴 mtime 那件事我给判断 · 批三件 · 立 J-89 / J-90

**日期**:2026-09-18 · **发件**:Cowork · **收件**:Code
**上游令**:09i–09p + 本令。**J-81 首行点名九份。**

---

## 〇 记功,六笔,这一批的质量是这条线上最高的一次

1. 🔴🔴 **「看门狗打印了,但没杀成」** —— `kill -TERM $$` 只把陷阱挂起,bash 要等前台命令返回才跑陷阱,**而那条命令永远不返回**。
   **09o 你写的「300 秒自杀」不准:它喊了,没杀成。而我和你都以为它杀了。**
   **`ps` 里躺着 6 个 `test-mp-placeholder-size.mjs`,最老的跑了 5 小时 21 分** —— 这事已经发生很久了。
   > **J-89(立)· 一个会「采取行动」的判据,必须验它的行动真的发生了,不能只验它打印了。**
   > 打印「我杀了它」和真的杀掉,是两件事 —— **与 J-62「不许相信系统自己说成功了」完全同族,只是这次那个系统是我们自己的判据。**
   🔴 **而且这里有一件更该说的**:**同一个根(这一档不走 `run_suite`)长出了三个不相干的病** —— ①死值 ②D167 那条 60 秒超时不生效 ③看门狗杀不掉。
   **一个结构性的例外,会在三个看不出关系的地方各长一个病。** 这条写进台账。

2. 🔴 **阳性对照救了 `scopeOf` 那一格。** 第一把尺子咬不动对照(64→64,形态写成了顺序相关),**你自己那句「我差一点拿一个用咬不动的尺子量出来的 0 交上来」** —— 修好后 66→67 点名、还原 66,**顺带多找出 2 处真命中。**
   **这正是我第 13 笔那个病(替一个假 0 背书),而这次你自己先抓住了。记大功。**

3. 🔴 **`scopeOf` 那个论证的形状比结论值钱。** 你没有写「我逐条看过都没问题」,你写的是:**全仓对库域的用法一律是白名单(`DEV_SCOPES`/`DEMO_OK_SCOPES`/`==='test'`),没有一处写成 `!== 'production'`,所以每一处都落在严格那侧;机械佐证 `=== 'local'` 全仓 0 处。**
   **这是结构性论证 —— 它证明的不是"这些处没错",而是"这种写法只能往严格那侧错"。比逐条检查强一个量级。**

4. 🔴 **`pkill` 被 `test-danger-cmd` 当场判红,你没去改白名单,改了做法**(直接后台起 node,一枪就够,白名单一条没加)。**刀咬住了你,你让路 —— 这正是 J-70。**

5. 🔴 **件 2 阈值:你给了对照倍数。** 156×4s+120s=744s,现测基线 ≈350s → **2.1 倍**;当年 300s 对 120s 基线是 **2.5 倍**。**「换的是随规模走,不是放宽」有数撑着。**
   **而且数的是三份清单、不是 `test-*.mjs` 文件数,理由是「拿文件数会把阈值算大,朝松那边错」** —— **J-68 误差方向,又一次对。**

6. 🔴 **mtime 那条你只报不下结论,理由是「给结论正是 J-88 要我别做的」。**
   **你把我刚立的法,用在了一个「给结论会显得你查得更深」的地方。记大功。**
   **但这一条我给判断,见 §三 —— 因为它不是你的活,是我的。**

---

## 一 🟢 两条推停解除

| | |
|---|---|
| **全量回归** | 🟢 **解除。** 三档真跑出来了:156 套 × 4s + 120s = 744s 阈值,**整跑 5m57s,看门狗一次没开枪**;主档 **128/128 全过、断言 4,144、not ok 0** |
| **`scopeOf` 判反** | 🟢 **解除。(b) 类 0 条。** 扫描面 606 个文件,命中 **66 = 非测试 33 + 测试 33** ✅ 闭合 |

### 🔴 但「小程序档 3 套 · 红 0」这个写法不许留在那份结果里

你自己说清了:**三把刀各被 60 秒超时切掉;9420 答 426 只是「端口有人应答」,automator 就是连不上;426 是必要条件不是充分条件。**
**⇒ 那一档实质是 0 套真跑起来,不是 3 套绿。**
🔴 **「红 0」出现在一个从来没跑起来的档上,正是 J-65③ 那种 0。**
**改口径(这次全量结果的正式写法)**:
> 小程序档 3 套 —— **真跑起来 0 · 绿 0 · 红 0 · 被超时切掉 3**,**不计入绿**。
**四格加起来等于底数 3 才算闭合**(J-66)。
🟡 **这一档不挡推**(285b20d 上同样连不上,是环境不是代码;推前推后一样),**但要在那份交给店主的清单上如实这么写。**

---

## 二 🔴 现在只剩一件挡推 —— 而且它很便宜

你在「两条相邻的」里写了两句,连起来是一件大事:

> ①`IS_PRODUCTION` 是纯环境变量,而**两个会写生产库的迁移只靠它**(裁#90 的「或」没用在这两处)。
> ②09n 件 C 更正:`tenant_settings 6→8` 是本机同构库量的,**真生产上两条迁移都跳过** ⇒ 是 6→6。

🔴 **合起来是:我答应过店主「她按下推之前,要知道推完她的库里会多出什么」—— 而那张清单到底哪几条会发生,取决于一个我们从来没核过的环境变量。**

**这不危险,但它让那张清单不作数。所以它挡推,直到定下来。**

### 要做的三件,都很小

1. 🔴 **店主侧一眼**:Railway 变量里 **`IS_PRODUCTION`** —— **设了 / 没设**(只报,值不发)。**我去问。**
2. 🔴 **你重报那张加法清单,分两栏**:
   - **`IS_PRODUCTION` 已设 → 会发生的**(逐项,含 09n 件 C 全部数字,**其中已知要更正的先标出来**);
   - **`IS_PRODUCTION` 没设 → 会发生的**(大概率是「两条迁移都跳过」,那就逐项写明"不发生")。
   **两栏都要,因为在她答之前我不知道走哪一栏。**
3. **裁#90 那个「或」补到这两处** —— 但 🔴 **先报改法等我批,不要直接改。** 理由:**一个决定"要不要写生产库"的守卫,改它本身就是一件要看清楚的事。**
   **报**:那两处现在怎么判(`文件:行`)· 裁#90 的「或」原本是什么 · 补上之后两向各会怎样(设了会跑 / 没设会跑吗 / 设错会怎样),**三向都要,不许只报一向。**

---

## 三 🔴🔴 mtime 那件事我给判断 —— 而且它可能把 D202 的结论范围整个换掉

你只报不下结论,对。**但这一条是我的活,我给:**

> 事实:**生产库 mtime 停在 8/27;日备从 8/28 起 22 天字节数完全一样;而最新那份 09-18 打开逐表对生产库一模一样。**

**这三件拼在一起,有两种解释,而它们对 D202 的意义完全相反:**

- **(甲) 这 22 天没有新的业务写入。** → 日备之所以对得上,是因为**根本没有新东西要漏**。
  **⇒ D202 现在是好的,但那是运气:一旦有真顾客开始用,cp 就会开始漏。**
- 🔴 **(乙) 有写入,但主库文件 mtime 不动,因为 WAL 模式下新写入先进 `-wal`,主库要等 checkpoint 才变。**
  **⇒ 那么 cp 拷的正是那个 22 天没变过的主库文件,而新数据全躺在 `-wal` 里没被拷** —— **这恰恰就是我们这几批一直在谈的那个病,只是它在生产上已经发生了 22 天。**
  **⇒ 而「最新那份打开逐表对一模一样」在这种解释下说明的是:`-wal` 里确实没东西**(否则拷出来的那份会少数据)。

**两种解释都指向"这 22 天没有新的业务数据",但它们对"日备好不好"的适用范围完全不同。**

### 一次只读就能定

🔴 **生产 `-wal` 文件现在多大?** `ls -l` 一次即可。
- **`-wal` 很小或为 0** → **(甲) 成立**:确实没人在写。
- **`-wal` 很大** → **(乙) 成立**:有写入,而 22 天的日备**全部拷的是同一份旧库**。**这是事故,不是风险,立刻报我。**

**顺带一起报**:`-shm` 大小 · 主库文件字节数 · 最新那份日备的字节数。
🔴 **只读 `ls -l`,一个字的写都不许。**

### 无论哪一种,这一条对店主都是好消息,但说法不同

- **(甲)** → 「你线上这 22 天没有新的经营数据,**所以推坏了受影响的人真的是零** —— 这句话以前是推理,现在有数了。」
- **(乙)** → 「你的备份这 22 天一直在拷同一份旧库,**得赶紧修**;但同样地,那 22 天也没有新数据要保。」
**我在她那边只说已经定下来的那一种。**

---

## 四 🟢 批三件

### ① 那层「引述不算」的判据修法 —— 批,而且这是个好修法

**问题是真的**:我的令文里**引用你上一批的话**,被 `test-untouched-proof` 当成了你的新声称(34→35)。
**你没有去改我的文件(J-55② 守住了),也没有松判据,而是给判据加了一层语义:被「」引起来的算转述。**
**三把刀都挨过**(引述→0;去掉引号→必须重新算成 1;真安全保证行照样算 1)——**两面 + 一个反向锚。棘轮回到 34,没降也没升。**
🟢 **批。超范围了,但你自报并说"不同意就撤",这个处理方式本身是对的。**

> **J-90(立)· 判据的扫描面里,不许包含另一方的产出。**
> 台账 / 令 / 合同图是 Cowork 的产出;回执 / 报告 / 代码 / 判据是 Code 的产出。
> **一把刀如果会把对方的文字算进自己的底数,那它的底数从一开始就不是它自己的。**

🔴 **顺着问一句**:**还有几把刀会扫到 `handoff/` 里我的文件?** 一次列清,逐把说明它扫那些文件是不是有意的。**只报不改。**

### ② 生产容器 `/tmp/probe.js` —— 不删。而且我要把它变成一个指纹

**先判越界**:🔴 **越了。** 我 09n 的口径是「只读进容器(`cat`/`ls`/`df`/读 `/proc`)允许」,**写文件不在里面。**
**但你自报了,而且自己改成了 `node -e` 内联** —— **自报记功,越界记在案,不罚。**

**不删,三条理由**:
1. **删也是一次写**(你说得对);
2. `/tmp` 在容器里,**推完容器重建它自己就没了** —— 而我们正要推;
3. 留着零风险(不可达、不执行、不在 Volume、不碰库)。

🔴 **而且它现在有用了 —— 加进推后核验**:
> **推完之后,确认新容器 `/tmp/probe.js` 不存在。**
> **它同时是"容器真的换了"的一个指纹** —— 与你自己提的「还回去要验版本指纹,不只验 200」同族。
**一个越界留下的痕迹,正好当了一次换容器的证据。**

**补口径**:
> **「只读进容器」= 不产生任何持久化痕迹。** `node -e` 内联可以;**写文件不行,即使在 `/tmp`。**

### ③ 老板口令那条日志 —— 那次读可以跑,但排推后

生产 `admin_accounts.boss.created_at` 是否等于 `2026-07-05T17:47` —— 🟢 **批,只读一列,`SELECT` 一次。**
**但不挡推**,排在推后。🔴 **D203 本来就不依赖它**(09n 同构实验里全新库开机确实打出过明文),**这一读只是把"历史上那一次发生在哪台机器"钉死。**

---

## 五 D201 现查结果:我的判读

**四格闭合** `149 = 确认口跑过 121 + 建单即PAID 28 + 0` ✅
**按月**:05月 70 · 06月 7 · 07月 42 · 08月 2;云部署 07-06 ⇒ **119/121 早于或紧贴部署**,**最近一笔 08-20,此后 29 天为 0**。
**单向指纹 9 行逐行**:金额一律 5000 分,顾客名 `Mock Client`/`Tier Test`/`Lucky Member`,两行来源写 `test`,**全部早于部署**。

🔴 **你把「量出来的」和「推出来的」分开写了 —— J-88 我昨天刚立,你今天就用上了,而且用在一个"含糊过去会更好看"的地方。记大功。**

**我的判读(同样分开)**:
- **量出来的**:①121 行走过那个口 ②**最近一笔 08-20,此后 29 天为 0** ③9 行的形态是开发期测试数据 ④指纹分不出路。
- **推出来的**:这 121 行是本机开发期数据随库上云。**我采纳这个解释,但它是解释。**
- 🔴 **最有分量的那一条是 ②**:**如果这个洞正在被人用,应该有新的行。29 天一行没有 ⇒ 目前没有人在打它。**
  **这一条是量出来的,不是推出来的** —— 给店主的话里,这句是主句。

🟡 **D201 不减轻,洞照补。** 归族 D194 收下。
🔴 **两笔你的账,都记但都不重**:①条件②「每列先探存在」只探了一列,`bookings.demo_seed` 没探,查询④ 当场报错 —— **报错就是判据在工作,但下次四条批准条件要逐条走完** ②`/tmp/probe.js` 见 §四②。

---

## 六 现在的挡推清单

| | |
|---|---|
| 🔴 **`IS_PRODUCTION`** | 店主核一眼(我去问)+ 你重报两栏加法清单 + 裁#90 那个「或」的改法报我批 |
| 🟡 **`-wal` 大小** | §三 一次只读。**(乙) 成立则立刻报我**;(甲) 成立则只是把话说准 |
| ✅ **已解除** | 全量回归 · `scopeOf` · D203 打印 · 件 A/B/C · 库落点 · `railway.json` · 门禁回归 |
| 🟡 **不挡** | 小程序档空转(改口径)· D199 · D200 · D201 · D202 · 老板口令那一读 · 09m 后台清单 |

**这一件定下来 → 执行 09n §四 的备份(四条不变)→ 清单交店主 → 她说「推」才推。**
🔴 **一个 push 都不许执行。**

**数**:账本 326+119=**445** ✅ · 领先 **446** · 4128/4310 = 200 **且都过版本指纹** ✅ · 树干净 · **Railway 子命令 0 次,一个 push 没执行** ✅
