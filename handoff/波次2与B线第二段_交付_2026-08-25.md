# 两线并行批 · 交付(2026-08-25)

**真库一行没写;生产一行没写**(乙线只做到"演练 + 出清单",④ 执行等店主核过 dry-run)。
42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-25_波次2.txt`

## 棘轮(wc -l 现测)

```
apps/api/local-server.mjs   18,687 → 18,675   (−12)
apps/web/admin.js            8,968 →  8,945   (−23)
apps/web/customer.js         2,786 →  2,786   (不变)
```
本批新增功能让两个文件先涨了(local +41 / admin +139),**同批还清**:
- `apps/api/pricing-categories.mjs`(53 行):大类 CRUD 原来商家线一份、平台线一份 —— 抽成**一份两条线共用**;
- `apps/web/finance-trend.js`(169 行):S6 动的就是财务趋势域,按公约② 顺手搬出。

---

# 甲线|波次2 网页后台功能波

## 关帐条件:四件是否全绿

| 件 | 状态 | 断言 |
|---|---|---|
| **S4** 员工管理四板块 | ✅ | 顺序=技师业绩→薪资方案→技师排班→账号管理;业绩目标并入①且排在业绩排行**之后**;重复的「本月趋势」卡已删 |
| **S5-a** 通用设置页 | ✅ | 五块齐(语言/币种/我的密码/昵称头像/**财务密码**);财务密码**只剩一处** |
| **S6** 财务趋势两条 | ✅ | 服务内容占比=圆环图(百分比+金额写在图例上);主图表柱子悬停出「收入/支出/净赚」 |
| **S13** 目录管理两条 | ✅ | 模块①按大类分组(未归类单独一组排最后);平台侧大类字典管理界面 + 后端平台线 |

### 关帐句

> **波次2 于 2026-08-25 关帐:「P2 · P4 双端齐平」四件全绿。**

### 逐件说明

**S4**:🔴 **删块没有删入口** —— 那张重复的「本月趋势」卡头上挂着「＋ 添加技师」和「薪资方案」两个入口,
我把它们先搬进「业绩排行」卡的头部再删卡。断言把这条锁死(`addTechnicianButton` / `salaryPlanButton` 必须在)。
实拍:四个页签顺序正确,`technicianPerformance` 已不存在,添加技师入口还在。

**S5-a**:分工按令写进页面副标题(「通用=小程序环境与账号,门店=门店个性化」)。
- **币种是只读的**:它由平台按店配置下发(币种红线 §八),这里如实说明 🔒,**不放假开关**。
- 🔴 **S5-b 挂起**:这一页**零**「忘记密码/短信验证码」入口 —— 不做发不出短信的按钮,断言扫这件事。
- 实拍:五块齐,财务页那份已消失(`financeLockBody` 全仓只剩一个)。

**S6**:
- ①占比圆环:取所选区间**合计**,百分比与金额**写在图例上**(不用把鼠标停上去猜),圆心是合计金额。
  零数据出空态句,**不画一个假圆**。实测(代表性数据):美甲 55.0% / 美睫 28.3% / 加项 10.0% / 产品 6.7%,合计 **100.0%**。
- ②主图表悬停:柱子上浮出「月份 · 收入 · 支出 · 净赚」。`title` 保留(键盘/读屏与触屏还靠它)。
- ⚠️ 沙箱这家店**完整月份没有收入**(数据都在当月),所以页面上真实渲染的是**空态句** ——
  圆环是用代表性数据在渲染层验的(L4 反例数据律),截图受浏览器面板不滚动限制,以 DOM 读数为证。

**S13**:
- ①按大类分组:分组键=`categoryId`(大类字典),组名读同一份字典;**没挂大类的不藏起来**,
  单独一组「未归类」排最后并提示怎么补 —— 藏起来等于让店主永远发现不了漏挂。
  实拍立刻暴露一个真事:这家店 7 个上架项目里 **6 个没挂大类**。
- ②平台侧大类字典:`platform.html` 新增「大类字典」tab(增删改 + 顺序 + 可约开关 + 项目数),
  后端走**既有平台配置族**(`/platform/tenants/:id/categories`,`isPlatform()` 闸)。
  **商家只读是既定口径(v1.4)不变**:商家线那条路由的口径一个字没动。
  删除保护(「该大类下还有项目就不许删」)在**共用模块**里,两条线同一条规则,不因为在平台就放松。
  真跑:GET 200 / POST 201 / PATCH 200 / DELETE 200 / 非平台 token **401**。

---

# 乙线|B 线第二段:**演练完成,等你核 dry-run**

按令「生产只在④、且核过 dry-run 之后才写」,本批**一步生产都没写**,把①②③的动作在沙箱**演练**了一遍:

| 步 | 演练结果 |
|---|---|
| ① 平台后台显式勾选 isDemo 建店 | `201` → 归属落地 **kind=demo**(走的正是 D73 那条路径) |
| ② dry-run 清单 | 见下 |
| ③ 两道锁自证 | 真店改归属 → **403 `HAS_REAL_MONEY`**;真店重置 → **403 `NOT_DEMO_TENANT`** |
| ④ 执行 | **没跑**(等你核过) |
| 收尾 | 演示店改回 real → 200;改回后再打重置 → **403**(闭环);演练店已清理 |

## 生产动作清单(**待你核过才跑**)

1. 在**生产**平台后台「新建商家」,勾选「这是演示店(账本可重置,不受只追加律保护)」,建 1 家:
   - 建议 id `demo-luckyluxe-prod`、名称「Lucky Luxe(演示)」、币种 CAD、时区 America/Toronto
   - 建完立即核对:平台列表「归属」列必须显示 **演示店**(kind=demo)
2. 跑 dry-run(铺设脚本对这家店会写什么):目录(服务/技师/充值套餐/次卡/券模板)+ 2 位演示顾客
   (跨店阿珍 / 本店常客),与沙箱那两家同一份剧本
3. 自动备份(与 demo-reset 同款:备份不成就中止,备份路径写进回报)
4. 执行铺设
5. 出对账:**lucky-luxe / jics-nail 五项(收入/账本行/单/顾客/结算单)零差异**

🔴 一个前提要你确认:**铺设脚本现在的硬边界是"只跑本机/沙箱"**(`BASE` 必须是 127.0.0.1/localhost),
这条边界你在上一批明令**本批不许改掉**。所以要在生产铺设,得先由你裁定这条边界怎么开
(比如加一个"仅当目标租户 kind=demo 且显式带确认参数"的例外)——**我没有自作主张改它。**

---

## 自己抓到的坑(四条)

1. **判据律第五次同坑**:S5-b 那条负向判据扫全文,被我自己写在 HTML 注释里的「S5-b 本轮挂起」命中。
   改成**先剥掉 HTML 注释再判真正会渲染的标记**。
2. **S4 差点把入口删没**:重复卡上挂着「添加技师」——这正是资产层那次判错的同一类。
   这次先把入口搬走再删卡,并把"入口必须还在"写成断言。
3. **大类 CRUD 差点写成两份**:平台线我一开始是照着商家线**抄了一遍**,抄完才意识到这就是
   "两份迟早各自长歪"。抽成共用模块,顺带把 local-server 的行数还清了。
4. **圆环在沙箱看不到**:这家店完整月份零收入,页面真实渲染的是空态句。
   没有把空态当成"做坏了",也没有拿空态冒充"做好了" —— 用代表性数据在渲染层验,并如实写明。
