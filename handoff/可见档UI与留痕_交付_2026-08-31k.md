# 可见档 UI 小合同 + 留痕裁定 · 交付(2026-08-31k)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。做完不说可以关帐。两小件,做完即收。

## 裁定一 · staff_visibility 店级三选一 UI(小合同五条逐条)

| 条 | 落法 |
|---|---|
| 1 位置 | 新模块 `staff-visibility.js` 挂**业绩目标面板顶部**店级一行(#staffVisMount,admin.js 一行挂载调用);「员工端展示(全店):只业绩 / 业绩+工资 / 只工资」段选,现值高亮;**仅此一处 UI**,小程序零编辑口 |
| 2 文案 | 三档注语逐字照合同(「只业绩=不展示工资估算」「业绩+工资=现状默认」「只工资=不展示业绩明细」,当前档注语随选显示+悬停 title);分工小注逐字照合同(「上面这行管全店给不给看;每位技师的『显示』开关管业绩分项细到哪。」) |
| 3 写口 | 既有 `PUT /admin/staff-visibility`(仅老板,后端 400 终闸);**点即保存**,保存后重新 GET 渲染 —— 高亮以库值回读为准,不以点击为准;作用面零动(三态行为断言=test-daily-close 既有 ok40-41 引注) |
| 4 判据 | test-daily-close +5:保存回读(PUT→GET 同值)/ 非法值 400 / UI 链(挂载位+两口+三档注语+分工注机械断言)/ **UI 不许静默**(catch 必须 toast 后端句 + render(true) 回读式) |
| 5 空白 | 无(合同盖满;英文界面未做——面板本身现仅中文注语,与既有业绩目标面板同状,如需双语请点名) |

**变异刀1(合同四)**:把保存失败的 catch 掏空成静默吞 → 「UI 不许静默」当场红点名 → cp 逐字节还原回绿。

## 裁定二 · 回落留痕(②主①辅)✅

- **②主**:店群陪审团新增 **I8**「staff_visibility 存了就必须 ∈ 三枚」(逐店审,与 I7 状态机同型;
  写口 400 挡 API,这条守直写库的坏值);不变量组标号 I1-I8。
- **①辅**:staffVisibilityOf 回落命中时 `console.warn` 一行(租户+坏值+「谁直写了库?」),照旧供默认档
  (可用性不动);写口 400 终闸零动。
- **变异刀2(I8)**:日结夹具直写库塞 `perf_onlyy` 不还原 → 陪审团 I8 红,**点名到店到值**
  (`[p2dc-a-…] 「perf_onlyy」不在三枚内`)→ 夹具还原逐字节校验回绿。

## 回报固定六项

1. **改了什么**:staff-visibility.js(新 42 行)+ admin.js 一行挂载 + admin.html 挂载位/标签/版本 bump
   (20260831k-vis01)+ styles 两行;local-server 回落 warn 四行;store-jury I8;daily-close +5 判据。
2. **在哪一层验**:断言层(UI 链机械 + 行为 PUT/GET/400)+ 库层(I8 逐店)。像素=店主走查(网页面板)。
3. **同类扫尽**:两把刀各红一次(UI 静默形 / 库坏值形)——判据出生就挨刀(突变自检条)。
4. **用什么数据验**:非法值 `perf_onlyy`(400 形与库坏值形同一株)。
5. **重复稳定性**:duo 套件绿→刀红→还原绿→全量回归(见下)。
6. **残留与不确定**:①面板注语中文单语(与既有面板同状,双语请点名);②队列下一件=营销三件(欠账表已排),
   再后=店主大走查(候令)→ 上线前硬门槛审视批。巨型文件:local-server 18,122 → **18,127(+5,回落 warn 与注释)**;admin.js 8,505 → **8,506(+1 挂载行)** —— 双微升随批报,排班域二期抵扣照计。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,501 项断言全绿**(上批 2,496 → +5:daily-close 60→64 计 +4、
  陪审团 I8 入列(5 项壳数不变,逐店面 +1 维);日结 64、jury 89 家 I1-I8)。
  日志:handoff/回归日志_2026-08-31k_65套件.log。端口已还:4128/4310 双活。
