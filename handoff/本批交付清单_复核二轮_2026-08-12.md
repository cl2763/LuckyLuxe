# 本批交付清单 · 店主复核二轮批(两拍板落地+D36🔴+D37 盘点)2026-08-12

> 判定口径:D36 清完 + D37 盘点交付 + 店主复核 → 才宣布「开检反馈 🔴🟠 全清+缺陷线闭环」。本批不宣布。

## 一、拍板①落地:积分口径 B(积分≡档位小计×1元=1分)

- 《财务总逻辑》恒等式区已先读(积分行 08-12 增补)。
- **赚分行单源 `pointsEarnRows`**:此前赚分推导散在**四处**各自实现(earnedPoints 余额/积分页明细/积分历史/serializeUser.points 用 totalSpent 直推)——D36 三账不齐的机制根。现在四处同源。
- 切换语义:切换时点 `2026-08-14T00:00:00Z`(常量+租户 points_policy 留痕行,幂等迁移);按**预约服务时间**划线,新旧两段互斥——切换前的单按旧口径(COMPLETED×标价)一分不动,切换后的新单=已签结算单×档位小计(**积分随签署产生**;未签不计;券不减积分;储值抵扣照常积分)。
- CI ㉔ 十条常驻:新单Δ≡floor(档位小计/100)、≠旧口径、未签 0 分、Σ明细≡余额、历史直算一分不动(TEST_DB 直连对账)、分币残数前提断言。
- 口径事实点名:AFTER_SALES 中的旧单不计积分(状态非 COMPLETED),结案回 COMPLETED 才计;切换后积分随签署、与预约状态轴解耦。

## 二、拍板②落地:lucky-luxe 开分级 + F3 分叉债收敛(四份删三留一)

- 血缘清单(按机制圈类=「等级梯子实现」):①server 全局 `MEMBER_TIERS`(5/12/25万);②server 客户列表**内联阈值**(10/30/60万,**与①不一致**——同一人两端两个等级,分叉债现症);③网页 customer.js 局部梯子;④小程序 api.js 局部梯子+`user.memberTiers || MEMBER_TIERS` 兜底。**四处全删**,唯一事实源=租户 `membership_config.tiers` → `tenantMemberTiers()` → serializeUser 下发,前端零自算。
- lucky-luxe 迁移:以原全局四档为初始配置开 tiersEnabled(幂等=决策标记 `tiers_decision_20260812`,店主日后在 S9 界面自改不会被重放覆盖)。生产随部署同步生效。
- 连带口径(如实报):①`userWaivesDeposit` 网页端的本地等级名单兜底已删,免定金只认后端 depositWaived;②jics(不分级店)自此**无人按等级免定金**——等级免定金机制在不分级店不存在,免不免走门店预约规则;③网页顾客端同步获得三减法(成长条/称谓/权益卡)——盘点清单「待核对差异1」顺带闭环。
- CI ㉕ 双租户常驻;L5 双租户实拍 jics 5/5(会员/无成长条/权益卡空)+ lucky 5/5(Platinum Member/成长条/4 档卡),截图 r2-*。

## 三、D36(🔴):《D36对账表_林小雅_2026-08-12.md》

- 三方逐笔全 dump,**没有算错的钱**;差 50=演示种子「充1000送50」赠送行从未入账(挂-004,不往真实租户账本补假钱);三账不齐=积分明细缺兑换行(真缺口已修)+三本记录范围本就不同(说明)。
- 渲染层实拍(店主那层):储值页 5/5(余额 864/4 行/币符 CAD $)+积分页 5/5(余额 932/含 −800 兑换行/Σ明细≡余额)。
- 四之九同档案商家端核对:客户列表 balance 86400=顾客端 864 ✓,tier=platinum(新单源)✓。
- **顺带抓获·币符写死 $ 类(L2 按机制圈)**:14 处清零(顾客端储值/积分/券包/消息;商家端 finance-txns/member/marketing/services/salary-month `'$-'` 搬运;网页 cadText/AI 摘要 chip/平台服务表),test-currency-scan 新增 3 条规则常驻(`'+$'` 拼接/`$${}` 模板/文案 $数字——旧规则只认 $ 贴引号或贴 {{ 的形态,这就是漏网原因)。

## 四、D37:《D37状态盘点_2026-08-12.md》(只盘不修)

- 两轴+两附加轴全枚举(A 预约态 6 值含 EXPIRED 无专属文案/B 单据态 3 值/C aftersales_status 全库空挂/D 更正链);每个界面槽位显示哪轴的映射表;**混乱根源=列表徽标 B 轴优先、详情页只显 A 轴,两轴抢一个槽位**。
- 数据残留清点:过期 CONFIRMED 12 单(真实两店)+林小雅 bk-d23-50123;EXPIRED 全在测试租户;显示层一行未动(L3),重构随 S8 出图。

## 五、验证与回归

- CI 套件 103 断言全绿(+㉔ 十条/㉕ 两条);42 套件全量回归一次全绿;4128 已还(--watch)。
- 收工闸门双租户:jics 13/13 + lucky-luxe 10/10。
- 走查工具修正两枚(非产品缺陷,已同步 tools/sim):①asCust 注入补「换店同构清币种缓存」——lucky 页面曾渲染 ¥ 是走查绕过 shop-select 的缓存污染,真实换店流程会清(已核 shop-select:43);②d36 金额断言兼容千分位两位小数形态。

## 六、组合矩阵(页内组合,四之六)

- 储值页:逐笔列表×余额×套餐文案(同屏同源同币符)5/5;积分页:余额×明细×兑换行(Σ=余额)5/5;me 页:称谓×成长条×权益卡跳转(双租户两态)各 5/5。涉钱页关键路径=积分/储值双页全组合已覆盖;跨页组合(签署→积分产生)由 CI ㉔ 断言链覆盖(未签 0 分→签署即计)。

## 假设清单

1. 积分切换时点取 `2026-08-14T00:00:00Z`(拍板日 08-12 之后、部署前的固定时点;写进代码常量+租户留痕行)。多伦多 08-13 晚 8 点后、北京 08-14 早 8 点后的新预约走新口径。
2. 切换划线键=预约服务时间(「新单」按服务日理解);旧单切换后才签的,仍按旧口径(不追溯原则的保守读法)。
3. lucky-luxe 初始梯子照抄原全局四档(标签含英文 Member 字样,未改文案——内容店主 S9 自改)。
4. EXPIRED 态无专属顾客端文案(落「待支付」兜底)——D37 已盘点,随 S8 图定。
