# 死口候刀 6 条 · 名单(08-30h 拉平批④,**只报名单,店主点头后删**)

> 六条=判定表 2026-08-29 标「候刀/候查」的三族;行号为 2026-08-30 现测(local-server.mjs)。
> 删法预告:整块删 + 门禁扫描器/finance-key 闸里的引用一并清 + 「零读方」断言防复活;
> `/admin/stored-value/consume`(410 墓碑)**不在刀下**——裁定保留挡旧调用。

| # | 路由 | 位置(文件:行) | 句子(路由首行原文) | 取代它的活口(证据) |
|---|---|---|---|---|
| 1 | `GET /admin/recharge-tiers` | local-server.mjs:12655 | `if (path === '/admin/recharge-tiers' \|\| path.startsWith('/admin/recharge-tiers/')) {` | 充值档位已由 `recharge-packages` 取代;网页编辑块 A5 收敛时退役(admin.js:8256 注「整块退役——CI 零读方断言防复活」),两端 grep 零调用 |
| 2 | `POST /admin/recharge-tiers` | 同块 :12661 | `if (req.method === 'POST' && !tierId) {` | 同上 |
| 3 | `PATCH/DELETE /admin/recharge-tiers/:id` | 同块(tierId 分支) | `const tierId = path.split('/')[3] \|\| null` | 同上;连带清两处闸引用:12490(财务钥匙前缀)、12499(豁免清单) |
| 4 | `GET /admin/merchant-leads` | local-server.mjs:13733 | `if (req.method === 'GET' && path === '/admin/merchant-leads') {` | 平台位面 `/platform/leads` + page-leads 面板在用;此条=仅默认租户的旧位面,两端 grep 零调用 |
| 5 | `PATCH /admin/merchant-leads/:id` | local-server.mjs:13737 | `if (req.method === 'PATCH' && path.startsWith('/admin/merchant-leads/')) {` | 同上 |
| 6 | `POST /admin/finance/change-password` | local-server.mjs:14062 | `if (req.method === 'POST' && path === '/admin/finance/change-password') {` | 网页财务设置真调的是 `/admin/finance/lock-settings`(admin.js:2921);两端 grep 零调用;连带清 14082 的豁免引用 |

**不动帐**:`touch-rules`(等企微,挂起在册)· `identity-merge-queue`(等出图)· `ops/import-db`(运维)·
`salary/my-estimate` + `staff-visibility`(并员工端批,见剩余清单 #2/#5)· `stored-value/consume`(墓碑保留)。
