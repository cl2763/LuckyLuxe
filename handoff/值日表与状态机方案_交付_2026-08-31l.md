# 值日表 + 已报价状态机方案 + 对表与预研 · 交付(2026-08-31l)

> 安全保证:**生产库未动 · 本机库未动**。未推 main。做完不说可以关帐。
> 回执顺序照令:三(口径一行)→ 一(值日表)→ 工资族对表 → 二(方案)→ 四(预研)。
> 注:队首「营销三件」已于 31n 勘正结案(零欠账,head 截断数漏),本批顺位开工。

## 三 · 口径确认一行 ✅

确认无出入:券临期与到店提醒**只有「发出去」那一下等 ICP** —— 队列生成/记录页/规则配置/文案模板
全部活着(P3+31m 现状,套件 49 项在守);工资族两细项**不等任何外部件**(纯内部,已当批对表见下)。

## 一 · 值日表 ✅(小合同六条逐条)

| 条 | 落法 |
|---|---|
| 1 开关默认关 | tenant_settings `duty_enabled` 默认关;**关=schedule-day 响应整块不下发=两端零渲染**;门店设置各加开关(网页新「值日表」行 duty-setting.js;小程序门店设置 switch 行),同口 GET/PUT /admin/duty-setting |
| 2 位置 | 今天台面底部「值日」行,两端同批同位(网页 .tb-duty / 小程序 .dv-duty,图例行上方) |
| 3 交互 | 行内列当天在岗技师名,老板点=勾(实色),再点=取消,可多人,勾选即存(POST /admin/duty/mark);按店×按天记(duty_marks 表 PK=店×日×技师) |
| 4 可见 | 勾权=仅老板(后端 403 闸)+ 只许当天(canEdit=owner∧今天);员工端同位置只读可见(同一响应块,chips 禁点) |
| 5 数据 | 新表 duty_marks(technician×date),**不碰排班 is_working 语义**;历史日只读=写口非当天 400 |
| 6 空态 | 开了没勾人 → 后端出句「今天还没安排值日」两端同句 |

判据入 test-today-board(28→**38**):默认关零块/空态句/勾读回/再点取消/历史日 400/关回即消失/两端渲染链
+ 台面 API 白名单 4→**5**(duty/mark 入册随批报);陪审团 **I9**(值日标记零串店/开关值合法,I1-I9 逐店)。
**变异刀**:渲染无视开关 → 「默认关零块」当场红(泄漏块整个上招)→ 还原逐字节校验绿。
过程咬获两小口:构造处漏传 readBody/apiError(500 现形即修)、套件夹具变量越块(techD→t1)。

## 工资族两项对表 ✅

1. **阶梯=超额累进:已实现,符合 11 项方案口径,零缺陷。** 现测:`commissionByLadder` 的
   `progressive` 分支按档分段累加(8,000×10% + 4,000×12% = ¥1,280,test-salary-v2 第 85 行钉死),
   「落档全额乘」是并存的**阶段(whole)**模式 —— 方案 08-05 写「没实现」是当时快照,P2.2 落地批已补齐,
   双模式+边界例(恰在档界)都有断言。
2. **员工历史流水**:业绩侧**有**(我的业绩页 每日流水/月度/年度三页签,双端);工资侧**半缺** ——
   `salary/my-estimate` API 支持 `month` 参数(历史月可查),但两端 UI 薪资卡只出当月,没有月份切换
   → **登缺陷 D91**(小件:薪资卡加月份切换,API 零动),修不修与排期请裁。

## 二 · 已报价状态机:实施小方案 ✅(先方案后动工,等你核)

全文=handoff/已报价状态机_实施小方案_2026-08-31l.md。骨架:会话=6h 静默切段(可配)/有效期 48h(可配,
读时判过期不落定时器)/四态读推导不另存列/工作台三色横幅句后端出/改价二次确认+quote_price_changes
留痕表/与 quote_requests 现有字段映射表齐。**待核三点**:两默认数、quote-state 并入会话详情还是独立口、
AI 改口话术入 kb 与否。核过即开工,未动一行。

## 四 · 数据迁移预研 ✅(只查不建)

全文=handoff/数据迁移预研_2026-08-31l.md。①两家旧软件**名字待店主提供**后逐家实测导出面;
②有导出:P6 导入映射草案已列(吃现有 import/customers 地基:dryRun/幂等/legacy 桶,期初余额不进账本红线);
③无导出:扫码自建档+自报余额+截图核对+账调录期初的运营式路径,报多/报少/纠纷话术三风险各配对策。

## 回报固定六项

1. **改了什么**:schedule-board.mjs 值日域(表/开关/读随 schedule-day/写口,161→约 230 行)+
   local-server 三针(构造依赖/ensureSchema/dispatch 带 adminSession)+ 两端值日行 + 两端设置开关
   (duty-setting.js 新)+ today-board 套件 +10 + 陪审团 I9;两份方案/预研文档。
2. **在哪一层验**:断言层(38+I9 逐店)+ 刀红-绿;像素=店主走查(值日行已属三新走查点之外的第四点,
   **请把它并进走查单**)。
3. **同类扫尽**:duty 写口权限=owner+当天双闸;读块=响应级零下发(不是前端藏)。
4. **用什么数据验**:开关开/关两形、勾/取消/历史日、越权形(mark 非老板 403 由 owner-only 闸盖)。
5. **重复稳定性**:duo 三轮(两修一绿)→ 刀红 → 还原绿 → 全量回归(见下)。
6. **残留与不确定**:①D91 待裁;②状态机方案待核三点;③迁移预研等两家软件名;
   ④棘轮:local-server 18,127→**18,131(+4:三针+注释)**、admin.js 8,508→**8,509(+1 挂载)**随批报;
   ⑤员工端值日只读的小程序 chips 视觉(禁点态)像素=开发者工具验。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,524 项断言全绿**(31m 的 2,514 → +10:today-board 28→38;陪审团 I1-I9 × 89 家)。
  日志:handoff/回归日志_2026-08-31l_65套件.log。端口已还:4128/4310 双活。
