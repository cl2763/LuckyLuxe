# 走查总表 · 四个环境 / 用哪个账号 / 看什么(2026-08-24 · Cowork 编,活文档)

> **密码一律不写在这里**,只写用户名。密码见本机 `handoff/本地自查账号.txt`(已 gitignore,不进仓不进聊天)。

## 一、先记这张表:一共四个"世界",互相之间数据不通

| # | 你打开的地方 | 连的是哪个库 | 里面是什么数据 | 有哪些店 |
|---|---|---|---|---|
| ① | **微信开发者工具**(小程序) | **沙箱 4310** | 演示/测试数据,随便玩 | Lucky Luxe · 小婕的店(沙箱) · 小婕的店(演示) |
| ② | 浏览器 **127.0.0.1:4310** | 同上(网页顾客端) | 同 ① 一套数据 | 同上 |
| ③ | 浏览器 **127.0.0.1:4128** | **本机真库镜像** | 你店与小婕店的真实数据(本机副本) | Lucky Luxe · Jie's Nail 小婕 + 一堆压测遗留店 |
| ④ | 浏览器 **luckyluxeatelier.com** | **线上生产库** | 真顾客真数据,会记账 | Lucky Luxe · Jie's Nail 小婕 |

**"网页和小程序对不上"的头号原因**:小程序在 ①(沙箱),你网页开的是 ④(生产)。**不是版本旧,是两个库**。要对比必须同一个世界:小程序看 ①,网页就开 ②。

## 二、账号:两端同一套账号,但要跟对"世界"

**关键规则:账号属于"库",不属于"端"。** 同一个账号在小程序和网页都能登(两端共用后端),但**只在它所在的那个库里存在**。

| 你要登的身份 | 用户名 | 在 ①② 沙箱 | 在 ③④ 真库/生产 |
|---|---|---|---|
| 你(Lucky Luxe 老板) | `boss` | ✅ 有 | ✅ 有 |
| 你店员工 | `avalin` / `miachen` | ✅ 有 | ✅ 有 |
| 小婕(Jie'Nail 老板) | `boss-jienail` | ❌ **没有**(沙箱无 jics-nail 这家店) | ✅ 有 |
| 小婕店员工 | `staff` / `staff2` | ❌ 没有 | ✅ 有 |

> **你今天登不进小程序商家端,就是这条**:小程序连沙箱,沙箱里没有 `jics-nail` 这家店,`boss-jienail` 自然不存在 → 401。
> **在小程序里要看定金与会员体系,请用 `boss`**(Lucky Luxe 老板),密码见凭据文件。

**顾客端**:不用账号。小程序里在「我的」页底部「切换演示身份(沙盒)」换人;网页 ② 用 `?store=` 指定门店,例如 `http://127.0.0.1:4310/?store=jics-sandbox`。

## 三、"测试用"和"给顾客用"的分界

| | 我自己检测用 | 顾客真正会用的 |
|---|---|---|
| 小程序 | 开发者工具连沙箱 4310(账号 `boss`) | 体验版/正式版连 **生产**(账号 `boss` / `boss-jienail`,真密码) |
| 网页 | `127.0.0.1:4310` | `luckyluxeatelier.com` |
| 数据 | 演示数据,错了随便重来 | **真钱真账,动之前先想** |

**上传体验版/正式版前必做**:`miniprogram/utils/api.js` 顶部 `USE_LOCAL_SANDBOX` 改 `false`(否则包里连的是你电脑,顾客打不开);上传完记得改回 `true` 继续本地开发。

## 四、走查一段功能的标准姿势(避免再次"对不上")

1. 先确定看哪个世界:**日常走查一律用沙箱**(小程序 ① + 网页 ②)。
2. 两端对比时,**网页必须开 `127.0.0.1:4310`,不是线上域名**。
3. 同一段功能,两端选**同一家店、同一个顾客**再比。
4. 发现不一致 → 截图两张(两端各一)+ 说明你当时在哪个世界,发 Cowork。

## 五、待办(Cowork 记)

- 沙箱租户命名混乱:沙箱里小婕的店叫 `jics-store`/`jics-sandbox`,真库叫 `jics-nail`,三个名字一件事,走查时容易认错 → 待收编。
- 真库镜像(4128)里有大量压测遗留店(`dbl-a-*`/`r2s-*`/`p25-a-*` 等十余个),选店页会看到 → 待清理或标记为非展示。
