# 小批 10h · 🟢 签署留档两步全上线 · 立 J-108 · 像素那件改口径 · 乙档第二批开工
2026-09-21 · Cowork 出令

**上游令点名(J-81):** `小批10g…` `6814f4247d601e902a3e6f62eb02ada8` · `小批10g补…` `6a18111279cf732e742021da955db17f` · `小批10f…` `d981e59d731cf7dec80803ff4fb17a2b` —— 三份落地,回执收到。
🟢 **`06d2905 → 489b87a` · CI 绿 check-run `106123900434` · 轻量六项 + 你自加两项全过。签署留档两步全上线。**

---

## 〇 🔴 本批最值钱的一笔:**你差点报出去的那个假缺陷,你自己拦下了**

L5 第 1 次好、第 2 次起变空态 —— **这看起来就是硬缺陷,而且是最容易得分的那一种「我抓到一个真 bug」。**
**你没报,先量了变量:**

```
注入后 auth 尾号 …6lirhPm4  →  第 1 次进 me 变成 …SwaCunxA
小程序里直打接口:{"status":200, "n":0}
```

**小程序启动会自己登录,把注入的令牌换成它自己那个人(柳南舟2),而那个人确实没有文件 —— 空态是对的,错的是夹具。**

**这和 10e 那个 `width: 0` 是同一族,而且现在有三个实例了。所以把 J-95 扩成完整版:**

### 立 J-108(J-95 的三元组完整版)
> **一个量出来的数,必须带着三样才算数:在**哪台机器**上量的 · 在**哪一刻**量的 · **对谁**量的。**
> 少任何一样,它都可能是真的,也都可能是夹具的影子。
> - **机器**(J-95):Mac 绿 / CI 红
> - **时刻**(10e):`scrollIntoView` 与重绘之间读到的 `width: 0`
> - **对象**(本批):量的是柳南舟2,不是 Ada

🔴 **一个假缺陷比一个漏掉的缺陷更贵** —— 漏掉的还会再被咬到;假缺陷会让人去改一段本来是对的代码。**你这一次是自己把它拦在门里的。**

---

## 二 🟢 J-107 立法当天就用上一次 —— 教科书

`i18n.js:22/42` 的 `paid` 是**零调用方的死键**。
> 「老习惯会顺手删 —— **10f 授权的是改措辞,不是删键**,所以只改值,死键另账登记等你裁。」

**这正是 J-107 要的那个动作:不是不能删,是那一刻没有被授权删。**

**现在我明写授权:** 🟢 **批准删除 `i18n.js` 那两个零调用方的 `paid` 键**,和乙档第二批一起做。条件:删之前把「零调用方」再量一次(J-96:别拿一把恒为空的搜索当证据)。

---

## 三 🟢 10f 那件:你先量底数,省掉了一刀

「两端共用词典」08-25 A2 已经做过了(`customer.js:1574` = `order?.statusText || ''`,后端唯一出口 + 零回落)——
**所以这一刀不重做,只加判据守住别倒回去。** 🟢 **这是「动手前先量底数」的正确样子**(我 #26 那笔栽过的坑,你没栽)。

活的违规只有一处,`paidDone`「定金已支付」→「**门店已确认收到定金**」,5 条判据含两条反向守。**收。**

---

## 四 像素那件:**改口径,不再试第五种办法**

**网页端四法全黑帧,已经花掉两轮。** 我不让你再试第五种。

**新口径(对这一屏永久生效):**
> **顾客端这两屏的验收标准 = 实看(`innerText` 现读)+ 逐字对图 + 两条负例不在屏幕上 + L5 五次一致。**
> **像素图记为长期欠账,不挡任何事。**

🔴 **而且有一条你和我都拿不到、只有店主能做的最便宜的路:她自己截图。**
**我已经请她:手机开小程序「我的」截一张 · 电脑开顾客端档案页截一张,发给我。30 秒,比我们再写四段脚本都强。**

**小程序那次纯 CSS 改动后只单次复验、没重跑 L5** —— 🟢 **如实挂着,判得对**。**补跑排下一批开头,不挡。**

---

## 五 记你三功

1. 🟢 **推前备份是先拍的,不是事后补的** —— 10a 那次你没赶上,这次赶上了。**这就是纪律落地的样子。**
2. 🟢 **在生产容器里验证了 10c 修的那个 CLI 缺陷** ——「**在它当初出事的同一台机器上验的**」。**这句话本身就是 J-95。**
3. 🟢 **幂等复核第二次主动做** —— 又一次部署,`user_identities` 仍 181、重复组仍 51,**一行没多**。**令里没要求,你自己在守。**

---

## 六 下一批(顺序写死)

1. **小程序 L5 补跑**(纯 CSS 那次之后)
2. **乙档第二批 ≤10 处**,先修会落进对账 / 结算 / 储值的;**`i18n.js` 那两个死键一起删**
3. **等店主两张截图**(她给了就贴进交付清单,给不了就按 §四 新口径结案)
4. 🔴 **D207 那 51 行 · D207-b 那 1 行:仍然冻结,仍然等我批 `SELECT` 原文。本令没批。**

**下一段的大件(先别动,等我出令):** AI 快速修图(等店主给样片)· 功能验收(40 个涉钱口剩 30 · 11 条追不到口剩 4 · D199 · J-60 那 23 处)。

---

## 七 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:小程序 L5 补跑 · 乙档第二批 + 死键删除(含「零调用方」重量一次)· 三行口径。

**停线一个字没松。**
