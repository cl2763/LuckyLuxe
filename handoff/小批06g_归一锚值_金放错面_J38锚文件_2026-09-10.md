# 小批 06g · 归一锚值 · 金放错面 · J-38 锚文件

**开单:2026-09-10** · 上游:06f(`69e35f6` → `dd91256` → `f177097`)· 裁 #29–#32 · 立律 J-40 / J-41 / J-42

---

## 〇、先记我一笔(第四笔):06f §三那条判据,是我写坏的

我在 06f 里把归一的判据写成「`--accent` / `--accent-dark` **定义 0 处、使用 0 处**」。

**改个名字就能满足它。** 这不是你钻空子 —— 是我给了一把**量名字**的尺子,去量一件**关于值**的事。

你没钻。你在回执里自己写:

> 本批只做到「`--accent` 这两个名字消失」,值还活着 …… 只换字面量写法等于把第二套真相搬个家。所以登记不动 —— 不拿「名字没了」冒充「值统一了」。

**是你这句话把我的判据照出来的。** 这一笔记我账上。

> 🔴 **立律 J-40 —— 归一的判据锚值,不锚名。**
> 凡「统一到某某」的判据,必须扫**被淘汰的那个色值/那个量本身**;名字只是它今天的马甲。
> 同族:`#c8a47e` 与 `rgba(200,164,126,…)` 是同一个真相的两种写法,判据要一起认。

§四 就是按 J-40 重开的判据。

---

## 一、我现查到的(2026-09-10 · 头 `f177097` · 全部我自己跑的)

**你做到的:**

- `node tools/design-token-diff.mjs` → **共 21 条 · 红 0 · ✅ 令牌与合同图逐条一致**(七块全在)。
- `--accent` / `--accent-dark`:应用源码 **0 处** —— 名字确实没了,判据 ⑤ 是真绿。
- 棘轮我逐把核过:`styles.css` 写死色 **34 → 31 已往下收**(`tools/pre-regression.sh:134`);
  `test-untouched-proof.mjs:117` 的 `LEGACY_CAP` 仍是 **34**(另一把尺子,本批未动 —— 对)。
  **没有抬棘轮,没有换尺子。**

**但值还活着 —— 全仓 222 处**(`grep`,排除 `handoff/`):

| 色值 | apps/web | miniprogram/pages | miniprogram/assets | apps/api | 合计 |
|---|---|---|---|---|---|
| `#c8a47e` | 9 | 68 | 0 | 1 | **78** |
| `#9b7655` | 3 | 83 | 21 | 1 | **108** |
| `#b5885d` | 4 | 31 | 0 | 0 | **36** |

其中 `apps/web/platform.html:8` 与 `apps/web/sign.html:10` 各有一行
`:root{--gold:#c8a47e;--bronze:#9b7655;…}` —— **这就是搬了家的那两个。**

**更大的一处在小程序:**

- `miniprogram/pages/**/*.wxss` 里写死 `#RRGGBB` **1,978 处**,而 `var(--…)` 只有 **434 处**。
- 其中「**写死金底 + 写死白字**」在同一条规则里的有 **29 处** —— 正是你 06f §一 刚治过的那一族(2.02),
  只是它们住在小程序里,而你的判据 ⑥ 只认 `background: var(--brand…)`,**一条都没扫到**。

店主原话(她在 06e 那一轮中途插的,我没及时转给你 —— 这也算我一笔):

> **「你要记得小程序里也是一样的要引用这两套色系啊。」**

---

## 二、裁 #29 —— 七处「金压面」超标:先分筐,**不许先改图**

你报的 7 处里最典型的是 `--brandd` 压 `--wall` = **4.08**。

**先说结论:那张图店主刚刚两档逐条看过、逐条批过。不许为这 7 处去动它。**

> 🔴 **立律 J-41 —— 图=合同,合同要重新拍板才能改。**
> 用**用法**能修好的,不许用**改图**去修;真要改图,由我拿浅/深两张并排图去找店主拍板,不是你改完再报。

我自己量过:`--brandd` 压卡/纸是好的 —— 深 `#e2c79c` 压 `#2a221c` = **9.59**,浅 `#8a6f45` 压白 = **4.73**。
所以 4.08 那处的病**不在色,在把金放到了 `--wall` 那层面上**。

**做法:7 处逐条归到三筐之一,回执里逐条写清是哪一筐、为什么。**

1. **放错面** —— 金字本该压 `--card` / `--paper`,却落到了 `--wall` 或别的次级面上
   → **改用法**:要么把它挪回该压的面,要么把那个面改成 `--card`。
2. **它其实不是字** —— 描边、图标、分隔线、进度条填充,不承担阅读
   → **进白名单**,逐条写理由,条数上棘轮(照你 ⑥b 的写法)。
3. **用法怎么改都过不去** —— 只有这一筐才算「色不对」。
   → **一条都不许自己改图**。列出来交给我,我去找店主拍板。

**三筐条数相加必须 = 7**(不许有第四种去处,不许「顺手改了就不列」)。

---

## 三、裁 #30 —— 第三把金 `#b5885d` 一起归一;并把你那条证据做成探测器

`#b5885d` 全仓 36 处:`apps/web` 4 处(`admin.html:27` / `:44` / `:311`、`customer-tags.js:47`),其余 31 处在小程序。
它跟 `#c8a47e` / `#9b7655` 是**同一族**:同样**没有深色档的值**。**并入 §四 的归一一起做。**

另外 —— 你这批拿来证明「`--accent` 没有深色值」的那条证据,**比结论本身值钱**:
你发现同一个元素在两档量出来**对比度完全相同**。那不是这一次的手工推理,那是一把**通用探测器**。

> 🔴 **立律 J-42 —— 两档同值即漏档。**
> 同一元素在浅档与深档量得的对比度**完全相等** ⇒ 它用的色值**不随档位变** ⇒
> 要么是**有理由的白名单**(印刷色、品类色、图片里烧死的色),要么就是**漏了深色档**。

请把它做成 `tools/contrast-sweep.mjs` 的一个开关(或一把独立小刀):两档跑完做差,**差为 0 的元素全列出来**,逐条落白名单或落红。

**本批只要求「能报出名单 + 名单进回执」** —— 名单里的存量不必本批清完,清的节奏走 §四 的棘轮。

---

## 四、裁 #31 —— 归一判据按 J-40 重开:**锚值**

把 06f 的判据 ⑤ 从「扫名字」改成「**扫值**」:

- **扫描面**:`apps/web` / `miniprogram` / `apps/api` 三棵树 —— 沿用你 ⑤c 的**按棵树自证下限**,不许缩面。
- **目标值**:`#c8a47e`、`#9b7655`、`#b5885d`。
  大小写不敏感;**`rgb()` / `rgba()` 写法的同色也要认**,否则又是一次换马甲(J-40 的同族条款)。
- **判据形状**:硬零 + 白名单;白名单**逐条写理由**,条数上棘轮。
- **存量太大,本批不要求清零**:本批把判据建起来,**把当前实际条数设成棘轮初值**
  (照 `小程序 wxss 写死色棘轮 ≤ 2443` 那把的写法),以后**只许降**。
  本批必须真降下来的,是下面两件。

### 本批必须做完的两件

**① `platform.html:8` / `sign.html:10` 那两行 `--gold` / `--bronze` 定义要死。**

这两页**不引 `design-tokens.css`** —— 这正是 D188 第一条要修的结构病。
所以:**先把令牌文件引进这两页**,再 `--gold` → `--brand`、`--bronze` → `--brandd`,
最后把 `:root` 里这两个定义删掉。两页要**两档各出一张图**(J-32 / J-38)。

> 你在 06f 说「接引用是 D188 的事,本批只登记不手补」—— **登记是对的,但这两页现在就补**:
> `platform.html` 是平台唯一入口、`sign.html` 是顾客签单页,不能等结构批。
> 其余没引令牌的页面**仍按 D188 统一做**,不许在这批零敲碎打。

**② 小程序那 29 处「写死金底 + 写死白字」。**

回执里逐条列 `file:line`。金底改令牌、字色一律 `--hero`。
**规矩丙(金底上的字一律 `--hero`)对小程序同样有效**,
把你判据 ⑥ 的扫描面从 `background: var(--brand…)` **扩到「写死的金色底也算金底」**。
改完复量,回执给**改前 / 改后两列对比度**。

### 本批只登记、不动的一件

`miniprogram/assets/*.svg` 里 **21 处 `stroke="#9b7655"`**:
SVG 当图片加载吃不到 CSS 变量,「让图标跟档位走」是**单独一件事**(两套图 / `wxss` 滤镜 / 改字体图标)。
本批只要在回执里给出**你建议的走法 + 代价**,我来裁。**不许顺手先改一半。**

---

## 五、裁 #32 —— J-38 的尺子改成锚「**这一段改过的文件**」

你这批撞上的:一个基线提交把 6 张图判成了「修前的图」,而那 6 张其实是修后的。

**你没有松判据,你去重拍了 —— 先记你一功。** 这正是 J-38 想要的那种反应。

但判据本身太粗:它拿「任意一次最后提交」去比图的时刻。改成:

- 每段的对照说明里本来就写了**这一段改了哪些文件** → J-38 拿
  **「最后一次触碰这些文件的提交」**的时刻去跟图的时刻比。
- **拿不到文件清单时,退回原来那把粗尺子**(宁严勿松 —— 宁可让你多重拍一次)。
- 判据自己要在抬头报清楚:**这次用的是哪把尺子、比的是哪个提交号**(J-39)。

---

## 六、顺序 · 停线

**顺序**:§四①两页接令牌 → §四②小程序 29 处 → §四 值扫判据 + 棘轮初值 → §二 七处分筐 → §三 探测器 → §五 J-38 换尺子后重跑带图的段。

**停线一条不松**:
不推 main · 不碰生产 · **写 4128 既有行仍停**(D185 §二我还没批)· 不看密钥文件 ·
不许 sleep 轮询 · 不许松判据 / 抬棘轮 / 换尺子 · **不许改合同图来凑代码**(§二 第 3 筐交我)。

密码一个字都不进回执、不进队列、不进任何交接文件 —— 老规矩(四之十 第 6 条)。

---

## 七、回执要写到

1. §四 判据的**棘轮初值**:三个色值分别多少处、合计多少。**这个数以后只许降。**
2. `platform.html` / `sign.html` 接令牌**前后**,两档各一张图,**图带拍摄时刻**。
3. 小程序 29 处逐条 `file:line`,**改前 / 改后**两列对比度。
4. §二 七处的三筐名单(相加必须 = 7),**第 3 筐单独拎出来给我**。
5. §三 探测器报出的「两档完全相同」名单:条数 + 前 10 条。
6. §四 那 21 个 SVG 图标:你建议的走法 + 代价。
7. §五 换尺子后重跑本批**所有带图的段**,报它这次比的是哪个提交。
8. **造病验红**:§四 值扫判据 · §四 规矩丙扩面到小程序 · §五 J-38 新尺子 ——
   **各造一次病、各报红一次、各点名到 `file:line`、各回绿。**
9. 有任何一条你**没照字面做**,回执写「我没照字面做,原因是 X」—— 不许默默换做法。

---

**这一单的由来,一句话记住**:名字换了不算归一,值还活着就是第二套真相。
判据锚名不锚值,是我的错;把它照出来的,是你那句「不拿『名字没了』冒充『值统一了』」。
