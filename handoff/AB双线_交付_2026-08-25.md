# A/B 双线 · 交付(2026-08-25)

**真库一行没写,生产一行没写。** 42 套件 EXIT=0:`handoff/回归日志_42套件_2026-08-25_AB双线.txt`

## 棘轮(wc -l 现测)

```
apps/api/local-server.mjs   18,717 → 18,708   (−9)
apps/web/admin.js            8,968 →  8,968   (不变)
apps/web/customer.js         2,762 →  2,775   (+13,A2 状态句出口 + en 词典)
```
本批动 local-server 净 +10(平台列表下发 kind / 改归属分发 / statusText / import),
**同批还清**:会员码域搬出 `apps/api/member-code.mjs`(32 行,已 git add),净 −9。

---

## A 线

### A1【只看不动】清单已单独成文

`handoff/A1_网页顾客端vs小程序顾客端_对比清单_2026-08-25.md`

四类共 **17 项**,其中 🔴 涉钱/状态 **5 项**单列:
- 🔴 M1 订单状态句两端各一份本地词典(词还不一样:CONFIRMED 小程序「待服务」/网页「已支付」)
- 🔴 M2 会员码两端各写一份回落(后端已有唯一算法)
- 🔴 M3 网页无选店入口(**归 A3,只登记未动**)
- 🔴 M4 网页无「预约完成」落点
- 🔴 M5 储值展示入口数不同(数字同源,不算口径错)

a 类(网页缺)6 项:AI 客服 / 门店导航 / 选店 / 预约完成页 / 绑定页 / 消息中心(疑似)
b 类(网页多)4 项:资产总览 / 独立券页 / **礼品卡(需确认是否已下线)** / 设置页
c 类(口径不同)4 项、d 类(结构不同)3 项。

**A3 两件按令原样记下、一行未动**:品牌层级(平台名与商家名主从)、选店口径复议(D45)。

### A2【已做】M1 落地:订单状态句改后端唯一出句

这是 A2 授权范围里的「文案口径 + 只读块与后端同源」:
**取小程序已拍的那套词**(待支付/待服务/已完成/已取消/已过期,售后轨道开着时=「售后」),
后端 `bookingStatusText()` 照抄下发,两端直渲。**顾客看到的字与小程序原来一模一样**,不是口径变更。
en 仍各端本地词典(后端句是中文,与 `listBadgeText`、动作 label 同一处理方式)。

**断言照第三案那条写法(㋠ 组 9 条)**:把两端各自的取句代码**从源文件抠出来、真跑在同一份 booking 上**再逐字比 ——

| 态 | 两端 | 结果 |
|---|---|---|
| 待支付 | 逐字相同 | 「待支付」✅ |
| 待服务 | 逐字相同 | 「待服务」✅ |
| 已完成 | 逐字相同 | 「已完成」✅ |
| 售后中 | 逐字相同 | 「售后」✅ |
| 反例:后端没给句子 | 网页显示空串,不本地编 | ✅(零回落律) |

②「网页不再用本地词典渲染某张单的状态」用**取代码行**判(`statusLabel(order.status)` 命中数 = 0),
不扫全文 —— 判据律新细则。

---

## B 线第一段

走查卡单独成文:`handoff/B线走查卡_演示店_2026-08-25.md`。要点:

- ① 判据改 `tenants.kind='demo'`,与 `demo-reset.mjs` **共用同一个 `isDemoTenant()`**;
  非 demo 拒绝并说明**是被拦了**;真店黑名单保留作第二道锁;**生产硬边界一行没改**。
- ② 走查卡数字**全部取自顾客端公开接口**(不是查库):两店各两位演示顾客,
  等级/成长值/累计消费/到店/储值/次卡/券/积分/历史订单(含已签署单)逐项有真值;
  跨店同一 openid 的两份档案数字一眼可分辨(888/366、1110/1500、剩 4/剩 2、CAD/¥)。
- ③ 幂等:重跑打印「已有,跳过」,数字不变。
- **第二段(写生产)一步没动。**

### B 线撞到的洞(已补,报备)

D73 把归属改成"建店时显式勾选",却**没给已建的店留修改入口** ——
`jics-sandbox` 名字叫「小婕的店(演示)」、归属却是 `real`,既铺不了也重置不了,
而我写在错误提示里那句"归属只能在平台后台显式设置"当时是**空话**。
本批补上 `PATCH /platform/tenants/:id/kind`(real↔demo 两态、test 不许人工设、必填原因、落 ops-log)
+ 平台后台「归属」列与切换按钮(二次确认直写后果)。㋡⑦ 断言常驻。

---

## 顺带两件

1. **小程序 L1 实拍:仍缺**。微信开发者工具自动化端口(9421)本批期间一直是关的,
   我不拿"应该一样"糊弄 —— 该比的东西已经在 CI 里每次跑(㋟/㋠ 两组都是真跑两端代码比对)。
   端口开着时补拍。
2. 提审那两件的公众平台步骤**没有重写**,仍在 `handoff/五线并行批_交付_2026-08-25.md` 第丙线 3。

---

## 自己抓到的坑(三条)

1. **走查卡第一版把「卡包」报成 0 张** —— 我的取数脚本读错层级(`j.timecards` vs `j.cardPack.timecards`)。
   查库确认数据在,**先查库再下结论,没把自己的脚本错报成产品缺陷**。
2. **㋠④ 第一版真跑就崩**(`orderStatusText is not defined`)——抠代码时只抠了 EN 词典、
   没把函数体带上。这类"抠源码真跑"的判据,抠漏了会以异常形式暴露,比默默绿掉好。
3. **沙箱服务是旧代码,判据当场骗了我一次**:刚加完"平台列表下发 kind",铺设脚本仍报
   `kind=real` —— 因为 4310 跑的还是加字段之前的进程。重启后才对。
   教训:改了下发字段,验之前先确认那台服务是新代码。
