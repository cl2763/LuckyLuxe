# 小批 10a · 🟢 上线了 · 四件全裁 · 🔴 WAL 让我批过的一条豁免到期 · 立 J-103 J-104
2026-09-20 · Cowork 出令

**上游令点名(J-81):** `夜班令15…` md5 `df6d35c74df5a20c215d15c86fd912b6` · `夜班令15续…` `a8e59f37b9bf291bef3e1ee5b4183705` · `小批09y…` `98a7cc7ccf0a9a88e8995f8e1b93f8c3` —— 三份桌面/仓一致,回执收到。

---

## 〇 🟢 `285b20d(08-26) → aa5e705` · CI 绿 run 35494233911

**两个半月来第一次。D194 那条无门禁建人的路,从线上下来了。** 六项、(丙) 四条、三行口径,我逐项核过,**没有一项是「说了但没证」。**

**但我用的是和坏消息一样的尺子(见 §一),这是你应得的,不是不信任。**

---

## 一 D194 那个 0:证据链我认,但**补最后十秒**

你给的是:`grep -c "path === '/auth/google/demo'" → 0` 与 `grep -c 'function registerGoogleDemoUser' → 0`。
**这两个 0 是在仓里量的,不是在线上那台容器里量的。** 成立需要三段:
① `/health` 报 `commit=aa5e705` ⇒ 线上跑的是这一版
② `/tmp/probe.js` 不在 ⇒ 容器真换了,不是老容器接着答
③ `aa5e705` 的源码里那两处是 0

**三段都在,结论成立 —— 但它是推出来的,不是量出来的(J-88 分栏)。**
🔴 **边界令② 明许「只读进容器(`cat`/`ls`/读 `/proc`)」,所以补一次直接量是十秒的事:进容器 `grep -c` 一次那两个串。**
**这是本案最贵的一个 0,它值一次直接量。** 补完写一行进回执,**不许再去打那个路由一次(J-72,你没打,对)。**

### 立 J-103
> **一个让你高兴的结论,要按和让你难过的结论一样的标准验。**
> 人对坏消息天然多问三句,对好消息天然少问三句 —— **而「上线成功」是这一整案里最贵的一句话。**
同族 J-95(绿要说清在哪台机器上量的)。**J-95 管的是地点,J-103 管的是力度。**

---

## 二 🔴🔴 WAL 上线,让**我自己批过的一条豁免**到期了

**你报的:`journal_mode` delete → wal。** 这一条我要回头改我三天前的裁决。

**我在 09x 批准保留 `local-server.mjs:137` 那处 `copyFileSync(mainDbPath, …pre-import…)`,理由是你给的、我认了的那句:「`VACUUM INTO` 要独占读锁,而这一处跑在接口线程、导入事务之前」。**

**那个理由是在「生产是 delete 模式」这个条件下成立的。现在条件变了:**
- **WAL 模式下读不阻塞写** ⇒ 「会打架」这个前提**可能已经不成立**;
- **更要紧的:`cp` 一个 WAL 库,会丢掉 `-wal` 里还没 checkpoint 的那一段** ⇒ 这处快照的风险**从「静默变旧」升级成「就是会旧」**。

**⇒ 重开 D202-b,挡「导入功能被使用」,不挡上线(J-82)。** 要做的:
1. **在一个 WAL 库上现测**:在那个位置调 `VACUUM INTO` 到底会不会真打架 —— **量出来,不许按理由推**(J-83)
2. 打不架 ⇒ **换掉 `copyFileSync`,走 `db-backup-core.mjs` 那个唯一出口**(它自带「当场打开验一次」)
3. 真打架 ⇒ 保留,但**拷之前必须先 checkpoint**,并且我 09x 加的那条断言(快照必须打得开 + 逐表读出行数)**从「事后验」改成「拷完立刻验,不过当场红」**

🟢 顺带核过:`db-backup.mjs` 已走 `backupDb`(VACUUM INTO + 当场验),`knife-backup.sh` 的 `cp` 在造病台不碰生产,`demo-reset.mjs:47` 是死参 —— **白名单只剩这一处,所以它更值得治。**

### 立 J-104
> **一条豁免是在某个环境条件下批的。那个条件变了,豁免自动到期,要重新报批 —— 不是继续有效到有人想起来为止。**
> 批豁免的时候必须写下**它挂在哪个条件上**;条件一变,是**提出豁免的人**负责回来说一声。
同族 J-87(按绝对值设的阈值要随规模重估)。**J-87 管数,J-104 管条件。**

---

## 三 四件裁决

### 1. 推后那次备份 —— 🔴 **补拍,现在就拍,而且理由不是你说的那个**

你说「生产库 mtime 从 8/27 起没动过,所以 09-18 那份在**数据上**是当期的」—— **数据这一面我认。**
**但库的形状变了:表 79 → 92 · `journal_mode` delete → WAL。**

> **拿一份 79 表的旧形状备份,去还原给一份 92 表的新代码 —— 那是 schema 不匹配。**
> **一份还原不回去的备份,和没有备份是一回事** —— 这句话是我在 D202 里立的,现在轮到它咬我们自己。

**补拍口径:`VACUUM INTO`(不许 `cp`)· 拉回本机 · 打开验可读 · 表数必须是 92 · 五表行数 · 两头 sha256 · 路径与字节数写进回执。**
**顺带把「自动备份的第一拍有没有真的落下来」一起报**(推的时刻 UTC 向后第一个跨过 04:00 UTC 的 6 小时节拍)。

### 2. D205 + J-102(12 支种子逐支冒烟)—— **批,排第二段第一批,不挡任何事**

- **D205 修法按 15-续 裁死的走:不许动币种红线去迁就脚本。**
- **J-102 的落地只要最便宜的那一档**:能跑通、不报错即可,**不验内容**。贵了就没人维护,没人维护的判据下场和这支坏种子一样。

### 3. 兜底队列 0 件 —— 🟢 **不追究,而且你判得对**

**整夜的正确优先级就是 (丙) 和上线,兜底队列本来就是「还有时间才做」的那一档。**
**你如实写「代价说清,不粉饰」——这正是 J-82 想要的样子:不挡的事就说不挡,做不完就说做不完,不要为了交差去动优先级更低的东西。**

### 4. 合同图 v1 四屏 —— 🟢 **我自己查完了,答案在这儿**

**图出了,而且早就发布了。** 「**签署文件留档**」这张图 **09-12 就发布过**,四屏齐(屏1→屏4,含「第一次充值没签权益书在结算页提醒」那一条)。
**所以不是没画、也不是没送到 —— 是那天之后又过了几十批,那张卡片沉进历史里了,她记得批过但翻不到。**
🔴 **从今天起的口径(不立法,写进台账):凡是要店主看的图,发布之后,在当批回执里把链接重贴一次;隔批再提到它,也要再贴一次。一张找不到的图,等于没画。**

---

## 四 记你四功:**这一夜咬到你四次,四次都是真病**

1. 🔴 **`test-admin-accounts` 凭证路径焊死在 `local-data`** —— 回归跑在临时库上,它一直在读店主本机那份凭证去验**另一个库**;而且 `else` 分支 `checks += 3` **不打 ok 行**。
   **一个不打 ok 行就加分的分支,就是假绿的本体(J-86 同族)。两处都修了,这一笔最重。**
2. **全仓 4 处硬写沙箱库路径,第一轮只改了 2 处,自己发现并全走唯一出口** —— **「改一处硬路径永远是一类」**(J-78 同族),重扫 0 残留,闭合。
3. **`seed-jics-nail` 跑不通** —— 已立 D205,而且它换来了 J-102。
4. **我 09p 那把还原指纹判据在 CI 上误红** —— **「判据没错,期望值取错了」**,归得准:沙箱故意住临时目录,判据却一律拿仓根比。**这是 J-97 的同族:参照系随环境变,而判据把参照系写死了。**

**四件里有三件是你自己发现、自己归族、自己改完两面验的。** 这一夜的价值不止在上线。

---

## 五 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. 🔴 **进容器直接 `grep -c` 那两个串的结果**(只读,十秒)
2. 🔴 **补拍的备份**:路径 · 字节数 · sha256 两头 · **表数 92** · 五表行数 · `VACUUM INTO` 不是 cp
3. **自动备份第一拍落下来没有**
4. **D202-b**:WAL 库上现测 `VACUUM INTO` 在那个位置打不打架(**量出来的**),以及按哪一条处置
5. D205 / J-102 只要一句「已排第二段第一批」
6. 三行口径

**停线一个字没松。上线了不等于松线** —— 生产只读、不碰 Railway 任何配置变量 volume、不向生产发会写入的请求、顾客端加注册口一律停下问。
