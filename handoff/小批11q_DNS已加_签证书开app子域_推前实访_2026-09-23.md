# 小批 11q —— DNS 已加:签证书 → 开 app 子域 → 实访 200 → 才推

店主 2026-09-23:`app` A 记录已加 → `82.157.154.185`。足部加收 = 加价 100 与时长无关(CSV 已改)。

## 顺序(一步验一步,任何一步不过就停并报)
1. 只读:`dig +short app.jingshengyouji.com` 必须返回 `82.157.154.185`;不是就停(DNS 没生效,不签)
2. 官网实访 200(改动前基线,红线:官网掉线一秒都不行)
3. `certbot --nginx -d app.jingshengyouji.com`,**同一个 Let's Encrypt 账户**(邮箱那条纪律不变:只用于这台机器,不写进别处)
4. `ln -s sites-available/app… sites-enabled/` → `nginx -t` → reload
5. 实访三条,全要 200:`https://app.jingshengyouji.com/health`(`dataScope: live`)· `/platform` 登录页 · `/?store=jics-nail`;**官网再测一次 200**
6. **五条全过 ⇒ 推 `0c5ca0c` + `8909005`**(推前清单 + 当期备份照旧)
7. 推完:生产只读实访 `app` 域名下小婕店签署链接域名 = `app.jingshengyouji.com`(11p 那条判据的生产版)
8. 写给店主:平台账号那两个变量现在可以设了(引用 `11p_三_…` 那份)

## 不许
- 不碰官网那份 nginx 配置 · 不碰 Railway 变量 · 证书私钥不进任何回执
