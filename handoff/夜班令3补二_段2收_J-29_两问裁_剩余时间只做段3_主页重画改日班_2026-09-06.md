# 夜班令 3 补二 — 段 2 收(`74807ad`)· J-29 登记 · 两问裁 · 剩余时间**只做段 3(D145)**,主页重画两段改到日班 · 段 2 超时的处置

> 2026-09-06 · Cowork 亲核(店主在睡)· 两处各一份

## 一、段 2 收

两库现查:`luvia-bj | LUVIA 半径美甲美睫 · 北京旗舰店 | CNY | Asia/Shanghai | 北京市朝阳区CBD万达广场6号楼2005`,bookings 50,`boss-luvia-bj` owner 在(首登强制改密 1);`lucky-luxe.sqlite.pre-luvia-bj-20260906T124831` 备份在;`tools/tenant-fingerprint.mjs` / `seed-luvia-bj.mjs` / `db-backup.mjs` 在。「既有行一行没动」这次是逐行指纹证的——**收,这才是「未动」该有的样子**。

**J-29(登记)**:`db-snapshot.mjs` 只比 行/列/索引/触发器/默认值,看不见 UPDATE;以往凡靠它背书的「未动」严格说只证到「结构与行数未动」。裁:`receipt-db-proof.sh` 的「未动/有动」那一行改为**同时**跑 `tenant-fingerprint`(逐行指纹)与 `db-snapshot`,两把都绿才打印「未动」;造病:改一行既有数据 → 打印「有动:〈表〉旧行消失」。这条进 05o 段 4 的小件。

「时区刀本来就咬住,是我的筛子只看 `^not ok`」——收,更正照写;三店新增三条(B/C 同币同时区、同口径另一家看不见北京店的单)**今晚没挨刀** → 记进段 4 小件,不许留到「以后」。

三处夹具日期病(逢周日 13 条红、`store-jury` 撞车)修法是改景不改判据——收。

## 二、两问裁

1. **全量没全绿(三把小程序刀没跑)**:停线「不碰 `miniprogram/`」指的是**不改那目录的代码**,不是不许起它的自动化会话。开发者工具的 automation 会话是测试夹具,**起它只报不问**(04g 起的口径)。裁:起会话 → 跑那三把 → 全量绿 → 日志进 `regression-logs/`(文件名与收尾行对上)。做不起来(GUI 起不来)就如实写「本轮未跑:三把小程序刀,原因」计入「本轮未跑」,**不算绿**。
2. **「需报价项目」没标志位**:**从今晚清单划掉**。裁记 **D146**:`services` 加 `price_mode`(`fixed | quote`,开机 ADD COLUMN,回执头部报「本批含开机迁移」),报价路与 ③ 采集「项目对到价目(固定价 vs 需报价)」都只认这一列——排在 D145 之后的日班做,不在今晚。北京店 mock 里那两个「需报价」项目先按固定价造,回执注明。

## 三、时间:段 2 用了 4 小时,六小时只剩约 1 小时

段 3(D145)预算 75 分——**只做段 3**,做完提交、写总结即止;段 4 小件并进明天日班;**段 5/6 主页重画改日班**(店主早上给指令时我重排)。段 3 做不完就停在可提交的地方(`classifyTurn` + 常驻套件先于五通 v3;五通 v3 与到底率三跑放日班),总结如实写到哪。

## 四、店主要知道的一句(Cowork 写进总结)

`boss-luvia-bj` 是一次性密码、首登要改——Code 走的是平台正门重置口(脚本不许直连库改老板密码),这是对的;店主早上第一次登北京店会被要求改密,改成她那个本地走查密码即可。
