# 小批 09x · 裁「三套要活沙箱的判据」· 批(乙)但绑三条 · (丙)排推后第一件 · 立 J-98 J-99
2026-09-19 · Cowork 出令

**上游令点名(J-81):** `小批09w_CI红真根因_lst在干净checkout上是随机的_记我第33笔_立J96J97_2026-09-19.md` md5 `2160a51b0b75ddf7156489fe66f10fa5` —— 你回执首行已点名,**桌面/仓一致,收到。**

---

## 〇 先认三件你做对的

1. **09w 两处改完,两面刀过,干净 clone 旧版 exit 1 / 新版 exit 0 —— 同一个 clone。** 这是本案第一次有人把「两台机器不一样」变成**可重复的对照实验**,而不是各说各话。
2. **(乙) 你提前做了,而且你自己在回执里说了「它成了唯一拿得到原因的路」。** 🟢 **合规,不记账。** 我批的是**内容**(合成一条 · 不加 `bash -x`),两条你都守住了;时机提前并主动报备,正是 J-82 想要的样子。
3. 🔴 **第一版盲取末 80 行被长度上限截断在中间** —— 你把它归到「取错了那一段」,归对了。**这和「只显示前 10 条」是同一条法:一个有上限的展示窗口,决定你看见哪一段的不是重要性,是位置。** 改成红行优先一次就拿到,干净。

**以及你自己记的那一笔,我接受,并且给它立成法**(见 §四 J-99)。**「我复现不了」有两种:真的复现不了,和没找对那个变量** —— 这句话值这 24 天。

---

## 一 裁:批 **(乙)**,但绑三条,少一条都不算批

**先说为什么不是 (甲)。** 把这 3 套挪到门关档,**覆盖面照样掉,而且掉得更隐蔽** —— 门关档的红是单独列的,不进主档那本账。**(甲) = (乙) 的坏处全有,加上没人看得见。否。**

**也说为什么今天不是 (丙)。** (丙) 是对的答案,但它今天做不完(要现造沙箱库 + 起服务 + 喂样本单和 B 店顾客),而**现在挡在生产前面的那个洞(D194)已经开了两个半月**。**让 (丙) 挡着 D194 再多开一夜,是拿更大的风险换更好的形状。**

### 批 (乙) 的三条绑定(缺一不可)

1. 🔴 **「未跑」必须被计数、被点名。** 主档收尾除了 `not ok` 那一行,必须再打一行:
   `未跑 N 套(拿不到夹具):<逐个套件名>`
   —— 只写「未跑」不写几套、不点名,等于没写。
2. 🔴 **N 设上限棘轮 = 3,只许降,不许涨。** 涨了当场红。
   **理由在 §三 J-98:不计数的「未跑」,是「假绿」的慢动作版。** 今天是 3 套;没有棘轮,半年后它会是 30 套,而每一步看起来都合理。
3. 🔴 **本机全量这 3 套仍必须真跑、真绿。** 回执口径从今天起**由两行升三行**:
   - `本机全量:128/128 · 4,152 · not ok 0(哪台机器)`
   - `GitHub CI:绿 / 红(run id)`
   - `GitHub CI 未跑:N 套(逐名)` ← **新增,不许省**

**并按 J-85 记让路账:这是一笔明写的覆盖面欠账,欠账人是 (丙),排推后第一件。** 不许拿「(乙) 已经批了」当成这件事结了。

---

## 二 (丙) 我先替你量了一下,不是让你从零估

我在那台 Linux 上读了仓库(不改任何产出,J-55②):

- 🟢 **`apps/api/start-sandbox.sh` 存在**(5,021 字节),用法 `bash apps/api/start-sandbox.sh [DATA_DIR] [--ai-env <文件>]`,**收 DATA_DIR 位置参数** —— CI 上可以指一个临时目录,不碰仓内 `sandbox-data/`。
- 🟢 `.gitignore:26` 确认 `apps/api/sandbox-data/`,所以库确实要现造 —— **但 `tools/` 下有 12 支 `seed-*.mjs`**,其中 `seed-demo-rich.mjs` 与 `seed-jics-mirror.mjs` 看名字正对着那条红要的「样本单」与「B 店顾客」。
- 🟡 `test-tenant-ownership.mjs:70` 开 `apps/api/sandbox-data/lucky-luxe.sqlite`、`:86` 打 `http://127.0.0.1:4310` —— **路径是写死的**。(丙) 要么让 CI 把库造到那个固定路径,要么把这两处改成可配。

**(丙) 的回执我只要一份可行性,不要你直接干:** 哪一支 seed 能造出那条红要的两样东西 · 起沙箱要不要 `ALLOW_DEMO_ADMIN_LOGIN`(**CI 属 `DEV_SCOPES` 里的 `ci`,不是生产,可以;但要在回执里明写它只在 CI 那一步存在**)· 估时。

🔴 **(丙) 落地那天,N 的棘轮从 3 降到 0,那才是这件事真正结案。**

---

## 三 立 J-98

> **一个判据在拿不到夹具时,允许它说「未跑」——「未跑」不等于「通过」,这是对的(J-58①)。**
> **但「未跑」必须被计数、被逐名点出、并设一个只许降的上限。**
> **不计数的「未跑」,是「假绿」的慢动作版:每一次多一套都有正当理由,而没有任何一步看起来像在放水。**

同族:J-86(恒成立的报错比没有报错更坏)· J-53②(回落成空值 ⇒ 永远通过)。**区别在于速度:那两条是当场变假绿,这一条是一年后才变。**

## 四 立 J-99(这条是你挣来的)

> **说「我复现不了」之前,先说清楚你认为的变量是哪一个。**
> **复现失败必须点名变量,不点名的「复现不了」不算结论 —— 它只是一句感觉。**

你上一批说「本机没有容器运行时,复现不了 Linux」,把轴认成了 **macOS vs Linux**;真正的轴是 **工作树 vs 干净 checkout**,而 `git clone --depth 1 file://$(pwd)` 你一直做得到。**一旦点名了变量,就会发现手里早就有那把工具。**

---

## 五 D202 的断言:收下

`apps/api/test-backup-wal.mjs` ⑤a/⑤b,快照生成后必须打得开并逐表读出行数(现测 2 表 · a=300),**且带反向守(截断成废文件必须红)**。🟢 **两面都有,这才叫立住了。** 基线 4144→4146 的 +2 对得上。

---

## 六 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. (乙) 三条绑定逐条落地在哪一行:①「未跑 N 套(逐名)」那行的实际输出 ②N=3 的棘轮写在哪 ③三行口径的回执样例
2. **干净 clone** 预检 exit 0(已有,复述即可)
3. 🔴 **CI 上绿(run id)** —— 这是这次唯一的过关线(J-95)
4. (丙) 的可行性回执(§二 三问),**不要直接干**
5. 推完六项核验 + 推后第一次自动备份的节拍(推的时刻向后第一个跨过 04:00 UTC 的 6 小时节拍)

**停线一个字没松:** 不碰 Railway 任何配置/变量/volume · 不向生产发任何会写入的请求 · 不改合同图凑代码 · 不松判据(**(乙) 不是松判据:它把「验过」改成「没验」,是把话说得更准,不是更松**)。
