# 本机任务队列

> 此文件由 Cowork 端的 Claude 写入,由「执行本机任务.command」触发 Claude Code 执行。
> 执行规则:按顺序执行;每个任务完成后在 handoff/本机任务结果.md 记录结果;全部完成后把任务标记 [已完成]。
> 只做文件里写明的事,不自由发挥;拿不准的记入结果文件。
> 背景:上一批在任务 4 阻断(云端非 GitHub 自动部署,git push 不上云)。本批按你建议的修正计划续做。
> Cowork 端已新增 `.railwayignore`(排除 local-data/864MB 库、.env、backups 等),解除你对 railway up 误传大文件的顾虑。

## 待执行任务(数据迁移·续做批)

### 任务 0:提交 .railwayignore
1. `git add .railwayignore handoff/本机任务.md && git commit -m "Add .railwayignore: exclude local data and secrets from railway up snapshots"`
2. `git push origin main`

### 任务 1:railway up 部署新代码
1. `railway up`(部署 b11bfa2+ 的代码;快照体积应在几 MB 量级,若上传过程显示异常巨大,中断并记录)。
2. 等待部署完成,`curl -s https://www.luckyluxeatelier.com/health` → 200。
3. **验证新代码确实上云**:POST /admin/auth/login,body `{"email":"nini3131254931@gmail.com","password":"x"}`,预期返回 403 且报错含"账号不存在"(新版门控生效的独有特征;若返回 200 说明还是旧代码,停止并记录)。

### 任务 2:开启导入开关
1. `railway variables --set "ALLOW_DB_IMPORT=true"`,等重新部署,`/health` 200。

### 任务 3:上传迁移库
1. `/tmp/lucky-luxe-migrate.sqlite`(上批已生成,3.7MB)仍在则直接用;不在则重新生成:`node tools/prepare-migration.mjs apps/api/local-data/lucky-luxe.sqlite /tmp/lucky-luxe-migrate.sqlite`(前置:确认本机无 local-server 进程)。
2. 沿用你上批的 `railway run` 注入 token 方式上传到 `https://www.luckyluxeatelier.com/admin/ops/import-db`(带 `x-confirm-import: yes` 头,不打印 token)。
3. 预期 `{"staged":true,"bytes":约3900000,...}`;等待约 60 秒服务重启。

### 任务 4:云端核对
1. `/health` → 200。
2. 用 token 请求 `/admin/customers`、`/admin/bookings`,与快照核对:users=33、bookings=160;`/admin/finance/lock-status` 预期 `{"configured":true}`。逐项记入结果文件。

### 任务 5:收尾
1. `railway variables --set "ALLOW_DB_IMPORT=false"`
2. `rm /tmp/lucky-luxe-migrate.sqlite`
3. 尝试 CLI 删除自定义域名 app.luckyluxeatelier.com;不支持则记录留给网页。
