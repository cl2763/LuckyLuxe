# 本机任务队列

> 此文件由 Cowork 端的 Claude 写入,由「执行本机任务.command」触发 Claude Code 执行。
> 执行规则:按顺序执行下面的任务;每个任务完成后在 handoff/本机任务结果.md 里记录结果
> (成功/失败、关键输出、遇到的问题);全部完成后把本文件中的任务标记为 [已完成]。
> 只做文件里写明的事,不要自由发挥;拿不准的记入结果文件而不是猜。

## 待执行任务

### 任务 1:提交并推送当前全部改动
1. `git status` 确认变更清单;确认其中**不包含** `local-data/`、`*.sqlite`、`.env`、`初始老板账号.txt`(若包含,停止并记入结果文件)。
2. `git add -A`
3. 提交,信息用:
   `Walkthrough fixes round2 + Cowork↔ClaudeCode pipeline: narrow-screen staff view, seed retry, CI subset flag, handoff protocol`
4. `git push origin main`
5. `git log --oneline -1` 把最新提交号写进结果文件。
