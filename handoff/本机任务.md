# 本机任务队列

> 此文件由 Cowork 端写入,Claude Code 执行。只做写明的事,不自由发挥;拿不准就停下记入 handoff/本机任务结果.md。
> 历史"数据迁移"批次已全部完成(见 git 历史)。以下为 2026-07-16 新批次。

## 【给 Code 的指令】提交本轮改动 + 全量回归 + 部署生产

Cowork 端本轮改了一批**后端安全修复 + 会员链路 + 顾客端功能 + 网页会员套餐界面**,需要你提交、跑全量回归、推生产。

### 一、先看工作区
`git status` + `git diff --stat`,确认改动主要在:
- `apps/api/local-server.mjs`(后端安全 + 会员/seed;以及上一提交已含的会员套餐券/published-works/schedule-week 若还在工作区一并带上)
- `apps/web/admin.js`、`apps/web/admin.html`(网页老板端「会员套餐/券」界面 + 版本号 `20260716-member`)
- `miniprogram/**`(顾客端+商家端一批页面;小程序不走 Railway,但要一起进 git)
- `启动服务器.command`(加 `--env-file-if-exists=apps/api/.env`)

**不要提交**:`apps/api/.env`、`apps/api/local-data/`、`backups/`、密钥/初始密码文件(确认在 .gitignore 内)。

### 二、后端关键改动(便于写 commit + 回归重点看)
1. **安全越权修复(重点)**:
   - `GET /bookings` → `requireCustomer` + 只返本人订单(此前不带 userId 返全店)。
   - `GET /users/:id` → `requireCustomer` + 只能查自己(此前任意 id 无鉴权)。
   - `POST /bookings` → `requireCustomer` + 强制 `user_id=登录用户`(此前无鉴权+取自请求体)。
   - `POST /bookings/:id/cancel` → `requireCustomer` + 归属校验(此前可取消任意订单)。
   - `POST /payments/mock/confirm` → `requireCustomer` + 归属校验。
2. `serializeUser.balanceCents` → 真实 `storedValueBalanceCents(user.id)`(此前写死 0)。
3. `seedDemoBookingsForUser` + `signInWechatMiniUser` 挂钩:仅 `ALLOW_DEMO_ADMIN_LOGIN=true` 且首登无单时铺演示数据;服务查询用 `UPPER(type)='NAIL'/'LASH'`;生产开关关闭不启用。

### 三、全量回归(全新库,dataDir 已隔离)
```
bash apps/api/run-all-tests.sh
```
要求 **21 套件全绿**(重点 customer-service-matrix 66/66、finance-core、stored-value、staff-portal、admin-accounts)。
- 本轮改的 `/bookings`、`/users/:id`、`/payments` 是顾客侧接口,测试走 `/admin/*`,理论不受影响。**若有套件变红,先停下贴回结果文件,别硬改测试。**

### 四、绿了再 commit + push
- commit message:`安全修复(bookings/users/payments 越权)+ 会员余额真算 + 演示铺单(大小写修复)+ 网页会员套餐券 + 顾客端会员链路`
- `git push origin main` → 等 GitHub Actions 全绿 → Railway 自动部署。
- 部署后冒烟:`/health` 200;`/admin/packages`、`/admin/coupons` 无授权 401;**`GET /bookings` 无 token 返 401**(确认安全修复上线)。

### 五、需要店主本人的(别替做)
- 微信支付:关联小程序 appid↔商户号、开通「小程序支付」、下载 API 证书+设 APIv3 密钥——店主在微信支付商户平台做。拿到密钥/证书后配到 **Railway 变量 + 本地 .env(不入 git)**,再回 Cowork 让我写后端下单/回调。
- 不要动 `miniprogram/utils/api.js` 的 `USE_LOCAL_SANDBOX`。

完成后把 **回归汇总 + push 结果 + Railway 状态 + 冒烟结果** 写回 `handoff/本机任务结果.md`,任务标 [已完成]。
