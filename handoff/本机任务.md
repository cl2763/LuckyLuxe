# 本机任务队列

> 此文件由 Cowork 端写入,Claude Code 执行。只做写明的事,不自由发挥;拿不准就停下记入 handoff/本机任务结果.md。

## 【给 Code 的指令】提交 网页第二批:员工管理页重组+考勤+业绩核查+小记画像+积分商城+AI召回+薪资方案 + 回归 + 部署 【待执行】

> 纯前端批次:只改 `apps/web/admin.js` + `apps/web/admin.html`,**零后端改动**(全部调用既有端点)。Cowork 端已 `node --check` 通过、本地 4128 对三个读端点冒烟通过(考勤看板/奖品列表/小记画像数据结构均正确)。

### 一、本批改动范围(git status 应只看到 admin.js、admin.html、handoff 文档)
`apps/web/admin.js` + `admin.html`(`ADMIN_BUILD='20260802g-web-b2'`,admin.html 两处 `?v=` 已同步;新增样式放 admin.html 内联 `<style>`,**未动 styles.css**):
0. **页面重组(店主 2026-08-02 定,当日三轮调整后终稿)**:侧栏「排班管理」改名**「员工管理」**(en: Staff;员工角色仍显示"技师业绩"),页内分两板块 tab:「📅 计时排班」(原周排班格子/默认时段/排班申请原样)与「📈 技师业绩」(技师业绩卡+薪资方案入口+打卡考勤)。技师卡**精简排版**:统计只留 **本月服务 X 单 + 本月业绩**(删"月人数"[口径=本月所有状态单的去重顾客,与完成单口径打架易误读,店主拍板去掉]与"好评率待点评"占位;首页技师小卡同步);按钮只留「💰 薪资方案」+「⋯ 账号管理」,编辑/停用/登录账号收进展开行。内部 page id 仍为 schedule,零路由改动。
1. **考勤看板(员工管理→技师业绩,仅老板)**:调 `GET /admin/attendance/today`:在岗/已下班/超时 三指标卡 + 每人行(状态色、上下班时刻、已工作/加班时长,超时行高亮);「修正/补卡」走 `PATCH /admin/attendance/:id`(无记录传 id='new'+technicianId+date 补卡),保存自动重算加班;切到业绩板块时自动刷新。「设打卡WiFi」按约定网页端不做(需真机取 BSSID)。
1b. **财务页「👥 员工工资」独立 tab + 业绩核查融合(店主定:工资月月变,不属于财务设置)**:员工工资从 财务设置 折叠项迁出,成为财务页左侧导航独立 tab(`data-fin-tab="payroll"`),月份跟随财务月份。页内三步:**① 本月试算**(原试算/±调整)→ **② 业绩核查**(同一响应的 attributionNotes 逐条:顾客/金额/名下技师/备注;+ 近14天完成单未写小记按技师汇总 `/admin/service-notes/pending?days=14`,「提醒TA」发站内 nudge `POST /admin/staff-nudges`;老板点「✓ 完成本月核查」,localStorage 按月记录,可撤销)→ **③ 锁定→发放入账**。**核查门槛(店主定,网页端拦截)**:未核查时锁定按钮 disabled+点击兜底拦截;小程序端不拦(那边锁定前本就展示归属备注核对块)。曾做过独立「月度业绩核查」卡在员工管理,店主要求并回员工工资,已删。
1c. **财务指标显隐+动效(店主 v4 设计稿定稿,四轮迭代)**:① 指标卡改为 本日收款/本月收入/本月支出/**本月净赚(已入账)** 四个真数(净利率撤下常驻卡);净赚**一实一虚**——工资未发放时小字「扣待发工资后约 $X(预估)」,取新工资试算实时合计(`owner.salaryEstimateCache`,替代读旧薪酬表的失效计提);② 旧四进度环(今日/本月/收支平衡/年度)删除(`financeRing` 函数已移除),改为:**「✦ AI 智能总结」卡**(模板即时生成 bullet,无 emoji:节奏预估/平衡线状态/目标差距/工资待发,微光+逐条浮现动效)+ **设了才显示**的目标进度条(带平衡线黑刻度+悬浮口径说明,标「估算」)/年度条(单独设了年目标才出现);没设目标只留一行淡引导「去设置」;③ **「设个目标」聚焦弹窗**(cs-lightbox):目标类型(净利/营收)/月目标/变动成本率(**「使用智能建议值」深棕按钮**,建议率=(上月总支出−固定支出)÷上月收入,上月无收入不出按钮;提示句:新店默认25%…之后每月用上月真实值)/年目标可选,保存走既有 `PUT /admin/finance/targets`;④ 动效:金额数字滚动(1秒,10秒内不重播)、进度条生长、AI总结逐条浮现,均只在渲染时播一次。财务设置里原目标设置折叠项保留(文案同步),两处同一端点。
2. **小记画像**:客户档案详情页(renderCustomerDetail)在运营信息与消费记录之间插入「服务小记 · 画像」只读区:调 `GET /admin/customers/:id/notes`,画像标签分组展示(**安全项红底醒目**)+ 到店统计(次数/平均间隔/常做)+ 小记时间线(日期·项目·技师·原文)。
3. **积分商城管理**:会员套餐/券页新增第三卡「积分商城」:奖品列表(`GET /admin/points-prizes`,含券面额/积分/库存/已兑/限兑/有效期);「+ 新增奖品」支持选现有券或**当场建券**(先 saveCoupon → 拿新券 id → `POST /admin/points-prizes`,参考小程序 points-mall 流程);编辑走 PATCH;上/下架切换;「撤销兑换」输券码调 `POST /admin/points-mall/revoke`。
4. **AI 召回话术**:客户档案列表卡与详情页,**沉睡S 层**客户显示「✦ AI 召回」按钮 → `POST /admin/ai/recall-copy {userIds:[id]}` → 话术自动复制到剪贴板(clipboard 被拦时降级弹窗手动复制)。
5. **薪资方案配置(店主 2026-08-02 补充要求,按其要求与小程序同逻辑放员工管理处)**:入口在 员工管理→技师业绩——区头「💰 薪资方案」按钮(列表总览:全店默认+每技师)+ 每张在职技师卡「💰 薪资方案」按钮(直达该技师编辑),点开为弹窗面板(cs-lightbox);与小程序 员工管理→薪资方案 同字段同口径(`GET|PUT /admin/salary-plans`、`DELETE /admin/salary-plans/:techId`):全店默认+每技师专属覆盖(可删除恢复跟随默认);三模板(纯提成/底薪+阶梯提成[浮动绩效,最多8档表格可增删]/底薪+固定提成)+手工费/耗卡%/充值%/加班费率与计费单位;保存立即生效,财务页已解锁时同步刷新工资试算;「员工工资(月结)」无方案提示指向该入口。仅 owner 可见(员工端不渲染按钮,后端亦 owner-only)。

### 二、回归
`bash apps/api/run-all-tests.sh` **21 套件须全绿**(本批零后端改动,理论零影响;任何红:停下,失败输出原文写结果文件,不改测试)。跑完按 `启动服务器.command` 配置重启本机 4128。

### 三、提交 + 部署
commit(建议信息:`feat: 网页端员工管理页(排班/业绩两板块)+考勤看板+月度业绩核查门槛 + 小记画像 + 积分商城管理 + S层AI召回 + 薪资方案配置`)→ push main → CI 全绿 → Railway 自动部署。

### 四、生产冒烟(只读,记入结果文件)
1. `/health` 200。
2. 浏览器打开生产 `/admin` → 控制台打印 **`[admin] build 20260802g-web-b2`**;生产 admin.js 内含 `renderAttendanceBoard`/`data-sal-review`/`savePrize`/`loadCustomerNotes`/`openSalaryPlanPanel`/`openGoalSetupModal` 标识;生产 admin.html 内含 `fin-ai`(内联样式)。
3. `GET /admin/attendance/today`(token)→ 200 含 `{date,working,done,overtime,rows}`(既有端点,验未回退)。
4. `GET /admin/points-prizes`(token)→ 200(生产 `{prizes:[]}` 属正常)。
5. `POST /admin/points-mall/revoke` body `{"code":"LL-FAKE-0000"}`(token)→ 404「券码不存在。」(无写入)。
6. `POST /admin/ai/recall-copy` body `{}`(token)→ 400「缺少顾客。」(无写入,不耗 AI)。
7. `PATCH /admin/attendance/nonexistent-id-000` body `{"clockIn":"09:00"}`(token)→ 404「考勤记录不存在。」(无 technicianId+date 不会建档,无写入)。
7b. `GET /admin/salary-plans`(token)→ 200(生产未配置=`{defaultPlan:null,plans:[]}` 属正常);`PUT /admin/salary-plans` body `{}`(token)→ 400「阶梯模板至少要有一档。」(校验拦截,无写入)。
8. 登录后视觉走查(员工管理两板块:计时排班/技师业绩[精简技师卡+⋯账号管理展开+薪资方案弹窗+打卡考勤]、财务页新指标区[四真数+AI智能总结+目标条或引导行+设个目标弹窗与智能建议值]、财务→👥员工工资 tab 的 试算→②业绩核查→锁定 三步与核查门槛、客户详情画像区、会员页积分商城卡)**留店主本人**(不代输密码,照旧)。
9. **不在生产修正打卡/不建奖品/不撤销兑换/不配薪资方案/不造数据。**

### 五、禁止项
不提交 `.env`、`local-data/`、`backups/`;不动后端文件;不动生产数据。

完成后结果写 `handoff/本机任务结果.md`,本任务标 [已完成]。

---

## 【给 Code 的指令】提交 企微微信客服·出站链路(自建应用一期)+ 回归 + 部署 【待执行,在网页第二批之后跑】

> 背景:企微客服**入站**链路(回调验签/AES解密/AI回复/转人工)早已在库;本批补**出站**:gettoken(缓存+自动续期)、kf/sync_msg 拉真实顾客消息(cursor 持久化于 tenant_settings)、kf/send_msg 把 AI 回复发回顾客。**纯新增,守卫在密钥之后**:`WECOM_CORP_ID`/`WECOM_CUSTOMER_SERVICE_SECRET` 未配置时走原 mock/JSON 路径,行为与现网完全一致(Cowork 已本地冒烟:mock webhook POST 返回结构不变,AI 正常回复);因此 matrix 66 与 21 套件零影响预期。

### 一、改动范围
仅 `apps/api/local-server.mjs`:新增 wecomOutboundReady/getWecomAccessToken/wecomApiPost/readWecomKfCursor/saveWecomKfCursor/sendWecomKfText/syncAndProcessWecomKfMessages;webhook POST 在「加密payload+Token+OpenKfId+密钥齐备」时立即 200 'success' 并异步拉取处理,否则走原路径。`git status` 应只见 local-server.mjs + handoff 文档(+若店主已给密钥则 .env 本地改动,**照旧不入 git**)。

### 二、回归 + 提交 + 部署
1. `bash apps/api/run-all-tests.sh` 21 套件全绿(任何红:停,原文写结果文件)。
2. commit `feat: 企微微信客服出站链路(sync_msg拉取+send_msg回复,密钥未配时行为不变)` → push → CI → Railway。
3. 若店主已提供密钥:把 WECOM_CORP_ID / WECOM_CUSTOMER_SERVICE_SECRET / WECOM_CUSTOMER_SERVICE_TOKEN / WECOM_CUSTOMER_SERVICE_AES_KEY / WECOM_OPEN_KFID 写入 Railway 环境变量(和本机 .env),**不写进聊天/不入 git**。

### 三、生产冒烟(只读)
1. `/health` 200。
2. `GET /wechat/customer-service/webhook?echostr=test`(不带合法签名)→ 403(验签生效)。
3. 密钥未配时:POST mock JSON 到 webhook → 200 ok:true(原行为)。
4. 跑完重启本机 4128。完成后标 [已完成]。

---

## 【给 Code 的指令】提交 免定金开关(无支付备选)+ 回归 + 部署 [已完成 2026-08-02,commit 1317720,详见结果文件]

> 第三批之后的新改动,量小。
### 改动范围
后端 local-server.mjs:`GET|PUT /admin/booking-rules`(tenant_settings key=booking_rules;PUT owner-only);createBooking 读 `onlineDeposit`(默认 true,关=depositRequired 0→顾客自约免定金直接 CONFIRMED,无支付环节;adminDirect 不受影响);**AI 免定金感知**:buildCustomerServiceContext 仅 onlineDeposit===false 时注入 depositMode 事实,ai-utils createCustomerServiceReply 接收附 prompt(**默认不注入,matrix 行为零变化,CI 默认路径不受影响**)。
小程序:门店设置→预约规则加「线上定金」开关(确认弹窗)。
网页 admin.js:客户档案页加 RFM 分层(汇总条+徽章,只读展示);ADMIN_BUILD='20260801b-rfm-web'+admin.html 两处 ?v= 已同步。生产网页冒烟:控制台 build 字符串 + 客户档案页出现分层汇总条。
**归属备注**:bookings 加 attribution_note 列(迁移);`POST /admin/bookings/:id/attribution-note`(员工限本人单 assertStaffCanAccessBooking,备注自动带记录人+日期);estimate 未锁定响应加 attributionNotes(当月带备注的单);小程序台面动作单加「✎ 归属备注」、工资试算锁定前显示备注核对块。冒烟补:`POST /admin/bookings/<不存在id>/attribution-note` → 404 不写入。
### 执行
1. 21 套件全绿(重点 entitlements/finance-core:默认开=行为与原一致,测试理论零影响;有红先停)。跑完重启本机 4128。
2. commit `feat: 线上定金开关+AI感知 + 归属备注 + 网页RFM分层` → push → CI → Railway。
3. 生产冒烟:`GET /admin/booking-rules`(token)→ 200 `{rules:{onlineDeposit:true}}`(默认开,生产行为不变);`PUT` 不带 body 改动 → 不执行(**不要在生产改开关**)。
4. 结果写结果文件,标 [已完成]。

---

## 【给 Code 的指令】提交 积分商城全链路 + 券发放期限覆盖 + 服务类型扩充 + 回归 + 部署 [已完成 2026-07-31,commit 843a8d6,详见结果文件]

> 自 0cc1594 以来的改动。后端 local-server.mjs + 小程序前端(照旧入库不经 Railway)。

### 一、本批改动范围
后端(local-server.mjs):
1. **积分商城**:新表 `points_transactions`(**append-only 触发器**:UPDATE/DELETE RAISE ABORT)+ `points_prizes`;余额=完成单推导+台账(`earnedPoints/pointsLedgerSum/pointsBalance` helpers);`serializeUser.points` 改为同口径(推导+台账);owner 端点 `GET|POST|PATCH /admin/points-prizes`、`POST /admin/points-mall/revoke`;顾客端点 `GET /my/points-mall`、`POST /my/points-mall/redeem`(BEGIN IMMEDIATE 事务:复核库存/限兑/余额→发券→扣分→库存减)。
2. **券发放期限覆盖**:`grant` 与 `grant-batch` 接受可选 `validDays`(1-365)覆盖本批到期日,不改券模板。
3. **服务类型**:schedule-day groupOf 补 CARE/OTHER→护理色相;`/admin/services` 与平台端建服务类型校验放开为 NAIL/LASH/CARE/OTHER(两处,报错中文);顾客端 `/services?type=care` 聚合返回 CARE+OTHER。
4. **储值归属技师**:stored_value_transactions 加 technician_id 列(迁移);充值/耗卡端点收可选 technicianId(校验本租户);薪资 estimate 的充值/耗卡提成按归属流水当月汇总。
5. **工资手动调整**:新表 salary_adjusts;`PUT /admin/salary/adjust`(owner+财务钥匙,已锁定 409,非零必须备注);estimate/快照含 adjustCents/adjustNote。
小程序:points 页占位→真商城、merchant/points-mall 新页(奖品可当场新建券)+manage 入口、customers 群发券弹窗可输本批有效期、service-edit/services 支持 护理/其他、顾客服务页第三 tab「护理·其他」、member 充值加经手技师、salary-month 调整/充值/耗卡提成行、券包 revoked 显示、我的页积分商城入口修复、app.json 注册。
网页 `apps/web/`(财务纪律:随本批一并部署生产):admin.js 财务页「工资」小节整体换新版薪资系统(试算/调整/锁定/发放入账,与小程序同 API 同口径;旧薪酬草稿 UI 移除,旧 API 未删不影响);储值充值/耗卡表单加「经手技师」下拉;`ADMIN_BUILD='20260801-salary-web'` + admin.html 两处 `?v=` 已同步。

### 二、回归 + 部署 + 冒烟
1. `bash apps/api/run-all-tests.sh` 21 全绿(注意:serializeUser.points 口径加了台账项,台账为空时数值不变,理论零影响;`entitlements`/`customer-service-matrix` 若有红先停)。跑完按 `启动服务器.command` 配置重启本机 4128。
2. commit(建议:`feat: 积分商城全链路 + 券期限覆盖 + 储值归属技师 + 工资调整 + 服务类型四类 + 网页版薪资系统同步`)→ push → CI → Railway。
2b. 生产网页冒烟:打开 `https://www.luckyluxeatelier.com/admin`,控制台应打印 `build 20260801-salary-web`;财务解锁后「工资」小节为新版试算(生产未配方案=每人「未配薪资方案」提示,属正常);储值表单出现「经手」下拉。
3. 生产只读冒烟:`/health` 200;`GET /admin/points-prizes`(token)→ 200 `{prizes:[]}`;`POST /admin/points-prizes` body `{}` → 404「券不存在」(不写入);`GET /my/points-mall` 无 token → 401;`POST /my/points-mall/redeem` 无 token → 401;`GET /services?type=care` → 200(生产无该类=空数组);`PUT /admin/salary/adjust`(token、无 finance key)→ 403。**不在生产上架奖品/不造数据/不调整工资。**
4. 结果写 `handoff/本机任务结果.md`,标 [已完成]。

---

## 【给 Code 的指令】提交 工资入账闭环 + 沉睡召回周报 + 真机布局修复 + 回归 + 部署 [已完成 2026-07-31,commit 0cc1594,详见结果文件]

> 自 2570239 以来的改动。后端 `apps/api/local-server.mjs` + 小程序前端;小程序端照旧不经 Railway,入库即可。

### 一、本批改动范围
后端(local-server.mjs):
1. **工资入账闭环**:salary_payrolls 迁移加 paid_at/paid_by/txn_id;`POST /admin/salary/payout`(owner+financeKey:须已锁定未发放,事务内逐人 insertFinanceTransaction(expense/工资/source=payroll,tags=payrollId,哈希链)+标 paid;重复 409);unlock 已入账→403 ALREADY_PAID;`GET /admin/salary/pending-payout`(owner+financeKey);estimate 锁定响应加 paid/paidAt;my-estimate 加 payrollLocked/Paid/Total。**只追加账本,无任何 UPDATE/DELETE 流水。**
2. **沉睡召回周报**:`GET /admin/recall-digest`(owner):按周(门店时区周一)懒生成沉睡客 top5 AI 话术,存 tenant_settings key=recall_digest;CI 无 AI 密钥走 fallback 模板,不阻塞。
3. pending 端点响应加 `date` 锚点字段(前端跨天标注用)。
4. 服务类型扩充:schedule-day groupOf 补 CARE/OTHER→护理色相映射(小程序 service-edit/services 同步支持 护理/其他 两类)。
小程序:salary-month 发放按钮/已发放态、finance 待结工资卡、my-performance 发放状态、home 召回周报横条+服务小记回看7天、attendance 设WiFi大按钮、gallery/customers/finance/customer 四处 calc→百分比双列修复。

### 二、回归 + 部署 + 冒烟
1. `bash apps/api/run-all-tests.sh` 21 全绿(重点 finance-core:payout 用 insertFinanceTransaction 哈希链,若 ledger verify 有红立刻停)。跑完记得按 `启动服务器.command` 配置重启本机 4128。
2. commit(建议:`feat: 工资锁定→账本入账闭环 + 沉睡召回周报(按周AI话术) + 真机布局修复`)→ push → CI → Railway。
3. 生产只读冒烟:`/health` 200;`GET /admin/salary/pending-payout` 无 finance key → 403;`POST /admin/salary/payout` 无 key → 403(不写入);`GET /admin/recall-digest`(带 token)→ 200(生产无沉睡客则 count=0,验路由;**允许**其在生产 tenant_settings 写一条 digest 缓存,只读数据无害);`GET /admin/service-notes/pending` 响应含 `date` 字段。**不在生产锁定/发放任何工资。**
4. 结果写 `handoff/本机任务结果.md`,标 [已完成]。

---
> 上上批(安全修复+会员链路 de8b372)、上批(P0 多租户+入驻 725434b)均已上生产。以下为新批次。

## 【给 Code 的指令】提交 服务小记(表+AI结构化+端点)+ 直接排单全链路占位 + 作品管理技师隔离 + 演示顾客小记 + 回归 + 部署 [已完成 2026-07-31,commit 2570239,详见结果文件]

> 自上次部署 89c522b 以来的全部后端改动一次交清。前端小程序改动(orders/schedule/staff/customer/service-note/customer-profile 等)不走 Railway,由店主用微信开发者工具预览/上传,不在本任务范围;本任务只管**后端回归 + 部署**。

### 一、本批后端改动范围(git status 应看到 apps/api/local-server.mjs、apps/api/ai-utils.mjs)
`apps/api/ai-utils.mjs`:
1. 新增 `createServiceNoteInsights({rawText, serviceName, customerName})` —— 把技师口述服务小记结构化成 `{summary, styles, personality, preferences, companions, safetyFlags, other}`;中文系统提示,怕疼/过敏等归 `safetyFlags`;失败由调用方兜底 fallback。返回沿用 aiJson `{data}` 外壳(调用处已拆 `.data`)。

`apps/api/local-server.mjs`:
1. **服务小记(P0-②)**:新增 `service_notes` 表(迁移块,`id/tenant_id/user_id/booking_id/technician_id/technician_name/service_name/raw_text/structured_json/created_by/created_at`);`parseJson2()` 安全解析;`POST /admin/service-notes`(员工/老板均可写,原文→AI 结构化[失败 fallback 不阻塞]→存,支持 bookingId 或裸 userId+technicianId);`GET /admin/customers/:id/notes`(时间线 + 汇总画像:标签去重、安全项单列、到店统计)。员工写小记经 `assertStaffCanAccessBooking` 守卫。
2. **老板直接排单全链路占位(P0)**:bookings 加 `direct_deposit_unpaid` 列(迁移);`createBooking(body, opts)` 支持 `adminDirect`(强制 CONFIRMED、免在线定金门、放宽闭店/技师未排班/营业时段/技师-服务绑定,**仍占 booking_slots、仍防冲突**)+ `depositPaid`;`POST /admin/bookings/direct`(owner-only,已有 userId 或 newCustomerName 自动建档→复用 createBooking);`schedule-day` 端点补 `userId/group/arrivalState/isNewCustomer/isDesignated/ownerDirect/depositUnpaid` 与 `activeCount/pendingCount`。
3. **作品管理技师隔离**:`GET /admin/published-works` 对 `adminSession.role==='staff'` 追加 `AND b.technician_id=?`(本技师只看本人服务单作品);owner 不变。
4. **演示顾客小记(仅本地/演示)**:新增 `seedDemoServiceNotes(tenantId)`,启动时**仅当 `ALLOW_DEMO_ADMIN_LOGIN==='true'`** 才跑;**逐顾客判空**(`NOT EXISTS ... service_notes`)给最近完成、尚无小记的前 3 位老客铺内置结构化小记,其余保持无小记形成对照。**生产 `ALLOW_DEMO_ADMIN_LOGIN` 关 → 该函数不执行,生产零影响**。
5. **待写小记端点(主页用)**:`GET /admin/service-notes/pending`——按单判断(COMPLETED 且无对应 service_note),`?date=` 默认门店今天、`?days=`回看窗口(≤14);`adminSession.role==='staff'` 自动加 `AND b.technician_id=?` 只见本人。只读查询,无写入。本地真机冒烟 200 数据正确。
6. **站内提醒(老板→员工)**:新表 `staff_nudges`(CREATE IF NOT EXISTS 迁移);`POST /admin/staff-nudges`(owner-only,校验技师属本租户)、`GET /admin/staff-nudges/mine`(员工拉本人未读,无 technicianId 返回空)、`POST /admin/staff-nudges/:id/read`(标已读,按 tenant+technician 双重过滤)。本地真机 201/读取/库核对通过。
7. **直接排单时长可调**:`validateBookingInput` 收 `durationMin`(30–360,取整到 30 的倍数);`assertBookable` **仅 `opts.adminDirect` 时**用它覆盖服务标准时长(end/booking_slots/total_duration_min 同源);普通顾客预约忽略该字段,行为不变。
8. **报错中文化**:SLOT_UNAVAILABLE 及 booking 链路 5 处英文文案改中文(时间格式/服务下架/技师不做此服务/门店休息/技师休息);已 grep 确认无测试断言这些字符串。
9. **员工打卡考勤(P1-⑤)**:新表 `attendance_records`(UNIQUE tenant+tech+date)+ `store_wifi`;端点 `POST /admin/attendance/clock`(BSSID 白名单校验:已配置不匹配→403,未配置→放行标未验证;下班自动算加班)、`GET /admin/attendance/today`(员工/老板两视图)、`PATCH /admin/attendance/:id`(owner 修正/补卡)、`GET|POST|DELETE /admin/store-wifi`(owner)。均为新表新端点,不动既有链路。本地真机冒烟:看板数字与工时计算正确。
10. **薪资方案(P1-④)**:新表 `salary_plans`(UNIQUE tenant+technician_id,空串=全店默认;后追加 `recharge_pct` 列迁移——充值提成与耗卡提成分开配);端点 `GET /admin/salary-plans`、`GET /admin/salary-plans/effective`、`PUT /admin/salary-plans`(输入服务端夹紧)、`DELETE /admin/salary-plans/:techId`、`GET /admin/salary/estimate?month=`(**owner + requireFinanceKey 财务门禁**;已锁月份返回快照)、`GET /admin/salary/my-estimate`(员工本人)。**月度锁定**:新表 `salary_payrolls`(UNIQUE tenant+month+tech),`POST /admin/salary/lock`(owner+财务钥匙,快照存档,重复 409)、`POST /admin/salary/unlock`(删快照)。计算只读聚合(COMPLETED 单+考勤加班),不写财务账本(账本对接后续批次)。本地真机:PUT 200(rechargePct 落库)、estimate/lock 无钥匙 403、Coco 数字手工核对一致。
11. **客户分层 RFM(P1-③)**:`/admin/customers` 查询补三列聚合(completed_count/first_visit_at/last_completed_at,均 CASE WHEN COMPLETED,既有字段不变);新端点 `POST /admin/ai/recall-copy`(owner-only,≤6人:聚合 service_notes 画像 → Qwen 一人一条召回话术,AI 失败落模板;CI 无密钥走 fallback 不阻塞);ai-utils 新增 `createRecallMessages`。本地真机:customers 42 人新字段正确,AI 话术真实 Qwen 生成成功(自动引用画像安全项/常做项目)。
12. **分层规则可调 + 群发券**:新表 `tenant_settings`(通用 key-value);`GET|PUT /admin/segment-rules`(PUT owner-only,阈值服务端夹紧);`POST /admin/coupons/:id/grant-batch`(owner-only,≤200人,已持有 active 同券跳过、受 total_qty 限制,返回 granted/skipped)。本地真机:规则读写生效、grant-batch 校验路由通过。

### 二、回归
`bash apps/api/run-all-tests.sh` **21 套件须全绿**。说明:CI 无 AI 密钥→createServiceNoteInsights 走 mock/fallback,不阻塞;新增表/列均为迁移加法(`duplicate column` 容错),兼容存量;seedDemoServiceNotes 在 CI(无 ALLOW_DEMO_ADMIN_LOGIN)不触发。Cowork 本机已 `node --check` 全过、只读复查真库小记两态成立无重复。任何红:停下,失败输出原文写 `handoff/本机任务结果.md`,不改测试。

### 三、提交 + 部署
commit(建议信息:`feat: 服务小记(表+AI结构化+读写端点)+ 老板直接排单全链路占位 + 作品管理技师隔离 + 演示顾客小记(仅本地)`)→ push main → 等 CI 全绿 → Railway 自动部署。

### 四、生产只读冒烟(记入结果文件)
1. `/health` 200。
2. `GET /admin/customers/<任一真实userId>/notes`(带 OWNER_TOKEN)→ 200,返回 `notes` 数组 + 画像汇总字段(生产该顾客可能 0 条,只验路由通、结构对)。
3. `POST /admin/service-notes` body `{}`(带 token)→ 400 `BAD_REQUEST`(空内容拦截,不写库)。
4. `POST /admin/bookings/direct` body `{}`(带 token)→ 400/校验错(路由通,无脏写)。
5. `GET /admin/schedule-day?date=<今天>`(带 token)→ 含 `ownerDirect/depositUnpaid/userId` 等新字段。
6. `GET /admin/service-notes/pending`(带 token)→ 200,返回 `{count, items}`(生产可能 0 条,验路由通、结构对)。
7. `GET /admin/staff-nudges/mine`(带 owner token)→ 200 `{nudges:[]}`(owner 无 technicianId 返回空,验路由通);`POST /admin/staff-nudges` body `{}` → 400(不写入)。
7b. `GET /admin/attendance/today`(带 token)→ 200 含 `{date,working,done,overtime,rows}`(生产无记录=全 0/none,验路由);`POST /admin/attendance/clock` 带 owner token → 400「当前账号未绑定技师」(owner 无 technicianId,不写入)。
7c. `GET /admin/salary-plans`(带 token)→ 200 `{defaultPlan:null,plans:[]}`(生产未配置);`GET /admin/salary/estimate`(带 token、**不带** x-finance-key)→ 403 FINANCE_LOCKED(门禁在);**不在生产配置任何薪资方案**。
8. **确认生产未铺演示小记**:`ALLOW_DEMO_ADMIN_LOGIN` 生产为关 → seedDemoServiceNotes 不跑;如需可只读抽查 service_notes 是否仅有真实写入(勿在生产造数据)。

### 五、禁止项
不提交 `.env`、`local-data/`、`backups/`;不动 `USE_LOCAL_SANDBOX`(仍 true 供本地演示);不在生产造演示数据;微信支付/企微不替店主做。

完成后结果写 `handoff/本机任务结果.md`,本任务标 [已完成]。

---

## 【给 Code 的指令】提交 排班技师维度+到店状态+足部美甲 + 首页店卡 + 作品墙B + 有迹品牌/Logo + 演示登录旁路 + 回归 + 部署 [已完成 2026-07-26,commit 89c522b,详见结果文件]

### 一、本批改动范围(自 8a727e5 以来)
后端 `apps/api/local-server.mjs`:
1. **排班技师维度日视图**:新增 `GET /admin/schedule-day?date=&storeId=` —— 按天返回 openTime/closeTime/isClosed + 在岗(或当天有单)技师 + 每单明细(顾客名/项目/serviceType/**group[hand|foot|lash|care]**/**arrivalState[pending|active|done]**/起止/时长/**isNewCustomer**/**isDesignated**)+ activeCount/pendingCount。新客=该顾客本店无更早单;指定=备注含 指定/指名/点名;group 由服务名识别(足/护理),其余按类型。
2. **到店状态**:bookings 加 `arrived_at` 列(迁移块,`duplicate column` 容错);新增 `PATCH /admin/bookings/:id/arrival`(body.arrived true→记 arrived_at,false→清)。arrivalState=COMPLETED→done / arrived_at→active / 否则 pending。**不进主状态机、不碰财务**;「标记完成」仍走原 `/status` COMPLETED。
3. **首页店卡**:`GET /stores` 每店附 `hours`(business_hours 七行),供顾客端店卡"今日营业时间/营业中"。
4. **作品墙方案B**:`GET /portfolio` 在 portfolios(按技师,兼容)外新增 `works`(平铺,每图带 technician+serviceType/serviceName,LEFT JOIN services 用 name_zh)+ `categories`(该店实有作品品类去重)。
5. **演示登录旁路**(仅本地/演示):`signInWechatMiniUser` 顶部——`ALLOW_DEMO_ADMIN_LOGIN==='true' && body.demoLogin===true` 时以演示顾客 demo-cust-01 登录 body.tenantId(复用 seed+serialize+miniAuthFor,mode='demo-mini')。`customerFromMiniToken` 加回退——demo-openid 前缀且演示开关开时按用户 id 查。**生产 ALLOW_DEMO_ADMIN_LOGIN 关 + 真实 openid,两处均不触发**(已本地验证:无 demoLogin 标记回到 503/真实微信路径)。

网页 `apps/web/`:admin.html/admin.js(品牌→有迹,`ADMIN_BUILD='20260722-youji'` + 两处 `?v=` 同步)、platform.html(品牌→有迹 + logo)。

小程序 `miniprogram/`:
- `pages/merchant/schedule/*`:顶部"技师维度/月历"切换(默认技师维度,月历原功能保留);技师维度=横轴技师(横滑)、纵轴时间、色块(group 色相 × arrivalState 三态:未到淡色/进行中满色+绿框+左绿条+时间前●/完成灰+✓)、空档"+空·约"、顶部 今日单/在做/在岗/空档、分享长截图提示、图例下沉;点色块动作单(老板)标到店/完成/改回未到/看详情,员工只看详情。单位统一 rpx(PX_PER_HOUR=120)。
- `pages/home/*`:一体店卡(轮播封面+店名+营业状态[按门店时区]+地址导航/复制+今日营业时间+一键拨打),删底部门店展示,删美甲/美睫快捷按钮,店卡放大;品牌→有迹 + 临时 logo。
- `pages/portfolio/*`:整页重写为方案B(技师头像条+动态品类筛选[该店实有才显示]+双列瀑布流+每卡技师+「同款›」);`utils/api.js` 加 getPortfolioWall()。
- 品牌:app.json/entry.wxml(slogan「把经营交给 AI,把时间还给手艺」)/me.wxml/merchant manage+me.wxml/admin index.js+json/i18n.js → 有迹;`assets/images/youji-logo.png`(临时 logo)。
- `utils/api.js`:mini-login 带 `demoLogin: USE_LOCAL_SANDBOX`;`LOCAL_API='http://127.0.0.1:4128'`。
- `assets/images/entry-bg.jpg` 压缩(减包体);`project.config.json`(本地开发者工具编译设置,无害)。

### 二、回归
`bash apps/api/run-all-tests.sh` **21 套件须全绿**。注意:演示登录旁路仅 `body.demoLogin===true` 触发,CI 无此标记 → mini-login 走原路径,不受影响(已本地验证)。arrived_at 迁移为加列,兼容存量。若有红,停下记结果文件。

### 三、提交+部署
commit(建议信息:`feat: 排班技师维度+到店状态+足部色 + 首页店卡 + 作品墙B + 有迹品牌`)→ push main → 等 CI 全绿 → Railway 自动部署。

### 四、生产只读冒烟
- `/health` 200。
- `GET /stores` 每店含 `hours` 数组。
- `GET /portfolio`(默认租户)返回含 `works` 与 `categories`。
- `GET /admin/schedule-day?date=<今天>`(带 OWNER_TOKEN)返回 technicians/bookings/activeCount 字段。
- `PATCH /admin/bookings/<不存在id>/arrival` → 404(不写入)。
- **不在生产造演示数据**;演示数据仅本地库(排班 6 技师、7/23 排满、林小雅丰满资产等)不随部署。

### 五、禁止项
不提交 `.env`、`local-data/`、`backups/`;不动 USE_LOCAL_SANDBOX(仍 true,供本地演示;小程序正式发布前另行置 false);不动微信支付/企微。



## 【给 Code 的指令】提交 AI客服页+预约同款+券积分储值P1+AI链路修复 + 回归 + 部署 [已完成 2026-07-20,commit 8a727e5,详见结果文件]

### 一、本批改动范围
- `apps/api/local-server.mjs`:
  1. **券 P1**:`coupon_grants` 表(user×券×店,一次性核销码);`POST /admin/coupons/:id/grant`(发券,qty 控制+有效期+生成码);`POST /admin/coupons/redeem`(核销,本店校验/过期自动标/重复 409)。
  2. **顾客资产读取**:`GET /my/coupons | /my/stored-value | /my/points-history`(requireCustomer × resolveTenant)。
  3. **AI 客服按店**:`/ai/customer-service` 入口 `tenantContext.enterWith(resolveTenant)`;`buildCustomerServiceContext` 的 services/stores 查询加 tenant 过滤。
  4. 平台:`/platform/tenants` 加 `monthBookingCount`(本月/累计口径分列)。
- `apps/api/ai-utils.mjs`(**AI 链路修复,重要**):
  1. `callOpenAICompatible` 加 AbortController 超时(默认 45s,env `AI_TIMEOUT_MS`)——原来无超时,大请求无限挂起(店主遇到"生成中"卡死)。
  2. 附图改为仅 `AI_VISION_MODEL=true` 时启用(且≤300KB)——qwen3.6-plus 纯文本模型,image_url 会被拒/大 base64 拖死。
  3. 请求体加 `enable_thinking: process.env.AI_ENABLE_THINKING==='true'`(默认关思考)——qwen3 思考链导致 30s+,关闭后实测 社媒文案 11.8s / 客服 9.6s。
- `apps/web/platform.html`:概览+商家表显示「本月/累计订单」两列。
- `miniprogram/**`:AI 客服聊天页 `pages/ai-chat`(+首页入口);预约同款(portfolio 动作单→booking 预设横幅/技师/参考图,含 onLoad/onShow 双刷新丢预设的修复);券包/储值/积分三页接真(`/my/*`);商家端 客户档案「发券」、member 券tab「扫码/输码核销」;app.json。
- **不要提交**:`.env`、`local-data/`、`backups/`。

### 二、回归
```
bash apps/api/run-all-tests.sh
```
要求 21 全绿。说明:CI 无 AI 密钥→mock 路径,ai-utils 的超时/思考开关不影响 mock;Cowork 本机在券/资产端点加入后已跑过全绿,ai-utils 最后两处小改(视觉开关/thinking)后未重跑,**以你这次为准**。任何红:停下写结果文件。

### 三、commit + push
- message 建议:`AI客服聊天页(按店scope)+ 预约同款 + 券发放/核销/顾客资产P1 + AI链路修复(超时/附图开关/关thinking)+ 平台本月累计口径`
- push → CI → Railway。

### 四、生产冒烟
1. `/health` 200;`GET /my/coupons` 无 token 401。
2. `POST /admin/coupons/redeem`(railway run 注入 token)body `{"code":"LL-FAKE-0000"}` → 404(路由通,无写入)。
3. `GET /platform/tenants`(token)→ 含 `monthBookingCount` 字段。
4. **AI 提速验证**(会消耗少量 token,可接受):railway run token 调 `POST /admin/ai/social-copy`(任选一个已完成 bookingId,image 传空)→ 200 且 **耗时 < 25s**;`POST /ai/customer-service`(公开)问"美甲多少钱"→ 200 < 20s。
5. 不在生产发真券/不动租户状态。

### 五、禁止项
- 不动 `.env` / `USE_LOCAL_SANDBOX` / 本地 demo-shop-b;微信支付/企微不替店主做。

完成后结果写 `handoff/本机任务结果.md`。

---

## 【给 Code 的指令】提交 /admin 多租户贯通(解闸)+ 回归 + 部署 [已完成 2026-07-18,commit ffd6be1,详见结果文件]

### 一、本批改动(全部在 apps/api/local-server.mjs)
1. **AsyncLocalStorage 租户上下文**:`tenantContext` + `currentTenantId()` 改读上下文;/admin 入口按登录账号租户 `enterWith`。顾客/公开路径无上下文=默认店,行为不变。
2. requireAdmin/adminFromSessionToken/adminForEmail 带 `tenantId`;`defaultStoreId()` 租户感知。
3. 中央守卫:assertStaffCanAccessBooking/Quote 加租户归属(跨店 404)。
4. 列表/写入补租户过滤:bookings、technicians(含 POST/PATCH/schedule)、services(GET/POST/PATCH)、customers(非默认店只列本店活跃顾客,默认店全量不变)、conversations、quote-requests、reminder-tasks、schedule-week、schedule-batch、schedule-requests、staff-accounts(新账号写 tenant_id)、published-works、daily-brief、business-hours、store-info、special-dates、demo seeds(仅默认店)、merchant-leads(仅默认店 owner=平台)。
5. 财务钥匙绑定发放租户(跨店用 FINANCE_LOCKED)。
6. **解闸**:移除 /admin/auth/login 的 TENANT_PORTAL_NOT_READY(新商家老板可直接登录,只见己店)。

### 二、Cowork 已本机验证(供你复核基线)
- 全量回归 **21 套件全绿**(matrix 66/finance-core/staff-portal/admin-accounts/tenant-isolation 全过)。
- 多商户 E2E:平台建店→新老板登录→services/technicians/bookings/customers 全 0、leads 403、business-hours 只见本店、财务门禁独立→加服务只进己店、默认店 6 个不变。

### 三、你要做的
1. `git status` 核对(只应有 local-server.mjs + handoff 文档;敏感文件零暂存)。
2. 重跑 `bash apps/api/run-all-tests.sh` 确认 21 全绿(全新临时库)。
3. commit(message:`商家端 /admin 按账号租户贯通(ALS 上下文+全量过滤+财务钥匙绑店)+ 解除多租户登录闸`)→ push → CI → Railway。
4. 生产冒烟(只读):/health 200;`GET /bookings` 无 token 401;主钥匙 `GET /admin/services` 数量与改前一致(默认店行为不变);`GET /admin/merchant-leads` 主钥匙 200(主钥匙=默认店=平台)。
5. 结果写 handoff/本机任务结果.md。

---

## 【给 Code 的指令】提交 平台后台全套(platform)+ 登录闸 + 回归 + 部署 [已完成 2026-07-17,commit 7847a08,详见结果文件]

### 一、本批改动范围(git status 应看到)
- `apps/web/platform.html`(**新文件**):平台运营控制台——左侧菜单(概览/商家管理/商家配置/入驻线索+3个规划置灰);概览四指标卡+待办+速览;新建商家(凭据一次性+店码scene);商家配置五标签(门店/营业时间/服务价目/技师/AI知识库);线索管理+「转入驻→建店」预填联动。店主已按 UI 原型确认并本地走查通过。
- `apps/api/local-server.mjs`:
  1. `GET /platform` serve platform.html。
  2. `/platform/*` 端点(全部仅 `Bearer OWNER_TOKEN`):`GET /platform/overview`(月订单+待配置商家)、`GET/POST /platform/tenants`(列表/一键建商家=tenant+门店+owner账号)、`POST /platform/tenants/:id/toggle`(默认店禁停)、`GET/PATCH /platform/leads`、五段配置 `GET|PUT|POST|PATCH|DELETE /platform/tenants/:id/{store|business-hours|services|technicians|kb}`(store 保存同步该租户 AI 事实;新服务自动派给在职技师;新技师自动可做在售服务)。
  3. **多租户登录安全闸**:`/admin/auth/login` 对 `tenant_id !== DEFAULT_TENANT_ID` 的账号返回 403 `TENANT_PORTAL_NOT_READY`(商家端 /admin 尚未按租户贯通,防新商家看到默认店数据;贯通后移除)。默认店账号不受影响。
  4. 演示铺单进一步收敛:仅 `loginTenant === DEFAULT_TENANT_ID` 时触发(上批是按店,本批只默认店)。
- **不要提交**:`.env`、`local-data/`、`backups/`。

### 二、全量回归(全新临时库)
```
bash apps/api/run-all-tests.sh
```
要求 **21 套件全绿**。重点:`admin-accounts`(登录闸只影响非默认租户账号,测试账号都在默认租户,理论不受影响)、`tenant-isolation`、`customer-service-matrix`。任何红:停下,原文写结果文件,不改测试。

### 三、绿了 commit + push
- message 建议:`平台运营控制台(platform.html + /platform/* 租户/线索/五段配置)+ 多租户登录安全闸 + 演示铺单仅默认店`
- push → CI 全绿 → Railway 自动部署。

### 四、生产冒烟(记录到结果文件)
1. `/health` 200;`GET /platform` 200(页面本身公开,数据接口有门禁)。
2. `GET /platform/tenants` 无授权 → 401。
3. 用 `railway run` 注入真实 OWNER_TOKEN(不打印)调 `GET /platform/tenants` → 200 且列表含 lucky-luxe;`GET /platform/overview` → 200。
4. **不要在生产创建测试商家/不要动生产租户状态**(只读冒烟)。
5. `GET /bookings` 无 token 仍 401(此前修复不回退)。

### 五、禁止项
- 不动 `USE_LOCAL_SANDBOX`;不动本地 demo-shop-b;微信支付/企微/类目不替店主做。

完成后:回归汇总 + push/CI/Railway + 冒烟结果写 `handoff/本机任务结果.md`,任务标 [已完成]。

---

## 【给 Code 的指令】提交 P0 多租户底座 + 商家入驻 + 全量回归 + 部署 [已完成 2026-07-17,commit 725434b,详见结果文件]

### 一、本批改动范围(git status 应看到)
- `apps/api/local-server.mjs`:
  1. **商家入驻**:`merchant_leads` 表 + `serializeMerchantLead`;公开 `POST /merchant-leads`(留资,无需登录);owner `GET/PATCH /admin/merchant-leads`(状态 new→contacted→onboarded/rejected)。
  2. **P0 多租户(默认安全)**:`validTenantId()`/`resolveTenant()`(读 `x-tenant-id` 头或 `?tenantId=`,校验 tenants 表,无效回退 DEFAULT);顾客侧 `GET /stores /services /technicians /portfolio` 加 tenant 过滤;`GET /bookings` 按 (user, tenant);`POST /bookings` 写入 tenant(validateBookingInput+INSERT 加 tenant_id);`GET /availability` 校验 storeId 属于当前租户;新增公开 `GET /shops`(active 租户×有效门店)。
  3. **每店独立会员**:`serializeUser(user, tenantId)`、`userBookingStats(userId, tenantId)`、`storedValueBalanceCents(userId, tenantId)`、`insertStoredValueTransaction({...tenantId})`;mini-login 按 body.tenantId 结算会员并返回。
  4. **演示铺单收敛**:`seedDemoBookingsForUser(userId, tenantId)`(服务查询 UPPER(type));仅 `ALLOW_DEMO_ADMIN_LOGIN=true` **且 loginTenant===DEFAULT_TENANT_ID** 且该店无单时触发。
- `miniprogram/**`:入驻申请页 merchant-apply + 登录页入口;多租户(app.js 解析 scene/query 存 lucky_tenant;request 带 x-tenant-id;mini-login 带 tenantId;refreshMember 按店刷新会员缓存;me/points/stored-value onShow 刷新);shop-select 选店页(+扫码进店);home 门店条+无租户兜底;及本批顾客端页面改动。
- 文档:handoff 下 多商户架构方案/平台后台与AI客服_设计/升级与待办清单/开发日志。
- **不要提交**:`apps/api/.env`、`apps/api/local-data/`、`backups/`;`.env.example` 若有改动必须只含占位符。

### 二、全量回归(全新临时库)
```
bash apps/api/run-all-tests.sh
```
要求 **21 套件全绿**。注意:
- 多租户改动是**默认安全**设计(不带租户头=DEFAULT_TENANT_ID,行为与改前一致),测试主要走 /admin 与不带头的顾客端,理论全绿。
- 重点看 `tenant-isolation`、`entitlements`、`customer-service-matrix`(66)、`stored-value`、`finance-core`。
- **任何套件变红:停下,把失败输出原文写入结果文件,不要改测试、不要硬修。**

### 三、绿了再 commit + push
- commit message 建议:`P0 多租户底座(顾客侧租户scope+每店独立会员+选店/扫码)+ 商家入驻(merchant-leads)+ 演示铺单收敛`
- `git push origin main` → CI 全绿 → Railway 自动部署。

### 四、部署后生产冒烟(全部记录到结果文件)
1. `/health` → 200
2. `GET /shops` → 200,列表应只有 Lucky Luxe(生产库没有测试店)
3. `GET /services` 不带头 → 与改前相同的服务数;带 `x-tenant-id: nonexist` → 同样结果(回退默认,不报错)
4. `GET /bookings` 无 token → 401(上一批修复仍在)
5. `POST /merchant-leads` 带 `{"shopName":"smoke测试店","phone":"000"}` → 201;`GET /admin/merchant-leads`(带 owner 凭证)能看到 → 然后 PATCH 状态为 rejected 作标记(生产别删,留作首条冒烟记录亦可,备注 smoke)
6. `GET /admin/packages` 无授权 → 401

### 五、需要店主本人 / 禁止项
- 微信支付商户平台、企业微信、类目/主体决策:不替做。
- 不动 `miniprogram/utils/api.js` 的 `USE_LOCAL_SANDBOX`(本地联调中)。
- 本地 sqlite 里的 `demo-shop-b`(B 测试店)是**本地演示数据,不进 git、与生产无关**,不要清。

完成后:回归汇总 + commit/push + CI + Railway + 冒烟结果 写入 `handoff/本机任务结果.md`,任务标 [已完成]。
