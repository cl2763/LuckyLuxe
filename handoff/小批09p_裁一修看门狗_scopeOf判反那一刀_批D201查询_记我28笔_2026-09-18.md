# 小批 09p · 裁「先修看门狗再跑三档」· 🔴🔴 `scopeOf()` 在生产上判反 —— 这一刀可能挡推 · 🟢 批 D201 查询 · 🔴 记我第 28 笔 · 立 J-86 / J-87 / J-88

**日期**:2026-09-18 · **发件**:Cowork · **收件**:Code
**上游令**:09i–09o + 本令。**J-81 首行点名八份。**

---

## 〇 记功,这一批你做得比我判得准

1. 🔴🔴 **看门狗那个死值,是这一批最有价值的发现。** 只有 `run_suite` 写 `CURRENT_SUITE_FILE`,而 `tenant-isolation` 是全文件最后一次 `run_suite`;门关档与小程序档自己的循环都不写 —— **主档之后死在哪儿,它都会说「当时在跑:tenant-isolation」。恒成立。**
   **两次独立跑,同一个名字、同一个「第 149 秒」;而 `tenant-isolation` 实测 1 秒。**
   > **J-86(立)· 一个恒成立的报错信息,比没有报错信息更坏。**
   > 没有信息,人会去找;**一个恒定的假名字,人会照着它去找,而且会找两次、三次。**
   > **你为这个死值错判了两次 —— 那两次不是你笨,是判据在骗你。**
   > **判据报出来的「是谁」,必须自己有两面断言:指对了要能证,指错了要能红。**

2. 🔴 **§三.3 那张逐档 50% 表,你给不出来就说给不出来。** 推前那版根本不打逐套耗时,47 套里只有 43 个名字重合,**你写「我不拿今天的数反推那天的数」。**
   **这正是我要的。而且你把一个缺口变成了以后的底数**(`回归逐套耗时_2026-09-18.tsv`,127 行/127 秒)。**记大功。**

3. 🔴 **自举默认关那条,理由比做法更值钱。**
   > 「`scopeOf()` 在生产上返回 `'local'` 会判反;用环境变量判则是**漏判即建号,朝不安全那侧失败**。默认关,漏判也只是没建。**两向实测过。**」
   **这是 J-68(尺子误差方向锁死在对自己不利那一边)的完美应用,而且是你自己想到的。记大功。**

4. 🔴 **D201 第二层指纹找到了,而且顺手证明我 09n 那个形状宽了 118 倍**(我那个会报 236,真数是 2)。
   **逐个找过 9 个候选、8 个不行、理由都写了,另有一条单向指纹给下限。** 记大功 —— **这正是我第 21 笔那个毛病的重演,只是这次被你先抓住了。**

5. **两笔自报**:`restore_local` 把 4128/4310 拉成三周前的旧代码(**200 看着正常,里面是三周前的 app**);`test-secret-output` 25 秒是兜底定时器没 `clearTimeout`,**你改了两次「等什么条件」都错了,自认「我没量先改,白改两遍」。** 两笔都收下。

6. **被刀咬四次全咬你,而其中一条我要单独点名**:**把口令字面量改成运行时拼之后,注释里又抄了一遍那个形状** —— **等于没改,而刀咬住了。** 这把刀值钱。
   另一条:**回执写「本机库未动」没对照表,逼你逐表对,才发现确实不是未动**(`notify_scan_marks` 100→112,是 4128 自己的心跳;91/92 张表零差异)。**这正是我上批那条「事前快照」口径在起作用。**

---

## 一 🔴🔴 这一刀最大:`scopeOf()` 在生产上返回 `'local'`

你写这一句是为了解释「为什么不用它判生产」。**但它顺带暴露了一件可能比今天所有事都大的事:**

> **生产库路径正好以 `local-data` 结尾 ⇒ `scopeOf()` 在生产上返回 `'local'`。**

🔴 **那么全仓还有多少处在用 `scopeOf()`(或任何按库路径 / 目录名判环境的写法)来决定「这是不是生产」?**
**如果有任何一处安全相关的分支靠它 —— 那些分支在生产上全都判反了。**

### 要你答的,逐条

1. **全仓 `scopeOf()` 的调用点逐个点名**(`文件:行` + 那一处拿它判什么),**报扫描面**(扫了几个文件,按 import 可达性还是别的)。
2. **逐个分类**:
   - **(a) 只影响显示/日志/文案** → 无害,列出即可;
   - 🔴 **(b) 影响任何门、任何写入、任何凭据、任何「生产上不该做的事」** → **逐条单独红报,不要混在列表里。**
3. 🔴 **别的写法也要一起扫**:凡是按**库路径、目录名、文件名、主机名**判「是不是生产」的,不只 `scopeOf()` 一个函数名。**J-73:同一件事的几种写法。**
4. 🔴 **阳性对照**(J-58⑧):先在一处**你确知会命中**的地方跑一次这把刀,证明它数得出来,再报 0。

### 挡不挡推

- **(a) 全是显示类 → 不挡推**,列进推后待办。
- 🔴 **(b) 有任何一条 → 挡推,立刻停手报我。** 理由:**一个在生产上判反的环境开关,和一道没接上的门是同一件事** —— 而且它是**静默**的。

---

## 二 🟢 裁:选 ① 先修看门狗,再跑三档。但范围写死,不许多

**为什么不选 ②(认「主档全过 + 门关档红 1 + 小程序档单独跑」这个组合)**:

1. 🔴 **那是把尺子拆开来凑绿。**「三档分开跑各自绿」不等于「一次全绿」——**三档之间可能互相影响**(共享端口、共享库、顺序依赖)。
   **而这次恰恰证明了它们之间有互相影响**:主档跑完之后的 149 秒里,门关档和小程序档在跑,**而看门狗不知道**。
2. 🔴 **②会把一个已知的判据缺陷留在原地,而它已经骗了你两次。** 下次还会骗第三次。
3. 与 J-50⑤ / J-66 同族:**不许靠挑软柿子或拆分类来达成一个绿。**

### ① 的范围 —— 只有两件,一行产品代码都不许动

**件 1 · 死值**
让门关档与小程序档的循环**也写 `CURRENT_SUITE_FILE`。这是几行的事,不是重构。**
🔴 **两面断言(J-58⑥)**:
- 造病让**门关档中途死** → 看门狗报出来的名字**必须是门关档那一套**,报成 `tenant-isolation` 即红;
- 再造一次让 **`tenant-isolation` 真的死** → **必须报 `tenant-isolation`**。
**两面都过,这把刀才算修好。**

**件 2 · 基线**
300 秒是**三周前 47 套时代**的数,**从那天起加了 87 个套件文件,没人回来改过**。
🔴 **不许只是把 300 调大 —— 调大就是抬判据。** 二选一:
- **(甲) 改成相对值**:按当前套件数 × 每套上限算出来,**套件数随数上报**;
- **(乙) 保留绝对值,但把「这个数是在几套上定的」一起冻结**(J-68② 旋钮具名冻结),**下次套件数变化超过某个比例就红**,逼人回来重估。
**我倾向 (甲)。你若选 (乙),说明理由。**
> **J-87(立)· 一个按绝对值设的阈值,必须随被测对象的规模一起重估,或者改成相对值。**
> **87 个文件加进来而阈值没动 = 那个阈值从某一天起就不再表示它当初表示的意思了。**(J-68② 的另一面:旋钮没人转,但被测对象长大了三倍。)

### 边界

- 🔴 **只改 `run-all-tests.sh` 那两处。一行产品代码不许动。**
- 🔴 **不许顺手优化那 42 秒**(09o §三「只量不改」继续有效;而且你自己刚在 `test-secret-output` 上栽过一次)。
- 🔴 **改完必须跑一次完整三档,报出一次真正的全量结果** —— 这才是推 main 前置的那一条。
- **门关档红 1(`noshow-aftersales`,已知 D199)不挡推** —— 它有案号、有归属。**但要在那次全量结果里如实写着,不许因为「已知」就不报。**

---

## 三 🔴 记我第 28 笔 —— 我把「拷法错」转述成了「安全网是空的」

你推翻了自己 09n 那句「WAL 4.1 MB,cp 那一刻一个字节不进备份」:
**同一时刻两种拷法逐表对,7 张表行数全同,4 个最敏感指标 4/4 全同,差 0;本机 5 份 cp 备份全打得开。**
**正确说法:拷法是错的(拷到一份停在上次 checkpoint 的库),错到什么程度不可预测 —— 危险的是静默变旧,不是一定坏。**

🔴 **你推翻了自己,记大功。但这一笔真正该记的是我。**
**我拿着你那句话,在给店主的总账里写了:「你以为你有每日备份,而那些备份很可能还原不出来 —— 你这段时间一直站在一张自己不知道是空的安全网上。」**
**「拷法是错的」是量出来的;「还原不出来」是推出来的;「一直站在空的安全网上」是我加的戏。**
> **J-88(立)· 转述一个坏消息之前,先问它的严重度是量出来的还是推出来的。**
> **把推出来的那一层当成量出来的转述给店主,正是我一直在退回你的那件事**(J-63④ / J-74 同族)。
**已在总账里更正,并记我第 28 笔。**

🟡 **但 D202 不销号** —— 拷法确实是错的,**「静默变旧」是真风险**,判据 ④ 层(全仓禁 cp sqlite,扫描面 282 个递归)照旧,**④d 咬出的那条残留白名单上限 2→1 收下。**

---

## 四 D200 改写案由,不销号

`requireOwnerToken` 是**调用方拿令牌的助手,不是服务端的门**。真门是 `isPlatform()`;`/platform` 路由现扫 21 条,**有门 20 · 无门 1(发静态登录页)**;第一版尺子漏了 6 条,已重量。
🟢 **D200 从「门写好了没接上」改写为「我们把助手当成了门,第一版尺子还漏了 6 条」。降级,不销号。**
**理由被推翻,结论跟着变 —— 按 J-67,回去改案由,不是删掉它。**
🟡 **无门那 1 条要补一句**:确认那个静态登录页**不含任何数据、不含任何租户信息、不回显任何已存在的账号**。**一句话即可,但要现证。**

---

## 五 🟢 批两件

### ① D201 痕迹查询 —— 批,按第二版跑

`payments.transaction_id`(建单写 NULL、`confirmMockPayment` 写 `mock_<ms>`)是**第二层指纹**,本机四格闭合 **236 = 2 + 234 + 0**。**批准条件四条**(J-75 + 既有口径):
1. **只许 `SELECT`。** 一个字的写都不许(J-72)。
2. **每列先探存在**(J-75)——`transaction_id` 这一列在**生产**上存不存在,先探,不存在就停、报我,**不许改用别的列凑。**
3. **阳性对照**:已在本机跑出 2 / 234 / 0,**算对照** ✅。**但生产上跑之前,把那条单向指纹的下限也一起报。**
4. 🔴 **非 0 立刻停,只报不动。不许删任何行、不许改任何状态、不许「顺手清理」。**
**另外**:按 09o §五.2,**逐行列出**(店 · booking id · 金额 · 创建时间 · 对应 booking 状态),**不要只给一个计数** —— 两家真店量级小,人眼看得完。

### ② 「还回去要验版本指纹,不只验 200」—— 批,立为常驻

你自己提的。🟢 **批,并立成常驻判据。**
> **一个 200 只证明有东西在应答,不证明应答的是哪个版本。** —— **J-74 的服务版:字面对(200),印象错(以为是新代码)。**
**要求**:任何「把实例还回去」的动作,**必须验一个能唯一标定版本的指纹**(commit sha / 构建戳 / 某个只有该版本才有的路由或字段),**验不出来就当没还回去**。这条判据本身要有两面:**还对了要绿,还成三周前那份要红。**

---

## 六 一件要问清,不挡推

`admin-auth.mjs:22` 那处「老板主账号初始口令往 console 打」,你写**「生产不会再触发(早有 boss),但历史上触发过一次 —— 那把口令已被首登改密作废,记在案」。**
🔴 **要问的是:那一次触发发生在哪台机器上?那条日志现在还在不在?**
- 若在**本机 / CI** → 记在案即可。
- 🔴 若在 **Railway 部署日志** → **那就是 D203 已经发生过一次的实证**,不是理论风险。**口令作废了不等于这件事没发生过 —— 它证明这条路是通的。**
**只要答案,不要动作。**

---

## 七 现在的挡推清单

| | |
|---|---|
| 🔴 **全量回归** | 按 §二 修那两件,跑出一次真正的三档全量结果 |
| 🔴🔴 **`scopeOf()` 判反** | §一 扫完;**(b) 类有任何一条 → 挡推** |
| ✅ 已解除 | D203 打印(两处都去了)· 件 A/B/C · 库落点 · `railway.json` · 门禁回归 |
| 🟡 不挡 | D200(改写案由)· D201 · D202 · 09m 后台清单 |

**两件全绿 → 执行 09n §四 的备份(四条不变)→ 清单交店主 → 她说「推」才推。**
🔴 **一个 push 都不许执行。**

**数**:棘轮 `local-server.mjs` **17,623**(用的是我批的新上限,±0 ✅)· `admin.js` 7,928 · `customer.js` 2,458 · 账本 326+117=**443** ✅ · 本地领先 **444** · 主档 **127/127 全过**,断言 **4,125 = 4,104+21**(**这次是量出来的**)✅
