# 02c/02d 交付一段:**小程序自动化打通** + 六条归因全部定案 + 网页侧先行三件

**安全保证:生产库未动 · 本机库未动。** 未推 main。

---

## 〇 · 最要紧的:小程序能被我真验了

**卡点在我这边,不在你的环境** —— 你的第二次推翻是对的,我又记一笔。

| 我之前的做法 | 真相 |
|---|---|
| 把**服务端口 31176** 当 ws 端点去 connect | 服务端口 ≠ 自动化端口,连错了口 |
| `cli auto` 报「无输出」 | **我没抓 stderr**;它其实一直在说话:`✔ IDE server has started, listening on 31176` → `preparing` |
| 卡在 `preparing` 三次 | **旧 IDE 实例没清干净**(我先前 `automator.launch` 留下的半开状态,`cli quit` 没真退) |

**打通的动作**:`pkill` 掉全部 devtools 进程 → 全新 `cli auto --project …/miniprogram --auto-port 9420`
→ 输出 `✔ Using AppID: wx247cd8ad9907430d` / `✔ auto` → 9420 监听 → `automator.connect(ws://127.0.0.1:9420)` **连上**。

**现在能做**:读当前页、`evaluate` 执行页内代码、`reLaunch/navigateTo` 跳页、读页面 `data()`、**真截图**。
→ 小程序侧从此与网页端同等待遇:真启动、真点、真截图。

---

## 一 · 六条归因:三条挂起的全部定案(**用真跑的证据,不是读码猜**)

### 归因1③ 首页文字重叠 —— **真截图现场确认**(证据:`mp-home.png`)

第一次真正看见小程序首页。**你报的三症全部复现**:
1. **轮播位置整块空缺** —— `heroSlides 条数: 0`,店卡下面直接跳到「技师作品」,没有任何占位
2. **服务卡假图** —— 「演示·经典单色」与「演示·美睫自然款」**两张卡是同一张店铺环境照**
3. **文字重叠** —— 「CAD $198」被下一节标题「人气美睫」**压住**,清晰可见

→ 三症与归因1①②的代码定位**完全对上**;第③症是卡片区高度不足导致下一节压上来。

### 归因3 值日点不动 —— **定案:`canEdit=false` 时静默 return**

真跑两次对照:
| 场景 | `dv.duty` |
|---|---|
| 无登录态 | `{enabled:true, techIds:[], canEdit:**false**, note:"今天还没安排值日"}` |
| 老板登录态 + 今天 | `{enabled:true, techIds:["tech-ava","tech-lina","tech-mia"], canEdit:**true**, note:""}` |

**数据链是好的**。真因在前端:`tapDuty` 第一行 `if (!d || !d.canEdit) return` ——
**canEdit 为 false 时直接返回,不给任何反馈**。而 canEdit=false 会在两种真实情况下发生:
① 台面翻到了**非今天**的日期 ② 登录态/角色不对。
你点了没反应、系统一句话不说 —— **静默失败器族 + 空态说真话**的又一案。
(01t 我改的 `loadDayView` 那处是对的,但那只是第二个 bug;第一个 bug 一直在,我从没在小程序里点过所以没发现。)

### 归因4 报价横幅缺席 —— **定案:数据是好的,是"找不到有报价的会话"**

真开 `bigdemo:lucky-luxe:1` 会话页,读到:
```
quoteBanner: "本次会话已报价(已过期)CAD $688 · owner · 25 天前 — 需重新确认"
quoteBannerCls: "expired"
```
**横幅数据完全正常。** 真因是可用性:**会话列表上没有任何标识告诉你哪个会话有报价**,
你随机点开的都是 none 态(横幅按三态正确地不显示),看起来就像"功能没做"。
→ 正是你 02a 第五条要的**夹具矩阵铺满** + 列表加报价标识。
附带:`profile: null`(该会话未绑档案)—— 顾客卡此时应显示「还没绑定档案」而不是空白,与你第五条⑤对上。

---

## 二 · 网页侧先行(不受自动化阻塞的部分,已动刀)

### D107 老板端签署块 ✅(裁③,你说最重的那条)
`renderBookingDetail` 补「服务签署单」区:签署时刻 / 五步账逐行 / 多份单逐份 / **「查看签署原件 ›」**。
**同一份 `booking.payment` + 同一个 `data-dc-snapshot` 浮层**(日结那边在用的),两端零第二份实现。

### 裁② 假图连根 —— 同族扫尽出 **7 处**(不止你点的那一行)
| # | 位置 | 回落成 |
|---|---|---|
| 1 | `miniprogram/utils/api.js:71` | `store-cover.jpg`(**归因2 的根**) |
| 2 | `miniprogram/utils/api.js:518` | `member-profile.jpg`(头像) |
| 3 | `miniprogram/pages/me/index.js:127` | `store-cover.jpg` |
| 4 | `miniprogram/pages/orders/index.js:86` | `store-cover.jpg` |
| 5 | `miniprogram/pages/order-detail/index.js:48` | `store-cover.jpg` |
| 6 | **`apps/web/admin.js:4468`** | `nail-addon.jpg`(**老板端也在编**) | ✅ 已拔 |
| 7 | **`apps/web/share.js:109`** | `nail-french.jpg`(**分享页拿示例图当这单作品**) | ✅ 已拔 |

**网页 2 处已拔**(改为返回空,交渲染层占位出口)。
**小程序 5 处待落**:自动化刚打通,按 02d「小程序侧一刀不动」的谨慎口径,这 5 处与 D103 一起在下一段同批落 + 真截图验。

### D104 裸控件:先修点名处 ✅
「线上预约收定金」(`booking-rules.js:18`)裸 `<input type=checkbox>` → 改 **`.ui-sw` 行右开关**,与 D98 值日行同一颗。
(这正是 01t 我该做而没做的同类扫尽 —— L2 漏。)

---

## 三 · 下一段(自动化已通,可以做了)

1. **D103 小程序首页三症**:轮播改三态(有图/占位/不该有)、假图 5 处连根、卡片排版修 —— **逐项真截图对照**
2. **归因3 修**:`canEdit=false` 给人话(后端 note 已有字段位),不再静默 return
3. **归因4 修**:会话列表加报价标识 + 夹具矩阵七态铺满 + 顾客卡「还没绑定档案」
4. D104 其余裸控件(网页 checkbox 20 + select 30、小程序 checkbox/radio 13)
5. 版本指纹上小程序屏(环境自证律)
6. 三端逐屏基线对照出表

**做完了这一段,等你验;下一段我接着做。**
