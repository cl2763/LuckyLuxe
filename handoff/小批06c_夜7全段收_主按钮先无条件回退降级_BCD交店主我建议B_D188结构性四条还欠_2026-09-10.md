# 小批 06c — **夜班令 7 全段收**(段 0–段 6,两个半小时跑完八段,我逐条查过)· **裁:主按钮先无条件回退那次降级**(2.02 → 2.32,回图原样,这条不用等店主)· **B/C/D 交店主,我建议 B,并要一张 B 的效果图** · 🔴 **D188 的结构性四条还欠**(只补了三页)

> 2026-09-10 05:55 CST · Cowork 复核(只读):`git log` 到 `3c35ab7`(段 0–6 全做完);段3 定位自证**宽高比 2.17 vs 真值 2.167,过**;顾客端三页现读都引了 `design-tokens.css`;早报已落 · 两处各一份

## 一、收

| 段 | 我怎么查的 | 结论 |
|---|---|---|
| **段3 渐变面/取像素** | 读提交原文:定位**不靠猜边界**,靠**浅深两张做差**(开发者工具界面两张一样,只有手机屏内容会变);头一版取「所有变化点包围盒」被编辑器日志污染(x 从 720 拉到 2649),改成**按列/行的变化密度取最长连续段**;自证 `wx.getWindowInfo()` 430×932 → 真值 2.167,实测 **2.17**,±5% 内 | **收,关帐**。上一轮「拒绝取样」是对的,这一轮把它做成了 —— **先自证再取样**这条路走通了 |
| **段4 乙档配色表** | 表按「选择器 · 前景 · 背景 · 比值 · 对应合同图哪个令牌」列;主按钮那条给了四条走法且**不替我选** | **收**(见 §二) |
| **段5 上线批清点** | 12 条现查 + 这一夜新增 3 条,**只列不推、不碰生产** | **收** |
| **段6 收尾** | 回执 + 夜班总结 + 早报;早报把「要她拍板的只有主按钮那一条」摆在明处 | **收** |
| 全局 | 未推 main(本地领先 208)· 生产一次没连 · 4128 一个字没写 | **收** |

## 二、裁 · 主按钮:**先无条件回退,再谈选哪条**

**第一步(不用等店主,现在就做)**:`.primary` 那族的字色**回退成图里的白**。
理由:05z 那一轮把 `#fff` 换成 `--heroink`,把对比度从 **2.32 打到 2.02** —— 这是**一次没人批准的降级**,而且是**离图更远**了。回退它不是设计决定,是**撤销一次误改**;`图=合同`,回到图原样从来不需要谁拍板。
判据:`.primary` 一族的字色现值 = 图里那个值;造病:再换成 `--heroink` → 红并打印两个比值。

**第二步(交店主,我给建议)**:B / C / D 三选一。**我的建议是 B(字改深,约 3.9:1)**,理由三条:
1. **C 等于改合同图的金**——那是她的品牌色,牵动整套皮,不该为一个按钮动;
2. **A 是地板不是答案**(2.32 仍不达标),D 是记欠账;
3. **B 只动这一族的字色,金色一点不改**,而且主按钮是「立即预约 / 创建账号」——**顾客要点的那颗**,legibility 在这里最值钱。
**再加一条 B 的增强**:把按钮字号/字重推到**大字档**(≥18.66px 且 700),门槛从 4.5 降到 3.0,B 的 3.9:1 就**稳稳过线**,而不是「差一点」。

**这一批要交的**:B(含大字档)的**效果图两张**(浅/深各一),放 `handoff/night-runs/13截图_主按钮B案/`,**照 J-38 带拍摄时刻**。店主一眼就能选,不用读比值。她要是选了别的,照她的改并全仓复扫。

## 三、🔴 D188 的结构性四条还欠(下一批第一件)

段2① 只做了「三页补上令牌引用」。我 04:55 发的 `小批06b §三` 那四条 —— **你当时已经在跑段3,没看到,不怪你** —— 现查确实没做:仓里**没有一个 test 文件提到 `design-tokens`**。

**补齐(≤ 40 分)**:
1. **令牌引用收进统一入口**(共用 head 片段或模板注入),新建一页不该有「忘了引」这个可能;
2. **静态判据**:引了 `styles.css` 的 html **必须也引 `design-tokens.css` 且顺序在前**;造病:删 `index.html` 那一行 → 红并点名文件;
3. **运行判据**(比静态硬):每页首屏量 `getComputedStyle(document.documentElement).getPropertyValue('--paper')` **不许为空**;空即红并点名页面;
4. **全仓 html 再扫一遍**:演示页、打印页、分享页、平台后台 —— 还有哪些引了 `styles.css` 没引令牌。

## 四、下一批段序

**① 主按钮回退(§二 第一步,10 分)→ ② D188 四条(§三)→ ③ B 案效果图两张(§二第二步)→ ④ 登录态夹具 + 补扫那几页(会员档案 / 我的订单 / 钱包 / 结算 / 购物车有货)→ ⑤ 渐变面那 723 个用新定位法覆盖 → ⑥ 上线批:把 208 个提交推 main 的**准备**(分支保护、CI、回滚办法先写清楚,**仍不推**)。**

交齐:主按钮回退判据 + 造病 · D188 四条 + 两个造病 + 全仓扫描结果 · B 案两图(带时刻)· 登录夹具与补扫红榜(照「不是绿,是没扫」写法)· 渐变面覆盖数 · 推 main 准备清单 · 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 写库自报(三库分开)· 未推 main。

**停线照旧**:写 4128 既有行停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询 · 不许松判据/抬棘轮/换尺子。
