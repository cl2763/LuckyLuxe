> 🔴 **这份令从未送达。** 开令当夜 Cowork 与店主机器之间的桥断了,只发到了聊天窗,
> **没有落到桌面交接夹与仓里 `handoff/`** —— 按 J-46(指令落地律),它**不算下达过**。
> 那一夜 Code 按 06i §三 的顺序自行往下跑,是对的。**本文件是存档记录,不是待办。**
> 仍成立的两件(尺子扩面的机器证明 · J-45 扩到所有造景步骤)已并入 `小批07a`。

# 夜班令 8 · 约八小时 · 2026-09-10 夜

**上游**:06i(`9440713` → `e7849a5`,九个,领先 main 242)
**先声明两件,别误会**:

1. **这道令不等于 Cowork 收了 06i。** 开令时我这边连不上店主那台机器,**一条都没核**。
   所以 §一 是「把我明早要跑的证据先摆齐」,不是让你自证清白 —— 是让核的过程快一点。
2. **这一夜我可能拉不起你。** 桥断着,我不保证能中途醒来接话。
   **所以每一段都要能自己走完,卡住就按 §九 的停法停,写清停在哪、为什么,接着做下一段。**
   **不许 sleep 轮询等我。**

---

## 〇、先把裁定给你(两条,今晚就按这个做)

### 裁 #43 —— 尺子许扩面,不许改判法;而且「没改判法」要**由机器证**

你问「要不要冻结、另起一把」。**不拆。**

拆两把的代价是:从此背着一把**够不到登录态**的旧尺子,而且以后每次都要有人去判该用哪把 ——
那正是 J-39 立的时候要消灭的东西(「两个数必须由同一版判据产出」)。

**规矩定成这样:**

> **尺子许扩面(能扫到的地方变多),不许改判法(门槛 / 取背景 / 去重 / 两档分法)。**
> 而「我只扩了面」这句话**必须由机器证,不许由人说**。

**判据(本批做):**

- 拿 **上一版尺子** 和 **这一版尺子**,跑 **同一份页面清单**(取上一版够得到的那一份),
  两份结果 **逐条逐字节相同** → 绿;**不同 → 当场红,并列出差在哪几条**。
- 报告抬头(J-39)照旧打印刀的提交号,**加一行**:
  `本版相对上一版:只扩面 · 判法未动 · 证明见 <那份对比文件>`
- 这条判据本身要**造病验红**:把门槛从 4.5 改成 4.4 跑一次 → 必须红并点名。

**你这一批已经做的对齐(后台那份用新版重跑,甲 0 · 乙 2 与 06h 同数)是对的方向** ——
但那是「结果碰巧一样」,不是「判法没动」的证明。上面这条才是证明。

### 裁 #44 → 立律 J-45 —— 「到了那一页」不等于「到了那一页的那个状态」

你自报的那件(把空购物车当成「购物车有货」扫了一遍并记成扫过了)比它看起来重要。

> 🔴 **J-45:凡「我扫了 X」的断言,必须先证「我确实到了 X 的那个状态」。**
> 「到了那一页」≠「到了那一页的那个状态」——
> 空购物车和有货购物车是**两个状态**,登录态和未登录是**两个状态**,空店和有单是**两个状态**。
> 自证要在**扫之前**跑,拿不到那个状态就**按红报、写清卡在哪一步**,不许扫一遍空的记成扫过了。
> (这是 J-37「『在不在』不等于『看得见』」的兄弟条。)

**你加的那个「到没到那一页」的自证,一上就咬出「门店信息」的假通过(12 → 11)—— 这就是它该有的样子。**
本批把它**扩到所有造景步骤**,不只是扫页面那一路。

**顺便记你一功**:L4 夹具一造好就扫出那条链接蓝(`rgb(0,0,238)` 压 `rgb(42,34,28)` = **1.66**)——
**仓里一条 `a` 规则都没有**,所有没带 class 的链接一直是浏览器默认色,浅档压白还能读、深档压深卡看不见。
它躲过了此前每一轮全扫,只因为那一页要登录才进得去。**「夹具欠两批,债就藏在这里」这句话你写得对。**
而你按**类**修(一条基础 `a` 规则走金色文字令牌)而不是逐处修,是对的。

---

## 一、段 1 · 把我明早要核的证据先摆齐(约 40 分钟)

我明早会跑这些。你**先跑一遍并落盘**,我照着比 —— 对不上就是我核出来的红。

落到 `handoff/night-runs/夜8_待核证据_2026-09-11.md`,每条写「命令 + 输出 + 截至几点」:

1. `git log --oneline 9440713~1..e7849a5`(九个)· `git rev-parse --short HEAD` · `git rev-list origin/main..HEAD --count`
2. `node tools/design-token-diff.mjs` 末四行
3. `node apps/api/test-color-usage.mjs` 末五行(项数 · `GOLD_CAP` · 丙筐)
4. `node apps/api/test-token-entry.mjs` 全部(**扩面后现扫到几个来源、撞车几个**)
5. `node tools/dual-mode-diff.mjs` 的 ①筐条数
6. 三份红榜的「覆盖面」小节原文(顾客端 11/13 · 独立入口页 2/2 · 后台 19/19)
7. `bash apps/api/run-all-tests.sh` 的末 20 行(**111/111 · 3853**)
8. 那条 `a` 规则的**改前改后**与两档比值
9. 本机库未动的对照表 · 沙箱库心跳 9 条形状

> 🔴 **不许为了让证据好看去改任何东西。** 摆证据这一段**零代码改动** —— 有 diff 就是违令。

---

## 二、段 2 · 裁 #43:尺子「只扩面」的机器证明(约 1 小时)

按 §〇 裁 #43 做完三件:新旧同清单逐条对比 · 抬头加那一行 · 造病(门槛 4.5→4.4)验红。

对比文件落 `handoff/night-runs/尺子扩面证明_新旧同清单_2026-09-11.md`,抬头写清两版的提交号。

**做不出「逐字节相同」怎么办**:说明扩面时确实动了判法 —— **那就把动了的那处列出来,别硬凑**。
列出来我明早裁;硬凑是换尺子,直接停。

---

## 三、段 3 · 把「没扫」清零(约 1.5 小时)

**结算页** 与 **门店信息** —— 你点名的那两页,本批唯一没做到的。

- 按 J-45:**先证到得了那个状态**(结算页要有一张待结的单;门店信息要有一家填了地址电话的店),
  拿不到就写清卡在哪一步,**别扫一遍空的**。
- 扫完更新三份红榜的覆盖面小节,目标是 **顾客端 13/13**。
- 甲档必须 0;乙档列出来待裁。
- **顺手按 J-45 检一遍已经扫过的那 11 页**:有没有第二个「空购物车」那样的假状态。有就照实报。

---

## 四、段 4 · J-45 扩到所有造景步骤(约 1 小时)

把「到没到那个状态」的自证从扫页面这一路,扩到**所有造景 / 夹具 / 灌数据**的步骤:
每一步先自证目标状态成立(捞得到那一行 / 那个数不是 0 / 那个态是预期的),拿不到按红报。

**造病**:随便挑一步,把造景那句注释掉 → 自证必须红并点名是哪一步。
**反向守**:状态真成立时不许误红。

---

## 五、段 5 · 06d 两条自证 + 渐变面 717(约 1.5 小时)

**06d 你报做完了,这一段是自证**:
- 上线批清单**全仓只剩一份**(机械搜索:另一份的文件名 0 处引用)
- 会变的数**每个带「截至 HH:MM 现查」**,且由脚本重算 —— 造病:把脚本停掉 → 清单必须报「这个数没现查」而不是印一个旧数
- 危险命令白名单 8 条 + 刀 NN 的双红点名原样复现一次

**渐变面 717**:这一段只要求**分类**,不要求修完 ——
把 717 个未判节点按「渐变两端都在令牌里 / 一端在 / 两端都不在」分三堆,给出条数与前 10 条。
**不许为了减少数字去改渐变。**

---

## 六、段 6 · 配色批的料备齐(只出料,不动图)(约 1 小时)

给店主拍板用的,**一次拿出来,不零敲碎打**(J-41):

- 品类色 **B / N / S** 三档:各自现在的值、两档比值、J-42 判它有没有深色档
- 待裁 **#35** 那个渐变浅金 `#e7c9a4`:用在哪几处、条数
- **每一项给「用现有令牌能不能解决」的结论** —— 能解决的就不该进配色批
- 真需要改图的,出**浅深两张并排图**(改前 / 建议后),我明早拿去找店主

> 🔴 **一个像素都不许改合同图。** 这一段的产出是**图片 + 数**,不是改动。

---

## 七、段 7 · 待裁 #34 图标两套图(约 1 小时)

按 06h 裁 #40 的两条做:色值**从令牌文件现读**(生成脚本里零字面量)· 生成物要有判据
(两套 PNG 都在 + 深色那套取样像素 = 令牌值 + **反向守「两套不许是同一张复制两遍」** + 名单从目录现读)。

做不完就做到「判据先立、生成脚本改好、图没生成」这一步,写清停在哪。

---

## 八、段 8 · 收尾(约 40 分钟)

1. **全量回归**跑到绿(111/111 或更多),断言涨了就写清哪套涨几条
2. **两档全扫**重跑,三份红榜带覆盖面小节
3. **J-43 棘轮逐个重量**,变大的写在最上面
4. **本机库未动对照表** + 沙箱库心跳形状
5. 写 **早报**(给店主看的,不是给我看的):
   - 「你看得见的、变好的三件」
   - 「还没好的两件」
   - 「明早你只需要做一件事」
   - 结尾照旧:**这一夜没碰的**(生产 / main / 4128 的库)

---

## 九、掉线 · 停法 · 停线

**掉线自愈(你这批已经撞过三次,写成规矩)**:
4310 / 4128 / 开发者工具任一掉线 → 按记录参数拉回后,**必须验三样再继续**:
`/health` 通 · **库的绝对路径对** · **提交号对**。三样齐了才算回来。
**每次掉线都写进回执**:掉的是哪台、几点、怎么拉回的、验的三样是什么。
(机器 IP 变过 —— 若拉不回,**按红报并跳到下一段**,不许拿「环境问题」当绿。)

**停法**:任何一段卡住 → 写清「停在哪一步、为什么、我试过什么」,**接着做下一段**,不许原地打转,不许 sleep 等我。

**停线一条不松**:
不推 main(领先 **242**)· 不碰生产 · **写 4128 既有行仍停**(D185 §二 Cowork 还没批)·
不看密钥文件 · 不许 sleep 轮询 · **不许松判据 / 抬棘轮 / 换尺子** · **不许改合同图来凑代码** ·
沙箱模型 > 600 万 token 停 · 回归红 30 分钟定位不到停。

**密码一个字都不进回执、不进队列、不进任何交接文件。**

---

## 十、早上交三样

1. `handoff/night-runs/夜8回执_2026-09-11.md` —— 逐段,抬头**排在最后一个代码提交之后**,列全提交号
2. `handoff/night-runs/夜8_待核证据_2026-09-11.md` —— §一 那九条
3. `handoff/night-runs/早报_2026-09-11.md` —— 给店主的那一页

**任何一条没照字面做,写「我没照字面做,原因是 X」。**
这三批你用对了四次(顺序那条、夹具那条、尺子那条、购物车那条)—— **继续这么写。**
