# 员工端等同 · 剩余清单(08-30h 拉平批③,**清单先行,店主核对后动工**)

> 现状列全部**现码机核**(grep 双端调用面,2026-08-30 现测),不抄 08-28 旧表 ——
> 旧表之后已清掉的(A1/A2 账调、A6 小记图、A7 网页开单、A9 现金手记、RFM、segment-rules、
> stored-value/txns 合流、membership/members)不再列。

## 一、员工端等同(块 2 正主)

| # | 条目 | 网页 | 小程序 | 处置建议 |
|---|---|---|---|---|
| 1 | 我的业绩 `/admin/my-performance` | **0** | 1(my-performance 页) | 网页员工登录侧补「我的业绩」只读视图,读同一条口(员工端评审报告有图) |
| 2 | 员工补偿自查 `/admin/my-compensation-estimate` | 1 | **0** | 小程序员工「我的」补一块;连带并案:`/admin/salary/my-estimate`(两端 0:0,与本条同义两套口)→ 接的时候收敛成一条,另一条进死口退役 |
| 3 | 员工工作台 A4:workbench / 我的排班 / 报价试算 | 无对应 page | 3 页 | 分件:①我的排班=网页员工侧已能看 schedule-week(只读)→ 逐屏对确认即可;②报价试算器=网页补视图(复用 /admin/pricing/preview,零新后端);③工作台聚合页=网页补一屏(拼既有口) |
| 4 | 打卡(attendance) | 修正/补卡在网页 ✅ | 打卡在小程序 ✅ | **维持豁免**(硬件边界裁定#6:BSSID 只有真机拿得到);不算欠账 |
| 5 | 员工可见范围 `/admin/staff-visibility` | **0** | **0** | 漏接(两端都没入口)。请裁:出 UI(挂员工管理页)还是退役 |

## 二、编辑口归位(裁定:编辑=网页)

| # | 条目 | 网页 | 小程序 | 处置建议 |
|---|---|---|---|---|
| 6 | 充值套餐 `/admin/recharge-packages` | 2 文件 | 2 文件 | 机核两端都在调 —— 需**逐屏对**:若小程序仍有「编辑」动作则收为只读(裁定 08-27 编辑=网页);只读展示照留 |
| 7 | 次卡套餐 `/admin/timecard-packages` | 1 | 1 | 同上逐屏对 |
| 8 | 次卡设置 `/admin/timecard-settings` | 1 | 0 | ✅ 已归位(编辑只在网页),齐,零动作 |

## 三、配置类(网页=总和,网页缺)

| # | 条目 | 网页 | 小程序 | 处置建议 |
|---|---|---|---|---|
| 9 | 预约规则 `/admin/booking-rules`(线上定金开关) | **0** | 1(store 页) | 网页门店设置补同一开关(读写同口;与 deposit-config 摘要同区) |
| 10 | 排班设置 `/admin/schedule-settings` | **0** | 2 | 网页排班页现走别的口 —— **两套路由做一件事的分叉债**,并成单口后网页接同一条 |
| 11 | 打卡 WiFi `/admin/store-wifi` | **0** | 1 | 存值编辑网页可做(读 WiFi 靠真机,但「填 BSSID/名单」是配置);请裁:网页补编辑 or 维持豁免句 |

## 四、查看类(先逐屏对,再定补不补)

| # | 条目 | 现状 | 处置建议 |
|---|---|---|---|
| 12 | 已发布作品 `/admin/published-works`(web 0 / mini 2) | 网页 aiGallery 走别的口 | 逐屏对等价性;等价=写进「合理单端」白名单,不等价=补 |
| 13 | 召回摘要 `/admin/recall-digest`(web 0 / mini 1) | 设计初衷是老板首页横条 | 网页老板首页补横条(读同口,懒生成已在后端) |
| 14 | AI 摘要/洞察·财务差块·payroll 确认(B 组 ❓ 数条) | 双端各覆盖一部分 | 逐屏对出差集表再裁(不在本批清单内自作主张) |

**动工顺序建议**(核对后按此开):一 1→2→3 员工端正主 → 三 9→10 配置并口 → 二 6/7 逐屏对 → 四。
