# 夜班令 10 · 八小时 —— 先把「顾客登得进来」这条路打通,再回头清尺子漏层

**下达时刻**:2026-09-12 深夜
**店主状态**:**睡了。八小时内没人答话,一切按本令自己判断,不要等。**
**总纲**:**D190 全段放行**(店主已把图里三问全答「按你的建议来」)→ 07f §一 五件 → 排班五色 → `OWNER_TOKEN` 批量切。
**红线**:不推 main · 不碰生产 · 不看密钥文件 · 不许 `sleep` 轮询 · 不许松判据/抬棘轮/换尺子 · 不许改合同图凑代码 · 不写 4128 既有行。

---

## 〇 开工三件(十分钟内做完,回执第一行报)

1. **补落地**:把桌面交接夹里的 `小批07g_D190令_顾客端登录路_手机号做锚_2026-09-12.md` **和本令**各复制一份进仓里 `handoff/`,**四个 md5 全报**(桌面两个 + 仓里两个),不一致就停。
   Cowork 本次连不上仓目录,这两份的仓内副本**只能由你落**,不是我漏了(J-46)。
2. **开批快照**:`git rev-parse HEAD`、领先 main 多少、回归数、断言数、各棘轮值,**全部记下**,收尾要逐个对。
3. **预检**:`tools/ext-deps-check.mjs --strict` 跑一遍。**缺件就点名报,别绕过**。

---

## 店主已拍板的三件(图里的三问,全准)

- **① 过渡期撤按钮换引导卡** —— **准**。段三**放行**,今夜就做。
- **② 手机号 0.03 元/次收费** —— **接受**。按「只在这个顾客还没有手机号时问一次」的口径做。
- **③ 头像昵称可跳过** —— **准**。「跳过」必须留着,跳过落默认名「微信用户」,**不许拦路**。

**图(「顾客怎么登进来」v1)现在是合同。** 差一处判据就红,要改图先停下来报 Cowork(J-41)。

---

## 段一 · 三条判据先造病验红(**最重要的一段,顺序不许反**)

**先写判据、先让它红,再去实现。** 反了这一段不算。

1. **「所有判据至少跑一遍演示门关掉的环境」**
   - 回归套件加一档 `DEMO_LOGIN_ALLOWED=false`(即不设 `ALLOW_DEMO_ADMIN_LOGIN`),顾客端相关判据在这一档下**必须全部跑一遍**。
   - **造病**:把这一档去掉 → 必须红在「少跑了一档」上。
   - **这一档现在就会红一片 —— 那是真相,照实报。** 不许为了绿去放松任何判据、改任何门槛。
2. **「前端调的每一个接口,后端必须真的有」**
   - 扫 `apps/web/*.js` + 小程序里所有请求路径字面量,和后端注册的路由表对一遍。**只许后端有前端没调,不许前端调了后端没有。**
   - **造病**:把 `/auth/google/start` 留着 → 必须红,且报出 `文件:行号`。
   - 白名单具名冻结 + 写理由(J-51)。
3. **「界面上有按钮,那条路就必须走得通」**
   - 顾客端登录区渲染出来的每个可点入口,都要在 `DEMO_LOGIN_ALLOWED=false` 这一档下走通到「拿到 token」。
   - **造病**:把两个死按钮加回去 → 必须红。

**三条的造病红行原文贴回执。**

---

## 段二 · 小程序四步

1. **微信静默登录** —— `wx.login` 拿 code 换 openid/unionid,**不弹任何框**。
2. **要一次手机号** —— `getPhoneNumber`,顾客点一下。
   - **判条件写清楚**:只在这个顾客**还没有手机号**时出现;已有手机号的顾客这一步**不出现**。回执里贴这个判条件的原文。
   - 解密后写进 `user_identities.phone`。**解密失败要拒绝并报错,不许回落成空号继续走**(J-53 同族)。
3. **认人** —— **复用现有「手机号认领轻档案」严格逻辑**(`local-server.mjs:5680-5705`),四条一条不许放松:本店 + 号完全一致 + 没绑过微信 + **唯一一条**。不满足就走段四。
4. **头像昵称** —— 顾客自己选/填,**「跳过」留着**,跳过落「微信用户」,直接进店。

**一件事一处真相**:两端认人走**同一个函数**,不许各写一份。

---

## 段三 · 网页过渡态(**已放行**)

- 登录区**撤掉**「邮箱注册/登录」与「使用 Google 登录」两个入口(`apps/web/customer.js:945-951`)。
- 十个私有页(`privateViews()`,`customer.js:694`)未登录时换成**一张引导卡**:「在微信里打开」+ 小程序码。
- **公开页(店铺/作品/价目)照常可看,不要求登录。**
- `/auth/google/start` 这条**前端调用删掉**(后端本来就没有;`/auth/google/demo` 不动)。
- 代码注释写口径:**没有入口不丢人,有入口但点不动才丢人。**
- **网页目标态(手机号 + 短信码)今夜不做** —— 短信要单独申请,不拦上线。

---

## 段四 · 撞车进队列

手机号匹配到**多于一条**档案 → **不认**,写 `identity_merge_queue`,顾客照常按本次 openid 进店,后台出待处理。

- 不自动合并;不把顾客挡在门外。
- 写入字段:租户、手机号、命中档案 id 列表、来源端(mini/web)、时刻。
- **造一条撞车样例**跑通,贴回执。

---

## 段五 · 两档回归 + 焊死

- 段一三条判据**转常驻**,进预检。
- 全量回归**两档都跑**:`true`(本机沙箱)与 `false`(生产口径)。
- 回执报:两档各自 通过数/总数、断言数、**因为加了 false 档新暴露的红逐条列,一条不许瞒**。

---

## 段六 · 07f §一 五件(尺子漏层,J-56)

1. `tools/contrast-sweep.mjs` **把祖先的 `opacity` 累乘折进颜色**(现在全文件只有 `:223` 提到 opacity,只做「<0.1 跳过」,**从不折、也不看祖先**)。
2. **考勤页纳入扫描面**(现在 19 页里没有它,搜 `考勤`/`attendance` 零命中)。
3. **同尺重扫**(J-39):修好的尺子把全部页面重量一遍,出**新旧对照**。
4. **甲档非 0 就照实报** —— 不许找补、不许调门槛。
5. 出一份**「哪些已落地的结论是这把尺子量出来的」清单**,逐条重量或标注存疑 —— 包括我写进早报的那句「甲档三端两档全 0」。

---

## 段七 · 排班五色落地(07f 已定案)

- **整行 `opacity:.5` 取消**(口径:不重要用颜色深浅表达,不用透明度)。
- 五色定死:在岗 `--good` · 超时 `--bad` · 已下班 `--flat` · 休息 `--flat`(用不同底/文案区分)· **没排 = 不出徽标**。
- `rest` 与 `none` **拆开,不许同色**。
- **改完用段六修好的尺子量**,别量两遍。
- **「请假/调休」代码里没有,不许造。**

---

## 段八 · `OWNER_TOKEN` 按试点形状批量切

- 试点形状不动:只把现用那把写进 `DATA_DIR/.owner-token`,测试改成问 helper 要;**发布口只在 `ci`/`sandbox` 落地**。
- 迁移棘轮 **102,只许降**。
- 切完接 `secret-gate.mjs` 的闸,`local-server.mjs:161` 那条字面量回落**拔掉**(裁 #60)。
- 拔完**验出口**:不设 `OWNER_TOKEN` 时**拒绝启动**并打印可读的拒绝语(J-53)。

---

## 段九 · 收尾自证(留足 30 分钟)

- 全量回归两档 + 全部棘轮现读,和 §〇 快照**逐个对**;**降了要按 J-50 给归因**(真修好 / 扫描面缩了 / 判据放松了)。
- `tools/batch-footer.sh` 现出提交号、领先 main 数、读数时刻(**不许手抄,J-38**)。
- 本机库(4128)与生产库**未动**,按结构化标记写(J-49:`未动对照:<路径>`,**不许换同义词**)。
- **写一份早报**给店主,放桌面交接夹:`早报_2026-09-13_登录路打通.md`。早报只写三样:**今夜真做成了什么 · 哪里红着没修完 · 明天要她拍板的是什么**。

---

## 停线条件(碰到就停,把红行原样贴出来等 Cowork)

- 段一三条判据**造不出红**(咬不动)→ 停,别往下实现。
- `false` 档回归红**超过 30 分钟定位不到** → 停。
- 要动 `lucky-luxe` 任何一行历史数据 → 停。
- 需要改合同图才能过 → 停。
- 需要任何密钥 / 要看 `.env` → 停。`WECHAT_MINI_TOKEN_SECRET` 店主已灌,**不许查看、打印、拷贝**。
- 沙箱模型用量 > 600 万 token → 停。
- 同一处判据连续三次改不对 → 停,写清你试了哪三种。

---

## 回执要写什么(按段写,不要合并)

1. 第一行:§〇 的四个 md5。
2. 段一:三条判据的**造病红行原文**。
3. 段二:四步各自落点 `文件:行号`;「只问一次手机号」的**判条件原文**。
4. 段三:撤了哪几行、引导卡落在哪、公开页清单。
5. 段四:队列字段清单 + 撞车样例。
6. 段五:两档回归数 + **false 档新暴露的红逐条**。
7. 段六:新旧对照 + 那份「出自这把尺子的结论」清单。
8. 段七:五色最终值 + 用新尺子量出的比值。
9. 段八:棘轮值 + 拒绝启动的出口验证。
10. **底数闭合**(J-48):每把刀「量过的个数 / 去重后的行数 / 没验成的 / 被裁的」。
11. 没照字面做的地方写「**我没照字面做,原因是 X**」。
12. 没做完的段落写「**未做,原因 X**」——**不许用「已完成」盖过去**。
