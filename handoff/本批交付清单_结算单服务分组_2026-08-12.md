# 本批交付清单 · 结算单服务分组完整版 · 2026-08-12

> 图=合同:《结算单服务分组_UI设计图_2026-08-12.html》v2.2(v1/v2/v2.1 作废)。
> 价格计算引擎不动;结构重排+支付菜单化。⬜=图上没画,按最合理做记假设。

## 一、组内六件套(每组一份;组①默认在,组②由按钮创建)

| # | 项 | 状态 |
|---|---|---|
| G1-1 | ①价格体系:每组独立段选;默认系统会员判定自动选档;可手动改;改档留痕(现有口径落到组) | ✅ L1/L2 + CI ⑬(快照 tierChangedFrom 只落改档组);「已改档,将留痕」标签见 G-L5 截图 |
| G1-2 | ②服务项目:组内单选自动替换(沿用 e8ea109 逻辑,作用域=本组) | ✅ L1/L4;再点已选=取消 |
| G1-3 | ③加项目录:按本组主项目类别过滤;未归类的各组都显示(映射日后 S1 可配) | ✅ L1/L4(甲↔睫切换,目录跟着换);过滤=甲/睫域启发式,记假设 A-3 |
| G1-4 | ④自选填写行:本组独立(名称+金额+添加;行显示金额可移除) | ✅ L1(钻球 ¥50 行)+ L4(换主项不清自选) |
| G1-5 | ⑤本单技师:本组 1–2 位;双技师分成按既有规则店长日结核定,零新口径 | ✅ L2/L6(两组技师各自独立;第三位被拒);技师入组标题行 |
| G1-6 | ⑥被服务者:本组独立;留空=本人;朋友填称呼不建档,单据推卡主签 | ✅ L2 + 下游一遍(快照 servedPersonName 随组=「小美」) |
| G1-7 | 「＋添加第二个服务项目」按钮在组①末尾之后;组②整组同构 | ✅ L2(组② 六件套齐全) |
| G1-8 | 屏2 样式:已收起组显示摘要行(项目·主项·技师+展开▸;内容一行小字) | ✅ L2(addGroup 后组① collapsed+summary 非空,G-L2 截图) |

## 二、单级四件(整单一份,在所有组之后)

| # | 项 | 状态 |
|---|---|---|
| S-1 | ①定金:有收取记录才出开关(v1.2 口径不变),挂到带预约的那一组所属单 | ✅ L5(fixture 定金 ¥100:有记录出开关、抵扣 10000、关开关归零) |
| S-2 | ②支付构成·菜单式:储值卡抵扣/已收定金抵扣/线下收款/现场充值再抵扣,可组合勾选;每次变化后端重算回显;无余额不显示储值项、无收取记录不显示定金项 | ✅ L5 首轮余额 0 → 储值行隐藏;充值后出现;组合/纯线下两态后端重算核对 |
| S-3 | ②-2「去帮顾客充值 →」小字链接弹内嵌充值面板:复用既有代充流程,赠额只按套餐自动算;充完自动回本单、余额刷新、选项变可勾;轻档案同样可充 | ✅ L5 ×5(轻档案「自动化验证-勿动」充 ¥30+赠 ¥3 → 余额+33、面板自动关、储值自动勾);财务解锁门禁沿用不放宽(假设 A-5) |
| S-4 | ③分组明细+合计:各组标题行(项目N 主项·技师[·被服务者])→组内子行逐行原价划线→原价合计(划线)→档位小计→较原价共优惠(绿)→抵扣行(储值/定金)→到店应收;**原价与共优惠行不许丢** | ✅ L1(956/546/410 + 划线行≥2)/L2(两组分列) |
| S-5 | ④推送签署:现有闭环全保留(多张单同组,待签 1/N 顺序签;新客弹码规则⑤照旧) | ✅ 下游一遍:两张同组 groupIndex 1/2、2/2,各自单号;新客 fixture 弹码文案照旧 |

## 三、行为规则

| # | 项 | 状态 |
|---|---|---|
| R-1 | 组② 整组 ✕ 移除;组①只可替换不可移除 | ✅ L3(移除组② 合计回落、组① 不动;组① ✕ 被拒) |
| R-2 | 组内换主项目:类别不符的加项自动清除并提示 | ✅ L4(甲→睫:卸本甲/浅贴甲片自动清,自选行保留,明细 0 残留) |
| R-3 | 两组内容互不串(价档/项目/加项/自选/技师/被服务者) | ✅ L6(改组② 三处,组① 六字段快照+金额行逐字节不变) |

## 四、下游同构与金额红线

| # | 项 | 状态 |
|---|---|---|
| D-1 | 签署快照/日结/顾客端按同样分组呈现(组=单,双单口径现成路径) | ✅ 下游一遍:快照价档/被服务者随组、日结按两张单号分列;顾客端=既有双单路径(本批零改动) |
| D-2 | 业绩分成按现行规则落组(组技师各归各组的单) | ✅ technicians 随各 sheet 入库(CI ⑬ 建单验证);分成核定流程零改动 |
| D-3 | 金额红线:全部行由 computeSettlement 算出、前端零运算;组级合计由后端组预览一次给出 | ✅ 每轮 L1–L6 都把页面 formBody 原样重放后端直连比对,组级 JSON 逐字节一致 |
| D-4 | 护栏沿用+按组断言:每组预览行≡该组勾选;组合计≡各单后端合计(CI 断言常驻) | ✅ 页面侧 D22 护栏分组版 + CI ⑬ 14 条常驻(suite 35→49 断言) |

## 五、验收 L5 各 5 次

| # | 路 | 状态 |
|---|---|---|
| L-1 | 单组全流程 | ✅ 5/5(G-L1 截图) |
| L-2 | 加组②(不同价档、不同技师、不同被服务者) | ✅ 5/5(G-L2) |
| L-3 | 移除组②(整组消失、合计回落、组①不动) | ✅ 5/5(G-L3) |
| L-4 | 组内换主项目→类别不符加项自动清+提示 | ✅ 5/5(G-L4) |
| L-5 | 支付菜单组合(储值+定金+线下/纯线下/充值面板进出充完回单再抵扣) | ✅ 5/5(G-L5 ×3 张;首轮 0/5 是脚本断言把 automator 的 null→undefined 工具痕迹当缺陷,截图定性后修断言,产品无缺陷) |
| L-6 | 两组互不串 | ✅ 5/5(G-L6) |

## 假设清单(图上没画,按最合理做;等店主/Cowork 裁决)

- **A-1 优惠券维持单级**,挂组① 的单(图 v2.2 未画券区;一单一张口径不变)。
- **A-2 整单规则(足部加收/甲片重复利用)维持单级**,发给组①(图 v2.2 未画)。
- **A-3 加项类别过滤=甲/睫域启发式**(类目名含「甲」=甲域、含「睫」=睫域、其余=未归类各组都显示);正式映射表 S1 再配。
- **A-4 档位充值按「实收+赠额」一笔入储值**(充值=预收不进收入,口径不变),note 拆明实收与赠额;实收/赠送分笔的正式账目口径随 S2。券/项目类赠送 S2 支持,面板先只显示说明。
- **A-5 充值面板权限沿用既有门禁**(老板会话+财务密码解锁),不放宽。合同写「技师不离开结算单就能充」——技师会话点链接会被引去财务页解锁(解不开=403)。**要不要给技师放行代充,请店主裁决**;本批不动权限口径。
- **A-6 明细子行逐行原价划线**(会员价≠原价的行都划);图 mock 里加项行有合并展示,按规则文字优先逐行。
