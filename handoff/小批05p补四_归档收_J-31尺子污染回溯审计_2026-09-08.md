# 小批05p 补四 — `ef0c6ea` 收(9 份明细落仓、`ranOn` 在、跑机默认落仓、预检核出处)· 🔴 J-31 尺子污染 → 历史三跑回溯审计(10 分钟)· 其余原序

> 2026-09-08 · Cowork 现查:`ai-eval-results/05p_预约到底率_{老尺,新尺,北京店_新尺}_第1–3轮` 九份在,uid 带执行随机段(`br-05p-old-1-mtpw9z3u-0`),`ranOn` 在;05l/05n 历史文件 uid 形如 `br-05l-final1-0`、`br-05n-old1-0`——每轮标签不同,但 `final1`/`fin2` 这种命名暗示同标签前有过试跑 · 两处各一份

## 一、收

跑机每次执行换全新对话、北京店文件名带店范围、`/tmp` 分支删净、预检核回执出处(含不带目录的写法)、「预热」撤了不留看不懂的改动——**收**。老尺 6→5、新尺 10 不变、北京店 4/6 ×3,读版 v3 已按文件改——收。

「量不准的是尺子,不是代码」——**收进口径,登记 J-31**:评测跑机的会话 id 必须每次执行唯一,任何复用 = 污染;此规矩适用于 `booking-rate` / `fact-hit` / `human-five` / `run-200` / `run-edge-80` 五把(单轮的也不例外——转人工/接管态会跨执行残留)。

## 二、回溯审计(现在做,不等段 4)

历史上报过的到底率:05l 6/7/6、05n 老尺 7/7/7 · 新尺 8/8/10、05o 基线 0/12、今晨 6/6/7。**每一份归档明细逐通核**:拿 `convId` 去沙箱库 `conversation_messages` 数该会话在**这一轮第一句之前**是否已有消息(按 `created_at` 早于该轮 `ranOn`/文件 mtime);有 → 标「污染」,无 → 标「干净」。输出一张表 `handoff/ai-eval-results/到底率历史审计_2026-09-08.md`:文件 · 干净/污染通数 · 该轮数字是否可信。读版 v3 那段「改数原因」按审计结果补一句;诉求总账里的曲线我按这张表改。**不重跑历史,只判可不可信。**

## 三、其余照 05p 原序:段 4 小件 → 主页重画段 5、段 6。
