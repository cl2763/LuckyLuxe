# 小批05r 补一 — D154 `af5e79d` **收(带三条现修)**· 🔴 回执说「交了两张截图」仓里一张都没有 · 大数「CAD $25885 CAD」币种出两遍、无千分位 · 待裁 #1 裁:补录不过休息日闸,照合同落今天 · J-33 keep-list 只许沙箱 · 然后 D155

> 2026-09-08 · Cowork 现查 4310 与仓(`af5e79d` 的 stat、`D154_逐块对照表`、`admin.html` diff、备份文件 integrity、`wechat_conversations` 现状)· 两处各一份

## 一、收

- 两栏(550.93 | 479.06)、折线用后端 `spark`、台面 `TodayBoard.mountInto` 同一根节点 `#todayBoard`、旧四卡老板分支渲染删掉(员工分支那五张不是这批的事)、月目标不编、宿主三列网格改块流、`request()` 用错店会话这两个坑追出来——**收**,而且第二个坑正是 J-32 的机器版:证据是真的,只是证的不是那家店。
- 660 → 1:备份 `lucky-luxe.sqlite.pre-j33-20260907T143839` 我跑了 `integrity_check` = ok,里面还是 656/4——**收**;快照刀看不见 UPDATE、指纹刀报 1188 行消失——J-29 立对了,记进口径。
- 待人工只数 `needs_human`——收。
- 回归红 1 条没降基线凑绿——**对**,裁在 §三。

## 二、🔴 三条现修(D154 收尾,合计 ≤ 60 分)

**1. 截图零文件。** 回执写「本轮只交了旗舰店今日/本月两张与休息日态」,`git show af5e79d --stat` 没有一张图,仓里 `find -name '*.png' -newer today-board.js` 也只有 09-03 的老图。「交了」而没有文件,与 J-27 同族。裁:六张一次补齐(旗舰店今日 / 小婕店 4128 今日 / 北京店 4310 今日 / <900 窄屏 / 休息日 / 失败态),**落仓** `handoff/核验截图_2026-09-08/D154/`,文件名带店与态;回执里每张一行「文件名 · 店 · 态 · 视口宽」。没落仓的截图 = 没有截图。

**2. 大数币种出两遍、无千分位。** 对照表 #6 现测 `CAD $25885` + 小字 `CAD`——顾客看到的是「CAD $25885 CAD」;图 §三 是 `<small>CAD</small>2,486`。根子:`money()`(`admin.js:832`)只出一个拼好的串,首页又自己补了小字。裁:**不许首页拼币符**(红线不动),在同一出口旁加 `moneyParts(cents) → { code, symbol, text }`,`text` 带千分位;`money()` 本身也加千分位(全仓受益);首页大数 = `<small>{code}</small>{text}`,四小牌照 `money()`。判据:`[data-dh-metric="revenue"]` 的 `textContent` 里币种码**只出现一次**;`text` 匹配 `^\d{1,3}(,\d{3})*(\.\d+)?$`;造病:首页自己写 `¥` → `test-currency-scan` 红(已有刀)。

**3. 三店都要看。** 对照表只在 4310 旗舰店测;4128 小婕店与 4310 北京店各开一次页面(截图即 §二 1 那两张),对照表右栏补两列「4128 / 北京店」各写一句现测。

## 三、待裁 #1 · 裁:补录**不过休息日闸**,照合同**落今天**

休息日闸的白名单理由是「顾客这天约不上」,管的是**预约**;老板在休息日补录前一天的单是**记账**,不是约。合同二形二「补录到已日结的日子 → 落今天」不改。所以:① 补录路径不经休息日闸(闸的白名单里加这一条理由,随码复核);② 单上照旧注明「服务发生于 X 月 X 日」;③ 休息日若有补录账目,日结照常可做,「待日结」照常提示——账在哪天记就在哪天结,这不怪,怪的是把钱挪到一个还没到的日子。判据:两条钱链判据**周一也要绿**——造景把夹具店「今天」设休再跑;造病:让补录过闸 → 红。`test-observe-fixes` 那 11 条从「本轮未验」回到验;基线 48 → 59 回来才算关。不采「落下一个营业日」:那是把今天记的账写到明天。

## 四、J-33 · keep-list 口径只许沙箱

「除保留名单外全关」在 4310 可以(没有真顾客),但工具要**自己拒绝**在别处跑:`close-eval-conversations.mjs` 在 `CEC_MODE=keep-list` 时,目标库路径不在 `apps/api/sandbox-data/` 下就退出不写(造病:指到 4128 库路径 → 拒绝);保留名单写在工具顶部,回执抄一遍。以后跑机收尾仍按 J-31 随机段认领自己的会话,keep-list 只是这次清历史用。

## 五、然后 D155(小程序 AI 同一出口),按 05r §二 原文;再补段 7。

## 六、交齐

六张截图路径 · `moneyParts` + 币种码只出一次判据 · 三店对照表 · 待裁 #1 落地(周一绿、造病红、基线 59)· keep-list 沙箱守门 · 提交号 · 回归/断言增量 · 整跑时长 · 棘轮两数 · 未推 main。
