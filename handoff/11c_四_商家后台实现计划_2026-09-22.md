# 11c §四 · 商家入驻实现计划 —— **只列,不动手**

> 2026-09-22 · 现查 `platform.html`(1,004 行)+ 11 组 `/platform/*` 口 + `tenants` 表结构
> 合同图(每批重贴):
> · 有迹平台后台 `https://claude.ai/code/artifact/7254c183-89fc-45df-b019-7345f117e95c`
> · 有迹新店入驻向导 `https://claude.ai/code/artifact/11de3384-9ce3-47bf-aa21-2436aab60c93`
> · 有迹商家入驻手册 `https://claude.ai/code/artifact/30efd5dd-f32f-4828-99cd-2c640580d2c7`

---

## 〇 🔴 先纠一件事:**「那条线只有图,没有一行实现代码」不对**

11c §四 这么写,但**量出来不是这样**。平台后台已经建得相当完整:

**已有的口(11 组)**:`/platform/tenants`(建店)· `overview` · `billing` · `leads` · `kb` · `ops-log` · `backup` · `session`
**每家店能配的**(`/platform/tenants/:id/…`):`store` · `business-hours` · `technicians` · `services` · `categories` · `membership-config` · `kb` · `kind` · `listed` · `owner-password`

**`platform.html` 已有的屏**:概览 · 商家速览 · 新建商家 · 商家管理 · 商家配置(门店信息 / 营业时间 / 技师 / 服务价目表 / 大类字典 / 会员制度 / 品牌事实 / 常见问答)· 商家订阅总览 · 套餐计费(+1月 / +1年 / 加量)· 入驻线索 · 待办 · **CSV 导入四步**(粘贴或上传 → 试跑报告 → 确认执行 → 执行结果)· 重置老板密码 · 吊销所有设备

> **⇒ 「开一家店 + 把它配到能用」这条路,后台今天就能走完。**

## 一 🔴 真正缺的那一块:**D197 v2 的「两条路分叉」一个字都没有**

图 v2 的核心是**第一屏问一句话**,按答案分两条完全不同的路:

> **「顾客点开客服号,资料页底下那行小字『由某某企业提供服务』—— 你要它写谁?」**
> · 答「写有迹」⇒ **平台主体**:当天开店,商家不注册企微、不认证、不配密钥
> · 答「写我自己店的公司」⇒ **个人主体**:商家自己注册企微 + 企业认证(1–5 工作日),再交六个字段

**现查结果(精确):**

| 查什么 | 结果 |
|---|---|
| `platform.html` 里 `主体` / `企业微信` / `企微` / `corpId` / `向导` / `分叉` | **0 处**(一个字都没有) |
| 建店口收的参数 | `id` · `name` · `plan` · `initialTerm` · `isDemo` —— **没有主体** |
| `tenants` 表的列 | `id name plan status created_at updated_at plan_expires_at finance_password_hash auto_renew finance_lock_enabled kind listed` —— **没有任何主体/企微列** |

**⇒ 缺的不是「后台」,是「入驻那一问和它分出来的两条路」。**

## 二 🔴 最小可用切片 —— **判别式:第一个商家真正入驻,最少需要哪几屏?**

**答案:小婕走的是哪条路,决定了切片大小。**

- 半径(店主自己)走的是**个人主体**(手册里写着)。
- **小婕如果走「平台主体」**,那她**一屏都不需要新加** —— 现有的「新建商家 + 商家配置」就够,**当天能开**。

> 🔴 **所以最小切片是:先确认小婕选哪条路。**
> **选平台主体 ⇒ 这一件这两天不用写代码,用现有后台就能让她开始用。**
> **选个人主体 ⇒ 才需要下面那几样。**

**这一问请你替我问她。** 问法照图上那句(不要问「平台主体还是个人主体」,没人知道那是什么)。

## 三 如果要做(个人主体那条路),动哪几个文件

| 文件 | 干什么 | 预计行数 | 棘轮 |
|---|---|---|---|
| `apps/api/tenant-onboarding.mjs`(新) | 主体字段的读写 + 六字段校验 + 状态机(待认证/已认证/已接通) | ≈ 220 | 否 —— 新功能新模块(公约①) |
| `apps/api/local-server.mjs` | **只加一行分发** | 🔴 **+1 行** | 🔴 **是(17,539 只许降)** |
| `apps/web/platform-onboard.js`(新) | 向导第一屏 + 两条路各自的后续屏 | ≈ 260 | 否 |
| `apps/web/platform.html` | 挂载点 | +3 行 | 1,004 行,未超线 |
| `apps/api/test-tenant-onboarding.mjs`(新) | 判据 | ≈ 200 | 测试不设限 |

🔴 **后端那 +1 行照旧走 (甲) 抽取抵掉**(`local-server.mjs` 里还有可搬的:`snapshot` / `signature` 那一族)。**不申请 (乙) 额度。**

## 四 建表 / 加列 —— 「推完会多出什么」

| 变化 | 数 | 会不会覆盖既有数据 |
|---|---|---|
| `tenants` 加列 `subject_kind`(`platform` / `own`),默认 `platform` | 现有 6 家店全部落到 `platform` | 🔴 **要确认**:半径实际是 `own`,**加完要单独改这一家**,否则它的主体是错的 |
| `tenants` 加列 `wecom_corp_id` / `wecom_agent_id` / `wecom_secret_ref` | 全部 NULL | 不会 |
| 🔴 **密钥本身不入 `tenants` 表** | —— | **只存引用,真值进 env 或密钥文件**(停线:密钥不进库不进日志) |
| 新表 | **0 张** | —— |
| 一次性迁移 | **0 条** | —— |

🔴 **列一律走 `try/catch ALTER TABLE ADD COLUMN`**(纪律 8),并配 `test-schema-consistency` 断言。

## 五 判据清单 —— **跨租户那几条是主体**

这一件碰的是**别人的店**,所以判据的重心不在「功能对不对」,在「会不会串味」:

| # | 断言 | 反向守 |
|---|---|---|
| 1 | 🔴 **A 店的平台操作改不到 B 店的主体字段** | 造得出阳性(J-58①) |
| 2 | 🔴 **非平台令牌一律 401**(这几个口全是 `/platform/*`) | 换平台令牌必须 200 |
| 3 | 🔴 **企微密钥零落库 / 零进日志** | 扫描 + 造病:塞一个假密钥进去必须红 |
| 4 | 主体字段只许是 `platform` / `own` 两值 | 第三个值 → 400 |
| 5 | 🔴 **改主体要留痕**(谁、何时、哪家店、改成什么)—— 走 `actorOf` 唯一出口 | 留痕三样缺一红 |
| 6 | 建店幂等:同 id 重建 → 409,不产生第二家 | —— |
| 7 | 🔴 **半径那家店的主体在迁移后是 `own` 不是默认值** | 迁移后逐家核对 |

## 六 我的建议(等你裁)

1. 🔴 **先问小婕走哪条路** —— 这一问决定这两天要不要写代码
2. 选**平台主体** ⇒ **今天就能让她开始用**,这一件排到第二段
3. 选**个人主体** ⇒ 按上面做,**但要先等她的企业认证**(1–5 工作日),代码可以并行写

**顺带一笔文档漂移(小,但会让人找错地方)**:
`CLAUDE.md:86` 写的是 `POST /platform/tenants/:id/owner-password/reset`,
**真实路径是 `/platform/tenants/:id/owner-password`(没有 `/reset`)**。下次动 CLAUDE.md 时顺手改。
