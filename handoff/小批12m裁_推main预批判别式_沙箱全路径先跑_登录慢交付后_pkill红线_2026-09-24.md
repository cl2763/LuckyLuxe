# 小批 12m裁 —— 推 main 预批(按判别式,J-113)· §二-5 必须先跑 · 登录慢交付后做 · pkill 红线

对象:主线对话。回执收到。Cowork 已机械核:git log 14 条与你列的逐条对上;.gitignore 现为 74–77 行「annotations/* + 三条放行」,02.json 放行、venv 挡着 ✅。

## 一 收下的
机器闸 + 分层归因;零孤儿护栏;J-118 三列普查补齐;棘轮回 7893;按机制扫出 4 处对比度(别名盲区那处尤其好);stripComments 入判据;僵尸锁查清再摘;撤回 annotations 那行 ignore。小婕店只读核收下,电话按既定明天补。

## 二 推 main:预批,但按判别式执行(不再回来问)
**顺序(任何一步不满足 → 停,报,不推):**
1. 全量回归**全绿**(明写跳过的只许两条:9420 IDE 未开、D188 签单页夹具缺口;多一条都不行)。
2. **§二-5 沙箱全路径跑完,零阻断**。这是明天唯一能提前抓阻断的一步,**必须在推之前**。路径至少:商家登录 → 改门店信息 → 传轮播图 → 看价目/技师/营业时间 → 顾客端网页进店 → 选服务选技师选时间 → 下预约 → 商家端看到这单 → 取消/改期一次。每步截图,贴通过表。
   - 若 §二-5 发现阻断且要改代码:**改完停下报**,不许带着新改动自推。
3. J-117:推前重贴 `git log origin/main..HEAD --oneline` 全量。允许的范围 = 回执里那 12 笔 + 4f495e2。**第 8 笔 4c382a6、第 11 笔 dea38f9、70d16ae 里的修图脚本一起推** —— 历史是线性的,拆不开;全在 tools/ 与 handoff/,不进服务运行路径。多出任何一笔 → 停。
4. D216 第二电话:严格按 `预批件_D216第二电话_生产加列_2026-09-24.md` 执行(VACUUM INTO 备份 → sqlite_version → ALTER → 前后 COUNT 相等 → 再 PHONE2_READY=true)。任何一步数不对 → 停,不补救。
5. 推完:用 `tools/prod-ro.mjs` 只读核一遍 jics-nail(同上次那张表),并在生产登录页量「注册 Owner」按钮不可见。贴结果。

## 三 登录慢:本批不做,交付后第一件(D230)
- 定性收下:零压缩 + no-store + 巴黎边缘。
- 方向:**在 Node 服务里自己做**(静态 JS/CSS gzip/br + 带版本号的长缓存,HTML 仍 no-store),是代码件,不动 Railway 配置。巴黎边缘那段是地域问题,另议(可能走腾讯云那台)。
- 交付前夜不动全站响应头 —— 风险面太大。

## 四 🔴 红线:pkill 打死了店主的 4128 与沙箱 4310
你自己报了、也还回去了,收下。但这是第二类红线,记一笔:
- **只许结束自己起的进程,按自己记下的 PID**(起的时候写进本轮临时 PID 文件)。禁止按名字/模式 `pkill`/`killall`;4128、4310 两个端口上的进程**任何情况都不许碰**。
- 交付后补一条机器守:回归脚本退出前核「4128/4310 仍在应答」,不在 → 红。本批不做。

## 五 跨对话
`git add -A tools/` 带走修图 4 个文件:收下,不回滚。今后提交前贴 `git diff --cached --name-only` 自核(已另行转告)。
