# 结案批 · 交齐(03t §四 终态:修 / 答 / 挂)

> 2026-09-03 · 覆盖 03q → 04e 共 **19 个提交**(`b59fe1a` … `2fc332e`)
> **未推 main** —— 这一整串都还在本地,一次没推过。
> 终态只有三种:**修**(已改并有常驻判据)· **答**(查清了,不需要改)· **挂**(明确挂在哪一批)。

---

## 〇、总账

| 终态 | 条数 |
|---|---|
| **修** | 20 |
| **答** | 6 |
| **挂** | 9 |

---

## 一、缺陷册(D104 – D136)

| # | 一句话 | 终态 | 落在哪 / 挂在哪 |
|---|---|---|---|
| D104 | 原生对话框族(`wx.showModal` 等)全仓 34 处 | **修** | 03t;`test-native-dialog` 7 项常驻 |
| D109 | 徽章「休息中」而服务卡照常可点 | **答** | 后端 6365/6375 拦得住;日历置灰 → **挂:主页重画批** |
| D112 | 中文态仍挂 `Popular nail / Popular lash` | **挂** | 主页重画批(那一区整块要重画) |
| D113 | 底部 tabbar 选中态形制不符 | **挂** | 主页重画批(形制随图) |
| D121 | 演示数据无标记 | **修** | 03t;`demo-mark.mjs` + `test-demo-mark` 8 项,97/97 打标、幂等 |
| D123 | 令牌作废 | **修** | 03p |
| D124 | 写库脚本默认目标库 | **修** | 03g/03o/03q;`test-db-target-guard` 15 项(按解析点判) |
| D126 | `users.tenant_id` 有 DEFAULT | **挂** | **上线硬门槛批**(去 DEFAULT 要重建表,已扩为 31 张表) |
| D127 | 跨租户单(单在 A 店、档案在 B 店) | **修** | 03x/04a;两道闸 + 拆档迁移脚本(六张跟单表同事务) |
| D128 | `INSERT INTO users` 漏写 tenant_id | **修** | 03v;`test-tenant-explicit` 白名单式硬 0 |
| D129 | 会员梯子名 | **修** | 03t;`test-tier-label` 8 项 |
| D130 | `user_identities` 身份表串味 | **修** | 04a;代码 + 存量(沙箱 113→0 / 本机 96→0)+ `test-identity-tenant` 8 项;**生产存量挂:上线批** |
| D131 | 31 张表同族漏写 tenant_id(9 处) | **修** | 04b;九处全修、棘轮硬 0、非默认租户行为判据 |
| D132 | 会话主键不带租户 + 企微入站回落默认 | **修** | 04c;唯一索引 + 新 id 形态 + `open_kfid` 路由 + `test-conversation-tenant` 15 项;**生产索引与 kfid 映射挂:上线批** |
| **D133** | 一轮没命中白名单 → 整通对话永久静音 | **挂** | **AI 预约闭环批**(12 场景基线咬出;对照实验四步为证) |
| **D134** | `PUT /admin/kb/facts` 静默丢键回 200 | **挂** | AI 预约闭环批(修法已定:不认识的键 400 点名) |
| **D135** | quoted 段任意消息都生成草稿 **且把过期时间重置回未来** | **挂** | AI 预约闭环批(04e 重跑加重:过期旧价会被自动续期) |
| **D136** | 定金事实一致性 | **答** | 04e 逐项并排:**不是模型改写事实**,两店配置本来不同,模型两次都忠实;「Silver 会员」也来自店数据 |

## 二、事故与护栏族

| # | 一句话 | 终态 | 证据 |
|---|---|---|---|
| 事故一 | 02x `seed-bigdemo` 默认目标 → 148 行写进本机库 | **修** | `requireTarget` + 无默认目标(D124 族) |
| 事故二 | 03d 手敲 curl 冲销了一条真实收入 | **修** | 写库自报律·手令形态;`test-db-target-guard` 按解析点判 |
| 护栏族 · 三列清单 | 手写清单两个方向都错过(说有的没有、真有的没写) | **修** | 03z/04a;清单**由刀生成** + ④「清单 ≡ 当前提交现扫」常驻 + 机制判据 ④b |
| 护栏族 · 事务回滚 | 「包了事务」的九处里 `ledger-guards` 根本没有事务 | **修** | 03y;包进事务 + `test-txn-rollback` 造病验回滚 |
| 护栏族 · 未动须有证 | 回执写「未动」却无对照表 | **修** | 03q → 04c;快照四栏(表/行/列/**索引**)+ 棘轮 34 |
| 护栏族 · 扫描面 | 未 `git add` 的新文件对所有护栏刀隐形 | **修** | 04a ⑤;04c ⑤b(被 `.gitignore` 挡住的也要为 0) |
| 护栏族 · 断言增量 | 「全过」只说明没红,不说明跑过 | **修** | 计数即证;04a 当场抓出 16 条断言在空转 |
| 口径④ | 顾客侧回落默认租户 | **修** | 04d-1;fail-closed + 13 夹具补头 + 两层判据 + 两端真机 |

## 三、02b 五条现查

| # | 终态 | 结论 |
|---|---|---|
| ①②③④ | **答** | 03u 逐条现查,四条无需改动 |
| ⑤ | **修** | 03u 现修(红线那条) |

## 四、挂起清单(逐条写明挂在哪一批)

| 挂什么 | 挂在哪一批 |
|---|---|
| D109 日历置灰 · D112 两行英文 · D113 tabbar 形制 | **主页重画批**(图 v3.1 已是合同) |
| D133 整通静音 · D134 KB 丢键 · D135 草稿/续期 | **AI 预约闭环批**(图由 Cowork 出,出图前不动那一路) |
| D126 去掉 31 张表的 `DEFAULT 'lucky-luxe'` | **上线硬门槛批** |
| D130 生产存量身份修复 · D127 生产那一条拆档 | **上线硬门槛批**(两件要同一批上,理由见回执 04a §五 B) |
| D132 生产建索引 + **企微接通前每店先填 `open_kfid`** | **上线硬门槛批**(不填则进线一律拒收) |
| 网页「门店设置 → 企微客服账号」那一行 | S5 收尾批(那批要把通用设置段搬出 `admin.js`,顺手落) |
| 沙箱 58+ 通走查会话 | 店主看完再清;清时只删这些,不动别的 |

## 五、交齐时的硬数

- **最后一次全绿回归**:`handoff/regression-logs/回归日志_2026-09-03_04e_*_89套件.log` · **89/89**
- **断言基线**:**2,897**(尺子=纯 `^ok`,不含基线套件自己那 5 条)
- **巨型文件**:`apps/api/local-server.mjs` **18,145** / 棘轮线 18,243 · `apps/web/admin.js` **8,550** / 棘轮线 8,552
- **四库四名**:

| 库 | 本结案批期间 | 证据 |
|---|---|---|
| **生产库** | **未动**(全程只读诊断四次) | 每次只读输出都在对应回执里 |
| **本机库** | 写过一次(D130 存量修复,220→207→234)· 其余各批**逐表零差异** | 04a §六 / 04c §五 / 04d-1 §三 对照表 |
| **沙箱库** | 写过多次(D130 修复、D132 索引与新表、12 场景走查会话) | 各批对照表逐条列了来源 |
| **回归临时库** | 每次新建、跑完即删 | `run-all-tests.sh` |

- **推送状态**:**未推 main**。19 个提交全在本地,`git status` 除 `deliverables/`(店主自己的图)外干净。

