# 回执 04a — `0ec3bff` 三病现修 · 🔴 D130 身份串味 · 🟡 D131 报数(第一轮)

> 生产库未动(只读诊断两次)· 沙箱库与本机库各写一次(D130 存量修复,逐表行列对照见 §六)
> 未推 main。第二轮(AI 复测 12 场景)另交。
> **提交号 `25f9249`** —— 回归日志文件名带同一个号(店主 04a §一 病一 要求的对应关系)。

---

## 〇、一句话

三病都认、都修;**修的过程里又咬出四条**,其中两条是**我自己的判据在说谎**:
① 第一版 ④b「清单里不许出现刀本身」是**黑名单判据** —— 造病验红当场没红;
② D130 修复脚本的「已有正确行」用的是**扫描时的快照** —— 在沙箱真数据上当场撞唯一键。
两条都归《幂等判据律》与《白名单判据 > 黑名单判据》,已换成机制判据并各自造病验过红。

---

## 一、`0ec3bff` 三病

### 病一 · 改动没入库 —— 认,并答您问的那一句

**回归日志跑的是工作区版**。证据:03z 日志第 3066 行写着「现扫出 **14** 处」,
而 `HEAD` 里那份还是手写九处 —— node 读的是磁盘,不是 git。
**顺带我自己那句「12 处」也是错的**:我手扫时是 12,可当批新写的两个文件让它变成了 14,
而我把 12 写死在了断言的**文案**里。这一版把处数从文案里拿掉了 ——
**判据不许锚在会变的字面量上,连它的解说词也不该**。

本批 `test-txn-rollback.mjs` 已入库,重跑输出见 §六;日志文件名带新提交号。

### 病二 · 清单在同一提交上已过期 —— 认,改法比您裁的多一层

您裁的三条都落了,但根不在「忘了重生成」:**生成清单的尺子和守清单的刀,各写了一份**。
所以先把尺子收成**唯一一份** `tools/guard-scan.mjs`,三处共用同一个 `scanWriteSites()`:

| 用它的 | 用来做什么 |
|---|---|
| `tools/gen-guard-checklist.mjs` | 生成清单(现在是薄薄一层 CLI) |
| `apps/api/test-db-target-guard.mjs` | 守「清单 ≡ 当前提交现扫」 |
| `apps/api/test-txn-rollback.mjs` | 第三列逐个验回滚 |

**排除面**(您裁的第 2 条)写成**带理由的表 + 棘轮**,并**印在清单头的「尺子」段里**:

```
排除面   「刀本身」——(a) 路径含 /test- 或 /run- 的;(b) 显式声明的两个:
         · tools/guard-scan.mjs      —— 尺子本体
         · tools/gen-guard-checklist.mjs —— 清单生成器
```

**您没看到的另一面,我现测才发现**:刀咬自己**两个方向都会说谎** ——
生成器不但被当成「含事务的写库点」列进了清单,还因为源码里有 `requireTarget` 这个词
被 ①c 当成**「接了护栏」**,所以那条判据对它一直是绿的。

清单已重生成:`tools/migrate-cross-tenant-bookings.mjs` 在册(A 类 · 直连库 · ✔ requireTarget · ✔ 有事务),
生成器与尺子都不在册。尾行那个「A 类未接护栏 2」也修了 —— 那两个是打 COS 的
(`ops-cos-delete` / `smoke-cos-upload`),**差额当场解释掉,不留给人猜**。

#### 🔴 我的 ④b 第一版是黑名单判据,造病当场没红

第一版写的是「清单里不许出现 KNIVES 里那几个文件」——
把生成器从 KNIVES 里拿掉,**判据就不再去找它**,于是绿。
这正是您 08-27 立的那条在**判据自己身上**重演一遍。

换成机制判据:**清单里列出的每个文件,「会写库」的证据必须是代码里真的在写,
不能是尺子里当判据用的那串字面量** —— 剥掉正则字面量与字符串字面量后仍留得下证据才算数。
(`method: 'POST'` 那一种证据本来就长在字符串里,所以拆成两问:代码型看剥字符串后的文本,
HTTP 型看只剥正则的文本。**宁可让第二层误红,不许让它误绿**。)

#### 造病验红四把(两层能被同一把刀分开 —— 判据四)

| 造的病 | ④ 清单≡现扫 | ④b 机制判据 | ④b2 排除面藏真货 | 点名 |
|---|---|---|---|---|
| A · 清单删掉一行(过期) | **红** | 绿 | 绿 | 第 57 行:现扫「migrate…」≠ 文件「ops-cos-delete」 |
| B′ · 把尺子本体从排除面拿掉 | **绿**(清单跟着重生成就一致了) | **红** | 绿 | `tools/guard-scan.mjs` |
| C · 往排除面里塞一个真写库脚本 | 绿 | 绿 | **红** | `tools/clean-test-tenants.mjs` |
| 还原 | 绿 | 绿 | 绿 | 14/14 |

**归因**:同一份数据喂旧判据 ①c(A 类都接了 requireTarget),三次造病下**旧的都是绿的** ——
新层确实在守新东西,不是数量凑巧对上。

#### 🔴 造病连两次没红,查到最后发现一个洞:扫描面对**未入册**的新文件是瞎的

底数是 `git ls-files`。新写的 `tools/guard-scan.mjs` 在 `git add` 之前,
零默认目标 / 必接护栏 / 含事务验回滚**三条判据一条都不管它** —— 刀不是没咬,是**没看见**。
归族 03g 那次「目录不递归 → 20 个脚本整体漏网」。
已落常驻第 ⑤ 条:**工作区不许躺着未入册的 `tools/**.mjs|sh` 与 `apps/api/**.mjs|sh`**。

### 病三 · 「A 类未接护栏 2」的差额 —— 见上,已解释进清单

---

## 二、拆档脚本「只拆了一半」—— 认,这是本批最重的一条

您说的对:**我的「储值/结算 0」是行数没变,不是归属对了。**

**表清单改由 schema 现取,不手写**:16 张表带 `user_id`;
其中**六张同时带 `booking_id`** = 这一次到店的从属记录 → **跟单走**;
只带 `user_id` 的八张 = 他在别店的资产 → **不动,但逐表打数报出来**;
`user_identities` 单列(见 §三)。

| 改了什么 | 怎么改的 |
|---|---|
| 演练输出 | 每条候选单**逐表打挂载数**(跟单 6 张 + 资产 8 张 + 身份 1 张) |
| `--apply` | **同一个事务**里把六张跟单表中 `booking_id=该单 AND user_id=旧档案` 的行一并指过去 |
| 收尾尺 | `bookings` + 六张跟单表**逐表** `x.tenant_id<>u.tenant_id` 必须 0(原来只量 bookings) |
| 形状判据 | users 只许 +新建数,**别的表行数一行不许动**(跟单是 UPDATE) |

### 带病彩排(自造样本,原库只读复制)

样本比您要求的多三张:**一张跨租户单 + 六张跟单表各 1 行 + 资产 1 行**,全部 `彩排_` 前缀自造。

| 路径 | 结果 |
|---|---|
| **新建档案** | dry-run 逐表报数、**一个字没写**(逐表行列零差异);apply 后 users **+1**、别表行数全 0、跟单 **6 行改指**;六张跟单表逐条核到新档案;**资产那行留在旧档案**(对);新档案名下储值 0 / 本单外结算 0 |
| **复用已有档案** | dry-run 报「指到已有档案」、零写入;apply 后 users **+0**、别表全 0、跟单 6 行改指;资产同样没跟着走 |
| **幂等** | 再跑一次:待处置 0、尺全 0、**逐表行列零差异** |

### 造病验红(您点名的那个)

`MIGRATE_SKIP_FOLLOW=service_notes` 把 `service_notes` 排除出「跟单走」:

```
迁移后 · 尺:… service_notes=1 …
逐表验尺:🔴 还有串味:service_notes=1        退出码 1
```

**归因**:同一份数据喂**旧尺子**(只量 bookings)= **0**,而 `service_notes` 实际还串着 1 行 ——
旧的绿、新的红,证明新尺子在看的正是它以前看不见的那一层。

### 🔴 造病还原时撞出第二个洞:落单的跟单行,按单迁移**修不回来**

造病跑过一次之后,单已经指到新档案、跟单行还留在旧档案上;
这时 `bookings` 那条尺是 0、待处置也是 0,**脚本自己修不了**(还原重跑照样红)。
补了第二遍**按行修**,窄口径只认证据确凿的那一种:
这一行自己串味,**而它那张单的顾客恰好就在这一行的租户里**。
⚠️ 没写成「跟单行的 user 必须等于单的 user」—— 那条不成立:带朋友来时一张单上多张结算单、各是各的人
(本机库现测这种情形 0 条,但设计上合法)。还原后回绿、退出码 0。

---

## 三、🔴 D130 身份串味 —— 现修(代码 + 存量 + 刀 + 造病)

**病与危害与您查的一模一样**,我把危害路径**真跑了一遍**当判据(不是纸上推演)。

### 修了什么

1. **回填四条带上 `tenant_id`**(跟着这个人自己的 `users.tenant_id` 走);
   `upsertUserIdentity` 逻辑**一个字没改**(您裁的)。
   顺手把这一域搬出巨型文件 → `apps/api/user-identity.mjs`(100 行,公约②边改边拆)。
2. **静态刀扩到 `user_identities`**(`test-tenant-explicit` ①d/①e,金丝雀正反各一);
   另加**第二层**:`INSERT … SELECT` 的**列名与 SELECT 两边都要有 tenant_id`** ——
   列名写了、SELECT 忘了照样落默认值,而 ①d 只看得见列名那一半。
3. **存量修复** `tools/repair-identity-tenant.mjs`(无默认目标 · 默认演练 · 先备份 · 一个事务 · 前后打数 · 幂等)。
4. **行为判据**新套件 `test-identity-tenant`(8 条),跑在库的**临时副本**上,跑完删。

### 存量修复结果(【自报】两个目标库的绝对路径见 §六)

| 库 | 修前 | 处置 | 修后 | 两把尺 |
|---|---|---|---|---|
| **沙箱库** | 374 行 / 错标 **113** | 改标 55 · 删 58 | 316 行 | 错标 0 · 唯一键重复 0 |
| **本机库** | 220 行 / 错标 **96** | 改标 83 · 删 13 | 207 行 | 错标 0 · 唯一键重复 0 |

本机库错标分布与您查的**逐项一致**:`demo-ai|email 24` · `demo-ai|phone 24` ·
`jics-nail|phone 16` · `jics-nail|wechat_miniprogram 31` · `hoptest-demo2|wechat_miniprogram 1`。
幂等重跑:报 0 条、库一分不动。

### 🔴 修复脚本自己有过一个判据错误,被沙箱真数据当场咬出来

第一版按**扫描时**算好的「目标租户下已有正确行」判改标还是删 ——
同一批里前一行改完,后一行的目标键就被占了,而它手里那个数还是旧的 → **撞唯一键**。
这就是您 08-25 立的**幂等判据律**的同一句话:*会被别的正常操作改小的量,不能拿来当判据。*
改法两条:①排程与真跑走**同一个出口** `planIdentityRepair`;②真跑时**每一步再现查一次**,
排程与现查不一致就抛错 —— 不许悄悄按模拟走。
改完预计与实测严丝合缝(沙箱 55/58、本机 83/13)。

### 行为判据八条(其中④是危害路径复现)

| # | 判据 |
|---|---|
| ① | 回填四条**列名与 SELECT 两边**都有 tenant_id |
| ② | 零命中先证刀能咬:历史坏版必须被 ① 咬中 |
| ③ | **两店同号各归各**:A 行 lucky-luxe / B 行 jics-nail,user_id 各归各 |
| ③b | **逐字段对照**:A 店那次 upsert 之后,B 店那行一个字段都不许变 |
| ④ | **危害路径复现**:把 B 那行改成错标 → A 再绑同号 → B 那位的身份**被改指给 A** |
| ⑤ | 存量修复两把尺归零 |
| ⑥ | 清空身份表跑现版回填 → 错标 **0** |
| ⑥b | 反向守:同一份数据喂**历史坏版** → 错标 **60**(一把怎么跑都 0 的尺子证明不了什么) |

④ 有个细节值得记:**先删 A 自己那行、再把 B 改成错标** —— 唯一键是
`(tenant_id, provider, provider_user_id)`,A 的正确行还在时根本改不成错标。
**而这正是真实病灶的形状**:回填一人一行,旗舰店那位当时还没有行,别店那位的行先被落成了 lucky-luxe。

### 造病验红(两把,各自还原回绿)

| 造的病 | 静态 | 行为 | 归因 |
|---|---|---|---|
| D′ · 回填四条去掉 tenant_id | ①红(点名 `user-identity.mjs:22/25/28/31`)· ①d 红 | **⑥ 红**(现版错标 113,不是 0) | ⑥b 照样绿(它测的是历史坏版,与产品码无关) |
| E · upsert 改回用 `currentTenantId()` | 绿 | **③/③b 红** —— B 店那行被改指 | 同上 |

> 造病做法说明:这三把是**文件扫描 + 模块直驱**的刀,每次 `node` 都是新进程,
> 不存在「热改了事」的问题;涉及服务的那一类仍按重启走。

### 🔴 现测撞出第三个洞:我新刀里的**金丝雀被另一把刀数成了违规**

`test-identity-tenant` 里那两条**故意不写 tenant_id 的历史坏版**,
被 `test-tenant-explicit` 当成产品违规咬了。**判据的金丝雀是已知阳性,不是产品代码。**
排除面已接到共用出口 `isKnife` 上(同一份,不许各刀各写一句)。
副作用是扫描面从 200 收到 **110 个产品源文件**,③ 反向守的门槛随之从 180 降到 100 ——
**这是面的定义变了,不是面缩水了**,差额写在判据文案里,不闷声改数字。

---

## 四、🟡 D131 报数(report-only,**一处没修、一处没白名单**)

表清单**由刀从 schema 现取**(`pragma` 找带 DEFAULT 的 `tenant_id` 列),不手写。

**31 张表 · `INSERT` 共 82 处 · 漏写 `tenant_id` 共 9 处**(全部在产品码里,不在测试里):

| 表 | INSERT | 漏 | 位置 | 我的分类(供您裁,未动) |
|---|---|---|---|---|
| `quote_requests` | 3 | **2** | `local-server.mjs:4984` · `:14423` | 4984 是**真业务口**(顾客提报价);14423 是演示种子 |
| `admin_accounts` | 3 | 1 | `admin-auth.mjs:14` | 初始老板账号 |
| `bookings` | 5 | 1 | `local-server.mjs:14353` | 演示种子 |
| `reminder_tasks` | 2 | 1 | `local-server.mjs:4974` | **真业务口**(建提醒) |
| `services` | 5 | 1 | `local-server.mjs:1222` | 旗舰店初始种子 |
| `stores` | 2 | 1 | `local-server.mjs:1203` | 旗舰店初始种子 |
| `technicians` | 3 | 1 | `local-server.mjs:1207` | 旗舰店初始种子 |
| `wechat_conversations` | 4 | 1 | `local-server.mjs:14398` | 演示种子 |
| 其余 23 张 | — | **0** | | |

**加了一条棘轮 ≤ 9**(报数期只许降不许升)。这不是白名单 —— **一处都没放行**;
它只是「不许再变坏」那条线,等您看完数再裁逐处修还是分批。
另有反向守:表清单 ≥31 张、INSERT 底数 ≥60 处,清单被写死或扫描面缩水立刻红。

**上线硬门槛批那一行已按您裁的改成「31 张表」**(记在 `test-tenant-explicit` 的在案行里)。

---

## 五、生产只读报数(两项,**只读诊断,一个字没写**)

### A · 拆档:那一条跨租户单在 16 张表里的挂载数

```
生产带 user_id 的表 15 张(本机库 16 张;差的是 timecard_refunds —— 次卡退款表还没随部署上去)
生产表总数 79(本机库 87)
跨租户单 1 条 · 单属 jics-nail / 人属 lucky-luxe
  跟单走: booking_drafts=0 deposit_disposals=0 deposit_receipts=0 reminder_tasks=0 service_notes=0 settlements=0
  资产:   archive_bind_tokens=0 coupon_grants=0 deposit_retains=0 member_timecards=0
          points_transactions=0 quote_requests=0 stored_value_transactions=0 · user_identities=1
```

**结论**:生产那一条**只需要移 `bookings` 一行**,六张跟单表一行都没有。
唯一挂着的是 `user_identities=1`,归 D130;拆档脚本不碰身份表,
拆完新档案带着 openid,那位下次登小婕店时由 upsert 自然建 `(jics-nail, …)` 那一行
(`idx_users_tenant_openid` 是**按租户**唯一,两店各一份档案合法)。

### B · `user_identities` 分布

```
总 105 行 / 64 人 · 错标 51 行 · 唯一键重复 0
   lucky-luxe | demo-ai          | email                24
   lucky-luxe | demo-ai          | phone                24
   lucky-luxe | demo-jienail     | wechat_miniprogram    2
   lucky-luxe | demo-lucky-luxe  | wechat_miniprogram    1
带 DEFAULT 'lucky-luxe' 的表:31 张(与本机库一致)
```

**51 行全是演示租户**(demo-ai / demo-jienail / demo-lucky-luxe),**没有一行是真店**。
小婕店那位不在错标里 —— 因为她的档案本身就被吞进了旗舰店,身份行跟着档案走反而是「自洽」的;
**拆档之后它才会变成错标**,所以两件事要**同一批上生产**。

生产存量修复等您另批。上线批清单已加一行:**部署包含 D130 回填修正**。

---

## 六、回归 · 未动 · 棘轮

### 回归 **87/87**,断言 **2,854 → 2,873(+19)**,「本轮未跑」**0**

**增量对账(计数即证)—— 一分不差**:

| 套件 | 增量 |
|---|---|
| `identity-tenant`(新套件,D130) | **+8** |
| `db-target-guard`(④/④b/④b2/④b3/④c/⑤) | **+6** |
| `tenant-explicit`(①d/①e/D131 前置/棘轮/反向守) | **+5** |
| **合计** | **+19** ✔ |

覆盖面 **87/87 套**(0 套打不出 ok 行)。

> 两个数字的口径别混:**基线 2,873** 是 `test-assertion-baseline` 的尺子(不含它自己那 5 条);
> 文末自证块里的 **2,878** 是日志里 `^ok` 的**原样行数**(含基线套件自己的 5 条)。差额恒为 5。

### 🔴 中途一次红,值得记:16 条断言差点悄悄没跑

第二跑报「合计 +3」,而我加的是 19 条 —— **差额 16 立刻暴露**:
三把小程序刀(`mp-placeholder-size` 3 / `mp-overlap` 4 / `mp-home-sections` 9)
走了「automator 不可用 → 本轮未跑」那条路。两个原因:①微信开发者工具自动化端口 9420 没起;
②`miniprogram-automator` 的安装目录随上一次的临时目录被系统清了。
两样都补上后第三跑 87/87、+19 对齐。
**这正是《断言增量律》要防的事** —— 「全过」只说明没红,不说明跑过。
(顺带记一处环境坑:`npm` 缓存目录 `~/.npm` 属主是 root,普通装会失败;
我用了独立 `--cache` 目录绕开,**没有动系统权限**。要根治得您本人跑那句 `sudo chown`。)

### 巨型文件棘轮(只许降)

| 文件 | 上批 | 本批 | |
|---|---|---|---|
| `apps/api/local-server.mjs` | 18,242 | **18,215** | ↓27(身份域搬去 `user-identity.mjs`) |
| `apps/web/admin.js` | 8,550 | **8,550** | 持平 |

### 「未动须有证」· 两库逐表行列对照(批前快照 → 批后现测)

**【写库自报】本批写过的两个目标(绝对路径)**:
- 沙箱库 `…/apps/api/sandbox-data/lucky-luxe.sqlite`
- 本机库 `…/apps/api/local-data/lucky-luxe.sqlite`
- **生产库:未动**(两次只读诊断)

| 库 | 有差异的表 | 来源 |
|---|---|---|
| **本机库** | `user_identities` 行 220→**234**(+14) · 列持平 | D130 修复 −13、改标 83 → 207;随后 4128 起服跑**修好的回填**新建 **27** 行(24 demo-basic + 3 lucky-luxe,`identity-bf-` 今日)。207+27=234 ✔ |
| | `notify_scan_marks` 行 20→24(+4) | 起服后按租户写今日日扫标记(产品正常路径) |
| **沙箱库** | `user_identities` 行 374→**328**(−46) | 修复 −58、改标 55 → 316;起服回填新建 12 → 328 ✔ |
| | `notify_scan_marks` +32 · `notification_logs` +6 · `reminder_tasks` +10 | 沙箱租户 45 家,起服后当日首次日扫(产品正常路径) |

**两库错标现测:本机库 0 · 沙箱库 0**;唯一键重复:两库均 0。
备份两份已落盘(`…lucky-luxe.sqlite.pre-d130-2026-09-03T07-*`),没删。

### 顺带答您在附录里点名的一行:`notify_scan_marks +1` 是谁写的

**是您 4128 的调度器,不是回归。** 那一行是
`jics-nail | 2026-09-03 | 2026-09-02T16:00:29.149Z` ——
小婕店在上海时区,多伦多时间 09-02 中午那一刻上海跨到了 09-03,
`notify-scheduler` 按**每租户每本地日**写一行日扫标记(`local-server` 之外的模块,行 362–364)。
所以它每天每租户会 +1,属产品正常路径。

---

## 七、自评 L1–L5 与残留

| 条 | 自评 |
|---|---|
| **L1 末端验证** | ⚠️ 本批全是后端/脚本/判据,**没有界面层**;末端在「库里的行」和「刀的输出」,都给了原样输出 |
| **L2 同类扫尽** | ✔ 三处按机制定义扫:①「刀咬自己」→ 排除面收进 `isKnife` 唯一出口 ②「表清单手写」→ 三处都改 schema 现取 ③「预计与实做两处算法」→ 收进 `planIdentityRepair` |
| **L3 无图不结案** | n/a(无界面) |
| **L4 反例数据** | ✔ 彩排样本**带病造**(六张跟单表各 1 行 + 资产 1 行);D130 行为判据用**两店同号**这个反例;沙箱真数据当场咬出我修复脚本的判据错误 |
| **L5 重复稳定** | ✔ 两个写库脚本各跑幂等重跑;判据套件各跑 ≥3 次(造病/还原来回) |
| **判据五 断言增量** | ✔ +19 对齐,且**中途因为差 16 而红过一次**,见 §六 |
| **判据六 刀留痕** | ✔ 每把造病都有落刀凭据(点名内容原样贴出);没有「刀没红」而无凭据的情形 |

### 残留与不确定(照实写)

1. **D131 九处一处没修**(您裁的 report-only)。我按「真业务口 / 种子」分了类附在 §四,等您裁。
2. **生产存量修复未跑**(身份 51 行 + 拆档 1 单),等您另批;两件事**要同一批上生产**(理由见 §五 B)。
3. `timecard_refunds` 表生产上还没有(生产 79 表 / 本机库 87 表)—— 拆档脚本按 schema 现取,
   生产上自然少这一张,**不影响**(它是资产表,本来就不动)。
4. **未推 main**;不说可以关帐。
5. `~/.npm` 属主是 root 这件事我绕过去了,**没有根治** —— 根治要您本人跑那句 `sudo chown`。

---

## 八、§四 AI 复测的**不用密钥那部分前置,本轮已做完**(带一个要紧的发现)

### 做完的四件

| 件 | 现测证据 |
|---|---|
| `.gitignore` 加 `apps/api/.env.*` + `!apps/api/.env.example` | `git check-ignore -v apps/api/.env.ai-sandbox` → `.gitignore:18:apps/api/.env.*`;而 `apps/api/.env.example` **不被忽略**(白名单生效) |
| `test-credential-scan` 加 **env 钥匙行**金丝雀 | 新形态 `env钥匙行`;全仓现扫**新增命中 0 处**(不是靠放行换来的绿) |
| `start-sandbox.sh` 可选 `--ai-env <文件>` | 不传 = 现状一个字不变(mock,不花钱);传了会打印一行「⚠️ 真模型会**真花钱**」 |
| `AI_REQUIRE_REAL=true` 无 key 的**造病失败输出** | 见下 |

**造病 F**(env 钥匙行):真把一行 `AI_API_KEY=sk-…假值` 的文件 `git add` 进来 →
`not ok ① :: 1 处未登记:canary-env-04a.txt:2[env钥匙行]`。
**归因**:同一份数据喂**旧形态集**命中 **0 处** —— 旧的绿、新的红,层次分明。造完当场移除,回绿 4/4。

**造病 G**(`AI_REQUIRE_REAL=true` 且不给 key):

```
HTTP 500
{"error":{"code":"INTERNAL_ERROR","message":"AI_REQUIRE_REAL is true, but no real AI provider/API key is configured."}}
  at aiJson (apps/api/ai-utils.mjs:123)  at createCustomerServiceReply (ai-utils.mjs:464)  at handleWecomInbound (local-server.mjs:4161)
```

**响亮失败,没有悄悄回落 mock** ✔。还原(不带 `--ai-env`)后同一条消息回 201、`provider = mock`。

### 🔴 但造病 G 顺手撞出一件对 12 场景更要紧的事

我第一条探针打的是「做美甲多少钱?」—— **HTTP 201,9 毫秒就回了**,
`reply.source = quote_intake_template`。也就是说:**它压根没到模型那一层**,
`AI_REQUIRE_REAL` 自然没机会发作。换一句「你们店几点关门?周日营业吗?」才 500。

**规则/模板层会在模型之前答掉一部分消息**(`quote_intake_template` / `quote_intake_state` /
`quote_draft_guard` / `quote_waiting_state` … 现扫十余个 `source`)。
所以 12 场景的判据里**必须逐轮记录 `reply.source` 与 `reply.provider`** ——
否则会把「规则答的那几轮」当成「真模型答的」,而**这正是判据律要防的**:
判据必须能证伪你要证的那件事。这一条我已写进 §四 的执行口径,第二轮回执逐轮附上这两个字段。

### 只剩一步要您本人做

`apps/api/.env.ai-sandbox` 那五行的**值由您填**(`AI_PROVIDER` / `AI_BASE_URL` / `AI_MODEL` /
`AI_API_KEY` / `AI_REQUIRE_REAL=true`)。**我不复制、不打印、不写进任何文件**;
**`.gitignore` 规则已生效**(上表有 check-ignore 输出;文件本身由您建,现在不存在)。填好说一声,我就跑 12 场景。

> **措辞更正(04b §一.3)**:上一版这里写的是「文件已被 ignore」——不准确。
> 准确说法是:**`.gitignore` 的规则已生效,文件本身还不存在**(现查 `apps/api/.env.ai-sandbox` 无此文件,
> 这是对的,不该由我建)。`git check-ignore` 对不存在的路径也照样答,所以那句输出只证明**规则**,不证明**文件**。

<!-- 回归自证 批次=04a 套件=87 断言=2878 日志=handoff/regression-logs/回归日志_2026-09-03_04a_25f9249_87套件.log -->
