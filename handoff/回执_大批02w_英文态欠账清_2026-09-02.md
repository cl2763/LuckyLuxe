# 回执 · 大批 02w —— 英文态欠账清 + 外线扫干净 + 白名单补「什么时候要动」

## 一 · 英文态欠账:您判得对,那是**证据替换**

我用「接口层三租户对照」顶替您指定的「顾客端首页店卡英文真截图」——
而 `name_en` 这条链**恰恰是本批新加的**,渲染层能不能吃到,接口对照证明不了。
您查造景脚本查得也准:`seed-demo-twin.mjs` 里**根本没有 `name_en`**,所以那不是"空态规则在起作用",
是**镜像店的景没铺全**。

### 补法:两件事分开验,各用一家真店

| 验什么 | 用谁 | 结果(DOM 实读,不是我看图猜) |
|---|---|---|
| **本店英文态有名字** | 镜像店(补 `nameEn`) | `data.shopName="LUVIA (Demo)"` · **DOM 实读 `"LUVIA (Demo)"`** ✔ |
| **空态说真话** | `jics-nail`(她真没有英文名) | `name_en=null` → **DOM 实读 `"Jie's Nail 小婕"`**,不空白 ✔ · 零回落(未贴任何本店/别家名)✔ |

**造景补齐**(您说的 D108 同族):
- `seed-demo-twin.mjs` 镜像店加 `nameEn: 'LUVIA (Demo)'` 并透传给建店接口;
- **小婕演示店故意不给 `nameEn`** —— 她真的没有英文名,**这本身就是两态的天然夹具**,不是我造的假;
- 平台建店接口 `/platform/tenants` 补收 `nameEn` 写入 `stores.name_en`;
- 已存在的镜像店由迁移**条件补**(`name_en IS NULL OR ''` 才补,不覆盖商家改过的值 —— 您 02w 收编的那条律)。

**三店英文名现取**:
```
lucky-luxe       中文='LUVIA 半径'        英文='LUVIA'
demo-lucky-luxe  中文='LUVIA 半径(演示)'  英文='LUVIA (Demo)'
jics-nail        中文="Jie's Nail 小婕"    英文=None      ← 空态夹具
```

**真截图三张**(`handoff/attachments/`):
`改名后_顾客端首页店卡_中文_2026-09-02.png` · `..._英文_2026-09-02.png` · `改名后_空态验证_小婕店英文态_2026-09-02.png`

英文那张图上店卡显示 **LUVIA (Demo)**,顶栏平台品牌仍是「有迹 YOUJI」(您红线二:平台名不动),
右上语言键在 EN,`Switch shop ›` / `Map · Copy` / `All hours ›` / `Call` 全英文 —— 英文态是真进去了。

**⚠️ 顺带看见一处(只报不动)**:英文态下「人气美甲 / 人气美睫 / 技师作品」等区块标题仍是中文
(截图可见)。这是 **D112 中英混排**那一族,已在册、排在后面,本批不动。

## 二 · 外线扫干净 —— 又比您列的多一片

您列了 8 个目录,我现扫**全仓库根**发现同族还有 **15 个顶层文件**(`.zip` / `.mp4` / `.png`):
`LuckyLuxe-Remotion-concept-v1/v2.zip`(89M+93M)、`LuckyLuxe-room-tour-concept-v1/v2.mp4`(63M×2)、
`LuckyLuxe-设计交付包*.zip` 五个、`LuckyNail-127sqm-*.zip` 三个、两张 poster.png、两个 ClaudeDesign zip。

**一并搬走,共 28 项 / 872 M**,`mv` 到 `~/Desktop/有迹_外线归档_2026-09-02/`,原样不改名、未用 `rm`。
两边 `ls` 回显自证;`git status` 空(本就不在库)。

**两个我没搬,报您**:
- **`tmp/`(47M)**:里面是 `auth-debug-preview.png` / `annotated-ui` 这类**有迹自己的调试产物**,不是外线;
- **`output/`(145M)**:我先查了引用再搬 —— 里面确是 `LuckyLuxe-V5-final` / `效果图交付.zip` 等装修效果图,**已搬**。

### 仓库根最终清单(**零「待判」**)

| 归属 | 条目 |
|---|---|
| 有迹·主体 | `apps` `miniprogram` `docs` `handoff` `tools` `agents` `deliverables` |
| 有迹·配置/说明 | `CLAUDE.md` `README.md` 四个 `.command` `project.config.json` `nixpacks.toml` `railpack.json` `railway.json` |
| 有迹·交付证据(已入库) | `核验截图_2026-08-20/21/22/23` |
| 有迹·本机调试产物(未入库) | `tmp/` |
| 本机工具私有配置 | `project.private.config.json`(微信开发者工具) |

**仓库体量 2.8 G → 2.0 G。** 这一族三次出现(LUVIA 23 个 → 两条外线 → 这 28 项),**现已一次扫干净**。

## 三 · 白名单补「什么时候要动」(裁定三)

四条不动的都补上了触发条件,不再只写"为什么不动":

| 条目 | 什么时候要动 |
|---|---|
| `merchant-forgot` 的 `LuckyLuxe-Support` | **真实外部标识**(平台客服微信号),非店名字面量。**更换平台客服微信号时,这里同步改** |
| `supabase/001_init.sql` | 一旦这套 SQL 被复活(重新建库/迁回 supabase),须同步改成新店名 |
| `prisma/seed.ts` | 一旦 prisma 这条链被复活,须同步改成新店名 |
| `src/main.ts` | 这套 Nest 入口被复活时;它只打给开发者看 |

## 四 · 您那半我照收,并给出我这边的口径

您说底数 43 文件/98 处是两把尺量的(文件数含无空格、处数只含带空格)。**我这边也照您的新律办**:
本批所有底数都写明尺子。店名刀的尺子写死在判据注释里:
```
形状正则  /(Lucky\s*Luxe|LUVIA\s*半径|LUVIA(?!\w))/
扫描面    apps/api · apps/web · apps/wecom-gateway · miniprogram · tools
          扩展名 .mjs .js .ts .json .html .wxml .wxss .sql .sh;排除 node_modules/regression-logs/三个库目录
现测      534 个文件 · 82 处命中 · 白名单 32 条(按文件+处数,不按行号)
```

## 五 · 回报固定六项

1. **改了什么**:建店接口收 `nameEn`(**原地改行不加行**,棘轮卡在 18243 上)· 造景补镜像店英文名 ·
   迁移条件补 `name_en` · 白名单四条补触发条件 · 28 项外线搬走。
2. **在哪一层验的**:小程序**真页 DOM 实读**(不是看图猜)+ 接口层三租户 + 库层。
3. **同类扫尽**:外线同族从"8 个目录"扫成"28 项",仓库根现零「待判」;白名单里"不动的东西"全部补了触发条件。
4. **用什么数据验的**:两家真店的天然两态(镜像店有英文名 / 小婕店没有),不是我造的假空值。
5. **重复稳定性**:截图脚本连跑 3 次(中文/英文/小婕空态),DOM 实读一致。
6. **残留与不确定**:
   - 英文态区块标题仍中文(D112 同族),**已在册,本批不动**。
   - `tmp/`(47M)是有迹自己的调试产物,**未搬,待您裁要不要清**。
   - 中文那张截图取的是镜像演示店(小程序默认连的就是它);**真店 `lucky-luxe` 的店卡截图**需要切租户,
     本批未拍 —— 若您要,下批补。

**自评 L1–L5**:L1 末端(真页 DOM 实读 + 截图)· L2 同类扫尽(外线 8→28)· L3 有书面标准 ·
L4 反例数据(小婕店真空态)· L5 重复稳定(3 次一致)。**全过。**

---

**棘轮**:`local-server.mjs` 18,243/18,243(**本批加功能仍零上涨** —— 原地改行不加行)·
`admin.js` 8,552/8,552 · `customer.js` 2,811 · `me` 638 · `settlement` 929 · `orders` 829(全持平)。
新增:日志 41→42 · 自证块 5→6 · 店名白名单 32 条/82 处。

**断言**:基线口径 2,763 → **2,763(+0**,本批无新判据,只补证据与理由);全日志纯 `^ok` **2,768**。
**全量回归**:✅ **73/73 套件通过**。
**生产库未动 · 本机库未动 · 生产库不碰 · 未推 main**。

<!-- 回归自证 批次=02w 套件=73 断言=2768 日志=handoff/regression-logs/回归日志_2026-09-02w_73套件.log -->
