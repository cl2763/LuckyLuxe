# 小批 12j 补 —— 第二批 44 张样片到位(店主说「你说的这些元素我都有」);归一化编号 45 起;标注优先难类

店主 09-24:又给了 44 张,放在桌面夹 `AI修图样片/`。授权同上(可当 D222 训练/验收集;真客照禁令不变)。¥0,不送模型 B。

## §一 入库(¥0)
1. 清单已由我按文件名与《样片来源清单》去重出来:`31o_修图验证_2026-09-22/_第二批44张_文件清单_2026-09-24.txt` —— 新增 44 个文件 + 10 个文件名带「 2」的疑似第一批重复。**疑似重复按像素哈希与 `_input/01–41` 比对**,重复不入集,不重复补编号;比对结果写进清单。
2. 归一化**同一工具同参数**(HEIC→JPG、EXIF 摆正、顶部裁脸规则、fitSize 到 1780×2596 送模型规),进 `_input/` 编号 **45 起**;《样片来源清单》续写第二批,88 张(或 88+)总表一份。
3. 每张跑 `metrics.mjs` 四数 + 12i §二 的甲色三档机器判,合并进 41→88 张分档表;**三档各有多少张先报**,12i 的 15 张扩样从 88 张里挑(优先第二批,因为她说难类都在这批)。
4. 有脸的按老规矩裁;裁不掉的(脸在手后面)标「不入集」并报。

## §二 标注顺序改(12j §一-3)
第二批里的红甲、裸甲、纯黑甲、暗光、立体钻优先标;目标:D222 三档测试集各 ≥ 20 张(12h §三 判据)先凑齐。每 10 张报耗时不变。

## §三 判据
- `_input/` 张数 = 41 + 3 + 新增数(去重后);来源清单行数与之相等;分档表行数相等。
- 无人脸:归一化后每张顶部 400 px 抽 4 张给我看(J-112:验一次不算,批量抽验)。
- ¥0;`apps/` 0。
