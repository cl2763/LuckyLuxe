# 小批 07d 回执 —— 卡住回归的是我 07c 装的那道闸

> 2026-09-12 · 上游 `4b3fd83`(07c)· **未推 main** · **生产库未动 · 本机库未动**
> 密钥一个字没进本文件

---

## 〇、先认一条:红的不是「两台库名」,是**我 07c 加的那道 `--strict`**

你贴的那一段里,**「两台库名分得清」其实是 ✅**(`4128=(取不到) · 4310=sandbox`,两个值不同)。
我现跑一遍,`❌ 预检红` 出自**我 07c 装的 `ext-deps-check --strict`** ——
缺 `MP_AUTOMATOR` 就把整轮回归拦住。

**这一条 07c 我没报。你的裁定方向完全对**:一件环境没配好的事,不该挡住一百多套跟它无关的判据。
已改成 ⚠️ **提醒不拦**,理由写在码上:**缺 automator 时三支 mp 刀自己就会红** ——
覆盖面一点没少,判决只是往后挪几分钟;这道闸的价值是「**第 5 秒点名缺哪一个**」,
不是再判一次同样的红。

> 代码落在 `c8191f7`(§一/§二/§三 一并;当时 `git add -A` 把三段一起带进去了,提交信息里只写了 §一 与棘轮归因,
> §二/§三 的逐条在本回执 §三/§四)。

---

## 一、§一② 预检分三态(裁 #58)

| 态 | 判法 | 结论 | 现测 |
|---|---|---|---|
| ① 两台都在、库域分得清 | 两个值都取得到且不同 | ✅ | `4128=local · 4310=sandbox` |
| ② 有一台**没起来** | `/health` 取不到 | ⚠️ **提醒,不拦** + 打印「怎么办」 | 退出码 **0** |
| ③ 起来了但**撞名** | 两台报同一个名字 | 🔴 | 退出码 **1**,报 `4501=sandbox · 4502=sandbox` |

②档现在长这样:

```
  有服务没起来   ⚠️ 提醒(不拦):4310 没起来;全量跑在 ci 临时库上,**不依赖这两台**
     如果没起来的那台是 4128,多半是还没配顾客令牌签名密钥(J-53)——
        怎么办(这把钥匙由店主亲手创建与灌入,Code 不查看、不打印、不拷贝):
          1) 生成一把:  node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
          2) 写进 apps/api/.env(已 gitignore),一行:
               WECHAT_MINI_TOKEN_SECRET=<上一步那串>
          3) 重新启动。
        ⚠️ 不要设成跟 OWNER_TOKEN 一样 —— 密钥不复用(J-53 口径③),设一样照样拒绝启动。
        ⚠️ ci / sandbox 两个库域不受此限(用明写的开发值),所以回归与沙箱照常跑。
```

那段话由 `mini-token-secret.mjs --howto` 出,**预检不另抄一份说明书**(一件事一处真相)。

**顺手治了一件:端口原来是写死的 4128/4310 —— 那样③档根本没法造病**
(第一刀就落不下去,因为 4128 上是你那台真服务)。改成 `PRE_LOCAL_PORT`/`PRE_SAND_PORT` 可覆盖,
日常一个字不用改,造病时把两台起在 4501/4502 就行。

---

## 二、§一③ 那段说明书本身上判据 · §一④ 启动参数收成一处

### ③ 说明书上判据(`credential-scan ④i/④j/④k`)

| 条 | 验什么 | 造病 | 结果 |
|---|---|---|---|
| ④i | 里面那行 `node -e "…"` **真跑一遍**,要吐出 ≥32 字符 | `randomBytes`→`randomBytez` | 🔴 报「out=0 字 Command failed」 |
| ④j | 它叫人写进的路径**真被 gitignore 盖住** | 改成 `apps/api/keys.txt` | 🔴 报「路径=apps/api/keys.txt git check-ignore=false」 |
| ④k | 反向守:两条验的是 `refusalText()` **现出的那段**,不是判据里抄的副本 | — | ✅ |

### ④ 启动参数收成一处(新件 `tools/start-local.sh`)

| | `启动服务器.command` | 旧 `restore_local` |
|---|---|---|
| `--env-file-if-exists=apps/api/.env` | ✅ | ❌ ← J-53 之后这一条要命 |
| `ALLOW_DEMO_ADMIN_LOGIN=true` | ✅ | ❌ |
| `HOST=0.0.0.0` | ✅ | ❌ ← 没有它手机连不上 |
| `--watch` | ✅ | ❌ |

两边**现在都调 `tools/start-local.sh`**;拉不回来时把 `/tmp/ll-local-restored.log` 末 10 行打到屏幕上。
判据 `④l`(两边都调、都不再自己拼)/`④m`(那一处四样参数一样不少)/`④n`(失败要 tail 日志)。
**造病**:拿掉 `--env-file-if-exists` ⇒ ④m 红并点名,**且行为层真起不来**(日志里就是那段说明书);还原后 4128 又起来了。

### 🔴 收敛过程中我自己栽了两跤,都留在码上

1. **丢了 `env_clean`** ⇒ 回归专用环境变量漏进活服务,收尾自证当场红:
   `🔴 [收尾自证] 4128 合并窗 0s / 封顶 12s ✖ 默认应为 4s / 12s`。
   改法:把 `REGRESSION_ONLY_ENV` 那张清单**收进 `start-local.sh`** —— 任何调用方都拿到一台干净的服务。
2. **`--bg` 那支把赋值放在 `env -u` 之前**,被 `-u` 抹掉 ⇒ 那台 `demoLogin=false`,而前台那支是 `true` ——
   **收敛完又分叉了,而且分叉藏在参数顺序里,肉眼不易见**。
   立判据 `④m2` 验两支**逐字同序**,造病(把赋值挪回 `-u` 前)⇒ 红。

> 这两跤都是「**收敛本身会引入新的分叉**」—— 收一处真相的时候,得把那一处的**全部**行为都带过去。

---

## 三、§二 J-54 落地:接令牌**锚语义,不锚色值相同**(裁 #59)

### ② 三处借色 —— 每处先写语义,再挑令牌,再现量两档

| 处 | **它表达的是什么** | 落的令牌 | 浅 | 深 |
|---|---|---|---|---|
| `merchant/orders/index.wxss` 护理块「激活」 | **进行中 / 选中** | `--good / --goodbg` | **4.68** | **7.79** |
| 同文件 售后单蓝徽标 | **提示 / 信息**(不是「新客」那个身份) | `--tagw / --tagwbg` | **7.20** | **7.05** |
| `apps/web/admin.js:2157` 排班「上班中」 | **在岗 / 正常** | `--good`(压 `--paper`) | **5.05** | **9.66** |

**现量顺手咬出两件真的**(不是「换令牌更好看」那种):

- 护理块激活现状 `#3f6b52` 压 `#cfe0d6` = **4.45** —— **差 0.05 不到 AA**;
- 排班五个状态色**全是写死 hex 压在随档变的 `--paper` 上**:

| 状态 | 浅档 | 深档 | 判 |
|---|---|---|---|
| **working**(已改) | 5.76 | **2.96** | ⚠️ 乙档欠账 → 已换 `--good`(5.05 / 9.66) |
| overtime | 5.15 | **3.31** | ⚠️ 乙档欠账 · 语义=**超时/异常** → 建议 `--bad`(5.30 / 6.93) |
| done | **3.47** | 4.92 | ⚠️ 浅档欠账 · 语义=**已完成/收工** → 建议 `--flat`(5.57 / 6.68) |
| rest / none | **2.52** | 6.78 | ⚠️ 浅档欠账 · 语义=**休息/未上班** → 建议 `--flat`(同上) |

**我只改了你准的那一个(`working`)**,另外四个量了没动 —— 等你拍语义(J-54:先写语义再挑令牌)。

**`--acc` 我量过也报了,但没用它**:你建议的是「`--acc`(强调)或 `--good`,现量取过 AA 的那个」。
两个都过 AA(`--acc/--goodbg` 5.36 甚至更高),**但 `--acc` 是青绿、`--goodbg` 是浅绿,色相不配**,
而且仓里**没有 `--accbg` 与它成对**。所以取 `--good`。这条你可以推翻。

⚠️ **一条教训**:注释里**不许写色值**。我头一版把解释用的 hex 写进 wxss 注释,
那把尺子**连注释一起数**,棘轮当场从 2353 涨到 **2354**。

### ① 渐变卡三张的 J-42 现查(**只出结论,一个字没改**)

换档发生在 `tokens.wxss`(令牌本身随 `@media` 变),页面 wxss 只用 `var(--…)`;
而这一页 `prefers-color-scheme` 块 **0 个**、`data-theme` 选择器 **0 个**。逐条解析两档:

```
  .tcard.a    浅 background:var(--brand);color:var(--hero)   深 background:#d3b483;color:#15130f   ✅ 随档走
  .tcard.b    浅/深 linear-gradient(135deg,#3f6b52,#5d8a70)  逐字节相同                            🔴 漏深色档
  .tcard.n    浅/深 linear-gradient(135deg,#3b6ea5,#6b95c4)  逐字节相同                            🔴 漏深色档
  .tcard.s    浅/深 linear-gradient(135deg,#8a5a52,#a5786f)  逐字节相同                            🔴 漏深色档
```

**结论:两档同值 ⇒ 跟品类色同一个病 ⇒ 归「小程序 wxss 令牌化欠账」**(配色批已撤销),不是配色决策。
**顺带一件值得记**:同一行四张卡,**A 档已经随档走、B/N/S 不走** —— 深色下一屏里两种行为。

---

## 四、§三 `OWNER_TOKEN` 两层闸(裁 #60)· **闸写好了,测试还没切**

你准许停在这一步,我就停在这一步,照实报。

### 闸的形状:**复用了**,而且是真复用不是照抄

新件 `apps/api/secret-gate.mjs`(74 行)—— 三件套工厂:`resolve()` 不抛 / `require()` 决定怎么死 /
`refusalText()` 给说明书。**`mini-token-secret.mjs` 已经改成用它**(不是并排再写一套),
新件 `apps/api/owner-token.mjs`(69 行)也用它。
判据 `④r` 现验:**两把钥匙的闸都来自同一个工厂**,且随机那一支走的是 `randomBytes` 不是固定字面量。

### 两层

| 层 | 库域 | 行为 | 判据 | 现测 |
|---|---|---|---|---|
| 一 | `local` / `production` / `unknown` | 没显式设 ⇒ **拒绝启动**,点名缺 `OWNER_TOKEN` | ④o | ✅ |
| 二 | `ci` / `sandbox` | **每轮随机生成**,落到那一轮 `DATA_DIR/.owner-token` | ④p | ✅ 同一 DATA_DIR 两次同一把;换 DATA_DIR 换一把 |

`readOwnerToken()` 是给 90+ 个测试接的那个共用口(显式环境变量优先,否则读 `DATA_DIR/.owner-token`),判据 `④q` 已验。
**造病**:把 `devValue` 改回固定字面量 ⇒ ④p/④q/④r **三条红**;还原回绿。
拒绝启动那段话现测**不含 token 本身**。

### 🔴 **没接进 `local-server.mjs`,90+ 个测试也没切** —— 这是刻意停的

一接上去,全仓拿 `'owner-demo-token'` 当 Bearer 的测试当场全红,
那一步要**连同「测试统一改接 `readOwnerToken()`」一起做**,不能半截上线。
`credential-scan` 白名单里那条豁免已经改写成「闸已写好、还没接上」,**豁免有效期 = 到测试切完为止**。

---

## 五、这一批全量跑了**五遍**,前四遍各咬出一处

| 第几遍 | 停在哪 | 咬出什么 |
|---|---|---|
| 1 | **预检** | 「对比度全扫过期了」—— 我改了 `admin.js` 与两支 wxss,界面指纹变了。**红得对**,去重跑了全扫 |
| 2 | `test-store-name` | 我把那句 banner 从 `.command` 搬进新脚本,**店名跟着搬**而白名单没跟。改法不是加白名单条目(它只减不增),是**把品牌从 banner 里去掉** —— 本机开发横幅不需要店名 |
| 3 | 收尾自证 | **回归专用环境变量漏进活服务**(见 §二④ 第 1 跤) |
| 4 | 自查 | `--bg` 与前台**参数顺序不一致**(第 2 跤) |
| 5 | — | **112/112 全过,两台都自己回来** |

**对比度全扫重跑结果**:文字节点 4,894 · 渐变面上判不了的 717(如实报)·
**甲档 0 · 乙档 2**(与上一份一样 —— 我这一批的改动没引入新的)。
新红榜 `handoff/night-runs/对比度红榜_07d后_后台_2026-09-12.md`。

---

## 六、六项结构 + 数

**① 改了什么** 见上四节。**② 在哪一层验的**:静态 23 条 + **行为层真起进程/真拉服务** + **像素层重跑全扫**。
**③ 同类扫尽**:密钥常量 179 个模块全扫(具名 13)· 裸 exit(0) 178 把刀全扫 · 启动参数两支逐字比 ·
品类三 hex 全仓点清(只剩三张渐变卡,已登记)。**④ 用什么数据验的**:真文件、真 git、真进程、真渲染、真起停。
**⑤ 重复稳定性**:全量五遍;每把造病刀红→还原→绿各一轮。**⑥ 残留与不确定** 见 §七。

**回归**:**112/112 全过** · 整跑 **2 分 44 秒** · **断言 3,906 → 3,917(+11)**(全在 `credential-scan` 12→23)·
覆盖面 112/112 · 零断言套件 0 · **收尾自证 4128 与 4310 都 ✔**。

**棘轮(J-43 逐个重量 · J-50 归因)—— 没有一个变大**:

| 棘轮 | 上批 | 本批 | 变化与归因 |
|---|---|---|---|
| 小程序 wxss 写死色 | 2,353 | **2,350** | ↓ −3 · **归因:真修好了** —— 护理块两个色值 + 售后徽标一个,差额对得上 |
| `local-server.mjs` | 17,707 | **17,707** | 未变 |
| `admin.js` | 7,928 | **7,928** | 未变(hex 换成 `var(--good)`,行数不动) |
| `customer.js` / `party.js` / styles.css 写死色 / UTC 判天 | — | 2,799 / 31 / 31 / 12 | 全未变(逐个用判据自己那条命令重量过) |
| 三个旧金 / 丙筐 / 未动无标记存量 | — | 183 / 16 / 34 | 未变 |

**写库自报**

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **生产库未动** | 这一批零次 `railway` 调用 |
| **本机库** | **本机库未动**(逐表行数) | 开工前/收尾后各一次:**92 张表,有变化 0 张** |
| **沙箱库** | 有动,全是回归夹具与心跳 | 五遍回归,每遍自造自收 |

> **未动对照:handoff/night-runs/07d_本机库未动对照_2026-09-12.md**

**服务**:4128 ✅(`miniSecretSet=true` —— 你那把钥匙已经贴上了)· 4310 ✅。

---

## 七、残留与不确定(照实列)

1. **`OWNER_TOKEN` 闸没接上、90+ 个测试没切**(§四)—— 你准许停在这一步。
2. **排班另外四个状态色**(overtime/done/rest/none)量了没改,等你拍语义(§三)。
3. **渐变卡三张**只出结论没改,已归小程序 wxss 令牌化欠账(§三①)。
4. **待裁 #35** 仍在令牌化欠账里,本批没动。
5. `users`/`services` **49 处租户条件**、夜9 §段5 那**两处「靠自觉撑着」的回落**,都仍未动。
6. 本机库仍只比**逐表行数**,没留逐行指纹基线。

---

## 八、自评 L1–L5 + 停线自查

| | 做到没有 |
|---|---|
| L1 末端验证 | ✅ 配色重跑**像素层全扫**;启动收敛**真起真停看 /health** |
| L2 同类扫尽 | ✅ 五类各有全仓扫;其中两处是判据/自证先咬出来的 |
| L3 无图不结案 | ⚠️ 本批没出新截图 —— 配色那三处的证据是**重跑的全扫红榜**(比截图更末端),渐变卡是**机械解析**不是像素 |
| L4 反例数据 | ✅ 每条新判据先造真会发生的假违规(④i/④j/④m/④m2/④r 各一刀) |
| L5 重复稳定 | ✅ 全量五遍;三态各现测;造病刀红→绿各一轮 |

**停线自查**:未推 main ✅(领先 284)· 未碰生产 ✅ · **4128 既有行未写** ✅(逐表零差异)·
**未看密钥文件** ✅(探 `.env` 只看「起不起得来」,一个字没读)· 无 sleep 轮询 ✅ ·
**没松判据、没抬棘轮** ✅ —— ⚠️ 但有一条要说清:**我把 `ext-deps-check` 从「拦」改成了「提醒」**,
那是**按裁 #58② 的口径**(它拦的那件事,三支 mp 刀自己会红,覆盖面一点没少),不是松判据;
**没改合同图** ✅ · **密钥零字进本文件** ✅。

**没照字面做的**:
1. §二② 护理块那处你写的是「`--acc`(强调)或 `--good`,现量取过 AA 的那个」——
   两个都过 AA,**我取了 `--good`**,理由是色相与没有 `--accbg` 成对(§三)。这一条你可以推翻。
2. §三 `OWNER_TOKEN` **停在「闸写好、测试没切」** —— 这是你明写准许的那一步。
