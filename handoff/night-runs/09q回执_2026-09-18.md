# 09q 回执 · 🔴 **甲 成立,而且比甲更干净** · 两栏清单量出来了(**两栏真的不一样**)· 裁#90 改法报批

**上游令点名(J-81)**:09i · 09j · 09k · 09l · 09m · 09n · 09o · 09p ·
**09q `181043e622fa639e9fb3098757a1d45d`** —— **九份桌面/仓里 md5 全一致。**

🔴 **一个 push 都没执行**(`origin/main` 仍是 `285b20d`)

---

# 一 · 🔴🔴 §三 mtime 那件事:**(甲) 成立,而且根因比你给的两个选项都更干净**

## 一次 `ls -l` 定了,但它带出来一个更好的解释

```
/app/apps/api/local-data/
  lucky-luxe.sqlite          5,980,160 字节   2026-08-27 04:56:40 UTC
  🔴 没有 lucky-luxe.sqlite-wal
  🔴 没有 lucky-luxe.sqlite-shm
backups/lucky-luxe-2026-09-18.sqlite  5,980,160 字节   2026-09-18 04:55:11 UTC
容器当前时间 2026-09-18 13:35 UTC
```

**你给的判别式是「`-wal` 很小 → 甲 / 很大 → 乙」。现测是第三种:`-wal` 根本不存在。**
所以我又追了一层(都是只读):

```
PID 1 打开着的库文件:  fd 17 -> /app/apps/api/local-data/lucky-luxe.sqlite   ← 服务确实在用这个文件
库自己记的 journal_mode: delete        ← 🔴 生产库根本不是 WAL
page_count 1460 × page_size 4096 = 5,980,160 字节  ← 与文件大小完全对上
```

## 🔴 为什么不是 WAL —— **不是「设 WAL 失败了」,是那段代码还没上线**

```
apps/api/db-concurrency.mjs(WAL + busy_timeout)  进仓于 0b3675d · 2026-09-05
线上跑的是                                        285b20d      · 2026-08-26
现证:git show 285b20d:apps/api/db-concurrency.mjs  → 文件不存在
现证:285b20d 的 local-server.mjs 里 applyConcurrencyPragmas 出现 0 次
```

**⇒ `delete` 是 SQLite 的默认值,没有任何东西悄悄失败。**

## 结论:**(甲) 成立**,而且比 (甲) 更强

| 你的 (甲) | 现测 |
|---|---|
| 「这 22 天没有新的业务写入」 | ✅ 成立 —— 主库 mtime 停在 8/27,`page_count` 与文件大小严丝合缝 |
| 「日备对得上是因为根本没新东西要漏」 | ✅ 成立 |
| 「一旦有真顾客开始用,cp 就会开始漏」 | 🟡 **要改一个字**:**现在不会漏,因为不是 WAL**;`delete` 模式下 cp 的风险是「拷到事务中间的撕裂状态」,不是「漏掉 WAL」 |

**⇒ 给店主的话,按 (甲) 那一版说:**
> **「你线上这 22 天没有新的经营数据,所以推坏了受影响的人真的是零 —— 这句话以前是推理,现在有数了。」**

🔴 **但这件事有个尾巴,而且它要进那张加法清单** —— 见 §二。

---

# 二 · §二.2 · 两栏加法清单 —— 🔴 **两栏真的不一样,而且我 09p 那句更正是错的**

**全文**:`handoff/night-runs/09q_推后加法清单_两栏_2026-09-18.md`

**做法**:拿 **285b20d 起一个空库**当基准(**79 表 / `journal_mode=delete` —— 与生产库现测两项都对上**),
再用**当前代码**在它的两份拷贝上各开一次机:**A = `IS_PRODUCTION` 没设 · B = 已设**。

| 项 | 基准 | **A(没设)** | **B(已设)** | 差? |
|---|---|---|---|---|
| 表数 | 79 | 92(+13) | 92(+13) | 一样 |
| 🔴 `journal_mode` | **delete** | **wal** | **wal** | 一样 |
| `service_prices` | 0 | 6 | 6 | 一样 |
| `user_identities` | 0 | 2 | 2 | 一样 |
| 🔴 `tenant_settings` | 4 | **8**(+4) | **6**(+2) | **差 2 行** |

**A/B 差异表数 = 1。** 差的就是两条一次性迁移的标记
(`demo_retire_backfill_v1` / `tenant_kind_backfill_v1`)—— **B 栏两条都不写。**

## 🔴 §二 那件事的分量在这里(A 栏独有、而且不是"加行")

| 迁移 | `IS_PRODUCTION` 没设时它会做什么 |
|---|---|
| `backfillTenantKindOnce` | 按 **id 前缀**把 `tenants.kind` 从 `real` **改判**成 `demo`/`test` |
| `retireLegacyDemoArchives` | 给名字**含「演示」**或 `id LIKE 'demo-%'` 的 `users` 打 `退役·旧口径演示档案` 标 |

🔴 **这两件都是「改内容」,不是「加行」。生产有 6 家店,里面有没有会被前缀误圈的 —— 我在本机测不出来。**
**这就是为什么这一格必须先定 `IS_PRODUCTION`。**

## 🔴 那条新的、我们俩此前都没列进清单的

**推完第一次开机,生产库会从 `delete` 变成 `wal`** —— WAL 是**写进文件头的持久设置**,
店主的库会多出 `-wal` / `-shm` 两个伴生文件。**这一条两栏都会发生,必须写进给她的清单。**

> **而且它同批自洽**:WAL 一上来,「cp 备份漏掉 WAL」从理论变现实 ——
> **而把备份改成 `VACUUM INTO` 的 09n 件 A 正是同一次推。风险与解药同批上线。**

## 🔴 更正 09p「相邻②」—— 我那句两半都不准

我写:「`tenant_settings` 6→8 那两行是两条迁移标记;真生产上会跳过 ⇒ 是 6→6。」

1. **那两行不是迁移标记。** 现测是 `salary_adjusts_migrated` 与 `lucky-luxe/s1_storefront`;
   两条迁移标记在 09n 那个同构库里**本来就已经在了**。**我当时没打开看,是推的。**
2. **「真生产上是 6→6」也不对。** B 栏是 **+2**。

🔴 **而且第一遍实验我也没验出来** —— 基准里那两条标记已经存在,**一份不可能露馅的数据**(L4)。
摘掉标记重做,A/B 才分出 2 行差。**判据律我引了这么多遍,自己造夹具时又踩一次。**

## 还有一条数字更正

**`platform_accounts` 现在是 0 → 0,不是 09n 件 C 报的 0 → 1** ——
09p 落了 D203 甲支之后自举默认关,开机日志明说「本次未建」。

---

# 三 · §二.3 · 裁#90 的「或」补到那两处 —— **改法报批,一行没改**

**全文**:`handoff/night-runs/09q_裁90的或补到两处迁移_改法待批_2026-09-18.md`

**现在怎么判**:`local-server.mjs:168` `IS_PRODUCTION = isProductionEnv()`(纯环境变量)→
`:16762` `backfillTenantKindOnce({isProduction})` · `:17495` `retireLegacyDemoArchives({isProduction})`。**两处都只有一个判据。**

**提议(一行)**:`const TREAT_AS_REAL = IS_PRODUCTION || !DEV_SCOPES.has(DATA_SCOPE_NAME)`
🔴 **第二个判据必须写成 `!DEV_SCOPES.has(...)` 而不是 `=== 'production'`** ——
生产被 `scopeOf` 判成 `'local'`(09p §一),拿 `=== 'production'` 判**在生产上永远不成立,等于没加**。

## 三向都报

| 向 | 改前 | 改后 |
|---|---|---|
| ① `IS_PRODUCTION` **已设** | 跳过 | 跳过(**行为一个字不变**) |
| ② **没设** | 🔴 **会跑**(就是那个缺口) | **跳过**(`local ∉ DEV_SCOPES`)—— 缺口堵上 |
| ③ **设错**(四种组合:生产变量丢了 / 本机常设 `NODE_ENV=production` / 库域判成 `unknown` / 沙箱误设) | 两种会跑 | **四种一律跳过** |

🔴 **代价只有一个,必须你裁**:**本机库也会被判成"真环境",那两条迁移在店主本机也不再跑。**
现测本机库里两条标记**都已经在了**(跑过了,当下无影响),**但本机库以后若重置,就不会自动跑了。**
沙箱与回归临时库不受影响(在 `DEV_SCOPES` 里)。

---

# 四 · §一 小程序档口径 —— **改了,而且改进脚本里**

你要的正式写法,现在是脚本自己打的(不靠我在回执里手写):

```
[小程序档小结] 3 套 —— **真跑起来 0 · 绿 0 · 红 0 · 被超时切掉 3**
   四格闭合:0(其中绿 0 + 红 0) + 切掉 3 = 3 套 ✅ **空转不计入绿**
```

**「红 0」不再单独出现在一个从来没跑起来的档上。**

---

# 五 · §四① · 哪几把刀读了 Cowork 的文件

**全文**:`handoff/night-runs/09q_判据扫描面_谁读了Cowork的文件_2026-09-18.md`

**扫描面 214 个文件**(`test-*.mjs` 136 + `tools/*.mjs` 78)。字面量 `handoff` 命中 31 个,
**其中 17 个是 AI 意图名**(`after_sales_handoff` / `handoffRequired`),与目录无关。**真读文件的 14 个。**

| 类 | 数 | 结论 |
|---|---|---|
| 🔴 **不分作者扫整棵 `handoff/`** | **1** | `test-untouched-proof.mjs:101`(`git ls-files handoff`)—— **就是本批撞上那把**。09p 打的是症状补丁,**根治待批** |
| 只读**自己产出** | 11 | 有意,合规(`delivery-evidence` 筛的是「回归自证块」这种**只有我会写**的机制,不是文件名) |
| 读 **Cowork 产出**,但那正是它该读的 | 2 | `design-token-diff` 读**合同图**、`ext-deps-check` 读**外部件清单** —— **当标准,不是算进底数** |

🔴 **一句提炼**:**判据读对方的东西分两种 —— 「当标准」可以,「算进底数」不行。**
J-90 禁的是后一种;两种在代码里长得一样(都是 `readFileSync('handoff/…')`),
**以后新加读 `handoff/` 的刀,注释里要写明自己属于哪一种。**

---

# 六 · §四② `/tmp/probe.js` · §四③ 老板口令那一读

- **`/tmp/probe.js` 不删** ✅ 收下三条理由。**已登记进推后核验**:
  **推完确认新容器里 `/tmp/probe.js` 不存在 —— 它同时是「容器真的换了」的指纹。**
- **补口径收下**:**「只读进容器」= 不产生任何持久化痕迹;`node -e` 内联可以,写文件不行,即使在 `/tmp`。**
  本批四次进容器,**全部 `node -e` 内联或 `ls`,没有再落任何文件。**
- **老板口令那一读排推后** ✅ 没跑。

---

# 七 · 本批的「量出来 / 推出来」自检(J-88)

| 结论 | 哪一种 |
|---|---|
| 生产库不是 WAL,是 `delete` | **量**(只读 `PRAGMA journal_mode` + 目录里没有 `-wal`/`-shm` + `page_count×page_size` 对上文件大小) |
| 不是 WAL 是因为那段代码没上线 | **量**(`git show 285b20d:apps/api/db-concurrency.mjs` 不存在;那一版里调用次数 0) |
| 这 22 天没有新业务写入 | **量**(mtime + 文件大小 + PID 1 确实开着这个文件) |
| A/B 两栏差 2 行 | **量**(两次开机逐表对账,并反证 B 那次 `IS_PRODUCTION` 真生效) |
| 推完会变成 WAL | **量**(基准 delete → A/B 都是 wal) |
| 生产 6 家店里有没有会被前缀误圈的 | 🔴 **两种都不是 —— 我测不出来,如实挂着** |

---

# 数(现测)

| | 数 |
|---|---|
| 主档 | **128 套全过 · 断言 4,144 · not ok 0** |
| 门关档 | 25 套 · 红 1(`noshow-aftersales` = D199,已知) |
| 小程序档 | 3 套 —— **真跑起来 0 · 绿 0 · 红 0 · 切掉 3**,四格闭合 ✅ |
| 整跑 | **5 分 57 秒** · 看门狗 744s **没开枪** |
| 断言 | **4,144(±0 —— 本批只改了一行报数口径,没加判据)** |
| 巨型文件 | `local-server.mjs` **17,623(±0)** · `admin.js` 7,928(±0)· `customer.js` 2,458(±0) |
| 本机服务 | 4128 = 200 · 4310 = 200,**两台都过版本指纹** |
| 账本 | `8cf9acc` 领先 326 + 本批 121 = **447** ✅ · `origin/main` 仍是 `285b20d` |

**安全保证**(03q 立律)
**未动对照:handoff/night-runs/09q未动对照表_2026-09-18.md**

---

# 八 · 现在还挡着推的

| | |
|---|---|
| 🔴 **只剩一件** | **`IS_PRODUCTION` 在 Railway 上设没设**(你去问店主)。两栏清单已备好,她答完直接对号入座 |
| 🟡 等你批 | 裁#90「或」的改法(**代价:本机库以后重置不再自动跑那两条迁移**)· `test-untouched-proof` 扫描面根治 |
| ✅ 已解除 | 全量回归 · `scopeOf` · D203 · 件 A/B/C · 库落点 · 门禁回归 · **mtime 那件事(甲 成立)** |
| 🟡 不挡 | 小程序档空转(口径已改)· D199 · D200 · D201 · D202 · 老板口令那一读 · 09m 后台清单 |

🔴 **一个 push 都没执行。**
