# 05w 回执 —— §二~§六 + jics 时区 + #14 + L5 全做完(2026-09-09 夜)

> **未推 main** · **不碰生产**(本批没有任何一条命令连过生产:不推 main、没 railway ssh)
> 提交 `830da9a` → `9016048` 五个 · 全量回归 **108 套全过** · 整跑 **2 分 40 秒**
> 断言基线 **3,780 → 3,790(+10)** —— 增量对得上:新加的 dashboard-pulse ⑩ 组正好 10 条
> 巨型文件棘轮:`local-server.mjs` **17,708 → 17,708** · `admin.js` **7,928 → 7,928**(都未升)

## 一、六项结构(店主定的回报格式)

### ① 改了什么

| 段 | 做了什么 | 提交 |
|---|---|---|
| §二 | D184 两把刀从 `test-seed-rich`(不进全量)**搬进 `test-dashboard-pulse`**(在 DEFAULT_SUITES 里);空店里判据会在 0 上绿,所以按造景律先造景:三位顾客 + 今天一笔首充 + 去年一笔首充(非当期反例) | `830da9a` |
| §三 | 裁 #22:**病根是写死色**。`app.wxss` 全局面 + 商家首页 + 商家「我的」整页共 **88 处**字面色改读令牌;小程序 wxss 写死色棘轮上线(**2,742**,只许降) | `da314ca` |
| §四 | 裁 #23:`reminder_tasks` 进白名单但**逐行过形状**(新刀 `tools/heartbeat-shape.mjs`),**放行清单逐行打印**;数字带读数时刻;`notification_logs` **我没有自己加**,照实报「有动」并写待裁 #26 | `c8d1b57` |
| §五 | J-37 回扫:11 个取 DOM 的判据逐个过,**补两处**只问「在不在」的 | `2ab3e9b` |
| §六 | 两条跑机口径落进脚本(收摊 = 收文件 + 收进程 · 不许接 `| tail`);#14 静态判据 + 存量清单;L5 网页三档**补满 5 次** | `9016048` |
| 附 | `jics-store` 时区改 `Asia/Shanghai`(**沙箱库**,走正门路由) | 同上 |

### ② 在哪一层验的

| 事 | 验在哪一层 | 现测值 |
|---|---|---|
| D184 两条 | **全量回归的接口层**(不是渲染中的那一帧 —— 您 §七 记的那一笔) | `today/week/month/year` 各:接口 1 === SQL 1 · 1 ≤ 顾客总数 3 |
| 裁 #22 | **两层**:静态(那几个面零字面色)+ 行为(开发者工具里量 computed 底色) | 浅 纸=`rgb(250,248,243)` 卡=`rgb(255,255,255)`;深 纸=`rgb(25,27,25)` 卡=`rgb(32,36,31)` |
| 裁 #23 | **库层**(倒推哈希对开批指纹快照) | 11 行 PENDING→SENT,最早 `00:00:59.651Z` 最晚 `09:00:03.950Z` |
| J-37 两处 | 页面层(包围盒 / 宽高) | 见 `handoff/J37回扫清单_2026-09-09.md` |
| L5 三档 | **店主会看到的那一层**(真点按钮 + 量 body 底色) | 5 轮 × 三档 **29 条全绿**,五轮量到的值完全一致 |
| jics 时区 | **两端各读一次**(顾客端公开口 `/stores` + 商家端 `/admin/store-clock`) | 两处都是 `Asia/Shanghai`,门店当地 19:43 |

### ③ 同类扫尽

- **写死色**:网页那把刀只盯 `styles.css`(382)→ 现在小程序 `**/*.wxss` 一并纳入,棘轮 **2,742**(实际条数,不留空隙);令牌文件与 base64 生成物不计。
- **UTC 判天**:`substr(appointment_start,1,10)` 全仓 **12 处**上棘轮 + 存量清单逐条写用途;顺手白名单式再扫别的长相(`date()` / `strftime`)= **0 处**。
- **判据住在跑不到的地方**:预检加一扫(只提醒不拦),现在点名 `seed-rich` 一处。
- **J-37**:11 个取 DOM 的判据逐个过,不是列举。

### ④ 用什么数据验的(反例数据律)

- D184:**去年那笔首充**当反例 —— 今日该是 1 不是 2(⑩c),空店会让判据在 0 上绿,所以先造景;
- 裁 #23:两把刀都在**库的副本**上落(本机库只读),刀 FF 改 payload → 形状红并点名是哪一行;刀 GG 删两行 → 「行只增不减」红并报 270→268;
- 裁 #22:刀 EE 把商家「我的」`.group` 退回写死白 → **静态层与行为层各红一条**,都点名选择器、都报量到多少;
- L5:刀 HH 让选了档不挂 `data-theme` → **20 条红**。

### ⑤ 重复稳定性

L5 那条这次是**真做满的**:5 轮,每轮**重新进出页面**,每轮点三档,四样一起验(点得到 / 存住 / 挂对 / 底色跟着变),
再加一条「刷新之后还是刚才那一档」。五轮之间量到的值**完全一致**(浅 `rgb(250,248,243)` · 深 `rgb(25,27,25)` · 跟系统 dark · 刷新后 dark)。

### ⑥ 残留与不确定

- **待裁 #25**:小程序站内两档**只在 2/67 个页面生效**(theme class 只挂了两页)。这是我量出来的第二半病根,覆盖率棘轮已常驻(只许升),铺满是一批的量。
- **待裁 #26**:`notification_logs` 要不要跟着进心跳白名单(1:1 对得上:今天 11 行日志 ↔ 11 行发出)。**我没自己加**,所以本批库结论照实写「有动」。
- **J-37 欠一句**:「没被别的层压住」只探中心一个点,压住半边的过得去;要不要升到四角+中心五点,请裁。
- `mp-*` 三套要 `MP_AUTOMATOR` 才跑得出断言;不给就**按红处理**(这次第一遍就是这么红的,补上环境变量后 108 套全过)。

## 二、L1–L5 自评

- **L1 末端验证**:✅ 三处都在店主会看到的那一层验(小程序真窗口两张图 · 网页真点五轮 · 库的放行清单)。
- **L2 同类扫尽**:✅ 三条(写死色 / UTC 判天 / J-37)都给了机械扫描口径与棘轮。
- **L3 无图不结案**:✅ 小程序深色两张真窗口图(`08截图/01`、`02`),与 05v `07截图/02` 那张病的证据并排。
- **L4 反例数据**:✅ 见 ④。
- **L5 重复稳定**:✅ 网页三档补满 5 次。**小程序那三档没做 5 次**(只各切一次 + 量色):开发者工具一轮 reLaunch 要十几秒,五轮 × 三档 × 两页会把这一段拖到十几分钟,**如实写在这里**,要补请裁。

## 三、写库自报(绝对路径)

| 库 | 这一批动没动 | 证据 |
|---|---|---|
| **生产库** | **没连过** —— 不推 main、没 `railway ssh`、没有任何一条命令指向生产 | 本批命令全在本机;无生产对照表,不假装有 |
| **本机库** `/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite` | **有动(心跳)** —— `reminder_tasks` 11 行按形状放行、`notify_scan_marks` 每店 +1;`notification_logs` **不在白名单**,所以结论按刀写的是「有动」 | 原文见文末 |
| **沙箱库** `/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite` | **动了一列**:`stores.timezone` 中 `store-jics-store` 由 `America/Toronto` → `Asia/Shanghai`,走正门路由 `PUT /platform/tenants/jics-store/store`,**只改 timezone 一列** | 两端各读一次:顾客端 `/stores` = `Asia/Shanghai`;商家端 `/admin/store-clock` = `Asia/Shanghai` · 当地 19:43 · `¥` |
| **回归临时库** | 每跑新建、跑完即删(`test-dashboard-pulse` 的三位顾客与两笔首充就造在这里) | `run-all-tests.sh` |

## 四、我自己咬到的(都当场修了,记在注释里)

1. **判据看代码不看注释**:`mp-theme-proof` 用 `indexOf('.theme-dark{')` 找令牌块,找到的是**注释里**提到的那一串,深色两条判据自己变 `NaN`。改成行首锚定真规则块。
2. **取样点取错 ≠ 页面坏了**:跟系统档量 `.page` 必然透明(底由 `page{}` 那个节点画);L5 那把刀更狠 —— 我先量了透明的 `main`,五轮全 `rgba(0,0,0,0)`,**而「五轮一致」那条居然绿了**;再加 `/^rgb/` 把 `rgba(0,0,0,0)` 也算「量到颜色」。两层废判据叠一起。现在:先证「真有底色」,再谈一致。
3. **前置该验被测本身**:L5 头一版拿「首页大屏出态」当前置,它跟外观三档毫无关系 —— 红在一件与被测无关的事上,也是一种废判据。
4. **反引号第四次**:注释写进模板串里,反引号当场把串截断(今天在 `theme-l5-proof` 又踩一次)。已在那段注释里写死警告。
5. **`| tail` 我自己先踩**:`mp-theme-proof | tail -16` 挂了 8 分钟 —— 正是 §六 那条口径要防的,所以这条口径落成了脚本里的拒跑闸。

---

## 附:库对照表原文(J-23:这一段由 `tools/receipt-db-proof.sh` 打印,人不许手写)

```
**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/06_本机库快照_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 76→87(+11) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     notify_scan_marks  行 54→58(+4) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 5 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notify_scan_marks · 只新增 1 行(10→11)
    hoptest-demo2 · notify_scan_marks · 只新增 1 行(10→11)
    luvia-bj · notification_logs · 只新增 11 行(61→72)
    demo-ai · notify_scan_marks · 只新增 1 行(10→11)
    demo-basic · notify_scan_marks · 只新增 1 行(10→11)

  🔴 既有行被改动/删除 1 处:
    luvia-bj · reminder_tasks · **旧行消失 11 行**(行 80→80,其中新增 11)
```

**心跳形状刀原文**(`node tools/heartbeat-shape.mjs <库绝对路径> handoff/night-runs/06_本机库快照_开批.fp.json`):

```
ok 1 - ① 倒推前提成立:reminder_tasks 里 PENDING 的 196 行,updated_at 全都还等于 created_at
ok 2 - ② reminder_tasks:本批状态推进的 11 行,**每一行都只动了 status / sent_at / updated_at**(倒推哈希在开批快照里找得到 = 其余列一个字节没变)
ok 3 - ③ reminder_tasks 行只增不减(开批 270 → 现在 270)

════ 心跳白名单放行清单(截至 2026-09-09T12:01:14.413Z 共 11 行)════
  luvia-bj · reminder_tasks · ntask_mtpcebkc_5dpn1h · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebki_elw5vx · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebkn_pq1l96 · PENDING→SENT · 排定 2026-09-09T01:30:00.000Z · 发出 2026-09-09T01:30:59.744Z
  luvia-bj · reminder_tasks · ntask_mtpcebkt_vqfo34 · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebky_i0fyjj · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebl3_r581c4 · PENDING→SENT · 排定 2026-09-09T04:30:00.000Z · 发出 2026-09-09T04:30:00.066Z
  luvia-bj · reminder_tasks · ntask_mtpcebla_v5mgln · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblf_5dcfjp · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblk_2r4r0d · PENDING→SENT · 排定 2026-09-09T07:30:00.000Z · 发出 2026-09-09T07:30:03.683Z
  luvia-bj · reminder_tasks · ntask_mtpceblp_l2q4zk · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  luvia-bj · reminder_tasks · ntask_mtpceblv_nuubst · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  ── 最早 2026-09-09T00:00:59.651Z · 最晚 2026-09-09T09:00:03.950Z(共 7 个不同时刻)

✅ 心跳形状 3 条全过 —— 这 11 行按裁 #23 的形状放行
```

🔴 **本机库:有动** —— 以下表不在服务心跳白名单里,**本批不许写「未动」**:
```
     notification_logs  行 76→87(+11) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```
```
