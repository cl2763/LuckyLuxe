# 09r 回执 · 「或」落地(九向现测)· D73 影响面查询待批 · 🔴 新立那把刀第一天就咬到真的了

**上游令点名(J-81)**:09i · 09j · 09k · 09l · 09m · 09n · 09o · 09p · 09q ·
**09r `73a1e6b75f4b81ac0a213974ee5bf33b`** —— **十份桌面/仓里 md5 全一致。**

🔴 **生产库本批一次都没连** · **一个 push 都没执行**(`origin/main` 仍是 `285b20d`)

---

# 一 · §三 「或」落地了,九向全现测

**出口**:`apps/api/data-scope.mjs` 新增 `treatAsReal({ dataDir, env })`,形状与 `demoLoginAllowed` 一模一样
(**两个判据任一说「这是真的」就当真环境办**)。
**调用**:`local-server.mjs:168` 一行里算出 `TREAT_AS_REAL`,两处迁移换成它。

```
🔒 当真环境办 → 跳过   生产库 · RAILWAY_ENVIRONMENT 已设
🔒 当真环境办 → 跳过   生产库 · 变量全丢了            ← 🔴 这一向就是那个缺口,堵上了
🔒 当真环境办 → 跳过   本机库 · 无变量                ← 你接受的代价
🔒 当真环境办 → 跳过   本机库 · NODE_ENV=production(店主 shell 就这样)
🔓 开发档 → 会跑      沙箱库 · 无变量
🔒 当真环境办 → 跳过   沙箱库 · 误设 NODE_ENV=production
🔓 开发档 → 会跑      回归临时库 · 无变量
🔒 当真环境办 → 跳过   回归临时库 · NODE_ENV=production(裁#37 那个坑)
🔒 当真环境办 → 跳过   库域判不出(unknown)
```

**九向里只有两向是「会跑」,而且都是开发档。误差方向全部朝「跳过」倒。**

## 🔴 棘轮反而降了:17,623 → **17,621(−2)**

第一版我把注释摊在 `local-server.mjs` 里,**+1 破了你 09p 批的上限**。
按裁#89(破棘轮只有三种正确反应:抽取 / 问 / 举证),**选了抽取**:
理由全文搬进 `data-scope.mjs`(`treatAsReal` 定义处),巨型文件里只留一行。

## 两条锚跟着收紧(J-58③:反向锚不许落在自己会抹掉的字面量上)

`㋜③` 与 `㋟⑤` 原来锚的都是 `isProduction: IS_PRODUCTION` —— **一个被本次改动抹掉的字面量**。
**没有换一个同样松的字面量,而是要求**:①调用处用 `TREAT_AS_REAL` ②`treatAsReal` 真的是那个「或」
③**新增反向守**:那个「或」的第二个判据**不许写成 `=== 'production'`**(生产被判成 `local`,那样写等于没加)。

🔴 **我 L2 没扫干净**:修完 `㋜③` 我没机械搜同类,**让全量替我找到了 `㋟⑤`**。
**「改一个字面量」永远是一类,不是一处。**

---

# 二 · §二 D73 改判影响面 —— **查询原文已贴,生产上一个字没跑**

**全文**:`handoff/night-runs/09r_D73改判影响面_查询待批_2026-09-18.md`
**工具**:`tools/d73-impact.mjs` —— **只 `SELECT`,零写入;不调那两个迁移函数,而是把规则原文照抄在内存里算。**

## 🔴 规则原文里那一行,你点的靶子就在上面

```js
const head = String(r.id).split('-')[0]      // ← 「按 id 前缀判这家店是什么」= D73 废弃的判法
```
**`jics-nail` 与 `jics-store` 的 `head` 都是 `jics`** —— 今天 `jics` 不在那张前缀表里所以今天不会撞,
**但判法本身就是被废弃的那条。**

## 阳性对照(J-58⑧)已跑,两条都数得出来

本机库上:**查询② 四格闭合 0 + 3 + 3 = 6 ✅** · **查询③ 命中 10 行**
(`jics-nail` 2 行:「店主验签」「爽约演示客」;`lucky-luxe` 8 行:「林小雅(演示)」等。底数 75 / 74)
**⇒ 这把刀能咬到东西,不是空转。**(本机那 10 行**已经带着标签**,因为这条迁移在本机早跑过;
**生产上没跑过,所以在那边命中就是新打标。**)

## 🔴 要批的那一条 + 一个选择

脚本**线上那份代码里没有**(本批新写,线上跑 `285b20d`)。两条路:
- **(甲) 内联成 `node -e`**(不落文件,合 09q 补的「只读进容器 = 不产生任何持久化痕迹」)—— **我倾向这条,已备好**;
- **(乙) 推完再跑** —— 但**推之前正是要知道这个数的时候**,等于不查。

## 🔴 我在写这个脚本时又踩了一次 J-75

第一版用了 `users.created_at` —— **本机库的 `users` 根本没这一列**,当场炸。
**09p 刚因为「只探了一列」栽过(`bookings.demo_seed`),这版又来。**
现在脚本里有**列存在性预探**:用到的**每一列**逐个探,缺哪一列点名说哪一列,**不改用别的列凑**;
逐行那一栏也不编 `created_at`,改成「店 · 名字 · id · 现有标签」。

---

# 三 · §三 尾巴:那两条迁移的去留 —— **立 D204,待裁**

**全文**:`handoff/night-runs/09r_那两条迁移的去留_待裁_2026-09-18.md`

两条现在在生产库与本机库**都不跑了**(=你说的「被冻结」),只在沙箱/回归临时库还跑。

| | 历史任务完成了没有 | 倾向 |
|---|---|---|
| `backfillTenantKindOnce` | 本机库标记已在(跑过);生产的 6 家店 `kind` **本来就是显式设的** | **销号** |
| `retireLegacyDemoArchives` | 本机库命中 10 行**全都已带标签** ⇒ 跑完了 | **销号**,或只留 `id LIKE 'demo-%'` 那一条(不认名字) |

🔴 **销号 = 把代码删掉,不是把开关关掉**(裁#107 同一精神)。三选一在那份文件里,**等你裁**。

---

# 四 · §四 生产每日自动备份的出口与调度 —— 再点一次名

```
出口: apps/api/db-backup.mjs:96  dailyBackup()  →  snapshotDb()  →  backupDb()(VACUUM INTO,唯一实现)
触发: apps/api/local-server.mjs:17602
       if (process.env.BACKUP_ENABLED === 'true') { runBackup(); setInterval(runBackup, 6*3600*1000) }
       —— 开机跑一次,之后每 6 小时一次;文件名 lucky-luxe-<YYYY-MM-DD>.sqlite,当天已存在就跳过;30 天滚动清理
```
**与 09o 说的一致:按需快照与生产日备是同一个出口。**(生产现测 35 份日备,时间戳全在 04:55,与 6 小时节拍吻合。)

🔴 **已登记进推后待办**:**推完第二天 04:55 那次自动备份跑完后,打开逐表点行数与生产对**
—— **那是第一次在 WAL 下跑的自动备份,09n 件 A 改对没有,只有那一次能证明。**

---

# 五 · §五 判据扫描面根治 —— 落了,而且我自己那条棘轮写坏过一次

`test-untouched-proof` 的扫描面从 `git ls-files handoff`(整棵树)收窄为**排除上游令两个家族**
(`handoff/小批*` / `handoff/夜班令*`)。**09p 那层「引述不算」留着当第二道**(照你说的不撤)。

**两面刀**:③s 同一句话放进「令」形状路径 → **不进底数**;
③t 同一句话放进「回执」形状路径 → **必须进底数,且真被认成 1 条声称**。
**收窄后存量仍是 34,棘轮没降也没升。**

**顺带摘了 3 条死条目**:名单里原有 3 条上游令(夜班令9 / 小批05i / 小批07b),
收窄后**已经不在扫描面上** ⇒ 死条目。**按裁#105(抽取要把棘轮降下来、不留余量):38 → 35,一条余量不留。**

## 🔴 而我给它配的那条棘轮,第一版是坏的

我写的是「**排除掉的份数 ≤ 122,只许变少**」。
**可上游每发一份令,这个数就 +1** —— **一条注定每批都红的棘轮,比没有还坏**(它训练人忽略红)。
🔴 **更坏的是:我在它上面的注释里已经写明了「这个数会随上游发令而自然增长」,然后还是写了 `≤`。**
**文档里认出的问题,没有走进代码。**

**改成闭合而不是上限**(J-66):`排除面 123 + 被测面 391 ≡ 全量 514`。
闭合守「这个划分没被悄悄改过」,「不许把令以外的东西划进排除面」由 ③q 逐个核名字守 ——
**两条都不随发令次数漂移。**

---

# 六 · 🔴 新立那把版本指纹刀,第一天就咬到一件真的

本批我在等全量时**用错了一条命令**:`for` 循环超时后那句 `kill -9` 把**正在跑的回归**打断了,于是:

- **4310 被连带打死** —— 已按 `/tmp/ll-sandbox-4310.env` 记录原样拉回;
- 🔴 **4128 答 200,但库指着 `/tmp/ll-ci-data.1STV2a/lucky-luxe.sqlite`** ——
  **回归中途被打断时,租户隔离那一段起的 CI 实例赖在 4128 上**(D176 同族第二例)。

```
🔴 4128:库路径不在期望的树里 —— 期望 …/new-chat 下,实际 /tmp/ll-ci-data.1STV2a/lucky-luxe.sqlite
   这正是 09p 那一次的形态:200 是真的,版本是错的。
```

**⇒ 09p §五② 你批的那把刀(`tools/verify-restored.sh`),立起来第一天就抓到一次真事故。**
**而那个临时目录跑完就会被删** —— 店主这时候打开后台,看到的是一个**马上要消失的空库**。
两台都已从主树重起并过指纹。

## 顺手装了一条护栏:预检第⑨条

`test-db-target-guard ⑤`(没 `git add` 的新文件对每把刀隐形)**连着三批咬我**(09p / 09q / 09r),
**每次都是等整跑六分钟才知道,而这件事 6 秒就能知道。**
**判据没问题,问题是它站的位置太靠后。**
→ 把同一条挪进 `tools/pre-regression.sh` 第⑨条,**拦(不是提醒)**,两面刀现测:
造一个未入册文件 → **红并点名**;`git add` 之后 → **回绿**。

---

# 数(现测)

| | 数 |
|---|---|
| 主档 | **128 套全过 · 断言 4,150 · not ok 0** |
| 门关档 | 25 套 · 红 1(`noshow-aftersales` = D199,已知) |
| 小程序档 | 3 套 —— **真跑起来 0 · 绿 0 · 红 0 · 切掉 3**,四格闭合 ✅ |
| 整跑 | **5 分 27 秒** · 看门狗 744s **没开枪** |
| 断言 | 4,144 → **4,150(+6)**:`test-untouched-proof` 21 → 26(**+5**,③p 闭合/③q/③r/③s/③t)+ `㋜③b` 反向守 1 |
| 🔴 棘轮 | `local-server.mjs` 17,623 → **17,621(−2)** · `admin.js` 7,928(±0)· `customer.js` 2,458(±0) |
| 本机服务 | 4128 = 200 · 4310 = 200,**两台都过版本指纹** |
| 账本 | `8cf9acc` 领先 326 + 本批 123 = **449** ✅ · `origin/main` 仍是 `285b20d` |

**安全保证**(03q 立律)
**未动对照:handoff/night-runs/09r未动对照表_2026-09-18.md**

---

# 七 · 现在的挡推清单

| | |
|---|---|
| 🔴 **D73 改判影响面** | 查询原文已贴、阳性对照已跑。**等你批(甲=内联 / 乙=推后)** |
| 🔴 **`IS_PRODUCTION`** | 店主核一眼(你在问)。🟢 **但「没设 → 会跑」那一向本批已经堵上了** |
| 🟡 等你裁 | **D204** 那两条迁移销号还是改判法 |
| ✅ 已解除 | 全量回归 · `scopeOf` · D203 · 件 A/B/C · 库落点 · 门禁回归 · `-wal`(甲档坐实) |
| 🟡 不挡 | D199 · D200 · D201 · D202 · 小程序档空转 · 09m 后台清单 |

🔴 **一个 push 都没执行。**
