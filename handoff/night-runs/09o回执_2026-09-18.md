# 09o 回执 · D203 去了(还扫出第二处)· D202 我把上一批那句话推翻了 · 🔴 回归那条:量出来了,**不是回归**

**上游令点名(J-81)**:09i `fa3fed3e…` · 09j `61f7e1d6…` · 09k `6024e959…` · 09l `2a17c211…` ·
09m `4f9faf06…` · 09n `5e7fd59b…` · **09o `55967034412fb8ef6bde5d3e1a0daf65`** —— **七份桌面/仓里 md5 全一致。**

🔴 **Railway 面板一次没碰** · **`railway` 任何子命令一次没用** · **一个 push 都没执行** · **D201 一个字没在生产上跑**

---

# 🔴 一 · 回归那条:量出来了,答案是「**不是回归,是判据自己报错了地方**」

## 结论先放

> **`tenant-isolation` 从来没卡过。它跑了 0 秒,而且它是主档最后一套。**
> **看门狗那句「当时在跑:tenant-isolation」是个死值** —— 主档之后的两档根本不更新那个字段。
> **300 秒是被三档 154 次执行吃掉的,不是被某一套吃掉的。**

## §三.1 · 逐用例计时 · 最慢三套

主档 126 套**逐套耗时合计 125 秒**,最慢的是:

| 名次 | 套件 | 耗时 | 占主档 |
|---|---|---|---|
| 1 | 🔴 **`test-user-write-auth`** | **41s** | **33%** |
| 2 | `test-merge-window` | 20s | 16% |
| 3 | `test-auth-surface` | 8s | 6% |
| | 其余 123 套合计 | 56s | 45% |

**头两套吃掉主档的 49%。** 而 **`user-write-auth` 是我 09l 加的那一套** —— 41 秒一套,我自己写的。

## §三.2 · 🔴 推前/推后对照 —— 这一格才是答案

| | **285b20d(推前,线上那份)** | **HEAD(推后)** |
|---|---|---|
| 套件 | **47 套,一档** | **154 次执行**(主 126 + 门关 25 + 小程序 3)|
| `test-*.mjs` 文件 | **48 个** | **135 个** |
| 墙钟 | **31 秒 · 跑完 · 全过** | 主档 149 秒,整跑 >300 秒 |
| 有没有看门狗 | **没有**(那时还没立)| 300 秒 |

> **47 → 154,是 3.3 倍。** 31 秒 → 主档一档就 149 秒。
> **看门狗那句「基线约 2 分钟」是它被立的那天的基线**,从那天起我们又加了 87 个套件文件,
> **基线没人回来改过。**
>
> 🔴 **所以「是不是回归」的答案是:不是。** 没有哪个改动让它变慢 ——
> **是判据的覆盖面涨了 3 倍,而报警线还停在三周前。**

## §三.3 · 变慢的到底有几个 —— **这一格我给不出你要的那张表,说明原因**

你要「逐档列出推前/推后耗时,标出所有变慢超过 50% 的」。**做不到,因为推前那一版根本不打逐套耗时**
(`⏱` 那行是后来才加的),而且 **47 套里只有 43 个名字与今天重合**。
**能对的只有总量那一格(上表),逐套那一格没有可比的基准。**
🔴 **我不拿今天的数去反推那天的数** —— 那是编的。

**能给的替代**:今天这一版的**全量逐套耗时表**已落盘 `handoff/night-runs/回归逐套耗时_2026-09-18.tsv`,
**这一份就是以后的基准**。下一批再跑,才谈得上「谁变慢了 50%」。

## §三.4 · 在哪一步卡住 —— **哪一步都没卡**

- 主档 126 套**跑完**(第 149 秒);
- 门关档 25 套**跑完**(红 1,是那一档的真相);
- 到小程序档时撞上 300 秒,**被看门狗一枪打死**。

**⇒ 没有「卡住」这回事,是总时长超线。**

## 🔴 判据自身的缺陷(我给它单独立一条,按「判据也是代码,也该有缺陷号」)

```
run-all-tests.sh:340   printf '%s(第 %s 秒起跑)' … > "$CURRENT_SUITE_FILE"     ← 只在 run_suite 里写
run-all-tests.sh:404   run_suite tenant-isolation env                          ← 全文件最后一次 run_suite
run-all-tests.sh:438   门关档自己的循环                                        ← 不写这个字段
run-all-tests.sh:514   小程序档自己的循环                                      ← 也不写
```

**⇒ 主档之后无论死在哪儿,看门狗都会说「当时在跑:tenant-isolation」。**
**这不是偶发,是恒成立。** 我为此错判两次:第一次归因到 mp 档、第二次归因到 tenant-isolation,
**两次都是被这个死值牵着走的** —— 正是你立 **J-84** 要防的那件事:
**「先问报警器指的是哪一件,再问是什么引起的」。**

**建议(本批没改,因为 §三 的边界是只量不改)**:门关档/小程序档各自也写一次那个字段;
看门狗的 300 秒跟着套件数走,而不是写死一个当年的数。**怎么改归你裁。**

---

# 二 · D203 ✅ 去了,而且 L2 扫出**第二处**

## ①点名那一处

`platform-auth.mjs:151` 原来打的是「一次性密码:<12 位明文>」。**现在一个字符都不打。**
现测(全新 ci 库,`PLATFORM_ADMIN_BOOTSTRAP=1` 强开自举):

```
[platform] 已建平台账号 platform-admin(首登强制改密)。🔴 一次性口令**未输出、也未落盘**
整份启动日志里「口令形状的串」命中 = 0
```

**刀(突变自检条,四步走全了)**:①把那行改回明文 → ②重启 → ③**红,并报得出是第 1 行** →
④还原 + 重启 → **回绿**。(报红时我把值遮掉了 —— 判据里也不许念钥匙。)

## ②🔴 L2 同类扫尽,扫出第二处:**老板主账号的初始口令也在往 console 打**

```
apps/api/admin-auth.mjs:22  console.log(`[账号] 老板主账号已创建 用户名: boss 初始密码: ${initialPassword} …`)
```

**它同时写文件又打日志。** 文件(`local-data/初始老板账号.txt`,gitignore、改密后自动删)是**设计好的交付口**;
**那行 console 是多出来的一份**,而生产上它会进 Railway 部署日志。
**改法不是两个都去掉**(去掉文件就没人拿得到口令了):**留文件、去日志**。
于是文件从「备份的一份」变成**唯一那一份** —— 所以它写不进去**不能再静默吞掉**了,
现在会大声说「口令没能落盘,这一把已经取不到,请走平台后台重置」。

> 这一处**生产上不会再触发**(线上早有 boss 账号,`if (已有 owner) return`)。
> 🔴 **但它历史上触发过一次** —— 也就是说**老板初始口令可能早就在某次部署日志里**。
> 那把口令**早就被首登改密作废了**(D195 §3),所以不是活的洞;**但它是这一族的第一案,记在案。**

## ③常驻判据 `test-secret-output`(15 条),**白名单式**

- **扫描面 371 个**非测试 .mjs/.js(apps/ tools/ miniprogram/),条数下限上棘轮;
- **①静态层**:全仓「输出语句里插密钥类变量」现扫 **2 处,全部落进白名单**(每条写明为什么不是值):
  `wecom-probe.mjs` 打的是 `mask(token)`;`local-server.mjs:17597` 的 `OWNER_TOKEN.slice(0,8)`
  **已被 `if (IS_PRODUCTION)` 挡住**(生产只打「已配置,不在日志中显示」)——
  放行的是那个**产品级判断**,不是写法本身;
- **②行为层**:真起一台服务,在启动日志里搜口令形状的串 = **0**;
- **③甲支合同**:默认开机 → **从库那头验** `platform_accounts = 0`(不看日志怎么说)。

🔴 **这套判据第一次跑就红了 2 条,两条都是判据自己的病**(J-69 摆在这儿):
`/\b(name|Name|…|\?\s*'|…)/` —— **那个 `\b` 管着整个括号组**,于是 `secretName`(N 前是字母)
和 `x ? '有' : '没有'`(`?` 前是空格)**两类都漏掉了**。**它是被自己的反向守刀咬出来的。**

---

# 三 · 🔴 09m 问 3 + D200:平台后台现在靠什么进,以及甲/乙我判了甲

## 问 3 · 三条门,逐条 `文件:行`

```
local-server.mjs:12711  const isPlatform = () => {
:12713    Bearer <OWNER_TOKEN>                          ← ① 令牌
:12714    platformAuth.fromSession(bearer)              ← ② 账号密码换的会话(要 platform_accounts 有行)
:12715    platformSessions.verify({cookie, UA})         ← ③「记住这台电脑」
:13046    POST /platform/session 要先 fromSession 过    ← 现证:③ 只能由 ② 换来
```

**白名单式核过一遍**:`/platform` 路由现扫 **21 条**,**有门 20 · 无门 1**,20+1=21 ✅。
唯一无门那条是 `:10393 GET /platform` —— **发静态登录页 `platform.html`**,页面本身不带数据。
(🔴 第一版尺子写成 `path (===|\.startsWith\()`,"path" 后强求一个空格,**漏了 6 条**;
重量之后才是 21。**尺子缺一层,结论作废** —— J-56。)

## 🔴 判定:**甲**

**生产库 `platform_accounts` 现在是 0 行** ⇒ ②③ 在线上**都无人可用** ⇒ **今天走的就是 ① OWNER_TOKEN。**
**这个账号现在没用。**

## 于是照你的预裁做了:**不在这次推里建它**

`bootstrapAndReport` 现在**默认不建**,要建得显式 `PLATFORM_ADMIN_BOOTSTRAP=1`。

**为什么是「默认关」而不是「生产关、别处开」**:
- `scopeOf()` 在生产上返回的是 **`'local'`**(生产库路径正好以 `local-data` 结尾)—— 拿它判生产**会判反**;
- 拿 `NODE_ENV/RAILWAY_ENVIRONMENT` 判,则是**漏判即建号** —— 朝不安全那侧失败(J-68 反着来)。
- **默认关**:漏判也只是「没建」,**没有任何一条路会凭空生出一把取不到口令的平台钥匙。**

**两向实测**:不设变量 → 日志「本次未建」+ **库里 `platform_accounts = 0`**;
设 `=1` → 真建一次,**日志里零个口令形状的串**。

## D200 · `requireOwnerToken` 本该守的是不是 `/platform/*`

**不是。** `requireOwnerToken()`(`owner-token.mjs:101`)是**调用方拿令牌的助手**,
被 80 个测试套件 + 几个 seed 工具 + `local-server` 自己引用,**它不是服务端的门**。
服务端那道门是 `isPlatformKey(req)`(`:1392`)与 `isPlatform()`(`:12711`)。
**D200 的前提要改写**:要查的不是「requireOwnerToken 守没守住」,而是「21 条 `/platform` 路由是不是都过 `isPlatform()`」——
**上面那张表就是答案:20/21,剩下 1 条是静态页。**

---

# 四 · D202 · 🔴 我把 09n 那句话推翻了,是我自己量翻的

## ①②落点与份数(只读列目录)

| 目录 | 份数 | 最早 |
|---|---|---|
| `apps/api/local-data/backups`(本机库的按需快照)| 5 | 2026-07-14 14:32(864 MB)|
| `backups/`(沙箱快照)| 1 | 2026-09-08 |

**生产那批日备在 Railway Volume 上,本批没去列** —— 列它要进容器,而我 09n 那次
`railway ssh` 被对端关了连接,**没有第二次去撞**(09o §一 的口径是只读可以,但我不确定它现在还通,不确定的事不写进回报)。

## ②🔴 挑最新的打开逐表点行数 —— **5 份全打得开**

```
✅ lucky-luxe.pre-imgclean-20260829-024347.sqlite  [9.6 MB · 82 表]
     users=185 · bookings=1436 · settlements=269 · finance_transactions=1300 · tenants=5
✅ lucky-luxe-20260825203231-挂大类前.sqlite       [11.2 MB · 78 表]   同上五项一致
✅ lucky-luxe-20260824154302-清理测试租户前.sqlite  [11.2 MB · 78 表]   users=242 bookings=1642 …
✅ lucky-luxe-20260714-150249.sqlite               [0.4 MB · 35 表]   (那时还没有 settlements 表)
✅ lucky-luxe-20260714-143356.sqlite               [864 MB · 35 表]   (同上)
```

**这五份都是 `db-backup.mjs` 那条 cp 路做出来的,五份全打得开、逐表点得出行数。**

## 🔴 ③于是我必须推翻自己 09n 写的那句话

09n 我写:「**本机 WAL 此刻 4,144,752 字节,cp 那一刻这 4.1 MB 一个字节不进备份**。」
**这句话是错的。**

**WAL 文件的大小 ≠ 落在主库之外的数据量** —— WAL 不会自己缩,里面绝大多数帧早就 checkpoint 进主库了。
09o 用**同一时刻两种拷法**逐表对账(cp 一份、`VACUUM INTO` 一份,VACUUM 那条看得见 WAL):

```
表                    cp 那份    VACUUM 那份   差
users                    206         206       0
bookings                1482        1482       0
settlements              269         269       0
finance_transactions    1306        1306       0
tenants / admin_accounts / coupon_grants  …    0
```
再换**最敏感的四个指标**(各表 `MAX(created_at)` / `MAX(rowid)`)重量一遍:**4/4 全同,差 0。**

> **正确的说法**:拷法是错的 —— 它拷的是**一份停在上一次 checkpoint 的库**。
> **错到什么程度不可预测**:可能一行不差(现测就是),也可能像我那个合成实验一样**连表都没有**。
> **危险的是「静默变旧」,不是「一定坏」** —— 还原回来不报错,只是少了最近那几笔,没人会发现。
>
> 🔴 **所以给店主的话要改成**:你的每日备份**不是假的**,但**它能不能还原到最新那一刻,是碰运气的**。
> 我 09n 把它说成「4.1 MB 一个字节不进备份」,**那是吓人,不是事实。已改口,09n 回执里也加了更正框。**

## ③改法与常驻判据(§二.3)

`test-backup-wal` 新增 **④ 层:全仓不许再出现「cp 一个 sqlite」的写法**。
- **扫描面 282 个**(apps/ + tools/ **递归**)—— ② 那一层只扫 `apps/api/*.mjs` **平铺 129 个**,`tools/` 里 cp 一个库它完全看不见;
- 类按**机制**定义(拷的对象是不是 sqlite),不按**长相**(附近有没有 "backup" 字样);
- 现扫 **1 处,落进白名单**(导入前快照:跑在接口线程里、导入事务之前,而 `VACUUM INTO` 要独占读锁 —— **09n 未点名,单独登记等你裁**);
- 🔴 **④d 当场咬出一条残留白名单**:`demo-reset.mjs` 早就改走 `snapshotDb` 了,白名单里那条没有对应命中 ——
  **留着它 = 下次有人真在那儿 cp 一个库时自动被放行。已删,上限 2 → 1。**

## ④旧备份一个都没删、没覆盖 ✅

---

# 五 · D201 · 🔴 **找到第二层指纹了,底数从 236 缩到 2**

**全文**:`handoff/night-runs/D201_痕迹查询_第二版_找到第二层指纹_2026-09-18.md`(**一个字没跑**)

`payments.transaction_id`:**建单**那一行写 **NULL**(零定金单生下来就是 PAID);
**`confirmMockPayment`** 写 **`mock_<毫秒>`**。⇒ **只有确认口跑过才有 `mock_`。**

本机库阳性对照:
```
MOCK 行底数 236 = ㋐ mock_<ms> 2 + ㋑ NULL 234 + ㋒ 其它 0   ✅ 四格闭合
```
> 🔴 **236 里只有 2 行是确认口写的。** 我 09n 那个形状会报「236」,
> **而那 234 行是建单即付清的零定金单,跟这个洞一点关系没有。**
> **我第 21 笔(宽指纹数出 57 行)差点原样再来一遍。**

**逐个找过 9 个候选,8 个不行,理由逐条写在那份文件里。**
还找到一条**单向**的:确认记录若其顾客没有任何登录身份,**它不可能走有门那条** ——
给的是无门那两条被用过的**下限**,不是总数。

**第 2 问的答案**:改形状为**逐行列出**(店/单号/金额/时间/单状态/有没有微信身份),两家真店量级小,人眼读得完。

---

# 六 · 🔴 我自己捅的一个篓子,必须自己点名

**跑 285b20d 那一版做对照,把店主本机的 4128 和沙箱 4310 换成了那一版的旧代码。**

老脚本的 `restore_local`/`restore_sandbox` 会从**它自己那棵工作树**把服务拉起来 ——
于是跑完之后 4128 是 200、看着一切正常,**里面跑的却是三周前的 `local-server.mjs`**
(`lsof` 现证 cwd = `/private/tmp/ll-09o/wt-285b20d/apps/api`)。
**店主这时候打开后台,看到的是一个三周前的 app,而且没有任何东西会告诉她。**

- **谁抓住的**:全量回归的**预检**——「两台库名撞名了:4128=(没有这个字段)」。
  那个字段是后来才加的,旧代码没有。**预检那条判据这次救了场。**
- **已处置**:两台都停掉、从主树 `tools/start-local.sh` 与 `start-sandbox.sh` 重新拉起,
  现证 cwd 回到主树、`dataFile` 分别指向**本机库**与**沙箱库**;`285b20d` 那棵工作树**已删**
  (留着它,下次谁再跑一遍就又把 4128 换成旧代码)。
- **归族**:「回归跑完必须把服务还回去」那一条的**新变种** ——
  **还回来了,但还的是别的版本。** 老条款只管「活没活」,不管「是不是这一版」。
  **建议加一条:还回去之后要验版本指纹,不只验 200。归你裁。**

## 还有一笔:我在一个「慢」上连改两次都改错了地方

`test-secret-output` 雷打不动跑 25 秒。我改了两次「等什么条件」——
**根因根本不是等的条件,是那个 25 秒兜底定时器没人 `clearTimeout`**,
结论早就拿到了、`resolve` 也调了,**Node 还是要等它烧完才肯退**。清掉之后 **25s → 0.78s**。
🔴 **这正是 §三 那句「先量不先修」** —— 我这次就是没量先改,白改两遍。

---

# 数(现测)

| | 数 |
|---|---|
| 主档 | **127 套 / 127 全过 · 断言 4,125 条 · 红 0**(现测,第 4916 行之前零 `not ok`)|
| 门关档 | 25 套 · **红 1** = `noshow-aftersales`(**已知,就是 D199 那一套**,那一档的红单独列不并进主档)|
| 小程序档 | 🔴 **没跑到** —— 整跑在它这一档撞上 300 秒看门狗 |
| 断言 | 4,104 → **4,125(+21)** —— 两项都是**单跑数 `ok` 行数**,不是数我写了几个 `assert`:`test-secret-output` **15 条(新)** + `test-backup-wal` **10 → 16(+6)**。15+6=21 ✅ 🔴 总数仍是账面(4,104 那个基数本身就没量过,见 09n 回执) |
| 巨型文件 | `local-server.mjs` **17,623(±0,用的是 09o §四① 批的新上限)** · `admin.js` 7,928(±0)· `customer.js` 2,458(±0)|
| 本机服务 | 4128 = 200(**本机库**)· 4310 = 200(**沙箱库**)—— 版本指纹已核,跑的是主树 |
| 账本 | `8cf9acc` 领先 326 + 本批 117 = **443** ✅ |

**安全保证**(03q 立律:没有对照表不许写那句)
**未动对照:handoff/night-runs/09o未动对照表_2026-09-18.md**

- **生产库未动** —— 凭据是「**本批与它之间一次连接都没发生过**」:`railway` 任何子命令 0 次、
  对线上域名任何请求 0 次、D201 四条 `SELECT` 一个字没跑。
- 🔴 **本机库这句我不写成「未动」。** 我自己没写过它一个字(只只读查询 + 两次拷到 `/tmp`),
  **但它不是没变**:逐表对照 **92 张表,91 张行/列全零差异,1 张有差** ——
  `notify_scan_marks` **行 100 → 112(+12),列 3 → 3**。
  那 12 行是 4128 那台服务自己的通知扫描心跳,**不是本批任何一个动作写的**;列没变 ⇒ 没做过结构改动。
  **基准是批中 17:45 拍的那一份,不是批首** —— 我开批时没打快照,如实写,不拿批中的冒充批首。
- **沙箱库**:本批没有一步指向它;4310 被回归打死后按记录原样拉回(`dataFile` 现证)。

---

# 七 · 现在还挡着推的

| | |
|---|---|
| ✅ 已解除 | D203(打印去了 + 甲支不建号 + 常驻判据 15 条)· 件 A/B/C · 库落点 · 门禁回归 |
| 🟠 **半解除** | **全量回归**:**主档 127/127 全过、4,125 条断言零红** —— 这一格干净了。<br>但**三档跑不完整**:小程序档被 300 秒看门狗打死。<br>🔴 **我没有抬看门狗**(它是判据);🔴 **也没有去优化那 41 秒**(§三 的边界是只量不改)。<br>**要不要就这样推,归你裁** —— 我把两条路都摆出来:<br>①**先裁看门狗那条判据的缺陷**(死值 + 基线停在三周前),改完再跑一次三档;<br>②或者认「主档全过 + 门关档红 1 是已知 D199 + 小程序档单独跑」这个组合。|
| 🟡 不挡推 | D201(推前推后一样,且已重出查询等批)· D202(已发生很久,且现测五份备份全打得开)· 09m 后台清单 |

🔴 **一个 push 都没执行。**
---

# 八 · 本批回归实证(两次独立复现)

| | run-HEAD6(本批终跑)| run-HEAD3(本批中途)|
|---|---|---|
| 主档 | **127 套 · 4,125 断言 · 红 0** | 跑到 92 套被 `db-target-guard` 拦下 |
| 门关档 | 25 套 · 红 1(`noshow-aftersales` = D199)| — |
| 小程序档 | 撞 300 秒看门狗 | — |
| 看门狗那句 | 「当时在跑:**tenant-isolation(第 149 秒起跑)**」 | 同样是这一句 |

🔴 **两次跑,同一个套件名、同一个「第 149 秒」** —— 而 `tenant-isolation` 那一次的实测耗时是 **1 秒**,
并且它是主档**最后一套**。**这就是那个死值的复现证据,不是我猜的。**

**逐套耗时表已落盘**:`handoff/night-runs/回归逐套耗时_2026-09-18.tsv`(127 行,合计 127 秒)——
**这一份就是以后比「谁变慢了 50%」的基准**,推前那一版没有可比的数据。

## 本批被判据咬住的四次(全是咬我自己,照实列)

| # | 谁咬的 | 咬中什么 |
|---|---|---|
| 1 | 回归**预检** | 4128/4310 被我换成了 **285b20d 的旧代码**(见 §六)|
| 2 | `test-db-target-guard ⑤` | 新判据文件 `test-secret-output.mjs` **没 git add** —— 没入册的文件对每一把护栏刀都是隐形的 |
| 3 | `test-credential-scan ①` | 我在判据里写了个**口令形状的字面量**当造病素材;改成运行时拼之后**又被咬一次**,因为**注释里把那个形状原样抄了一遍** |
| 4 | `test-untouched-proof ③` | 我这份回执写了「本机库未动」却**没有对照表** —— 逼我去逐表对,才发现**本机库确实不是「未动」**(见「安全保证」那一格)|

**四次全是「代码写了 ≠ 做完了」那一族**,而且**四次都不是我自己发现的,是刀发现的。**

