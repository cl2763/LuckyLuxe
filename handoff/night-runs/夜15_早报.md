# 夜15 早报

## 1️⃣ 上线了

🟢 **上线了。** `285b20d`(08-26)→ **`aa5e705`** —— **两个半月来第一次。**
CI:**绿** · run `35494233911` · check-run `106034279245`

## 2️⃣ 生产动没动

**动了,而且只动了该动的:**
- 表数 **79 → 92**(+13,全是空表,与推前那份清单逐条对上)
- `journal_mode` **delete → wal**(推前就预告过的那一条)
- 触发器 **19 → 21**(+`conv_msg_no_delete`/`conv_msg_no_update`,**原 19 条一条不少**)
- 🔴 **两家真店五个数零差异**:`lucky-luxe` users 37 / bookings 161 / settlements 0 / fin 30;`jics-nail` 0/1/0/0;tenants 6

### 🔴 D194 —— 单独一行

```
/app/apps/api/local-server.mjs   grep -c "path === '/auth/google/demo'"   →  0
                                 grep -c 'function registerGoogleDemoUser' →  0
```
**那条无门禁建人的路,从线上消失了。**(按 J-72 **没有再去打它一次** —— 代码里没有它,就没有它。)

## 3️⃣ §一 那一件

**搜不到「拉不起来」** —— 而且这次是**字面答案**:CI 注解里 `ci.log 里「拉不起来」出现 0 次`。
沙箱在 CI 上起得来,缺的是**夹具数据**。**09y §一 成立。**

## 4️⃣ (丙) 四条验收

| # | 验收 | 结果 |
|---|---|---|
| 1 | 四套在 CI 上真跑 · 收尾 `未跑 0 套` | ✅ |
| 2 | `NOT_RUN_CAP` 3 → **0** | ✅ |
| 3 | 只种临时 DATA_DIR,不碰仓内 `sandbox-data`、不碰 4128 | ✅ 种在 `$RUNNER_TEMP/ll-ci-sandbox/sandbox-data`;**命名以 `sandbox-data` 收尾,于是种子自带的「只许沙箱」护栏一个字都不用改** |
| 4 | `ALLOW_DEMO_ADMIN_LOGIN` 只在 CI 那一步 | ✅ 逐字见下 |

```yaml
      - name: Seed sandbox fixtures (CI only)
        env:
          SANDBOX_DATA_DIR: ${{ runner.temp }}/ll-ci-sandbox/sandbox-data
          SEED_BASE_URL: http://127.0.0.1:4310
        run: node tools/ci-seed-sandbox.mjs

      - name: Run all regression suites
        env:
          SANDBOX_DATA_DIR: ${{ runner.temp }}/ll-ci-sandbox/sandbox-data
```
**`ALLOW_DEMO_ADMIN_LOGIN` 这两步都没写** —— 它由 `start-sandbox.sh` 自己设,**只活在那台沙箱进程里**,
CI 的库域是 `ci`(∈ `DEV_SCOPES`),不是生产。

**夹具两面验**(临时沙箱上实跑,不按名字猜):种之前 B店顾客 0 / A店样本单 0 / demo_seed 0 → 种之后 **1 / 176 / 185**。

## 5️⃣ 兜底队列

**没做** —— 整夜都在 (丙) 与 CI 上。**代价说清:兜底队列 0 件。**

## 6️⃣ 我新发现的问题

### 量出来的
1. 🔴 **`test-admin-accounts` 的凭证文件路径焊死在 `apps/api/local-data/`** —— 而回归跑在临时库上。
   **它一直在读店主本机那份凭证,去验另一个库的自举。** 而且 `else` 分支 `checks += 3` **不打 ok 行**(静默)。已修两处。
2. 🔴 **全仓 4 处硬写沙箱库路径**(第一轮我只改了 2 处)—— 已全部走唯一出口,重扫 0 残留。
3. 🔴 **`seed-jics-nail` 跑不通**:`PUT /admin/kb/facts → 400 UNKNOWN_KB_KEY: currency`。**立案 D205。**
4. 🔴 **我自己的还原指纹判据在 CI 上误红**:沙箱故意住临时目录,而它一律拿仓根比 ⇒ `restore_sandbox` 回 1 ⇒ `set -e` 让整轮**无声退 1**。**判据没错,期望值取错了。** 已改成跟 `SANDBOX_DATA_DIR` 走。

### 推出来的
- **那 12 支 `seed-*.mjs` 里大概率还有别的坏的**(J-102)——D205 是第一支被跑到的。**没量过,是推的。**

## 7️⃣ 要店主裁的

| # | 事 | 挡不挡上线 |
|---|---|---|
| 1 | 🔴 **推后那次备份我没赶上**:CI 一绿 Railway 立刻开始部署,我去补做时容器已 `stopping`。手上最新的是 **09-18 那份**(5,918,720 字节,sha256 已验,五表当时与生产逐项对上);而生产库 mtime **从 8/27 起就没动过**,所以那份在数据上是当期的。**要不要现在补拍一份「推后」的**,归你定。 | **已上线,不挡** |
| 2 | **D205**(`seed-jics-nail` 坏了)· **J-102**(12 支种子逐支冒烟进回归) | 不挡 |
| 3 | **兜底队列 0 件** —— 签署留档计划 / 40 口破坏测试 / 11 条归口 / D199 / J-60 那 23 处,全欠着 | 不挡 |
| 4 | **合同图 v1 四屏店主看不到** —— 我这一夜没查(在 (丙) 上) | 不挡 |

---

**三行口径**
```
本机全量:128/128 · 4,146 条断言 · not ok 0(店主这台 Mac)
GitHub CI:🟢 绿(run 35494233911)
GitHub CI 未跑:0 套
```
