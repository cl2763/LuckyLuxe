# 10d 回执 · 2026-09-20

**点名(J-81):** `小批10d_我量了那51行有没有牙齿_批开第二步_D207立案_立J105_记我第37笔_2026-09-20.md` md5 `6c8baa72529a1698d380c262c7caae3d`(桌面/仓一致)
**合同图(每批重贴):** 签署文件留档 v2 → https://claude.ai/code/artifact/6366076a-97e4-4af4-b711-cc2e7468e94f

---

## 1. 🔴 §〇 那个闸:`tenantId === null` 到底有没有人调 —— **没有,并且造病台实测坐实**

### 静态面:三个调用点,一个都到不了那个分支

| 调用点 | 传的第三个参数 | 能不能是 `null` |
|---|---|---|
| `wecom-conversation.mjs:114` | **不传**,走默认 `currentTenantId()` | ❌ `currentTenantId()` = `(store && store.tenantId) \|\| DEFAULT_TENANT_ID` —— **回落,不回 null** |
| `local-server.mjs:5678` | `loginTenantEarly = validTenantId(body.tenantId)` | ❌ `validTenantId` 末行 `return defaultTenantId` —— **回落,不回 null** |
| `local-server.mjs:3317` | 注入给 `createWecomRecord` | ❌ **该模块零调用点**(只在解构形参里出现一次)—— 死注入 |

**⇒ 全仓没有任何一条路把 `null` 传进去,而且两个默认值来源都不产生 `null`。**

### 🔴 造病台实测(J-58①:造得出阳性才算)

在临时 ci 库上,把生产那 51 行的**形状原样复制**一份,再从 `lucky-luxe` 侧走正门登录:

```
造病:users.tenant_id=demo-x   而 user_identities.tenant_id=lucky-luxe   (与生产那 51 行一模一样)

🟢 阳性对照(真属于 lucky-luxe 的人)→ 认到 vG        ← 探针咬得动
🔴 那 51 行的形状              → **没认成 vX**,新建了一个 lucky-luxe 顾客
```

> **结论:那 51 行是脏数据,不是登录洞。它们在店主店名下挂着,但没有牙齿。** —— 与你的判断一致,而且这次是走完整条正门量出来的。

⚠️ 顺带一句如实的:探针那一次**新建了一个顾客**。那是小程序 mini-login 的正常行为(新 openid = 新顾客),
**小程序本来就是开号面**,与「网页顾客端不许加建号口」那条红线是两件事。这一句我单独说明,免得看着像踩线。

## 2. `DEFAULT 'lucky-luxe'` 同类扫尽 —— **31 张表,而且我把「症状」也量了**

对照两份生产备份(推前 09-18 那份 79 表 / 推后 09-20 那份 92 表):

```
推前:31 张表带 DEFAULT 'lucky-luxe'
推后:0 张   ← 这次部署一次去干净了
```

<details><summary>31 张表逐名(点开)</summary>

`stores · technicians · services · bookings · wechat_conversations · quote_requests · reminder_tasks ·
ai_response_feedback · ai_conversation_states · ai_learning_examples · finance_transactions ·
finance_recurring_rules · staff_compensation · stored_value_transactions · admin_accounts ·
membership_packages · coupons · coupon_grants · staff_nudges · attendance_records · store_wifi ·
salary_plans · salary_payrolls · service_notes · points_transactions · points_prizes ·
service_categories · service_prices · recharge_tiers · users · user_identities`
</details>

### 🔴 但你说「这一问比那 51 行本身重要得多」——所以我没停在列名单,我去数了后果

对这 31 张表逐张查「**这一行的租户,和它父对象的租户对不上吗**」(生产只读):

```
🔴 父子租户对不上的行,全仓合计 52 行
   user_identities  user_id→users : 51    ← 已知的那 51
   bookings         user_id→users :  1    ← 🔴 新的
```

### 🔴 那 1 行是真的,而且不是演示数据

```
booking_mt1iixg3_046wjx
   tenant_id      = jics-nail          ← 订单挂在小婕的店
   user_id        = user_mt1iifi2_8970pu
   users.tenant_id= lucky-luxe         ← 但这个人记在半径名下
   status = CONFIRMED · source = wechat_miniprogram · demo_seed = null · 2026-08-20
```

**这就是 `jics-nail` 唯一的那一单。**(真店基线里 `jics-nail: bookings=1`,就是它。)

**那个人查清了**:名字 `LL-I28970PU`(小程序自动生成号)· **无手机号** ·
**0 结算 / 0 储值 / 0 小记**(没有钱挂着)· 共 2 单 · 同形的 `LL-%` 顾客 lucky-luxe 名下共 3 个。

**看起来是一次试用登录(很可能是你或小婕自己点的),但这一句是我推的,不是量的 —— 请你认一下是谁。**
🔴 **不管是谁,事实成立:小婕店里唯一的订单,顾客记在你店名下,她那边看不见这位顾客。**

**我没动生产。** 清不清、怎么清,连同那 51 行一起等你裁(按 10d §一 三步走)。

### 🔴 一处我量不到的,如实写明

`ai_response_feedback`(97/97 全在 lucky-luxe)与 `ai_learning_examples`(68/68)**没有父对象可对**,
所以这把尺子**看不见它们有没有串味**。不是「验过没问题」,是**验不到**。

## 3. J-105 · D206 甲档那两处钱 —— **改完了,而且现测过**

`actorOf` **抽成唯一出口** `apps/api/actor-name.mjs`(10b 我在 `signed-docs.mjs` 里写过一份,
这次抽出来,**不并排留第二份** —— 判据 ④c6 盯着「全仓 `function actorOf` 只许一处」)。

**三处涉钱路径全改,主钥匙现测落库值:**

| 路径 | 改前 | 改后(现测) |
|---|---|---|
| 现金手记**建单** | `\|\| ''` → **空串** | ✅ `平台主钥匙` |
| 现金手记**冲销** | `\|\| ''` → **空串** | ✅ `平台主钥匙` |
| 「**记一笔**」 | `\|\| 'owner'`(乙档,但涉钱) | ✅ `平台主钥匙` |

**乙档 66 处已分级**(完整清单在 `handoff/D206_主钥匙留痕普查_2026-09-20.md`):
🔴 **涉钱 40 处** · 🟠 不涉钱 26 处。
建议先做这几族:`refund-routes`(退款 3)· `finance-reverse`(冲销 1)· `daily_closes`(3)· `salary_payrolls`(3)· `settlement_*`(5)。
**但最省事的仍是第三条(10c 提过、还没裁):让 `requireAdmin` 的主钥匙分支自带身份,70 处一行不改就都对了。**

## 4. 🟢 第二步(顾客端只读)—— **做完了,两端都做,97 条判据全绿**

### 后端(`/my/*` 只读两条)

```
GET /my/signed-docs                      列表
GET /my/signed-docs/:id/pages/:n         取页
```
**没有第三条,没有任何写口。** 四个条件全写进 SQL(不是取出来再判):
`user_id = ? AND tenant_id = ? AND status='active' AND customer_visible = 1`

### 10b §四 七条 + 10d §二 点名那两条,逐条

| 断言 | 阳性对照 |
|---|---|
| ⑫a 顾客看自己的 → 200 **且看得到东西** | 零命中不算通过 |
| ⑫b 🔴 **作废的不显示** | —— |
| ⑫c 🔴 **`other` 默认不给看**;手动开了开关的那份看得到 | 同一条里正反都验 |
| ⑫d 🔴 顾客形状**不带商家侧字段**(`created_by`/`voided_*`/`customer_visible` 零出现) | —— |
| ⑫f/g 🔴 **条件二(同一家店)**:他店本人 → 拒 | ⑫f 先造阳性:A 自己取这一页 → 200 |
| ⑫h 🔴 **条件一(本人)**:同店他人 → 404 | 与 ⑫f 成对 |
| ⑫i 🔴 不登录 → 拒 | —— |
| ⑫j/k 🔴 取页那条路**自己再挡一遍**(作废的 / 不可见的) | 读写两道闸同族 |
| ⑫写口×4 🔴 `POST`/`DELETE` 四条**不存在** | 裁#104:只断言不存在 |
| ⑫m 🔴 两个条件**在 SQL 里同时出现** | 取出来再判就会有人忘了判 |
| 白名单 🔴 `/my/*` **只许两条**,逐条逐字对上 | 新来的自动红 |

### 两端界面(双端同批律)

- **网页顾客端**:`apps/web/customer-signed-docs.js`(新文件,**自己挂载**)
- **小程序顾客端**:`components/signed-docs-mine/`(新组件),`pages/me/index.wxml` 只加一个标签

🔴 **两个超线文件一个字没加**:`customer.js` **2,458 行(超 1,500 上限)**、
`me/index.js` **591 行(上限 600,只剩 9 行)** —— 公约③「超线文件不许再加新功能」。
判据 ⑬f 盯着这两个文件里不许出现 `signed-docs` 字样。

⑬a–⑬h 另守:两端零写请求 · **两端都不传任何 id** · 渲染链闭合 · 两端零拼串 ·
连按钮都不给 · 网页那侧照抄了 D77「换店就当没登录」· **拿不到就整块不出现,不留空壳**。

## 5. 🔴 我在同一个毛病上栽了**第四次**,这次改的是函数不是补丁

⑬e 又红了 —— 尺子咬中的是**我自己写在 wxml 注释里**的「没有上传/删除/作废按钮」那句话。
`codeOnly()` 剥 `/* */` 与 `//`,**不剥 wxml 的 `<!-- -->`**。

前三次(⑥e / ④c3 / ⑨a)我是就地补一条,第三次才抽出 `codeOnly`;
**这一次补进函数本身** —— 注释有三种写法,一处治全部,不再一种一种打补丁。

## 6. 🔴 回归又抓了我一件 —— **门关档少跑一套**

```
not ok ①a 底数闭合:**该跑 21 套 · 跑了 20 套 · 该跑没跑 1 套** —— signed-docs
```

规则(现读 `test-demo-gate-coverage.mjs:56-61`):**碰顾客登录态 + 起真服务**的套件必须进门关档。
第一步时 `test-signed-docs` 只打 `/admin/*`,不碰顾客登录;**第二步用了正门登录,两条都占上了,却没进那一档。**

**做对的动作是把它加进 `DEMO_GATE_SUITES`,不是给它开豁免** —— 判据自己写着
「做对事情让『该跑』变多,不是变少」。加完两档各跑一遍,绿。

## 7. 三行口径

```
本机全量:129/129 套件 · 4,249 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:本批未推 —— 10d §六 没写推,而 §一 的清理要等你批 `SELECT` 原文;等你一句
GitHub CI 未跑:—
```

**断言增量对账(判据五):4,218 → 4,249(+31)。**
`test-signed-docs` 66 → 97 = **+31**。**数字对得上,没有一条是静默跳过的。**

**棘轮三项(回归自己量的):**
```
local-server.mjs   17,539 ≤ 17,539   (净 0:第二步只加了模块内的路由,巨型文件只改了一行注入参数)
admin.js            7,893 ≤  7,893   (没动)
小程序 wxss 写死色   2,350 ≤  2,350   (新组件零字面色)
```

---

## 要你裁的(按挡不挡排)

| # | 事 | 挡不挡 |
|---|---|---|
| 1 | 🔴 **`jics-nail` 唯一那一单的顾客记在你店名下** —— `LL-I28970PU`,无手机号、零钱、2 单。**是谁?** 认一下再定清不清 | 🔴 **这件最该先看** |
| 2 | 🔴 **那 51 行 + 这 1 行怎么清** —— 按 §一 三步走:当期备份 → 造病台用那份备份跑通 → `SELECT` 原文报批。**我一个字没写进生产** | 不挡 |
| 3 | **D207 结构性那一问**:`user_identities.tenant_id` 被写入却不参与任何解析判断。要么让它参与(加一致性断言),要么建表注释写死「冗余记录,不作判据」 | 不挡 |
| 4 | **D206 乙档 40 处涉钱** 要不要按 J-105 全改;还是走第三条(改认证层,70 处一行不改就都对) | 不挡 |
| 5 | **本批推不推** —— 10d §六 没写推;第二步已做完且全绿,要推我随时可以 | —— |
| 6 | `ai_response_feedback` / `ai_learning_examples` **没有父对象可对,那把尺子看不见它们** —— 不是验过没问题,是验不到 | 不挡 |

## 自评 L1–L5

- **L1** ⬜ 第二步两端界面**没有截图**:后端 97 条判据 + 渲染链闭合断言都绿,但**顾客端那两屏我没在浏览器/开发者工具里看过**。按 L3 挂着,不写成已核验。
- **L2** ✅ `actorOf` 抽成唯一出口(不留第二份);`codeOnly` 第四次栽后**改函数不打补丁**;`DEFAULT 'lucky-luxe'` 从 31 张表查到 52 行后果。
- **L3** ✅ 图 v2 第 5 屏六条规矩逐条落到代码与判据。
- **L4** ✅ 反例数据造得够狠:同店他人 / 他店本人 **分开各造一次阳性**;作废的、不可见的各验取页。
- **L5** ⬜ 仍没做「连续 5 次进出页面」—— 这一批两端界面根本没开浏览器,如实挂着。
