# 待裁(夜班令 6,2026-09-09 夜)· #20 起

| # | 事 | 我的倾向 | 不裁的后果 | 裁定 |
|---|---|---|---|---|
| 20 | **小程序 dots 的「选中那颗变宽」在开发者工具里量不到生效。** 现测:选中那颗的 class 确实是 `dh-dot-i on`(automator 读到的),`.dh-dot-i.on{width:28rpx}` 也在 wxss 里,但量出来五颗都是 5px(=10rpx)。加了 `flex:none` + `min-width` 之后还没能复量 —— automator 会话在复量前僵死了(D167 那件事)。 | 倾向:**保持现在这两条防御性写法**(`flex:none` + `min-width`),并在段 6 里**不再只靠宽度**表达「当前是哪一颗」:给选中那颗叠一条 4 秒走满的进度条(那是独立元素,不吃 flex 收缩)。这样即使宽度那条在某些渲染器上不生效,「看得出来在转」也成立。 | 不裁的话,店主那句「看不出来这个版面有自动轮播」可能只治好了一半。 | ✅ **已自解**:段 9 复量到「恰好一颗是胶囊」——`flex:none` + `min-width` 那两条就是修法(flex 子项的 width 会被当成可压缩的建议值)。段 6 的进度条照做,两重保险。 |
| 21 | **`mp.screenshot()` 在这台机器上必卡死**(其它 automator 调用全正常:connect / currentPage / pageStack / $$ / offset / size / attribute / data 都秒回)。 | 倾向:小程序端的验收改成**布局实测**(新刀 `tools/mp-layout-proof.mjs`:量五小数是否一行五列、大数字几段、dots 几颗、店名有没有出屏),截图那一项**如实报「交不出来」**,不拿 DOM 冒充图(J-32)。要真截图得在开发者工具里手点,或换一台机器。 | 不裁的话,小程序端每一段都会卡在「截图交不出来」上,而实际上布局是量得到的。 | |
| 22 | 🔴 **小程序「站内选深色」那一档没生效**(「跟随系统」那一档不受影响)。现测:`page.data().themeClass` 确实是 `theme-dark`、类也挂在根 view 上、`tokens.wxss` 里 `.theme-dark{...}` 那 30 个令牌一条不少(令牌刀绿),**但屏幕上还是浅色**(截图 `07截图/02_小程序_商家首页_深色.png` 可见:白底深字,只有英雄块是深的 —— 那块本来就深)。 | 倾向:**病根多半是「CSS 变量定义在非根节点上,子树读不到」**——`page{...}` 那一层的令牌是生效的(浅色态与系统深色都对),挂到里面那个 `view` 上就不灵。两条修法二选一,请裁:①**不靠变量继承**,在 `.theme-dark` 下把几个面(`.dh-card` / `.sec` / `.rowcard` / `.page`)的底色字色**直接写死一份**(值仍来自令牌表,但落成实属性);②**改用系统主题一条路**(只留「跟随系统」),站内三档只在网页端提供 —— 小程序端如实写「跟系统」。 | 不裁的话,「我的 → 外观 → 深色」点了没反应,比没有这个开关更糟。 | |
| 23 | **本机库(4128)这一轮对照出来是「有动」,但不是本批写的。** 逐行指纹:`luvia-bj · reminder_tasks · 旧行消失 6 行`(行数 80→80)。现查这 6 行是 `arrival_reminder` 由 `PENDING` 翻成 `SENT`,时间戳 00:00 / 01:30 / 03:00 / 04:30 —— **是 4128 自己的提醒调度器在跑**(`notify-scheduler.mjs`,每次扫描把到点的任务改状态)。本批对 4128 **只读过 8 个显示名,0 处写**。 | 倾向:`reminder_tasks` 与已经在白名单里的 `notify_scan_marks` **是同一族**(服务活着就会变的心跳表),建议收进 `receipt-db-proof.sh` 的心跳白名单,并把理由写在 `notify-scheduler.mjs` 那段代码旁(随码复核律)。**但我不自己加** —— 那等于「为了让自己的结论变绿去松判据」,这条口子一开,「未动」这句话就不值钱了。 | 不裁的话,只要 4128 开着,以后每一批的「本机库未动」都会红,而红的原因和交付无关 —— 久了就会有人习惯性忽略它,那比红更糟。 | |
