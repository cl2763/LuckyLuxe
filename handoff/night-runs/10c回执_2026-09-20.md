# 10c 回执 · 2026-09-20

**点名(J-81):** `小批10c_五裁_记我第35第36笔_D206立案_不抬棘轮改图_2026-09-20.md` md5 `e13f0f0f81b9131ece3f73de432dcad2`(桌面/仓一致)
**合同图(每批重贴):** 签署文件留档 v2 → https://claude.ai/code/artifact/6366076a-97e4-4af4-b711-cc2e7468e94f

---

## 1. `pricelist` 三处一致

| 处 | 现状 |
|---|---|
| 机器名 | `rights` / **`pricelist`** / `other`(`signed-docs.mjs:DOC_TYPES`) |
| 中文名 | 会员充值权益确认书 / **价格表确认书** / 其他 |
| 判据 | `test-signed-docs` ②c 断言三个 key 的顺序与 `types[1].label === '价格表确认书'`,**并写明「不是护理同意书」** |

🟢 图不用改。**是 10b §〇 那张表错了,你已更正。**

## 2. 选中点改墨绿 —— **棘轮没抬,是图改了**

- 小程序 `.sdoc-opt.on .sdoc-k`:`--brandd` → **`--accd`**;网页 `.signed-type.is-on` 与 radio 的 `accent-color` 同改(两端同一个样子)
- `test-color-usage` ⑥b:**16 条 ≤ 棘轮 16** ✅ —— **10b 抬到 17 的那一格,退回去了**
- 判据源码里留了一段说明:「**棘轮退回 16,而且不是我改回来的 —— 是图改了**」

### 🔴 深色档实拍(沙箱 4310,`prefers-color-scheme: dark`)

```
.signed-type.is-on   border-color = rgb(99, 210, 195)
.signed-type.is-on input accent-color = rgb(99, 210, 195)      ← --accd 的深色档值
```
实拍图里那个选中点是**墨绿实心 + 内环**,深底上清清楚楚。
**你那三个理由我实测都成立** —— 尤其第①条:它不靠金色在深色档上撑。

## 3. `cosDeleteObject` 已删

- `apps/api/cos-upload.mjs` **删掉 18 行**(85 行,原 99 行),导出名单从 6 个减到 5 个
- 仓里只剩一行墓碑注释(与 `registerGoogleDemoUser` 同姿态),写清了三条理由与「从 git 历史取回」的路
- 血缘护栏 `test-signed-docs` ⑪a 自动跟着变:**导出 6 → 5**,解构名单仍然一个不缺

## 4. 🔴 三份旧日备体检 —— **全绿,但真正的原因不是「侥幸」**

```
2026-09-20  5,980,160 字节  integrity=ok  journal=delete  表79 触发器19  [users77 bookings1242 payments149 identities105 settlements10]
2026-09-05  5,980,160 字节  integrity=ok  journal=delete  表79 触发器19  [同上]
2026-08-21  5,910,528 字节  integrity=ok  journal=delete  表78 触发器19  [users72 bookings1232 payments139 identities101 settlements0]
```

**三份全开得了、integrity 全 ok。但我要改你那句「侥幸」的措辞,因为量出来的原因更硬:**

> **三份的 `journal_mode` 全是 `delete`。**
> 生产是**今天 06:31 才切到 WAL** 的,而卷上 31 份日备最新那一份拍于 **04:55** ——
> **也就是说:卷上不存在任何一份「WAL 库上 cp 出来的」备份。那个危险组合从来没发生过。**

⇒ 我建议台账写成这一版(比「侥幸」准):
> **「09-20 之前的日备都是 delete 模式下拷的,结构上成立,已抽三份验过;
> 但 09-20 06:31 之后代码已改走 `VACUUM INTO`,所以 cp-on-WAL 这个组合一次都没出现过。」**

🔴 **你那条「不许拿旧日备做还原」我照样执行**,但理由换一个 —— 不是「怕它是坏的」,是:
**08-21 那份只有 78 张表、settlements 0 行,而现在是 92 张表。**
**拿旧形状还原给新代码是 schema 不匹配** —— 这正是你 10a §三.1 写的那句话,现在轮到它管旧日备。

## 5. 屏 1 像素图 —— 🔴 **没拿到,写死了**

四种办法都试过(桌面滚动 / 缩窗 430×820 / 把区块 `position:fixed` 钉住 / 钉住后切回桌面),**四次全是黑帧**。
**元素确实渲染了**(同一次调用现读):`rect {x:20,y:20,width:420,height:294}`,
`innerText` = 「签署文件 | 线下签完的纸,拍照存这儿 | 全部 2 份 › | 会员充值权益确认书 | 09-20 上传 · 平台主钥匙 | 有效 | 价格表确认书 | … | 已作废 | + 上传文件」——**与图逐字一致**。

**已按 §一⑤ 在交付清单里写死:「这一屏 `已逐字核对 · 未取得整块像素图`。」不许用逐字冒充像素。**
(同一个面板本批**拍到过弹层**,包括上面那张深色档 —— 所以不是面板不能用,是这一页在那个会话里合成不出来。)

## 6. 🔴 D206 清单 —— **只列不改,而且立案时的描述要纠正**

完整清单:`handoff/D206_主钥匙留痕普查_2026-09-20.md`

**先纠正:D206 说「留痕都会是空的」—— 量出来不是这样。**
```
主钥匙 PUT /admin/kb/facts → tenant_kb_facts.updated_by = "owner"       ← 不是空串
主钥匙 POST …/signed-docs  → signed_docs.created_by     = "平台主钥匙"   ← 10b 改过 actorOf 的
```
全仓主流写法是 **`adminSession.email || 'owner'`(49 处)**。
**我 10b 那处之所以是空串,是因为我自己写了 `|| ''` —— 那是我的写法,不是全仓的写法。**
⇒ **真正的病不是「空」,是「说不出是哪一个人」。**

| 档 | 落库是什么 | 处数 |
|---|---|---|
| **甲** | 空串 / undefined | **4** |
| **乙** | 一个角色词(`'owner'` 49 · `'staff'` 8 · `'admin'` 7 · 其他 3) | **67** |
| **丙** | 已走 `actorOf` | 1 |

### 🔴 甲档四处里**有两处是钱**

| 位置 | 写的是什么 |
|---|---|
| 🔴 `store-content-routes.mjs:68` | **现金手记 `createdBy`** —— 抽屉里钞票多了少了 |
| 🔴 `store-content-routes.mjs:75` | **现金手记冲销 `createdBy`** |
| `local-server.mjs:14485` | 小记 `technician_name`(前两级兜底通常兜得住) |
| `local-server.mjs:2100` | AI 复核标记操作人 |

**现金手记是抽屉账。一笔说不出是谁记的现金调整,在盘点差异里等于没记。**

**改法我列了三条(都没做)**,第三条最省事但改认证层,要你点头:
**`requireAdmin` 的 `OWNER_TOKEN` 分支本身带一个身份(如 `id:'platform-owner-token'`),72 处一行不改就都对了。**

## 7. 🔴 `user_identities` +76 —— **预告过,但算式对不上,而且查出一件真事**

**预告过。** 09q §③ 预告的是**算式**不是数字:
> 新增行数 = Σ(四列各自非空的 users 数) − 这四类里已存在的对应身份行数
> 「生产上的数我没量,不拿估的数填」

**现在拿生产实数对账:**
```
Σ 四列非空 users = 129   (email 69 · wechat 8 · google 7 · phone 45)
推前这四类已有身份行 = 104
⇒ 按算式预测新增 = 25
   实际新增        = 76        🔴 差 51
```

### 差的这 51 行是什么 —— 查到底了

```
推前:重复组 0 组          现在:重复组 51 组(单组最多 2 行)
抽一组:user demo-ai-cust-1 / provider email / provider_user_id 完全相同
   旧行 tenant_id = lucky-luxe   created_at 2026-08-04
   新行 tenant_id = demo-ai      created_at 2026-09-20 06:31:42
   而这个人自己:users.tenant_id = demo-ai
```
唯一索引是 `(tenant_id, provider, provider_user_id)` —— **两行 tenant 不同,所以都塞得进去。**

⇒ **开机迁移做的是对的**(按 D130 写了**正确的** `tenant_id`),
**但它没清掉 51 行旧的、tenant 写错成 `lucky-luxe` 的行。**
根因在更早:旧表 `tenant_id` 有 `DEFAULT 'lucky-luxe'`,不带租户的 INSERT 全落进了店主的真店。

**影响面(全部量过,不是推的):**
```
串味 51 行 / 共 181 行 · 涉 27 个人
按店分:lucky-luxe←demo-ai 48 · ←demo-jienail 2 · ←demo-lucky-luxe 1
凭据长相:@example.invalid 24 · 138000… 24 · demo-o… 3     ← 全是种子/演示凭据
两家真店:jics-nail 身份行 0 · lucky-luxe 名下 105 行,其中 51 行主人不在本店
```
🟢 **零真实顾客受影响。** 🔴 **但店主的真店名下,挂着 51 行属于演示店的人的身份行。**

**我没动生产**(只读)。**这要你裁:清不清、什么时候清。**
⚠️ 顺带一个小发现:`demo-ai` 这家店在 `tenants.kind` 里标的是 **`real`**,名字却是 `demo-` 开头 —— 单独记一笔。

## 8. 收尾 → 推 → 轻量六项 → 等你批

见本回执末尾「推后核验」一节(推完补)。

---

## 三行口径

```
本机全量:(见末尾,推前那一跑)
GitHub CI:(推完填 run id)
GitHub CI 未跑:
```
