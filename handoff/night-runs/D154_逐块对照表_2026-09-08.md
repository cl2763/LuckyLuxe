# D154 · 图 §三 逐块对照表(17 行)—— 图上每块 → 页面上哪个节点

> 2026-09-08 · 沙箱 4310 `/admin`,真登录(OWNER_TOKEN 口),1440×900
> 判据锚**选择器**不锚文案;静态判据在 `test-dashboard-home`(44 条),排版由截图背书。

| # | 图 §三 的块 | 页面节点 | 现测 |
|---|---|---|---|
| 1 | 顶行旧卡(已确认/待支付/月服务/总服务) | `#metricGrid` | **子节点 0**(渲染代码已删,不是只隐藏) |
| 2 | 独立 AI 日报块 | `#aiBriefPanel` | 首页 `hidden` |
| 3 | 「演示数据」按钮 | `#fullDemoSeed` | 首页 `hidden` |
| 4 | 英雄区两栏 | `[data-dh-hero]` | 现测 `550.93px 479.06px` 两列 |
| 5 | 周期条 + ⤢ 全屏占位 | `[data-dh-periods]` / `[data-dh-full]` | 四档在;全屏钮 `disabled` |
| 6 | 大数 + 币种小字 | `[data-dh-metric="revenue"]` / `[data-dh-cur]` | `CAD $25885` + 小字 `CAD` |
| 7 | 月目标 % | — | **不出**(门店没有这个配置项,编一个就是假数;已登记) |
| 8 | 比上期(人话) | `[data-delta]` | 「▲ 505.1% **比上月**」;上期 0 时「上期 0,没法比」 |
| 9 | 近 N 点折线 | `svg[data-dh-spark] circle` | 本月 **12 点**;今日 7 点;全 0 不画(空店现测 0 个) |
| 10 | 此刻四格 | `[data-dh-hero-left] [data-dh-now]` ×4 | 4 个,在左栏内 |
| 11 | 下一位卡 | `[data-dh-next]` / `-time` / `-go` | 有;没有下一位时出 `[data-dh-next-none]` |
| 12 | 右四小牌 | `[data-dh-tiles] [data-dh-metric]` ×4 | 现金 `CAD $25986` / 总卡耗 `CAD $0`(次卡「—」)/ 新增持卡 0 人 / 到店人次 48 人 |
| 13 | AI 今日一句(通栏) | `[data-dh-ai-line]` \| `[data-dh-ai-none]` | 现为「今天还没有一句」——日报**没有落库**,首页不为它新起模型调用(已登记待裁) |
| 14 | 今日台面(左下) | `[data-dh-board] #todayBoard` | **原样嵌入** `TodayBoard.mountInto`;根节点 `#todayBoard` 与工作台同名 |
| 15 | 今日要处理(右下) | `[data-dh-todo]` / `[data-urgent="1"]` | 客服待人工排前;0 项无节点;全 0 出真话 |
| 16 | 底部两栏 | `[data-dh-bottom]` | 台面 : 要处理 = 1.6 : 1 |
| 17 | 休息日 | `[data-dh-state="closed"]` 在左栏内 | 「今日休息」替换此刻四格与下一位;大数/小牌/要处理照出 |

## 顺手治好的两处「看起来没问题」的坑

- **宿主是三列图表网格**:`#dashboardCharts.dashboard-chart-grid` 原来 `display:grid` 三列,
  我的三个 section 被塞进三个 351px 的格子里 —— 竖着看是「三块叠着」,横着看是「切成三条」。
  改成块流才排得开。**这一条在 DOM 证据里看不出来**,只有开页面才看得见(这正是 D154 的由来)。
- **浏览器里存着别的店的会话**:段 6 我读的 DOM 其实是「开单走查店」(`zj-open-cad`)的数据 ——
  `request()` 用的是 localStorage 里的旧登录态,不是我以为的 OWNER_TOKEN。
  这一版清了会话重登,才是 `LUVIA 半径` 的真数(现测 `/admin/auth/me` 逐次确认)。

## 未交(如实)

- 三店各一张、窄屏、失败态三张截图**没交**:本轮只交了旗舰店今日/本月两张与休息日态。
  原因:这一轮时间都花在把布局按图排出来 + 追那两处坑上,截图批量补在下一轮。
- 「客服待人工 660 → 1」已做(见回执),口径改成只数 `needs_human`。
