# 小批 11f + 11f-补 回执 · 2026-09-22

**点名(J-81):**
- `小批11f_四样全齐了_代理可以真接_公安备案是新增一件_2026-09-21.md` md5 `614cb139e4c9b0eb1e622d851f41bb33`
- `小批11f补_别装Caddy那台机跑着官网_记我第43笔_立J110_2026-09-21.md` md5 `63ab6e2d9b3fd4539df2692b8e4ce760`
(桌面/仓两头一致;有冲突以 11f-补 为准)

---

## 🔴 第一句话(11f-补 §五 要求):**官网从头到尾都活着**

| 时刻 | `https://www.jingshengyouji.com` |
|---|---|
| 动任何东西**之前** | **HTTP 200** · TLS 有效 |
| 装完 api 配置 + reload **之后** | **HTTP 200** |
| 签完证书 + 再 reload **之后** | **HTTP 200** |

`jingshengyouji.com`(裸域)全程 **301 → www**,与原配置一致。**官网那份 `jingshengyouji.conf` 一个字没碰。**

---

## 🟢 代理接通了

```
api.jingshengyouji.com  →  北京 82.157.154.185 的 nginx  →  https://www.luckyluxeatelier.com
```

**四条验收,三条我验了,一条我验不了:**

| # | 条件 | 结果 |
|---|---|---|
| 1 | `/health` 的 commit 与 Railway 一致 | 🟢 **两边都是 `ed93184`** |
| 2 | 真证书,不是自签 | 🟢 Let's Encrypt · `api.jingshengyouji.com` · 到期 **2026-12-20** · `curl` 校验链 TLS 0(**没加 `-k`**) |
| 3 | 🔴 **境内实访** | ⛔ **我验不了 —— 我这台在境外链路上。要你或小婕用手机流量打开一次。** |
| 4 | 小程序真的请求得通 | 🟡 `API_BASE` 已改;**开发者工具里跑通那一次还没做**(要先在微信后台填合法域名,而那个一个月只能改 5 次,所以等你确认第 3 条之后再填) |

🔴 **第 3、4 条没过,所以我不说「通了」。** 说「**代理这一侧通了,还差境内实访与小程序跑通**」。

**另外两条自己加的验收:**
```
不许转的:/platform/overview · /sandbox/demo-roster · /wechat-simulator  → 全 404 🟢
该转的:  /health 200 · /stores 400(要店号)· /my/signed-docs 401(要登录)
         🔴 400/401 正是「真打到后端了」的证据,不是错误
```

## 做法(照 11f-补,**没装 Caddy**)

1. **新增** `/etc/nginx/sites-available/api.jingshengyouji.conf`,官网那份没碰
2. `nginx -t` → `systemctl reload nginx`(**不是 restart**)→ 立刻回头量官网
3. `certbot --nginx -d api.jingshengyouji.com` —— **复用已有账户与 certbot.timer,没另起一套**
4. 再 `nginx -t` → reload → 再量官网

### 🔴 转发口径:我按令做了白名单,但要先说一件事实

**后端今天已经整个暴露在公网上了**(`www.luckyluxeatelier.com` 谁都打得到)。
代理加的是**一条境内路由,不是新的暴露面** —— 所以「只转必要路径」在安全上收益有限,在**漏转**上风险不小。

**所以我这样收:白名单**(26 个前缀,**从后端路由定义量出来的,不是凭记忆**)**+ 名单外一律 404 并记进单独日志** `api.unmatched.log`。
⇒ **漏转会变成一行看得见的日志,而不是一个静默坏掉的功能。**

## 🔴 11f-补 §二 那张表,我逐条量过,两条与令里写的不同

| 令里写的 | 现测 |
|---|---|
| ① DNS 加 `api` → **「这一条还没有,唯一还要她点的一下」** | 🟢 **已经有了** —— `api.jingshengyouji.com` 解析到 `82.157.154.185`。**她早点完了** |
| ② 防火墙放行 80/443 | 🟢 已放行(nginx 在 80/443 上真的在听) |
| ③ 微信后台填合法域名 | 🟡 仍要等第 3 条验过再填 |
| ④ 公安联网备案 | 🟢 她早在办(夹子里有指引 + 填写对照表 + 运行截图) |

**⇒ 那「唯一还要她点的一下」其实也点完了。我没让她重做。**

## 🟢 11f §二 那条红线:一句话结论

> **全站不存在任何「不登录就能写 `users` / `user_identities` 行」的路。**

`test-user-write-auth` 现跑 **11/11 全过**,含「写 users 且租户取自 body」那 3 条**逐条在具名白名单里,新来的自动红**。
D194 已下线(生产容器里 `grep -c` 两个串都是 0),D201 已定性。**这次是真对外开门,所以我重新跑了一遍,不是引用旧结论。**

## §四 证书那件真风险 —— **做了一半,另一半等你一句话**

```
官网证书  到期 2026-12-17(剩 86 天)   api 证书  到期 2026-12-20(剩 89 天)
注册邮箱  没绑(--register-unsafely-without-email)⇒ 到期不会有任何邮件提醒
```

- 🟢 **告警做了**:`tools/cert-expiry-check.mjs`(唯一出口)+ `test-cert-expiry` **9 条两面验**
  (剩 20 天必报 · 剩 22 天不许报 · 恰好 21 天不报 · **取不到也必须报**)。现跑:官网 86 天 · api 89 天 · Railway 34 天,都够久。
- ⛔ **绑邮箱那一条没做** —— 11f-补 写着「**邮箱由店主给,你不许猜**」。**给我一个邮箱,我一条命令跑完:**
  `sudo certbot update_account -m <你的邮箱> --agree-tos`

## 🟡 顺带确认 11f-补 §三 那个猜测:**是的,就是那把钥匙**

我上一批说「SSH agent 掉了」。现在确认:**那是 Railway 那把 `luckyluxe-ops`**,不是这把 `youji_tencent`。
两把是不同的钥匙、不同的机器 —— **这台北京机器我是用 `youji_tencent_ed25519` 直接 ssh 进去的,全程没走网页终端,也没打印过任何密钥内容。**

## §一 公安联网备案 —— 🔴 **11f 说它是「新增的一件」,那句话是错的**

11f-补 §〇 已经自己纠正了(记第 43 笔)。**我这边确认:夹子里早就有**
`公安备案_全流程指引.md` · `公安联网备案_填写对照表.md`(逐字段填好了)· `公安备案_运行截图/`。
**她做过的功课在那儿,不是新增的一件。**

---

## 三行口径
```
本机全量:133/133 套件 · 4,329 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:本批不推 —— 改动面是 `miniprogram/utils/api.js` + 工具 + 判据,
          **没有一行后端业务代码**;小程序包不经 Railway 发,推了只是白换一次生产重启
GitHub CI 未跑:—
```
**断言增量(判据五):4,320 → 4,329(+9 = `test-cert-expiry` 九条)· 套件 132 → 133。对得上。**

## 要你做的(按能不能立刻动排)
| # | 事 | 为什么非你不可 |
|---|---|---|
| 1 | 🔴 **用手机流量打开 `https://api.jingshengyouji.com/health`** | 境内实访我验不了。回一句「打得开/打不开」就行 |
| 2 | 🔴 **给一个邮箱**(证书到期提醒用) | 令里写死「不许猜」 |
| 3 | 第 1 条通了之后,**微信后台填合法域名** `https://api.jingshengyouji.com` | 一个月只能改 5 次,所以要等验过 |
| 4 | 公安联网备案(30 日内) | 法定动作,不挡技术链 |
