# 夜班总结(2026-09-08 · 夜班令 5)

> 一段一行。店主早上只看这一份;细节在各段回执与 `进度_2026-09-08.md` 里。

## 每段一行

| 段 | 结果 | 提交 | 一句话 |
|---|---|---|---|
| 7a | ✅ | `333e38c` | **D150 出口薄壳**:`handleWecomInbound` 改名 Core、原名变壳,14 个 return 一个没动 —— 报价路那条**真的被守住了**(上一批就是漏在它身上)。改写只改流水里同一行,原话进 `meta`。 |
| 7b | ✅ 一半 | `3404f03` | **D152 反面那一半上闸**:没折扣却说「券后」当场拦。🔴 正面那一半**做不出来** —— 三段式在现引擎**没有出口**(问价会先进报价采集、最后转人工),已报待裁。 |
| 7c | ✅ | `333e38c` | 合并窗改 **4 秒 / 来一句刷新 / 12 秒封顶**,`/health` 报三个数;回归加 **5 分钟看门狗**(上一批「22 分钟才发现是自己开的窗」以后由脚本替我发现)。 |
| 7d | ✅ | `dd332d1` | **六通 v4 落仓**:`handoff/ai-eval-results/05q_像人六通_原文_v4_2026-09-08.md`(通五改连发三句验合并窗,通六北京店带新客券)。 |
| 8 | ✅ | `0b0d7c4` | **D149 平台改密码登录**:令牌从此只给脚本 / API,登录框只认账号密码;两库各建 `platform-admin`(密码只进本地文件),首登强制改密。 |
| 9 | ✅ | `bb9ba5d` | **小程序老板视角按图 §一 重画**,旧块整段退役;顺手咬出**币种红线一处**(换店后人民币店被显示成加元的样子)——改法是让后端下发 `currencyDisplay`。 |
| 10 | ✅ | `205eebf` | **员工打卡门**:打卡不再是待办,是门;没打卡没有大屏,打卡失败不许假装打了,休息日不出钮。 |
| 11 | ✅ | `0ba659a` | **前台全屏大屏**(图 §五):顾客可能看到 → **现金业绩与新增持卡默认不显**;这里是全仓**唯一允许轮询**的地方。 |
| 12 | ✅ 一半 | 本次 | 店主读版一页 `handoff/05r_主页重画_店主读版_2026-09-08.md` + 四张图。①平台 AI 包状态 UI **没做**(要你先用密码登录点一遍才有意义,留给下一批)。 |

## 待裁(#6 起,共 3 条)

- **#6 段 10 那三张打卡门截图是「造态图」,不是真员工登录** —— 沙箱库里没有员工账号。
  下一批建个员工测试号再跑真登录 + 真打卡。
- **#7 合并窗封顶只能拿短参数验**(1 秒窗 / 3 秒封顶),不是真的 4 / 12 秒;
  参数由静态判据守、机制由行为判据守,两层合起来才完整。
- **#8 我自撤的一条**:我先写「图 §五 没有可执行规格,报 Cowork」——
  **其实 §五 写得很全,是我没读到**。撤回写在原处不删:
  「报待裁」不该被拿来当没读图的挡箭牌。

(#1–#5 是前几批的,已由你裁完并落码。)

## 六通 v4 在哪

`handoff/ai-eval-results/05q_像人六通_原文_v4_2026-09-08.md` —— 跑于 09-08 周二,
每通都是新会话(J-31),通五改成连发三句间隔 1 秒(验合并窗),通六在北京店、店里带新客券。

## 收尾状态

- **回归**:最后一跑 **107 套全绿**,断言 **3,608**,整跑 2 分 20 秒。
- **服务已还回去**:4128 活 · 4310 活且库路径是 `apps/api/sandbox-data/lucky-luxe.sqlite`。
- **棘轮**:`local-server.mjs` **17,711**(与上一提交持平;本夜新增的东西全在新模块里)·
  `admin.js` **7,928**(没动)。
- **造病备份**:`knife-backup.sh check` 零残留 `.pre-k`。
- **未推 main · 未碰生产 · 未看密钥文件。**

## 「未动须有证」—— 本机库**有动**,四处逐条说清

不含糊:今晚**本机库确实有动**,四类,没有一类是「悄悄改了数据」。

| 动的地方 | 为什么 | 是不是本夜代码写的 |
|---|---|---|
| `conversation_messages` 全表指纹变了(1,293 行) | **D150 给它加了 `meta` 列**(改写留证用)。加列会让每一行的指纹都变。**现测 `meta` 非空的行数 = 0** —— 一行内容都没被改写过 | 是(schema 迁移,按交付纪律 8 走 ALTER,老库也要跟上) |
| `platform_accounts` 0 → 1 | **D149 自举的 `platform-admin`**,写前已备份(`/private/tmp/ll-backup-本机库-d149-*.sqlite`) | 是,且是店主 05q §三 明令要建的 |
| `notification_logs` +8 · `notify_scan_marks` +6 | 提醒调度器在跑(服务心跳族) | 否 |
| `reminder_tasks` 8 行状态变了 | 同一个调度器把 PENDING 发出去改成 SENT(最新一条 06:00) | 否 |

**生产库未动**(整夜一次都没连过);**沙箱库有动**(评测、截图、现测留下的会话与夹具,那是它的本职)。

## 我自己犯的三个错(都写在回执里了)

1. **段 10 第一把刀根本没落**:替换锚点写错了,「没红」被我当成「判据弱」。
   第二次给替换加了 `assert` 才真落刀、真红 —— 刀留痕律说的就是这件事。
2. **段 11 没读图就报待裁**:上面 #8,已自撤。
3. **段 9 第一轮三张截图一模一样**:平台主钥匙没带换店头,三张拍的是同一家店。
   截图刀因此加了一条自证:三张图分不开就当场红。
