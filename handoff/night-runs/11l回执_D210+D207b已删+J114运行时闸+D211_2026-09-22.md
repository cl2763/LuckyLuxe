# 小批 11l 回执 · D210 · D207-b 已删 · J-114 运行时闸 · D211

**上游令**:`handoff/小批11l_两条语句不跑改走正门_立J115_店主四件落地_D210D211_2026-09-22.md`
**md5 两端一致**:桌面 `8212987c1b1ceac379c991583355792d` · 仓库同值
⚠️ 令里没给 md5(消息只给了抬头),**这个值是我自己算的**,如实标出来。

> **生产写入两件事,各自独立备份,没有合拍**:
> · D207-b 删除 → `backups/lucky-luxe-20260922T084103Z-pre-D207b.sqlite`(6,139,904 字节 / 94 表 / 已验证可打开)
> · 68 张未来单 → `backups/lucky-luxe-20260922T085753Z-pre-future68.sqlite`(同上)——**但那一件最终没执行**,备份留着
> 本机库与沙箱库对照表见文末。

---

## §五 七件,做完七件

| # | 事 | 结果 |
|---|---|---|
| 1 | **D210** 平台建店表单 | ✅ 四个框 + 必选无默认 + **27 条判据新套件** + 真点一遍走查 |
| 2 | **D207-b** 8 行 | ✅ **五条全中 ⇒ 已批 ⇒ 已在生产删除**,四项核验全过 |
| 3 | **68 张未来单** | 🔴 **条件③ 不成立 ⇒ 不批,没删**(见下) |
| 4 | **21 个账号停用** | 🔴 **条件① 不成立 ⇒ 不批**,而且是**前提问题**(见下) |
| 5 | **161 单只读清单** | ✅ 量完,**答案不是 0** |
| 6 | **10 支脚本运行时自检** | ✅ 十支**逐个**造病验过 |
| 7 | **D211** | ✅ 那句写死的英文店介绍已删 + 6 条判据 |

---

## 一、D210:平台建店那一屏

**改了什么**:加**币种 / 时区 / 门店电话**三个框,`mCity` 改名「地址」(后端字段名不动),
POST body 真带上 `currency` / `timezone` / `phone`。
🔴 **币种与时区没有默认值,必选** —— 11l 原话:默认值就是这条缺陷的来源。

**新套件 `test-tenant-create-front-door` 27 条**,四组:

| 组 | 验什么 |
|---|---|
| ① | 四个框在 · **POST 真带上了**(框在但不发等于没做)· 两个下拉第一项是空 value(必选)· 前端真拦 |
| ② | 🔴 **正门建店顺手做的七件事逐条在场** —— 这张清单是**从正门代码读出来的**(J-115 推论 A),不是凭记忆列的 |
| ③ | **行为层真建一家 CNY / Asia/Shanghai 的店**:201 · **老板账号有** · 初始密码非空(**一个字符不打印**)· 到期日有值 · 币种 CNY · 时区上海 · 地址电话落进去 · 三大类 seedDefaults 真跑了 |
| ④ | 🟢 **阳性对照**:不传币种/时区照样建得出来,而且**回落仍在**(CAD / 非上海)—— 证明 ③ 不是「反正都对」,而是**这一屏传了才对** |

🔴 **④ 那组同时把 D210 的缺陷本身钉成了常驻断言**:回落一天不改,界面必须传这件事就一天不能松。

**我自己抓到一条假绿**:④c 第一版写的是 `st2?.timezone !== 'Asia/Shanghai'` ——
请求失败时 `st2` 是 undefined,`undefined !== 'Asia/Shanghai'` **照样成立**,
于是那条断言在「整个对照根本没建成」的时候报绿。改成**先要求那家店真在,再谈它的时区**。

---

## 二、D207-b:五条全中 ⇒ 已在生产删除

| # | 条件 | 实测 |
|---|---|---|
| 1 | 备份 | ✅ `…T084103Z-pre-D207b.sqlite`,94 表,已验证可打开 |
| 2 | 8 个 id 逐个存在 + `jics-nail` count=1 | ✅ 8/8 在,count = 1 |
| 3 | 结算单与账本指向 = 0 | ✅ 0 / 0 |
| 4 | 彩排 | ✅(见下) |
| 5 | 影响行数逐表 1/4/2/1 合计 8 | ✅ |

### 彩排:🔴 第一版是假对照,我重做了一次

本机库**没有**那一条,所以按**造景律**自己把景造出来。
第一次我随手挑了一位半径顾客 —— 他在半径**本来就 0 单**,于是「他的单还在(0 → 0)」**什么也证明不了**(J-58①)。
换成**在半径真有 11 单**的顾客重做:删完 **11 单一单没少**,这才是真对照。

### 生产执行与核验

```
删前:jics=1 全库单=1242 账本=1160 那位顾客在半径=1
  OK booking_slots 影响 4(应 4)
  OK booking_status_history 影响 2(应 2)
  OK payments 影响 1(应 1)
  OK bookings 影响 1(应 1)
  合计 8(应 8)   COMMIT 完成

1) jics-nail 单数 1 → 0        OK(店主知道删完是 0 单)
2) 那位顾客本人 还在            OK
3) 他在半径的单 1 → 1          OK
4) 账本笔数 1160 → 1160        OK
+) 技师鹤辰 还在 · 项目高光猫眼 还在
+) 全库单数 1242 → 1241(应 −1)
+) 那 8 个 id 逐个已消失:0 个还在(应 0)
```

---

## 三、🔴 68 张未来单:条件③ 不成立,不批

11l §三.2 条件 3 写死:「这 68 单**没有一单**的顾客是『绑了微信且 `demo_seed` 为空』—— **有一单是,这一条不批**」。

**实测**:68 单**全挂在同一个人「Lucky Member」**名下,而他**绑了微信**,68 单的 `demo_seed` **全是 NULL**。
⇒ **条件③ 当场不成立。停这一件,不等,往下跑。**

🔴 **我没有自己判断「Lucky Member 这名字一看就是假的」**(J-109:她看得见的范围才是她授权的范围)。

**顺带答了店主问的那个数(条件④)**:
🔴 **占时段** —— `booking_slots` 挂着这 68 单的有 **240 行**。
另有 `booking_status_history` 139 行 · `payments` 68 行 · **结算单 0 · 账本 0**。

---

## 四、🔴 21 个测试账号:**前提不成立**

11l §三.3 条件 1:「先读代码:账号停用的机制是哪一列哪个值(`status='suspended'`?)—— 贴那一行代码,不许猜」。

**读出来的是**:

- 那 21 个是 **`users` 顾客档案**,不是登录账号。
  生产 `admin_accounts` 一共只有 **2 个**:`boss`(Lucky Luxe Owner)与 `avalin`(Ava Lin)。
- `users` 列:`id,display_name,phone,email,wechat_open_id,google_id,tenant_id,tags_json,notes,birthday,is_migrated,legacy_total_spend_cents,avatar_url`
  —— **没有任何状态列**。`user_identities` 同样没有。
- `local-server.mjs:12933` 那行 `status === 'active' ? 'suspended' : 'active'` 是**租户**的开关,不是顾客的。
- 全仓搜「停用顾客 / disableUser / suspendUser / blockUser」→ **0 处**。
- 🟢 21 个里**绑微信的 = 0**(条件③本来会过,是条件①过不去)。

**所以店主那句「能,全停」没法直接执行 —— 被问的那个东西没有「停」这个动作。**
已写成 A/B/C/D 四选一进「等天亮」。

---

## 五、161 单只读清单:**答案不是 0,是 2 人 120 单**

16 位顾客 / 161 单。🔴 **`demo_seed` 全部是 NULL** —— 演示过滤器一单都抓不到。

| 顾客 | 绑微信 | 单数 | 期间 | 来源 |
|---|---|---|---|---|
| 🔴 **Lucky Member** | 是 | **119** | 2026-05-19 → 2031-06-12 | `-`, 小红书 |
| 🔴 **LL-I28970PU** | 是 | **1** | 2026-08-27 | 小程序 |
| 8 位「(演示)」 | 否 | 各 3 | 2026-05 → 07 | `demo-seed` |
| Tier Check / Tier Test / Lucky Luxe Owner / Mock Client / Lucky Member(另一个) | 否 | 1–4 | — | test / 小程序 / admin_manual |

⚠️ **`LL-I28970PU` 就是 D207-b 那位顾客** —— 他在半径那 1 单是 2026-08-27 从小程序下的 COMPLETED 单。

**三位技师**:Mia Chen(21 单 / 排班 1 / 可做 5 项 / 无账号)· **Ava Lin(10 单 / 排班 2 / 🔴 有员工账号 `avalin`)** ·
Lina Zhou(**130 单 / 473 时段 / 排班 0**)。作品集与首页轮播:`hero_slides` 0。

---

## 六、J-114 运行时闸:10 支逐个验

新建 `tools/never-on-production.mjs`。**判断接现成的 `data-scope.mjs` 的 `scopeOf()`**(11l:不许另造一套)。
🔴 **白名单式**:只放行 `ci` 与 `sandbox`,`local` / `production` / **`unknown` 一律拒** ——
黑名单(「不是 production 就放行」)在路径判不出来时会默默放行,那正是这条闸要防的。
走 HTTP 的先打 `/health` 把端口翻译成库路径再判(端口会骗人,只有库路径不会);**打不通就拒**。

**造病(10 支逐个,不许验 1 支推 10 支)**:

| 组 | 指向 | 结果 |
|---|---|---|
| 直连库 3 支 | **本机库**(local,最危险那档) | ✅ 全部退出码 **2** 并报 J-114 |
| 走 HTTP 7 支 | 判不出库域的服务 | ✅ 全部退出码 **2** 并报 J-114 |
| 🟢 反向守 | 沙箱库 / 回归临时库 | ✅ 放行 |
| 🟢 反向守 | 生产路径 / `/whatever` | ✅ 拒 |
| 🟢 反向守 | 造病那一轮本机库行数 | ✅ `users=206 bookings=1482` 与 11k 收批基线**一个没变** |

**判据 9 条进 `test-demo-seed-guard`**(81 → 99 项,含 10 支逐支行为断言)。

### 🔴 两处冲突,两处都是判据咬出来的

1. **我这把闸顶掉了「生产例外七条」的措辞。**
   `seed-demo-twin.mjs` 本来就有七条那道闸,而套件断言的是**它的措辞**。我插在最前面,于是先说话的是我。
   改法:**让更具体的那道先说话** —— 挪到七条之后。两向复验:
   不带 `--production-seed` → 先说「第①条」;带齐七条参数绕过之后 → **J-114 接力拦住**。
2. **我那 10 行 `process.env.X` 是新的裸解析点。**
   `test-db-target-guard` ①d 按**解析点**判(不按文件):每个目标读取都必须紧挨 `requireTarget`。
   改法不是绕过它,是**我的闸不再自己读 env** —— 接到各脚本已有的 `requireTarget` 结果常量上。
   现在 **62 个解析点,未守住 0**。

---

## 七、D211:英文店介绍

`i18n.js` 原来写死 `description: 'A refined nail and lash atelier. Store details are placeholders in this demo.'`
**发给所有租户**,而 `pages/store-location/index.wxml:13` 真会印出来。

先量(11l 要求):`stores` **没有 description / description_en 列** ⇒ 按令**只删那句写死的,不加列**。
出口改成 `wx:if="{{store.description}}"`(缺席可见律:没有就整块不出现)。

**6 条判据**:全仓零残留 · 英文档不再造 description · 🟢 **反向守**:
address/phone/businessHours 那三条**如实说明的翻译还在**(那是翻译,不是编造)· 出口有才渲染 ·
**行为层**真跑一遍英文档 → `undefined` · 🟢 店真有介绍时照样透传。

---

## 八、固定六项

**① 改了什么**:D210 表单 + 新套件 27 条;D207-b **生产删 8 行**;J-114 运行时闸 + 10 支接入 + 9 条判据;
D211 删写死句 + 6 条判据;护栏清单重生成;两把界面刀重跑补报告。

**② 在哪一层验的**:行为层(真建店、真删、10 支真跑)+ 生产数据层(只读 + 一次受控写入)+ 机制层(全仓扫描)。
**像素层没验** —— 见第九节。

**③ 同类扫尽**:解析点 62 个未守住 0 · 种子 14 支全过 · 出口 31 处 · 扫描面 195 文件,四个数都带分母。

**④ 用什么数据验的**:D207-b 彩排**第一次用的顾客在半径 0 单,是假对照**,换成真有 11 单的重做;
D210 用真建店而不是读代码;J-114 造病指向的是**本机库**(最危险那档),不是编的路径。

**⑤ 重复稳定性**:相关套件跑了 10 余轮;全量回归 1 轮全绿;两把界面刀各重跑一次。

**⑥ 残留与不确定**:「等天亮」6 条。生产上 68 单与 21 个档案**原样未动**。

**断言增量对账(判据五)**:**4,406 → 4,457(+51)**,逐套对上:
`tenant-create-front-door` **新套件 +27** · `demo-seed-guard` 81 → 99(**+18**)· `store-placeholder` 49 → 55(**+6**)。

**巨型文件:`local-server.mjs` 17,539 · `admin.js` 7,893 · `customer.js` 2,458(三个都未动)**

**门关档**:29 套红 2 套(`mini-ai-same-outlet` / `noshow-aftersales`)—— 11i 已 A/B 证过是存量。

---

## 九、L1–L5 自评

| 律 | 做到没 | 说明 |
|---|---|---|
| **L1 末端验证** | 🟡 **部分** | D210 验到**接口与数据**(真建店、六样逐个查),**没有在浏览器里打开那一屏看四个框长什么样**。 |
| **L2 同类扫尽** | ✅ | 四个数都带分母;10 支逐个而非抽样 |
| **L3 无图不结案** | 🟡 | 新建商家那一屏没有设计图,没标已验证 |
| **L4 反例数据** | ✅ | 造病指向本机库;彩排换成真有单的顾客;④ 组把「回落仍在」钉成常驻 |
| **L5 重复稳定** | 🟡 | 套件多轮;**没做「连续 5 次进出页面」**(无交互类交付) |

🔴 **L1 这一格欠了三批了**:11j 那四个输入框、11k 那两处、本批这一屏,
**我一直在证明「代码接上了」,没有一次打开浏览器看它长什么样。**
下一批用开发者工具把这三批的界面一次性截图收口 —— **这条我自己记着,不用你催。**

---

## 附 · 本机库 / 沙箱库对照表(不写「未动」)

对基线 `11i_*_收批基线_2026-09-22.json`:两库都有变化,如实贴 ——
**建表迁移 + 通知调度器心跳**;**列 / 索引 / 触发器 / 默认值四项全部持平**;
涉钱三张表(`settlements` / `finance_transactions` / `stored_value_transactions`)本批**没有任何本地写入**。
J-114 造病那一轮**特意核过**:本机库 `users=206 bookings=1482`,与 11k 收批基线**一个没变**。
**未动对照:handoff/night-runs/11l回执_D210+D207b已删+J114运行时闸+D211_2026-09-22.md**
