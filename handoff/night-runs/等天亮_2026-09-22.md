# 等天亮 —— 2026-09-22

> 建于 J-113(補令 §四):**夜里碰到需要人的地方,不等,写到这里,往下跑。**
> 🔴 每条第三行必须是**一个能用「是/否」或「A/B」答完的问题** —— 写成「请裁决」等于把活推回去。
> 店主/Cowork 醒来照这页答,一条一行。

---

## 1. 🔴 `luvia-bj` 这家店在生产上不存在 —— 11j §一 整件没跑

- **事由**:11j §一 要把 `北京市朝阳区建外街道CBD万达广场6号楼2005室` / `18536823102`
  写进 `luvia-bj` 的 `stores` 行。
- **卡在哪一步**:補令 §二 预批判别式**第 1 条就不成立**。
  生产库只读现测(2026-09-21T18:5xZ,Railway 生产容器):

  ```sql
  SELECT id,name,address,phone,timezone,currency,tenant_id FROM stores WHERE tenant_id='luvia-bj';
  -- 返回行数 = 0
  ```

  生产上**全部租户只有六个**:`demo-ai · demo-basic · demo-jienail · demo-lucky-luxe · jics-nail · lucky-luxe`。
  **没有 `luvia-bj`。** 仓库里有 `tools/seed-luvia-bj.mjs`(22,738 字节),但它没在生产上跑过。
  按預批第 1 条 ⇒ **不批,这一件停,接着跑下一件**(已照办,判据 A/B 当晚做完了)。
- **我需要你答的那一个问题**:
  > **北京旗舰店在生产上还没开出来 —— 是要我先把它开出来(跑 seed),还是这家店根本还不该上生产?(A 开出来 / B 先别开)**

  🔴 附一句给你判断用:生产上那家 **`jics-nail`(`Jie'Nail 美甲美睫專門店`)地址是真的** ——
  `长兴南街华润悦府13号楼1单元2005室（20层）`,电话是空的。
  两个地址都以 `2005室` 结尾但不是同一个地方。**如果你说的「北京旗舰店」其实指的是 `jics-nail`,那这条的答案就完全不同了。**

---

## 2. 判据 C:接口层该不该发占位值(11j §二 明写「今晚不裁」)

- **事由**:`GET /stores` 的响应里现在**原样带着** `"address":"Address TBD"`。
- **卡在哪一步**:两种口径都说得通,11j 明写今晚不裁,不许我自己选一个当成已裁。
- **我需要你答的那一个问题**:
  > **A 接口照发,前端各自挡(现状,今晚已把两端出口全挡上) / B 接口就不发占位值,出口只管渲染?**

  参考:本批已经把**小程序 6 个出口 + 网页 4 个出口**全部挡住了,所以选 A 也不漏;
  选 B 的好处是少一层依赖前端的信任。

---

## 3. 词表要不要加「待定」

- **事由**:11j 判据 A 明列七个词(`TBD`/`待填`/`待补充`/`占位`/`placeholder`/`N/A`/`未填`),
  但同一段的理由里写着「再下次是别人手打的**待定**」。
- **卡在哪一步**:按 J-107(法只授权它明写的那件事),我只实现了明列的七个,没自己加第八个。
- **我需要你答的那一个问题**:
  > **「待定」加进占位词表吗?(是 / 否)**

  加的话两端各改一行,判据自己会守住两份一致。

---

## 4. 🔴 D207-b 那 8 行(補令 §五 点名:不许预批)

- **事由**:`jics-nail` 唯一那一单记在半径名下。
- **卡在哪一步**:補令 §五 自己裁了 —— **动的是交易记录,不是两个展示字段**,
  「只删交易不动人」这个裁法的边界得有人眼确认删的就是那 8 行。
  这是 J-113 里「提前批不了 ⇒ 本来就不该排进夜班」的正例。
- **我需要你答的那一个问题**:
  > **(等你看过那 8 行之后)删的就是这 8 行吗?(是 / 否)** —— 需要我先把 8 行原文贴出来的话说一声。

---

## 5. `lucky-luxe`(多伦多那家)的真地址电话

- **事由**:生产上 `store-ontario-01` 的 `address` / `phone` 仍是 `Address TBD` / `Phone TBD`。
  11j §四 说种子三处硬编码今晚不改,等真值拿到了三处一起换。
- **卡在哪一步**:我没有这家店的真地址电话,而 J-109 死线明写 11j 那句授权**只到 `luvia-bj` 一家**,
  不许顺手推广。
- **我需要你答的那一个问题**:
  > **多伦多那家的真地址和电话是什么?(给值即可;或答「先空着」)**

---

## 6. 11i §五 裁决表 13 条还没画勾

`handoff/11i_五_lucky-luxe残留清单_只量不改_2026-09-22.md` 末尾那张表。
其中第 9、10 两条(68 张 2030/2031 未来单 · 21 个测试账号)和本页第 4 条同族。

- **我需要你答的那一个问题**:
  > **那张表你打算今天过一遍,还是要我先把最急的三条挑出来单独问?(A 我自己过 / B 你挑三条)**
