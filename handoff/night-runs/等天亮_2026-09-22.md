# 等天亮 —— 2026-09-22(11k 之后更新)

> J-113:夜里碰到需要人的地方,不等,写到这里,往下跑。
> 🔴 每条第三行必须是**一个能用「是/否」或「A/B」答完的问题**。

## ✅ 11k 已裁掉的(留档,不用再答)

| 原第几条 | 裁法 |
|---|---|
| 1 `luvia-bj` | **A 开出来**,但🔴**不许跑 seed**(立 J-114)→ 转成下面第 1 条 |
| 2 判据 C | **A:接口照发,前端各挡** —— 附带的出口普查判据**本批已做** |
| 3 「待定」 | **加** —— 本批已加,仅此一词 |
| 4 D207-b | **贴** —— 本批已贴,转成下面第 2 条 |
| 5 多伦多真值 | **先空着** —— 降级为欠账,不再算风险 |
| 6 11i 裁决表 | **B:Cowork 挑三条** —— 转成下面第 3 条 |

---

## 1. 🔴 北京旗舰店:两条写入语句可以跑吗

- **事由**:11k §二.1 裁「开出来」。预批五条里①②⑤已过,③是唯一保留的人眼关卡。
- **卡在哪一步**:语句已贴 → `handoff/11k_北京旗舰店第一段_写入语句待看_2026-09-22.md`
  (只含 `tenants` 一行 + `stores` 一行;彩排已过,人和钱全为 0;幂等被唯一键挡住;别家店零影响)
- **我需要你答的那一个问题**:
  > **那两条语句可以跑吗?(可以 / 不可以 / 改一处再跑)**

  ⬜ 顺带一格:`plan` 我照抄了 `jics-nail` 的 `studio`。要给别的档就在答里说一声。

## 2. 🔴 D207-b 那 8 行,删的就是这 8 行吗

- **事由**:11k §二.4 裁「贴」。8 行原文已贴 → `handoff/11k_D207b_八行原文_只读_2026-09-22.md`
- **要点**:单子属 `jics-nail`,顾客属 `lucky-luxe`(串味就在这一行);
  钱是 `MOCK`(从没真收过);**0 张结算单、账本 0 笔**;
  🔴 **`jics-nail` 全部单数 = 1** —— 删完小婕店是 0 单(她还没开张,这是对的,但请确认她理解)
- **我需要你答的那一个问题**:
  > **删的就是这 8 行吗?(是 / 否)**

## 3. 11i 裁决表:Cowork 挑的三条

11k §二.6 挑了三条给店主,原样搬过来:

- > **① 三位技师 Mia Chen / Ava Lin / Lina Zhou 是真人吗?(是 / 否)**
- > **② 68 张 2030/2031 年的未来单,占不占排班时段?(占 / 不占 / 不知道)**
- > **③ 21 个测试账号能不能一次性全停?(能 / 不能 / 要先看名单)**

## 4. 🟡 新登记:英文档的店介绍是写死的,所有店共用一句

- **事由**:本批做出口普查时撞到 `miniprogram/utils/i18n.js:484`:
  ```js
  description: 'A refined nail and lash atelier. Store details are placeholders in this demo.'
  ```
  英文模式下**所有租户返回同一句**,而且原文里写着 **`in this demo`**。
  这属于《假数回落红线》那一族(顾客看得见的字段回落到写死值),但**不在 11k 范围内,我没动。**
- **我需要你答的那一个问题**:
  > **这一条单独立案吗?(是 / 否 / 并进下一批顺手改)**

## 5. 🟡 新登记:那 10 支「永不对生产跑」的脚本,要不要再加一道运行时闸

- **事由**:本批按 J-114 给 10 支会造人造钱的种子脚本写死了文件头,并进了回归。
  **但文件头是给人看的,脚本本身跑起来不会拦。**
  现在拦它们的是 `db-target.mjs`(不许有默认目标库)与生产例外七条,**不是 J-114 这条**。
- **我需要你答的那一个问题**:
  > **要不要让这 10 支在启动时自检「目标不是生产」,不是就拒绝执行?(要 / 不要 / 下批再说)**
