# 等天亮 —— 2026-09-22(11m + 11m 补 之后更新)

> J-113:夜里碰到需要人的地方,不等,写到这里,往下跑。
> 🔴 每条第三行必须是**一个能用「是/否」或「A/B」答完的问题**。

## ✅ 本批做完的

| 事 | 结果 |
|---|---|
| AI 修图入口三态 | ✅ 壳做完,**拨 `on` 会 409**;26 条判据;三态行为逐档验过 |
| L1 截图收口(欠三批) | ✅ **3 张真图**,而且新加的「可见性判据」当场拦下 2 张假图 |
| 北京店永久 | ✅ 11 处 `plan_expires_at` 逐处量过,**NULL 一律当不过期**;入口**已存在**,不用新加按钮 |
| 两家店全景盘点 | ✅ `handoff/night-runs/两家店全景盘点_只读_2026-09-22.md` |

## 🔴 本批**按判别式拒绝执行**的

| 事 | 卡在第几条 |
|---|---|
| 多伦多两批测试单(|S|=161) | **条件④** —— `finance_transactions` 指向 S 的 **= 2 笔**,不是 0 |
| 三位技师停用 | 前置是「§一 那批单删完之后(先单后人)」—— 单没删,**前置不成立** |

---

## 1. 🔴 多伦多那 161 单:卡在两笔账本

- **事由**:11m §一 条件 4 写死「`settlements`/`finance_transactions` 指向 S 的 = 0;**不是 0 ⇒ 不批**(涉钱,不预批)」。
- **实测**:S = 161 单(68 张 2030 后的单**全在**三位技师名下,所以并集还是 161)。
  条件②③⑤ 全过 —— 顾客全部 ∈ {Lucky Member / LL-I28970PU / 未绑微信},
  **`lucky-luxe` 不在 S 里的单 = 0 张**(她那句「没有任何真顾客」在这里是自洽的)。
  **只有条件④ 挡住**:两笔收入账。

  | 账本 id | 金额 | 日期 | 备注 | 挂的单 | 顾客 |
  |---|---|---|---|---|---|
  | `fin_mt0hx3r7_focfvo` | **¥168** | 2026-08-19 | 订单 LL6440032275 完成自动入账 | `booking_mr7xkxj6_nsdapd` | 林小雅(演示) |
  | `fin_mt1iyvf2_cam5pl` | **¥278** | 2026-08-20 | 订单 LL3040919389 完成自动入账 | `booking_mt1iu9k8_mkxi5i` | LL-I28970PU |

  两笔都是 `type=income` / `source=booking` / `created_by=boss`,单完成时自动入账。
  🟢 **钱是假的**:全库 `provider <> 'MOCK'` 的支付 **= 0 笔**(从来没收到过一分真钱)。
- **我需要你答的那一个问题**:
  > **这两笔收入账(¥168 + ¥278)怎么办?(A 一起删 / B 先红字冲销再删单 / C 留着账、只删单)**

  🔴 我倾向 **B** —— 账本是只追加的,直接删掉两笔收入等于抹账;冲销留痕更符合已有口径。**但这是钱的口径,我不自己定。**

- **顺带答你问的那个数**:🔴 **占时段** —— `booking_slots` 挂着那 68 单的 **240 行**;
  整个 S 连带的是 `booking_slots` **481** · `booking_status_history` **277** · `payments` **137**。

## 2. 三位技师停用:等第 1 条

- **事由**:11m 补 §三 写的是「§一 那批单删完之后(**先单后人**,顺序不许反)」。
- **卡在哪一步**:单没删,前置不成立。停用本身是可逆的,但**顺序是你写死的**,我不自己调。
- **我需要你答的那一个问题**:
  > **第 1 条还没定之前,要不要先把三位技师停用?(要 / 不要,等单删完一起)**

## 3. 🔴 盘点撞出来的:`jics-nail` 顾客数 = 0,而绑微信的顾客有 4 个不是 1 个

- **事由**:全景盘点现读 ——
  - **`jics-nail` `users` = 0**。小婕店现在一个顾客档案都没有。
  - **`lucky-luxe` 绑微信的 = 4 个**:`Lucky Member`(…enid,119 单)· `LL-I28970PU`(…Ro3w,1 单)·
    **`LL-3LSVBVGA`(…944w,0 单)· `LL-04TI54NU`(…G6CU,0 单)** ← 后两个我之前不知道存在
- **我需要你答的那一个问题**:
  > **`LL-3LSVBVGA` 和 `LL-04TI54NU` 这两个绑了微信、0 单的账号,也是你测试用的吗?(是 / 不是 / 不确定)**

## 4. 🔴 储值余额:5 个人合计 ¥2,744 还挂着

- **事由**:`lucky-luxe` 储值余额非 0 的 **5 人,合计 274,400 分**(`Lucky Member` 一人占 2 笔 / 33,200 分)。
  钱是 MOCK 充的(没真收),**但余额是真记着的**。清顾客档案之前必须先定这 5 笔。
- **我需要你答的那一个问题**:
  > **这 5 笔储值余额怎么处理?(A 直接清零 / B 走退卡流程冲掉 / C 先不动)**

## 5. 🔴 `ai_response_feedback` 97 条 + `ai_learning_examples` 68 条 —— **那是你自己批过的话术**

- **事由**:它们挂在 `lucky-luxe` 上,但**不是假数据** —— 是你 2026-06/07 逐条批过的 AI 回复样本。
  明天那扇「清空测试数据」的门如果按租户一刀切,**会把它们一起清掉**。
- **我需要你答的那一个问题**:
  > **清多伦多店的时候,这 165 条 AI 话术要保留吗?(保留 / 一起清 / 先导出再清)**

## 6. 还挂着没答的三条(11l 留的)

- > **`avalin` 这个员工账号还要留着吗?(留 / 停用 / 删)** —— 你说过 avalin 是假技师,但账号留不留没答
- > **正门收到没有 `currency` 的请求时,继续默认 CAD 还是直接 400?(A / B)**
- > **那 21 个测试顾客档案怎么处理?(A 直接删 / B 加一列做软停用 / C 打标签藏起来 / D 先不动)**

## 7. 提审前置清单要加的那一行(11m §三 点名)

> 「AI 修图卡带 `soon` 还是 `off` 上审核版 —— 等 12a 第一轮对比包出来店主定」

已登记。**这一条不用现在答**,等 12a 反馈。
