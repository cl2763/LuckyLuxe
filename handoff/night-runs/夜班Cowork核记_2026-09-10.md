# 夜班 Cowork 核记(2026-09-10 夜 · 夜班令 7)

## 核记 #1 · 04:38–04:55 CST

**先自己查**:`git log` 已到 `a4b9c72`(夜7 段0/段1/段2 都做完了,75 分钟内八个提交);两台 `/health` 现读 `dataScope=live` 但 **`dataScopeName` 分别是 `sandbox` / `local`**;服务重启于 04:32,**跑的是段1 之后的码**。

**核过的**

| 事 | 我怎么查的 | 结论 |
|---|---|---|
| **J-39 同尺重量** | 开两份红榜抬头对读:修前(同尺)与修后**门槛同为 < 2:1**;修前**甲档去重 77 / 乙档 597**,修后 **0 / 106** | **收,关帐**。真相比原来那版**好**:乙档不是涨到 489,是**从 597 降到 106** |
| **J-39 落地** | 红榜抬头现在打印 `刀 tools/contrast-sweep.mjs @ 6ad336b` + `被测代码 apps/web @ 6ad336b` + **界面文件内容指纹 `57a36354a321`** | **收,而且超出我裁的** —— 我只要求刀提交号,它把「被测代码」和「文件内容指纹」也一起印了 |
| **段1 dataScope** | 两台 `dataScope` 仍是 `live`,但那是**旧二元语义**(`live` vs `test`,line 13031 在用);新加的 `dataScopeName` 是 `sandbox`/`local`,**两台不同名** | **接受**(见 §裁一) |
| **段2① 顾客端塌页** | 读提交原文:`index/share/wechat-simulator` 三页**只引 `styles.css`、不引 `design-tokens.css`** → 05z 令牌化之后 `var(--paper/--ink/--card)` 全空 → 底色透明、字色回落纯黑,**整页塌掉** | 🔴 **这是真事故,单独立号**(见 §裁二) |
| **顾客端红榜的诚实** | 抬头逐条写了扫到哪(未登录四个 tab)、**没扫到哪**(要登录的页 / 购物车有货态),并写「**不是绿,是没扫**」 | **收,这句话就是我要的** |
| 棘轮 | `local-server.mjs` 17,708 → **17,707**(降,允许) | 收 |

**记一笔表扬**:段2 那条「顾客端也要扫」我是当作补全扫描面提的 —— 结果**一晚上就抓到一个整页塌掉的事故,而且是上一批自己捅的**。它自己把这句写在提交里(「这是我上一批捅的,而对比度刀只扫商家后台,所以没看见」),没往下藏。

**这一轮没有待裁文件**(`待裁_2026-09-10.md` 不存在),我另发两条裁,见 `小批06b`。

**下一次**:06:00 CST 左右;11:20 后不再排、改写早报。

---

## 核记 #2(收尾)· 08:04 CST

**先自己查**:`git log` 仍停在 `3c35ab7`(段 6 收尾),**没有新提交** —— Code 跑完退出了,店主还没醒、还没粘 `小批06c`。早报与 06c 两处 md5 各自一致(`1518296a` / `4c483135`)。

**趁空我把两份还没人核过的文件读了,又查出两条小的(写成 `小批06d`)**

1. **上线批清单被拆成两份,而且第一份里的数已经过期**:
   `handoff/上线批清单_2026-09-09.md`(12 条,文件时间 **01:07**)+ `handoff/上线批清点_2026-09-10.md`(段 5 新增 3 条)。
   第一份第 1 行写「本地领先 `origin/main` **194** 个提交」,而 05:52 的早报写的是 **208** —— **同一件事、两个数、都没带读数时刻**。
   这是 J-39 的同族(数要带尺子/时刻),而且上线批清单正是那种**会被人当准数用**的文件。
2. **夜班总结里它自报「手滑 pkill 掉开发者工具」**:想写「不杀」,命令却在 `echo` 前面先执行了,automator 断掉三个套件报「未跑」,重开后复跑到 108/108。**照实写,收**;但这类命令**下次可能杀到别的东西**,要立护栏。

**这一轮不再排定时醒** —— Code 已跑完在等,空等没有意义。店主一说话我就接着干。
