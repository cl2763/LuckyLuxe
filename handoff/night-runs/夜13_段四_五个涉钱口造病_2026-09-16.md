# 夜 13 段四 · **十个**涉钱口破坏测试(六格全闭合)

被测:造病台 `tools/j62-knife-bench.mjs`(隔离端口 4191 + 自己的临时库;裁 #98 自证:4128/4310 全程 200 未碰)
靶子:**人读代码定的**(生成器三改不对已停,见待裁队列)· 定错靶子由台子 J-58④ 兜住

---

## 一 · 🔴 头条:建单那条落库,**没有一条判据咬到**

| | |
|---|---|
| 口 | `/admin/settlements`(建单,挂着 17 条 A 类断言)|
| 刀 | `db.prepare(\`INSERT INTO settlement_groups (id, tenant_id, booking_id, …)\`)` **整条注掉** |
| 判据套件 | `noshow-aftersales`(759 条断言)|
| **六格** | 红 **0** · 无关 342 · 🔴 **该咬没咬 26** · 说不清 391 · 没跑到 **0** —— 合计 **759 ≡ 底数 759 ✅** |
| 裁#106 两级三数 | 表级该咬 26 → 列级移出 **0** → 剩余 **26** |
| **J-58④ 证刀** | **同一处换成必然会红的形态 → 红了** ⇒ **这条路径确实被执行到了** |

> **759 条断言全跑完(没跑到 0),一条都没红。** 而刀已证咬到。
> 结论只有一个:**建单这条口上的判据,验的是回执,不是事实。**
> 更难看的是那 26 条 ——**它们读的正是 `settlement_groups` 这张表**,刀写的也是它,
> **表级列级都相交,却照样绿。**

**这不是产品缺陷,是判据缺口** —— 接口照样回 201,下游照样跑通,**只是库里那一行没写**。
这正是 J-62 第一款要防的形状,而它落在**涉钱最核心的那条口**上。

---

## 二 · 其余四个口

| 口 | 套件 | 红 | 无关 | 该咬没咬 | 说不清 | 没跑到 | 合计≡底数 | 红的是不是该红的那条 |
|---|---|---|---|---|---|---|---|---|
| `/admin/settlements/:id/amend` 更正落库 | `amend-linkage` | 1 | 4 | 0 | 2 | **27** | 34 ✅ | 🔴 **不是** —— 红的是撞上的(`①-5 业绩同步减`)|
| `/admin/settlements/:id/allocate` 业绩分配 | `audit-fix` | 1 | 21 | 0 | 11 | **57** | 90 ✅ | 🔴 **不是** —— 撞上的(`边界值:100/0 分成`)|
| `/admin/stored-value/recharge` 充值落库 | `noshow-aftersales` | 1 | 12 | **23** | 22 | **701** | 759 ✅ | 🔴 **不是** —— 撞上的(`Cannot read properties of undefined`)|
| `/admin/daily-close` 日结确认 | `daily-close` | 1 | 16 | **2** | 15 | **30** | 64 ✅ | 🔴 **不是** —— 撞上的(`屏5a 含分项`)|

**四个口的共同形状**:套件是 fail-fast,刀一落**半路就断**(没跑到 27 / 57 / **701** / 30),
红的那一条是**撞上的**(与刀无读写交集)。

🔴 **所以这四个口的结论是「没验成」,不是「没守住」** —— 两者必须分开写(J-57 / 没跑到那一格存在的全部理由)。
充值那口尤其刺眼:**759 条里 701 条没跑到**,等于这一刀只考了 7% 的卷面。

---

## 二之二 · 兜底段又加了两个口

| 口 | 套件 | 红 | 无关 | 该咬没咬 | 说不清 | 没跑到 | 合计≡底数 | 判 |
|---|---|---|---|---|---|---|---|---|
| `/admin/finance/transactions` 账本落库 | `cash-notes` | 1 | 9 | 0 | 20 | 29 | 59 ✅ | ⚠️ 红的是**撞上的**:`⑨a① 记一笔(现金)成功` 报 `INTERNAL_ERROR … reading 'id'` —— **它红是因为服务器 500 了,不是因为它去查了库**。这一条恰恰是 J-62 说的「验的是回执」:回执没回来就红,回执回来了就绿,**库里写没写它不看** |
| 🔴 `/settlements/:code/claim` 扫码认领落库 | `scan-sign` | **0** | 14 | **21** | 18 | **0** | 53 ✅ | 🔴 **第二个「跑满了还全绿」** · J-58④ 已证刀咬到 · 裁#106:表级该咬 23 → 列级移出 2 → **剩余 21** |

**所以「跑满了还全绿」现在有两个**:`/admin/settlements`(建单)与 `/settlements/:code/claim`(扫码认领)。
两个都在**顾客那条线**上:一个是**这单钱从哪来**,一个是**这个人是谁**。

## 二之三 · 兜底段又加了三个口 —— **其中一个是好消息**

| 口 | 套件 | 红 | 无关 | 该咬没咬 | 说不清 | 没跑到 | 合计≡底数 | 判 |
|---|---|---|---|---|---|---|---|---|
| 🟢 `/admin/settlements/:id/void` 作废落库 | `double-sheet` | **1** | 0 | 0 | 2 | 51 | 54 ✅ | ✅ **红了,而且因果链是通的** —— `① 撤回后同一预约可以重开: 409`。**这是十个口里唯一一个守住了的。** |
| `/admin/coupon-grants/custom` 发券落库 | `coupon-settle` | **0** | 30 | **14** | 42 | **0** | 86 ✅ | 🔴 **第三个「跑满了还全绿」** · J-58④ 已证刀咬到 |
| `/admin/finance/compensation` 薪酬落库 | `finance-goals` | 0 | 2 | 0 | 6 | 10 | 18 ✅ | ⚠️ 没跑完,**没验成** |

**发券那一刀还顺带证明了台子一条护栏是对的**:那句 SQL 全仓三处,台子**拒绝在不唯一的地方落刀**
(「不唯一(3 处),要指名第几处才许落刀」)—— 拒得对。指名第 3 处(口在 `:12641`,那一处在 `:12667`)之后才落下去。

## 三 · 这一段真正产出的三个数

1. **十个口,六格全部闭合到底数**(759 / 34 / 90 / 759 / 64 / 59 / 53 / 54 / 86 / 18)—— 没有一条断言掉在格子外。
2. **红的是该红的那条:1 / 10** —— `/admin/settlements/:id/void`(作废)守住了。**其余九个没有。**
3. **三个口是「跑满了还全绿」**(建单 · 扫码认领 · 发券),三个都有 J-58④ 的证刀;其余六口是「没跑完」。
   **能下定论的四个里,一个好(作废)、三个坏。**

## 四 · 下一步(不在本段做,写进待办)

- **建单口那 26 条「该咬没咬」逐条看**:它们读 `settlement_groups` 却不红,说明读的是**返回体里的字段**而不是库里那一行(J-62②)。改法:按 `㋚5a` 那个形状,**从库里把那一行读回来比**。
- **四个 fail-fast 套件**:刀落下去半路断,等于大半张卷子没考。要么把它们改成记账继续(像 `customer-paths`),要么给这几口单开不 fail-fast 的判据套件。
- **`/admin/settlements/preview` 现查:它一条库都不写**(`settlement-routes.mjs` 里零条写库语句)——
  所以挂在它上面的 **14 条 A 类断言没有「落库那一行」可切**。这不是漏,是**这口本来就不落库**;
  它们该验的是「算得对不对」,不是「写进去没有」。**单列,不进造病名单。**
