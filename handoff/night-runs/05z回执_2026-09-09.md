# 05z 回执 —— 深色不可读全站清零 + 接完 05y 五件(2026-09-09 深夜)

> **未推 main** · **不碰生产** · 提交 `be04c49` → `54a82b2`(本批两个大提交 + 收尾)
> 全量回归 **108 套全过** · 整跑 **2 分 35 秒** · 断言 **3,799 → 3,799(+0,这一批加的是刀不是套件断言)**
> 巨型文件棘轮:`local-server.mjs` **17,708 → 17,708** · `admin.js` **7,928 → 7,928**(都未升)

## 一、店主那句「我只是发现了一处而已」——现在给的是全站的数

| | 甲档「看不见」(< 2:1) | 乙档「AA 欠账」(2–4.5:1) |
|---|---|---|
| **修前**(同一把修好的刀量的) | **237 处** | 437 处 |
| **修后** | **0 处** | 489 处 |

- 扫的是 **13 个侧栏页 × 页内每个标签页 × 浅/深两档**,**5,014 个自己持有文字的可见节点**;
- 背景取「**实际绘制的那一层**」:半透明逐层合成到 body(店主撞见的那块正是「字有色、面是祖先画的」);
- **另有 723 个**文字节点压在**渐变面**上 —— computed 拿不到渐变的实际色,**这把刀判不了**,
  既没算绿也没算红,**如实写在红榜抬头**(要覆盖它们得走截图取像素,登记待办)。
- 红榜全文:`handoff/night-runs/对比度红榜_修前_2026-09-09.md` / `…_修后_….md`(逐条:页面 · 标签页 · 档位 · 选择器 · 文字前 20 字 · 前景 · 背景 · 比值 · 门槛)。

**为什么分两档**(这是我加的一刀,请裁):第一跑照「< 4.5 即红」出了 **1,337 条**,
其中**浅色档就占一半**——而浅色档是这套皮原本的样子。逐条看下去是「品牌金 #b5885d 压白底 = 3.16:1」
「奶白字压金按钮 = 2.32:1」这一类,**色值直接来自合同图那份令牌**。
把它们弄到 4.5:1 就得改令牌 = 改合同图,按纪律我不自己动。所以:
**甲档 = 真的看不见(< 2:1),必须清零;乙档 = AA 欠账,列清单待裁。**
两档的原始条数都在报告里,**您要并档随时并**。

## 二、改了什么(§三:先面后字)

| 轮 | 改了多少 | 说明 |
|---|---|---|
| ① 面(background / border) | **171 处** | 按亮度与色相映射到 `--card/--wall/--paper/--line/--hero/--goodbg/--badbg/--tagwbg/--tagmbg` |
| ② 字(color) | **182 处** | → `--ink/--sub/--heroink/--good/--bad/--tagw/--tagm/--brandd` |
| ③ 渐变面两端 | **16 处** | 渐变里的浅/深两端换令牌,中间的半透明高光留着(那是动效不是面) |
| ④ `admin.js` 内联 | **19 处** | 您说的「23 个」我扫出来是这些 |
| ⑤ `admin.html` 内联 `<style>` | **252 处** | 🔴 **您没提到的第三处**:admin.html 里还有一大片内联样式,同类扫尽一并纳入 |

`styles.css` 字面色 **382 → 40**,棘轮跟着收到 40(只许降)。

**顺手咬出三个真病**:
1. **按钮默认字色是浏览器的 `buttontext`(黑),不继承 `--ink`** → 深色档下所有没写 color 的按钮都是黑字压深卡(1.33:1)。一行 `button{color:inherit}` 收干净;
2. **`.nfy-tab.on` 拿字色令牌当面用**(`background: var(--ink,…)`)—— 浅档看着没事,深档翻成浅底浅字。**这一处是改之前就坏的**,归族「一个令牌只回答一个问题」;
3. 我那几刀留下 24 处 `var(--a, var(--b))` 嵌套回落,清掉。

## 三、判据自己错了四次(都记在码上)

1. **半透明背景没合成**:6% 白盖在深底上被当成奶白面 → 误报一片「奶白压奶白」;
2. **渐变面**:computed 拿不到色,往上找会找到不相干的白卡 → 这一类**不判**、单独计数;
3. **「登进去了没有」问的是 `#sidebarGeneralSettings` 存不存在** —— 它是静态节点,没登录也在,
   于是刀在**登录页**上扫了 15 个节点还报出结论(J-37 在判据自己身上复发);现在问的是它**可见**且令牌框已消失;
4. `shot-freshness` 没给 `--since` 时把**第 0 个参数**吃掉 → 只比了 1 张图还报绿。
   四条都是同一族:**少验了不报,比验错了更危险**。

## 四、接完 05y 那五件

| 件 | 结果 |
|---|---|
| **重拍整机图** | `11截图_整机重拍/01_…_20260910-010812.png`:大数字 **CAD $ 5,668**(无 `.00`)与小牌一致,导航栏 + tabbar 都深 |
| **J-38 机械比对** | 新刀 `tools/shot-freshness.mjs`。现测**当场咬到**:五页两档那十张拍于 00:42、而代码提交在 00:45 → 十张全红 → **重拍**;现在 11 张全绿 |
| **开批快照进预检** | 本机库 / 沙箱库 各要结构 + 指纹两份,缺哪个红并点名。造病:删掉沙箱那份指纹 → 当场红 |
| **tabbar 量出来** | 页面 `$` 进不了自定义组件,但**在 app 里** `selectComponent('#mtabbar')` + `createSelectorQuery().fields({computedStyle})` 拿得到:深档 `rgb(32,36,31)` = `--card`,浅档 `rgb(255,255,255)`。刀 QQ(写死成白)→ 静态层与量出来那层**各红一条** |
| **flaky 计数** | `handoff/night-runs/flaky/perf-viz.md` 记下第 1 次崩的现场 + 怀疑方向;预检:同一套件记满 2 次就红并停线。造病:补一行假的第 2 次 → 当场红 |
| **上线批清单** | `handoff/上线批清单_2026-09-09.md` 12 条逐条现状,每条带现查依据 |

## 五、没做成的那一半(如实写)

**整机 PNG 取像素这条路,我没做成。** 刀写了(`tools/mp-tabbar-pixel.mjs`,用无头 Chrome 把 PNG 画进 canvas 读像素),
但**在图里定位手机屏**这一步没过自证:截的是整个开发者工具窗口,我先按「跟工具底色不一样」找,
咬到的是工具**底部那排控件**;改成从上往下找连续回到底色的位置,又咬到面板缝。
两次的宽高比自证都不对(0.02 / 2.85,真值约 2.2),**刀就拒绝往下取样**——
这是对的形状(不猜),但**结论是它没验成**。tabbar 的结论由上面那条「组件里量出来的值」给。
要把像素这条也做成,得先有个稳的手机屏定位(比如按动态岛那块纯黑找锚点),登记待办。

## 六、写库自报(三个库分开)

| 库 | 动没动 | 证据 |
|---|---|---|
| **生产库** | **没连过** | 本批命令全在本机;不推 main、没 `railway ssh` |
| **本机库** `/Users/changliu/…/apps/api/local-data/lucky-luxe.sqlite` | **未动**(刀说的) | 心跳三表逐行过形状刀;原文见文末 |
| **沙箱库** `/Users/changliu/…/apps/api/sandbox-data/lucky-luxe.sqlite` | 🔴 **有动 —— 两行** | `lucky-luxe · ai_usage` 旧行消失 1 行(2026-09 那行 `used` 从旧值变 2347,`updated_at` 17:08:15)· `demo-lucky-luxe · tenant_settings` 旧行消失 1 行(`membership_config`,`updated_at` 17:09:37) |

**沙箱那两行我不装明白**:时刻落在我自己跑图与跑全量那两段里,**大概率是我的走查/回归驱动出来的**
(`ai_usage` 是 AI 用量计数;`membership_config` 是会员配置)——但**我没有直接证据指到是哪一步写的**,
所以这里写「有动」并把两行摊开,不写「未动」。⚠️ 另外现查一条:**4310 的 `dataScope` 是 `live` 不是 `test`**,
也就是说**套件护栏不会拦对它的写**——这条值得单独看一眼,登记待裁。

**这一批第一次有了沙箱开批快照**(`handoff/night-runs/开批快照/`),所以才比得出这两行;
上一批只能写「按形状看像心跳」——这就是把它挪进预检的价值。

---

## 附一:本机库对照表原文(J-23:由刀打印,人不许手写)

```
**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/开批快照/本机库_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/本机库_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 76→87(+11) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     notify_scan_marks  行 54→60(+6) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/开批快照/本机库_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 7 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notify_scan_marks · 只新增 1 行(10→11)
    jics-nail · notify_scan_marks · 只新增 1 行(10→11)
    hoptest-demo2 · notify_scan_marks · 只新增 1 行(10→11)
    luvia-bj · notification_logs · 只新增 11 行(61→72)
    luvia-bj · notify_scan_marks · 只新增 1 行(4→5)
    demo-ai · notify_scan_marks · 只新增 1 行(10→11)
    demo-basic · notify_scan_marks · 只新增 1 行(10→11)

  🔴 既有行被改动/删除 1 处:
    luvia-bj · reminder_tasks · **旧行消失 11 行**(行 80→80,其中新增 11)
```

**心跳形状刀原文**(`node tools/heartbeat-shape.mjs <库绝对路径> handoff/night-runs/开批快照/本机库_开批.fp.json`):

```
ok 1 - ① 倒推前提成立:reminder_tasks 里 PENDING 的 196 行,updated_at 全都还等于 created_at
ok 2 - ② reminder_tasks:本批状态推进的 11 行,**每一行都只动了 status / sent_at / updated_at**(倒推哈希在开批快照里找得到 = 其余列一个字节没变)
ok 3 - ③ reminder_tasks 行只增不减(开批 270 → 现在 270)
ok 4 - ② notification_logs(只许追加):开批那些行**一条不少**地还在(改一个字节或删一行都会少)
ok 5 - ③ notification_logs 行只增不减(开批 76 → 现在 87)
ok 6 - ② notify_scan_marks(只许追加):开批那些行**一条不少**地还在(改一个字节或删一行都会少)
ok 7 - ③ notify_scan_marks 行只增不减(开批 54 → 现在 60)

════ 心跳白名单放行清单(截至 2026-09-09T17:11:27.767Z 共 18 行)════
  lucky-luxe · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  demo-ai · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  demo-basic · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  hoptest-demo2 · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  jics-nail · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  luvia-bj · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  luvia-bj · reminder_tasks · ntask_mtpcebkc_5dpn1h · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebki_elw5vx · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebkn_pq1l96 · PENDING→SENT · 排定 2026-09-09T01:30:00.000Z · 发出 2026-09-09T01:30:59.744Z
  luvia-bj · reminder_tasks · ntask_mtpcebkt_vqfo34 · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebky_i0fyjj · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebl3_r581c4 · PENDING→SENT · 排定 2026-09-09T04:30:00.000Z · 发出 2026-09-09T04:30:00.066Z
  luvia-bj · reminder_tasks · ntask_mtpcebla_v5mgln · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblf_5dcfjp · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblk_2r4r0d · PENDING→SENT · 排定 2026-09-09T07:30:00.000Z · 发出 2026-09-09T07:30:03.683Z
  luvia-bj · reminder_tasks · ntask_mtpceblp_l2q4zk · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  luvia-bj · reminder_tasks · ntask_mtpceblv_nuubst · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  luvia-bj · notification_logs · 本批新增 11 行(只追加,旧行一条没动) · —→追加 · 排定 2026-09-09T00:00:59.651Z · 发出 2026-09-09T09:00:03.950Z
  ── 最早 2026-09-09T00:00:59.651Z · 最晚 2026-09-09T09:00:03.950Z(共 7 个不同时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)

✅ 心跳形状 7 条全过 —— 上面这些按裁 #23(状态推进)与裁 #26(只许追加)两种形状放行
```

✅ **本机库:未动**(快照与逐行指纹**两把都绿**;服务心跳表 `notification_logs notify_scan_marks` 除外 —— 调度器每天每店写一行、到点把 PENDING 推成 SENT,行行都过了形状刀,放行清单见上,与本批代码无关)
```

## 附二:沙箱库对照表原文

```
**沙箱库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/开批快照/沙箱库_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/沙箱库_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 283→284(+1) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     reminder_tasks  行 869→871(+2) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/开批快照/沙箱库_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 2 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notification_logs · 只新增 1 行(183→184)
    lucky-luxe · reminder_tasks · 只新增 2 行(623→625)

  🔴 既有行被改动/删除 2 处:
    lucky-luxe · ai_usage · **旧行消失 1 行**(行 2→2,其中新增 1)
    demo-lucky-luxe · tenant_settings · **旧行消失 1 行**(行 6→6,其中新增 1)
```

🔴 **沙箱库:有动** —— 逐行指纹查出既有行被改/被删(快照那把刀看不见这一类):
```
    lucky-luxe · ai_usage · **旧行消失 1 行**(行 2→2,其中新增 1)
    demo-lucky-luxe · tenant_settings · **旧行消失 1 行**(行 6→6,其中新增 1)
```
```
