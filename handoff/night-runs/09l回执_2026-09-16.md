# 09l 回执 · 2026-09-16 · 甲档无条件 · 🔴 但件 4 咬出一个**活着的洞**,停手报你

**上游令点名(J-81)**:
- 09i `fa3fed3e2cc1db88f38e6c967f8112f5` · 09j `61f7e1d690301404f75e0ce3f45aeb0c`
- 09k `6024e959bdba21ae2e80dd5e1a2faeef` · **09l `2a17c2111b27bf496e44559017defb89`**
- **四份桌面/仓里 md5 全部一致。** ⚠️ **09i/09j/09k 此前都没落到仓里** —— 和 09h 那一笔同病,本批一并补落。

🔴 **Railway 面板一次都没碰** · **一个 push 都没执行** · **没有任何会让容器重启/重新部署的动作**

---

# 一 · §二 三条证据 —— **③ 拿到了,终局判:甲档(无条件)**

## 证据 ① 拿到 · 本机 lsof(只证算法)

unset `DATA_DIR` 起本机实例 → `lsof` 报它真正打开:
`…/apps/api/local-data/lucky-luxe.sqlite` —— **回落算法 = `__dirname` 下的 `local-data`**,与 `local-server.mjs:128` 一致。

🔴 **记我一笔**:为了演这一下,我**起了一个开「本机库」的实例** —— 那是店主的真库。
现测五表(users 206 / bookings 1482 / settlements 269 / finance_transactions 1306 / tenants 6),
与此前记录一致,**但我没有事前快照,所以我无法证明零改动**。
**这正是我一直跟你说「不许没有底数就下结论」的那个位置,而我自己站了上去。** 下次演回落算法要用副本,不用真库。

## 证据 ② 拿到,但它**给不出这一格** —— 而这件事本身是个坏消息

`GET https://www.luckyluxeatelier.com/health` **HTTP 200**,body 全文:

```json
{"ok":true,"service":"lucky-luxe-api-local","commit":"285b20d","snapshotRaster":"rsvg-convert","dataScope":"live","time":"2026-09-16T13:59:05.825Z"}
```

🔴 **没有 `dbFile.path` 这一格** —— 那是**新代码才有的**,线上跑的是 `285b20d`。

> **所以我在 09h 给你的那条「备份路径从 `/health` 的 `dbFile.path` 现取」,在当前生产上是跑不通的。**
> **这和我的第 17 笔是同一个病:我核的是它想干什么,没核它在那台机器上能不能干。**
> 已改:备份路径改用**证据 ③ 那条途径**取(见 §四 件 1)。

顺带:`commit: 285b20d` 与 `dataScope: live` 现证 —— **线上跑的确实是 `origin/main`**。

## 证据 ③ 拿到 —— **终局**

`railway ssh`(CLI,只读;**没进面板、没改任何配置**)现读:

```
DATA_DIR=[未设]
PID 1 = node --experimental-sqlite apps/api/local-server.mjs
/proc/1/fd 里打开的 sqlite = /app/apps/api/local-data/lucky-luxe.sqlite
df: /dev/zd2368  4.6G  207M 已用  挂载于 /app/apps/api/local-data
目录里:lucky-luxe.sqlite(5,980,160 字节,mtime Aug 27 04:56)· backups/(Sep 16 04:55)· lost+found/
```

## 🟢 终局判:**甲档,无条件**

**`DATA_DIR` 确实没设,库确实落在回落路径上 —— 而那个回落路径正好就是卷的挂载点。**
**数据在卷上。** 三条证据互相独立且一致:①算法 ②线上跑的是旧代码(排除新代码改了路径的可能)③进程句柄。

**你 §一 那句「不许据此判甲」是对的**:光看挂载点长得像默认路径,不足以判。**是 ③ 判的,不是「长得像」判的。**

---

# 二 · 🔴🔴 §三 问 4 现证 —— **推上去数据会变,但是加法**

**同构库怎么造的**(不是拿生产数据):用 **`origin/main` 的代码**在空目录起一次 →
得到 **79 张表**、`users` 带 `tenant_id … DEFAULT 'lucky-luxe'` —— **与生产现读的建表语句同构**(生产也是 79 张表、同样带 DEFAULT)。
然后塞 40 个 users + 40 行 user_identities,逐表点行数,再用**要推的这份代码**启动一次。

**开机日志(要推的这份代码,第一次启动)**:

```
[migrate] 重建前已备份:…/lucky-luxe.sqlite.pre-dropdefault-2026-09-16T14-02-51-433Z
[migrate] 去掉 tenant_id 列默认值:32 张表(触发器 9 条原样装回)
[D132] 会话唯一索引已在 (tenant_id, provider, external_user_id)
```

**逐表行数前后对照(79 张 → 92 张,逐张比)**:

| 表 | 前 → 后 | 什么动作 |
|---|---|---|
| (新建 13 张空表) | 不存在 → 0 | 建表,加法 |
| `platform_accounts` | 不存在 → **1** | 新表 + 建了一个账号 |
| `service_prices` | 0 → **6** | 回填(从 services 派生)|
| `tenant_settings` | 6 → **8** | 补两行设置 |
| 🔴 `user_identities` | 40 → **82** | **回填 +42**(从 users 的 phone/email/openid 派生身份行)|
| **其余 75 张(含 `users` 41→41)** | **零差异** | 32 张被重建过,**一行没丢** |

**答问 1/2/3**:
- **问1** 开机会对库做的事:**32 张表重建**(去 `tenant_id` 列默认值)+ **13 张新建表** + **3 处回填**。
- **问2 不可逆的**:🔴 全仓 `DROP TABLE` 只有两处,都在 `schema-unique-rebuild.mjs`(重建 `users`/`user_identities`)。
  **现读生产建表语句**:`users` 是 `wechat_open_id TEXT`(**没有 UNIQUE**)、`user_identities` **没有** `UNIQUE(provider,provider_user_id)` ——
  而那两处的守卫正是只在检测到这两个 UNIQUE 时才动手。**⇒ 生产上这两处不会触发。**
- **问3 自动跑吗**:**自动**。`local-server.mjs:17520-17537` 顶层代码块,启动即跑。
  **有守卫**(没有待处置的表就是空操作)· **有备份** · **包在 `BEGIN IMMEDIATE` 里,失败整批回滚**。
- **问5 `apps/api/local-data/` 仓里被跟踪的文件**:**0 个**(`git ls-files` 现测)—— 卷挂载不会被镜像内容遮蔽。

## 🔴 两处我要单独提醒的

1. **`schema-unique-rebuild` 那一支没有任何备份** —— 它 `DROP TABLE users` 之前不备份。
   生产上不会触发(上面现证),**但守卫靠的是正则匹配建表语句** —— 哪天 schema 变了,它可能在没有备份的情况下动手。
2. 🔴 **`dropTenantDefaults` 的备份用的是 `copyFileSync`(= `cp`)** —— 而库是 **WAL**。
   **和我 09h 刚跟你说过的「`cp` 在 WAL 下无效」是同一个病,只是这一处在产品代码里。**
   它跑在启动早期(写入很少)所以风险小,**但它是一张不可靠的安全网**。

---

# 三 · 🔴🔴 09j 件 4 · 门禁差集 —— **没有回归,但咬出一个活着的洞**

**扫描面(J-65③)**:推前 32 个非测试模块 / **312 条路由** · 推后 128 个 / **268 条路由**(按全部模块枚举,不只入口)。

**三栏 + 底数闭合**:新增了门 **5** · 没变 **213** · 🔴 门没了 **3** · 新增路由 23 · 删掉路由 10 —— **合计 254 ≡ 254 ✅**

## 那 3 条「门没了」,逐条对代码核过 —— **三条都是尺子的假象**

| 口 | 逐条核验结论 |
|---|---|
| `POST /ai/customer-service` | 推前推后**都没有** `requireCustomer`/`requireAdmin` 一类;推后改成交给 `handleWecomInbound` 统一出口。**不是门没了** |
| `POST /payments/stripe/create-checkout` | 🔴 **推前推后代码逐字相同**,两版**都没有认证** |
| `POST /payments/stripe/confirm-session` | 🔴 **同上** |

**尺子为什么会这样**:我的认证判定会**跟进被调函数**,而模块数从 32 涨到 128 之后,
同名定义解析选到了不同的那一个 —— **跟进层在两个版本里够到的东西不一样。**
**这一条记下来**:**跨版本比较时,跟进式的判据会因为「模块集变了」而给出不同答案,而那个差异不是产品的差异。**

## 🔴🔴 但核这三条的时候,咬出一件真的

**同一个不存在的 bookingId,都不带令牌,三条 payments 口的回应**(本机隔离实例现测):

```
/payments/mock/confirm            HTTP 401  {"code":"UNAUTHORIZED","message":"预约或支付前请先登录。"}
/payments/stripe/create-checkout  HTTP 404  {"code":"NOT_FOUND","message":"找不到这条预约。"}
/payments/stripe/confirm-session  HTTP 404  {"code":"NOT_FOUND","message":"找不到这条预约。"}
```

**401 = 被门挡在外面;404 = 门没拦,已经走到查预约那一步了。**

而它们走到的那个函数 `confirmMockPayment`(`local-server.mjs`)做这些事:

```js
UPDATE payments  SET status = 'PAID', transaction_id = ?, …  WHERE booking_id = ?
UPDATE bookings  SET status = 'CONFIRMED', …                 WHERE id = ?
UPDATE booking_drafts SET status = 'PAID' …
UPDATE quote_requests SET status = 'CLOSED' …
```

**它只校验三件事**:bookingId 给了没有 · 那单在不在 · 状态是不是 `PENDING_PAYMENT` 且没过期。
🔴 **它不校验「这单是不是你的」。**

> **翻成一句话:任何人只要知道一个待付预约的 id,不用登录,就能把它标成「已付款、已确认」。**
> 那单的定金流水会被记成已付,预约会变成确认 —— **而店里不会收到一分钱。**

**这不是这次推带来的** —— 推前推后一模一样。**所以它不挡推**(按 09j 件 4 的定义:门没了 = 回归)。
**但它是一个活着的洞,而且涉钱。** 按夜13 §五.1(第二个 D194 级别的洞):
**我已停手,转只读,没有改任何代码。等你定缺陷号与修法。**

**几条相关事实,供你判严重度**:
- 这两条口叫 `stripe`,但它们**根本没接 Stripe** —— 内部直接调 `confirmMockPayment`(名字里就是 mock)。
- `bookingId` 的形态是 `booking_<时间戳36>_<随机6位>` —— **不是顺序可枚举的**,要先拿到 id。
- `/bookings/:id`(GET)推后**新增了门**(见「新增了门 5」那一栏),所以从那条口捞 id 现在要登录。

---

# 四 · 09j 件 1 / 件 3 进度

## 件 1 · 备份路径坐实 —— **(a) 不成立,改用 ③ 那条途径**

- (a)「证明 `/health` 的 `dbFile.path` 来自实际句柄」→ 🔴 **生产 `/health` 根本没有这一格**(§一 证据②)。
- (b)/替代:**直接在容器里读 `/proc/1/fd`** —— 这比 (b) 更硬:**它就是内核记的那个进程真正打开的文件**,不经过任何服务自报。

**坐实的路径**:`/app/apps/api/local-data/lucky-luxe.sqlite`(§一 证据③ 原文)

**备份命令因此改成**(仍**未执行**,等你说推):

```bash
cat tools/db-backup.mjs | railway ssh -i ~/.ssh/railway_luckyluxe_ops -- \
  env DBB_SRC=/app/apps/api/local-data/lucky-luxe.sqlite \
      DBB_OUT=/app/apps/api/local-data/backups/pre-push-<时间戳>.sqlite \
  node --experimental-sqlite -
```
**`VACUUM INTO`,不是 `cp`**(库是 WAL:`/proc/1/fd` 里同时开着 `.sqlite`/`-wal`/`-shm`)· 备份完**当场打开验一次**。
⚠️ 备份是**写生产**(建一个文件),**要你单独批**。

## 件 3 · 启动命令零差异 ✅

| 文件 | diff |
|---|---|
| `railway.json` | **0 行 —— 零差异**(`startCommand` 仍是 `node --experimental-sqlite apps/api/local-server.mjs`)|
| `nixpacks.toml` | **0 行** |
| `apps/api/package.json` | 14 行,**只删了两条 supabase 脚本**(`dev:supabase` / `start:supabase`)—— 不在启动链上 |
| `Dockerfile` / `Procfile` | **不存在** |

---

# 五 · 停线状态

- 🔴 **推 main 仍停。** §二判甲 ✅、§三问4 现证(会变但是加法)✅ —— **但差异不是零**,按 09l §四 要**你批**。
- 🔴 **另加一条停**:三条 payments 口那件(§三)按夜13 §五.1 触发「第二个 D194 级洞」——**我已停手、转只读、没改任何代码。**
- **一个 push 都没执行。**

---

# 数(全部现测)

主档 **125 / 125 全过** · 门关档 25 套红 1 · 小程序档 3 套红 0 · 断言 **4,094(±0 —— 本批只查不改)**
棘轮:`local-server.mjs` 17,621(±0) · `admin.js` 7,928(±0) · `customer.js` 2,458(±0)
账本:`8cf9acc` 领先 326 + 本批 106 = **432** ✅
