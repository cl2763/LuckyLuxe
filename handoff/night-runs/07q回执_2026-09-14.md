# 07q 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `b6ecf4217bc8c921f6a5388c22a68019`(全 32 位)
**读数时刻**:2026-09-15 00:40:54 CST
> 本批提交(**3** 个,从 `9a146a9` 数起)· HEAD `2b04c84` · **领先 `origin/main` 316 个** ——
> 对账:`9a146a9` 领先 313 + 本批 3 = **316** ✅
> 这些数都由 `bash tools/batch-footer.sh 9a146a9` 现出,**一个都不手写**(裁 #65 / #93)

---

## §一 · 五问**第二版** —— 七处逐条改完,**重贴了,一条都没跑**

全文:`handoff/生产只读五问_查询原文_第二版待批_2026-09-14.md`(桌面夹同一份)。**第一版作废。**

| # | 改成什么 | 为什么 |
|---|---|---|
| ① | `is_migrated <> 1` → **`IS NULL OR <> 1`**,并单列「`is_migrated` 为空的行数」 | `NULL <> 1` 求值为 NULL 不是 TRUE → 那一行**两个分类都不计入** → 底数不闭合 |
| ② | 积分 = **已签署结算单推的赚分 + 台账行** | 我上一段刚查清「赚分不在台账表里」,**下一段就写错了**(J-55) |
| ③ | 同号判断**三档并排**(字面 / 去符号 / 后十位) | 产品侧「号完全一致」认不出 + 查询侧按字面比数不出 ⇒ **真有问题时恰好报「没问题」** |
| ④ | 问 3 加 **`status` 分布** | 只给总数,对不上账时不知道差在哪 |
| ⑤ | 新增**第 0 条指纹** | 没它,五个数就没有「什么时候、对哪份数据」量的(J-38) |
| ⑥ | 问 4 加 **余额为负**两列(只报) | 超扣 / 退款异常也是有账要算的人 |
| ⑦ | `-readonly` 打不开 → **停下来问** | 危险的诱惑是「顺手去掉 `-readonly`」—— 那一刻所有护栏都没了 |

### ② 的代价,在本机库上**现测**出来了(不是理论)

| 租户 | 旧口径(只查台账表) | 新口径(赚分 + 台账) | **会被漏掉的人** |
|---|---|---|---|
| `lucky-luxe` | 0 | **44** | **44** |
| `jics-nail` | 0 | **18** | **18** |

> **旧口径会把 `lucky-luxe` 全部 44 个有积分的顾客数成 0。**
> 你说的「一个从没兑换过的老顾客会被数成积分 0」—— 现测就是这样,**而且是全军覆没**。

### ① 的底数闭合,现算了(你要的那一条)

| 租户 | 没有手机号 | 没号_导入 | 没号_非导入或未知 | **相加** | `is_migrated` 为空 |
|---|---|---|---|---|---|
| `jics-nail` | 59 | 0 | 59 | **59 ✅** | 0 |
| `lucky-luxe` | 52 | 0 | 52 | **52 ✅** | 0 |
| `hoptest-demo2` | 1 | 0 | 1 | **1 ✅** | 0 |

**逐店闭合。** 而 `is_migrated` 为空的是 **0 行** ——
**这恰恰说明第一版那个 NULL 陷阱在本机库上「跑得通」,它要到生产才会露馅。**

### ③ 三档,在本机库先跑了(你要的并排)

| 租户 | 有号档案数 | 档1 字面 | 档2 去符号 | 档3 后十位 |
|---|---|---|---|---|
| `lucky-luxe` | 22 | 0 | 0 | 0 |
| `jics-nail` | 16 | 0 | 0 | 0 |
| `luvia-bj` | 20 | 0 | 0 | 0 |

**三档全 0,而且三档之间没有差。**
🔴 **但我不把它当成「没问题」** —— 本机库的号是脚本按模板生成的(`139xxxxxxxx`),**格式天生一致**。
按你新加的停线:**这里的 0 只能当成「这把尺子在这份数据上量不到差」。**
真正的老顾客名单是几年里不同人手录的,**那才是三档会拉开差的地方。**

### 顺带看到一个数,本机库尚且如此

**「有余额 + 没手机号」:`lucky-luxe` 已经 8 人,`jics-nail` 3 人。**
那正是 §一③ 说的那批人:**上线第一天扫码会变成新人,而她们兜里有钱。**

> **等你批。批了我就跑,并报:连的哪个库 / 跑了几条 / 每条原文 / 零写语句的证据 / 指纹的读数时刻。**

---

## §四② · 裁 #99:造病台开跑前检查工作区干净(**连欠两批,这批清了**)

**根子照录**:台子在动源码,而我**同时还有未提交的真改动**在同一个工作区里 ——
**还原的时候就分不开了**,所以我才伸手去拿了 `git checkout --` 那把禁用的刀。

**三条路都验了**:

| 情形 | 现测 |
|---|---|
| 工作区脏 | `🔴 拒绝开跑:工作区有 4 处未提交改动(裁 #99)` · **退出码 2** |
| `KNIFE_ALLOW_DIRTY=1` 强开 | `⚠️ …但 KNIFE_ALLOW_DIRTY=1 强开了 —— 这一轮造病的还原不保证干净`,**大声说出来再跑** |
| 工作区干净 | `[裁#99 自证] 工作区干净 —— 台子还原的时候不会和别的改动搅在一起`,放行 |

---

## §四③ · A 类 246 条**按口聚类**

全文:`handoff/A类断言按口聚类_2026-09-14.md`(`node tools/assert-reads-fact.mjs` 现出)

| | 数 | 怎么来的 |
|---|---|---|
| A 类断言 | **246 条** | 静态筛现出 |
| **落在多少个口上** | **83 个** | 按「这条断言前面最近一次改动型调用打的路径」聚类 |
| 追不到口的断言 | 11 条(**单列,不混进上面那堆**) | 同上 |
| 其中**涉钱的口** | **35 个** | 口径按路径 + 断言名里的钱字样 |
| 其中**顾客能走的口** | **5 个** | 路径以 `/my` `/bookings` `/payments` `/auth` 等开头 |

> **一个口造一次就够 —— 246 条不用造 246 次,造 83 次。**
> 你估 40–60,**实际 83**,比估的多一半。

**涉钱最重的几个口**:`/settlements/:x/sign`(20 条)· `/admin/settlements`(17)·
`/admin/settlements/preview`(14)· `/admin/settlements/:x/amend`(9)· `/admin/stored-value/recharge`(9)。

**已造第一刀**(挂断言最多的那个口):

```
══ 造病:/settlements/:code/sign · 签署落库(涉钱,挂 20 条 A 类) ══
   ✅ 红了,而且红的就是那条:㋚5a … 库里那张单 LU-…-VLW0 状态 **pending_sign** :: 签字=200
```

**「签字接口回 200,而库里那张单还是 `pending_sign`」—— 正是 J-62 要抓的那个形状。**

---

## 顺手治的一件(又一次 J-61:数执行不数提及)

**预检里 J-34 那条 `grep` 把全量拦下了**,咬的是 `j62-knife-bench.mjs` 里**两处提及**:
一处是注释里的案底,一处是**拒绝开跑时那句解释文案**。
——**把规矩解释清楚的那段话,自己把规矩顶红了**,与 `danger-cmd` 咬 `echo "…不用 pkill…"` 同族。

换成 `tools/knife-restore-scan.mjs`:**剥注释 + 按位置判引号外**,只认真执行;
**不整段剥字符串**(git 命令的参数本来就住在引号里,剥完认不出真货 —— danger-cmd 那次先踩过)。

🔴 **而且这把新刀第一版是写坏的**:正则末尾 `\b` 跟在 `--` 后面**永远匹配不上**,
全仓扫出来是「零处」——**看起来干净,其实是瞎的**。
是我给它加的 `--probe` 自守当场戳穿的(`真执行=false`)。修好后:造病真写一行 → 点名;还原 → 回绿。

> **先证刀咬得到,再信它报的 0。** 这一条今天用在自己身上了两次(这里,和 §一③ 那三个 0)。

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **119 / 119 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **21 套 · 红 8** | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 0** | 同上 |
| 断言(在册) | **4,017** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:4,017 **+0** —— 本批**没有新增断言**(改的是工具与扫描器口径,不是判据条数)。

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**。

**棘轮**:`local-server.mjs` **17,650**(不动)· `admin.js` 7,928(不动)· J-60 欠账 28(不动)

**跨批账本自检**:`9a146a9 领先 313 + 本批 3 = 316` ✅(机械对账)

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
**生产库:五问待批,一条语句都没跑。** 本机库:只做过只读查询(`readOnly: true`)。

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §一 跑五问 | **等批** —— 按令停在重贴这一步 |
| §四③ 其余 82 个口的造病 | **未做,原因:本批只造了挂断言最多的那一个(签署口)作为样板;83 个口逐个造要按你的优先级排** |
| §四④ 门关档红 8 · J-60 继续降 · 9 条冻结理由 | **未做,没排到** |
| §四⑤ 段 C · D191 第三/第四形态 | **按令等五问的数** |
| 07o 剩下 4 条没走过的路 | **未做**,已点名排期 |
| §三 积分「不是存的是算的」 | **按令只登记,不做** —— 上线后队列 |
