# 10e 回执 · 2026-09-21

**点名(J-81):** `小批10e_那一单要先认得出来_立J106_第二步推之前必须实看_2026-09-21.md` md5 `1db6c2adf22d5b57f0d00a829a1a7287`(桌面/仓一致)
**合同图(每批重贴):** 签署文件留档 v2 → https://claude.ai/code/artifact/6366076a-97e4-4af4-b711-cc2e7468e94f

---

## 1. 🔴 §〇 取数 —— **这回是店主答得了的形状**

> **你说得对:我给的是机器号,她脑子里没有机器号。** 下面这张表她看一眼就知道是不是自己。

| | 日期时间 | 店 | 项目 | 技师 | 标价 / 应付 | 状态 | 来源 |
|---|---|---|---|---|---|---|---|
| ① | **2026-08-21 14:00** | **Jic's Nail 小婕** | **高光猫眼** | **鹤辰** | **468 / 368** | CONFIRMED | 小程序 |
| ② | 2026-08-27 14:00 | LUVIA 半径 | 柔金贝母设计 | Lina Zhou | 278 / 228 | COMPLETED | 小程序 |

*(金额按门店币种,未写币符 —— 币种红线)*

**🔴 两单都建于 2026-08-20,相隔 9 分钟**(`12:44:40` 与 `12:53:29`),**都走小程序**。

**这个人**:显示名 `LL-I28970PU` · **人记在 `lucky-luxe`** · 只有微信 openid(**无手机号 / 无邮箱 / 无 Google**)·
`users` 表**没有 `created_at` 列**,所以注册时刻取不到(如实写,不猜)。

**一句话**:**人记在【lucky-luxe】· 单下在【jics-nail + lucky-luxe】。**
**`jics-nail` 顾客数 0 · 订单数 1。** 同形 `LL-%` 顾客半径名下共 3 个,另两个都是 0 单。

### 🟡 可以请店主这样问她自己

> **「8 月 21 日下午 2 点,小婕店里有一单『高光猫眼』,技师鹤辰,标价 468 应付 368;
> 同一个人 8 月 27 日又在半径做了『柔金贝母设计』—— 两单都是 8 月 20 日那天前后脚下的。
> 是不是你或小婕当时自己试小程序下的?」**

**我没有再往下推。** 那一行**一个字没动**,已单挂 **D207-b**(功能后果,不是卫生问题)。

## 2. 🔴 §三 实看四项 —— **网页端做完了,小程序端卡在你手上**

### 🟢 网页顾客端:实看 ✅ · 两条负例 ✅ · L5 ✅

造景(沙箱 4310,顾客 Ada):商家端 3 份 = 权益书(有效)+ 价格表(**已作废**)+ 其他(**默认不给看**)。

**屏幕上顾客看到的**(`innerText` 现读,不是接口回包):
```
我签署过的文件
会员充值权益确认书
2026-09-20 签署 · 2 页

这些是店里存档的原件,只能查看
```

| 负例 | 屏幕上 |
|---|---|
| 价格表确认书(**已作废**) | 🟢 **不在** |
| 店里自己的表(**`customer_visible=0`**) | 🟢 **不在** |

**L5 连续 5 次进出,五次完全一致:**
```
第1..5 次: 离开后已移除 ✅ · 回来有块 ✅ · 1142×140 ✅ · 只有一份 ✅ · 两条负例不在 ✅ · 文案逐字相同
零异常
```

⬜ **像素图没取到**:四种办法(滚动居中 / 缩窗 / `position:fixed` 钉住 / `zoom` 裁剪)**全是黑帧**,
`zoom` 还明说「region crop not yet supported」。
🔴 **照屏1 的口径写死:这一屏「已实看 · 已逐字核对 · 未取得像素图」。不许用逐字冒充像素。**

⚠️ 一处我差点报错的,如实记:中途量到 `width: 0`,我先写了「布局缺陷」;
**再量一次是 1142×140,和兄弟区块同宽** —— 那是我在 `scrollIntoView` 与观察器重绘之间读到的瞬时值。**我当场更正了,没让它进结论。**

### 🔴 小程序顾客端:**实看没做成,卡在只有你能做的一步**

```
$ cli auto --project miniprogram --auto-port 9420
✖ preparing
[error] { code: 10, message: '需要重新登录' }
```

**微信开发者工具要求重新扫码登录。** 我不能替你扫,也不会绕过登录。

**你这边 30 秒的事**:打开微信开发者工具 → 用手机扫码登录 → 告诉我一声,我立刻把这一侧补完(实看 + 两条负例 + L5)。

## 3. 🔴 所以:**本批没推**

10e §三 把「实看」写成了推的前置,而小程序那一半**没做成**。
**四项差一项,就不算齐。** 等你扫完码我补完,再推。

## 4. J-106 收到

> **同一个毛病第三次出现,就不许再打补丁 —— 必须去改那个产生它的东西。**

这一批没有第三次(⑬e 那一次已经是改函数了)。**记着。**

## 5. §四.2 乙档第一批 —— **8 处,现测过**

按你的口径「先修会落进对账 / 结算 / 储值的那些,一批不超过 10 处」:

| 文件 | 处数 | 是什么 |
|---|---|---|
| `refund-routes.mjs` 22 / 48 / 82 | 3 | **退款**(落对账) |
| `finance-reverse.mjs` 33 | 1 | **冲销**(落账本) |
| `local-server.mjs` 9587 / 9592 / 9619 | 3 | **日结确认 / 重开** |
| `local-server.mjs` 13661 | 1 | **储值操作人**(带 2 个写入点) |

**现测(主钥匙走储值那条)**:`stored_value_transactions.created_by = "平台主钥匙"` ✅(改前会是 `'owner'`)

**棘轮装上了**:`J-105d` —— 乙档「涉钱落角色词」剩 **32 处 ≤ 棘轮 32**,**只许降**。
🔴 **这条棘轮的意义是「不许一边修一边又写出新的」。** 下一批把 32 再往下压。

🔴 **巨型文件净 0**:`import` 与 `const actorOf = …` **都并进已有行**,`local-server.mjs` 仍 **17,539**。

## 6. 🔴🔴 全量回归抓到我一件 —— **我差点把一个业务口径偷偷改了**

```
❌ ⑭ 留痕 created_by=员工账号: 技代充mua0st0p vs mua0st0p
```

**我写的 `actorOf` 把字段优先级重排了**:全仓 70 处既有写法都是 `adminSession.email || …`(**email 优先**),
而我写成了 `displayName || name || email || username`(**displayName 优先**)。

⇒ **员工代充的 `stored_value_transactions.created_by` 从账号名变成了显示名。**

🔴 **那不是修缺陷,那是改口径。** 而且是**钱**的留痕口径 ——
「修复会改业务口径的,先改文档、先问」,我一条都没走,是**回归把我拦住的**。

**改回去了**:优先级恢复 `email → username → displayName/name`。
这个出口的职责是**把落空/落角色词的那一段补上**,**不是重排已经对的那一段**。
案底写进了 `actor-name.mjs` 抬头,下一个人动这个顺序时会先看见它。

> 🟡 值得单说一句:**这件事上「判据比我对」。**
> 我当时的想法是「显示名更像一个人,更合 J-105」——听起来是对的,
> 但它**悄悄改了一个已经在生产里跑着的钱的口径**。J-105 要的是「别留空、别留角色词」,
> 从来没要「把已经有名字的那些也换一个字段」。

## 7. 三行口径

```
本机全量:129/129 套件 · 4,252 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:🔴 **本批未推** —— §三 实看四项差小程序那一项,等你扫码
GitHub CI 未跑:—
```

**断言增量对账(判据五):4,249 → 4,252(+3)。**
`test-signed-docs` 97 → 100 = **+3**(J-105d 棘轮 / J-105e 第一批核对 / J-105f 反向守)。**数字对得上。**

**棘轮三项:**
```
local-server.mjs   17,539 ≤ 17,539   (净 0:import 与构造都并进已有行)
admin.js            7,893 ≤  7,893   (没动)
J-105 乙档涉钱剩余     32 ≤     32    (🆕 本批新立;10c 底数 40 → 修 8 → 32,只许降)
```

---

## 要你做的两件(第一件 30 秒)

1. 🔴 **打开微信开发者工具扫码登录** —— 小程序顾客端那一侧就差这一步。扫完说一声,我补完实看 + 两条负例 + L5,然后推。
2. 🔴 **认一下那一单**:「8/21 下午 2 点,小婕店,高光猫眼,技师鹤辰,468/368;同一人 8/27 在半径做了柔金贝母设计 —— 两单都是 8/20 前后脚下的。是不是你或小婕试小程序时下的?」

## 要你裁的

| # | 事 | 挡不挡 |
|---|---|---|
| 1 | 那一单认完之后怎么处置(D207-b) | 挡不住第二步,但它是**功能后果**:小婕看不见自己店里唯一那位顾客 |
| 2 | 那 51 行的清理 —— 仍等你批 `SELECT` 原文 | 不挡 |
| 3 | 乙档下一批(剩 32 处涉钱)要不要继续按 ≤10 处一批走 | 不挡 |
| 4 | 🔴 **还是那条最省事的**:`requireAdmin` 主钥匙分支自带身份,剩下 58 处一行不改就都对 | 不挡,但越早裁越省 |

## 自评 L1–L5

- **L1** 🟢 网页顾客端**在店主会看到的那一层实看了**(innerText + rect + 两条负例在屏幕上不在);⬜ 像素四次黑帧,写死。🔴 小程序端未做成,卡在扫码。
- **L2** ✅ `actorOf` 唯一出口;第一批 8 处按「对账/结算/储值」成族修,不是挑好改的修。
- **L3** ✅ 图 v2 第 5 屏六条规矩逐条有对应判据。
- **L4** ✅ 反例造得够:作废的 / 不可见的 / 同店他人 / 他店本人,四种分开各造一次。
- **L5** 🟢 **网页端补上了**:连续 5 次进出,五次逐项一致,零异常。⬜ 小程序端待扫码后补。
