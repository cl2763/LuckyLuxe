# 夜班令 4 · 待裁(2026-09-08 夜)

> 一事一条:问题 / 我倾向 / 不裁会怎样。Cowork 醒来裁完写进「裁」列。

| # | 事 | 我倾向 | 不裁会怎样 | 裁 |
|---|---|---|---|---|
| 1 | 「补录落今天」撞休息日闸:合同二形二要求补录到已日结日的单**落今天**,而今天若是休息日,休息日闸会拒单(REST_DAY)。两条规则各自都对,撞在一起没有答案。 | 倾向:**落下一个营业日**,并在单上注明「服务发生于 X 月 X 日」(与现有补录留痕同形);休息日照收补录会让日结/业绩落在一个不营业的日子上,更怪。 | 每逢休息日,`test-observe-fixes` 两条钱链判据跑不成(现已明写「本轮未验」,不是静默绿);真商家在休息日想补录前一天的单也会被拒。 | ✅ **店主 09-08 裁:补录不过休息日闸,照合同落今天**。明确**不采**「落下一个营业日」——「那是把今天记的账写到明天」。已落码:REST_DAY 闸加 `!opts.backfill &&`;`test-observe-fixes` 那两条不再因今天休息而跳过,今天休息反而更该跑。 |
| 2 | **与图不符一处,请裁**:图(`商家端主页重画_两端设计图_2026-09-03.html` §取数失败)写的按钮文案是「**下拉重试** · 或稍后再看」。这句在小程序对,在**网页端不对** —— 网页没有下拉刷新,等于教店主做一个做不到的动作。 | 倾向:**网页端改成「点一下重试 · 或稍后再看」**(这按钮本来就是点的,`data-dh-retry` 真会重取);小程序端到段 9–11 落地时**保留「下拉重试」**。本批已按这个改,并加判据 ⑫「网页首页里不许出现『下拉』」。 | 网页上留一句做不到的指令,属 D148「说人话」族;或者反过来 —— 我擅自改了图上的文案而没报,属「悄悄发挥」。两头都不好,所以摆上来。 | |
| 3 | **网页后台不显示当前是哪家店**(全仓无 `tenantName` 渲染,现测)。这批截三家店时才发现:三张图光看画面分不出是三家,只能靠数字。 | 倾向:**登记待排,不在本批动**(图上没画过这块,按「界面没图 → 报 Cowork」)。店主自己只有一家店,当下不咬人;多商户/平台代运维时是「看不出自己在哪」的隐患。 | 不裁就一直没有;等到平台侧代商家操作时,改错店的风险是真的。 | |


> Cowork 裁(2026-09-08 晚,详见 handoff/小批05r补一 与 补二):#1 补录不过休息日闸,照合同落今天(已落码 18901e1,收)· #2 网页「点一下重试」收,小程序留「下拉重试」,图记 v3.2 · #3 现在就做:顶栏「店主控制台」改显当前店名(D156,≤20 分,三店判据 + 造景/造病 + 三张新截图)。
| 4 | **D155 带出的文案口径变化,请裁**:顾客端 AI 未开通时的那句话变了。原来是「这边暂时不支持在线答复~你可以直接在小程序里选择项目预约,或致电 XXX 联系门店」;并进同一出口后变成 D147 那句「不好意思,我这边暂时没法自动回复,已经转给同事啦,稍后回您~」。 | 倾向:**先按 05r §二 的裁定用 D147 那句**(已落码),但建议下一批把「自助预约 / 门店电话」那半句**并进** D147 那一句 —— 两件事不冲突:承诺现在是真的(D155 之后顾客那句话真入库、真待人工,08-04 当初反对它的理由已经不成立,判据也从「比文案」翻成了「那句承诺兑不兑得了」),但**顺手给条能自己走通的路**对顾客更好。 | 不裁就等于悄悄把 08-04 那次为顾客加的「电话/自助预约」指路删掉了,而那是店主当时特意加的。 | ✅ **店主 09-08 裁:并成一句**。已落码:`aiOffText({ lang, phone })`,有电话就带电话、没电话一个字不提;判据翻成两条(承诺兑得了 + 指路在),两把刀验红。 |


> Cowork 裁(2026-09-08 晚,详见 handoff/小批05r补四):#4 并句——D147 那句 + 「想先约的话可以直接在小程序里选项目预约」(店有电话再加「或致电 XXX」);判据两条:承诺兑得了 + 指路在。段 7 顺手做。
| 5 | **D150(层层递进不复读)本批没做成,请知悉并裁下一步。** 我写了 `repeat-guard.mjs` 并接了两层(生成前提示 + 生成后硬守),现测**不生效**:报价采集那条路在 `factGate.check` 那一行**之前就 return 了**,我选的「一处收口」根本不在它的路上。 | 倾向:**下一批单独做**,做法先说清楚再动手 —— 要么把几处 early return 收敛到一个出口再挂守(动的是引擎骨架,得先跑一遍 66 项 matrix 打底),要么在 `appendWecomConversationMessage` 落库那一层挂(所有回复都从那儿过,但那时文本已经写进流水,替换会造成「顾客看到的」与「流水里的」两处真相)。两条路各有代价,请裁。 | 不裁就只能靠我选,而这块是已训练行为的地基 —— 选错了砸的是 matrix 66 项。**本批我把半成的代码撤干净了,一行死码没留。** | |


> Cowork 裁(2026-09-08 夜,详见 handoff/小批05r补五):#5 两条都不选——`handleWecomInbound` 改名 Core,原名变薄壳 `repeatGuard(await Core())`,14 个 return 不动;改写时 UPDATE 流水里同一行(meta 记 rewrittenFrom),一处真相;同主题第二次换答法、第三次转人工。合并窗:等完再答不变,默认 4 秒、来一句刷新、12 秒封顶、可配 2–10。回归加 5 分钟看门狗。顺序 D150 → D152 端到端 → 六通 v4。
| 6 | **D150 落地时给「会话流水追加锁」开了第二个例外形状,请复核。** 你裁的是「改写就改流水里同一行」,而那张表有追加锁(`conv_msg_no_update`),`UPDATE` 直接被 RAISE(ABORT) 挡下 —— 现测第一版就是这么红的。我按**已有的脱敏例外**同样的姿态开了第二个形状:`role='assistant'` + `redacted_at` 仍为空 + **触发器自己用 `json_extract` 对一遍 `meta.rewrittenFrom = OLD.content`** + role/source/会话/租户/时间戳一个字不许动。 | 倾向:**这样是对的**,而且比单纯放行更硬 —— 这个口子改得掉「顾客现在看到哪句」,**改不掉「当时说过哪句」**(原文被触发器钉死在 meta 里)。追加锁的本意(历史不可篡改)还在。判据 ⓪d 守着这条形状。 | 不复核就等于我一个人决定了「账本式追加锁可以再开一个口」。这类锁一旦开口子,下次谁想改行都会引用这一次。 | |
| 7 | **合并窗封顶的判据只能拿短参数验**(窗 1 秒 / 封顶 3 秒),不是真的 4 秒 / 12 秒。 | 倾向:**接受**。封顶是「窗被一直刷新时的上限」这个**机制**,机制对了参数只是数字;拿真参数验一次要 12 秒以上,整轮回归就被它一个人吃掉。参数本身由 ①c/①c2 静态守(`DEFAULT_S=4` / `CAP_S=12`),行为由短参数守 —— 两层合起来才是完整的。 | 不裁的话,下次有人看到「判据用的是 3 秒」会以为封顶没按 12 秒做。 | |


> Cowork 裁(2026-09-08 05:58 CST,代审 #3,详见 handoff/小批05r补六):#6 **收**——第二例外是「只放行能自证的改写」(触发器自己对 rewrittenFrom/rewrittenBy),补一条造病:rewrittenFrom 差一字 → ABORT;#7 **收**——机制用短参数验、数字由静态判据守,回执里把这句写给店主;附加:`MERGE_WINDOW_MS` 只许出现在测试实例环境,启动路径不许有(静态一条)。提醒:触发器开机重建属「含开机迁移」,回执/提交头要写。继续 7b → 7d → 段 8。
| 8 | 🔴 **D152 的三段式在现在的引擎里没有出口,请裁。** 段 7b 现测(北京店,真模型,多轮走完):问价 → 「本甲还是延长」→ 「要不要卸甲」→ 不用卸甲 → **「我已经把信息整理好,接下来转给人工/技师帮您判断」**。报价采集**故意**以转人工收尾,AI 从头到尾**不报价**。所以「折扣 → 原价 → 折后价」这句话**没有任何一条路会说出来**;我做的折扣事实注入,在这条路上等于没人听。 | 倾向:**反面那一半照做、正面那一半等你裁**。反面(没折扣就不许提折扣)本批已经从「提示词里劝」升级成**事实闸上拦**——那条不依赖谁来报价,顾客看到「券后 ¥348」的风险已经堵住了。正面三段式要落地,得先裁一件事:**AI 到底许不许自己报价?** ①许:那要给它一条「简单项目直接报价」的路(和转人工的分界线由你定);②不许:那 D152 的三段式应该改成**人工报价单**上的格式要求,而不是 AI 话术。 | 不裁的话,D152 会长期停在「事实喂进去了、没人用」——看起来做了,实际上顾客一次也见不到。 | |
| 9 | **D150 与报价采集连问的边界**,顺带请你看一眼。现测:采集连着问「本甲还是延长」「要不要卸甲」是**正常推进**,但顾客中途再问一次价,引擎重复了上一句 → D150 判定复读并换成「我可能没答到点上…」。 | 倾向:**当前行为可接受**(那一句确实是复读),但更好的做法是让采集在被打断时**先回答那个问题再回到采集**。这属于采集流程的改良,不属于 D150。 | 不裁不影响正确性,只影响顺不顺 —— 记一笔免得以后当成 bug 重查一遍。 | |
| 6 | **段 10 员工打卡门:没能用真员工号登一次。** 沙箱库(4310)里**没有员工账号**(`handoff/本地自查账号.txt` 明写:`staff` / `staff2` 是 4128 本机库的号,登 4310 会 403)。所以那三张打卡门截图是**造态截图** —— 把页面切到员工分支、喂进两种考勤态拍的:渲染的是页面自己的分支,不是我另画的假界面,但**不等于真员工登录走查**。 | 倾向:**下一批在沙箱建一个员工测试号**(脚本红线①允许动员工号,且必须同步写进 `本地自查账号.txt`),再跑一遍真登录 + 真打卡(打卡还要过门店 WiFi 白名单,沙箱里得先配一条)。本批不擅自建号 —— 建账号是写库,夜里没有单独批准。 | 不建号就永远只有造态图;而打卡这条链上最容易出事的恰恰是**真打卡接口**那一段(WiFi 白名单、重复打卡、跨天)。 | |
| 8 | ~~段 11 图 §五 没有可执行规格~~ **这条我撤回,是我没读到。** 我先写下「§五 只说了按钮存在」就登记待裁,**其实 §五 写得很全**(轮播 6 秒 / 60 秒轮询是唯一允许轮询处 / 默认隐藏现金业绩与新增持卡 / 门店设置一个开关默认关 / 财务锁一律遮 / 屏保 5 分钟 / 三种退出)。撤回登记在这儿,不悄悄删 —— 「报待裁」不该被拿来当没读图的挡箭牌。 | 已按 §五 实做,见段 11。 | — | ✅ 自撤 |


> Cowork 裁(2026-09-08 15:40 CST 日班,详见 handoff/小批05s):#6 收——4310 建员工测试号(INSERT-only + 备份,密码只进本地文件)真登录真打卡重拍;#8 **AI 许报 `price_mode=fixed` 项目的价**,三段式落在 fixed 路;quote 路照旧采集→人工但先说一句「有新客券可用,具体价格技师看过后报」,三判据 + 两造病;#9 收为改良 D157(采集被打断先答再回)。#8 自撤那条:收其诚实。
