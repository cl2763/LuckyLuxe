# 早报 · 2026-09-15(夜班令 12)

## 🔴 一、生产那份库,**就是服务在用的那份**

**一句话:是。而且它在 volume 上,数据持久。D192 销号。**

三处对得上,一处不差:

| 来源 | 现测 |
|---|---|
| **服务在用哪个文件** | 进程 pid 1 的 **`/proc/1/fd/17 → /app/apps/api/local-data/lucky-luxe.sqlite`**(读写打开) |
| **`stat`** | size **5,980,160** · mtime **2026-08-27 04:56:40 UTC** · **没有 `-wal` / `-shm`** |
| **`mount` / `df`** | **`/dev/zd2368` ext4 挂在这个路径**,4.6G,**独立挂载点 = volume**(根分区是 `overlay`) |
| **跑五问时打开的那份** | 指纹 **5,980,160 / Aug 27 04:56:40** —— **和上面同一份** |

**那个 18 天的 mtime,解释是:自 8 月 27 日起没有写入。** 不是数据丢了,也不是写在别处。

> 顺带澄清一件我上一份回执里没说清的:线上跑的 commit `285b20d` **就是 `origin/main` 本身**。
> 它「旧」是因为**我们本地领先 321 个提交没有推**(红线不推 main),**不是部署出了问题**。

---

## 二、今夜真做成了什么

### 1. 🔴 生产五问跑完了 —— 顺便结了「1,432 还是 1,242」那笔账

**两个数都对,但它们数的不是同一件事:**

- **`bookings` 全库总行 = 1,242**(所有租户加起来);
- **`lucky-luxe` 自己的预约 = 161 条**(CONFIRMED 111 · COMPLETED 29 · EXPIRED 11 · CANCELLED 10)。
- **1,432 这个数,在生产库里对不上任何一格。**

**要紧的两个数:**

| 问 | `lucky-luxe` 现测 |
|---|---|
| **同号多条**(三档:字面/去符号/后十位) | **全 0,三档之间也没有差** |
| 🔴 **没有手机号** | **28 / 37 人**(已绑微信仅 4 人) |
| 有余额的人 | 5 人,合计 **¥2,744**,其中 **2 人没有手机号** |

**所以:**

- **段 C(撞车队列)不急** —— 同号多条是 0。
- 🔴 **D191 第三形态(没有手机号)才是真的**:`lucky-luxe` **四分之三的顾客没有手机号**,
  她们卡在严格四条的**第二条**(号完全一致),**上线第一天扫码就会变成新人**。
  **其中 2 个人兜里有钱。**

**问 5 没跑** —— 撞上你写的那条内建停下来条件:生产 `coupon_grants` 里 **`unused` 10 张,比 `active`(4 张)还多**。

### 2. `unused` 那张券:确认是真病,已治

`unused` **产品代码里一处都没有**,而 `/my/coupons` **不筛状态原样下发** ⇒ 顾客看得见一张后端不认识的券。
已改成**白名单下发**(只出认识的四态),挡掉的在服务端日志点名。造病验红。
同类扫尽:顾客侧原样透出 status 的**只此一处**。

### 3. 造病台现在会报第四个数:**「没跑到」**

签署口的真账:**红 1 · 仍绿 43 · 声称成功却仍绿 4 · 没跑到 10**。

🔴 **而红的那 1 条是「双端同时点签」—— 并发那条撞上的。**
**把「签字落库」整条注掉,没有任何一条说「签字成功了」的断言变红。**
真正相关的那些(「签完变已签只读」「已签的单不再出新码」「定金守恒」)**全在「没跑到」那 10 条里** ——
**不能说它们守住了,也不能说没守住,它们没跑。**

### 4. 8 把刀全补上 `--probe`,而它第一次跑就咬到一把真的

`customer-paths-scan` **认不出 `` fetch(`${BASE}/x`) `` 这一类调用** —— 整类漏。
也就是说「顾客能走的路」那个数一直是偏少的,**而它长得跟一个完整的数一模一样**。

### 5. `/health` 现在自己会说它在用哪份库

加了三格(路径 / 大小+mtime / 行数),**每格现量**。
立刻见效:**4128 自报本机库、4310 自报沙箱库** —— 以后不用再 ssh 进容器翻 `/proc`。

---

## 三、明天要你拍板的

1. 🔴 **D191 第三形态怎么治** —— 生产 `lucky-luxe` **28/37 没有手机号**,数出来了。
   你 07p 说过三条出路(上线前补采 / 首次扫码引导填 / 后台「把这个微信号认到这份老档案上」),
   **哪条都要图,而图你说等数 —— 数在这儿了。**
2. **问 5 的口径**:`unused` 那 10 张券在生产上算不算「还能用」?
   算 → 五问里「有卡包的人」要把它数进去;不算 → 它们是需要清理的历史脏行。**清不清、怎么清,归你。**
3. **签署口那条判据链**:`scan-sign` 里「签字真落库了没有」这件事**现在没有判据守着**。
   补法我建议在 `customer-paths` 里加(它已经走通了开单+签字),**但要不要现在补,你定。**

---

## 数

| | 开批 | 收批 | 怎么来的 |
|---|---|---|---|
| 主档 | 119/119 | **120/120** | `run-all-tests.sh` 现出 |
| 门关档 | 21 套红 8 | **22 套红 8** | 同上 |
| 小程序档 | 空转 0 | **空转 0** | 同上 |
| 断言 | 4,017 | **4,025** | `assertion-baseline.json` 求和 |
| `local-server.mjs` | 17,650 | **17,631(−19)** | `wc -l` |

**断言闭合**:4,017 + 6(`coupon-status` 新套件)+ 2(`auth-surface` ㊙⑭/㊙⑭b)= **4,025** ✅
**棘轮归因:真修好了** —— 积分域五件摘进 `points-ledger.mjs`。

**读的是哪个库(J-64)**:生产数来自 `/app/apps/api/local-data/lucky-luxe.sqlite`
(5,980,160 B · mtime 2026-08-27T04:56:40Z);本机数来自 `apps/api/local-data/lucky-luxe.sqlite`。
**生产全程只读**:五问 8 条 SELECT + 4 次只读探查,**零写语句、零文件落地、没动 Railway 任何配置**。
