# 小批 05t 回执 —— 六通 v5 · 三店灌满 · 皮按图搬(三端)· D166/D167 · 段9/10 补件 · 平台 AI 包格

> 提交:`dc54bed`(段1)· `3a8c638`(段2)· `1c3e8cd`(段3)· `d85dcf4`(段4)· `cd15b4d`(段5)· `3a3edaa`(段6)· 段7 见本文末
> **未推 main**。生产库未动 · 本机库未动(证据见 §六)

---

## ① 改了什么(按段)

**段 1 · 六通 v5(欠了五次,这次跑了)**
落仓 `handoff/ai-eval-results/05q_像人六通_原文_v5_2026-09-08.md`,六通全新会话,跑在 4310 活服务(窗 4s)。
抬头如实写「D166 未修」;夹具三店当天营业/休息现取现写(走顾客端公开口 `/stores`)。
通六改成把 05s 修的五病一次走完:点名 `fixed` 问价 → 最便宜的 → `quote` 项带券名句 → 采集中插可约 → 接回待答句。
**顺手咬出跑机自己的缺陷**:日期取 UTC、星期取本机 —— 跨日界就对不上(抬头把 09-08 写成了周三,门店时区是周二)。已修,并在样本里加了一行事后更正(不改跑出来的原文)。

**段 2 · D169 三店灌满数据 + 首页三条死线**
`tools/seed-demo-rich.mjs`:只写沙箱库(闸在 `db-target.requireSandbox`),INSERT-only,幂等按「做过没有」(历史一次 + 每门店当天一次),账本走 `finance-ledger` 唯一写入口(哈希链咬得上)。
三店各:近 12 个月 + 本周 + 今天三态(已完成 8 / 在做 1 / 待到店 4)+ 下一位 + 已确认日结;顾客 ≥30 · 小记 ≥12 · 作品 ≥9 · 券卡储值 · AI 今日一句。金额量级与币种各随各店。
**灌的过程中咬出首页「五项待办」里三项是死线**(报出来的 0 与「真的没有」长得一样):
- **D170** 待报价数 `status='pending'`,库里真值是 `PENDING_STAFF`(沙箱现查 287 行,没有一行叫 pending)→ 改数真值;
- **D171** 待日结数「今天有一行 `daily_closes` 且 ≠confirmed」,而那张表**只在确认那一刻才插行** → 按店主原话改成「昨天有已完成单、却没 confirmed 日结」;
- **D172** `shift_requests` **全仓不存在**(调休申请功能没做),`try/catch` 把「没这张表」吞成 0 → 改成如实下发 `available:false`,**待裁 #12**;
- **D173** AI 今日一句从来没落过库(`owner.dashAiLine` 全仓没人赋值)→ 生成即存、首页只读今天那条;
- **D174** 大屏「下一位」的钟点切的是 UTC 原文(北京店 10:00 显示成 02:00)→ 走 `localParts`;
- **D175** `dayCounts` 抬头写着「与今日台面同一条规则」而实际是两套(UTC 日期前缀 vs 门店时区窗口)→ 照抄台面那一份。

**段 3 · D168 网页皮按图原样搬**
新件 `apps/web/design-tokens.css`:合同图那三段 `:root` **逐字符搬**(30 令牌 × 3 段),一个不增不减不改值;`styles.css` 里重复的 `--ink/--line/--paper/--card` 删掉(一处真相)。
新刀 `tools/design-token-diff.mjs`:现从合同图抽三段与令牌文件**逐条比名与值**,差一位就红并点名,已进预检。
字体三件套按合同图那一行引 Google Fonts(**方案①**,今天先保证看得见);**自托管 woff2 = 方案②,写进上线批**。
英雄区改深色块(`--hero` 底 / `--herogold` 金数字 / `--herogrid` 细网格),维度条、此刻四格、下一位卡、四小牌、AI 一句都在这块里(图 §三 就是这么画的);折线补齐三件(渐变 .35→0 + 金色描边 + 末点圆环);数字滚动 600ms(`requestAnimationFrame`,`prefers-reduced-motion` 直接跳);5 个 dots 可点可轮播;dh- 块里的 `var(--x, #兜底)` 全删。

**段 4 · D168 小程序 + 全屏态**
新件 `miniprogram/styles/tokens.wxss`(值与网页那份逐条相等,`app.wxss` 第一件引它);首页大屏改深色英雄块、写死色清零;dots 5 个可点、6 秒轮播(`setTimeout` 一次一排,`onHide`/`onUnload` 清掉);全屏态换皮(`--hero` 系 + 金色 Fraunces)。
**字体如实说**(店主明令不许假装):小程序不能引 Google Fonts → **中文就是系统字体,不是 Noto**;数字要 Fraunces 得走 `wx.loadFontFace`,而它只吃 https + 域名白名单,本地沙箱给不出 → **今天数字也是系统字体**。`utils/numfont.js` 把原因写在代码里、地址为空时回 `loaded:false`(不假装成功);自托管地址进上线批(**待裁 #18**)。
**双端同病**:网页那边查出 AI 一句没落库,小程序这边读的是 `pulse.aiLine` —— 那个字段在响应里**从来不存在**。两端一起改读 `/admin/dashboard/ai-line`。

**段 5 · D166 + D167**
D166 **不用打点就定位到了**(回复自带 `source` 标签):轮 4 走的是 `quote_intake_template`,不是中断口。病因是顾客说的「手绘定制」在价目表里没有这个名字(北京店叫「参考图定制款」)→ 点名不了 → 旧写法「点不了名就回空串」让开中断口 → 掉进采集模板,答了跨大类的 90–180 分钟,还另起一个新问题。
改法:点不了名时不再让开,答**采集里已定的那个大类**的真实区间,再把待答那句原样接回。现测:「美甲的项目大概 90–180 分钟。 那我们接着说,请问这款是做本甲还是需要延长?」
D167:`mp-*` 各带 **60 秒单套超时**(macOS 没有 `timeout`,自己数秒);超时算「本轮未跑」、点名、不写 tally、整轮继续;断言基线刀认 `--not-run`(不参与升降、不写回基线、**上限 3 套**)。

**段 6 · 段9/段10 补件 + 平台 AI 包格**
小程序金额零自算(`order-detail` 里那个自己 `/100` 再 `toFixed(2)` 的死码删掉);七柱里 0 的那根高度就是 0;
沙箱建员工测试号(走正规口 reset-password → 真登录 → 首登改密 → **真打卡**,WiFi 校验 `inVerified=true`,密码只进本地自查账号.txt);
员工首页「打卡」那条早已退役,判据补上;
平台商家列表加「AI 包」列,**句子由后端出**(`aiLabel`),拿不到出「—」不出「未开通」。

---

## ② 在哪一层验的

- **接口层**:`test-seed-rich` 66 条全打**活服务的接口**(不是脚本 exit 0)。
- **像素层**:10 张截图 + 探针带 `getComputedStyle`(大数字体 = `Fraunces`、英雄底色 = `rgb(42,37,33)` = `--hero`、渐变 stop = 2、dots = 5)。
- **对话层**:D166 用店主那四轮原样重放,读的是活服务真回复(带 `source` 标签)。
- **给不到的明说**:数字滚动那 600ms 截不出来(判据 + 造病背书);小程序四张实拍**没拍到**(见 §六)。

## ③ 同类扫尽

| 修的这一个 | 它属于哪一类 | 全仓还有几处 | 证据 |
|---|---|---|---|
| `dh-` 块的 `var(--x,#兜底)` | 兜底值=藏了第二套配色 | dh- 块 **0 处**;全仓其它块还有 **14 处** | `grep -c "var(--[a-z0-9]*, #"` = 14(清单在 §五,下一批清) |
| 小程序金额自算 | 金额不走唯一出口 | **0 处**(白名单式:全仓 `toFixed` 逐个必须落进「不是钱/是出口本身」,棘轮 ≤5) | `test-currency-scan` 新增 2 条 |
| 待办口径对不上真实状态值 | 静默失败器族 | 五项**逐项**过了一遍:两项修、一项如实下发 `available:false`、两项本来就对 | `TODO_SOURCES` + 例外清单 |
| 「端口活着=还回去了」 | D163 同族 | 4128 / 4310 **两台都改成认库** | 收尾自证现在打库路径 |

## ④ 用什么数据验的

- **反例数据**:北京店(CNY / 东八区、当地凌晨)与加拿大店(CAD / 西五区)并排 —— 时区与币种两条都能露馅(`下一位 02:00` 就是这么咬出来的)。
- **多对象**:三店 × 四维度 × 六指标;12 个月折线。
- **空态/异常**:待办里 `shift_requests` 表不存在;`scopedRange` 拿到 `care` 类型时回 null。
- **造病**:见 §五。

## ⑤ 重复稳定性 / 造病一览(每一刀都留了「刀已落」的凭据)

| 刀 | 造的病 | 结果 |
|---|---|---|
| A | 回归脚本里塞一句种子引用 | 预检那条当场红,还原后绿 |
| B | `quotePending` 退回数 `'pending'`(重启服务后跑) | 3 条红并点名三家店;**第一次没咬到** —— 「待办 ≥3 项」太松,按「刀落了不红先怀疑判据」把判据改成逐项点名 |
| C | 「下一位」时间退回 UTC 原文 | 1 条红(luvia-bj `02:00`),还原后绿 |
| D | `--herogold` 改最后一位 e→f | 令牌刀红并点名「图=#d9b97e 仓=#d9b97f」 |
| E | 令牌里删一条 `--herolive` | 令牌刀红并点名缺哪一条 |
| F | 折线去掉 `<defs>` 渐变 | ⑬g 红 |
| G | 数字滚动 600 → 0 | ⑬h 红 |
| H | dh- 块加回一个兜底色 | ⑬c 红并贴出那一行 |
| I | D166 的大类区间退回空串(重启服务) | 判据 2 条红 **且活服务上原病复现**(答回跨大类 90–180 + 另起新问题) |
| J | 关掉 `mp-*` 的单套超时分支 | 看门狗 25 秒把整轮打死,`turn-answer` 一条没跑 —— 这就是原来的样子 |
| K | 小程序里加一处金额自算 | 币符扫描红并点名文件行 |
| L | 模板串里的注释放一对反引号 | 内联脚本语法检查红(见 §五「自己埋的雷」) |

**J-35(判据自己的缺陷)**:令牌刀 `indexOf('page{')` 命中了**注释里**那一段,于是同时报出「30 条全缺」和「值逐条全等」——自相矛盾的绿。改成先剥注释再找。

**两个自己埋的雷,都当场立了护栏**:
1. **D176**(D163 同族第三例):回归被打断时,CI 实例赖在 4128 上开着**回归临时库**,而 `restore_local` 只看「端口有没有人应答」就早退。现场撞到(4128 的 `dataFile` 指着 `/tmp/ll-ci-data.CyLqxX`)。改成**认库不认端口**,收尾自证也一起认库。
2. **内联脚本语法**:我在模板字符串的 HTML 注释里写了一对反引号 → `platform.html` 整段脚本语法错 → 平台控制台点「进入控制台」**毫无反应**,而页面照样 200、照样渲染登录框。预检加一条:两个 html 的每段 `<script>` 都得能解析;造病当场红。

## ⑥ 残留与不确定

- **小程序四张实拍(三店首页 + 员工打卡门)没拍到**:DevTools 自动化会话僵死(D167 那件事本身)。护栏解决的是「别拖垮整轮」,不是「让会话活过来」—— 会话要重开一次自动化端口。按**未跑**计,不按通过计。
- **待裁 #12–#19**(见 `handoff/night-runs/待裁_2026-09-08.md`):调休功能做不做 · jics 时区 · 日期两套算法 · 四小牌深浅 · 圆角与形制集打架 · 深色态只做到首页 · 小程序字体 · 小程序折线。
- **深色态**:首页那块按令牌走完了;左侧栏 / 顶栏按钮 / 台面还是写死的浅色(截图 07 看得见)—— 全仓令牌化是下一批的事(待裁 #17)。
- **`04` 与 `07` 两张图字节数相同**:站内选深色与系统深色**渲染结果本来就一样**(两段令牌值相同),差别在「哪条规则生效」,探针分别记了 `主题=dark` 与 `主题=(跟系统)`。

## 回归 / 断言增量 / 整跑 / 棘轮

- **全量回归**:`bash apps/api/run-all-tests.sh`(带 `MP_AUTOMATOR`)—— **108 个套件全过**,整跑 **4 分 15 秒**。
  其中 `mp-placeholder-size` / `mp-overlap` / `mp-home-sections` **三套本轮未跑**(各 61 秒超时,D167 那条护栏当场生效,
  整轮其余 105 套照跑)。断言基线刀:在场 105 + 未跑 3 = 清单 108 ✔;未跑豁免 3 ≤ 3 ✔。
- **断言增量(计数即证)**:合计 **3662 → 3685(+23)**。分解:
  新增 `currency-scan +2` · `dashboard-home +12` · `mp-home-owner +14` · `v4-five-fixes +11` = **+39**;
  减去三套未跑的 `3 + 4 + 9 = 16` → 净 **+23**。**数字对得上**,差额有解释。
  (未跑的那三套**不写回基线**,保留原条数 —— 否则下一轮它们跑起来反而成了「涨」,基线被悄悄冲掉。)
- **棘轮两数**:`apps/api/local-server.mjs` 上批 17,708 → 本批 **17,708**(未升);
  `apps/web/admin.js` 上批 7,928 → 本批 **7,928**(未升)。历史最低同为 17,708 / 7,928。
- **未推 main**。

## 自评 L1–L5

- **L1 末端验证**:✅ 网页/全屏/平台都在店主会看到的那一层截了图并带 `getComputedStyle` 探针;**小程序那一端没做到**(见 §六),如实挂着。
- **L2 同类扫尽**:✅ 四类都给了机械搜索证据;其中「兜底值」全仓还剩 14 处(在别的组件里),已列。
- **L3 无图不结案**:✅ 皮这一段有合同图;小程序那四张没图 → 不标已验证。
- **L4 反例数据**:✅ 用北京店 + 凌晨 + CNY 当反例,咬出了「下一位 02:00」。
- **L5 重复稳定**:⚠️ **没做满**。交互类(dots 点击、维度切换)只在跑机里各走了一遍,没做「连续 5 次进出页面」。原因:本批时间都花在段 2–6 的落地上;下一批补,或由 Cowork 在走查时代跑。

---

## 附:「本机库未动」的对照表原文(J-23:这句话由刀打印,人不许手写)

**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/05t/本机库快照_开批_05t.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/05t/本机库快照_开批_05t.json
  ✅ 逐表零差异(**表、行、列、索引、触发器、默认值都比过**)—— 「本库未动」这句话有证据支撑
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/05t/本机库快照_开批_05t.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ✅ 既有行一行没动(8 个桶逐表逐行比过;只增不改)
```

✅ **本机库:未动** —— 快照逐表零差异 **且** 逐行指纹零旧行消失(两把都绿)
