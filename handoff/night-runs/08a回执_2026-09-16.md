# 08a 回执 · 2026-09-16

**本令 md5 两处一致**:`2e93008468d916f668fba15439bcc4b8`(桌面夹 + 仓里 `handoff/`,16,990 字节)
**生产库未动 · 本机库未动**(下面 §一 说明:痕迹查询**没跑成**,一条 SQL 都没落到生产上)
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## 一 · 🔴 §四 痕迹查询 —— **脚本就绪、阳性对照通过、但生产通道打不开**

按你的三条批准条件补齐后,**先在本机库自测**(这一步救了整件事,见下),然后去生产 —— **`railway ssh` 三次都被对端关掉,一条 SQL 都没发出去。**

### 🔴 先说一件必须先说的:**你批的那条 ③ 是跑不动的,错在我**

```
Error: no such column: created_at
```

`users` 表**没有任何时间列** —— 本机库没有,`origin/main`(生产跑的那份)的 `CREATE TABLE users` 里也没有:

```
users 列:id, display_name, phone, email, wechat_open_id, google_id, tenant_id,
         tags_json, notes, birthday, is_migrated, legacy_total_spend_cents, avatar_url
```

**我提交给你批的查询原文里写了一个不存在的列,而我提交之前没在本机库试过一次。**
批下来的那四条,**第三条是死的**。已改:时间从 `user_identities.created_at` 取(那条路每建一个人必写一行身份),并且**逐列先探存在**,生产 schema 与本机不同也不会炸。

> **这一笔归我。** 你 §四 批的时候说「这是这类查询以后的模板」—— 而模板里有一条跑不动的 SQL,
> 说明**「查询原文先贴等批」这道程序,少了一步「贴之前先在本机库跑通」**。自请补进流程。

### 🔴 阳性对照:这把尺子**不瞎**(本机库上现场证明)

本机库自测时,它**真的查出了 4 行**:

```
── ① {"n":4}
── ② {"users_total":206,"has_google":4,"has_wechat":47}
── ③ 4 行
   {"id":"user_mrm0leev_cx8p9s","tenant_id":"lucky-luxe","email_head":"ide***","no_phone":1,"no_wechat":1,"first_identity_at":"2026-07-15 11:46:53"}
   {"id":"user_mrne2gwl_28ybiw",…,"first_identity_at":"2026-07-16 10:57:13"}
   {"id":"user_mrneao9v_fpagvh",…,"first_identity_at":"2026-07-16 10:57:49"}
   {"id":"user_mrnebe6y_ivceg9",…,"first_identity_at":"2026-07-16 10:58:23"}
── ④ {"provider":"email","n":4} · {"provider":"google","n":4}
```

**这 4 行是本机库,不是生产库** —— `email_head = "ide***"` 是 `identity-google-…@example.com`,
**07 月 15–16 日 `test-identity-links` 打在 4128 上留下的测试残留**,不是攻击。

**但它的价值在于:在碰生产之前,我先证明了「这把尺子在有东西的时候是能数出来的」。**
不然生产上报一个 0,我没法分清那是「没有」还是「刀是瞎的」(J-58①)。

### 生产通道:三次,都停在同一处

| # | 试法 | 结果 |
|---|---|---|
| 1 | `railway ssh -- node …`(agent 里没钥匙)| `{"status":"no_ssh_key"}` |
| 2 | `ssh-add ~/.ssh/railway_luckyluxe_ops` 之后再试 | `Using SSH key from agent: luckyluxe-ops-20260804` → **`Connection closed by 66.33.22.3 port 22`** |
| 3 | 加 `--service api` 显式指定服务 | 同上,**Connection closed** |

连最简单的 `railway ssh -- echo hello` 也是同一句 —— **不是我的脚本的问题,是通道本身建不起来。**
`railway status` 正常(项目 `lucky-luxe-staging` · 环境 `production` · 服务 `api ● Online`),`railway whoami` 正常。

**按停线「同一处连试三次不通就停」,我停在这里,没有做任何绕路**:
没有新建 SSH 钥匙、没有改 Railway 任何配置、没有换别的入口。**那些都属于「改账号设置」,要你单独批。**

🔴 **所以这句话现在仍然不许说** —— **生产上有没有被打过,我不知道;不许说有,也不许说没有**(J-63 / 08a §四)。
**需要你做的一件事**:在 Railway 面板里确认那把 ops 公钥还在账号的 SSH keys 里(过期/被清掉是最可能的原因)。确认完我再跑,**四条查询原样不动**。

**脚本已入库**:`tools/prod-trace-d194.cjs` —— 三条硬条件齐(`readOnly: true` 显式传 · 目标库显式给无默认 · 先打印绝对路径+大小+mtime+inode),**非 0 时自己打印「停在这里,一行都不动」**。

---

## 二 · §九 那一问:**误咬** —— 文字已改回去,刀已补靶子

**判:误咬。** 证据是那把刀**自己的口径**:

```js
/* 去掉注释再看:J-61 数的是**执行**,不是**提及** */
const src = readFileSync(...).replace(/\/\*[\s\S]*?\*\//g, '').replace(/^\s*\/\/.*$/gm, '')
```

**它剥注释,不剥字符串。** 而我那句话是**白名单的「理由」字段**,一句说明文字,不是一处判定 ——
**字符串里的名字和注释里的名字是同一类东西:提及。**

> **所以 07z 我干的是:改了一段本来正确的文字,去迁就一把有假阳的刀。方向和裁 #88(拿产品迁就夹具)是同一个。**

**两件都做了**:
1. **文字改回去**(恢复成写明那两个变量名的准确版本);
2. **给那把刀补剥法 + 三条靶子**,而且剥法是**保守的**(J-68:误差只许往「更容易红」那边倒):
   - 只剥**含那两个变量名的字符串**(`'production'` 这种不含名字的照留 —— 否则 `NODE_ENV === 'production'` 这条真判定会被剥没);
   - 文件里一旦出现 `process.env[`(**动态取键**),**整份不剥** —— 那时字符串里的名字可能真是键,宁可误报不许漏报。

| 新靶子 | 现测 |
|---|---|
| ④d 反向守:**字符串**里的提及不算一处出口 | ✅ |
| ④e 正向守:两种**真判定**剥完照样中(剥字符串不许把刀剥瞎)| ✅ |
| ④f 保守守:出现 `process.env[` 时整份不剥 | ✅ |

`test-demo-gate-scope` 8 条 → **11 条全过**。

---

## 三 · §五追:其余带 probe 的刀,各报「靶子有几种长相」(**只报不改**)

先分一类:**J-73 只适用于「按文本形状找东西」的刀**。有两把不是那种,单列。

| 刀 | 靶子条数 | **长相数** | 覆盖了哪几种长相 | 🔴 我看着缺的 |
|---|---|---|---|---|
| `tools/readset.mjs` | 10 | **5** | 直接 SQL · 精确路径 · **模板插值路径** · **转发壳** · 名字提及(反面)| 双引号路径 · 多行调用 |
| `tools/user-write-census.mjs` | 7 | **4** | 花括号体 · **括号并列 `(a\|\|b)`** · **一行式无花括号** · 注释提及(反面)| `path.startsWith` 变体 · 多行条件 |
| `tools/bench-selfguard.mjs` | 7 | **3** | 旧写法 · 先判存在 · 文案里提及(反面)| `source X \|\|` · 反引号命令替换 |
| `tools/knife-restore-scan.mjs` | 3 | **3** | 真执行 · 字符串里提及(反面)· 注释里提及(反面)| 多行续行 · `git restore` 别名 |
| `tools/customer-paths-scan.mjs` | 4 | **2** | 单引号路径 · 模板串路径 | 🔴 **双引号** · `api.get('/x')` 之类包装 |
| `tools/success-state-scan.mjs` | 4 | **2** | `wx.showToast({…})` · `toast('…')` | 🔴 **`wx.showModal`** · 多行对象 · 模板串标题 |
| `apps/api/test-web-not-signup.mjs` | 4 | **2** | HTML 属性 · `<a href>` | 🔴 **JS 里 `location.href='/signup'`** · 内联 `onclick` · 拼串 |
| `tools/assert-reads-fact.mjs` | 4 | **2** | 单引号 HTTP 调用 · `db.prepare('…')` | 🔴 **模板串路径**(`` fetch(`${BASE}/x`) ``)· 双引号 |
| `tools/eval-citation-check.mjs` | 3 | **2** | 裸路径 · 反引号文件名 | markdown 链接 `[x](path)` · 多行代码块 |
| `tools/shot-freshness.mjs` | 3 | **2** | `> 文件清单:` · `- 改过的文件:` | 无序号/缩进变体 · 全角冒号 |
| `tools/success-state-trace.mjs` | 2 | **1** | 裸路由路径 | 🔴 **只有一种长相** —— 它内部仍要匹配调用写法,却没为那一层种靶子 |
| ——— | | | | |
| `tools/release-checklist.mjs` | 4 | **不适用** | 靶子是**布尔值**,读的是结构化字段,不按文本形状找 | —— |

**一句话小结**:**12 把里有 5 把的长相数 ≤ 2,而且缺的都是「同一件事的另一种常见写法」**
(双引号 / 模板串 / `wx.showModal` / JS 跳转)。**按 J-73,它们现在报的 0 只证明「它认得我种的那两种长相」。**
**只报不改,等你定哪几把先补。** 我的建议顺序:`success-state-trace`(只有 1 种)→ `assert-reads-fact`(缺模板串,而模板串是本仓最常见的写法)→ `test-web-not-signup`(裁 #103 的判据,缺 JS 跳转这一种)。

---

## 四 · §〇 那一问:**34 是污染值,不是重量出来的底数** —— 已回去改产出它的那份文件

**真值序列**:

| 批 | 实测 | 怎么来的 |
|---|---|---|
| 07y 收尾 | **28**(cap 32,绿)| 四套夹具改走正门的功劳**在 07y**,不在 07z |
| 07z 中途 | **34**(cap 32,红)| 🔴 **污染** —— 我新判据的 probe 靶子里写了**字面量 SQL**,被「夹具直连库贴」那把刀数了进来 |
| 07z 收尾 | **28** | 把靶子的 SQL 拼起来去掉字面量,回到真值;同时把 cap 从 32 收到 28 |

**所以 07z 回执写的「34 → 28」是错的** —— 它读起来像本批降了 6,**而本批一处真债都没降**,只是把上限收到实测值。
**已按 J-55 在 07z 回执(仓里 + 桌面两处)原地更正,原文划掉不删。**
具名冻结 10 → 9 同理:**9 是真值,10 是同一处污染。**

> 这一件正好是你 §〇 说的那句话的实例:**底数变大和变小一样要举证。**
> 我当时看到 34 只想着「怎么降回去」,**没问一句「它怎么会变大」** —— 问了就会当场发现是自己污染的。

---

## 五 · D195 最小版小图 —— **已出,等你点头**

`deliverables/D195_门店初始密码_小图_2026-09-16.html`(也发在对话里,可直接点开看)

三屏 + 五条规矩 + 密码长相 + 唯一真风险 + 分两步做。**按 J-41,它是合同图:点头之后做出来必须跟它一模一样。**

图里我把你那句话里**唯一需要改的地方**画出来了并单独框了一段:

> 你说「从平台后台把它粘给老板」,听起来像**随时都能进后台看到每家店的密码**。
> **不能那样做** —— 那个页面会变成**一份所有商家密码的明文清单**,一旦泄露所有店一起完蛋。
> 改成:**只有生成的那一刻能看到,刷新就没了,要就重置一次。**
> 对你的实际操作几乎没影响(你本来也是生成完当场就粘走)。

上线清单第 6 条的措辞更正也写进图里了(「亲手设」→「由店主在平台后台生成并下发;密码不经过 Cowork、不经过 Code、不进任何文件」)。

---

## 六 · 数(全部现测)

| | 数 | 增量与来源 |
|---|---|---|
| 主档 | **124 / 124 全过** | 与 07z 同 |
| 门关档 | **24 套 · 红 3**(判据欠账 3 / 挂缺陷号 0)| A 族 3 · B 族 0 |
| 小程序档 | 3 套 · 红 3 · 空转 0 | `MP_AUTOMATOR` 没设(仓外模块没装)|
| 断言 | **4,073** | 4,070 → 4,073(**+3**)|

**断言增量对账(判据五)**:预期 +3 = `test-demo-gate-scope` ④d/④e/④f **三条新靶子**;**现测 +3 ✅**

**棘轮**(`wc -l` 现测,带基准):
`local-server.mjs` **17,621(±0)** · `admin.js` **7,928(±0)** · `customer.js` **2,755(±0,仍压线,本批没动)**
**J-60 正门欠账**:**28 → 28(本批没降)**,cap 28 —— 见 §四,上一批那个「34 → 28」是污染读数

---

## 七 · 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| 🔴 **痕迹查询跑生产** | **通道打不开**,三次都停在 `Connection closed`。**需要你在 Railway 面板确认那把 ops 公钥还在**;确认后我原样跑,四条查询一字不改 |
| §五 那 5 把长相 ≤ 2 的刀 | **只报不改**(你 §五 要求的),等你定先补哪几把 |
| D195 最小版实现 | **等你对着小图点头再动手**(J-41:图=合同)|
| 40 个口破坏测试 + 11 条追不到口的断言 | **未做** —— 按你 §十二「先交 1–4」,本批先交前四件 |
| `㋚4` 补反例数据 · A 族剩 3 套 · 9 条冻结理由按 J-70 复核 | **未做** |
| `customer.js` 摘取 | 本批没动它(2,755 ±0)|

---

## 八 · 本批我自己记的两笔

1. **提交给你批的查询原文里有一条跑不动的 SQL**(`users.created_at` 不存在)——
   根因是**「查询原文先贴等批」这道程序少了一步:贴之前先在本机库跑通**。已自请补进流程。
2. **07z 回执把污染读数 34 当成底数写了进去** —— 看到数字变大时我只想着降回去,**没问一句「它怎么会变大」**。
   这正是 J-50② 的镜像面,而我上一批刚引用过那一条。
