# 待店主裁 · 2026-09-10(06f 批)

> 每条写清:**是什么 / 现查到的事实 / 我建议怎么走 / 不裁会怎样**。
> 本批**没有**因为等裁而停下任何一段 —— 下面四条都不挡收工。

---

## #29 乙档剩下那 7 条:合同图配色的边界(浅档金压浅色小面)

- **事实**:`--brandd` 浅档压**白卡** 4.73(过 AA);压 `--wall`(`#f1eee6`)**4.08**、
  压 `--tagmbg`(`#faf3e3`)**4.28** —— 差最后 0.2~0.4。
  归一之前这几处是 `--accent-dark` 的 3.54 / 3.87,**归一已经把它们抬上来了,但没抬过线**。
- **落在哪**:客户档案的客人标 · 客服工作台的头像圈 · 门店设置的套餐特性 · 订单管理跳转条 ·
  金卡标 · AI 图库「待传作品图」· 顾客端首页购物车角标与两个快捷图标。
- **三条走法**:①改合同图把浅档 `--brandd` 压深一点(全站跟着走,浅档的金整体沉一点)
  ②这几块小面的底从 `--wall`/`--tagmbg` 换成白卡(层次少一档)③记账不改(乙档清不到 0)。
- **我建议**:①,而且和 §「第三个金」那批一起做 —— 反正都要动金。
- **不裁会怎样**:乙档卡在 11 / 3,清不到 0;不影响任何功能。

## #30 第三个金 `#b5885d`(26 处)—— 和 `--accent` 同病,建议下一批归一

- **事实**:硬编码字面量,**没有深色档**(红榜里 light / dark 两行比值一模一样都是 **2.76**);
  `admin.html` 的 `.fin-goal-go` 是「金底 + 奶白字」= 主按钮改之前那个病。
- **分布**:`apps/web/admin.html` 3 · `apps/web/customer-tags.js` 1(高价值A 标)·
  **小程序商家端 22**(subscription / attendance / customers / member / salary-plan /
  salary-month / finance / staff / points-mall / orders 十页 wxss)。
- **建议走法**:与 06f §三 **同一套**(当字 → `--brandd`,当面/边框 → `--brand`,金底上的字 → `--hero`)。
  小程序那 22 处压在 wxss 写死色棘轮 2,443 里,归一会把棘轮往下带一截。
- **不裁会怎样**:第三套金一直在,深色档下它是浅档的金压暗棕 —— 就是 `--accent` 那个病没治完。

## #31 `--accent` 的两个值在 `platform.html` / `sign.html` 里**改名活着**

- **事实**:`apps/web/platform.html:8` 与 `apps/web/sign.html:10` 各写着
  `--gold:#c8a47e; --bronze:#9b7655` —— **正是本批从 `styles.css` 删掉的那两个值**。
  两页都不加载 `design-tokens.css`,自带整套局部调色板(`--ink/--paper/--soft/--line/--muted` 都有一份)。
- **我建议**:归 **D188**(`06c §三`:令牌引用统一入口 + 静态判据 + 运行判据 + 全仓 html 扫)那一批一起收;
  **只换字面量写法而不接引用,等于把第二套真相搬个家**,所以本批不动。
- **口径提醒**:店主的话是「任何时候不许两个值并存」。**本批只做到「名字消失」,值还活着** ——
  这句写在回执明处,不拿「名字没了」冒充「值统一了」。

## #32 判据 J-38(图要带拍摄时刻)的判天口径偏粗

- **事实**:它把 `apps/api/**` 整棵树当「会影响界面的代码」。本批提交一次
  `apps/api/assertion-baseline.json`(测试基线,改不了任何像素)就把 6 张刚拍的图判成过期。
- **我怎么处理的**:**没改判据**(改判据让自己的图通过 = 换尺子),**重拍了一次**。
- **要裁的**:要不要把口径收窄到「真会改像素的路径」。收窄能少逼几次重拍,
  但**判据收窄本身有风险**(可能漏掉真会改像素的后端渲染)。**不裁就维持现状**,代价只是偶尔多拍一次。
