# 09r §二 · 那两条迁移会改判谁 —— **查询原文 + 阳性对照,等批,生产上一个字没跑**

> 2026-09-18 · 店主 09r §二.4:**「查询原文先贴,我批了再跑。」** —— **照办,生产上一次都没连。**

---

## 〇 一句话

**算法不是 SQL,是把那两条迁移的规则原文照抄一遍在内存里算。**
工具:**`tools/d73-impact.mjs`**(**只 `SELECT`,一个字的写都没有;不调那两个迁移函数**)。
用法:`node tools/d73-impact.mjs <库文件路径>`

---

## 一 · 规则原文(照抄,`文件:行`)

### `backfillTenantKindOnce` —— `apps/api/ledger-guards.mjs:125-147`

```js
const LEGACY_TEST_PREFIX = ['nsas','dbl','p2dc','p2sc','p2sal','p2ft','p12','p25','r3s','r2s','authx','diag','p0hy','p2fl']
const LEGACY_DEMO_IDS    = ['demo-ai','demo-basic','hoptest-demo2']

const rows = db.prepare("SELECT id, kind FROM tenants WHERE COALESCE(kind,'real') = 'real'").all()
for (const r of rows) {
  const head = String(r.id).split('-')[0]
  if (LEGACY_DEMO_IDS.includes(r.id) || String(r.id).startsWith('demo-')) {
    db.prepare("UPDATE tenants SET kind = 'demo' WHERE id = ?").run(r.id)      // ← 改内容
  } else if (LEGACY_TEST_PREFIX.includes(head) || r.id === 'tenant-iso-b') {
    db.prepare("UPDATE tenants SET kind = 'test' WHERE id = ?").run(r.id)      // ← 改内容
  }
}
```

🔴 **`String(r.id).split('-')[0]` 就是「按 id 前缀判这家店是什么」—— D73 明令废弃的那个判法。**
🔴 **`jics-nail` 与 `jics-store` 的 `head` 都是 `jics`** —— 只要 `jics` 哪天进了那张前缀表,两家一起被判成 `test`。
**今天它不在表里,所以今天不会;但判法本身就是那条被废弃的。**

### `retireLegacyDemoArchives` —— `apps/api/legacy-demo-retire.mjs:19-22`

```sql
SELECT id, tenant_id, tags_json FROM users
 WHERE tenant_id IN ('lucky-luxe', 'jics-nail')
   AND display_name NOT LIKE '演示2-%'
   AND (id LIKE 'demo-%' OR display_name LIKE '%演示%' OR display_name = '店主验签')
```
命中的行会被加上 `退役·旧口径演示档案` 标签(**改 `users.tags_json` 的内容**)。
🔴 **`display_name LIKE '%演示%'` 打的是这两家真店** —— 那里的每一行都是真顾客。

---

## 二 · 🔴 阳性对照(J-58⑧)—— 先证它数得出来

**在本机库上跑过一次,两条都数出了非零结果:**

```
【列存在性预探】tenants(4 列) · users(4 列) —— 全部在 ✅

【查询①】tenants 共 6 行,逐行:
   lucky-luxe     kind=real  status=active  LUVIA 半径
   demo-ai        kind=demo  status=active  星野美甲(演示·AI版)
   demo-basic     kind=demo  status=active  悦容美甲(演示·基础版)
   jics-nail      kind=real  status=active  Jie's Nail 小婕
   hoptest-demo2  kind=demo  status=active  五跳演示店
   luvia-bj       kind=real  status=active  LUVIA 半径美甲美睫 · 北京旗舰店

【查询②】会被改判 0 家 · 算完不变 3 家 · 规则没覆盖到 3 家
         四格闭合:0 + 3 + 3 = 6 · 底数 6 ✅

【查询③】会打退役标的 users:**10 行**(jics-nail 2 · lucky-luxe 8)
   🔴 jics-nail  「店主验签」 / 「爽约演示客」
   🔴 lucky-luxe 「林小雅(演示)」…「赵敏(演示)」共 8 行
   底数:jics-nail=75 · lucky-luxe=74
```

🔴 **查询③ 在本机库上数出 10 行 —— 这把刀是**能咬到东西**的,不是空转。**
(本机库那 10 行**已经带着那个标签**了,因为这条迁移在本机早就跑过;**生产上没跑过,所以在那边命中就是新打标。**)

---

## 三 · 🔴 我在写这个脚本时又踩了一次 J-75

第一版用了 `users.created_at` —— **本机库的 `users` 根本没有这一列**,当场炸。
**09p 我刚因为「只探了一列」栽过一次(`bookings.demo_seed`),这一版又来。**
所以脚本里现在有一段**列存在性预探**:把用到的**每一列**列出来逐个探,缺哪一列就**点名说是哪一列**,不给堆栈、不改用别的列凑。
**逐行那一栏也跟着改了**:没有 `created_at` 就不编一个出来,改成「店 · 名字 · id · 现有标签」。

---

## 四 · 🔴 要批的那一条(生产上一个字没跑)

```bash
# 在生产容器里只读跑一次(脚本本身零写入;库用 readOnly 打开)
railway ssh "node --experimental-sqlite /app/tools/d73-impact.mjs /app/apps/api/local-data/lucky-luxe.sqlite"
```

🔴 **一个问题**:这个脚本**线上那份代码里没有**(它是本批新写的,而线上跑 `285b20d`)。
**两条路,都等你裁**:
- **(甲)** 把脚本**内联成 `node -e`**(不落文件,合 09q 补的口径「只读进容器 = 不产生任何持久化痕迹」)——
  **我倾向这一条**,已备好内联版;
- **(乙)** 等推完之后再跑(那时 `/app/tools/` 里就有它了)—— **但推之前正是要知道这个数的时候**,所以(乙)等于不查。

**批准条件(照 09r §二.4 与既有口径)**:①只许 `SELECT` ②每列先探存在(脚本已内建,缺列即停)
③阳性对照已给(上面)④**非 0 立刻停,只报不动,不许删任何行、不许改任何状态**
⑤🔴 **不许自己去改那两条迁移来「让它变成 0」**。

---

## 五 · 无论查出来是几,这一件都已经有一半结论了

**`TREAT_AS_REAL` 已按你 §三 的批落地**(本批),于是:

| | 改前 | **改后(本批已落)** |
|---|---|---|
| 生产 · `IS_PRODUCTION` 已设 | 跳过 | 跳过 |
| 🔴 生产 · **变量丢了** | **会跑** | **跳过** |
| 本机库 | 会跑 | 跳过(**你已接受的代价**) |
| 沙箱 / 回归临时库 | 会跑 | 会跑 |

**⇒ 「没设 → 会跑」那一向已经堵上了。** 查这一数是为了知道**万一它跑过会发生什么**,
以及**那两条迁移要不要销号**(你 §三 那条尾巴)—— **不是为了决定要不要推。**
