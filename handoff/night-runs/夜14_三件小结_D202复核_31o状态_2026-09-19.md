# 夜14 §三.6 · §三.7 —— 两件小的,一次说完

---

## §三.6 · D202 那条常驻判据落地复核 —— ✅ **在跑,而且刀咬得动**

**判据住在** `apps/api/test-backup-wal.mjs` **④ 层**(09o 批落地),**在主档里天天跑**。今天现跑:

```
ok ④a 扫描面:全仓非测试 .mjs/.js **282** 个(apps/ + tools/ 递归)
      ② 那一层只有 128 个平铺文件 —— 🔴 递归这一层就是为了补它
ok ④b 🔴 全仓「cp 一个 sqlite」现扫 **1 处**,全部落进白名单(白名单 1 条)
ok ④c 白名单只许变短(现 1 ≤ 1)—— **一条余量不留**
ok ④d 白名单零残留:每条都对应一个现存命中
ok ④e 🔴 造病:一行「cp 一个 sqlite」必须被咬中
ok ④f 反向守:cp 一个非库文件不许被咬中
```

**唯一那 1 处白名单**:`apps/api/local-server.mjs` 的**导入前快照** ——
理由随码写着:「跑在接口线程里、导入事务之前,而 `VACUUM INTO` 要独占读锁 —— 09n 未点名,登记等裁」。
🔴 **这一条还欠你一个裁** —— 它是全仓**最后一处** cp 一个库的地方。

**另外两条(③a/③b)也在守**:两处开机重建的调用处都带了 `dbPath`,**不传就没有备份**。

---

## §三.7 · 31o AI 修图验证件状态 —— **一句话:三样全缺,而且缺的顺序是对的**

| 件 | 状态 |
|---|---|
| **钥匙**(百炼 / 火山)| 🔴 **没设**(`DASHSCOPE_API_KEY` / `QWEN_IMAGE_KEY` / `ARK_API_KEY` 三个都没有)· **我没开,也没去看任何值** |
| **脚本** | 🔴 **不在** —— `tools/` 下没有任何修图验证脚本(现扫 `ai-edit` / `retouch` / `修图` / `31o` 四个词,命中 0) |
| **料** | 🔴 **不在** —— 20 张自店样片与豆包指令,**台账记着「候样片与豆包指令」,店主还没给** |

🔴 **但这三样缺的顺序是对的,不是欠账**:
31o 定死的路径是 **样片 + 指令 → 跑画质验证 → 她亲验挑预设 → 出设计图 → 实现**。
**第一步就卡在「她给料」** —— 料没到,脚本写了也没东西可跑,钥匙开了也只是白烧。
**所以现在不动手是对的**,不是拖着。

**料一到,我这边要补的就两样**:①沙箱开钥匙(零入档)②一个跑 `8–10 条候选预设 × 20 张样片` 的脚本,出**前后对比包 + 翻车率 + 单价耗时**。
