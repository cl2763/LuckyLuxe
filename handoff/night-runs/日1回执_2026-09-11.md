# 日班令 1 回执 —— 假红清掉 · 白屏路径治了 · 刀会报错了 · 租户纵深补齐

> **提交清单由脚本产出,原样贴**(日1 §五 4、07a §九 1):
>
> ```
> $ git log --oneline 8b4a574..HEAD
> 1a297e3 日1-段5b 收尾补跑当场又咬出两种「不是声称」+ 我自己放走 4 篇真声称的那一刀
> e94d4df 日1-段5 回执 + 三库逐表对照表 + 把 users/services 那 49 处登记进总表
> d04c1db 日1-段1b 出口搬进 apps/web/party.js —— customer.js 有行数棘轮,加功能不许加行
> 251bd7a 日1-段3b 补租户条件的**读回口径**修正:用「写的时候用的那一个」,不用环境里的 currentTenantId()
> 374a8dc 日1-段4b J-48 的真判据:底数闭合(打过记号 = 量过 + 没验成 + 被裁 + 重复少写);只断言「量过≥去重后」抓不住把去重挪到量之前那一刀
> db0d802 日1-段4a J-48:报告同时打印「量过的个数」与「去重后的行数」并断言前者 ≥ 后者(去重只许少写行,不许少量节点)
> ed6e441 日1-段3 裁#47:按 id 查 technicians / stores 一律带租户条件 + 白名单式判据
> b6f7080 日1-段2 J-47:扫页面的刀同时收页面错误(onerror/unhandledrejection/console.error),有错先报错;抛过错的页这一轮结论作废,报告抬头单列,退出码不许当绿
> 889a250 日1-段1 裁#45:白屏路径 —— 先造病截图,再按类修,最后复现同一幕
> 0ca87bf 日1-段0 裁#49/J-49:「未动」这条判据改成认标记不认词,棘轮 34 一个数没抬
> $ git rev-list --count 8b4a574..HEAD
> 10
> ```
> 加上本回执与收尾那一提交,**本回执排在最后一个代码提交之后定稿**。
> 领先 `origin/main` **264** · **未推 main · 未碰生产库** · 密码一个字没进回执

---

## 段 0(时间盒 60 分)· 假红清掉,**棘轮 34 一个数没抬** ✅

**现测**:`test-untouched-proof` **12/12 全过** · 棘轮 **34** · 现测存量 **34 篇** —— 没抬也没降
(旧账本来就都是无标记的真声称,重量之后一篇不多一篇不少)。

**改法**(J-49):判据认**结构化标记**,不认自由文本里的词。

1. 标记格式写死在判据里:`未动对照:<对照表路径>`,且**只认像真路径的**(带 `/`、`.md` 收尾);
2. **逐行**分「在作声称」与「在否认/引述/讨论」——夜 8 那句
   「但『本机库未动』这句话这一夜**我不能说**」是**否认**,不算声称;
3. 留 `PROOF_LEGACY`(立律前用「逐表零差异」那类措辞举过证的不追溯)——
   **不是放宽,是不把旧账算成新账**:一并判红会让棘轮立刻涨,而那正是 J-49 要防的「拿棘轮吸收判据缺陷」。

**造病三条 + 反向守,全部常驻在判据里**(不是一次性的刀):

| 条 | 内容 | 现测 |
|---|---|---|
| ③a | 写了标记、但对照表文件**不存在** → 红 | 0 处指空 ✅ |
| ③c | 写了「未动」当声称、**却没标记** → 必须算违规 | ✅ |
| ③d | 写了标记但文件不存在 → 必须算违规(J-27 同族) | ✅ |
| ③e | **反向守**:像夜 8 那样**否认**的 → **不许红** | ✅ |
| ③f | 反向守的反向守:平铺直叙的真声称**不许**被 DENY 吞掉 | ✅ |

**活刀 OO**(真往 `handoff/` 塞一篇无标记的声称):**35 > 34 当场红** → 补上标记 → 回绿 → 删掉 → 12/12。

顺带修一处:`MARK_RE` 头一版把台账里「未动对照:<文件路径>」那句**描述格式**的话也咬了 ——
拿占位符当声称,就又成了认词不认物。

---

## 段 1(90 分)· 白屏路径:**两张图放一起** ✅

**造病(修之前)** —— 图 `白屏路径/购物车_没技师的货_修之前_*.png`:

```
onerror: Uncaught TypeError: Cannot read properties of null (reading 'name')
[现场] 正文 401 字 · 出现「未指定」:否 · 页面错误 1 条
🔴 这一页抛过错 —— 按「这一页崩了」报,不许说成「少了某个元素」
```

**造病做法换过一次,写在码上**:头一版想在沙箱库把某单的 `technician_id` 指到不存在的 id ——
**数据库不让**(`FOREIGN KEY constraint failed`)。这恰好印证你的定性:**订单那一路今天不会天天炸**。
改走**草稿/购物车那一路**(`booking_drafts.technician_id` 本来就可空),而且**一个字不写库**
(购物车住在 localStorage)。

**修法按类不按处**:

| 层 | 改了什么 |
|---|---|
| 服务端四行 | `4605/4606/5309/5310` 一律 `.get(...) || null` —— 不再把 `undefined` 漏给前端;**一行没加**,巨型文件仍 17,707 |
| 前端 | 三个出口 `partyName / partyField / partyId`,拿不到显示「未指定」;**11 处**(9 处 + 排期那 2 处)走出口,**没有九处各加一个 `?.`** |

**复现同一幕(修之后)** —— 图 `白屏路径/购物车_没技师的货_修之后_*.png`:
**正文 186 字 · 出现「未指定」:是 · 页面错误 0 条 · 购物车整页在**。
另有对照组 `正常货_修之后`(不出现「未指定」,一切照旧)。

**判据**进 `test-display-text`(11 → 16 条):顾客端**零处**直取 `.technician.`/`.store.`(剥注释后扫)·
三个出口在且回落是「未指定」· 服务端四行都带 `|| null` · `party.js` 的 script 标签必须排在 `customer.js` **之前** · 反向守。
**刀 PP**:把一处改回直取 → 当场红并点名 `apps/web/customer.js:2170`;还原回绿。

**回归当场咬出一件,已修**:三个出口写进 `customer.js` 让它从 2,811 涨到 2,817,**顶破了行数棘轮**。
按公约搬进新模块 `apps/web/party.js` —— `customer.js` 现 **2,799 行**(比棘轮还低 12 行)。
**加功能不许加行**,这一条是回归替我守住的。

---

## 段 2(45 分)· 刀收页面报错,**有错先报错** ✅

`contrast-sweep` 装上 `window.onerror` / `unhandledrejection` / `console.error` 的收集
(**赶在页面脚本之前**装,否则首屏那一次抛错根本收不到)。
抛过错的「页 × 档」:报告抬头**单列一节**、控制台**先报错再报数**、**退出码不许当绿**
——「元素没找到」和「这一页渲染崩了」是两个结论。

**刀 QQ**(往 `platform.html` 塞一句必抛的渲染错):

```
· 平台控制台 · dark
    onerror: Uncaught TypeError: Cannot read properties of undefined (reading 'y')
❌ 有 2 个「页 × 档」抛过错 —— 这一跑不许当绿
```
**反向守**:同一页不塞错时,报告里是「✅ 这一跑没有任何一页抛错」,零误报。

---

## 段 3(45 分)· 十处租户条件 → **机械扫出来是 15 处** ✅

清单上写的是 10 处(含 `5544` 那个子查询),**全仓机械扫是 15 处**:
`local-server.mjs` 12 处 + `booking-drafts.mjs` / `quote-state.mjs` / `schedule-board.mjs` 各 1 处。
**靠列举被测对象的判据永远漏没列的那几个** —— 这正是白名单式要解决的事。

**补法分两种,写在码上**:

- **序列化那四行用 `row.tenant_id`,不用 `currentTenantId()`** ——
  序列化可能在没有租户上下文的地方被调到,那时 `currentTenantId()` 会回落到旗舰店,
  **反而会把别家店的正常数据判成 null**(那是新造一个病);
- `/admin/*` 里那几处本来就跑在租户闸门之后,用 `currentTenantId()`;子查询跟着 `b.tenant_id`。

**判据**(`test-tenant-explicit` 15 → 19 条):全仓不带租户条件的必须 **0 处**;
白名单 **1 条**并写了理由 —— `SELECT tenant_id FROM stores WHERE id = ?`,
**它的用途就是问「这门店属于哪一家租户」**,加条件等于先要答案再问问题;两条反向守(会咬 / 不误咬);刀不许咬自己。
**刀 RR**:拿掉一处 → 当场红并点名 `apps/api/local-server.mjs:13443`;还原回绿。

### 🔴 这一段我要单独报两件

**一件是我自己造的病,回归咬出来的**:补租户条件时我有两处**读回来用了 `currentTenantId()`,
而写进去用的是别的那一个** ——

- 建技师那条路:`INSERT` 用的是路由里的局部 `tenantId`(平台侧可能指定别的店),
  读回来用 `currentTenantId()` → 取不到 → `Cannot read properties of undefined (reading 'id')`;
- 员工昵称默认值:那一段**上一行**就算好了 `const tid = me.tenantId || currentTenantId()`,我却又写了一次。

口径定死:**读回来要用写的时候用的那一个租户**。和序列化那四行用 `row.tenant_id` 是同一条道理 ——
**一件事一处真相,不许在旁边再猜一次**。**补纵深最容易新造的病就是这个**,照实写在这里。

**另一件,只报不改**:把同一把刀放宽到 `users` / `services` 试扫,**49 处**不带租户条件。
同一族、population 大得多,**不是这一段 45 分钟能做完的** —— 按你写的「不要顺手修完就过去」,
**按裁定范围收口,这 49 处单独登记待排**。
我没有把它拔高成「跨店漏」:同一族定性照旧是**纵深没做满**(那些 id 也来自本店自己的行),
但它比 technicians/stores 那 15 处大三倍,值得单独一批。

---

## 段 4(60 分)· 两个数 + 新刀跑 5 次 ✅

**J-48 两个数**已进报告与控制台:

```
**量过的个数 220** / **去重后的行数 3**(J-48:前者必须 ≥ 后者)
底数闭合:打过记号 268 = 量过 220 + 没验成 2 + 被裁 3 + 重复少写 43
```

**判据不是「量过 ≥ 去重后」那一条** —— 那条抓不住「把去重挪到量之前」(两个数会一起变小)。
真判据是**底数闭合**:`打过记号 = 量过 + 没验成 + 被裁 + 只少写一行的重复`。
**刀 SS**(把 `seen` 挪到量之前):

```
🔴 J-48 底数没闭合:打过记号 48 个,而 量过 38 + … = 38 —— 差 10 个,**多半是去重挪到量之前去了**
```
还原后闭合。

**新刀跑 5 次**(同参数、同页面),**5 行并排**:

| 次 | 后台 | 顾客端 |
|---|---|---|
| 1 | 量过 220 / 去重后 3 | 量过 38 / 去重后 0 |
| 2 | 量过 220 / 去重后 3 | 量过 38 / 去重后 0 |
| 3 | 量过 220 / 去重后 3 | 量过 38 / 去重后 0 |
| 4 | 量过 220 / 去重后 3 | 量过 38 / 去重后 0 |
| 5 | 量过 220 / 去重后 3 | 量过 38 / 去重后 0 |

**5 次一模一样** —— 220 这个数**稳**,可以当基线。甲档 0 · 乙档 0 也是 5 次一致。

---

## 段 5 · 收尾

**全量回归 111/111 全在场全过** · 断言 **3,853 → 3,867(+14)**:
`untouched-proof` +5 · `display-text` +5 · `tenant-explicit` +4 —— 增量与新增判据条数对得上。

**棘轮**:`local-server.mjs` **17,707** · `admin.js` **7,928** · `customer.js` **2,799**(2,811 → 往下 12 行)·
`styles.css` 写死色 31 · 小程序 wxss 2,356 · 三个旧金 183 · 丙筐 16 —— **一个都没变大**。

**写库自报(三库分开)**:

> 未动对照:handoff/night-runs/日1_三库逐表对照表_2026-09-12.md

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **未碰** | 这一批零次 `railway` 调用 |
| **本机库** | **既有行一个字没改**;`notify_scan_marks` 追加 6 行 | 逐表对照表在上面那一行;`heartbeat-shape` 9 条全过并逐行打印「只追加,旧行一条没动」 |
| **沙箱库** | **有动,全是心跳** | `notification_logs` 313→315 · `notify_scan_marks` 552→599 · `reminder_tasks` 912→914 |

**回归途中还修了一个「崩掉不是红」的毛病**:我删掉旧的 D188 报告之后,
`test-untouched-proof` 拿 `git ls-files` 列出的名字去读盘,**ENOENT 把整把刀崩掉** ——
崩掉连结论都没有,比红更坏。已改成「文件不在就跳过」。

---

## 四、没照字面做的地方(两处,都说清)

1. **段 1 的造病换了路径**:原文说「把某一单的技师行弄成查不到」,**数据库的外键不让**。
   改走草稿/购物车那一路 —— 同一个病(`undefined` 漏给前端)、更真实(那是正常业务态)、而且不写库。
2. **段 3 的判据范围收在 technicians / stores**:放宽到 users/services 会立刻冒出 49 处,
   那不是这一段能做完的,**顺手改掉才是真的危险**。已单独登记。

---

## 五、给店主看的话(三五行)

- **今天变好的两件**:① 顾客端那条「整页白」的路堵上了 —— 以前只要有一笔单的技师对不上,
  顾客点开购物车/订单就是一片空白;现在它会老老实实显示「未指定」,页面还在。
  ② 扫页面的机器人**学会报错了** —— 以前页面崩了它会说成「少了个按钮」,把人往错路上带。
- **还欠的一件**:同一类租户过滤在 `users` / `services` 上还有 **49 处**没补,已排进下一批,**不是今天这一批漏掉的**。
- **要你拍板的**:暂时没有。配色批那两张并排图我一个像素没动,等你有空再看。

---

## 六、🔴 收尾清单那条新规矩 —— **第一次跑就见效**

> 「回执落盘之后补跑一次 `test-untouched-proof`,**回执自己在扫描面里**。」

**补跑结果(回执落盘之后)**:

```
❌ test-untouched-proof 2/12 项未过
not ok 5 - ③ 回执自证棘轮:392 篇 … 存量 35 篇 ≤ 34
not ok 6 - ③a 写了「未动对照:」标记的文档,标记指的那份对照表文件真的在(现测 1 处指空)
```

**查下去是同一族(J-49)的另外两种写法**,两种都不是声称:

1. `夜班令8_未送达_存档` 里 **指令在点名要我交的那份东西**:「**本机库未动**的对照表」
   ——「未动 + 对照」是**名词**,而且那恰恰就是**标记自己的名字**;
2. `小批07a` 里 **你转述我没说这句话**:「而你**不说**「本机库未动」」;
   同一份文档里那行 `未动对照:handoff/night-runs/xxx对照表.md` 是**定义格式**用的占位文件名 ——
   **没有声称,标记就不承担举证责任**。

### 而我第一版的修法,自己犯了 J-49 的对偶错

我把**裸的「不说」**加进 DENY,于是
`安全保证:生产库未动 · 本机库未动。未推 main。做完**不说**可以关帐。`
这种**句尾另起一句**里的「不说」也被当成了否认 —— **一口气放走 4 篇真声称(34 → 30)**,
**而那个数看上去还像是判据变严了**。

改成**就近判**:只看「库名未动」前后各 14 个字的窗口。现测回到 **34 篇 ≤ 34**。

**三条常驻造病守住这一次的坑**:③g 名词用法不算声称 · ③h 转述不算声称 ·
**③i 就近判:句尾另起一句的「不说」不许放走真声称**(这一条就是拿我自己那一刀当样本)。

**补跑最终结果**:

```
[未动须有证] handoff 文档 392 篇 · 说了「未动」无对照表的存量 34 篇(棘轮 34)
✅ test-untouched-proof 通过 15 项
```

**全量复跑**:111/111 全在场全过 · 断言 **3,867 → 3,870(+3)**。

> 这一条规矩值不值,今天有答案了:**它第一次跑就抓到了两处判据缺陷,还抓到了我修它时新造的第三处。**
