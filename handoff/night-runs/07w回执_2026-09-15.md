# 07w 回执 · 2026-09-15(§一 / §四 / §五 / §六 先交)

**本令 md5 两处一致**:`5038907c7c2dc5a1d5b17c232053351d`(桌面夹 + 仓里 `handoff/`,与令里给的第三个值也对上)
**读数时刻**:2026-09-15 18:06:47 CST
**读的是哪个库(J-64)**:本机库 `apps/api/local-data/lucky-luxe.sqlite`;造病跑的是台子自己的临时库 `/tmp/ll-ci-data.*`

---

## §一 · 🔴 四格闭合表 —— **退回收下,而且退回是对的**

**我上一份报的「该咬没咬 0」是假的,真值是 1。** 根因你点破了:
台子**先用 `CLAIMS`(名字里说没说「成功」)过滤了一道**,只对通过过滤的绿分类 ——
**基线 14 条里 7 条一格都没落**,而那 0 恰恰是四个数里唯一一个能让人放心的数。

**已修**:所有非红**逐条落格,不做任何预过滤**;落完**台子自己加一遍**,加不上底数就拒绝出结论(J-66 第二款)。

### 逐条落格(现测,不是抄的;刀 = 注掉 `UPDATE settlements SET status='signed'`)

| # | 断言 | 实测 | 格 | 理由(可反驳) |
|---|---|---|---|---|
| 1 | 前置:实例起得来 | 绿 | 无关 | 它断言的是**服务进程起没起来**;造病改的是 `settlements` 那一行写不写。两者无交集 |
| 2 | 前置夹具:顾客从正门登录 | 绿 | 无关 | 断言的是 `users.wechat_open_id` 认人结果;不读 `settlements` |
| 3 | ㋚0a 本店真的开了定金 | 绿 | 无关 | 断言的是 `deposit_config` 读得到;发生在签字之前 |
| 4 | ㋚0 下出待付定金的单 | 绿 | 无关 | 断言的是 `bookings.status='PENDING_PAYMENT'`;签字尚未发生 |
| 5 | ㋚1 mock 支付落库 | 绿 | 无关 | 断言 `payments` + `bookings` 两张表;签字那一行不在其中 |
| 6 | ㋚1b 付过再付 → 拒 | 绿 | 无关 | 同上,且它读的是 `payments.transaction_id` |
| 7 | ㋚2 stripe confirm | 绿 | 无关 | 同 ㋚1 |
| 8 | ㋚3 卡包 | 绿 | 无关 | 断言 `coupon_grants`;不读 `settlements` |
| 9 | ㋚4 积分空态 | 绿 | 无关 | 断言「**还没消费时**出 0 条」;签字之前就成立 |
| **10** | **㋚5a 开单+签字造景** | **🔴红** | **红** | 库里那张单变成 `pending_sign` |
| **11** | **㋚5 积分跟过去了** | **🔴红** | **红** | 赚分从签署单推,单没签 → 0 条 |
| **12** | **㋚6 签完变已签只读** | **🔴红** | **红** | 另一入口读到的不是 `signed` |
| **13** | **㋚7 已签的单不再出新码** | **🔴红** | **红** | 单没签成 → 还能再出码 |
| 14 | ㋚8 定金守恒仍 ok | 绿 | **无关** | 🔴 **见下,这一条是本节的重点** |

**四格:红 4 · 仍绿-无关 10 · 仍绿-该咬没咬 0 · 没跑到 0 —— 合计 14 ≡ 底数 14 ✅ 闭合**
**说不清:0 条。**

### 🔴 第 14 条要单独说,而且它连带一处 J-55 更正

`㋚8 定金守恒` 一开始落在**「该咬没咬」**那一格(台子修好后第一次跑就报 1)。
我没有直接把它扫进「无关」,而是**去查了那条口读哪些表**:

```
auditDepositConservation() 读:deposit_disposals · deposit_receipts · deposit_retains · finance_transactions
                          —— 一个字都不碰 settlements
```

**签字那一行写不写库,它读的四张表一张都不变 ⇒ 它绿是对的,判「无关」。**
**可反驳**:指出它其实读了 `settlements`,这条判定立刻推翻。

> 🔴 **连带更正(J-55)**:07s §七③ 把「定金守恒」列为签署口三条该咬的判据之一,
> 而它**结构上不可能咬到签字落库**。
> **所以签署口实际是 ㋚6 / ㋚7 两条在守,不是三条** —— 我上一份回执里「三条判据守着」这句话,
> 按 J-55 在此更正为**两条**。

---

## §五 · 裁 #103 ② 那把「全站 0 处」的刀 —— probe 三样

**跑了没有**:上一批**只有套件内的 ㋝1b 自守,没有独立 `--probe` 入口** —— 按 J-58⑤ 这不够。**现在补跑了。**

**靶子**(两必中 + 两形似而非):

| 样本 | 该不该命中 | 现测 |
|---|---|---|
| `<button id="doSignup">创建账号</button>` | 该命中 | ✅ 命中 |
| `<a href="/signup">Create Account</a>` | 该命中 | ✅ 命中 |
| `/* 案底:这一屏原来叫「创建账号」 */` | **不该**(注释里的案底) | ✅ 不命中 |
| `const registeredCount = 3` | **不该** | ✅ 不命中 |

**现测命中数:2 / 2 该中,0 / 2 不该中 → 分得开。**
**修没修**:扫描逻辑本身没改(它本来就剥注释);**补的是 probe 入口**,让「0 处」这个结论有依据。

> 按你的要求:**probe 过了之后,「全站 0 处」这句话才立得住。** 在此重申这句话现在成立。

### ② 第三条「号在与不在,提示与返回体一致」—— **两份响应贴不出来,原因要说清**

**网页顾客端根本没有「按手机号查人 / 按手机号登录」那条口**(㋝3 现扫 0 条,造病加一条带 query 的 → 当场红)。
**没有那条口,就没有「号在」和「号不在」两种请求可发,也就没有两份响应可贴。**

这不是「我没做」,是**裁 #100 那个分法里的「保证」那一格**:
这个状态**本来就不该出现**,所以治法是把「不许有」写成判据,而不是去造出它来对比。

🔴 **但按你 §五 的字面要求,我如实报:两份原始响应,我交不出来,因为那条路不存在。**
**如果你要的是「万一以后有人加了这条路,两份必须一致」的判据**,那一条我可以加(现在是「不许有这条路」,更严)。**要不要加,归你。**

---

## §四 · 两问(机械 diff,不推断)

**1. 主档 120 → 122,哪两套?门关档的链条?**

| | 新增 | 它守的那件事 |
|---|---|---|
| 主档 116 → 118 | **`web-not-signup`** | 裁 #103:网页顾客端不是新顾客的入口(入口 0 处 / 不通向建档 / 没有按号查人的口) |
| | **`import-phone-guard`** | 导入时没手机号的行**要报出来并挡住**,不许静默导进去 |

**门关档:21 →(夜12 +1 `coupon-status`)→ 22 →(07v +2 `web-not-signup` `import-phone-guard`)→ 24。**
🔴 **不是一批加了三套,是跨两批。** 你问「哪三套」——**三套是对的,但它们不在同一批**,这一点我报清楚。

**2. 红 8 的分母变了,两笔分开:**

| | 数 | 怎么来的 |
|---|---|---|
| 新增那 3 套里红的 | **0 套** | 现读最近一次全量日志,`grep` 三个套名在门关红行里,零命中 |
| 原 21 套里红的 | **8 套,与上批同一批,一套没清也一套没多** | 逐条:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` · `identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` |

> **所以「红数没变」这句话在这里是成立的,而且举得出证:分母多的那 3 套全绿,红的还是原来那 8 个。**

---

## §六 · `customer.js` 压线 —— 摘取计划(一句话)

**本批动过 `customer.js`**(裁 #103 改文案),已经**先摘后动**:
款文与理由摘进 `apps/web/customer-auth-copy.js`,**2,758 → 2,755**,正好压在棘轮上。

**下一批的摘取计划(一句话)**:
> **把 `customer.js` 里那张中英文案表(`zh` / `en` 两个大对象,现测约 290 行)整块摘进
> `apps/web/customer-copy.js`**(照 `admin.js` → `admin-copy.js` 的先例),
> **摘完预计 `customer.js` ≈ 2,465 行**,给后面的改动留出约 290 行余量。

---

## 数(本节只报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **122 / 122 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **24 套 · 红 8**(新增 3 套全绿) | 同上 + 逐条 grep |
| 小程序档 | 3 套 · 红 0 · 空转 0 | 同上 |
| 断言(在册) | **4,040** | `assertion-baseline.json` 求和现测 |

**棘轮**(`wc -l < 文件` 现测,**带基准**):
`customer.js` **2,758 → 2,755(相对 2,758 是 −3;相对棘轮 2,755 是 ±0,正好压线)**
`local-server.mjs` **17,631(相对上批 17,631 是 ±0)** · `admin.js` 7,928(±0)

**跨批账本自检**:`8cf9acc 领先 326 + 5 = 331` ✅(上一批已现出;本批收尾再报一次)

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
**生产库未动**(本批一次都没连)· 本机库未动。

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §七 ④⑥⑦⑧⑨⑩ | **未做** —— 按你 §七「本批先交 §一 和 §五」,先交这两件 + §四 + §六 |
| ② 第三条的两份响应 | **交不出来,原因:那条路不存在**(见 §五);要不要加「万一以后有」的判据,等你一句话 |

