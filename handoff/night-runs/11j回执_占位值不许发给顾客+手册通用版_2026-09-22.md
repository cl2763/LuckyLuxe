# 小批 11j(含 11j 补)回执 · 占位值不许发给顾客 · 使用手册通用版

**上游令(两份,md5 两端一致)**
- `handoff/小批11j补_取消等批点_预批判别式_立J113_2026-09-21.md` — `661167766024c3ac276ae15d0dd02481`
- `handoff/小批11j_真地址电话落地_纠正TBD归因_立J112_2026-09-21.md` — `039804baaebf0669c848fc9ce760c267`

**先读补令,照办。** 补令把 11j §一.2「第二步:等我批」整步作废。
🔴 **本批全程没有停过一次**,J-113 那条「停下来等不是一种处置,是一次故障」守住了。

> **生产库全程只读**,这一批对它发出的每一条都是 `SELECT`。
> 本机库 / 沙箱库有本批开批快照(11i 收批那两份即本批开批基线),收批对照见文末。

---

## 一、🔴 预批判别式的结果:**第 1 条就不成立,§一 整件没跑**

按补令 §二,先跑只读:

```sql
SELECT id, name, address, phone, timezone, currency, tenant_id
FROM stores WHERE tenant_id = 'luvia-bj';
```

**生产库返回行数 = 0。**

| # | 预批条件 | 实测 | 结论 |
|---|---|---|---|
| 1 | 返回**恰好 1 行** | **0 行** | 🔴 **不成立 ⇒ 不批** |
| 2–5 | — | — | 前提已断,不再逐条查 |

**更进一步**:生产上**全部租户只有六个** ——
`demo-ai · demo-basic · demo-jienail · demo-lucky-luxe · jics-nail · lucky-luxe`,
**没有 `luvia-bj` 这个租户。** 仓库里有 `tools/seed-luvia-bj.mjs`(22,738 字节),但它没在生产上跑过。

⇒ 按补令:**不批,这一件停,原样贴回来,接着跑队列下一件。**
已登记进 `handoff/night-runs/等天亮_2026-09-22.md` 第 1 条,并**照补令要求写成一个能一句话答完的问题**:

> 北京旗舰店在生产上还没开出来 —— 是要我先把它开出来(跑 seed),还是这家店根本还不该上生产?(A 开出来 / B 先别开)

🔴 **附给店主一条判断依据**:生产上 `jics-nail` 那家店名是 `Jie'Nail 美甲美睫專門店`,
**地址是真的** —— `长兴南街华润悦府13号楼1单元2005室（20层）`。
它和令里给的 `北京市朝阳区建外街道CBD万达广场6号楼2005室` **都以「2005室」结尾但不是同一个地方**。
**如果「北京旗舰店」指的其实是 `jics-nail`,这条的答案就完全不同。** 我不替她判断。

---

## 二、§三 三家店的地址电话(只量不改,生产只读)

| 租户 | 店名 | address | phone |
|---|---|---|---|
| `lucky-luxe` | LUVIA 半径 | **`Address TBD`** | **`Phone TBD`** |
| 🔴 **`jics-nail`** | **Jie'Nail 美甲美睫專門店** | **`长兴南街华润悦府13号楼1单元2005室（20层）`** ← **是真地址** | **`""`(空)** |
| `luvia-bj` | — | **这个租户不存在** | — |
| `demo-ai` | 星野美甲 · 演示店 | `演示地址 · 仅用于产品演示` | `000-0000-0000` |
| `demo-basic` | 悦容美甲 · 演示店 | `演示地址 · 仅用于产品演示` | `000-0000-0000` |
| `demo-jienail` | 屿见美睫(演示店) | `""` | `""` |
| `demo-lucky-luxe` | 云栖美甲(演示店) | `""` | `""` |

**回答 11j §三 那张表里的三格:**

- `lucky-luxe`「现在还是吗?」→ **是,还是 `Address TBD` / `Phone TBD`**
- 🔴 `jics-nail`「地址电话是真的假的」(令里写「本批我最想知道的事」)→
  **地址是真的,电话是空的。不是 `TBD`,是空字符串。**
- `luvia-bj`「核对 §一.2」→ **核不了,这家店在生产上不存在**

---

## 三、🔴 判据 A / B —— 两端出口普查,**两端都比令里说的更漏**

### 我先把出口数全了(J-112:有几个出口就得验几次)

**小程序顾客端 6 个出口,原本一个都没挡:**

| # | 出口 | 字段 |
|---|---|---|
| 1 | `pages/home/index.wxml:49,51` | `store.address`(**带 `bindtap="copyAddress"`**) |
| 2 | `pages/home/index.wxml:63,65` | `store.phone`(**带 `bindtap="callStore"`**) |
| 3 | `pages/store-location/index.wxml:16,20` | `store.address` / `store.phone` |
| 4 | `pages/checkout/index.wxml:63,64` | `store.address` / `store.phone` |
| 5 | `pages/booking-done/index.wxml:23` | `order.store.address` |
| 6 | `pages/order-detail/index.wxml:36` | `order.store.address` |

**网页顾客端 4 个出口,原本只挡了 2 个:**

| # | 出口 | 原本 |
|---|---|---|
| 1 | `customer.js:967` `storeContactLine`(用在 781 / 1358 / 2056) | ✅ 有 `/TBD/i` |
| 2 | `customer.js:1813` 订单详情地址 | ✅ 有 `/TBD/i` |
| 3 | **`customer.js:939` 切换门店列表** | 🔴 **一点没挡** |
| 4 | **`customer.js:1778` 订单卡地址** | 🔴 **一点没挡** |

🔴 **所以 11j §〇 那句「网页端早就挡住了」对一半** —— 它对出口 1、2 成立,对 3、4 不成立。
**这正是 J-112 自己说的那件事,我把它用在了出令人身上。**

### 🔴 D108 复发登记(比令里说的重一档)

`home/index.wxml:49` 不只是把 `Address TBD` 印出来 —— 它是

```html
<view class="shop-card-row" wx:if="{{store.address}}" bindtap="copyAddress">
```

`'Address TBD'` 是 truthy ⇒ `wx:if` 成立 ⇒ **`bindtap` 跟着挂上**。
顾客点「导航·复制」,复制到的是 `Address TBD`;点「一键拨打」,拨的是 `Phone TBD`。

而紧挨着上面那行注释写的正是:
> 「🔴 D108:有地址才出「导航·复制」;没有就只留一句如实说明,**不挂 bindtap**
>   (不可用即不呈现,呈现即说明 —— 摆着又不响是第三种,永远错)」

**D108 修的就是这种死口,占位值把它复活了。** 护栏已加(套件 ③d)。

### 改法:回到唯一出口,不是补 6 个 wxml

新建 `miniprogram/utils/placeholder-words.js` + `apps/web/placeholder-words.js`(词表 + `realValue()`)。

- **小程序**:只改 `utils/api.js` 的 `toMiniStore()` 一处。
  `order.store` 也走它(`toMiniBooking` 里 `store: toMiniStore(booking.store)`),
  **所以 6 个出口一次全好** —— 这就是改唯一出口的理由,套件 ③e 把这条复用关系钉住了。
- **网页**:4 个出口逐个接上同一个出口,并**删掉全仓最后一处手写 `/TBD/i`**(套件 ④e 守零残留)。

**词表**:`['TBD', 'placeholder', 'N/A', '待填', '待补充', '占位', '未填']` —— **就是 11j 判据 A 明列的七个词**。
🔴 **令里理由段提到的「待定」我没有自己加**(J-107:法只授权它明写的那件事),
写成一句是/否问题进了「等天亮」第 3 条。

**两端为什么是两份**:小程序和网页是两个独立的包,谁也 import 不到谁(全仓零先例,现查过)。
所以各存一份,**由判据守住两份词表那一行逐字一致** —— 改一端不改另一端当场红。

---

## 四、判据套件 `test-store-placeholder` · 41 条全过

| 组 | 验什么 |
|---|---|
| ① | 两端词表**逐字一致** · 就是明列的七个词(不多不少) |
| ② | 判别式本身:TBD / 大小写 / 中文占位 / 空值 → 挡;🟢 **三条反向守**:两条真地址(**其中一条就是生产上 jics-nail 那条真值**)+ 真电话不许被误杀 |
| ③ | 小程序:`toMiniStore` 两个字段都过 · `addressText`/`phoneText` 也过 · 六个出口读的仍是那两个字段 · **D108 复发护栏** · `order.store` 复用同一映射 · 无裸兜底残留 |
| ④ | 网页四个出口逐个接上 · **全仓零残留手写 `/TBD/i`** · 词表件在 `customer.js` 之前加载 |
| ⑤ | 🔴 **J-107 行为等价**:8 个值逐个比「抽取前」与「抽取后」,对 TBD 那一类行为必须一模一样 |
| ⑥ | 🟢 **阳性对照**(令里点名要的):塞 `待补充` → 新判别式认得出;**而旧规则 `/TBD/i` 对同一个值是放行的** —— 证明真换了东西,不是数量凑巧对上 |
| ⑦ | 条数自守 `EXPECTED_CHECKS = 41`(判据五) |

**已进主档 + 门关档。**

### 两把刀(突变自检条)

| 刀 | 造什么 | 结果 |
|---|---|---|
| ① | 小程序那份词表加「待定」,网页那份不加(**最会发生的那种**) | 🔴 `①c 两端词表逐字一致` + `①d 七个词` 双红,退出码 1 |
| ② | 有人把 `address: realValue(...)` 改回 `store.address \|\| ''` | 🔴 `③a` + `③f` 双红 |
| 还原 | 两次都还原 | ✅ 41 条全过 |

### 🔴 条数自守当场咬了我一口

我第一版声明 `EXPECTED_CHECKS = 34`,实跑 **40** —— 判据自己红了。
顺着去看,发现 **③e 是我写的一条恒真断言**:`mpApi.__testOnly === undefined || true`。
**那正是 11j 说的「判据形状的祝福」。** 换成可证伪的那条(`order.store` 必须复用 `toMiniStore`),
并补了 ③f(无裸兜底残留),最终 41 条。

---

## 五、《有迹商家使用手册》通用版 v1(补令说「今晚最值钱的产出」)

`handoff/有迹商家使用手册_通用版_v1_2026-09-22.md`,**259 行**。

按 J-110 先数了已有的:桌面夹那份《使用手册.md》(90 行,2026-07-17)是**平台运营者**的手册,
满是机器名、且已过时(还写着「多商户商家端未开放」、店名还是 Lucky Luxe)。**商家手册是全新一份。**

**按 11e 的硬要求逐条对:**

| 要求 | 做到没 |
|---|---|
| 全程人话,一个机器名都不许出现 | ✅ 机械扫描 `tenant`/`endpoint`/`CSV`/`API`/`token`/「租户」→ **0 处命中** |
| 按她真会遇到的顺序,不按功能模块 | ✅ 登录 → 看今天 → 客人到店 → 收钱 → 签字 → 晚上对账;不常做的排第二部分 |
| 每一步配「做错了会怎样 / 怎么退回去」 | ✅ 每节都有,共 11 处 |
| 🔴 明写哪些现在还不能用 | ✅ 第三部分整节,**六条,每条都是现测的** |
| 不许把店名 / 技师名 / 价目表写进去 | ✅ 一个都没有 |

**第三部分那六条是量出来的,不是猜的**:
微信支付(`confirmMockPayment` 三处,`WXPAY_*` 全仓 0 处)· 小程序未上架(`USE_LOCAL_SANDBOX = true`)·
企微(生产 0 家配客服号,`external_contact` 全仓 0 处)· 订阅消息(全仓 0 处)·
门店码(无真码,11i §三 已量)· 档案不能合并。

🔴 **按 11e:手册要先给 Cowork 读一遍,「凡是读着要停一秒的句子都要改」。这一版是给 Cowork 的稿,不是终版。**
**还缺**:这一版写的是网页后台的路径,小程序商家端(35 个页)大同小异但按钮位置不同,下一版补一节。

---

## 六、🔴 棘轮当场拦了我一次

回归红在:

```
not ok 2 - ② 棘轮 8 项零上涨 :: apps/web/customer.js 2463 > 棘轮 2458(+5)
```

我加的注释把 `customer.js` 抬高了 5 行。**棘轮不许抬** ——
按裁#89 走「抽取」:把那段解释**搬进新建的 `placeholder-words.js`**(新文件不在棘轮上),
`customer.js` 只留一行指针。改完 **2458,正好等于棘轮**,套件 41 条仍全过。

**顺带**:预检还拦了两次,两次都拦得对 ——
① 新建的三个文件没 `git add`(没入册的文件对每一把护栏刀都是隐形的);
② **D188 运行判据过期** —— 我给 `index.html` 加了一行 script 并 bump 版本,
八件界面文件的指纹从 `bdc835e44873` 变成 `44441c214ee9`,上一份报告当场作废。
重跑并补了报告 `handoff/night-runs/D188运行判据_2026-09-22.md`,**12/12 全过**
(签单页要真单号才有内容,从**沙箱库**取了 `LU-20260920-MFOJ` 造景,正文 221 字 ⇒ 真渲染了)。

---

## 七、固定六项

**① 改了什么**
新建两份占位词表(两端)+ 判据套件 41 条;改 `toMiniStore` 一处(带好小程序 6 个出口)、
`customer.js` 四个出口;`index.html` 加载词表件并 bump 版本;进回归两档;
四份文档(手册 / 等天亮 / D188 报告 / 本回执)。
**生产库一个字节没写。**

**② 在哪一层验的**
渲染层(小程序 wxml 的 `wx:if` 读哪个字段、网页四个出口的模板串)+ 行为层(`realValue` 直接跑)
+ 生产数据层(只读 SELECT)。**像素没验 —— 见第八节自评。**

**③ 同类扫尽**
全仓 `/TBD/i` 手写残留 → **0**(判据 ④e 常驻);
小程序 store 地址/电话出口 → 6 个逐个点名;网页 → 4 个逐个点名。

**④ 用什么数据验的**
反向守用的是**生产上 jics-nail 那条真地址**,不是我编的;
阳性对照用 `待补充`,并证明**旧规则对它是放行的**。

**⑤ 重复稳定性**
套件跑了 6 轮(写、改 ③e、两把刀、两次还原);全量回归跑了 4 轮。

**⑥ 残留与不确定**
「等天亮」6 条(`luvia-bj` 不存在 / 判据 C / 「待定」加不加 / D207-b / 多伦多真值 / 11i 裁决表);
手册待 Cowork 读一遍;小程序商家端那一节没写。

**巨型文件:`local-server.mjs` 17,539(未动)· `admin.js` 7,893(未动)· `customer.js` 2,458(回到棘轮,未涨)**

---

## 八、L1–L5 自评

| 律 | 做到没 | 说明 |
|---|---|---|
| **L1 末端验证** | 🟡 **部分** | 验到渲染层的**代码**(`wx:if` 读哪个字段、模板串接没接上),**没有在真机/模拟器上截图看那六个页面现在长什么样**。按 L3,像素这一格挂 ⬜。 |
| **L2 同类扫尽** | ✅ | 两端出口逐个点名 + 全仓 `/TBD/i` 零残留常驻 |
| **L3 无图不结案** | 🟡 | 占位态「空框+相机+还没有图片」那一类有合同图;**门店地址空态长什么样没有图**,所以没标已验证 |
| **L4 反例数据** | ✅ | 拿真会露馅的数据验:`待补充`(旧规则放行的)+ 生产真地址(不许误杀) |
| **L5 重复稳定** | 🟡 | 套件与回归多轮;**没做「连续 5 次进出页面」** —— 本批无交互类交付 |

🔴 **最该补的仍是 L1 的像素那一格**:我证明了「映射层收成空串 ⇒ `wx:if` 落空 ⇒ 不挂 bindtap」,
这是**从代码推出来的**。真机上那六个页面现在到底显示「门店地址待补充」还是别的,**我没看过**。
**不写「全部通过」。** 建议下一批用开发者工具截六张图收口。
