# 🔴 D194 痕迹查询 · 生产库**非 0** · 查实是**我们自己的压测残留**

**读数时刻**:2026-09-16 02:55:25 CST
**读的是哪个库(J-64)**:`/app/apps/api/local-data/lucky-luxe.sqlite` · 5,980,160 字节 · mtime `2026-08-27T04:56:40.243Z` · inode 15
**只读标志**:`readOnly: true` 显式传 · **四条查询一字不改** · 🔴 **一行都没动**

---

## 一 · 结论先写

> **生产库里有 7 行带 `google_id` 的用户 —— 确实出自 `POST /auth/google/demo` 那条无门禁的路。**
> **但不是外人打的:是我们自己的 `e2e-stress-test.mjs` 在 2026-07-03/04 云部署验证期间打上去的。**
> **零消费 · 零预约 · 零结算 · 零储值 · 零券 · 零次卡 · 零定金 —— 七行都是惰性的。**

**三条独立证据,指向同一个结论:**

| 证据 | 现测 |
|---|---|
| **名字** | 七行的 `display_name` **全是 `Google E2E`** |
| **那个字符串只出现在一处** | `origin/main:apps/api/e2e-stress-test.mjs:57` `displayName: 'Google E2E'` —— **全仓再无第二处** |
| **时刻** | 2026-07-03 两次(活调用,带毫秒 ISO)· 2026-07-04 16:03:27 五条(**同一秒**,是身份回填迁移在给老行补身份,不是五次调用) |

**而 2026-07-03/04 正是云部署 + 数据迁移那两天。** 压测脚本的 `base` 取自 `process.env.API_BASE`(默认 `127.0.0.1:4000`)——
**那两天有人把 `API_BASE` 指到了生产上跑了一遍。**

---

## 二 · 四条查询原文结果(裁 #108 批准的那四条)

```
── ① 有没有被打过
   {"n":7}

── ② 底数(0 是「没有」还是「没扫到」靠它分)
   {"users_total":77,"has_google":7,"has_wechat":8}
   [schema] users 列:id, display_name, phone, email, wechat_open_id, google_id, tenant_id,
            tags_json, notes, birthday, is_migrated, legacy_total_spend_cents
            ⚠️ 生产比本机少一列 avatar_url —— 逐列先探存在这一步是必要的

── ③ 逐行形态(不取姓名 / 完整邮箱 / 电话)
   7 行,全部 tenant_id=lucky-luxe · no_phone=1 · no_wechat=1
   2026-07-03T13:17:05.931Z · 2026-07-03T13:23:57.776Z
   2026-07-04 16:03:27 ×5

── ④ 身份行
   {"provider":"email","n":7} · {"provider":"google","n":7}
```

## 三 · 非 0 预案要的三样(08a §四),逐样只读补齐

**甲 · 落在哪几个租户**:`[{"tenant_id":"lucky-luxe","n":7}]` —— **全在店主本店,没有跨租户。**

**乙 · 时刻分布**:见上。**`users` 表根本没有时间列**(生产与本机都没有),所以时刻只能从 `user_identities` 取;
那五条同秒的是**回填迁移**的时刻,不是创建时刻 —— **这七行的真实创建时刻,库里没有记录。**

**丙 · 有没有消费 / 预约 / 余额**(逐表数):

| 表 | 行数 |
|---|---|
| `bookings` | **0** |
| `settlements` | **0** |
| `stored_value_transactions` | **0** |
| `coupon_grants` | **0** |
| `member_timecards` | **0** |
| `deposit_receipts` | **0** |
| `finance_transactions` | 没有 `user_id` 列(按单号关联,这七行没单) |
| `payments` | 没有 `user_id` 列(按预约关联,这七行没预约) |

**丁 · 对照**:全库 `user_identities` 105 行,时刻跨度 `2026-06-24` → `2026-08-27` ——
**这七行不是孤立于某一天的异常簇,它们落在正常运营时间线的最开头那两天。**

---

## 四 · 这件事真正的后果(**不是入侵,但也不是没事**)

1. 🔴 **「生产 users 77」这个数里,有 7 行不是真顾客。** 此前所有引用 77 的地方(含 08-28 那次只读现测、上线清单、店主看的统计)**都把这 7 行算成了顾客**。**真顾客数是 70。**
2. **它证明了这条路确实被打通过 —— 在生产上,真的。** 此前只能说「代码层没有闸」,现在能说「**它被走过,而且走它不需要任何凭据**」。
3. **好消息是它没有造成任何业务后果**:七行零消费零预约,不进任何账。

---

## 五 · 我做了什么、没做什么

**做了**:四条批准的查询(一字不改)+ 预案要的三样只读补查 + 一条对照。
🔴 **没做**:**一行都没动、没删、没改、没标记**。也没有在生产上执行过任何非 `SELECT` 的语句。

**从现在起本班不再连生产。**

---

## 六 · 要你裁的三件(已进待裁队列)

1. **这 7 行怎么办**:留着 / 打标记(需写生产)/ 清掉。**我倾向:先留着,等 D194 推上线把那条路带走之后再一起处理** —— 现在动它,「别人打的」和「我们清理的」就再也分不开(你 08a 的原话)。
2. **「生产 users 77」这个数要不要全仓更正成 70**(含上线清单与此前回执)。
3. 🔴 **我对停线的解释,要你确认**:夜13 §五.2 触发了,我判成「**停这一件 + 从此不再碰生产**」,**不是停全班** —— 依据是 J-77(停线停那一件不停这一班)与你「要它跑满八小时」那句话。**这个解释是我做的,不是你给的,所以我把它摆出来。** 如果你的意思是停全班,那我这一夜后面的活都该作废重议。
