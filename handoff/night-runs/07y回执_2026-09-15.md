# 07y 回执 · 2026-09-15

**本令 md5 两处一致**:`206cdeed0c7d05a1fa179f4db25eef7e`(桌面夹 + 仓里 `handoff/`,15,109 字节)
**读数时刻**:2026-09-15 22:39:00 CST · **读的是哪个库(J-64)**:造病台自己的临时库(4191)/ 回归临时库 `/tmp/ll-ci-data.*` / D194 现测用的是 `/tmp/ll-ci-data.d194*` 一次性库
**生产库未动 · 本机库未动**(本批一次都没连生产)
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

# 🔴 先报最重的一件:**D194 是真的,但不在邮箱那条路上,而且比立案时更严重**

**`POST /auth/google/demo`(`local-server.mjs:10761`)一行,没有任何门禁。**

```js
if (req.method === 'POST' && path === '/auth/google/demo') return json(res, 201, { user: registerGoogleDemoUser(await readBody(req)) })
```

四档现测(每档一个全新一次性库,逐档数 users 行数):

| 档 | HTTP | users 行数 | 建人了吗 |
|---|---|---|---|
| 甲 门开着 | 201 | 1 → 2 | 🔴 是 |
| 乙 **门关着(门关档)** | 201 | 2 → 3 | 🔴 是 |
| 丙 **生产口径 `NODE_ENV=production`** | 201 | 3 → 4 | 🔴 **是** |
| 丁 **生产口径 `RAILWAY_ENVIRONMENT=production`** | 201 | 4 → 5 | 🔴 **是** |

再测一次「最坏形态」——**生产口径 · 不带 `x-tenant-id` · body 里指名别人家的店**:

```
POST /auth/google/demo  {"email":"anyone@evil.example","tenantId":"jics"}   → HTTP 201
库里:{"id":"user_mu2rwjbt_rh4pcb","email":"anyone@evil.example","tenant_id":"lucky-luxe"}
身份行:{"provider":"google","tenant_id":"lucky-luxe"}
```

**无需登录 · 无需门店标识 · 无演示门 · 建出来的人回落到默认店(= 店主本店 lucky-luxe)· 还顺带写一行 `user_identities`。**

**归族**:
- **「收了一半」第五案** —— `/auth/email/register|login` 被 `DEMO_LOGIN_ALLOWED` 收了(`:10735`),**它的同族兄弟 `/auth/google/demo` 一个字没收**。同族:D75 商家侧收口顾客侧漏 · 登录旁路 08-07 · 读写两道闸 · 校验只活在前端。
- **裁 #103 的第三处入口** —— 而且是**唯一一处在生产上活着的**。
- **假数回落红线**:租户回落默认店。

**边界要说清楚(红线:不碰生产)**:以上全部是**本机四档隔离实例**现测。
**生产我一次都没连,所以「线上现在能不能打通」我没有实测,只能说代码层这条路上没有闸。**
是否要在生产上只读验一次(只发一个必然失败的探测、不建人),**归你裁**。

**照 D194 立案的三问逐条答**:

| 问 | 答 |
|---|---|
| ① 生产上 `ALLOW_DEMO_ADMIN_LOGIN` 未设时会不会也建人? | 🔴 **会**(丙/丁两档现测 201 建人)。**这条路根本不看那个变量。** 按你立案时写的判据,**这是上线阻断级。** |
| ② 建出来的人落在哪个租户? | **回落默认店 `lucky-luxe`**;租户取自 **body 的 `tenantId`**(不是请求头),给了不存在的店就回落。邮箱那条路是 `resolveTenant(req, query)` 且不带头直接 400 `TENANT_REQUIRED` —— **两条兄弟路口径不一致**。 |
| ③ 与裁 #103 冲不冲突? | 🔴 **冲突**。裁 #103:「建档案只在两处发生」。**这是第三处,而且不需要任何人点任何按钮。** |

**我没有动它**(停线:D194 只查不改)。等你裁。

## 顺带更正:**邮箱那条路是好的,是我 07x 的措辞把你引偏了**

四档现测:门关着 / 生产口径 → `/auth/email/login` 一律 **403 DEMO_LOGIN_DISABLED,users 一行没动**。
07x 我在突变自检那一栏写的「门关档下邮箱登录**真的把人建出来了**」——
**那句话的前提是「我刚把那句 403 注掉了」**,脱离造病上下文看就成了一条实况缺陷。
**这一笔记我头上(第 14)**:**造病现场的观察,写进报告时必须带着「刀还在」三个字**,否则它读起来就是产品的实况。

---

## 一 · 裁 #106 那 3 条 —— 逐条名字 + 可反驳理由

**表级底数 2 → 列级移出 1 → 剩余 1**(裁 #106 第 5 条:三数同报;方向锁死,只许从该咬移到无关)

| 条 | 格 | 理由(可反驳) |
|---|---|---|
| `㋚8 财务红线:整条链跑完,定金守恒仍 ok` | **列级移出 → 无关** | 读 `settlements.{code,id,tenant_id,tags,category}` / 写 `settlements.{status,signature_data,signed_at,disclaimer_accepted,updated_at}` —— **表相交、列不相交**。**可反驳**:指出守恒审计读了 `status` 或 `signed_at` |
| `㋚4 GET /my/points-history(积分)· 空态` | 🔴 **留在该咬没咬** | 列级**也相交**:`pointsEarnRows` 那条 SQL 读 `settlements.status` / `signed_at`,正是刀写的两列。**它确实在爆炸半径里,只是夹具数据没有区分度**(那位顾客一张单都没有,签不签字都是 0 条)。**这是 L4 反例数据律的一个欠账,不是误报。** |
| `前置:实例起得来` | **说不清**(单列) | 它打 `/health`,读集取不到。**不扫进无关。** |

**要我怎么处置 ㋚4,请一句话**:(a) 给它补一条**有单**的反例数据(推荐)· (b) 具名冻结 · (c) 维持现状。

---

## 二 · 台子自守套件四条 —— 已落地,**上线首跑就咬出一条真病**

新套件 `test-bench-selfguard`(13 条,已进主档;主档 122 → 123 套):

| 条 | 守什么 | 两面 |
|---|---|---|
| ①a/①b/①c | **分类闭合**:加不上底数 → **台子自身报错退出**(不是打一行红字然后照常往下说结论) | 闭合的放行 / 不闭合的必须报错 / 报错话里要点出五个数与底数 |
| ②a/②b | **红必打标记**:判了红而报告里只有数字没有名字 → 台子自身报错 | 点了名放行 / 只有数字必须报错 |
| ③a–③e | **兜底不许把两种失败合成一种**(J-65②):全仓 `.sh/.command` 现扫 **0 处** | 旧写法必咬中 / 先判存在的不咬 / 文案里提到的不咬 / 白名单棘轮 ≤ 0 |
| ④a/④b/④c | **probe 两面**(J-58⑥):**真跑**每一把带 `--probe` 的刀,要求两个数都打出来且两面非零 | 12/12 · 白名单跳过 1 把(`scanner-probe.mjs` 是共用出口本身,没有靶子可种)· 把数 ≥ 11 缩水即红 |

**probe 两面数(每把刀现跑)**:

| 刀 | 该中 / 不该中 |
|---|---|
| `readset`(含裁#106 列级)| 4 / 2 · 列级另一组 1 / 3 |
| `bench-selfguard` 自守一/二/三 | 1/1 · 1/1 · 1/2 |
| `knife-restore-scan` | 1 / 2(本批从手写改成共用出口)|
| 其余 8 把 | 各自 ≥1 / ≥1,④b 现测 **12/12 全过** |

### 🔴 ④b 上线**第一次跑就红**,咬出 `tools/shot-freshness.mjs`

它的 probe 明明分得开,**但脚本打完 probe 没有收口** —— 照常往下跑整套扫描,把 `--probe` 当文件路径,
打出 `not ok - 找不到 --probe` / `not ok - 一张图都没找到`,**最后以退出码 1 收场**。
于是外面看到的是「这把刀 probe 挂了」,而真相是「probe 过了,结论被后面那段覆盖掉」。
**开关模式不收口 = 它报的结论被后面那段吃掉** —— 与 J-62② 同族。已治(`process.exit`)。
**按 J-69:这是首跑红,是真病,不是造病。**

---

## 三 · 门关档 **8 → 4**(本批 7 → 4)

| 套件 | 上批去向 | 本批实况 |
|---|---|---|
| `card-refund` | (a) 07x 修 | 🟢 绿 |
| `booking-intake` | (c) 排 07y | 🟢 **本批修,绿** —— 夹具现在走的是:商家 `/admin/bookings/direct` 建**带手机号的轻档案** → 顾客拿同一个号 `/auth/wechat/mini-login` → **严格认人四条**认领 |
| `deposit-config` | (c) 排 07y | 🟢 **本批修,绿** —— 同一条正门路 |
| `auth-surface` | (c) 排 07y | 🟢 **本批修,绿** —— ㊙⑩ 不再钉死演示种子里的 `demo-cust-06`,改成顾客**自己从正门进来**,拿她真实的 id/openid 去签 |
| `identity-links` | (c) 排 07y | 🔴 **仍红,但红得对** —— 见下 |
| `noshow-aftersales` / `schedule-v2` / `staff-portal` | (c) 排 07z | 未动 |

### 🔴 `identity-links` 这一套我**归错了族**,在此更正(J-67:理由错也要回去改)

07x 我把它归 **A 族(夹具走演示邮箱登录)**。**错了。**
它的**题目本身**就是「email / google 演示身份写不写得进 `user_identities`」——
而门关档正是把演示登录关掉那一档。**所以它是 B 族:判据与本档前提冲突。**

已按 `card-refund` 同一形态改成**两档各守各的那一句**,而且**两档都守住这一套真正的题目**:
- 门开着 → 演示身份那条路(原判据原样);
- 门关着 → ①演示两条路必须被拒 ②顾客**走正门**进来 ③**身份真的写进 `user_identities`** ④身份带租户 ⑤不重复。

**改完它还是红** —— 红在 `②b /auth/google/demo 也必须被拒: 201`。
**这一条红的是产品,不是判据。它就是上面那件 D194 的真身。**
**去向:(b) 具名冻结 · 解冻条件 = D194 那条路收口的那一天。** 判据不动,红留着。

---

## 四 · 共用夹具改了一处,附血缘清单(四之八①)

`customer-login-fixture.mjs` 的正门建档原来**只对 `REST_DAY` 换日子**。
现测 `auth-surface` 在已有真实预约的库上撞的是 **`SLOT_UNAVAILABLE`** —— 夹具于是红在「这个钟点有人了」上。
改成**日子 × 钟点两层退让**(`REST_DAY` / `SLOT_UNAVAILABLE` / `OUTSIDE_BUSINESS_HOURS` 都退)。

**血缘清单(全仓使用方 7 套,逐个跑过)**:
`test-auth-surface` · `test-booking-cancel` · `test-booking-intake` · `test-coupon-status` ·
`test-customer-paths` · `test-deposit-config` · `test-knife-selfcheck`
—— **主档 123/123 全过**,7 套无一回归。

---

## 五 · 数(全部现测)

| | 数 | 增量与来源 |
|---|---|---|
| 主档 | **123 / 123 全过** | 套件 122 → **123**(+`bench-selfguard`)|
| 门关档 | **24 套 · 红 4** | 上批 7 → **4**(本批 −3;跨两批 8 → 4)|
| 小程序档 | 3 套 · 红 3 · 空转 0 | 真因仍是 `MP_AUTOMATOR` 没设(`miniprogram-automator` 仓外模块没装)|
| 断言 | **4,056** | 4,041 → 4,056(**+15**)|

**断言增量对账(判据五)**:预期 +15 = 自守套件 **13** + card-refund 前批遗留 0 + auth-surface ㊙⑩a **+1** + identity-links 净 **+1**;**现测 +15 ✅**

**棘轮**(`wc -l` 现测,带基准):
`local-server.mjs` **17,631(±0)** · `admin.js` **7,928(±0)** · `customer.js` **2,755(±0,仍压线,本批没动)**

**跨批账本**:`8cf9acc` 领先 326 + 本批 28 = **354** ✅

---

## 六 · 记我一笔第 14

07x §五 突变自检那一栏,我写的是:

> 「`Error: 生产闸-3 … 必须 403 …: 200 {"user":{…}}` —— 门关档下邮箱登录**真的把人建出来了**」

**那一栏的上下文是「刀还插着」,但那句话本身没有带上这三个字。**
于是它读起来是一条实况缺陷 —— 你据此立了 D194,方向对,**路错了一条**。

**律(自请):造病现场的观察写进报告,必须在同一句里带上「刀还在」。**
证据句和结论句之间隔着一把刀,**读的人看不见那把刀,就会把证据当结论。**

---

## 七 · 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| `noshow-aftersales` / `schedule-v2` / `staff-portal` | **(c) 排 07z**,修法同 A 族(夹具换正门)|
| §十 ⑥⑦⑧⑨⑩⑪(生产 37 行 · 路径总数重量 · 10 张券 · 82 个口 · 冻结理由) | **未做** —— 按你 §十「先交 1/2/3」,先交这三件 + 第 4 件(门关档 4 套)|
| `customer.js` 摘取 | **本批没动 customer.js**(2,755 ±0),摘取计划照 07x §六 待做 |
| 🔴 **D194 等裁** | 只查不改。三问已答,**生产可达性我没实测(不碰生产)** |
