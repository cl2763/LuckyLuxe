# 06f 回执 —— `--accent` 归一做完 · 主按钮三件落地 · 乙档 46 → 11(同尺)

> 2026-09-10 · 提交 **`69e35f6`(06f-一)· `dd91256`(06f-二)· 本回执(06f-三)**
> **未推 main · 未碰生产库 · 4128 一个字没写**(本批只读它一次:开批/收工快照)

---

## 〇、六项结构

### ① 改了什么

**§二 主按钮三件(裁①:采纳 `--hero`,不用 `#221c17`)**

| 要的 | 做的 |
|---|---|
| 用点旁注理由 | `styles.css` `.primary` 上方新增一段:`--hero` 名义上是**面色**令牌,这里当**字色**用不是手滑 —— 它是两档里最深的暖棕,金底两档都浅,所以一个令牌管住两档;并写明若被改回 `--heroink`,两档只剩 2.52 / 1.73。**色值不写进注释**(写死色棘轮连注释一起数)。 |
| 判据锚规矩不锚数字 | 新判据 **⑦**:本判据文件里**零个调色板色值字面量**,所有比值一律从 `design-tokens.css` 现读现算。WCAG 的 3.0 / 4.5 不在此列(那是标准的门槛,不是我们的色值)。 |
| 加一条造病 | 刀 **WW**:把主按钮字色改成 `--heroink` → ①b **红**,并点名 `apps/web/styles.css:1862 —— color: var(--heroink);`。 |

**§三 `--accent` / `--accent-dark` 归一(裁②)**

- **76 处** `var(--accent*)` 全换:当**字** → `--brandd`(4 处 `color: var(--accent)` 也归到 `--brandd`,因为规矩甲不许 `--brand` 当字色);当**面 / 边框 / 描边 / accent-color** → `--brand`。
- `:root` 里那两条定义**删掉**,两个名字从应用源码里消失;原来那段「故意不动」的注释改写成裁定与理由。
- **连带治了同族三处**「金底 + 奶白字 = 2.02」(和主按钮改之前一模一样的病):
  购物车勾选圈 `.check.checked` · 客服筛选胶囊 `.cs-filter-pill.active` · 财务导航按钮 `.fin-nav-btn.active` —— 字色都改 `--hero`。
- 立成规矩 **丙**:**金底上的字一律 `--hero`**,判据白名单式(见下)。

**判据 14 → 23 条**(`apps/api/test-color-usage.mjs`,已在全量里):

| 新增 | 是什么 |
|---|---|
| ⑤ | 归一白名单式全仓扫:`apps/web` + `miniprogram` + `apps/api` 三棵树共 **598 个代码文件**,`--accent` 的**定义 0 处、使用 0 处**;只认真形状(`--accent:` / `var(--accent)`),先剥注释再判 |
| ⑤b | 反向守:`--brandd` 当字两档都 > 4.5(深 9.59 / 浅 4.73),证明归一的去处确实更好 |
| ⑤c ×3 | 覆盖面**按棵树**自证(一棵树整个掉了,总数还能靠另两棵撑住 —— 那就是没有判据的覆盖面) |
| ⑥ ⑥b ⑥c | 规矩丙:**15 条**画金底的规则逐条落筐 —— 要么自己写 `color: var(--hero)`,要么在「没有字的色块」白名单(4 条,逐条写理由,条数上棘轮) |
| ⑦ | 判据自守:锚规矩不锚数字 |

**顺带**:`styles.css` 写死色棘轮 **34 → 31**(归一带走两条定义;棘轮只许往下收)。

### ② 在哪一层验的

- **判据层**:`test-color-usage` 23/23;`design-token-diff` **共 21 条 · 红 0 条**(合同图一个字没改)。
- **渲染层**:三页 × 两档共 6 张图,带拍摄时刻,`shot-freshness`(J-38)**6/6 过**;
  我自己看了图:深色档财务页 —— 「趋势」选中的金胶囊现在是深字、读得清;浅色档客户档案页 ——
  「刷新」主按钮金底深字;顺带在图上确认了 §五① 那个 `#b5885d` 的「高价值A」标是全页最糊的一个。
- **像素/DOM 层**:两档全站重扫(下面 ③)。

### ③ 同类扫尽

| 类(按机制定义,不按长相) | 机械证据 |
|---|---|
| `--accent` / `--accent-dark` 的定义与使用 | 三棵树 598 个文件现扫 → **0 处**;刀 XX(塞回一处使用)、刀 YY(塞回定义)各红并点名行号 |
| 「金底 + 非 `--hero` 的字」 | 全仓样式文件抠出 **15 条**金底规则逐条落筐;刀 ZZ(新写一条金底奶白字)红并点名 `styles.css:5765 .cs-filter-pill.active` |
| 「判据里抄色值」 | 刀 AB 红并点名 `68: const lightBtn = ratio('#2a2521', '#b09468')` |

**归因**(判据四:同一把刀要能分出谁在守):同一份被刀的数据喂**上一版判据** ——
XX / YY / ZZ / AB **四把全绿**,说明红是新层咬的,不是数量凑巧对上;
WW 两版都红,但**新版点到行号、旧版只糊上整条规则** —— 差别正是「报得出是哪一处」。

**刀留痕**:五把刀的注入点 / 命中位置 / 时刻 / 还原后回绿,逐条打在日志里(`/tmp/knife06f.log`)。
刀顺手咬出**判据自身两处缺陷**,当场修掉:
①剥块注释时把换行也删了 → ⑥ 报出来的行号比真位置早几十行;
②行号落在「上一条规则的 `}` 之后」而不是选择器那一行。
修完复刀:`.primary` 报 **1857**、`.cs-filter-pill.active` 报 **5765**,与 `grep -n` 的真行号**一字不差**。

### ④ 用什么数据验的(反例数据律)

- **两档 × 全站**,不是抽查:后台 13 页 4,898 个文字节点、顾客端网页 4 视图 220 个,浅深各一遍。
- **同一把尺子**:新旧两份红榜抬头都是刀 `b283463` 且都不带 `+dirty`,`same-ruler` 各 **6 条全过**
  (含第④条「被测版本互不相同」—— 防的是同一份量了两遍)。
- **最像样的误改**当刀:字色 `--heroink`(2.52 / 1.73)、金底新规则、判据抄色值、归一回滚两向。
- **心跳判据**拿沙箱库的**副本**下了两刀(见 ⑥)。

**结果(同尺)**:

| 面 | 甲档「看不见」 | 乙档条数 | 乙档去重 | 配色对种数 |
|---|---|---|---|---|
| 后台 | 0 → **0** | 114 → **32** | 46 → **11** | 11 → **5** |
| 顾客端网页 | 0 → **0** | 26 → **5** | 12 → **3** | 4 → **1** |

**「新出现」只有一种**,查到底了:`--brandd` 压 `--wall` = **4.08** ×3(两面各 3 处)。
逐元素对过 —— 就是原来 `--accent-dark` 压 `--wall` 3.54 的**同样那三处**,选择器一个不差
(客户档案 `customer-tag` / 客服工作台 `cs-avatar` / 门店设置 `plan-feature.on`;顾客端是
`#cartBadge` / 两个 `quick-icon`)。**升了 0.54 仍差 0.42 到 4.5**,所以还留在乙档,
**不是新坏了三处**。全表见 `handoff/night-runs/乙档新旧同尺对照_accent归一_2026-09-10.md`。

**小程序本批没重扫,理由写在明处**:`git diff 1f859a0..HEAD -- miniprogram` **是空的**——
本批一个字没动小程序,而 `--accent` 在小程序里本来就 0 处。
所以 v3.4 那轮的数仍然成立(深色 甲 0 / 乙 0 · 浅色 甲 0 / 乙 11),**不是「这轮扫过了」**。

### ⑤ 重复稳定性

- 五把刀各走「造 → 跑(红且点名)→ 还原 → 重跑(回绿)」四步,**五把全过**;`knife-backup.sh check` 无残留 `.pre-k`。
- 全量回归 **109/109 全在场全过**,整跑 **2 分 40 秒**;断言 **3,816 → 3,825(+9)**,
  增量与新判据条数**对得上**(判据五:计数即证)。
- 收摊:4128 / 4310 都在,J-33 收摊自证「没有残留的跑机/造病进程」。

### ⑥ 残留与不确定

**棘轮**:`local-server.mjs` **17,707**(未升)· `admin.js` **7,928**(未升)·
`styles.css` 写死色 **31**(收紧)· 小程序 wxss **2,443**(未动)。

**写库自报(三库分开)**:

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **未碰** | 本批零次 `railway` 调用 |
| **本机库** | **未动** | 开批 / 收工两份快照:92 张表 11,366 行,**0 张表行数变动**;租户指纹**一致** |
| **沙箱库** | **有动,全是心跳** | `notification_logs` 300 → 302 · `reminder_tasks` 887 → 891;`heartbeat-shape` **9 条全过**并把放行的行逐条打印(两条 `reminder_tasks` PENDING→SENT、两条日志追加、旧行一条没动) |

**本批修掉的一个判据缺陷(判据也是代码,也会坏)**:
`heartbeat-shape` 对「**开批快照之后才新建、随即被推进**」的行报**假红** ——
它的倒推哈希当然不在开批快照里,因为那一行开批时根本不存在。
补法**不放宽已有那一档**:开批就在的行倒推不上照旧红(新增 **②c 反向守**),
只给「窗内新生」的行另开一档,而且那一档也要过形状(建于窗内 + 时刻不倒流 + 状态在允许集合)。
拿沙箱库**副本**下两刀验过:**刀 CD**(开批就在的行只按规矩推进)→ 绿;
**刀 EF**(同一行多改一列 `payload_json`)→ **红**,且被 ②c 点名「开批就在的一条都没被放走」。

**不确定 / 没做到的**:

- **L4 只做到一半**:乙档剩下那 11 条我只按**配色对**归了类,没有逐条去页面上看「这个字在真实数据下是不是常出现」。
- **顾客端登录态、小程序 `card-pack` 仍未扫**(夹具没造);**渐变面 717 / 30 个节点仍判不了**(棘轮压着,没长)。
- **J-38 的判天口径偏粗**:它把 `apps/api/**` 整棵树当「会影响界面的代码」,于是提交一次
  `apps/api/assertion-baseline.json`(测试基线,改不了任何像素)就把 6 张图判成过期。
  我没有去改这把刀(改判据来让自己的图通过 = 换尺子),**改成重拍了一次**。
  这条精度问题登记在 §二,由店主决定要不要收。

---

## 一、要店主裁的两件(都不影响本批收工)

### ① 乙档剩下那 7 条是**合同图的边界**,不是用法错 —— 三条走法

`--brandd` 浅档压**白卡**正好 4.73(过);压 `--wall`(`#f1eee6`)掉到 **4.08**、
压 `--tagmbg`(`#faf3e3`)掉到 **4.28** —— 都差最后 0.2~0.4。

| 走法 | 动什么 | 代价 |
|---|---|---|
| 甲 · 把浅档 `--brandd` 压深一点 | **改合同图**一个值,全站跟着走 | 浅色档的金整体沉一点(**审美归店主**) |
| 乙 · 这几块小面换个底 | 把 `customer-tag` / `cs-avatar` / `plan-feature.on` / 金卡标的底从 `--wall`/`--tagmbg` 换成白卡 | 视觉层次少一档 |
| 丙 · 记账不改 | 4.08 / 4.28 都在「读得清但差 AA 一点点」区间 | 乙档永远清不到 0 |

### ② 判据 J-38 的精度(要不要收窄)

现在「本批代码提交」只要 `apps/api` 下任何文件动了就算,包括测试基线、判据文件。
收窄成「只看真会改像素的路径」(`apps/web` / `miniprogram` / `apps/api` 里出 HTML/JSON 给前端的那些)
能少逼几次重拍,但**判据收窄本身有风险**(漏掉真会改像素的后端渲染)。**要不要动,请店主定。**

---

## 二、现查出来的两件同族的,**只报不动**(已登记,建议归下一批)

> 店主的口径是「**任何时候不许两个值并存**」。照这条,**本批只做到「`--accent` 这两个名字消失」,
> 值还在另外两个文件里活着** —— 这句必须说在明处,不许拿「名字没了」冒充「值统一了」。

### ① 第三个金 `#b5885d` —— 和 `--accent` 一模一样的病,**26 处**

- **也没有深色档**(硬编码字面量):红榜里 light / dark 两行的比值**一模一样都是 2.76**;
- **也是「金底压奶白字」**:`admin.html` 的 `.fin-goal-go` 就是那个病;
- 分布:`apps/web/admin.html` 3 · `apps/web/customer-tags.js` 1(高价值A 标)·
  **小程序商家端 22**(subscription / attendance / customers / member / salary-plan / salary-month /
  finance / staff / points-mall / orders 十页 wxss);
- 建议走法与 06f §三 **完全同一套**;小程序那 22 处本来就压在 wxss 写死色棘轮里,归一会把棘轮往下带一截。

### ② `--accent` 的两个值在另外两个文件里**改名活着**

`apps/web/platform.html:8` 与 `apps/web/sign.html:10` 各写着
`--gold:#c8a47e; --bronze:#9b7655` —— **正是本批从 `styles.css` 删掉的那两个值**。
这两页都不加载 `design-tokens.css`,自带整套局部调色板。
**为什么本批不动**:把它们接到令牌上正是下一批 **D188**(`06c §三`:令牌引用统一入口 + 静态判据 +
运行判据 + 全仓 html 扫)的事;只换字面量写法而不接引用,等于把第二套真相搬个家。

---

## 三、自评 L1–L5

| | 做到没有 |
|---|---|
| L1 末端验证 | ✅ 三页两档实拍并**自己看过**,不是只看断言绿 |
| L2 同类扫尽 | ✅ 三类各给机械证据 + 造病点名;**并且报出了本批治不了的第三类(`#b5885d`)** |
| L3 无图不结案 | ✅ 配色有合同图 v3.4;规矩丙是从图的用法推出来的,写进判据 |
| L4 反例数据 | ⚠️ **只做到一半** —— 两档全站扫到了,但剩下 11 条没逐条回页面看真实数据下的出现频率 |
| L5 重复稳定 | ✅ 五刀四步全过 · 回归 109/109 · 无残留进程与备份 |

**停线自查**:未推 main ✅ · 未碰生产 ✅ · 未看密钥文件 ✅(沙箱用的是代码里的默认口令
`owner-demo-token`,`start-sandbox.sh` 本来就不喂 `.env`)· 无 sleep 轮询 ✅ ·
**没有松判据、没有抬棘轮、没有换尺子** ✅(棘轮 34→31 是**收紧**;心跳那条是修**假红**,
且用 ②c 反向守 + 刀 EF 证明老行照旧红)· **没有改合同图来凑代码** ✅(21 条全绿是代码搬向图)。
