# 小批 05v 回执 —— D184 → 段1还原 → 裁#21 → D186 → D185(等批)→ D173 → D174/D176 → 六通 v6 → 三端截图对照表

> **未推 main** · **生产库未动** · **本机库:有动 —— 但不是本批写的**(原文与查证见文末附录,归 待裁 #23)
> 提交:`adee04c` → `d6e7000` **十个**(`5d07e6f..HEAD`)
> 巨型文件棘轮:`local-server.mjs` 17,708 → **17,708** · `admin.js` 7,928 → **7,928**(都未升)
> 三端截图对照表:`handoff/night-runs/07截图/对照说明.md`(14 个文件 / 12 个画面)
> 六通 v6 原文:`handoff/ai-eval-results/05v_像人六通_v6_原文_2026-09-09.md`(请打分)

## 一、每段一行

| 段 | 做了什么 | 提交 | 证据 |
|---|---|---|---|
| D184 | **「新增持卡 604」查清了:604 不是一个数,是一帧动画** —— 四张小牌共用一个滚动记忆键(`dh-tile-v:today`),第四张读到第一张的旧值就从它那儿往上滚 | `adee04c` | 死判据 +「持卡 ≤ 顾客总数」12 组全等;造病(键去掉 metric)→ 红 |
| 段1还原 | 标着「(演示)」的**八位改回原名** —— 上一批给夹具改名时把「这是演示数据」这条信息抹掉了,方向反了 | `459efc7` | 4128 那份现成对照表逐位还原;**只动沙箱库** |
| 裁 #21 | **小程序截图第三条路跑通** `screencapture -l <windowid>` | `0d18a6a` `2da8cbe` | 3 张真窗口图落 `07截图/01–03` |
| D186 | 台面分栏后右列被裁 → 「要么看得全,要么看得出还有」 | `5cb9766` | 刀量包围盒 + 提示;本批**又加一层**(见 §三 J-37) |
| D185 §二 | **等批,4128 一个字没写** | — | 只读清单 `D185_4128待清单_2026-09-09.md`;本批对 4128 **0 处写** |
| D173 | 上半:星期几收进出口(模型写错就地改)· 下半:店休给两个真替代 + 确认句不再被吞 | `e7bcede` | 活服务重放:「明天(9月10日,周四)」现推;确认句接得住 |
| D174 | 顾客告别时 AI **一个字都没回** → 任何态下告别都必须有回应,**静默一律算红** | `61ed8fe` | 判据 2 条 + 刀 AA;六通 v6 六通 **0 轮静默** |
| D176 | 上文在聊美睫,问「最便宜的是哪种」答成美甲 → 当句没点名品类就跟上文走 | `61ed8fe` | 判据 3 条 + 刀 AA;v6 通二轮 3 现测:答「裸感自然睫」 |
| D187 | **六通 v6 重放时自己看见的**:一条回复里同一句问了两遍 | `e60301b` | 判据 5 条 + 刀 BB 两处;活服务重放已消失 |
| 六通 v6 | 修完再跑一遍,原文落档 | `0e4a29b` | 机械四看:7 项表 0 · 超 120 字 0 · 重复句 0 · **静默轮 0** |
| 对照表 | 三端「图上元素 → 判据 → 截图」26 行 + 交不出图的 4 行 | 本次 | `07截图/对照说明.md` |

## 二、六项结构

**① 改了什么** —— 见上表。代码面共 7 个文件:`dashboard-home.js`(D184 滚动键)、`date-human.mjs`+`reply-hygiene.mjs`+`repeat-guard.mjs`(D173/D174/D187 出口)、`booking-intake.mjs`(店休两替代 + 确认支路)、`fixed-price-reply.mjs`(D176 品类回落)、`today-board.js`/`styles.css`(D186)。新判据 3 把刀升级。

**② 在哪一层验的** —— 三层都验了,分开写:
- **活服务层(店主会看到的那一层)**:六通 v6 在 4310 活服务上跑完,**原文逐句落档**;D173/D174/D176/D187 各自在活服务上重放确认。
- **像素层**:14 张截图(网页 CDP 真渲染 / 台面刀自带 / 小程序真窗口),对照表逐行指到文件名。
- **断言层**:全量回归(见 §五)。

**③ 同类扫尽** —— 逐条给机械证据:
- D184 的类 = 「共享一个记忆键的滚动值」:全仓 `dh-roll` 键现扫,每处必须带 `metric`,新来的自动红。
- D187 的类 = 「出口改了文本却不报 why,壳把它扔掉」:壳改成**看文本变没变**,不再依赖报告(同族:静默失败器)。
- D174 的类 = 「某一支回空串」:告别这一支补上;**不是告别就不硬塞话**(反向守,判据②)。
- 币符字面量:本批**自己撞红一次**(注释里抄了截图上的金额)——`test-currency-scan` 全仓 278 文件绿 = 0 残留。

**④ 用什么数据验的**(反例数据律)——
- D176 用**跨品类**上下文(上文美睫、当句不点名 / 当句点名美甲)两向各验一次;
- D174 用**空回复**夹具(壳层直接喂 `answerZh: ''`),这是活服务里偶发、单元里必现的那个态;
- D187 用**采集模板**那条路的真句子(它正是原来整段早退、刀落不到的那条路);
- D184 用**12 组全等**(三店 × 四维度)+ 持卡数 ≤ 顾客总数的死判据;
- 六通 v6 用**三家店 × 六种真顾客话术**,含北京店(¥ / 北京时间)。

**⑤ 重复稳定性** —— 六通 v6 **整跑两遍**(D187 修前 / 修后),两遍都 0 静默、0 重复句;
小程序布局刀本轮跑了 2 次(段 4 一次、收尾一次),12 条两次全过;台面刀三店 × 深浅跑了 3 轮。
**没做满 5 次**的:网页三档主题切换(真点过 1 次)。

**⑥ 残留与不确定** —— 见 §五「没做的 / 没做满」。

## 三、判据自己的两条(都按「刀落了不红先怀疑刀」处理)

1. **D187 的归因**:`dedupeTail` 的**单元判据一直是绿的**,而病在**出口那一层** ——
   函数对、位置错(排在采集模板早退之后)、且改了不报。**在缺陷存在时照样绿的判据就是废判据**;
   现在四条判据从出口验,刀 BB 两处各红一条。
2. **建议立 J-37「『在不在』不等于『看得见』」**(待裁 #24):
   台面那条「右边还有 ›」是 `position:absolute` 挂在横向滚动容器里的,而判据只问 `hidden === false`。
   **现测这一处是好的**,但刀 CC 把它改成 `left:100%` 挂到画布外面时:
   **老判据「提示=true」照样绿,新判据「在可见区里」红**并报出「提示右=982 可见区右=938」——
   这就是新层真在守的凭据(判据四:同一把刀要能分出哪层在守)。

## 四、顺手咬出来的真问题

- **两个 12 小时的僵尸进程**:昨夜 D167 那把「假套件永不结束」的刀,`run-all-tests.sh` 被知情地改掉了单套超时,
  刀跑完没退出 —— `test-mp-knifehang.mjs` 的 node 进程**活了 12 小时 52 分**(文件本身早删了)。
  已清。归 **J-33(跑机自己收摊)**:造病刀也要收摊,不能只收文件不收进程。
- **`run-all-tests.sh` 不许接 `| tail`**:脚本收尾会把 4128/4310 重新拉起来,
  那两个后台服务**继承了管道的写端**,`tail` 永远等不到 EOF —— 看起来像「回归卡死 25 分钟」,
  其实**早跑完了**,输出全卡在 tail 的缓冲里。以后一律 `> 日志文件 2>&1`。
- **币符扫描又咬到我自己的注释**(第二次):我把真机截图上那串金额原样抄进了注释。判据是对的,改的是注释。

## 五、全量回归 / 没做的

**全量回归**:`bash apps/api/run-all-tests.sh`(带 `MP_AUTOMATOR`)——**108 个套件全过**,整跑 **2 分 33 秒**(套件 2 分 16 秒 + 夹具与起停 17 秒)。
断言 **3752 → 3780(+28)**。

**断言增量对账**(判据五 · 计数即证 —— 「全过」只说明没红,不说明跑过):
- 本批新增 `check(` 源码行 **31** 条(`git diff 5d07e6f..HEAD -- apps/api/test-*.mjs`);
- 其中 **3 条在 `test-seed-rich` 里**,而这个套件**按 D169 的裁定就不许进全量**
  (预检第 ⑦ 条现扫:`run-all-tests.sh` 里对 `seed-demo-rich|test-seed-rich` 的引用必须是 0 处);
  它是**手动对沙箱跑**的,本批单跑过:**通过 100 项**;
- 其余 **28 条全部在顶层**(逐条查过缩进与外层块,没有一条被 `if (...)` 包住);
- **31 − 3 = 28 = 实测增量**,一条不差。

跑的过程里**自己红过两次,两次都是判据对、我错**(照录,不美化):
1. `test-currency-scan` 红 3 处 —— 我在注释里抄了真机截图上那串金额。改注释,不改判据。
2. `test-untouched-proof ③` 红 —— 我新写的两份文档里出现了「本机库未动」这句话却没带对照表,
   棘轮 34 → 36。**补的是对照表原文,不是把棘轮抬到 36**(抬棘轮 = 为了让自己变绿去松判据)。

### 没做的 / 没做满

- **L5 连续 5 次**:六通 v6 跑了 2 遍、小程序布局刀 2 遍、台面刀 3 轮;**网页三档主题只真点过 1 次**。
- **小程序「站内选深色」仍未生效**(待裁 #22 等裁);`02_小程序_商家首页_深色.png` 是**病的证据**,不是好的证据。
- **D185 §二**:等批,本批对 4128 **0 处写**。
- **jics-store 时区**(05u 已裁准改 `Asia/Shanghai`)与 **#14 存量清单**:本批没排,顺延。
- **中文字体**:小程序仍是系统字体(引不了外部样式表),05u 已裁接受;这句话没有改成「已按图落地」。

### L1–L5 自评

- **L1 末端验证**:✅ 做到。六通在活服务上跑、原文落档;14 张截图从店主会看到的那一层拍。
- **L2 同类扫尽**:✅ 做到,四类都给了机械证据(见 §二③)。
- **L3 无图不结案**:⚠️ 部分。小程序深色那一档**没有「对的样子」**可比,所以挂在待裁 #22,没标已验证。
- **L4 反例数据**:✅ 做到。空回复、跨品类上下文、采集模板那条路、12 组全等 —— 都是「能让它露馅」的那种数据。
- **L5 重复稳定**:⚠️ **没做满**。网页三档主题切换只真点过 1 次(1 次成功)。原因如实写:时间用在 D187 与两次回归上。

## 六、待裁

- **#24(新)**:建议立 **J-37「『在不在』不等于『看得见』」** —— 详见 `待裁_2026-09-09.md`,刀 CC 的红/绿对照已在里面。
- 结转:**#21**(截图三条路,第三条已跑通,建议关)· **#22**(小程序站内深色没生效,两条修法二选一)· **#23**(`reminder_tasks` 心跳白名单)。

---

## 附:「本机库」对照表原文(J-23:这一行由刀打印,人不许手写)

**结论行照录:🔴 本机库:有动。** 动的是什么、为什么与本批无关,查证如下 ——
`reminder_tasks` 那 8 行是 `arrival_reminder` 从 `PENDING` 翻成 `SENT`,
最后一次发出时刻 `2026-09-09T06:00:30Z`(= 门店时区 02:00),**是 4128 自己的提醒调度器在跑**;
本批对 4128 的操作只有**只读查询**(D185 清单、这份对照)。登记在 待裁 #23。

**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/06_本机库快照_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 76→84(+8) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     notify_scan_marks  行 54→58(+4) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 5 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notify_scan_marks · 只新增 1 行(10→11)
    hoptest-demo2 · notify_scan_marks · 只新增 1 行(10→11)
    luvia-bj · notification_logs · 只新增 8 行(61→69)
    demo-ai · notify_scan_marks · 只新增 1 行(10→11)
    demo-basic · notify_scan_marks · 只新增 1 行(10→11)

  🔴 既有行被改动/删除 1 处:
    luvia-bj · reminder_tasks · **旧行消失 8 行**(行 80→80,其中新增 8)
```

🔴 **本机库:有动** —— 逐行指纹查出既有行被改/被删(快照那把刀看不见这一类):
```
    luvia-bj · reminder_tasks · **旧行消失 8 行**(行 80→80,其中新增 8)
```

