# 10a + 10b 回执 · 2026-09-20

**点名(J-81)**
- `小批10a_上线了_四裁_WAL让一条旧豁免到期_立J103J104_2026-09-20.md` md5 `76e8d67fad0d849d0f1b2c97c38a1925`(桌面/仓一致)
- `小批10b_签署留档开工令_图v2顾客端只读_棘轮两处我裁了_2026-09-20.md` md5 `f4ccff4b0da4e890345a23b66a10dff1`(桌面/仓一致)
- 🔴 **10a 之前从没落过地**(仓里没有、没有回执),10b 点名它是上游我才发现。**两份一起补交。**
- **合同图(每批重贴,10a §三.4 新口径):** 签署文件留档 v2 —— https://claude.ai/code/artifact/6366076a-97e4-4af4-b711-cc2e7468e94f

---

# 甲 · 10a 五条

## 1. 🔴 进容器直接量 D194(§一 要的那十秒)

```
容器 8e124c9f234a · /app/apps/api/local-server.mjs(1,093,761 字节)
J-96 阳性对照  grep -c "path === '/health'"              → 1     ← 尺子咬得动
D194 第一串    grep -c "path === '/auth/google/demo'"    → 0
D194 第二串    grep -c 'function registerGoogleDemoUser' → 0
```
**这次是量出来的,不是推出来的。** 按 J-72 **没有再去打那个路由一次**。

🔴 **顺带如实报一个非零**:`grep -rn "auth/google/demo" /app/apps /app/tools | wc -l` → **16**。
逐条看过:**全是注释、墓碑与断言**(`local-server.mjs:5734/10747` 是「已删」的墓碑注释,
其余在 6 个测试文件与 2 个普查工具里)。**没有一条是活路由。**
—— 报 0 不能把 16 藏起来,所以两个数都在这儿。

## 2. 🔴 补拍的当期备份

```
容器内产出 : /tmp/prod-after-push-20260920T081500Z.sqlite  (拉完已删)
本机绝对路径: /Users/changliu/Documents/Codex/2026-04-29/new-chat/backups/生产库_推main后_20260920T081500Z.sqlite
字节数      : 6,135,808
容器 sha256 : ed2069e0a256b367cf500231a825bf857463f0acbccc09bdecc31241720ee334
本机 sha256 : ed2069e0a256b367cf500231a825bf857463f0acbccc09bdecc31241720ee334   ← 两头一字不差
拷法        : VACUUM INTO(走 tools/db-backup.mjs 唯一出口)—— **不是 cp**
表数        : 92 ✅(10a 要求的就是这个数)· 触发器 21 · PRAGMA integrity_check = ok
```
| 表 | 生产库 | 备份件 |
|---|---|---|
| users | 77 | 77 |
| bookings | 1,242 | 1,242 |
| payments | 149 | 149 |
| user_identities | 181 | 181 |
| settlements | 10 | 10 |

**两家真店**(同一次会话两头各读一次):`lucky-luxe` 37/161/0/30 · `jics-nail` 0/1/0/0 · tenants 6。
⚠️ 一处变动如实报:`user_identities` **105 → 181**(推前那份是 105)。`users` 仍是 77 没动 ——
这是开机迁移回填出来的,不是多了人。卷上也留下了迁移自己拍的 `lucky-luxe.sqlite.pre-dropdefault-2026-09-20T06-31-42-952Z`。

## 3. 自动备份第一拍 —— 落下来了,但**没赶上新代码**

```
/app/apps/api/local-data/backups/  共 35 份
最新 lucky-luxe-2026-09-20.sqlite  5,980,160 字节  Sep 20 04:55
容器 pid1 起于                                     Sep 20 06:31:41
```
🔴 **这一拍在部署前 96 分钟落的** ⇒ 它仍然是**旧代码 `cp` 出来的那一份**。
而且**卷上 35 份日备全是 `cp` 出来的**(字节数 35 份完全一样 = 主文件大小)。
好在那时生产还是 `delete` 模式,所以它们是好的。
**第一份由 `VACUUM INTO` 产出的自动备份是 2026-09-21 04:55 UTC** —— 下一批回来核。
(节拍不是 6 小时,是**每日 04:55 UTC** 一拍、30 天滚动;10a 里写的「6 小时节拍」与实况不符,以现测为准。)

## 4. 🔴 D202-b —— 量出来了,而结论比令里预设的更难看

**先纠一件事实:那处 `copyFileSync` 不在我当初说的位置上。**
它在 `local-server.mjs` **第 137 行、模块顶层、开机时**跑;而库连接 `new DatabaseSync` 在**第 142 行**。
我 09o 给的、09x 据以批豁免的那句「**跑在接口线程里、导入事务之前**」——**把位置说错了**(J-84:先定位,再归因)。

**四档现测**(`/tmp/d202b`,不是推的):

| 档 | 场景 | 结果 |
|---|---|---|
| 甲 | 137 行的**真实**位置(开机、进程里零连接) | `VACUUM INTO` ✅ **4ms** |
| 乙 | 我当初**声称**的位置(WAL + 另一连接握着写事务) | ✅ **6ms** —— 照样不打架 |
| 丙 | 对照:**delete 模式** + 握着写事务(= 09x 批豁免时的环境) | ✅ **2ms** |
| 丁 | 要害:WAL 库上 `cp` 主文件 | 🔴 读出来是 **`no such table: a`** |

⇒ **丙这一档说明:那个前提从来没成立过,不是「条件变了才到期」。**
J-104 的说法(条件变了 ⇒ 豁免到期)成立,但**实情比它难看** —— 它是个从一开始就不对的理由,
只是没人拿一把尺子去量过。如实记。

**处置**:按 10a §三.2 第 2 支 —— **换成唯一出口 `backupDb`**。
- `local-server.mjs` **`copyFileSync(` 调用点归零**
- `test-backup-wal` 两层白名单**双双清零**,上限从 `≤1` / `<=2` 一起拧到 **`=== 0`**(裁#105:抽取要压低棘轮,不留余量)
- 顺带清掉登记在案的两笔残留:`demo-reset.mjs` 形参里的死参 `copyFileSync` 摘掉,`local-server.mjs` 不再往里传

🔴 **还发现 ④c 那条判据自己在说谎**:标签写「≤ 1」而代码写 `<= 2`,**说谎的方向恰好是放松的那一边**。已两边归零并用同一个字面量渲染标签。

## 5. D205 / J-102 —— 已排第二段第一批

🔴 **但 J-102 当场就兑现了一次**:补拍备份时照 runbook 敲 `node tools/db-backup.mjs`,
**在生产容器里当场崩**:`ReferenceError: statSync is not defined`。**两个病**:
① `statSync` 用了没 import;② **CLI 自己又手抄了一遍 `VACUUM INTO` + 当场验**,而文件顶上就 export 着 `backupDb`
—— 「唯一出口」这四个字对 CLI 那条路是**假的**(一件事两处真相)。①是②的必然结果。
**改法只能是②**:删掉抄本,改调 `backupDb`。
**最坏的地方在于:VACUUM 其实做完了,命令却报失败** —— 照 runbook 走的人会以为「备份没成」而重来或放弃。

**永久护栏**(`test-backup-wal` ⑥a–⑥f,新增 6 条):真跑一次 CLI · 备份件打得开数得出行 ·
把那个病原样注回去必须红 · **同一份坏副本 `node --check` 照样绿**(证明只能靠真跑守)· CLI 零 `prepare('VACUUM INTO')` · 反向守。

---

# 乙 · 10b 第一步(商家端 + 后端 + 判据)

## 0. 🔴 开工前咬出的一处「图与令不一致」—— 已按图走,请裁

| | 图 v2 | 10b §〇 的表 / 我昨夜的计划 |
|---|---|---|
| 第二种类型 | **价格表确认书**(屏2 选项 · 屏4 两行 · 「已拍板①」逐字枚举,**共 3 处**) | **护理同意书**(**1 处**,机器名批的是 `consent`) |

**10b §〇 自己定了「以图 v2 为准」⇒ 取图。** 显示名 = 价格表确认书。
机器名图上没有(不可见),取 **`pricelist`** 而不是 `consent` —— 一个叫 `consent` 的字段里装价格表,是给下一个人埋雷。**记入假设清单,等你裁。**

## 1. 棘轮两处(10b §二 两条都照办,**没有动用 (乙) 额度**)

| 文件 | 上批 | 本批 | 净 |
|---|---|---|---|
| `apps/api/local-server.mjs` | 17,613 | **17,535** | 🟢 **−78** |
| `apps/web/admin.js` | 7,928 | **7,893** | 🟢 **−35** |

**抽走了什么:**
- 后端 (甲):**对象存储 COS 那一族 84 行** → 新模块 `apps/api/cos-upload.mjs`(99 行)。
  一个字没改,只把两处外部依赖改成注入(`createHmac` 自己 import、`IS_PRODUCTION` 由 `createCos({isProduction})` 传)。
  换回来的是 **3 行**(import + 构造 + 一行分发)。
  ⚠️ 如实登记(**没动它**):`cosDeleteObject` **全仓零调用方**。搬过来照样导出是为了「原样搬出」;删不删是你的裁决。
- 前端:`loadCustomerNotes` **42 行** → 新文件 `apps/web/customer-notes.js`(同一个页面的邻居面板,公约②)。
  `admin.js` 换回来的是 **7 行**(一个 section + 一次调用)。

## 2. 建表后「多出什么」(照 09n 件 C)

| 变化 | 数 | 会不会覆盖既有数据 |
|---|---|---|
| 新表 `signed_docs` | **0 行** | 不会 |
| 新表 `signed_doc_pages` | **0 行** | 不会 |
| 新触发器 `signed_docs_no_delete` / `signed_doc_pages_no_delete` | 2 条 | —— |
| 现有表改动 | **0 处** | —— |
| 一次性迁移 | **0 条**(D204 刚删两条,不再新增) | —— |

`test-schema-consistency` 现跑:两张新表被识别,**4 项断言全绿**(「没有表消失 / 没有列消失 / 老表新增列都写了 ALTER」)。
列另走 `try/catch ALTER`(`customer_visible` / `created_by_name` / `bytes`)。

## 3. 判据:`test-signed-docs` **63 条全过**,10b §四 七条逐条对上

| 10b §四 | 断言 | 反向守 |
|---|---|---|
| 1 删不掉 | ⑥a 直连库 `DELETE` → 触发器 ABORT · ⑥b 页也删不掉 · ⑥d 零 DELETE 路由 | ⑥c 同一把 DELETE 打在没上锁的表上必须删得掉(证明不是句柄只读) |
| 2 作废留痕 | ④b/④c 三样齐 · ④a 原因必填(后端拦) | ④d 重复作废 409 且不产生第二条留痕 |
| 3 越店 | ③b 读口 404 · ③e 写口 404 | ③b反 B 店取自己的 → 201(证明 B 这条路通) |
| 4 顾客只拿自己的 | 本批顾客端**不开口**,先守住**两个条件都判**的地基:③c 技师越界 404 | ③c反 服务过的 → 200 |
| 5 作废的 / 不可见的 | ⑤f 落库 `customer_visible`:rights/pricelist=1 · other=0 | ⑤g 开关能当场改 |
| 6 不登录 | ③a 401/403 | ③a反 同一条请求带 owner 令牌 → 200 |
| 7 多页顺序 | ⑦b 按 `page_no` 出、页数对 · ⑦c 取第 2 页拿到第 2 页 | ⑦d 取第 9 页 404(不许回落到第 1 页) |
| 红线 | 本模块零 `INSERT INTO users`/`user_identities` · 一个公开口都不开 | 喂一行真 INSERT 给尺子必须中 |

**🔴 读写两道闸(判据二)**:③a–③c 验**读**,③d–③e 验**写**,两个方向各有断言 —— `service_notes` 正是写口漏了一整批。

**corner case 覆盖**:①边界(0 页 / 21 页 / title 61 字)②空态 ③越权四种 ④幂等(重复作废)⑤异常输入(类型不在白名单 / other 不填名 / 非图片 / 不是 data: / 悬空 id)⑥最低层(直连库)。
**缺的一类如实写:并发时序** —— 这一件没有抢同一个资源的路径(两份文件互不影响,建单已包在事务里),没造出有意义的并发场景。

### 🔴 判据当场咬出**五个真缺陷**(全部已修,每个配了护栏)

1. **`page_no` vs `pageNo`** —— 详情口漏蛇形,取页口回驼峰,**同一个事实两种拼法**。前端必然有一处写错。
2. **`voided_by` 是空串** —— `OWNER_TOKEN` 那条路 `requireAdmin` 回的是 `{role,provider,technicianId,tenantId}`,**一个身份字段都不带**。
   口径②要的是「作废要留谁」,留个空串 = **留痕看起来做了,其实没记下是谁**。改成 `actorOf()`:说得出就写谁,主钥匙那条路明写「平台主钥匙」,**连这都说不出来就抛错**(判据一:不许用 `|| ''` 静默兜底)。
3. **`created_by_name` 同族** —— 一起改。
4. **`window.owner` 是 `undefined`**(浏览器现测)—— `admin.js` 里 `owner` 是 `const` 词法全局,不挂 `window`;
   而 `request`/`toast`/`escapeHtml` 是 `function` 声明,挂得上。于是我写的「用户切走就丢弃」守卫 **一次都没生效**(`&&` 短路把「守不住」悄悄变成「不守」)。改走裸 `owner`,并加 ⑨a–⑨c 三条护栏。
5. **radio 被全局 input 样式撑成大圆饼** —— 浏览器实拍才看见,合同图上是 15px 小圆点。按图钉死。

### 🔴 我自己的判据也咬了我三次 —— 同一个毛病

⑥e(test-backup-wal)、④c3、⑨a **都是「扫源码时把注释里的提及当成代码里的执行」**(J-61①)。
第三次就不再逐条打补丁:`test-signed-docs` 里所有源码扫描统一过 **`codeOnly()`**,注释先剥掉。
**注释里怎么写案底都不算数,只有真会跑的那一行算。**

## 4. 🔴 双端同批律:小程序商家端**同一批做了**

`miniprogram/pages/merchant/customer-profile/` **确实有同一个页面** ⇒ 律成立,没往后排。
- `utils/api.js` 四个口(与网页打**同一条**路径,不是各写一份)
- 页面:读 / 传(`wx.chooseMedia` 复用小记那条 base64 通道,公约④先搜复用)/ 作废
- wxml 两态 + 上传半屏 + wxss;**签署文件排在服务小记之上**(照图屏1)
- 断言 ⑩a–⑩g:**渲染链闭合**(api.js 映射 → setData 字段 → wxml `{{}}` 绑定,断一节就红)· 两端零拼串 · 两端都没有删除 · `wx.*` 都带 fail · 单页 107 行 ≤ 600

## 5. L1 末端验证律:在**店主会看到的那一层**验的

浏览器(127.0.0.1:4171,自己造景:林小雅 2 份一有效一作废 + 一位空态顾客)逐屏对图:

| 屏 | 现测(DOM 逐字) | 判 |
|---|---|---|
| 1 空态 | `还没有签署文件` / `拍照或从相册上传` + `+ 上传文件` | ✅ 逐字 |
| 1 有文件 | `全部 2 份 ›` · `会员充值权益确认书 / 09-20 上传 · 平台主钥匙 / 有效` · `价格表确认书 / 已作废`(灰+半透明缩略图) | ✅ 逐字 |
| 2 | `上传签署文件`·`取消`·`这是哪一份?`·三个选项逐字·`拍照`/`从相册选择` | ✅ 逐字 |
| 3 | `存进林小雅的档案`(未拍页时 disabled)·`多页的价格表可以连着拍,存成同一份。` | ✅ 逐字 |
| 4 详情 | `顾客/上传/页数/状态` 四项 · `2 页` · `有效` · `标为作废` · **`hasDelete: false`** | ✅ 逐字 |

⬜ **一处如实写明**:浏览器面板对**长页面滚动截图**有渲染偏移(整页截图拿到的是错位的空白区),
所以屏1 那一块**只有 DOM 逐字核验,没有整块像素图**;屏2/屏4 是 `position:fixed` 弹层,**拿到了像素图**。
这一条按 L3 挂 ⬜,不写成「已核验」。

## 6. 第二步(顾客端只读)—— **没做,按令**

10b §一「第一步没有全套判据绿之前,不许动第二步」。判据绿了,但**按 §五.6「做完第一步报我」先停在这儿**。
`customer_visible` 列已建好、口径已定死(`other` 落 0),第二步接上去即可。

🔴 **那条红线一个字没碰**:本模块**零公开口**、**零 `INSERT INTO users`**,判据里各有一条常驻守着。

## 7. 🔴 预检又咬了我两口 —— 都是真的

**第一轮预检直接红,全量没起。** 五条,两条是我这批带进去的:

1. 🔴 **`apps/api/signed-docs.mjs:async` —— 自由标识符**
   我把出口写成 `handle: async (...a) => Boolean(await handle(...a))`,
   预检那把扫描器把内联的 `async (` 当成一个「用到了却没声明」的标识符。
   **判据没错,是我给了它一个它读不了的写法。** 改成具名 `async function handleRoute(...)`,现扫 ✅ 无自由标识符。

2. 🔴 **小程序 wxss 写死色棘轮 2384 > 2350** —— 我给每个 `var(--x)` 都配了 hex 兜底(`var(--card,#fff)` 这种)。
   **兜底值也是写死色,深色态照样会漏白。** 现扫全仓既有 wxss:带 fallback 的 **0 处 —— 我是唯一一个这么写的。**
   遮罩那一层更要小心:既有写法都是裸 `rgba(...)`,但棘轮**只许降不许升**,
   **裁#89 不许为新功能抬** ⇒ 改成 `background:var(--hero);opacity:.45`,
   零字面色、不动令牌文件、也不加例外。**现扫 2350 = 基线,净 0。**

另外三条是「扫过期了」(我改了界面文件,红榜与运行判据必须重量):

| 项 | 处置 | 结果 |
|---|---|---|
| 护栏三列清单过期 | 重生成 + git add | ✅ |
| D188 运行判据过期 | 起沙箱 4310 + **自己造景**(从沙箱库取真单号 `RH-20260909-883C` 喂 `TRP_SIGN`) | ✅ **12/12 全过**(六页两档,令牌都取得到,body 底色都不是透明)· 报告 `D188运行判据_2026-09-20.md` |
| 对比度全扫过期 | 重跑 `contrast-sweep` 并落盘 | ✅ 甲档「看不见」**0 条** · 乙档「AA 欠账」38 条(去重 11 处,**存量,不是本批新增**)· 报告 `对比度红榜_10b签署留档_2026-09-20.md` |

⚠️ 一条提醒(预检不拦,如实转报):**J-43 棘轮对账说有 3 个棘轮「降了却没写归因」** ——
`local-server.mjs 17621→17613`(107a003)· `「未动」豁免具名清单 38→35`(b1d23d9)· `customer.js 2755→2458`(cd4ae24)。
**三条都不是本批的**(提交号都在前面)。本批自己那次下降(17613→17535)的归因写在提交信息里。

## 8. 🔴 全量回归第一轮又咬出一个 —— **抽取漏了调用方**

```
✗ /admin/store-clock 可读: {"error":{"code":"INTERNAL_ERROR","message":"cosConfigured is not defined"}}
```

(甲) 抽取把 COS 那一族搬进 `cos-upload.mjs` 之后,我**只解构了 `cosPutObject`** ——
而 `/admin/store-clock` 的自检块还在用 `cosConfigured()` / `cosUploadAllowed()`。**那条接口当场 500。**

🔴 **根因不是手滑,是我没做血缘清单**(波及面回归律①:改共享件必交「全仓使用方 + 逐个验证证据」)。
**是全量回归咬出来的,不是我自己想起来的** —— 这句照实写。

**血缘清单(补做):**

| 导出名 | local-server 代码里调用 | 全仓其他调用方 |
|---|---|---|
| `cosPutObject` | ✅ 签署快照 8660 · 签署文件模块注入 10299 | `signed-docs.mjs`(注入进去的) |
| `cosConfigured` | ✅ `/admin/store-clock` 自检 14373 | `test-settlement` 断言它 |
| `cosUploadAllowed` | ✅ 同上 | `run-all-tests.sh` 注释提到 |
| `cosAuthorization` | 0(只在模块内部用) | — |
| `cosDeleteObject` | **0** | **0 —— 全仓零调用方,等你裁** |
| `COS` | 0(只在模块内部用) | — |

**永久护栏**(`test-signed-docs` ⑪a–⑪c):模块导出的每个名字,只要 local-server 的**代码里**调到(注释不算),
就必须出现在解构里;**反向守**:从解构里拿掉一个仍被调用的名字,必须报缺;**覆盖面守**:两个名单都取到了才算数。
**下一次搬迁,漏一个就红。**

## 9. 🔴 常驻判据接着抓我三次 —— 全是真的,全按判据改(没有一次是放宽判据)

| # | 谁抓的 | 抓到什么 | 怎么改的 |
|---|---|---|---|
| 1 | **占位零回落律**(`test-image-placeholder` ②) | 我新加的 `<image src="{{item}}">` **没有字段级守卫** —— 一个读失败的空串会渲成灰方块,**看起来像「有图但坏了」** | 照本页小记图片那行现成写法(公约④ 先搜复用):`<block wx:for><image wx:if="{{im}}" …></block>` |
| 1b | 同上,自查带出来的 | 网页端详情里我写了 `<img src="${p.url \|\| p.data \|\| ''}">` —— 两个都空时留一个 `src=""`,浏览器画碎图图标 | 走唯一出口 `window.ImgPlaceholder.tag(p.url \|\| p.data, …)`。⚠️ 说清:`url \|\| data` **不是回落** —— COS 存的给 url、inline 存的给 data,**同一页的两种存法,不是两个语义** |
| 2 | **规矩丙**(`test-color-usage` ⑥) | `.sdoc-save` 画金底,字色我写了 `var(--card)` ——**浅色档下它恰好也是白的,看起来对,规矩上不对**;深色档 `--card` 变近黑,金底上就是深字压深底 | 改成 `var(--hero)`(规矩丙要求的那一个) |
| 3 | **wxss 写死色棘轮** | 我在解释规矩丙的**注释里**写了一个色号的十六进制 → 2351 > 2350 | 去掉。**判据连注释一起数,它数得对** —— 注释里的色号照样会被下一个人抄进代码 |

🔴 **这一节的意思不是「又修了三个小东西」。** 这三处 + 前面预检那两处 + 回归那一处 =
**六个缺陷全部是判据抓的,没有一个是我自己看出来的。**
而我在浏览器里**看过**那个金色按钮,它在浅色档下就是白字金底,**看上去完全正确** ——
`test-color-usage` 才知道深色档下它会塌。这正是判据律那句话的样子:**眼睛看对的,判据未必同意;该信判据。**

## 10. 🔴 常驻判据第二波:又是四次,其中**两次是把存量晒出来了**

| # | 谁抓的 | 抓到什么 | 怎么改的 |
|---|---|---|---|
| 5 | **二形法**(`test-ui-spec` ①) | 我随手写了 **4/6/9/10/14px 七种圆角**,容器侧棘轮 65 > 58 | 全换成形制集 `{999px, 8px, 50%, 0}` / 小程序 `{999rpx, 12rpx, 50%}`。**二形法不是审美偏好,是「一眼认得出是同一个系统」** |
| 6 | **规矩丙 ⑥b**(金底无字棘轮) | `.sdoc-opt.on .sdoc-k` —— 选类型时那个**金色选中小圆点**,金底、无字 ⇒ 17 > 16 | 🔴 **这是一次「抬」**。它是**合同图 v2 照搬**的(图上就是 `.opt.on .k{background:var(--brandd)}`),换颜色就与图不符,躲不掉。走裁#89 的「说明」支:棘轮 16→17,**在判据源码里具名写清是哪一条**,并列进「要你裁的」第一条 |
| 7 | **④h 临时目录前缀**(`test-credential-scan`) | `test-backup-wal` 里三个 `mkdtemp` 前缀不合规 —— 🔴 **其中两个(`wal-proof-` / `ll-impsnap-`)早就在**,只是那文件原先没有 `spawn`,④h 的过滤条件够不着它;**我为 ⑥ 加了 `spawnSync`,把存量一起晒了出来** | 三个一起改名成 `ll-ci-data.*`。**这不是误报要豁免,是扫描面变宽后看见了存量** —— 判据一个字没动 |
| 8 | **J-60 正门律**(`test-fixture-front-door` ①) | 我那条红线**反向守的探针字符串**里写着完整的 `db.prepare('INSERT INTO users…')`,被数成「夹具直连库贴」第 24 处,把只许降的欠账棘轮顶破 | **判据自己的探针不是被测对象**(J-61②)。照仓里现成写法把探针拆开 join。现 **23 = 基线** |

🔴 **两次「晒出存量」值得单说**:第 7 条不是我写坏了什么,是我让一把刀**看得更远了**。
按 L2 同类扫尽,这种时候的正确动作是**把存量一起改掉**,不是给自己开一条豁免 —— 已照做。

## 11. 第三波:两条,收口

| # | 谁抓的 | 抓到什么 | 怎么改的 |
|---|---|---|---|
| 9 | **空徽章律**(`test-empty-pill` ①) | 小程序那个状态徽章 `{{item.statusText}}` **绑单个表达式又没有 `wx:if`** —— 值为空会留一个空的描边框,看起来像「有个状态但没显示出来」 | 加 `wx:if="{{item.statusText}}"`(比登记白名单干净:空了就整个不出现) |
| 10 | **原生对话框零残留**(`test-native-dialog` ①) | 我写了 `window.prompt` 问作废原因 —— 而全仓 44 个 `apps/web/*.js` 的原生对话框**早就从 34 清到 0**,白名单也是 0 | 改走唯一出口 `window.UIDialog.text`。🔴 **原生弹窗在真机/内嵌浏览器里可能根本不弹,而 fail 是静默的**(弹层实点律) |

---

## 🔴 这一批的一句总结

**十个缺陷,十个都是判据抓的,没有一个是我自己看出来的。**
其中两个(临时目录前缀、二形法)是**我让刀看得更远之后,晒出来的存量**;
一个(金色选中点)是**合同图要求的,躲不掉,只能抬棘轮并具名报你**。
我在浏览器里**看过**那个金色按钮 —— 浅色档下白字金底,**看上去完全正确**,
而 `test-color-usage` 才知道深色档下它会塌。**眼睛看对的,判据未必同意;该信判据。**

---

# 丙 · 三行口径

```
本机全量:129/129 套件 · 4,224 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:未推(本批没推 main;纯等下一次代码提交一起走,生产部署纪律①)
GitHub CI 未跑:—
```

**断言增量对账(判据五「计数即证」):** 合计 **4,146 → 4,218(+72)**,套件 128 → **129**。
逐项:`test-signed-docs` 新立 **66** · `test-backup-wal` **18 → 24(+6)**。66 + 6 = 72 ✅ **数字对得上**。

**棘轮三项(回归自己量的):**
```
local-server.mjs   17,539 ≤ 17,613   (上一提交 17,613 · 历史最低 17,613)
admin.js            7,893 ≤  7,928   (上一提交  7,928 · 历史最低  7,928)
小程序 wxss 写死色   2,350 ≤  2,350   (基线,净 0)
```
🔴 **棘轮归因:真修好了 —— 抽取**(COS 那一族 84 行搬出 + `loadCustomerNotes` 42 行搬出;不是判据放松,也不是扫描面缩了)。

# 丁 · 要你裁的

| # | 事 | 挡不挡 |
|---|---|---|
| 1 | 🔴 **`pricelist` 还是 `consent`** —— 图说「价格表确认书」,令说「护理同意书」;我按图走了 | 不挡,但落库了,**越早裁越省事** |
| 2 | `cosDeleteObject` 全仓零调用方,搬过来照样导出 —— 删还是留 | 不挡 |
| 3 | 卷上 **35 份日备全是旧代码 `cp` 出来的**;第一份 VACUUM 日备要等 09-21 04:55 UTC | 不挡,下批核 |
| 4 | 屏1 那一块**没有整块像素图**(浏览器面板滚动截图错位),只有 DOM 逐字 | 不挡 |
| 5 | D205 / J-102 全量(12 支种子)仍排第二段 | 不挡 |

# 戊 · 自评 L1–L5

- **L1 末端验证** ✅ 做了(浏览器 DOM 逐字 + 两张弹层像素图),**但屏1 整块像素图没拿到,已挂 ⬜**。
- **L2 同类扫尽** ✅ 做了三轮:`copyFileSync` 调用点归零 + 两层白名单清零 + 两笔残留;`window.owner` 全文件扫;`codeOnly()` 统一三处尺子。
- **L3 无图不结案** ✅ 合同图 v2 取到了(在我自己的 Artifact 列表里,更新于 09-20),逐屏提了清单才开工。
- **L4 反例数据** ✅ 造景含:空态顾客 / 有 2 份的顾客 / 已作废的一份 / 跨店 B 店 / 服务过与没服务过的两个员工。
- **L5 重复稳定** ⬜ **没做满 5 次进出**。判据里的接口路径各跑过多次,但**界面的「连续 5 次进出页面」没做** —— 浏览器面板滚动不稳,做不成干净的 5 次。**如实挂着,不写成通过。**
