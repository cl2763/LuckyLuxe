# 不稳定计数 · `test-perf-viz`

> 裁(店主 05y §三①):**不许「重跑绿」当结案。**
> 崩溃现场(错误 + 夹具输入 + 时刻)落在这里;**同一套件第二次崩就升级为红并停线定位**。

| # | 时刻 | 现场 | 当时做了什么 | 处置 |
|---|---|---|---|---|
| 1 | 2026-09-09 夜(05x 全量第 5 跑) | `✗ Cannot read properties of undefined (reading '0')`,打印在 `ok 1 - 两家临时店建好` 之后、`ok 2` 之前 | 全量回归(MP_AUTOMATOR 已给);同一批前后各跑一次都绿 | 夹具改成「取不到分类就抛一句带现场的错」(`93e5f74`),**没有定位到根因** |

**下一次(第 2 次)再崩就停线**:按裁定,那时不许再靠重跑洗白 ——
要么当场定位,要么把这一条升级成 D 号缺陷登记。

**怀疑方向**(留给下一次照着查,不是结论):
`ensureCategory()` 先 POST 建分类、失败再 GET 回读。若 POST 返回 409 而 GET 那一刻
分类还没写进去(建店时平台会自动落三大类,两者可能撞车),`list` 就是空的 → `list[0]` 是 undefined。
现在那一支会打印「建=<状态码> <响应前 120 字> · 回读=<状态码> 共 N 条」,下次崩能直接读出是哪一种。
