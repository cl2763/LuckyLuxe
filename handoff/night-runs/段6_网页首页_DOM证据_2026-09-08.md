# 段 6 · 网页后台首页 —— DOM 现测证据(图 v3.1 §三 / §六 / §七)

> 2026-09-08 · 沙箱 4310 `/admin` · 真浏览器现测(不是我摆出来的样子)
> 判据按**节点在不在**验,不锚文案(图 §七 抬头)。

## 正常态(老板身份 · 今日维度)

```
态                 ["ready"]
维度按钮           今日(选中) / 本周 / 本月 / 本年
全屏按钮 disabled  true            ← 本批只占位(全屏态归段 11)
六格 key 与顺序    revenue · cash · cardUse · newCard · visits · bookings   ← §七③ 恒六项、顺序固定
大数字             CAD $0                                                   ← 币种随店,不是写死的
delta 六个都是     "none"          ← §七④ prev=0 → deltaPct=null,节点存在、无 ∞/NaN
卡耗次数节点       "0 次"          ← 钱与次两个数(次不折钱)
此刻四格           doing=0 waiting=0 done=0 total=0
下一位             「后面没有待到店的了」
空态真话           「今天还没有开单 · 暂无往日数据」
待办真话           「今天没有要处理的」   ← §七⑦ 全 0 时真话块存在、为 0 的项无节点
```

## 取数失败态(把 request 换成必然抛错的,现场造)

```
态             ["failed"]
真话           「业绩数据暂时取不到」
有旧数节点     false     ← §六:失败**不显示旧数**
整块出现 0     false     ← §六:失败**不显示 0**
有重试钮       true
```

## 休息日态(让 /now 回 closed)

```
态         ["ready", "closed"]     ← 大屏照显(图 §六原文),今日预约块换成休息
真话       「今日休息」
四格节点   false                   ← 今日预约块隐藏
```

## 还原

```
态  ["ready"]   ← 造完病还原,回正常态
```

## 顺手修的一处判据卫生

区块与格子原来共用 `data-dh-now`,判据的选择器把区块也数了进去(现测 DOM 里多出一个空格子)。
区块改成 `data-dh-now-block` —— **同名属性会让判据数错**,这类脏在写判据时看不出来,只有现测才现形。

## 未做(如实)

- 老板 × 四维度 / 全屏 / 员工打卡门的逐张截图:全屏归段 11、打卡门归段 10,本段只交网页老板首页。
- 「本月收入(账本)」那一格已从旧汇总行**退役**(营收只留大屏一处出口)。
