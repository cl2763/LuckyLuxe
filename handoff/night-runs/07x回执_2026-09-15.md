# 07x 回执 · 2026-09-15(§二 红点名 / §八 红 8 去向 / §七 两问 先交)

**本令 md5 两处一致**:`4d58da1eac49d0bfd19d3be695394aa7`(桌面夹 + 仓里 `handoff/`)
**读数时刻**:2026-09-15 20:08:53 CST · **读的是哪个库(J-64)**:造病跑的是造病台自己的临时库(端口 4191);回归跑的是回归临时库 `/tmp/ll-ci-data.*`
**生产库未动 · 本机库未动**(本批一次都没连生产)
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## §二 · 🔴 红那一格点名 —— **㋚6 和 ㋚7 都红了**

先把你点名要的那一句答了:

> **把签字落库整条注掉:`㋚6`(签完另一入口变已签只读)红 ✅ · `㋚7`(已签的单不再出新码)红 ✅ —— 两条都红,而且因果链都是通的。**

所以「签署口现在有判据守着了」这句话,**到这一刻才算立得住**。

### 红 4 逐条点名 + 因果链(刀的写集 = `{settlements}`)

| # | 红的那条 | 因果链 | 判 |
|---|---|---|---|
| 1 | `㋚5a 造景:开单 + 顾客自己签字` | 读集 ∩ 写集 = `{settlements}` | ✅ 链通 |
| 2 | `㋚5 积分真的跟过去了` | 读集 ∩ 写集 = `{settlements}` | ✅ 链通 |
| 3 | 🔴 **`㋚6 签完另一入口变已签只读`** | 读集 ∩ 写集 = `{settlements}` | ✅ 链通 |
| 4 | 🔴 **`㋚7 已签的单不再出新码`** | 读集 ∩ 写集 = `{settlements}` | ✅ 链通 |

**没有一条是「撞上的」** —— 这正是夜12 那次「红 1 却是隔壁幂等撞的」要防的情形。

### 五格闭合(现测,台子自己加的)

```
[刀账·五个数] 红 4 · 仍绿-无关 7 · 🔴 仍绿-该咬没咬 2 · 说不清 1 · 没跑到 0
              —— 合计 14 ≡ 底数 14 ✅ 闭合(J-66)
```

---

## §二附 · 🔴 三处 J-55 更正:**我 07w §一 交的那份四格表,错了两处**

新尺子(按读写集,见 §三)一上,07w 那份表**当场被推翻**。按 J-55 回来改:

### 更正一:`auditDepositConservation()` **读 `settlements`**

07w §一 我写的原话是「**一个字都不碰 `settlements`**」。
```
apps/api/local-server.mjs:7567
  const stl = db.prepare('SELECT code FROM settlements WHERE id = ?').get(r.settled_settlement_id)
```
**这句话是错的。** 而且是**我当时自己写的可反驳条件**(「指出它其实读了 settlements,这条判定立刻推翻」)把它推翻的 ——
推翻它的不是人,是 **`readset --probe` 第一次跑就报「含 settlements=true(该 false)」**。

### 更正二:四格数字变了

| | 07w 报的 | 07x 现测 | 差在哪 |
|---|---|---|---|
| 无关 | 10 | **7** | ㋚4 / ㋚8 从「无关」挪到「该咬没咬」;「前置」挪到「说不清」 |
| 该咬没咬 | **0** | **2** | 🔴 那个 0 **又一次是假的** —— 上一批是「没找过」,这一批是「按名字找的」 |
| 说不清 | (无此格) | 1 | 新开的一格 |

### 更正三:**结论对,理由错**

07w 我说「签署口是 ㋚6/㋚7 **两条**在守,不是三条」——
**「两条」这个数是对的**(现测两条都红、第三条 ㋚8 仍绿),
**但我给的理由(㋚8 不读 settlements)是错的**。真理由是:
㋚8 读的是 `settlements.code`,而刀写的是 `settlements.status` —— **表相交,列不相交**。

---

## §三 · 立 J-61 第三款 —— 台子已改成按读写集分类

按你的款文,台子不再用名字正则 `claimPat` 分类,换成:

- **刀的写集** = 被砍那条语句写了哪几张表
- **判据的读集** = 那一段源码碰到的表(suite 直接 SQL + HTTP 路径 → handler 体 → 跟进一层 + 转发壳)
- **该咬 ⇔ 读集 ∩ 写集 ≠ ∅**;取不到读集 → **说不清**,单列,不许扫进无关

新模块 `tools/readset.mjs`,带 `--probe`(**两面靶子,J-58⑥**):**该中 4 个 · 不该中 2 个 · 判错 0 个**。

### 🔴 这把新尺子**自己坏了五次**,每次都是反面靶子咬出来的

| # | 毛病 | 现象 | 治法 |
|---|---|---|---|
| 1 | 跟进函数用**定长 4,000 字符窗口** | 越过函数结尾漏进隔壁,deposit-conservation 读集混进 3 张不相干的表 | 改真花括号配对 |
| 2 | 路径正则**遇到 `${\}` 就断** | ㋚7 的 `/admin/settlements/${id}/sign-token` 解析不到 handler | 认模板,插值换通配 |
| 3 | 无脑跟两层/三层 | `/my/points-history` 读集**涨到 36 张**(几乎全库 schema) | 收成「一层 + 转发壳例外」 |
| 4 | 把 `.map(` `.all(` 当本地函数 | 拿去全仓找同名 `const`,找到别人家的 `db.prepare` | 排除点号前缀 |
| 5 | 同名定义**只取第一处** | 取到转发壳,真身(`points-ledger.mjs`)穿不过去 | 全取,拼起来 |
| 6 | **注释里的表名算进读集** | 「前置:实例起得来」因文件头注释被判成「该咬」 | 抽读集前先剥注释 |

🔴 **其中 #3 特别值得记**:深度一开,**四条正面靶子全过**,只有那条反面靶子红。
**一把「什么都咬」的尺子,只种正面靶子是永远看不出毛病的** —— 这就是 J-58⑥ 立出来要防的事,**当天就兑现了。**

### 仍绿-该咬没咬 2 条:**我不把它们改记成「无关」**

| 条 | 机械判据 | 我的复核(可反驳) |
|---|---|---|
| `㋚4 /my/points-history 空态` | 读集 ∩ 写集 = `{settlements}` | 该处顾客**一张单都没有**,签不签字都是 0 条 —— 这一刀改不动它的结果。**可反驳**:举出一个让它变值的数据 |
| `㋚8 定金守恒` | 读集 ∩ 写集 = `{settlements}` | 它读 `settlements.code`,刀写 `settlements.status` —— **表相交,列不相交**。**可反驳**:指出守恒审计读了 `status` |

**按 J-66③(未归类余数按最坏那格计),这两条我留在「该咬没咬」这一格,不动。**
要不要把尺子从**表级**细到**列级**,归你裁 —— 细到列会更准,但会多出一类新的说不清。

### 说不清 1 条

`前置:实例起得来` —— 它打的是 `/health`,读集取不到。**单列,不扫进无关。**

---

## §四 · 🔴 红那一格点名,也治好了台子自己的一个假 0

做 §八 的突变自检时撞出来的:

> **fail-fast 套件红起来不打 `not ok`** —— 它直接 `throw`,名字只出现在 stderr 的 `Error: <断言名>` 那一行。
> 台子原来只认 `not ok`,于是 `card-refund` 被刀咬红时,台面上报的是 **「红 0 · 没跑到 4」**。
> **「红 0 + 没跑到」和「它守住了」在数字上分不出来** —— 又一个看起来很干净的 0。

已治:认 `Error:` 行为红。治完同一把刀再跑:**红 1,点名 `生产闸-3`** ✅

---

## §八 · 门关档红 8 逐条去向 —— **本批 8 → 7**

先说机制,8 条不是八件事,是**两族**:

| 族 | 是什么 | 几套 |
|---|---|---|
| **A · 夹具走演示邮箱登录** | 这一档恰恰是把演示登录关掉的那一档 → 夹具造不出景(拿不到 token / 账号不存在 / 401) | 7 |
| **B · 判据与本档前提冲突** | 断言写死「本机邮箱登录必须 200」,而这一档本来就该 403 | 1 |

### 逐条去向

| 套件 | 族 | 红在哪一句(原文) | 去向 |
|---|---|---|---|
| `card-refund` | B | `生产闸-3 反例:沙箱/本地照旧可用: 403` | 🟢 **(a) 本批修,已绿** |
| `auth-surface` | A | `㊙⑩ 合法签发的令牌在同一条口上不是 401: 401`(夹具那个顾客是演示登录建的,门关档里不存在) | **(c) 排期 07y** |
| `booking-intake` | A | `⑤0 造景:并发店…造不出来按红 :: token=空` | **(c) 排期 07y** |
| `deposit-config` | A | `演示顾客登录可用: DEMO_LOGIN_DISABLED` | **(c) 排期 07y** |
| `identity-links` | A | `email register returns user id: DEMO_LOGIN_DISABLED` | **(c) 排期 07y** |
| `noshow-aftersales` | A | `Cannot read properties of undefined (reading 'accessToken')` | **(c) 排期 07z** |
| `schedule-v2` | A | `③ 下午班时段外的预约被拒: 401 预约或支付前请先登录` | **(c) 排期 07z** |
| `staff-portal` | A | `staff login works: 账号不存在` | **(c) 排期 07z** |

**(a) 怎么修的(不是放松)**:让它**两档各守各的那一句** ——
门开着(`/health` 现读 `guestIdUnsigned === true`)→ 必须 **200**;门关着 → 必须 **403 DEMO_LOGIN_DISABLED**。
**两支都断言,没有「跳过」那一支**(判据五:断言不许被条件块静默跳过);档次**从 `/health` 现读**,不看环境变量。

**(c) 的修法(A 族统一一条)**:把夹具从演示邮箱登录换成**正门**——
复用 `test-customer-paths` 那套「建档 + 贴 openid + 服务端签发 mini token」的写法(J-60①:造状态要走产生它的那条路)。
**为什么不是现在**:7 套夹具各自不同,一次全改会把本批的红点名与新尺子搅在一起,出了问题分不清是谁的。
**排期承诺:07y 修 4 套(auth-surface / booking-intake / deposit-config / identity-links),07z 修 3 套。**

### 🔴 顺手咬出一条:**mp 档的「自愈」从装上那天起一次都没跑过**

```
run-all-tests.sh:471   bash tools/mp-automator-up.sh || echo "⚠️ 自愈没成 …"
```
脚本开头已经 `cd "$(dirname "$0")"`(= `apps/api`)—— 所以它找的是 `apps/api/tools/mp-automator-up.sh`,
**那个文件不存在**;`bash` 报 `No such file or directory`,再被 `||` **吞成一句「自愈没成」**。
**静默失败器族又一案**:`||` 把「文件找不到」和「自愈失败」说成了同一句话。
已治:走绝对路径 + **两件事分开报**。治完真因露出来了 ——
**`MP_AUTOMATOR` 没设(`miniprogram-automator` 这个仓外模块没装)**,不是「会话不在」。

---

## §七 · 两问

**1. 门关档那 `07v +2` 是哪两套?与主档 +2 同物吗?**

**`web-not-signup` 与 `import-phone-guard`,和主档 +2 是同名同物** ——
机械证据:`git log -S"web-not-signup import-phone-guard" -- apps/api/run-all-tests.sh` 只出一笔 `6abc200`(07v),
**同一笔提交同时把这两套加进两个档的名单**,不是另外两套。

**2. 断言 4,040 是现测还是抄的?**

**现测。** 而且本批**涨了 1 条**:`[断言基线] 合计 4040 → 4041(+1)`。
你问的「三件都不增断言」——**两件不增,一件增 1**:
- 台子分类算法改写 → **不增**(那是 `tools/` 的工具,不在 122 套里)
- SIGNUP probe 入口 → **不增**(`--probe` 是单独入口,不打 `^ok` 行)
- `customer.js` 摘 3 行 → **不增**
- 🔴 **本批新加的 `card-refund 生产闸-3a` → +1**(判据五:增量对上了,预期 +1、现测 +1)

---

## §五 · 本批新判据自己先挨了一刀(突变自检条,四步齐)

被考的是本批新写的 `生产闸-3` 门关那一支。

| 步 | 做了什么 | 证据 |
|---|---|---|
| ① 造 | **走造病台**(裁 #101),把 `throw apiError(403, 'DEMO_LOGIN_DISABLED', …)` 整条注掉 | `[刀] 已注掉…(整条,语法已过)` |
| ② 重启 | 台子每轮**新起一个隔离实例**(端口 4191 + 自己的临时库) | `[裁#98 自证] 4128=200 · 4310=200`(全程没碰) |
| ③ 红且点名 | 🔴 **红 1,点名就是它** | `Error: 生产闸-3 … 必须 403 …: 200 {"user":{…}}` —— 门关档下邮箱登录**真的把人建出来了** |
| ④ 还原回绿 | 还原 + 全量回归 | `grep -c "throw apiError(403, 'DEMO_LOGIN_DISABLED'" = 1` · 门关档 `card-refund` 已不在红名单 |

**为此给造病台加了一样能力:按档造病**(`lane: { gateOff: true }`)——
不加的话,门关那一支**只在那一档执行**,刀根本落不到它头上。

**过程里还撞出台子两个毛病,一并治了**:
1. 「自带服务」判据只看有没有 `spawn` → `card-refund`(两样都干)被误判 → 回落 4128 → **被测试护栏当场拦下**(护栏做对了,是判据错了)。改判据:**起了服务 且 不读外部 BASE_URL**。
2. 新判据排在 `生产闸-1` 后面 → 刀一落,`生产闸-1` 先 fail-fast 抛掉,**新判据根本没跑到**。已把本档两条提到最前。

---

## 数(本节只报一次,全部现测)

| | 数 | 来源 |
|---|---|---|
| 主档 | **122 / 122 全过** | `run-all-tests.sh` 本批第 4 次跑 |
| 门关档 | **24 套 · 红 7**(上批 8,**降 1**) | 同上 |
| 小程序档 | 3 套 · 红 3 · 空转 0 | 同上 —— 真因已点名:`MP_AUTOMATOR` 没设 |
| 断言(在册) | **4,041**(上批 4,040,**+1**) | `[断言基线]` 行 |

🔴 **更正一条上批的数**:07w 我报「小程序档 3 套 · 红 0 · 空转 0」——
**今天连跑四次都是红 3**,而红的原因(仓外模块没装)不是今天才有的。
按 J-63 第①问,那个「红 0」**我拿不出现测依据,应按错报处理**。

**棘轮**(`wc -l` 现测,带基准):
`local-server.mjs` **17,631(±0)** · `admin.js` **7,928(±0)** · `customer.js` **2,755(±0,仍压线)**

**跨批账本**:`8cf9acc` 领先 326 + 本批 20 = **346** ✅

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §九 ④⑥⑦⑧⑨⑩ | **未做** —— 按你 §八末「本批先交 §二 和 §八」,先交这四节 |
| 门关档 A 族 7 套 | **(c) 已排期**:07y 四套 · 07z 三套,修法见 §八 |
| 9 条冻结理由 | **未交** —— 与 §九⑩ 同批,下一批一起给两栏(理由 + 解冻条件) |
| 尺子表级 vs 列级 | **请裁**:细到列更准,但会多出一类新的说不清(§三末) |
