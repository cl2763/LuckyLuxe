# 07l 未动对照表 · 2026-09-14

> 逐表对照「开批快照」与「交齐时现测」。**开批快照件**:`handoff/night-runs/开批快照/{本机库,沙箱库}_开批.json`
> 生成:`node tools/db-snapshot.mjs <库绝对路径> --diff <快照>`(**只读**)

## 本机库(`apps/api/local-data/lucky-luxe.sqlite`,端口 4128)

```

════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/本机库_开批.json
  🔴 1 张表有差异,**不许写「未动」**:
     notify_scan_marks  行 66→88(+22) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**读法**:业务表**逐表零差异**。唯一有差的 `notify_scan_marks` 是**通知扫描的心跳表** ——
4128 那台服务一直在跑,每轮扫描落一行,与本批改动无关(本批一行业务数据都没写过本机库)。
按 J-49「认标记不认措辞」:这里**如实报有差**,不把它说成「零差异」。

## 沙箱库(`apps/api/sandbox-data/lucky-luxe.sqlite`,端口 4310)

```

════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/沙箱库_开批.json
  🔴 3 张表有差异,**不许写「未动」**:
     notification_logs  行 313→363(+50) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     notify_scan_marks  行 552→725(+173) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     reminder_tasks  行 912→962(+50) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**读法**:三张都是**通知调度器自己产生的**(通知日志 / 扫描心跳 / 提醒任务),
沙箱那台 4310 一直在跑。业务表(顾客/预约/结算/账本/储值)**逐表零差异**。

## 生产库

**本批零接触** —— 没有跑过任何 `railway` 命令,没有任何出站写请求。
本批唯一涉及生产的改动是 `裁 #90`(演示门闸改「或」),它**只会让生产更严**,且未部署。
