# 夜班令 9 回执 —— 那条上线硬门槛查清了(**是 B,不是 A**)· 判据自己被咬出五个洞

> 本批提交(9 个,由 `git log --oneline be62699..HEAD` 现出,不手写):
>
> dabda3f 夜9-七a 棘轮收紧:customer.js 2811 → 2799(J-43 重量 + J-50 归因)
> ab2aa66 夜9-五六 段5 回落清单(只查不改)+ 段6 配色批备料(只出图和数)
> 81cac2c 夜9-四 party.js 贴身棘轮 + 「读写同源」判据(回读的租户必须是写进去时那一个)
> 1fa5bd6 夜9-三b 造病验红:四刀全咬 + 顺手咬出这把刀自己「红字照印退出码照绿」
> 8759589 夜9-三 J-50:棘轮对账加「本次变化 + 归因」列 —— 降了也要举证
> eea1872 夜9-段2 裁#50/J-51:PROOF_LEGACY 从**内容模式**改成**具名清单**冻住(38 篇)
> 375c8e4 夜9-段1b 三条造病各红一次(安全件):TT 写回常量→㊙⑥红 · UU 拆掉不签名令牌的闸→㊙④红 · VV 拿掉验签→㊙①红;各自还原回绿
> 3b2962e 夜9-段1(走 B)· 那一格改成现测 + 补「这一路确实是服务端签发」的判据 + 清单自己翻面
> 778f806 夜9-段0 访客身份串现查结论:**(B)** —— 三条路都已经是服务端签发,那条硬门槛是假红

> 2026-09-12 · 基线 `be62699` · **未推 main**(领先 **274**)· **生产库未动 · 本机库未动** ·
> 密码 / 密钥一个字没进本文件

---

## 〇、先答那一句:**上线批第 4 行那条硬门槛,今晚是 (B)**

**(B) 三条身份路都已经是服务端签发/校验;那条红是假红**,它的事实来源是
`apps/api/health-report.mjs:35` 一个**写死的 `true`** —— 从来没有量过任何东西。
逐条证据在 `handoff/night-runs/访客身份串_现查结论_2026-09-12.md`。

段 0 那句「不许为了对上『选 A』去把结论写成 A」我照做了:**结论就是 B,我没有去写实现**,
改的是**让那一格自己报真话**(J-52),然后清单自己翻面。

---

## 一、段 0(30 分钟盒)· 只出结论不出代码 ✅

三条路逐条现查,每条写清「谁签的、谁验的、会不会过期」:

| 路 | 结论 |
|---|---|
| 小程序/微信 | `signMiniPayload`(HMAC-SHA256)签,`customerFromMiniToken` 验签 + 验过期 + 对 openid,7 天 |
| 网页顾客端 | 只发 `Authorization: Bearer`,**没有任何客户端自造的串**;`requireCustomer` 只认两条 —— 签名那条,和演示那条(**被 `DEMO_LOGIN_ALLOWED` 结构性挡在生产外**) |
| 企微 | 那是**出站**链路;入站那条挂在 `/admin/*` 闸门之后,`externalUserId` 是会话标识不是账号身份 |

**顺手查到一件更该记的(只报不改)**:`local-server.mjs:163`
`IS_PRODUCTION = process.env.NODE_ENV === 'production' || Boolean(process.env.RAILWAY_ENVIRONMENT)`
是**纯环境变量判定**,与 06h 裁 #37 治过的 `scopeOf` 同族(那次结论是「认库不认环境变量」)。
**今天不是洞**(要开那条路得同时满足两件),但**门闩的材质跟我们刚换掉的那一种一样**。等你裁。

## 二、段 1 · 按 (B) 落地 ✅

1. `guestIdUnsigned` 从写死的 `true` 改成**现测**:它等于「这个进程还接不接受非服务端签发的顾客身份」,
   也就是 `DEMO_LOGIN_ALLOWED` 本身 —— **同一处真相,不在 health 里另抄一份**。
   **取不到报 `null`,不兜 true/false**(兜一个默认值等于又变回常量,静默失败器族)。
   现测:沙箱进程 → `true`;`NODE_ENV=production` 的进程 → `false`。
2. `test-auth-surface` **34 → 44 条**(+10):验签 / 验过期 / 对 openid / 不签名那条只在
   `DEMO_LOGIN_ALLOWED` 下可达 / `requireCustomer` 恰好两条路 / `/health` 那格不是常量 / 反向守 /
   行为层三条(伪造令牌 401、裸令牌 401、正经签名的**不**401)。
3. `test-merge-window:139` 那条 `guestIdUnsigned === true` **锚的是常量不是事实**,拆成
   ④b(是 boolean,不是 null)+ ④b2(跟这个进程的实际状态对得上)。
4. **造病三刀各红一次再回绿**:TT 把那格写回常量 → ㊙⑥红;UU 拆掉不签名令牌的闸 → ㊙④红;
   VV 拿掉验签 → ㊙①红。
5. 跑 `release-checklist.mjs`,第 4 行自己翻面。

## 三、段 2 · 07b §一 J-51「豁免也要具名」✅

`test-untouched-proof` 的 `PROOF_LEGACY` 从**内容模式**(「长得像老文档就放过」)
改成 **`PROOF_FROZEN` 具名清单 38 篇 + 条数上限**,清单只许变短。

**先证明洞在**(这一步是 07b 点名要的):造一篇**新**文档,写「生产库未动 · 本机库未动」+ 逐表零差异、
**不带标记** —— 旧判据 **绿**(它按内容模式把这篇也当老文档放了),新判据 **红**。洞是真的,补是真补。

## 四、段 3 · J-50「棘轮下降要举证」✅

`tools/ratchet-audit.mjs` 加「本次变化 + 归因」列。上一版的值取**写下这个数那次提交的父提交**,
差额当场算。归因**靠标记不靠措辞**:`棘轮归因:真修好了 / 扫描面缩了 / 判据放松了 …`。

**三种待遇不一样**:只有①算过,**且必须点名**(标记后面少于 8 个实字 = 没点名);
②③**照样红** —— 认罪不是免罪,数字变好看了而被守的那件事一点没少,写归因的好处是
**红的时候报得出是哪一种**。

**造病四刀 + 一次归因对照,全按预期**(在一次性 `git clone` 里做 —— 被测的刀读 git 历史,
造它的病必须真提交,而真仓留一次「故意放松」的提交再擦掉正是 J-34 不许的那类还原。
真仓一个字没动,刀末尾现打 `git status` 自证):

| 刀 | 做了什么 | 结果 |
|---|---|---|
| ① | 放松写死色正则 {3,8}→{6,8}(同一份文件数出来 31→29),棘轮调到 29,**不写归因** | 🔴「降了却没写归因」 |
| ② | 同一处归因写「判据放松了」 | 🔴 **照样红**,报「这是「判据放松了」导致的下降」 |
| ③ | wxss 的 find 加 `! -path "*/pages/*"`(2356→69),归因「扫描面缩了」 | 🔴 照样红,报得出是这一种 |
| ④a | 归因写「真修好了」但不点名 | 🔴「没点名」 |
| ④b | 补上点名 | ✅ **那一行翻绿** —— 证明它不是「见降就红」 |
| 对照 | 同一份放松后的代码喂 `eea1872` 那版旧刀 | ✅ **绿** —— 新列不是靠「数量凑巧对不上」误报 |

## 五、段 4 · party.js 贴身棘轮 + 「读写同源」判据 ✅

**① party.js 31/31**(新类语义「新件薄壳」)。为什么要贴身:它离公约上限 1500 差得远,
**公约那一档根本管不到它**,能一路长到 1499 都不红,而它存在的理由恰恰是「薄」。
造病:末尾 +2 行 → ② 红「33 > 棘轮 31(+2)」退出码 1;删回 → 0。**初值 31 是这一版判据量的**(J-43)。

**② 新判据「读写同源」**(`test-tenant-ownership` 5 → 8 条):同一路由段 · 同一张表 · 14 行内 ·
一写一读而**租户来源不同** ⇒ 红。案由是我自己这几批栽的:写用局部 `tenantId`、回读写 `currentTenantId()`。

**这把刀自己被咬出四个洞,四个都记在码上**(每一个都是「绿得毫无道理」那一类):

| # | 洞 | 现象 |
|---|---|---|
| 1 | 租户实参在 4 行窗口里找 | INSERT 那条把**下一行 SELECT 的** `currentTenantId()` 认成自己的,两条看起来同源 |
| 2 | `.replace(/\)$/,'')` 削掉每个函数调用的右括号 | `currentTenantId()` 变成 `currentTenantId(`,**同源也报不同源**(报文里那半个括号就是现场痕迹) |
| 3 | 只认一张固定名单 | 头一刀局部名叫 `knifeTenantId`,不在名单上 ⇒ 那两条语句**整条被跳过**,扫描面 436→436 纹丝不动,判据当然绿。**刀落了(4 行凭据在案)红没出来 = 扫描瞎了,不是代码干净** |
| 4 | 别名表按全文件建,后声明覆盖先声明 | **用真实拼法 `tenantId` 反而绿、用生僻名反而红** —— 同源不同源全看谁在文件里排后面。改成按路由段建表 |

第 4 条最值:**只跑一个变量名就会得出相反结论**。另外首版按函数体开窗把
`/admin/salary/unlock` 的写和 `/admin/salary/payout` 的读配成一对,报了 2 处假红 —— 改成按路由分段。
造病两刀(`tenantId` / `knifeTenantId`)**都红**,逐处报出
`表 salary_payrolls:L15035(写 resolveTenant()) ↔ L15036(读 currentTenantId())`;
还原(J-34 走 `knife-backup.sh`)后 8/8 绿、无 `.pre-k` 残留、`git status` 对该文件为空。

## 六、段 5 · `currentTenantId()` 回落清单 ✅ **一行没改**

全文 `handoff/night-runs/currentTenantId回落清单_2026-09-12.md`。三栏逐处 `file:line`。

| 类 | 处数 | 今天有没有真在回落 | 建议 |
|---|---|---|---|
| **A** 闸门前 23 条路由 | 23 | **没有**(直接调用 0 处;6 次间接调用**全部显式传了租户**) | 🔴 最该改:闸门前调它就抛错,风险面为 0、护住未来 |
| **B** 闸门自带 `\|\| DEFAULT_TENANT_ID` | **5**(一读三写 + AI 段) | 只在 `admin.tenantId` 为空时,那本身是数据异常 | 🔴 空就 403/500,不许当旗舰店 |
| **C** 默认参族 53 个函数 | 10 处零参调用 | 没有(全在上下文内) | 不整族改;加「闸门前不许用」的判据 |
| **D** 定时器 | 2 | 没有(调度器自己逐店循环) | 低 |

**没有一处今天正在把别人的数据回落成旗舰店。** 但 A 与 B 是**靠自觉撑着**的两处。

## 七、段 6 · 配色批备料 ✅ **合同图一个像素没动**

`handoff/night-runs/配色批备料/`:对照页 + **浅/深两张图**(拍摄时刻 `20260912-134906`)+ 备料文。

**结论:这一批可能根本不需要「配色」,只需要「接令牌」** —— 五项全能用仓里已有的成对令牌解决,
按段 6「能用现有令牌解决的就不该进配色批」,**五项都不该进**。

| 项 | 现值(处数) | 现比值 | 建议接 | 浅 | 深 |
|---|---|---|---|---|---|
| 回头客 B | `#3f6b52`(5) | 5.34 | `--good/--goodbg` | 4.68 | 7.79 |
| 新客 N | `#3b6ea5`(4) | 4.63 | `--tagw/--tagwbg` | 7.20 | 7.05 |
| 沉睡 S | `#8a5a52`(3) | 5.00 | `--bad/--badbg` | 4.69 | 5.84 |
| 待裁 #35 | `#e7c9a4`(5) | 7.13/9.21/9.21/6.45 · `.pfill` 4.52 但**没有字** | `--herogold` | 8.08 | 11.06 |

三个品类色**比值本身都够 AA** —— 问题不是「看不见」,是**写死 hex ⇒ 两档同值 ⇒ 深色档不存在**(J-42)。
**待裁 #35 建议撤出配色批**:它不是对比度问题,而且那四页**深色档块数现测 0**,是整页还没做深色档。

要你拍的只剩两件:①四档长相统一成「淡底深字」准不准 ②沉睡 S 借 `--bad` **语义**认不认。

---

## 八、六项结构

**① 改了什么** —— 见上七段;代码改动集中在四支判据 + 一个 health 字段 + 一个棘轮值。
**② 在哪一层验的** —— 判据层(44+18+8 条常驻)+ **造病层**(本夜共 **11 刀**:TT/UU/VV、WW、
J-50 四刀 + 归因对照、party.js 一刀、读写同源两刀),每刀有落刀凭据与还原自证。
**③ 同类扫尽** —— 读写同源是**白名单式全仓扫**(437 条带租户条件语句,豁免 0 且带上限);
`PROOF_FROZEN` 38 篇具名冻住;棘轮注册表加条数下限 `REGISTRY_MIN=11`,删一条即红。
**④ 用什么数据验的** —— 都是**真文件、真 git 历史、真进程**:`NODE_ENV=production` 起一个真进程量那一格;
造病在真提交上跑 blame;租户刀注入真路由。
**⑤ 重复稳定性** —— 全量回归跑了**两遍**(第一遍红,见 §九);造病刀各跑 2 次以上,结果一致。
**⑥ 残留与不确定** —— 见 §十。

### 数

**全量回归**:`MP_AUTOMATOR=… bash apps/api/run-all-tests.sh` ——
**111/111 全在场全过**,`not ok` **0**,整跑 **2 分 39 秒**(套件 2 分 20 秒 + 夹具与起停 19 秒)。
**断言 3,870 → 3,887(+17)**,逐套:auth-surface +10 · untouched-proof +3 · tenant-ownership +3 · merge-window +1。
**覆盖面 111/111,零断言套件 0**。

**棘轮(J-43 逐个重量,J-50 归因)** —— **没有一个变大**:

| 棘轮 | 上批 | 本批 | 变化与归因 |
|---|---|---|---|
| `customer.js` 行数 | 2,811 | **2,799** | ↓ −12 · **归因:真修好了** —— 日1 `d04c1db` 把三个 party 出口搬进 `party.js`(2,817 → 2,799,差额对得上) |
| `party.js` 行数 | (无) | **31** | 新立 · 贴身棘轮,已造病验红 |
| `local-server.mjs` | 17,707 | **17,707** | 未变 |
| `admin.js` | 7,928 | **7,928** | 未变 |
| styles.css 写死色 | 31 | 31 | 未变 |
| 小程序 wxss 写死色 | 2,356 | 2,356 | 未变 |
| UTC 判天 | 12 | 12 | 未变(**头一遍我拿掐短的命令量成 14** —— 把两行注释也数了;J-39 同一把尺子,已用判据原样那条命令重量) |
| 三个旧金 / 丙筐 | 183 / 16 | 183 / 16 | 未变 |
| 未动无标记存量 / 豁免具名 | 34 / (无) | 34 / **38** | 后者新立(J-51) |

棘轮对账表:`handoff/night-runs/棘轮对账_夜9_2026-09-12.md`(在册 **11** 个,量到 11 个)。

**写库自报(三库分开)**

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **生产库未动** | 这一夜零次 `railway` 调用 |
| **本机库** | **本机库未动**(逐表行数) | `tools/db-snapshot.mjs` 跑前跑后各一次:**92 张表,有变化的表 0 张**。口径说清:这把刀比的是**逐表行数**,不是逐行指纹 |

> **未动对照:handoff/night-runs/夜9_本机库未动对照_2026-09-12.md**
> (J-49:这句话要靠**标记**不靠措辞 —— 我原本写的是「未碰 / 零差异」,
> 判据按字面认「未动」,换个说法就绕过去了。**那等于用措辞躲判据**,改回标准说法并挂上对照表。)
| **沙箱库** | 有动,全是回归夹具与心跳 | 回归跑了两遍,每遍自造自收;`heartbeat-shape` 全过 |

**服务**:回归会 `pkill` 所有实例;跑完 **4128 与 4310 都自己回来了**,收尾自证打印了两边的库路径与窗参数,
我另外现查了两个端口都在听。

---

## 九、这一夜**红过一次**,照实写

第一遍全量回归 **2 项未过**,红的是 `test-assertion-baseline`:
**断言总数只涨了 +1**,而我这一夜明明加了 17 条。差额一查就是 **判据五(断言增量律)** 要抓的东西 ——

`mp-placeholder-size 3→0` · `mp-overlap 4→0` · `mp-home-sections 9→0`,**三套一条断言都没打出来**。
它们自己**报红报得很正确**(「automator 取不到 —— 按红处理」),
但查下去发现:**`miniprogram-automator` 这个模块根本不在这台机器上了**,而它的路径**没记在仓里任何地方**。
与「沙箱库放 `/private/tmp` 被系统清掉」同一族:**装在仓外的东西,路径必须记在仓里。**

处理:装回仓外(`~/ll-mp-tools`;撞到 `~/.npm/_cacache` 是 root 所有 —— **没有用 sudo**,
换 `--cache` 绕开),路径补写进 `handoff/走查总表_端口账号与环境_2026-08-24.md` §六,
然后**带 `MP_AUTOMATOR` 复跑全量** ⇒ 111/111 全过、断言 **+17**、覆盖面 111/111。

**差额闭合**:16(三套找回来的)+ 1 = 17 ✓ —— 判据五那条「差额必须有解释」对上了。
**护栏是好的**:少 16 条覆盖面没有静默溜过去,是判据当场把它顶出来的。

**已登记待做**:三套各自复制了同一段 `MP_AUTOMATOR` 解析(公约④ 该收成共用件),
并给它一个默认查找路径 —— 找不到仍然红,只是不必每次手打环境变量。

---

## 十、残留与不确定(照实列)

1. **`IS_PRODUCTION` 纯环境变量判定**(`local-server.mjs:163`)—— 只报不改,等你裁(§一)。
2. **段 5 那两处「靠自觉撑着」** —— A 类没有判据拦「新写一句不传参」,B 类五处空租户会静默写到旗舰店名下。**一行没改**,等你定。
3. **配色批五项**只出了料,**没做实现**(段 6 明令)。
4. **`users` / `services` 49 处租户条件**仍在总表里排队,本批没动。
5. **`MP_AUTOMATOR` 共用件 + 默认路径**(§九)已登记未做。
6. 本机库我只比了**逐表行数**,没做逐行指纹对照(跑前没留指纹基线)。下批起两样都留。

---

## 十一、自评 L1–L5 + 停线自查

| | 做到没有 |
|---|---|
| L1 末端验证 | ⚠️ 本夜六段里**只有段 6 有界面产物**(两张对照图,已出);其余五段的交付物是**判据与结论**,末端就是「刀红了没有 / 现读到的行号对不对」,我按这个验的 |
| L2 同类扫尽 | ✅ 三处都做成白名单式全仓扫 + 条数下限(读写同源 437 / PROOF_FROZEN 38 / 棘轮注册表 11) |
| L3 无图不结案 | ✅ 段 6 的建议全部附并排图;没图的地方我没标「已验证」 |
| L4 反例数据 | ✅ 每条新判据都先造一个**真会发生的**假违规;段 4 那把刀因此被咬出四个洞 |
| L5 重复稳定 | ✅ 全量跑两遍;造病刀各 ≥2 次;**但段 6 的对照图只拍了一轮**(重拍会换时间戳,不算复跑) |

**停线自查**:未推 main ✅(领先 274)· 未碰生产 ✅ · **4128 既有行未写** ✅(逐表行数零差异)·
未看密钥文件 ✅ · 无 sleep 轮询 ✅(等回归用的是「进程在不在」的阻塞判断)·
**没松判据、没抬棘轮** ✅(唯一动的是**收紧** customer.js,带 J-50 归因)·
**没改合同图** ✅ · **配色批只出料没做实现** ✅ · 密码密钥零字进文件 ✅。

**没照字面做的**:没有。段 0 的「不许为了对上 A 去写成 A」我照做了 —— 结论是 B,就写 B。
