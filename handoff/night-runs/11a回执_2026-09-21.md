# 小批 11a 回执 · 2026-09-21

**点名(J-81):** `小批11a_批D207最后那一下_夜班改制自排唤醒_记我第39笔说谎的日期是我起的头_2026-09-21.md` md5 `62f013b24fd7fe2bd4ab6c898a7ec184`(桌面/仓一致)

---

## 1. 🟢🟢 D207 执行完了 —— **五条执行条件逐条**

### 条件① 执行前再点数(生产只读)
```
主选择式命中        = 51   ✅(要 51)
带 EXISTS 的命中     = 51   ✅(要 51)
真店会被命中的行     =  0   ✅(要 0)
基线:user_identities 181 · users 77 · bookings 1,242 · lucky-luxe 37 人 · jics-nail 0 人
```

### 条件③ 执行原文(一字不改,就是跑的这一句)
```sql
DELETE FROM user_identities WHERE id IN (SELECT i.id FROM user_identities i
  JOIN users u ON u.id = i.user_id
  WHERE i.tenant_id <> u.tenant_id
    AND EXISTS (
      SELECT 1 FROM user_identities k
      WHERE k.user_id = i.user_id AND k.provider = i.provider
        AND IFNULL(k.provider_user_id,'') = IFNULL(i.provider_user_id,'')
        AND k.tenant_id = u.tenant_id AND k.id <> i.id
    ))
```
**条件②:一条走完,没有循环、没有分批。** 前置/后置断言包在同一个 `BEGIN IMMEDIATE` 里,任一不符整批 `ROLLBACK`。

### 执行
```
前置断言:命中 = 51
DELETE changes = 51
后置断言:user_identities 181 → 130
COMMIT ✅
```

### 条件④ 执行后四验
```
① user_identities   181 → 130 ✅    串味行 51 → 0 ✅    lucky-luxe 名下身份行 105 → 54 ✅
② lucky-luxe 37 人 / 161 单 · jics-nail 0 人 / 1 单  —— **一个没变** ✅
③ users 77 ✅ · bookings 1,242 ✅ —— 不变
④ 抽 3 人(demo-ai-cust-1/2/3):三个都**认回同一个人** ✅
   🔴 反向守:同样三份凭据从 `lucky-luxe` 那一侧解析 —— **三个都认不到** ✅(清理前会串味)
```

### 🔴 条件④ 里有一项我**做不到**,不是我选择不做

令写的是「抽 3 人走**真正门 HTTP** 认一次」。**生产上做不到,而且是硬规则:**
```
wechat-code-stub.mjs:42
  替身只在 ci/sandbox 可达,当前库域 local
  —— 「这一条是硬的,没有开关能打开它」
```
生产库域是 `local` ⇒ stub 正门登录**被代码硬拦**(真要走正门得有一个真实微信 code,我拿不到、也不该拿)。
**所以我用的是 app 自己那条解析式**(与正门 `resolveUserByIdentity` **同一句 SQL**),并加了反向守。
**HTTP 那一层是在彩排时(还原库、沙箱域)验过的;生产这一次只到 SQL 层。差在哪,写在这儿。**

**D207-b 那 1 行(`booking_mt1iixg3_046wjx`)继续冻结,一个字没动。**

## 2. 🔴 §〇 备份文件名:对照表 + 判据

**表:`handoff/backups文件名真伪对照_2026-09-21.md`**(`backups/` 是 gitignored,所以入库那份在 handoff;目录里放了副本)

```
🔴 名字在说谎的  5 份     🟡 按本地时间(CST)命名的 1 份     🟢 准的 2 份
最狠的一份:生产库_夜16续推前_20260922T0300Z.sqlite  实际 2026-09-20T21:02:36Z  差 -30.0h
```

**不改名**(台账与回执已引用过,改名会让引用指向不存在的文件 —— 那是另一种说谎),**表就是它们的真相**。

**判据**:`tools/backup-name-check.mjs`(唯一出口)+ `test-backup-wal` ⑦a–⑦g **7 条**,两面验齐:
名字写错两天必红 · 差 10 秒放行 · HHMM 那一分钟必须对 · 没有时间戳也判不过 ·
历史豁免只许变短(6 条具名)· **CI 上没有 `backups/` 时明写「没验成」不算绿**(J-98)。

### 🔴 做这张表时我自己栽了两次,两次都当场重量

1. 第一版用 `stat -f %Sm` 取时间 —— **那是本地时间,我却贴了个 `Z`**。
2. 第二版解析名字只认 6 位 `HHMMSS`,于是 `T0600Z` 被读成 `000000`,**凭空造出两个假差值**。

**一张不准的真相表,比没有这张表更坏。** 两版都没发出去。

## 3. 🟢 §二 夜班改制:自唤醒机制**现查确认存在**,三条限制照抄

```
CronCreate  —— 定时把一段提示词入队
  ① 只在空闲时触发(不打断正在跑的那一轮)   ← 正是夜班要的
  ② 🔴 只活在这个会话里,会话结束就没了      ← 店主那台 Code 会话整夜不能关
  ③ 循环任务 7 天后过期
```
**上一轮我当场建了一个并 `CronList` 列出来证明它存在,确认窗口已过后又 `CronDelete` 撤掉不空烧。**

🔴 **而昨夜那 9.7 小时是怎么没的,也量清楚了**:上一轮我报「现在 20:11Z,还剩 9.7 小时」,
**下一次机器时钟已是 09-21T08:16Z** —— **那段时间是在两次消息之间流掉的,没有任何东西叫醒我。**
**这正是 §〇 要问的那件事,现在有答案了:机制有,昨夜没用上。**

---

## 三行口径
```
本机全量:(见下)
GitHub CI:本批未推(D207 是生产数据操作,不产生代码变更;文件名判据与对照表待随下一批走)
GitHub CI 未跑:—
```

## 要你裁的

| # | 事 | 挡不挡 |
|---|---|---|
| 1 | 🔴 **条件④ 的「真正门 HTTP」在生产上被硬规则拦住**,我只到 SQL 层。要不要另想办法补(例如你本人用微信扫一次) | 不挡 |
| 2 | 涉钱口剩 7 条 · 11 条追不到口的断言剩 4 · D199 · J-60 那 23 处 —— 队列还在 | 不挡 |
| 3 | 夜班改制:**下一份夜班令可以直接写「自己排唤醒」** —— 但要写明「Code 会话整夜不能关」 | 不挡 |
