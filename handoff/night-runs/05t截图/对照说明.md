# 05t · 图上每个视觉元素 → 判据名 → 截图文件名(一行一条)

> 店主 05t 段 7 要的那张对照表。**合同图** = `handoff/商家端主页重画_两端设计图_2026-09-03.html` v3.1。
> 规矩:每一条都要能自己走完「图上有 → 有一条机械判据在守 → 有一张图能看」这三步;
> **走不完的那一条如实写「没有」**,不许拿别的东西顶上。

## 一、网页端(段 3)

| # | 图上的元素 | 判据(能机械证伪的那一条) | 截图 |
|---|---|---|---|
| 1 | 字体三件套 Fraunces / Noto Serif SC / Noto Sans SC | `test-dashboard-home ⑬d`(admin.html 引了那三个 family)+ `⑬e`(大数/小牌/此刻/下一位都是 Fraunces + tabular-nums);**跑机现测** `getComputedStyle` 大数字体 = `Fraunces`、标题 = `Noto Serif SC` | 01–07 |
| 2 | 30 个设计令牌 × 三段(浅 / 系统深 / 站内选深) | `tools/design-token-diff.mjs` 从**合同图现抽**三段,与 `apps/web/design-tokens.css` 逐条比名与值,差一位就红(进预检);两把刀验红:改 `--herogold` 最后一位 · 删一条 `--herolive` | 01–07 |
| 3 | 英雄区是**深色块**(`--hero` 底 + `--herogold` 金数字 + `--herogrid` 细网格) | `⑬f`(`.dh-hero` 有 `background-color: var(--hero)`,`.dh-big` 是 `--heroink`,`.dh-now-cell` 边是 `--herogrid`);跑机探针:英雄底色 `rgb(42,37,33)` = `--hero` 当前值(浅);深色态 `rgb(21,19,15)` = `#15130f` | 01 / 04 |
| 4 | 折线**带渐变填充**(.35 → 0)+ 末点圆环 | `⑬g`(`linearGradient` + 两个 `stop-opacity` + 金色描边 + 末点 `circle` 四件齐);跑机探针:`linearGradient stop` = 2、`circle` = 1;造病(删 defs)→ 红 | 01 |
| 5 | 数字滚动 600ms,`prefers-reduced-motion` 直接跳 | `⑬h`(`ROLL_MS = 600` + `requestAnimationFrame` + reduce 分支);造病(600 → 0)→ 红。**像素给不到**:滚动过程截不出来,截图只有终值 | 01(终值) |
| 6 | 指标轮播 + **5 个 dots**,选中那个会移动 | `⑬i`(轮播名单 5 个 = dots 5 个,`.on` 有 `width:14px`)+ `⑬j`(CSS 选择器跟着真实节点走:渲染的是 `button` 就不许只写 `i`);跑机探针:`dots=5 · dots选中=1` | 01–07 |
| 7 | dh- 块里零兜底色 | `⑬c`(`.dh-*{…var(--x, #硬编码)…}` 全仓 0 处);造病(加回一个)→ 红 | — |
| 8 | 深浅两态 | 站内选深(`data-theme="dark"`)与系统深(`prefers-color-scheme: dark`)**各拍一张**,两张的 `--hero` 都取到 `#15130f` | 04–06 / 07 |
| 9 | AI 今日一句显示门店当地时刻 | `⑬k`(时刻文本由后端 `storeClockText` 出,前端零格式化) | 01(北京店 01:12) |

## 二、小程序端(段 4)

| # | 图上的元素 | 判据 | 截图 |
|---|---|---|---|
| 10 | 令牌与网页**逐条相等** | 同一把 `design-token-diff` 比第三份 `miniprogram/styles/tokens.wxss`(30 × 2 段全等);第三段(站内选深色)小程序**没有对应物**,刀里写成 `mini:null` 并打印说明 —— 不当绿也不当红 | ⬜ 见下「没拍到的」 |
| 11 | 深色英雄块 / 金色 dots | `test-mp-home-owner ⑮d`(`.dh-hero{background:var(--hero)}`、`.dh-big` 是 `--heroink`、`.dh-dot-i.on` 是 `--herogold`)+ `⑮c`(那一段里零写死色) | ⬜ |
| 12 | dots = 5、`.on` 会移动 | `⑮e`(`buildOwnerHome().dots.length === 5` + `.on` 宽度 28rpx + 轮播名单与网页端同一份) | ⬜ |
| 13 | 轮播不发请求、计时器会被清 | `⑮f` / `⑮g`(`onHide`/`onUnload` 清 timer) | ⬜ |
| 14 | **字体**(如实) | `⑯`/`⑯b`:`utils/numfont.js` 里写明「小程序不能引 Google Fonts → 中文是**系统字体**,不是 Noto」「数字要 Fraunces 得 https + 域名白名单,本地给不出 → 今天也是系统字体」;地址为空时回 `loaded:false`,**不假装成功** | ⬜ |

## 三、全屏态(段 4 第 4 条)

| # | 元素 | 判据 | 截图 |
|---|---|---|---|
| 15 | 底色 `--hero` 系 + 金色 Fraunces 大数 | `test-dashboard-home ⑥*` 那一组(全屏态既有判据)+ CSS 现改:`.dh-fs` 用 `var(--hero)`、`.fs-big` 用 `Fraunces` + `var(--herogold)` | 08 / 09 |
| 16 | 币种跟店 | 08 = `CAD $` · 09 = `¥`(两张并排就能看出币种跟着店走,不是跟着上次看的那家店) | 08 / 09 |

## 四、数据(段 2)与平台(段 6)

| # | 元素 | 判据 | 截图 |
|---|---|---|---|
| 17 | 三店都有「今日/本周/本月/本年」的数、12 点折线不平 | `test-seed-rich`(66 条,打的是活服务的接口);造病:把一家店的种子单挪走 → 4 条红并点名 | 01–03 |
| 18 | 此刻三态 + 下一位 | `test-seed-rich ④`(在做 ≥1 / 待到店 ≥2 / 已完成 ≥3 / 下一位有人 + **钟点落在 08–22 点**,UTC 原文会露馅) | 01–03 |
| 19 | 五项待办 | `test-seed-rich ⑤`(四项逐项 > 0;`shiftApproval` 那张表全仓不存在 → 如实下发 `available:false`,待裁 #12) | 01–03 |
| 20 | 平台「AI 包」状态格 | 句子由后端出(`platform-ops.listTenants` 的 `aiLabel`),前端零判断;拿不到出「—」不出「未开通」 | 10 |

## 五、**没拍到的,如实写在这里**

- **小程序三店首页 + 员工打卡门(4 张)**:这一批**没拍到**。
  原因:DevTools 自动化会话僵死(就是 D167 那件事),`tools/mp-home-shot.mjs` 连上去之后不返回。
  D167 的护栏本批已经落地(单套 60 秒超时 → 报「本轮未跑」→ 整轮继续),
  **但护栏解决的是「别拖垮整轮」,不是「让会话活过来」** —— 会话本身要重开一次开发者工具的自动化端口。
  所以这四张按「未跑」计,不按「通过」计;下一批开工第一件事补拍。
- **数字滚动的过程**:截图只有终值,滚动那 600ms 截不出来 —— 由判据 `⑬h` + 造病背书,像素给不到就明说。
