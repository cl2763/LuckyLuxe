# 07r 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `ef22ee16d8fdf8a1fcc7b0ccf7b2cbf4`(全 32 位)
**读数时刻**:2026-09-15 01:03:29 CST
> 本批提交(**3** 个,从 `4cc12a9` 数起)· HEAD `a318e6b` · **领先 `origin/main` 320 个** ——
> 对账:`4cc12a9` 领先 317 + 本批 3 = **320** ✅
> 这些数都由 `bash tools/batch-footer.sh 4cc12a9` 现出,**一个都不手写**(裁 #65 / #93)

---

## §一 · 五处补丁逐条改完 —— 但**生产那一步撞停线,我停了**

落点:`tools/prod-readonly-five.mjs`(第三版,**全部 `SELECT`**,库一律 `{ readOnly: true }` 打开)

| # | 改成什么 |
|---|---|
| ① | 问 1 三档**各补「涉及顾客数」** → 六列(号码数 + 人数),因为要和「有余额的人数」对得上 |
| ② | 问 1 档2/档3 补 **`dig <> ''`**,并**单列「`dig` 为空的档案数」**(纯符号号码本身就是脏数据信号) |
| ③ | 问 4 / 问 5 的同号多条**各给档1(字面)+ 档3(后十位)两列** —— 两条路都通向「变新人」,原因不同 |
| ④ | **`CAST(st.subtotal_cents AS INTEGER) / 100`** —— 不依赖「列碰巧是 INTEGER」 |
| ⑤ | 新增**问 0b 状态分布 + 内建停下来条件**,**写进脚本里**,不是写进回执 |

### 🔴 §一⑤ 那个停下来条件,**在本机库上就当场触发了**

```
【0b 状态取值分布】
  coupon_grants: active 20 · unused 10 · expired 6 · used 6 · revoked 4
  settlements:   signed 182 · voided 73 · pending_sign 14

🔴 撞上内建停下来条件 —— coupon_grants.status = 'unused' × 10
   **问 5 没跑**(本机库这一轮)
```

**查下去的结果**(只读):

- **`unused` 这个状态在产品代码里一处都没有** —— 全仓唯一出现的地方是我自己那条停线正则;
- 那 10 条**全在演示店**(`demo-ai` 2 · `demo-basic` 8),`grant_kind = 'template'`;
- 🔴 **但 `/my/coupons` 不筛状态、原样下发**(`local-server.mjs:11201` 那条 SQL 没有 `WHERE status`)——
  **所以这 10 条顾客是看得见的,而它的状态是产品不认识的值。**

**`settlements` 那边**:`voided` / `pending_sign` 都不像「已完成」,**这一侧没触发**。

### 🔴 跑生产 —— **撞停线,停在那一步,一条 SELECT 都没跑**

| 查的什么 | 现测(全只读) |
|---|---|
| `railway` CLI | 在(4.65.0),已登录 |
| SSH | 第一次 `no_ssh_key`(agent 里零 identity);**加 `-i ~/.ssh/railway_luckyluxe_ops` 后通了** |
| **`sqlite3`** | 🔴 **`NO_SQLITE3` —— 容器上没有** |
| `node` | v22.11.0 在;`node:sqlite` 要 `--experimental-sqlite`,**加上就可用** |
| 库文件 | `/app/apps/api/local-data/lucky-luxe.sqlite` · **5,980,160 B** · **mtime Aug 27 04:56** |
| WAL | **没有 `-wal` / `-shm`**(所以补丁⑦ 那个 WAL 顾虑在这台上不成立) |
| 线上服务 | `www.luckyluxeatelier.com/health` → **200**(服务活着) |

> **按停线:「没有 `sqlite3` → 停下来问,不往生产上装东西。」条件触发了,我停。**
> 我把你做判断需要的事实查齐了,**但没有自己改用别的工具去绕过这一条。**

🔴 **另外一件我不下结论、只报上来**:
**库文件 18 天没动过(Aug 27),而线上 /health 是 200。** 没有 WAL 文件,所以不是「写在 WAL 里没落盘」。
这可能意味着几件不同的事(站点没有写入 / 运行实例挂的是别的卷 / 别的路径),**我不猜。**
**但它直接影响那五个答案代表什么** —— 所以先报。

**现成的下一步(等你一句话)**:容器上已有 `node --experimental-sqlite`,
用 `new DatabaseSync(path, { readOnly: true })` 是**同一个引擎层只读标志**,**不需要装任何东西**。
要不要改用它,**归你裁** —— 我不自己换工具。

---

## §二 · 追问的真答案(**而且我第一次问法自己搞错了靶子**)

**先认一处**:上一份回执我说「已造签署口那一刀,红出来的是…」——
那一刀跑的是 `customer-paths`,而那 20 条 A 类挂在**别的套件**上。
于是「仍绿 3 条」点的是**支付和卡包**,**跟被砍的口毫无关系,是噪音不是发现**。
已把「**靶子与套件要配对**」写进台子的注释里。

**改跑 `scan-sign`(它就是测签字那一套)之后,真答案是**:

```
[刀账] 这一刀:红 1 条 · 仍绿 43 条 · 其中**声称成功却仍绿** 4 条
⚠️ 套件红了,但红的不是「声称成功」那条:corner:双端同时点签,只成一次: [200,200]
```

🔴 **两件都要说清**:

1. **红的那 1 条是「双端同时点签,只成一次」—— 并发那条,撞上的。**
   把「签字落库」整条注掉之后,**没有任何一条说「签字成功了」的断言变红。**
   **这就是 07m 那个形状,而且这次在钱的路上。**
2. **`scan-sign` 是 fail-fast**(`check()` 里直接 `throw`)——
   无刀时 **53 条**,造病后**停在第 44 条** ⇒ **后面 9 条根本没跑到**:
   「签完另一入口变已签只读」「已签的单不再出新码」「snapshotHasInk」「定金守恒」**都没机会说话**。
   **所以这一刀的结论只能到这里:不能说那 9 条守住了,也不能说它们没守住 —— 它们没跑。**

**报告格式已按你说的固定下来**:每一刀输出
**「红 N 条 / 仍绿 M 条 / 其中声称成功却仍绿 K 条 + 仍绿点名」**,并进造病表。

---

## §三 · J-58 第五款:现存扫描器**三个数**

| | 数 | 怎么来的 |
|---|---|---|
| 扫描器共 | **8 把** | `tools/*.mjs` 里同时有 `readdirSync` 遍历 + 正则判形态的 |
| **有 `--probe` 自守的** | **1 把** | `knife-restore-scan.mjs`(07q 新写的那把) |
| 🔴 **没有的** | **7 把** | 逐个点名 ↓ |

`assert-reads-fact` · `customer-paths-scan` · `eval-citation-check` ·
`release-checklist` · `shot-freshness` · `success-state-scan` · `success-state-trace`

**未做,原因**:本批时间给了五处补丁 + 生产探查 + §二 那个返工。**补 probe 是下批第一件。**

---

## §一⑦ / §七 · 「看起来很干净」的 0,逐条说明是真 0 还是量不到

| 数 | 是真 0 还是量不到 |
|---|---|
| 问 1 三档在本机库全 0、且三档无差 | 🔴 **量不到** —— 本机库的号是脚本按模板生成的(`139xxxxxxxx`),格式天生一致 |
| 问 1 `dig 为空的档案数` = 0 | **真 0**(本机库没有纯符号号码) |
| 问 2 `is_migrated 为空的行数` = 0 | **真 0** —— 而这正说明**第一版那个 NULL 陷阱在本机库跑得通,要到生产才露馅** |
| 问 4 `余额为负的顾客数` = 0 | **真 0**(本机库) |
| `settlements` 侧没触发停下来条件 | **真的没有**(`voided`/`pending_sign` 都不像「已完成」) |

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **119 / 119 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **21 套 · 红 8** | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 0** | 同上 |
| 断言(在册) | **4,017** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:4,017 **+0** —— 本批**没有新增断言**(改的是查询脚本、造病台报告格式、扫描器口径)。

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**。

**棘轮**:`local-server.mjs` 17,650(不动)· `admin.js` 7,928(不动)· J-60 欠账 28(不动)

**跨批账本自检**:`4cc12a9 领先 317 + 本批 3 = 320` ✅(机械对账)

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
**生产库:只读探查 4 次(`which sqlite3` / `ls` / `node -v` / `/health`),零 SELECT 跑过,零写语句,零文件落地。**
本机库:只读查询(`readOnly: true`)。

---

## 顺手治的一件

`tools/prod-readonly-five.mjs` 没接 `requireTarget`,被 `db-target-guard ①c/①d` 当场咬住 —— **咬得对**。
我一开始想「这是只读的,要什么护栏」,但那条律治的病根不是「会不会写坏」,是
**「有默认值,所以打错了不报错」**:
> **只读打错库,报出来的数照样是错库的数,而它会被当成生产的答案写进回执。**
> **读错库比写错库更难发现。**
已接上(不给库 → 拒绝执行),并重生成《写库脚本护栏三列清单》。

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| 跑生产六条 | **停在停线上** —— 容器无 `sqlite3`。事实已查齐,换不换工具归你裁 |
| 问 5(本机库那一轮) | **按内建停下来条件没跑** —— `coupon_grants` 有 `unused` |
| §三 给其余 7 把扫描器补 `--probe` | **未做,原因:本批时间给了五处补丁 + 生产探查 + §二 返工。下批第一件** |
| §六④ 其余 82 个口的造病 | **未做** —— 而且 §二 刚暴露一件:**靶子要和套件配对**,得先把 83 个口各自的「拥有套件」标出来 |
| §六⑤ 门关档红 8 · J-60 继续降 · 9 条冻结理由 | **未做,没排到** |
| §六⑥ 段 C · D191 第三/第四形态 | **按令等五问的数** |
