# 夜16 早报

> 🔴 **续跑追加(夜16-续 `050da3d2c1dda72a58b010de67b85480`)**:早报不是收工信号,下面是续跑的部分。
> 时钟:开工 `2026-09-20T18:52:27Z` → 收工 `2026-09-21T06:52Z`。

**本令:** `夜班令16_十二小时_D208预批条件写死_不许停_AI修图今晚别碰_2026-09-22.md` md5 `a2fab69db44646aa42a4d4e8ed65d975`(桌面/仓一致)

---

## 1️⃣ 🟢 **小程序现在发得出去了**

```
主包  2,199 KB  →  1,294,665 B = 1,264 KB      闸 2048 · 你定的目标 1900
上传闸:✔ 真走了一次,过了
        cli upload · AppID wx247cd8ad9907430d · 开发版 · 未提审 · 未发布
```
**余量 636 KB。** 做法:`pages/merchant/**` 那 38 页进分包(顾客根本走不到那儿,却一直占着主包)。

⚠️ **(丙) 压图没做成,而且量出了为什么**:macOS `sips` 在 q45 / q60 / q68 **三档都把图压得更大**
(entry-bg 58 → 64 / 79 / 86 KB)—— **原图已经比它能产出的更优**;机器上没有 jpegoptim / mozjpeg。
**没为此装新依赖**(令里没授权)。**没删图、没换图。**

## 2️⃣ 生产动没动

**数据一个字节没动。** 两家真店五个数、表数、触发器,推前推后逐项一致。
生产只跑了 `SELECT` 与只读诊断;三次备份是 `VACUUM INTO` 到容器 `/tmp`、拉回本机后**当场删掉**。
🔴 **D207 那 51 行 · D207-b 那 1 行:整夜冻结,一个字没写**(推后复核:那一行仍在,1 行)。

## 3️⃣ 推了两次,两次都绿

| | commit | CI run | 轻量六项 |
|---|---|---|---|
| 上半场 | `489b87a → 56e1125` | `106135935336` ✅ | 全过 |
| 下半场 | `56e1125 → 1779e5d` | `106139833951` ✅ | 全过 |

**推前备份都是先拍的**:`…20260922T0000Z`(sha256 `52186127…`)· `…20260922T0130Z`(sha256 `2b339b00…`),两头一字不差、本机打开验过。

**六项之外我每次都多验两条**:
- **幂等**:`user_identities` 仍 **181**、重复组仍 **51** —— 又两次部署,一行没多
- **D207-b 那一行还在**:1 行,冻结口径在执行

## 4️⃣ 队列六件,做完六件

| # | 件 | 结果 |
|---|---|---|
| 1 | **D208** | 🟢 四条验收全过(见 1️⃣;判据 `test-mp-package-size` 12 条两面验;顾客主路四步 + 分包三页全打得开) |
| 2 | **小程序 L5 补跑** | 🟢 5/5 逐项一致,零异常 |
| 3 | **乙档第二批** | 🟢 10 处 + 删掉两个零调用方死键(删前按 J-96 校尺子) |
| 4 | **推 + 轻量六项** | 🟢 见 3️⃣ |
| 5 | **乙档第三批** | 🟢 10 处 ⇒ **涉钱剩余 32 → 22 → 12** |
| 6 | **兜底** | 🟢 两张盲区表走完两条路(立 `test-tenant-parent-scan`)· D209 立案 |

**棘轮全守住,而且都更紧了:**
```
local-server.mjs 17,539 净 0      admin.js 7,893 未动      小程序写死色 2,350 未动
J-105 涉钱剩余  32 → 12(只许降)  J-105 已修下限 14 → 24(只许涨)  主包体积棘轮:新立
```

## 5️⃣ 新发现

### 量出来的

1. 🔴 **我的尺子把 NULL 当成了悬空。** 查 `ai_response_feedback` 时用 `LEFT JOIN … WHERE 父 IS NULL` 数出 97,
   我先写成「97/97 悬空引用」。**分开数之后:`conversation_id` 为 NULL 97 行 · 真悬空 0 行。**
   那 97 行来自小红书 48 / 抖音 15 / null 33 / mock-chat 1 —— **这些渠道本来就没有企微会话,NULL 是设计如此。**
   已把「NULL ≠ 悬空」钉成两条常驻断言,含那次报错的数。
2. 🔴 **判据自己错了一次**:`J-105e` 原来断言「`local-server` 恰好 4 处 `actorOf`」——
   **那是数快照不是数性质**,我第二批修得更多,它就红了。改成**只许涨的下限**。
3. 🟢 **D206 甲档在生产上被看见了**:`ai_response_feedback` 有 **3 行 `created_by` 是空串** ——
   正是 `local-server.mjs:2100` 那一处 `adminSession?.email || ''`。**甲档不是理论,它在库里。**
4. 🟡 **D209 的影响面比立案时预设的小。** 10g 写「都答不了」;现查 `user_identities.created_at` 覆盖 **77/77**,
   **两家真店 0 人被回填污染**(脏的 13 人全是演示店)。**详见 D209,我把纠正写在前面。**
5. ⚠️ `miniprogram/pages/merchant/subscription/index.js:90` 有「已支付」——
   但那是**商家给平台付 SaaS 订阅费**,那笔钱确实经过平台,**措辞未必错**。**登记,没动。**

### 推出来的

6. 主包会一直长 —— 今天 1,264 KB,余量 636 KB。**这是推的,不是量的**:没人能说它多久会吃完。
   棘轮已经立上,再涨会当场红。

## 6️⃣ 要你裁的

| # | 事 | 挡不挡上线 |
|---|---|---|
| 1 | 🔴 **D207 那 51 行的清理** —— 仍等你批 `SELECT` 原文。整夜冻结,一字没写 | **不挡** |
| 2 | 🔴 **D207-b 那一行** —— 按 10g-补「冻结 · 身份未认定」执行中,再核时点 = 小婕开张前对账 | **不挡** |
| 3 | **D209 改法三选一** —— 我倾向 **C**(只加列、老行留 NULL、查询处如实标「估算」)。**B 那条会把一个已知不准的数写进库里变成看起来准的数** | **不挡** |
| 4 | **乙档剩 12 处涉钱** —— 继续 ≤10 一批?还是先看前三批的改法 | **不挡** |
| 5 | 🔴 **还是那条最省事的**:`requireAdmin` 主钥匙分支自带身份,剩下 12 + 26 处一行不改就都对 | **不挡,但越早裁越省** |
| 6 | **`ai-chat` 两张名单都没点到** —— 我按 J-107 留在主包没挪。要不要进分包 | **不挡** |
| 7 | **上传的是开发版,连的是本机沙箱**(`USE_LOCAL_SANDBOX = true`)。真要提审前必须改 `false` | 🔴 **挡提审**(不挡今天) |

---

## 三行口径

```
本机全量:131/131 套件 · 4,277 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:🟢 绿 ×2(106135935336 · 106139833951)
GitHub CI 未跑:0 套
```

**断言增量对账(判据五):** 4,257 → 4,269(+12 = `mp-package-size`)→ 4,277(+8 = `tenant-parent-scan`)。
套件 129 → **131**。**每一笔都对得上。**

## 自评 L1–L5

- **L1** 🟢 D208 的数来自**那台决定发不发得出去的机器**(`cli upload`),不是我估的;顾客主路四步真点过。
- **L2** ✅ 乙档两批共 20 处成族修(结算/对账/储值/券/工资),不是挑好改的修。
- **L3** ✅ 分包白名单严格照夜16 §二;`ai-chat` 没点到就没挪(J-107)。
- **L4** ✅ 体积棘轮两面验含「塞进**分包**不许红」;删死键前拿三个已知在用的键校过尺子(J-96)。
- **L5** 🟢 小程序 L5 补跑 5/5。
- ⬜ **没做到的**:网页顾客端像素图仍是黑帧(长期欠账,已按 10h §四 定性为不挡);D208 之后**没有重新跑一遍 `cli upload`**——
  第三批只改了后端,不影响小程序包,但严格说**最后那一版没再过一次那道闸**。如实挂着。


---

# 📌 续跑追加(夜16-续)

## 续跑做了什么

| # | 件 | 结果 |
|---|---|---|
| ① | **upload 前置闸** | 🟢 把 `api.js:6` 那句「上传前务必改 false」变成机器闸(`tools/mp-upload-guard.mjs` + `tools/mp-upload.sh`)。**J-92:注释里认出来的问题,代码里没处置,那句提醒就是在给它背书。** 7 条断言含两面验(true 必红 / false 回绿 / 读不到也必须拒) |
| ② | **乙档第四批** | 🟢 10 处 ⇒ **涉钱剩余 12 → 2**。剩的 2 处写清了为什么不动:参数名叫 `actorEmail`,改它要一起改参数名 = 一次口径改动,不在本批授权内(J-107) |
| ③ | **D207 `SELECT` 提案** | 🟢 只贴不动。**安全前置量出来了:51 行里 51 行都另有一条正确租户的同款身份行 ⇒ 删掉零损失**;反向守:两家真店 **0 行**会被命中。执行式带 `EXISTS` 子句,只删有副本的 |
| ④ | **兜底:涉钱口名单重整** | 🟢 「40 个口剩 30」**从来不是可对账的清单**。今晚做出来了:**41 条写口 · 真待办 14**。补了 **7 条口 · 29 条断言** ⇒ **剩 7** |
| ⑤ | **兜底:`sed -i ''` 可移植** | 🟢 探方言再用。🔴 **我改这一步时自己造了个无限递归又自己抓住了**(见下) |
| ⑥ | **第三次推** | 🟢 `1779e5d → ed93184` · CI `106148918660` · 轻量六项全过 |

## 🔴 续跑里我自己栽的两跤(都自己抓住了)

1. **无限递归**:把 `sed -i ''` 全局换成 `_SEDI` 时,**连辅助函数自己的 BSD 分支也换了** ——
   变成 `_SEDI() { _SEDI "$@"; }`,**而且正好只在 macOS(店主这台)挂**。
   改法写进注释:那一行改用 `sed -i ""`,免得再被同一条批量改动吃掉。**J-78:批量替换前先量全。**
2. **夹具红了两次,两次都是我读得不够细**:发券没给 `mode:'template'` · 建店用了 `chain` 档(而 `chain` 本来就含 AI 包,
   于是阳性对照被**正当地**拒)。**阳性对照红了,要先问是不是景造错了**(造景律)。

## 🔴 名单那一栏的诚实话

「启发式判已有破坏测试 27 条」——**那是会高估的数**(只看路径串出现在某测试文件里、附近有敌意字样)。
**所以我只拿「没有」那 14 条当待办,不拿 27 当成绩。**

## 续跑之后的三行口径

```
本机全量:132/132 套件 · 4,313 条断言 · not ok 0 · 未跑 0 套(店主这台 Mac)
GitHub CI:🟢 绿 ×3(106135935336 · 106139833951 · 106148918660)
GitHub CI 未跑:0 套
```

**断言增量:** 4,257 →(+12 mp-package-size)→ 4,269 →(+8 tenant-parent-scan)→ 4,277
→(+7 upload 闸)→ 4,284 →(+16 破坏测试)→ 4,300 →(+13 破坏测试补)→ **4,313**。**每一笔都对得上。**

**棘轮(全部更紧):**
```
J-105 涉钱剩余  32 → 22 → 12 → 2   (只许降)      已修下限 14 → 24 → 34  (只许涨)
主包体积 1,264 KB(真打包数)         local-server 17,539 净 0       小程序写死色 2,350
```

## 续跑新增的「要你裁」

| # | 事 | 挡不挡上线 |
|---|---|---|
| 8 | 🔴 **D207 清理:`SELECT` 原文与影响行数已贴**(`handoff/D207_清理提案_SELECT原文_2026-09-22.md`)。安全前置 51/51、真店 0 行。**等你批** | 不挡 |
| 9 | **乙档最后 2 处** —— 参数名叫 `actorEmail`,要连名字一起改,是口径改动 | 不挡 |
| 10 | **涉钱口还剩 7 条没破坏测试**,其中 `/admin/wecom/open-kfid` 与 `/admin/touch-rules` **很可能是我尺子误判**,下批先验尺子 | 不挡 |
