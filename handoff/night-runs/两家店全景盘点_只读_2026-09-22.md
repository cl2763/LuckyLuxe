# 两家店全景盘点(**只读**)—— 11m 补 §二

> **量的是谁**:**生产库**(Railway Volume → 容器内 `/app/apps/api/local-data/lucky-luxe.sqlite`)
> **哪台机器**:Railway 生产容器,`railway ssh`,全程 `SELECT` / `pragma_table_info`
> **哪一刻**:2026-09-22T10:3x–10:5xZ(D207-b 已删、11m §一 未执行的状态)
> **对谁**:`lucky-luxe`(多伦多)与 `jics-nail`(小婕)两家,其余四家演示店不在本表
> **一个字节没写。**(J-108)

---

## 〇 一句话

**表 94 张,带 `tenant_id` 的 82 张,两家店里真有数据的 33 张。**
其中 **只有 7 张在 `RESET_TABLES` 里** —— 也就是说,即便那扇 `demo-reset` 门能用,
**26 张有数据的表它一张也清不到。**

---

## 一、逐表(只列两家店都有数据的)

|表|lucky-luxe|jics-nail|在 `RESET_TABLES`|涉钱|
|---|---|---|---|---|
|`bookings`|161|0|是||
|`booking_slots`|488|0|是||
|`booking_status_history`|278|0|是||
|`payments`|138|0|是|💰|
|`finance_transactions`|30|0|是|💰|
|`stored_value_transactions`|7|0|是|💰|
|`after_sales_events`|1|0|是||
|`users`|37|0|🔴 否||
|`user_identities`|54|0|🔴 否||
|`technicians`|3|4|🔴 否||
|`technician_schedules`|3|0|🔴 否||
|`services`|7|**27**|🔴 否||
|`service_prices`|7|**83**|🔴 否||
|`service_categories`|3|6|🔴 否||
|`pricing_rules`|0|4|🔴 否||
|`stores`|1|1|🔴 否||
|`business_hours`|7|7|🔴 否||
|`admin_accounts`|2|1|🔴 否||
|`ai_response_feedback`|97|0|🔴 否||
|`ai_learning_examples`|68|0|🔴 否||
|`ai_usage`|1|0|🔴 否||
|`tenant_kb_facts`|5|4|🔴 否||
|`tenant_kb_entries`|0|4|🔴 否||
|`tenant_settings`|7|5|🔴 否||
|`tenant_entitlements`|0|1|🔴 否||
|`message_templates`|6|6|🔴 否||
|`membership_packages`|1|0|🔴 否||
|`finance_recurring_rules`|2|0|🔴 否||
|`finance_targets`|1|0|🔴 否||
|`staff_compensation`|1|0|🔴 否||
|`plan_change_requests`|1|0|🔴 否||
|`platform_ops_log`|1|0|🔴 否||
|`notify_scan_marks`|3|3|🔴 否||

---

## 二、🔴 有数据、却**不在** `RESET_TABLES` 里的 26 张(明天开门要单独裁)

按「明天那扇门要不要碰它」预分三类 —— **这是我给的分类建议,不是裁决**:

### A 类 · 店主说的「假的」,要清(6 张)
`users` 37 · `user_identities` 54 · `technicians` 3(**只 lucky-luxe 那 3 个**)·
`technician_schedules` 3 · `ai_response_feedback` 97 · `ai_learning_examples` 68

⚠️ `ai_*` 那两张是**店主自己批过的 AI 话术**(11i 量过:97 条 approved)。
**它们是真的训练成果,不是假数据** —— 我把它们放在 A 类只是因为它们挂在这家店上,**千万别顺手清掉**。
🔴 **这一格我特别标出来请你裁。**

### B 类 · 门店配置,**一张都不该清**(13 张)
`stores` · `business_hours` · `services` · `service_prices` · `service_categories` · `pricing_rules` ·
`tenant_kb_facts` · `tenant_kb_entries` · `tenant_settings` · `tenant_entitlements` ·
`message_templates` · `membership_packages` · `admin_accounts`

🔴 **`jics-nail` 的 `services` 27 项 / `service_prices` 83 条就在这一类** ——
店主原话:「我帮小婕配置的这些名字是我告诉你写进去、以及当时价格表是我告诉你写进去,**这些才是真的**」。

### C 类 · 说不清,要你定(7 张)
`finance_recurring_rules` 2 · `finance_targets` 1 · `staff_compensation` 1 ·
`plan_change_requests` 1 · `platform_ops_log` 1 · `notify_scan_marks` 3 · `ai_usage` 1

`platform_ops_log` 我倾向**永不清**(它是运维留痕,清了等于抹掉「谁在什么时候对这家店做了什么」)。

---

## 三、补令点名要单独量清的六格

### 1. `users` 与绑微信的

| 店 | users | 绑微信 | 是谁(openid 后四位,不贴全) |
|---|---|---|---|
| lucky-luxe | **37** | **4** | `Lucky Member`(…enid)· `LL-I28970PU`(…Ro3w)· `LL-3LSVBVGA`(…944w)· `LL-04TI54NU`(…G6CU) |
| jics-nail | **0** | 0 | —— |

🔴 **两个新情况**:
- 绑微信的是 **4 个,不是之前以为的 1 个**。`LL-3LSVBVGA` 与 `LL-04TI54NU` **各 0 单、0 储值** ——
  像是登录过一次没下单。**她们是谁,我不知道,也不猜。**
- **`jics-nail` 顾客数 = 0。** 小婕店现在一个顾客档案都没有(D207-b 那位顾客属 `lucky-luxe`)。

### 2. `technicians`

| 店 | 几个 | 名字 |
|---|---|---|
| lucky-luxe | 3 | Mia Chen · Ava Lin · Lina Zhou ← **店主说都是假的** |
| jics-nail | **4** | **小婕 · 鹤辰 · 苏苏 · 翠花** ← **店主口述的真名(11c 核过),一个字不动** |

### 3. `services` / 价目(「真的那部分」的对照)

| 店 | services | service_prices | service_categories | pricing_rules |
|---|---|---|---|---|
| lucky-luxe | 7 | 7 | 3 | 0 |
| **jics-nail** | **27** | **83** | 6 | 4 |

**小婕那 27 项 + 83 条价目是真的。** 这一格是「不许碰」的清单。

### 4. 账本 1160 笔的分布 —— **绝大多数不在这两家店**

| 租户 | 笔数 |
|---|---|
| `demo-ai` | **702** |
| `demo-basic` | **412** |
| `lucky-luxe` | **30** |
| `demo-jienail` | 10 |
| `demo-lucky-luxe` | 6 |
| `jics-nail` | **0** |

🔴 **有没有真钱**:`payments` 逐档现扫 ——
`lucky-luxe` 138 笔全是 `provider=MOCK`(PAID 123 笔 / REQUIRES_PAYMENT 15 笔);
**全库 `provider <> 'MOCK'` 的支付 = 0 笔。**
⇒ **这套系统从来没有收到过一分真钱**(微信支付还没接,11j 已量)。

### 5. 储值 / 积分 / 券

| | lucky-luxe | jics-nail |
|---|---|---|
| `stored_value_transactions` | 7 笔 | 0 |
| **储值余额非 0 的人** | 🔴 **5 人,合计 274,400 分(¥2,744)** | 0 |
| `points_transactions` | 见下 | 0 |
| `coupons` / `coupon_grants` / `member_timecards` | **0 / 0 / 0** | **0 / 0 / 0** |

🔴 **储值那 5 个人合计 ¥2,744 是这两家店里唯一「还挂着余额」的东西。**
钱是 MOCK 充的(没真收),但**余额是真记着的** —— 清 `users` 之前必须先决定这 5 笔怎么处理。
`Lucky Member` 一个人就占 2 笔 / 33,200 分。

### 6. 不在 `RESET_TABLES` 里的表 → 见 §二

---

## 四、🔴 为什么今晚不能清(补令 §一 的现读复核)

我读了 `apps/api/demo-reset.mjs`,证实补令说的两道锁:

```js
const PROTECTED_REAL_TENANTS = ['lucky-luxe', 'jics-nail']
```

**这两家永远改不成演示店、永远不能重置** —— 店主 2026-08-25 亲自裁的,防的就是「真店被当演示店清掉」。

**锁没错,变的是店主对数据的认定。**
🔴 **所以我没绕**:没改黑名单、没临时改 kind、没手写 30 张表的 DELETE(J-115)。

另外现读证实:`RESET_TABLES` **30 张里不含** `users` / `user_identities` / `technicians`
(演示店重置保留人,只清经营数据),而店主要的是**连假顾客和假技师一起清** —— 那是范围扩展。

---

## 五、给明天那扇门的四个提示(只是提示,不是设计)

1. **范围要逐表白名单**,不能复用 `RESET_TABLES` —— 它少 26 张、多出的那几张又不该动。
2. **B 类 13 张必须硬排除**,尤其 `jics-nail` 的 27 项 + 83 条价目。
3. **储值那 5 人 ¥2,744 要先有处置口径**(清掉?冲销?留着?)—— 它是账,不是数据。
4. **`ai_response_feedback` 97 + `ai_learning_examples` 68 是店主自己批过的话术**,
   挂在这家店上但不是「假数据」。**清之前请她单独看一眼。**
