# 🔴 (乙) 落地了,机制两面验过 —— **但真数是 4 套,不是 3,而上限是 3「只许降不许涨」**

> 2026-09-20 · 生产仍 `285b20d`,**一个字节没动**

---

## 一 · 三条绑定,逐条落地在哪

| # | 绑定 | 落在哪 | 现测输出 |
|---|---|---|---|
| ① | 未跑**被计数、被逐名点出** | `run-all-tests.sh` 收尾 | `⏳ 未跑 3 套(拿不到夹具):correction-reason tenant-ownership tier-label`<br>`✅ 全部 128 个套件通过 …· 其中未跑 3 套` |
| ② | **上限棘轮 = 3,只许降** | `run-all-tests.sh` · `NOT_RUN_CAP=${REGRESSION_NOT_RUN_CAP:-3}`;超了 `exit 1` 并点名 | 造病现测:未跑 3 ≤ 3 → 过;断言基线那把刀自己也有一条 `③b 未跑豁免有上限:3 套 ≤ 3` |
| ③ | **本机全量这 3 套仍真跑真绿** | —— | 🟢 **现测:未跑 0 套 · 128 套全真跑 · 断言基线 4146 → 4146(+0)· 在场 128 + 未跑 0 = 128 闭合** |

**机制两面刀都过**:沙箱夹具造不出来 → **退出码 77 + `[未跑]` 一行**;夹具在 → **真跑,退出码 0**。

---

## 二 · 🔴 但 CI 往下走,第 4 套出来了

```
not ok 9 - ⑤ 🔴 一条查询整批认出来:沙箱里造景写的行,`WHERE demo_seed IS NOT NULL` 必须捞得到
            :: {"marked":{"bookings":0,"settlements":0},"tags":[]}
❌ test-demo-mark 1/10 项未过
```

**同一类:沙箱是空的,造不出阳性。**

### 🔴 而我上一批报「3 套」那个数是错的

我那把尺子认的是 `fetch 4310` / `new DatabaseSync(sandbox-data)` / `${SANDBOX}`,
**认不出 `ensureSandbox()`** —— 而那正是全仓 8 个套件共用的那个前置出口。

**按正确的信号(用不用 `ensureSandbox`)重数:主档 124 套里 **4 套**:**
`correction-reason` · `tenant-ownership` · `tier-label` · **`demo-mark`**

**⇒ 尺子缺一层,上一批那个「3」作废(J-56)。真数是 4。**

---

## 三 · 🔴 所以我停在这里,不自己动

**09x §一 ②:上限 3,只许降不许涨,涨了当场红。**
**真数是 4 ⇒ 要么涨上限(你明令禁止),要么第 4 套另想办法。我不自己选。**

**三条路,给你裁:**

| | 做法 | 我的看法 |
|---|---|---|
| **(a)** | 上限 3 → 4,并按 J-85 记让路账 | **最省事,但它正是 J-98 警告的那一步** ——「每一次多一套都有正当理由」。**我不建议** |
| **(b)** | `demo-mark ⑤` 那一条**改成在沙箱里自己造景**(它本来就是「造景写行再捞回来」,只是依赖了别人先铺的数据) | 覆盖面一点不掉,上限守住 3。**我倾向这条**,但要动判据内部逻辑,**不是我该自己动的** |
| **(c)** | 直接上 **(丙)**:让 CI 起一台**有数据**的沙箱 | 一次解决全部 4 套,上限从 3 降到 0。**最正确**,也最重 |

🔴 **无论哪一条,我都要你一个字。** 今晚已经推了 6 次,每次都把根因往前推了一层
(管道 → `ls -t` → 沙箱夹具 → 第 4 套),**但「涨上限」这一步我不迈。**

---

## 四 · 今晚 CI 走过的六层(每一层都真的过去了)

| 推 | 提交 | 时长 | 退出码 | 死在哪 | 谁治的 |
|---|---|---|---|---|---|
| 1 | `a4d5700` | 26s | **2** | 管道判据 | 09v |
| 2 | `8773752` | 31s | 1 | 预检 `ls -t` | 09w |
| 3 | `131100b` | 130s | 1 | 进套件了,看不见 | —— |
| 4 | `4e43bbb` | ~2m | 1 | 注解生效但**取错了那一段** | 我自己改 |
| 5 | `95a8939` | ~2m | 1 | 🔴 拿到原因:`tenant-ownership ③c` | 09x 裁 (乙) |
| 6 | `3eadc2f` | ~3m | 1 | 🔴 **第 4 套 `demo-mark ⑤`** | **等裁** |

**六次,四个不同的根因,一次比一次深。**

---

## 五 · 我自己该记的两笔

1. 🔴 **我违了公约④「动手前先搜复用」**:全仓早有 `apps/api/test-need-sandbox.mjs`(8 个套件在用,而且它**会自己把沙箱起起来**),我又新写了一个 `sandbox-required.mjs`,还**把它排在前面**,结果**抢在那个能工作的机制之前把套件杀掉了** —— 覆盖面不是被环境拿走的,是被我拿走的。**已整个撤回并删掉。**
2. 🔴 **检查放错了位置**:第一版把「夹具造不出来」的判断放在 ③c 里,而 ③a 先一步就按红退出了。**「放在用它之前」这句话,我写判据时讲过很多次,自己写的时候放晚了一步。**

**两笔都是同一个毛病的两面:先动手,后看全。**
