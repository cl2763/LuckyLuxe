# 夜班 Cowork 核记(2026-09-09 夜)

> 规矩:每次醒来,**先自己查库查图查仓**,再看回执。回执说的和我查的不一致,以我查的为准并写进这里。

## 核记 #1 · 04:23–04:40 CST

**核了什么**

| 事 | 我怎么查的 | 结果 |
|---|---|---|
| 提交 | `git log` | 到 `5d07e6f`(夜 6 收尾),段 1–9 九个提交都在 |
| 两台服务 | `curl /health` | 4310 → `apps/api/sandbox-data/lucky-luxe.sqlite`,4128 → `apps/api/local-data/lucky-luxe.sqlite`;**窗 4 封顶 12** 两台都对 |
| J-27 回执引的文件 | `ls` | `tools/mp-layout-proof.mjs` · `tools/db-backup.mjs` · `05t回执_2026-09-09.md` · 18 张 `06截图` **全在** |
| 棘轮 | `wc -l` | 17,708 / 7,928,**未升**(与回执一致) |
| D177 | `grep ROTATE_MS|rotateAt|dots(` | `apps/web/dashboard-home.js` **0 处** —— 网页轮播删干净了 |
| D166 探针 | `grep d166-probe` | **0 处**,撤干净 |
| D170 | 直接读沙箱库 | 三店 `PENDING_STAFF` 各 2、夹具名 0 —— **真清了** |
| D172 / D171 | 开了两张实图 | 深色走完;叠字消失 |

**查出来回执里没有的三条**

1. **D184** 旗舰店「新增持卡 **604**」——同库同日用 `newCardCount()` 原样 SQL 查是 **1**;北京店图上 2、库里也 2(对得上)。→ 屏幕与库对不上,只有旗舰店这一路。
2. **D185** 4128 本机库还脏:`lucky-luxe` PENDING_STAFF **68** / 夹具名 **26**,`jics-nail` 夹具名 **12**。段 1 没清它是**对的**(我的停线拦着),但店主一开 4128 就会又看见——**两步走**,先出只读清单。
3. **D186** 台面并排分栏后**右边那列被裁**(`段3_J36_luvia-bj_dark.png`:棠棠 5 张并排,知夏那列露半张)。叠字治好了,看不全是新病。

**裁了什么**

- **#21**(小程序截图):**不准直接认输**。先给 `mp.screenshot()` 包 20 秒超时(纪律),再试第三条路 `screencapture -l <windowid>` 拍开发者工具窗口;三条都不成才准写「交不出来」,且要列清楚试过哪三条。布局实测可以当判据,**不许拿 DOM 冒充图**。
- **#20** 已自解,同意留档;「`flex` 子项的 `width` 会被当成可压缩的建议值」写进口径。

**记我一笔**

上一轮我说「北京店新增持卡 33」——**错了**,实图是 2,库里也是 2。没核实的数字不能当发现报出去,这条标准对我同样适用。

**发出去的指令**

`小批05v补一_…_2026-09-09.md`(md5 `afa2d2e9`,桌面交接夹 + 仓 `handoff/` 两处一致)。
顺序:D184 → #21 三条路 → D186 → D185 只读清单 → 段 10/11。

**下一次**:05:40 CST 左右醒,核 D184 修没修对(要看「两个数打印」)、#21 第三条路试没试、D186 有没有图。11:30 CST 后不再排,改写早报。

---

## 核记 #2 · 08:00–08:25 CST

**先查后说**:`git log` 仍停在 `5d07e6f`(比 #1 时一个新提交都没有);近 200 分钟只有我自己写的文件和两个 `-wal` 在动;`claude auth status` 仍 `loggedIn:false`。→ **Code 还停着,店主还没醒、还没粘那一句**。

**这一轮我自己干了两件(都只读,没等 Code)**

**① D185 的只读清单我自己出了** —— `handoff/night-runs/D185_4128待清单_2026-09-09.md`(md5 `c7557226`,两处一致),全程 `?mode=ro`,**0 处写库**。结果比预想更干净利落:

- 积压报价 69 条,日期全部落在 **2026-07-14 → 2026-08-08**,**今天(≥09-08)0 条** —— 没有一条是店里真有人在等的;
- 夹具名顾客 lucky-luxe **26 位** / jics-nail **12 位**,逐位带了挂单数/结算数/储值流水/次卡,最多的一位 11 单,没有一位挂着钱。

**由此补一条裁(§一 名单要再切一刀)**:`demo-cust-01..08` 那八位叫「林小雅(演示)」「Emma Liu(演示)」——**这些不许改名**。它们本来就是老老实实标着「(演示)」的演示顾客,看着不刺眼,改了反而假装成真人。要改的只有**名字里带随机 id 段或「测试/闸测/演示2-」的那些**(`运营字段测试-mrm0lewr`、`储值测试-mrm0leqb`、`测试-统一口径-0X`、`演示2-0X纯新客`、`临时新客测试`…)。**这条同样适用于沙箱库那次已经做过的改名** —— 回头核一下段 1 有没有把「(演示)」那八位也改掉了。

**② D184 我又追了一层,现在可以断定「不是数据漂移,是算错了」**

我把图上其它几个数也拿库对了一遍:现金业绩图上 5,614 / 库里现在 5,760,总卡耗图上 378 / 库里现在 238 —— **这两个数确实漂了**(夜里种子还在往库里加行),所以「屏幕和库不一样」本身不足以定案。但**新增持卡这条不一样**:

> 它的口径是「当期首次开卡的顾客,**按顾客去重**」。而 `lucky-luxe` 这家店在库里**总共只有 75 位顾客**。**604 比这家店的顾客总数还多 8 倍** —— 去重后的人数不可能超过人数本身。

所以 D184 **不是漂移、不是我查错时点**,是这条数从 SQL 到屏幕这一路上算错了。这句话请 Code 直接当判据的抬头写进去:**「新增持卡」任何时候都不许大于该店顾客总数**,这是一条不用查历史也能立刻咬住它的死判据(比 12 组全等更早生效)。

**这一轮没写指令给 Code**(它停着,写了也没人读),两条补裁写进 `小批05v补二`,连同 D185 清单一起,等店主粘那一句时一并生效。

**下一次**:09:50 CST 左右;仍无新提交就再记一次,11:30 CST 后不再排、改写早报补记。

---

## 核记 #3 · 09:55–10:20 CST

**先查后说**:`git log` 仍停在 `5d07e6f`;近 100 分钟动的全是我自己写的文件 + 两个 `-wal`;终端仍未登录。→ Code 还停着。

**这一轮又替它做完两件(全只读)**

**① 段 1 改名误伤 —— 我预判的那条,查实了,而且拿到了还原对照表**

沙箱库里那八位现在叫:`demo-cust-01 李婉宁` · `02 孙予安` · `03 何一诺` · `04 沈嘉言` · `05 许若曦` · `06 柳南舟` · `07 范知雅` · `08 苗昭`。
本机库(4128,没动过)里它们还是原名:`01 林小雅(演示)` · `02 王梦琪(演示)` · `03 Sophie Chen(演示)` · `04 张玲(演示)` · `05 Emma Liu(演示)` · `06 陈思思(演示)` · `07 李慧(演示)` · `08 赵敏(演示)`。

→ **段 1 那 28 位改名,把这八位一起改了**,正是补二 §二 说的误伤。**还原源现成**:4128 那份就是原名对照表(它一个字没动,这次反倒帮上忙了)。改回去只动 `display_name`、只动这八个 id。

**② D173 我读了 v5 原文,不再凭记忆 —— 并且把病缩到了一个很具体的地方**

原文逐字(`05q_像人六通_原文_v5_2026-09-08.md` 第 89 / 93 / 95 / 97 行):
- 89 `AI`:好的,**明天(9月9日,周三)** 可以。大概几点方便?
- 93 `AI`:**9月9日(周一)** 门店休息哦,换一天好吗?
- 95 `顾客`:好的,就这个时间
- 97 `AI`:想约哪天呢?

现查:`date-human.mjs` 本身**是对的** —— 我直接跑了它:`weekdayOf('2026-09-09')` = 周三,`humanDate('2026-09-09','2026-09-08')` = 「明天(9月9日,周三)」。第 89 行就是它出的。
所以第 93 行那个「(周一)」**不可能**是这只函数出的 —— 「周一」恰恰是这三家店的**每周休息日**(抬头第 13–15 行)。两种可能:① 模型自己写的这句(它照着模板的样子,把 KB 里「周一休息」当成了这一天的星期);② 另有一条路自己拼了星期。
**关键的现查**:`humanizeDates` 全仓只在 `booking-intake.mjs:623/624` 用,**只替换 `YYYY-MM-DD` 形状**;`reply-hygiene.mjs` 里**一个字都没管星期几**。所以只要那句里没有 ISO 日期,出口卫生就管不着它 —— **这个洞是真的,和是谁写的那句无关**。

→ 裁出一条**两种可能都咬得住**的判据(写进补三):**出去的每一句里,凡出现「M月D日(周X)」,那个「周X」必须等于 `weekdayOf` 算出来的星期;不等就红并把两个星期都打印出来。**

第二半(95→97 吞掉确认句):现查 `booking-intake.mjs:489–492` 的注释写着「店休就说店休,**并把日期清掉回 collecting 重问**」—— 日期被清了,顾客那句「就这个时间」就没东西可挂,于是退回第一问。**这是设计的直接后果,不是偶发**。

**下一次**:11:25 CST(11:30 前最后一次);仍无新提交就收尾写早报补记,不再排。

---

## 核记 #4(收尾)· 11:25 CST

**现查**:`git log` 仍停在 `5d07e6f`;近 70 分钟只有我自己写的五个文件在动。→ **一夜之后 Code 一直停着,店主还没醒。**

**收尾做了三件**

1. 早报里粘给 Code 的那一句,**改成指向补一 / 补二 / 补三三份**,并把段序改成与补三 §四**逐字一致**(原来那句还留着旧顺序,粘下去会和指令打架 —— 这种「两处说法不一样」正是我要求 Code 现场报出来的那类,自己也不许留)。
2. 交接夹里五份齐了:`小批05v补一/补二/补三` · `D185_4128待清单` · `早报`,和仓里逐份 md5 一致。
3. **诉求总账 artifact 这一轮不更新** —— 理由写在这里备查:自第 46 次更新以来 **Code 一个提交都没有**,新增的全是我自己的裁定,而这些已经完整落在早报、台账、总表、三份指令里;为了几条我方裁定去重写整页,是给店主制造「看起来有进展」的假象。**等 Code 真跑起来、有东西可核了再更新。**

**这一轮起不再定时醒**(令里写的 11:30 界限)。店主一说话就接着干:她粘完那一句我立刻转回每小时核。

**留给下一次的第一件事**:Code 一有新提交,先核 **D184 的死判据**(「新增持卡不许大于该店顾客总数」)——这条最便宜、一跑就见分晓;再核**段 1 改名有没有把那八位改回来**。
