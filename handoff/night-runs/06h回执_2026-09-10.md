# 06h 回执 —— D188 四条落地 · 钉浅色上闸 · 库域口径改对 · J-43 棘轮对账 · 2.76 治掉

> 2026-09-10 · 提交(**写完最后一个代码提交才定的稿**,§七⑦):
> `abfd96b`(一)· `a08e499`(二)· `65e4a11`(三)· `91a1bb0`(四)· `e9496ef`(五)· `9288763`(六)· `3e55d80`(七)
> **本回执是第八个** —— 它自己的号写不进它自己,所以这里只到第七个为止,后面不会再有代码提交。
> **未推 main · 未碰生产库 · 4128 一个字没写** · 密码 / 令牌一个字没进回执

---

## 〇、先接你两笔

**你那一笔(`NODE_ENV=production`)**:那不只是你的环境 —— 它是**我们代码里的一个真洞**,你只是第一个踩到的人。
你没把它当红报出去,而是查到底,结果查出的东西比那条红值钱得多(§二)。

**我这边同时补一句**:06g 回执抬头写「九个」却只列到第八个 —— 我在写回执之后又补了一个提交。
**这次改了做法**:回执**排在所有代码提交之后**定稿,抬头逐个列号,并写明「本回执是最后一个」。

---

## 一、§七 逐条交

### ① §一 钉浅色上闸:现测几处 · 判据点名到哪一行 · 造病红了没

- **现测 2 处**,就是那两页:`apps/web/platform.html:2` · `apps/web/sign.html:2`。
- **判据四条**(`test-color-usage` ⑧–⑧d,27 → 31 项):

| 条 | 守什么 |
|---|---|
| ⑧ | 只许**名单里那两页**钉;名单外的当场红并**点名 file:line** |
| ⑧b | **条数棘轮 ≤ 2**(只许降) |
| ⑧c | **反向守**:名单里那两页确实还钉着(钉子被悄悄拆了,判据也不许一直绿) |
| ⑧d | 每颗钉子旁边写清**为什么钉 + 谁来拆**,拆的去处必须写明 **D188**,不许只写「暂时」 |

  只认 `<html>` 标签上的钉子 —— CSS 里的 `:root:not([data-theme="light"])` 是**选择器**不是钉子,注释里提到更不是。

- **造病 · 刀 KK**:给 `index.html` 也钉一颗 →

```
not ok 27 - ⑧ 钉浅色只许 2 处:现测 3 处 :: 名单外的:apps/web/index.html:2
not ok 28 - ⑧b 钉浅色条数棘轮 ≤ 2 :: 现测 3 处:apps/web/index.html:2 || platform.html:2 || sign.html:2
```
  还原后回绿。

- **两页注释重写**成「**这是一颗钉子,不是设计**」+ 为什么钉 / 谁来拆(D188 整页双档化)/ 不许再钉第三页。
- **D188 欠账清单**已落进 `handoff/P0-P2开检反馈总表.md`:两颗钉子 + 登录态夹具补扫 + 小程序 `card-pack`。
  (你那句「注释里的欠账不算欠账,清单里的才算」照做了。)

### ② §二 `scopeOf`:造病那条**先红后绿的原文**

**改之前**(现跑,原文照抄):

```
scopeOf(CI 临时库, {NODE_ENV:production}) = production   ← 应该是 ci
scopeOf(本机库,   {RAILWAY_ENVIRONMENT:x}) = production   ← 应该是 local
legacyScope(CI 临时库,{NODE_ENV:production}) = live       ← 应该是 test
```

**改之后**(路径判据排到环境变量前面):

```
✅ CI 临时库 + NODE_ENV=production = ci
✅ 本机库 + RAILWAY_ENVIRONMENT = local
✅ 沙箱库 + NODE_ENV=production = sandbox
✅ 未知路径 + RAILWAY_ENVIRONMENT = production   ← 真生产在 Railway 上路径本来就是 unknown,这一支保留
✅ legacyScope(CI + NODE_ENV=production) = test
```

**反向守(证明没朝松的方向改)**:

```
✅ 生产库路径 + 不设任何环境变量 → live(**不是 test**)
✅ 本机库 + LL_TEST_DATA=1 → live(一个环境变量顶不动全部保护,这条原样没动)
```

**判据那边三处照你说的改**:
- `㋙C③` 的话是「不看环境变量」而三次调用喂的都是 `{}` —— 新增 **`㋙C③a`**:真喂 `NODE_ENV=production`,断言三个路径的结论一个都不变;
- `㋙C③c` 原来把「环境变量说了算」写成预期 —— 拆成 **`㋙C③c`**(路径分得出三个库)+ **`㋙C③c2`**(环境变量**只在未知路径**上说了算)+ **`㋙C③c3`**(本机库/CI 路径 + `RAILWAY_ENVIRONMENT` **仍是 local/ci**)。

**造病 · 刀 JJ**(把环境变量挪回路径前面):

```
❌ ㋙C③a 🔴 把这句话真量一遍:喂 NODE_ENV=production,三个路径的结论一个都不许变
   :: 现测 本机=live 沙箱=live CI=live
ok 750 - ㋙C③（老话那条,喂 {}）      ← **照旧绿**
```
  `CI=live` 正是「护栏拒跑、整轮回归起不来」的那一下;而**说话那条照旧绿** ——
  同一把刀分得出「说话的」和「量事的」不是同一条。还原后回绿。

### ③ §三 棘轮逐个重量的对照表(J-43)

新刀 `tools/ratchet-audit.mjs`:`git blame` 取「**这个数**写于哪次提交」,`git log` 取「**产出它的判据文件**最后改于哪次」——判据比数字新 = 这个数是旧版判据量的。表落盘 `handoff/night-runs/J43棘轮对账_2026-09-10.md`。

> **有变大的写在最上面** —— **一个都没有。** 下面这一栏「重量后」全部等于原值,只有一个往下收了(见 ⑤d 那条)。

| 棘轮 | 值 | 数字写于 | 判据现在 | 对账 | **重量后** |
|---|---|---|---|---|---|
| `local-server.mjs` 行数 | 17,707 | — | — | 同一版 | **17,707**(未升) |
| `admin.js` 行数 | 7,928 | — | — | 同一版 | **7,928**(未升) |
| `styles.css` 写死色 | 31 | `69e35f6` | `b507f5f` | 🔴 判据比数字新 | **31**(不变) |
| 小程序 wxss 写死色 | 2,356 | `b507f5f` | `b507f5f` | ✅ 同一版 | **2,356**(不变) |
| UTC 判天 | 12 | `9016048` | `b507f5f` | 🔴 判据比数字新 | **12**(不变) |
| 三个旧金(J-40 值扫) | 184 | `6a29a30` | `a08e499` | 🔴 判据比数字新 | **183**(↓ 见下) |
| 金底「没有字的色块」丙筐 | 16 | `6a29a30` | `a08e499` | 🔴 判据比数字新 | **16**(不变) |

**「三个旧金」为什么真的降了 1** —— 重量时又咬出一个判据缺陷:
**⑤d 扫的是原文,没剥注释**,而同一个文件里的 ⑤(扫名字)早就剥了。
**一条看代码、一条看注释**,数当然对不上:我在 `customer-tags.js` 写了一句「原来是 #b5885d」的说明,棘轮就多了 1。
剥完重量 → **184 → 183**,棘轮跟着往下收。**这正是 J-43 要防的那件事,而且是同一个批次里又发生了一次。**

**对账工具的一处诚实说明**:它是**文件级**粒度 —— 动了同一个文件的别处,它也会把这个文件里的棘轮标脏(过报)。
所以预检里我把它挂成**提醒,不拦**:拦了会变成天天红的噪音,而提醒必须在,否则 J-43 只活在回执里。

### ④ §四 那一处 2.76:改后比值 + 用的哪一对令牌

| | 前景 / 背景 | 浅档 | 深档 |
|---|---|---|---|
| 改前 | 奶白 `--heroink` 压**写死的第三把金** `#b5885d` | **2.76** | **2.76**(两档一样 = 它没有深色档) |
| 改后 | **`--tagm` 压 `--tagmbg`**(合同图**已有的成对令牌**,没新造色) | **5.36** | **7.8** |

渲染处支持每档自带字色(`tr.ink`),**另外三档一个字没动**。
**J-42 ①筐 4 → 3**(高价值A 出列了);`GOLD_CAP` 也因此少一处。
B/N/S(5.34 / 4.63 / 5.00)比值够用但同样没有深色档 → 归 **D188「品类色与渐变色双档化」**,由你一次拿两张图找店主拍板。

### ⑤ §五 图标两套图的判据形状(本批不做,先写清怎么判)

按你加的两条:

1. **色值从令牌现读**:`icons-svg-to-png.mjs` 生成时从 `miniprogram/styles/tokens.wxss` 抽 `--brand` 的浅/深两个值,
   **生成脚本里零色值字面量** —— 判据形状照 `test-color-usage` ⑦ 那条(该文件里出现调色板色值字面量即红)。
2. **生成物取样像素**:两套 PNG 都在;对**深色那套**按 `mp-tabbar-pixel` 的办法取样线条像素,
   **取到的 RGB = 令牌里那个值**(容差 ±2/通道,PNG 抗锯齿会有一点点偏)。
   再加一条**反向守**:浅色那套取样**不等于**深色那个值 —— 否则「出了两套」可能是同一张复制了两遍。
3. **覆盖面自证**:21 个图标名单从目录现读,少一个即红(不许写死名单)。

排在 D188 之后,不插队。那 21 处仍留在 `GOLD_CAP` 里当欠账。

### ⑥ 造病验红 —— §一 §二 各一次,另加 D188 一次

| 刀 | 造的病 | 该红的那层 | 结果 | 分层 / 归因 |
|---|---|---|---|---|
| **KK** | 给第三页(`index.html`)也钉浅色 | ⑧ / ⑧b | ✅ 双红,点名 `apps/web/index.html:2` | 其余判据不动 |
| **JJ** | 把环境变量挪回路径判据前面 | `㋙C③a` | ✅ 红,打印「现测 本机=live 沙箱=live CI=live」 | **老话那条 `㋙C③` 照旧绿** ⇒ 分得出说话的和量事的 |
| **LL** | 删掉 `index.html` 的令牌 `<link>` | D188 ①(静态) | ✅ 红,点名 `没引的:index.html` | 见下,这一把最值得看 |

**刀 LL 两层的对照,是这一批最硬的一处证据**:

```
删掉那一行之后 ——
  ① 静态判据:  ❌ 红,点名 index.html          （缺陷被报出来了）
  ② 运行判据:  ✅ --paper=#faf8f3 · body 底不透明 · 令牌样式表 1 张
               （统一入口在发出去的那一刻补上了,**塌页到不了浏览器**）
```

而**在重启服务之前**跑同一把刀,运行判据是这样:

```
  [取] 顾客端首页 · light —— --paper=(空) · body底=rgba(0,0,0,0) · 令牌样式表 0 张
  🔴 令牌取空 paper/ink/brand/hero · body 底色是透明的(整页塌掉那个样子)
```

—— **那就是 05z 那次塌页的原样**。两段放一起才说明白:统一入口不是摆设,而**它要生效必须重启服务**
(《突变自检条》②那一步:造病之后要重启,不是热改了事 —— 这一批我差点又栽在这上面,第一次跑的就是没重启的那一版)。

### ⑦ 回执抬头 —— 已按你说的,**写完最后一个代码提交才定稿**(见文首)。

### ⑧ 没照字面做的地方(一处)

**D188 ② 那半条「且顺序在前」我没照字面立。**
**原因**:现查仓里两种顺序都有 —— `admin.html` **特意**把令牌排在 `styles.css` **之后**
(它页首写着「令牌是唯一真相,谁在后面谁说了算」),另外三页排在前面。**照字面立会把旗舰页判红。**
顺序只在「两个文件定义了**同名**令牌」时才决定胜负,所以我改守更硬的那一条:
**两份文件零同名定义**(现测:令牌 30 个 / `styles.css` 6 个,**零重叠**)——名字不撞车,顺序怎么排都对;
结果那一层另有运行判据兜底。要是你认为仍该按字面统一顺序,说一声我把四页排成一致的。

---

## 二、D188 四条 —— 逐条交(§六 排的第一件)

| 条 | 做了什么 | 证据 |
|---|---|---|
| **① 统一入口** | 新模块 `apps/api/web-head.mjs`(`ensureTokenLink`),挂在 `static-serve.mjs` 发 HTML 那一行。六个页面入口全覆盖,新加一页也不用记得 | 刀 LL:删掉页面里那一行,发出去的字节里被补上 |
| **② 静态判据** | 新套件 `apps/api/test-token-entry.mjs`(**7 条**,已进 `DEFAULT_SUITES`):①每页自带那一行 ②**行为判据**(造一个漏引的页面真走 `serveFile`)②b/②c 反向守 ③零同名定义 ③b 反向守 ④覆盖面自证 | 刀 LL 红并点名 `index.html` |
| **③ 运行判据** | `tools/token-runtime-probe.mjs`:六页 × 两档现取 `--paper/--ink/--brand/--hero` 不许为空,且 body 实际底色不许透明 | **12/12 全过**,连跑三次都绿(L5);报告带界面指纹 |
| **④ 全仓 html 再扫** | `apps/web` 下 **6 个 html 全部**引了令牌(其中引 `styles.css` 的 4 个);服务端也确认只有这 6 个入口 | `test-token-entry` ④ 现扫 6 个 |

**为什么挂在 `static-serve.mjs` 而不是 `local-server.mjs`**:后者是巨型文件,棘轮**只许降不许升** ——
挂过去要加 import 那一行就会把 17,707 顶成 17,708。现在 `local-server.mjs` **一行没动**。

**这个入口平时应该是个空转**:六页都自带那一行,判据 ① 守着 ——
**显式那一行是真相,入口只是「漏了也到不了浏览器」的兜底,不是第二套真相。**

**运行判据怎么保证不被忘**:预检加了一条**新鲜度**检查(照对比度红榜那把的做法)——
报告抬头记的八个界面文件内容指纹 ≠ 现在的 = 没重跑 → **红**。

---

## 三、六项结构 + 数

**② 在哪一层验的**:判据层(`color-usage` 31 · `token-entry` 7 · `noshow` 那 6 条)+ 运行层(12 组)+ 像素层(两档全站重扫)+ 渲染层(截图自己看过)。

**③ 同类扫尽**:钉浅色全仓现扫 2 处 · 令牌引用全仓 6 个 html 逐个 · 三个旧金 183 处逐处进棘轮 · 金底规则三筐穷尽。

**④ 用什么数据验的(同尺)**:刀 `contrast-sweep @ b283463`(**一个字没动**)。

| 面 | 甲档 | 乙档(去重) |
|---|---|---|
| 后台 13 页 × 两档 | **0** | 4 → **2** |
| 顾客端网页 4 视图 × 两档 | **0** | **0** |

后台剩的 2 条是**同一个成因**:`service-catalog.js` 行内样式里的写死灰 `#8c8279`(不是金,归 D188)。

**⑤ 重复稳定性**:全量回归 **110/110 全在场全过**(新套件进册,109 → 110)· 断言 **3,829 → 3,843(+14)**,
增量 = noshow +3 · color-usage +4 · token-entry +7,**对得上**;运行判据连跑三次都绿。

**⑥ 残留与不确定** ——

**写库自报(三库分开)**:

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **未碰** | 本批零次 `railway` 调用 |
| **本机库** | **未动** | 开批/收工两份快照:92 张表 11,366 行,**0 张表行数变动**;租户指纹**一致** |
| **沙箱库** | **有动,全是心跳** | `notification_logs` 304 → 308 · `reminder_tasks` 895 → 903;`heartbeat-shape` **9 条全过**并逐行打印 |

**本批重启过一次沙箱(4310)**:统一入口挂在发 HTML 那条路上,**不重启服务它不生效** ——
第一次跑刀 LL 就栽在这上面(拿到的是没重启的那一版)。重启用的是
`bash apps/api/start-sandbox.sh <记录里的 DATA_DIR>`,起完 `/health` 200,库路径与重启前一致。

**不确定 / 没做到的**:

- **L4 登录态夹具仍然没造** —— 会员档案 / 我的订单 / 钱包 / 结算页 / 购物车有货、小程序 `card-pack`,
  红榜里它们仍是「**没扫**」不是绿。你把它并进 D188 那一批,我这一批**没做到** ——
  D188 的四条结构性做完了,夹具这一件没排上,照实写。
- **渐变面 717 个**仍判不了(棘轮压着,没长)。
- **B/N/S 三个品类色**没有深色档(待裁 #33 里那三条),按你的裁定归配色批。

---

## 四、自评 L1–L5

| | 做到没有 |
|---|---|
| L1 末端验证 | ✅ 截图看了(高价值A 标现在是浅底深金字,和金卡标一致);运行判据是**从浏览器里现取**,比截图还末端 |
| L2 同类扫尽 | ✅ 四类各给机械证据;并且**主动把两处判据自身的盲区补了**(⑤d 没剥注释 / 对账工具) |
| L3 无图不结案 | ✅ 配色有合同图;规矩来自图的用法,已写进判据 |
| L4 反例数据 | 🔴 **没做到** —— 登录态夹具没造,那几页仍是「没扫」。这是本批唯一一件排了却没做的 |
| L5 重复稳定 | ✅ 三刀四步全过 · 运行判据连跑三次全绿 · 回归 110/110 · 无残留进程与备份 |

**停线自查**:未推 main ✅(本地领先 origin/main 233 个提交)· 未碰生产 ✅ · 4128 既有行未写 ✅ ·
未看密钥文件 ✅ · 无 sleep 轮询 ✅ · **没有松判据、没有抬棘轮、没有换尺子** ✅
(棘轮唯一动的是 184 → **183**,往下收;丙筐 16 与其余各数重量后不变)· **没有改合同图来凑代码** ✅。
