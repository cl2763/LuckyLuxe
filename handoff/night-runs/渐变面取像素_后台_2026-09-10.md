# 渐变面上的字 —— 取像素判(computed 判不了的那一批)

> 跑于 2026-09-11T05:50:13.177Z · 跑在 `http://127.0.0.1:4310` · 端:admin
> 🔴 **产出这份数的刀:`tools/gradient-pixel-sweep.mjs` @ 17f0aa3**(J-39)
> **被测代码:`apps/web @ fac7721`** · **界面文件内容指纹 `22473994c8c7`**

> **判法**:前景色取 computed(那是确定值);背景**把字设成透明后整页截图**,
> 在页面里用 canvas 解码,对每个字的矩形取**最不利的那一个像素**(比值最小的那个)。
> **取最差不取平均** —— 渐变面上「平均够亮」没有意义,人眼看见的是最糊的那一小段。这是保守判法。

**量到 220 个 · 甲档 0 条(去重 0)· 乙档 0 条(去重 0) · 被裁掉没画出来的 3 个 · 没验成 2 条**

| 页 | 档 | 选择器 | 文字 | 前景 | 最差背景像素 | 比值 | 门槛 | 档次 |
|---|---|---|---|---|---|---|---|---|

## 被祖先裁掉、压根没画出来的(**不参与判定**,也不是「没验成」)

> 布局位置照给,像素上什么都没有 —— 典型是门店设置那台手机预览里被裁在框外的几行。
> 判法:问那一点上最上面的元素是不是它(elementFromPoint)。J-37「在不在 ≠ 看得见」同族。

- 门店设置 · `div.dep-card > div.l`「预约定金(订位费)」
- 门店设置 · `div.dep-card > div.v`「CAD $50.00」
- 门店设置 · `div.dep-card > div.n`「可抵扣本次消费」

## 没验成的(不是绿)

- 门店设置 · light · div.sub-hero > p.sub-eyebrow「当前套餐」:两张图在这个框里一个差异像素都没有(要么隐藏没生效,要么这行字本来就与底同色) —— **没验成**
- 门店设置 · dark · div.sub-hero > p.sub-eyebrow「当前套餐」:两张图在这个框里一个差异像素都没有(要么隐藏没生效,要么这行字本来就与底同色) —— **没验成**