# 夜班令 6 · 总结(2026-09-09 夜)—— 段 1–9 全做完

> **未推 main** · 生产库未动 · 本机库未动(对照表在文末)
> 全量回归:**108 个套件全过**,整跑 **2 分 31 秒**;断言 **3701 → 3752(+51)**
> 巨型文件棘轮:`local-server.mjs` 17,708 → **17,708** · `admin.js` 7,928 → **7,928**(都未升)

## 每段一行

| 段 | 做了什么 | 提交 | 证据 |
|---|---|---|---|
| 1 | **D170 演示面清干净** —— 积压报价请求 287 条置终态、38 位夹具用户改成像真顾客的名字(只动那三家店) | `63371b0` | 下一位卡从「运营字段测试-mrm0lewr」变成「姜屿」;判据 +9,造病点名 |
| 2 | **D171 台面不再叠字** —— 交叠的单按簇分泳道并排,列宽随泳道长 | `4540f9f` | 新刀量包围盒:三店 × 浅深 8 条全过;造病(去掉泳道)2 条红并点名压在一起的是哪两张 |
| 3 | **D172 深色走完 + D183 明暗双模式三档(两端)+ J-36 四条** | `c48aa0e` | 写死色 497→382;对比度实测表(四处 × 三档);真点三档验过存储与即时生效 |
| 4 | **D178 小程序按图重排** —— 五小数一行五列、大数字三段式、顶行不再被切 | `76ec2da` | 开发者工具里量:5 格同一行等宽、大数 3 段、店名整块在屏内 |
| 5 | **D179 图 §六 七行交互落地(两端)** | `ae94d1a` | 点/滑暂停并切指标 · 长按/点小牌跳页 · 点此刻格跳台面;落点表两端同一份(判据 ㉒e 就是那把尺) |
| 6 | **D180 轮播手感** —— 4 秒 + 进度条 + 首次提示 + 暂停变灰 | `ab6f8d9` | 造病(拿掉暂停)2 条红;进度用 animation 不用 transition(重画后 transition 不会自己跑) |
| 7 | **D181 小程序数字用上真 Fraunces**(三条路都试过) | `ab6f8d9` | 同一串「5,760」:装着 97px → 拿掉 99px → 还原 97px,**三次可复现** |
| 8 | **D177 网页去轮播** —— §一 的规矩被搬到了 §三,删干净 | `2ecbe71` | 活服务实测 9 秒指标名不变、dots=0;**同族越界表**逐节过完,越界只有这一处 |
| 9 | **D182 小程序折线改 canvas 按图** —— 渐变 + 描边 + 末点圆环 | `e97b248` | 画布里数金色像素 **2683**;造病去掉渐变 → **1994**,差的就是那层填充 |

## 三件「刀自己错了」的记录(都按「刀落了不红先怀疑刀」处理)

1. **J-36 对比度那把刀**:第一版改 `--ink` 造病,一条没红 —— 那四个取样点用的是 `--heroink/--herosub/--brandd`,压根不读 `--ink`。换掉之后 8 条红。
2. **台面那把刀**:第一版在 DOM 上改 inline style 造病,一条没红 —— 首页每 6 秒重挂一次台面,把改动冲掉了。改成在源码上造病(J-34 的 knife-backup),2 条红。
3. **小程序布局那把刀**:`.dh-big > text` 与 `.dh-dot-i.on` 取不到 —— automator 的 `$$` 不认子代组合器与复合类。同一时刻 `page.data()` 里三段齐、有一颗 `on:true`,**是刀写错了不是页面错了**。

## 顺手咬出来的真问题(都当场修了)

- **待办数字浅色下只有 4.08:1**(金色压在 `--wall` 上)—— 改用法不改令牌值(令牌是从合同图逐字符搬的),现在浅 4.73 / 深 9.66。
- **dots 选中那颗不变宽**:class 明明是 `dh-dot-i on`,宽度还是 10rpx —— flex 子项的 `width` 被当成可压缩的建议值,`flex:none` + `min-width` 修好(复量到「恰好一颗是胶囊」)。
- **币符扫描与店名扫描咬到了我自己的注释**:我在注释里引了店主截图里的原话(带币码与店名)。**判据是对的**,改的是注释 —— 这两把刀本来就是防「字面量出现在代码里」。

## 待裁(#20 起,见 `待裁_2026-09-09.md`)

- **#20** dots 变宽那条 —— **已自解**(段 9 复量通过),留档说明修法。
- **#21** `mp.screenshot()` 在这台机器上必卡死(其它 automator 调用全正常)。小程序端的验收改成**布局实测 + 画布取像素**,截图那一项**如实报交不出来**,不拿 DOM 冒充图。

## 没做的 / 没做满

- **段 10–11(D173/D174/D176 三条口径 · 六通 v6 · 三端截图对照表)**:没做。段 1–9 是今晚的正题,九段做完时间已经用满。
- **小程序截图**:交不出来(待裁 #21),证据是布局实测数字与画布像素数。
- **L5 连续 5 次进出**:交互类(点大数字暂停 / dots / 主题三档)各真点过一次,**没做满 5 次**。
- **jics-store 时区**(05u 已裁准改成 `Asia/Shanghai`)与 **#14 存量清单**:本令没排,顺延。

---

## 附:「本机库未动」的对照表原文(J-23:这句话由刀打印,人不许手写)

**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/06_本机库快照_开批.json
  ✅ 逐表零差异(**表、行、列、索引、触发器、默认值都比过**)—— 「本库未动」这句话有证据支撑
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/06_本机库快照_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ✅ 既有行一行没动(8 个桶逐表逐行比过;只增不改)
```

✅ **本机库:未动** —— 快照逐表零差异 **且** 逐行指纹零旧行消失(两把都绿)
