# 07m 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `a6b1c0a24f32f1e2080a84c7d4e3f28d`(全 32 位)
**读数时刻**:2026-09-14 17:34:02 CST
> 本批提交(**1** 个,从 `e3e428a` 数起)· HEAD `4e65ce4` · **领先 `origin/main` 306 个** ——
> 对账:`e3e428a` 领先 305 + 本批 1 = **306** ✅
> 这些数都由 `bash tools/batch-footer.sh e3e428a` 现出,**一个都不手写**(裁 #65 / #93)

> **每个手写的数后面都带「怎么来的」(J-63)。** 凡标 `现测` 的,命令写在旁边。

---

## §一 · J-62 第二款:判据也不许「验了回执就当验过了」

### 第一遍 · 静态筛(三个数)

| | 数 | 怎么来的 |
|---|---|---|
| 断言「写成功」总条数 | **503** | `node tools/assert-reads-fact.mjs` 现测 |
| **A 类(只读这一次调用的返回体)= 嫌疑名单** | **237** | 同上 |
| B 类(从事实那头读回来了) | **266** | 同上 |
| 另:验「该拒」的(不在本刀范围) | 20 | 同上 |

**这把尺子返工了两次,两次都记在码里** —— 名单不准就是废名单:
1. 第一版只看**断言自己那几行**有没有查库 → **479 条嫌疑,大半是错的**
   (真实写法是 POST → GET → 后面挂十几条断言读那个 GET)。
2. 第二版改成追变量来源,抽样一核**还是错**:
   `const wifis = (await request('/admin/store-wifi', {}, …)).data.wifis` —— GET 不写 `method`,认不出;
   `((await request('/services', …)).data.services || []).some(…)` —— GET **内联在条件里**,不在声明上。
   现在口径是:**带改动型 `method` 才算「写」,不带就是「读」**;条件本身与条件里的变量两头都看。

### 第二遍 · 造病(`tools/j62-knife-bench.mjs`)

台子三道闸,**每一道都是被自己咬出来的**:
① 切**整条语句**(不按行)—— 第一版按行切,把 `db.prepare(...).run(...)` 切成孤儿,
   **服务起不来 → 套件红 → 「红了」变成假阳**,还顺带把 4128/4310 带崩了(已拉回);
② 切完先 `node --check`,不过就判「刀落偏,不算验过」;
③ **记是哪一条红的** —— 套件红了但没有一条「声称成功」的断言红,**那还是仍绿**。

| 口 | 判据套件 | 注掉的那条落库 | 造病结果 |
|---|---|---|---|
| 头像落库·新客 INSERT(裁#94) | `mini-phone` | `INSERT INTO users (… avatar_url)` | ✅ 红,**红的就是 ㋐16** |
| `/auth/wechat/mini-phone` | `mini-phone` | `UPDATE users SET phone…` | ✅ 红,**红的就是 ㋐6** |
| `/bookings/:id/cancel` 释放时段 | `booking-cancel` | `DELETE FROM booking_slots` | ✅ 红,**红的就是 ㋘2**(`取消前 4 格 → 取消后 4 格`) |
| `/bookings/:id/cancel` 状态历史 | `booking-cancel` | `INSERT INTO booking_status_history` | ✅ 红,**红的就是 ㋘4** |
| `/my/stored-value/confirm`(顾客·涉钱) | `noshow-aftersales` | `UPDATE stored_value_transactions SET customer_confirmed_at` | ⚠️ **套件红了但红的是隔壁**(㋁ 幂等);**「㋁ 确认成功」仍绿** |

### 🔴 仍绿的,逐条点名(按 §九 停线:**不许当场改**)

**只有一条,就是 07l 那一条:`test-noshow-aftersales` 的「㋁ 确认成功(200+confirmedAt)」。**

```js
check('㋁ 确认成功(200+confirmedAt)',
  cf1.status === 200 && cf1.data.confirmed === true && Boolean(cf1.data.confirmedAt), …)
```

三个条件全取自返回体。**本批第二次造病,它第二次原样活下来。** 留着等你看名单。

> **另外 236 条 A 类没有逐条造病** —— 未做,原因:每条造病要跑一次它的套件(现测单套 2–12 分钟),
> 237 条跑不完。本批按你定的优先级只验了「涉钱 + 顾客能看见」那批里**有判据**的口。

---

## §二 · 裁 #93 领先 main 账不平 —— **选 ③**

**三选一:③ 两个数不是同一把尺子量的。**(**不是** ①,也**不是** ②。)

- **排除 ②**:`git reflog` 本区间**零 amend / rebase / squash / reset**,没有任何提交丢失。
- **真值**(每个都是 `git rev-list --count origin/main..<sha>` 现测):

| 点 | 领先 | 本批提交 |
|---|---|---|
| `fc0195f`(夜11 开批) | 300 | — |
| `9ebedfe`(夜11 收批) | **303** | 夜11 = **3 个** |
| `e3e428a`(07l 收批) | **305** | 07l = **2 个** |

- **两个错处**:①早报那句「本批 **1** 个提交,领先 302」—— 302 那一刻是对的(在 `128a040` 上读的),
  但同一句里的「1 个提交」是错的(那时已有 2 个,加上早报那次是 3 个);
  ②07l 的「**5** 个提交」是拿**夜11 的开批点** `fc0195f` 打出来的,不是 07l 自己的开批点。
  **302 + 5 ≠ 305,是因为这两个数的起点不是同一个。**

**护栏**(`tools/batch-footer.sh`):
① 基准写进输出(`本批 N 个,从 `xxx` 数起` + 一行对账);
② **跨批账本** `handoff/night-runs/领先账本.tsv` —— 每次现出记一行,下次自动核
「上批领先 + 这中间的提交数 = 本批领先」。

**造病红行**(把账本里 302 改成 300):
```
🔴 **领先账不平**:上批记录 `128a040` 领先 300,其后 3 个提交 → 应为 303,现测 305(差 2)
   三选一说清:①上批那个数本来就错 ②有提交被 amend/squash 掉 ③两个数不是同一把尺子量的(裁 #93)
```
脚本自身退出码 **1**(还原后 0)—— 不是只打了行红字。

---

## §三 · 立 J-63,开批快照现测

`tools/batch-snapshot.mjs`:**快照记下 HEAD 的 sha,对账时回到那个 sha 重新量一遍。**
抄来的数在「回到那一刻重新量」面前必然露馅。

**拿它去量夜11 的开批点**(现测):

| | 我当时写的 | 回到 `fc0195f` 现测 |
|---|---|---|
| 主档套件数 | 116 | **117** 🔴 抄错了 |
| `local-server.mjs` 行数 | 17,707 | **17,707** ✅ 本来就是对的 |

> 🔴 **2026-09-14 更正(店主 07n 裁 #97,J-55:结论被推翻要回去改产出它的文件)**
> 这一段原来写的是「**回到 fc0195f 现测是 117 / 17,708 —— 两个都是抄的**」。
> **那个 17,708 是带着 `+1` bug 的尺子量出来的。** 修掉 `+1` 之后重量,行数真值就是 **17,707**,
> **也就是我原本写的那个数本来是对的。**
> 我在同一段里发现了尺子的问题、改了尺子,**却没有回头重量用那把坏尺子得出的结论** ——
> 这正是 J-56 用在自己身上:**用一把刚被自己发现有问题的尺子量出来的结论,一律作废重量。**
> **更正后的结论:套数是抄错的(116 → 真值 117);行数没抄错。**

**造病红行**(把快照里「主档套件数」改成 118):
```
🔴 主档套件数   快照写 118,回到 e3e428a 现测是 116 —— **这个数是抄来的,不是量的**
🔴 1 个数对不上 —— J-63 第一问:它是量出来的,还是抄来的?     (退出码 1)
```

🔴 **第三问「单位对不对」当场咬了我一口**:我给 `wc -l` 加了 `+1`,量出来比棘轮大 1。
仓里棘轮用的是 `wc -l < file`,**同一件事要用同一把尺子**。已改,案底写进码里。

---

## §四① · 裁 #94 头像落库(本批修)

**落点**:
- 建列:`apps/api/user-columns.mjs` 的 `avatar_url`(走 `try/catch ALTER TABLE ADD COLUMN`,交付纪律⑧)
- 落库:`signInWechatMiniUser` **两条写口**(新客 INSERT / 老客 UPDATE)都带上
- 下发:`serializeUser` 出 `avatarUrl`(**没传过就是空串** → 前端出占位,占位零回落律)

**判据 + 造病红行**:
- ㋐16 **查库** `users.avatar_url` 真的是那一张 → 造病(注掉 INSERT)**红在 ㋐16 上** ✅
- ㋐16b 下发面闭合(位面要对:加字段要回到顾客那一层验)
- ㋐16c 没传过的人下发空串,**不许拿别人那张顶**

> 🔴 第一刀砍错了地方:砍的是 UPDATE,而 ㋐16 用新 openid ⇒ 走 INSERT,所以它照样绿。
> **刀砍在没人走的那条路上,等于没砍。** 已改成砍 INSERT,红了。

---

## §四② · 顾客侧取消预约补判据(不改实现)

`apps/api/test-booking-cancel.mjs`,**12 条**,四件事全部**从库里读回来**:

| | 现在的实际行为(现测,只记录不改) |
|---|---|
| 状态 | `bookings.status='CANCELLED'` + `cancelled_at` |
| **时段释放了没** | `DELETE FROM booking_slots` —— 释放了(㋘0 先证明取消前是占着的,防空转) |
| **定金怎么处理的** | 按 `deposit_config` 算 `cancellation_fee_cents` 写进单行;**不动账本** |
| **技师那边收得到** | `booking_status_history` 一行 CANCELLED + 原因 + `notifyScheduler.onBookingEvent` |

另加:㋘5 越权(别人取消 → 403 且**那张单还在、时段还占着**)· ㋘6 再取消一次 → 拒且两个数一分不动。

🔴 **㋘3b 空转登记**:本轮夹具是**零定金单**,`扣费 <= 已付` 在 0 <= 0 时恒真 ——
**扣费规则本轮没验到**。未做,原因:带定金的取消场景要先造一张付过定金的单,本批没造。

---

## §七 · J-60:32 → **28**(降 4,**不硬凑 5**)

- 转的是 `test-card-refund` ×3 + `test-backend-gate` ×1,共用出口 `bindWechatViaFrontDoor()`
- 转完它们**就该进门关档**了 → `DEMO_GATE_SUITES` 补 `backend-gate`,「该跑」**13 → 15**
  (这正是 J-50 第二款想要的方向:**做对事情让「该跑」变多**)

**没到 5 的两个原因**:
1. 32 处里 **10 处**(现测)是「导入顾客 → 贴 openid」这个形状,**被 D191 堵死** —— 见下;
2. `test-ledger-guards` 整套刻意不走 HTTP(注释写明「直连库建,免得走建店流程的一堆副作用」),转不了。

**9 条冻结逐条理由** —— 未做,原因:本批时间给了 §一 的两遍筛与造病台,**下批补**。
(现测冻结仍是 9 条,一条没涨。)

---

## 🔴 D191(本批最重的发现,**只报不改**,等你裁)

> **平台导入的老顾客,微信扫码登录时认不出来,会重开一份档案。**

现测复现(不是读代码读出来的):
```
① 平台替商家导入一位老顾客(累计消费 ¥5,880)→ 档案 user_mu0w2wey_gcaguu
② 她本人微信扫码登录(同一个手机号)→ 认成了 user_mu0w2wf3_yfk9l4
③ 这个手机号现在有 2 份档案:消费 ¥5,880 留在那份**再也认不出来**的上面
```

病根是两处**各自都对**的代码合起来成病:
`import-customers.mjs:72` 给每位导入顾客建 `provider='phone'` 身份行;
`local-server.mjs:5696` 严格认人第三条排除的是「**名下有任何 `user_identities`**」。

**怎么发现的**:不是读代码,是 **J-60 逼夹具走正门** —— 那条路第一次被真跑,当场就断了。
> J-60 立的时候说的是「夹具不许翻墙」。这次它证明了另一面:**翻墙翻久了,墙那边的门坏了都没人知道。**

三条治法与我的建议:`handoff/D191_导入的老顾客扫码认不出来_2026-09-14.md`。**归属口径归你裁。**

---

## 顺手治的(同一族:J-39 同一把尺子)

1. **「会写库」的判定有三把尺子** —— 生成器 / ①c 用裸 `WRITES` / ④b 用会剥字面量的 `isRealWriter`。
   后果:把 SQL 形态写在**正则里当判据用**的工具被判成「会写库却没接护栏」,
   **假阳会逼人改代码去躲判据**(J-49②)。三处统一到 `isRealWriter`,清单重生成。
2. `j62-knife-bench` 被 J-60 当成「直连库贴」(它存的是**搜索用的针**)→ 具名排除(J-61②)。
3. 造病台的 `nth` 默认值 0 让「显式指名第 0 处」和「没指名」长得一样 → 改成 `null`。
4. ⚠️ **我用了一次 `git checkout --`** 还原一个非造病的改动。J-34 禁的是变异流程里用它,
   这次不是造病,但危险的原因一模一样(它还原到 index/HEAD,不是「我改之前那一刻」)。
   已核没误伤。**记在这里,不辩解。**

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **117 / 117 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **19 套 · 红 8** | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 3** | 同上(会话不在;裁#91 的独立档) |
| 断言(在册) | **3,992** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:3,977 + **12**(`booking-cancel` 新套件)+ **3**(`mini-phone` ㋐16/16b/16c)= **3,992** ✅

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**(§八 第 ⑧ 位,没排到)。

**棘轮**(`wc -l < 文件` 现测):`local-server.mjs` 17,664 → **17,656** · `admin.js` 7,928(不动)
**棘轮归因:真修好了** —— `users` 的后加列摘进 `./user-columns.mjs`。

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
(本批与 07l 同一批次窗口内,对照表同一份;逐表现测:本机库业务表零差异,只有通知心跳表增行;生产库零接触。)

**开批快照对账**:`node tools/batch-snapshot.mjs --check handoff/night-runs/07m_开批快照.json` → **每个数回到那一刻都量得出来** ✅

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §一 另外 236 条 A 类逐条造病 | **未做,原因:每条要跑一次它的套件(单套 2–12 分钟),跑不完。** 本批按你定的优先级只验了涉钱/顾客可见那批 |
| §七 9 条冻结逐条理由 | **未做,原因:时间给了 §一 的两遍筛与造病台。下批补** |
| §八⑦ 段 C 撞车队列 | **未做,原因:排在 §一~§七 之后,没排到** |
| §八⑧ 门关档红 8 + card-refund 三条 | **未做,同上** |
| §八⑨ mp 档自愈脚本 | **未做,同上。按裁#91 的期限,这是 07m 应做项 —— 欠着,下批必须给,否则按你划的线走 ③** |
| §四② 带定金的取消场景 | **未做,原因:夹具没造付过定金的单;㋘3b 已把「没验到」登记成一条断言,不让它看着像验过了** |
| 真机层 | 小程序档**空转 3**,自动化会话不在;像素那一层仍然欠着 |
