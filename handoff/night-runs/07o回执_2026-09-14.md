# 07o 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `18f66a1995846f5b396e7be6bd2cdc82`(全 32 位)
**读数时刻**:2026-09-14 21:27:29 CST
> 本批提交(**1** 个,从 `a236329` 数起)· HEAD `6b1f3c6` · **领先 `origin/main` 310 个** ——
> 对账:`a236329` 领先 309 + 本批 1 = **310** ✅
> 这些数都由 `bash tools/batch-footer.sh a236329` 现出,**一个都不手写**(裁 #65 / #93)

---

## §一 🔴 三个数(**只读**,一行没改)—— **查完了,等你定段 C 的位置**

**SQL 原文**(`new DatabaseSync(path, { readOnly: true })` 打开):

```sql
SELECT COUNT(*) AS n FROM (
  SELECT TRIM(phone) p, COUNT(*) c FROM users
  WHERE tenant_id = ? AND phone IS NOT NULL AND TRIM(phone) <> ''
  GROUP BY TRIM(phone) HAVING c > 1)
```

| 租户 | 同号多条的号码数 | 这家店档案数 |
|---|---|---|
| `lucky-luxe` | **0** | 74 |
| `jics-nail` | **0** | 75 |
| `luvia-bj` | **0** | 20 |

### 🔴 但这三个数有一个**要紧的限定**,先说清楚

**我量的是本机库**(`apps/api/local-data/lucky-luxe.sqlite`,预约 1,482 条)。
**决定上线风险的是生产库** —— 按停线(不碰生产)**我没碰**。
CLAUDE.md 记的生产库是 users 77 / bookings 1,242,与本机库**不是同一份数据**。

> **所以:本机库这三个 0 不能直接当成「上线安全」。**
> 要那个数,得在 `railway ssh` 里跑一次**只读** sqlite 查询(按纪律那算诊断,不算写生产)。
> **要不要跑,你说。** 我不自己开这一步。

### 顺手量到另一件,和段 C 同族,而且数不是 0

严格四条的第二条是「**号完全一致**」。**没有手机号的档案,连这一条都过不去** ——
症状和 D191 一模一样:扫码 → 认不到 → 新建一份空白档案。

| 租户 | 档案总数 | **没有手机号** | 有号没绑微信 | 已绑微信 |
|---|---|---|---|---|
| `lucky-luxe` | 74 | **52** | 20 | 15 |
| `jics-nail` | 75 | **59** | 12 | 31 |
| `luvia-bj` | 20 | **0** | 20 | 0 |

**但本机库这批不是真顾客** —— 逐个看过名字:`Identity Test` / `储值测试-` / `自动化验证-勿动` /
`爽约演示客`……,而且 **`is_migrated=1`(老系统迁来的)的有 0 个**。
**生产库那一格是多少,同样要等你点头才量。**

---

## §二 · 那 8 条里最急的四条,**第一次被夹具走过**

`apps/api/test-customer-paths.mjs`(新,9 条)。**全部按 J-62 第二款:查库,不看返回体。**

| 口 | 从库里读回来的那几行 |
|---|---|
| ㋚1 `POST /payments/mock/confirm` 🔴涉钱 | `payments` **REQUIRES_PAYMENT → PAID** 且拿到 `mock_…` 流水号 · 预约 **→ CONFIRMED** · `booking_status_history` 留痕 **1 行** |
| ㋚1b 幂等 | 付过的单再付 → **拒**,流水号与留痕**一分不动** |
| ㋚2 `POST /payments/stripe/confirm-session` 🔴涉钱 | 预约 **→ CONFIRMED** · 支付 **→ PAID** |
| ㋚3 `GET /my/coupons`(卡包) | 商家发 1 张 → **库里 1 张 `active`** → 顾客端看得到 **1 张**,两边对得上 |
| ㋚4 `GET /my/points-history`(积分) | **库 0 条 → 顾客端 0 条**;本轮是**空态**(她还没消费过),空态也要走通、不许 500 |

**造病红行(三把,全红在「声称成功」那一条上)**:

| 刀 | 红行 |
|---|---|
| 注掉 `UPDATE payments SET status='PAID'` | `✅ ㋚1 :: payments 从 REQUIRES_PAYMENT → **REQUIRES_PAYMENT**` |
| 注掉 `UPDATE bookings SET status='CONFIRMED'` | `✅ ㋚1` |
| 注掉 `INSERT INTO coupon_grants`(商家发券那一处) | `✅ ㋚3 :: 库里 0 张() 顾客端 0 张` |

**造景也现测了**(㋚0/㋚0a):本店**真的开了定金** + 顾客自己下出一张 `PENDING_PAYMENT` ——
不开定金的话顾客下单直接 `CONFIRMED`,**支付那两条根本走不到**,那就不是「守住了」,是「没走到」。

### 剩下 4 条,点名 + 排期

| 路径 | 排期 |
|---|---|
| `/ai/reference-analysis` · `/ai/social-copy` | **下批**:两条都烧 AI 额度,要连「没开 AI 包该拒」一起验 |
| `/add-ons` | **下批**:套餐加项,跟着价目表那一批走 |
| `/store/deposit-policy` | **下批**:定金口径的顾客读口;本批 ㋚0a 已经顺带证明写口生效,读口还欠 |

---

## §三 · 裁 #100:那个 ⬜ **销号了**

判据 ㋙2c:`user_identities` 里**不许出现 `email` / `google` 的行**,现测 **0** 条。
㋙2b2 那一行 ⬜ 已删,理由写进码里:**不是够不着,是不该有。**

**先证刀咬得动(㋙2c0)** —— 这一步的过程值得留:

- ①走 `/auth/email/register` 正门造一行 → **不行**:`demo-gate-coverage ①e/①f` 立刻咬住
  「又多一套在用旧路」,而那张冻结清单**上限 5、只许变短**;
- ②直连真库插一行 → **也不行**:J-60 的「直连库贴」棘轮跟着涨(9 → 10);
- ③**在内存库里**照抄同一张表种一行,拿同一条查询跑:**0 → 1**,证明它数得到。
  **一行都不碰真库,也不顶任何棘轮。**

> **为了证一条判据去顶另一条棘轮,不划算。** 这是本批学到的一条。

---

## §四 · J-58 第四款:装进台子了,但**回放没做成**

款文已写进 `tools/j62-knife-bench.mjs` 抬头,逻辑也落了:
造病不红时,台子**自己补一刀「必然会红」的**(把落库那条换成直接抛);那一刀也不红 ⇒ 判「**刀没咬到**」,不算守住。

🔴 **但 §八.5 要的回放,我没做成 —— 如实报,不算验过。**

④ 那一支**只在套件保持绿时**才说话,所以回放需要一把「落在套件走不到的地方、**且不把套件弄红**」的刀。三次都没找到:

| 试的第几种 | 靶子 | 结果 |
|---|---|---|
| ① | 积分换券那条写 + `customer-paths` | 套件因别的原因红了 |
| ② | 平台导入那条身份写 + `mini-phone` | 套件红(常驻那 4 套里有人走导入) |
| ③ | 同上 + `wechat-stub` | 一样红 |

按停线「同一处连改三次不对就停,写清试了哪三种」——**我停了**,三种都写在码里。
**结论:④ 的代码装上了,但没有观察到它真的触发。** 下批换法:给台子加一个「不跑整套、只跑单条断言」的模式,才好构造保持绿的场景。

---

## 顺手治的三件(都是**判据自己的病**,同族:J-61 数执行不数提及)

1. **`danger-cmd` 把我一句 `echo "…不用 pkill…"` 当成执行了 pkill**,①② 当场红。
   第一版我改成「把字符串整段剥掉再判」—— **更糟**:`pkill -f "local-server.mjs"` 的**模式串就住在引号里**,
   剥完 ② 报「模式串太泛」,把**三条真护栏一起判红**。
   现在改成**按位置**判:只有落在引号**外面**的才算在执行。造病:真加一条 `pkill` → 仍然咬得住。
2. **J-60 把内存库的 `INSERT` 当成「直连库贴」**(㋙2c0 那一行)→ 按**句柄**认出 `:memory:`,
   比整文件排除更准(判据自己的探针不是被测对象,J-61②)。
3. **mp 自愈脚本不用 `pkill`**:白名单上限 3、只许变短;而**按端口收也不够** ——
   现踩:`lsof -ti tcp:9420 | xargs kill` 只杀掉监听的子进程,整只工具还活着,
   `cli auto` 附上去但自动化口起不来,**证活那一步直接红**。改用开发者工具自己的 `cli quit`,走通了。

---

## 两个追问

**1. 门关档 19 → 21 那一套是哪来的?**
`git show` 逐版 diff `DEMO_GATE_SUITES`:07n 新增的是 **`identity-claim`** 一套(19 → 20),
本批(07o)又加了 `customer-paths`(→ 21),现在是 **22**(加 `booking-cancel` 之后)。
**上一份回执我把 07n 那次写成「+2」是错的** —— 07n 只加了 `identity-claim` 一套,+1;
「19 → 21」里的另一个 +1 是 07m 的 `booking-cancel` 延后生效到那一次统计。
**现测口径:本批收尾 22 套。**

**2. §〇 现证的卡包和积分,报实测值。**
按 §二 走通之后补上,两个都是**按行对**的:

| | 实测 |
|---|---|
| 卡包 | 商家发 **1 张** → 库里 `coupon_grants` **1 行,状态 `active`** → 顾客端 `/my/coupons` 出 **1 张** |
| 积分 | 库里 `points_transactions` **0 条** → 顾客端 `/my/points-history` 出 **0 条**(空态,她还没消费过) |

> 🔴 **07n 的漏项在这里补上了**,而且你说得对:**这不是两件事,是一件** ——
> 卡包和积分当时报不出来,正因为那两条路一次都没被夹具走过。

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **119 / 119 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **22 套 · 红 8** | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 0** | 同上(`cli quit` 自愈生效) |
| 断言(在册) | **4,014** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:4,004 + **9**(`customer-paths` 新套件)+ **1**(`identity-claim` 12 → 13)= **4,014** ✅

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**。

**棘轮**:`local-server.mjs` **17,650**(不动)· `admin.js` 7,928(不动)· J-60 欠账 **28**(不动)

**跨批账本自检**:`a236329 领先 309 + 本批 1 = 310` ✅(`handoff/night-runs/领先账本.tsv`,机械对账)

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
本批对本机库**只做了只读查询**(`readOnly: true` 打开),一行没改。

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| 段 C 撞车队列 | **按令没开工** —— §一 那三个数已报,等你定位置 |
| 生产库那三个数 | **未量,原因:停线不碰生产。** 要量得你点头(只读诊断) |
| §四 回放 | **没做成**,三种都试过写在码里;④ 代码在,但**不算验过** |
| §六⑤ 造病台搬离 4128/4310 | **未做,原因:本批时间给了 §一~§四。** 已连欠两批,下批第一件 |
| §六⑥ 造病台查工作区干净 | **未做,同上** |
| §六⑦ 237 条按口聚类 | **未做,同上** |
| §六⑧ J-60 继续降 · 门关档红 8 · 9 条冻结理由 | **未做,没排到** |
| 剩下 4 条没走过的路 | **未做**,已点名排期(见 §二 末) |
