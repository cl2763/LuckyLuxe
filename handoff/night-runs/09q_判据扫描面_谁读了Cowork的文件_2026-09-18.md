# 09q §四① · 哪几把刀会扫到 `handoff/` 里 Cowork 的文件 —— **一次列清,只报不改**

> 2026-09-18 · 立 **J-90**:判据的扫描面里,不许包含另一方的产出。
> **扫描面**:`apps/api/test-*.mjs` **136 把** + `tools/*.mjs` **78 个** = **214 个文件**,现扫字面量 `handoff`。

---

## 〇 先把「提到」与「真读文件」分开(J-61①:数执行不数提及)

字面量 `handoff` 命中 **31 个文件**,但其中 **17 个**是 AI 意图名
(`after_sales_handoff` / `handoffRequired` / `silent-handoff` …)——**跟目录毫无关系**。
**真的去读 `handoff/` 目录或文件的:14 个。** 逐个如下。

---

## 一 · 🔴 只有一把刀**不分作者地扫整棵 `handoff/`**

| 刀 | 怎么扫 | 有意的吗 |
|---|---|---|
| **`test-untouched-proof.mjs:101`** | `git ls-files handoff` —— **整棵树,502 篇,不筛作者** | 🔴 **不是。这就是 09q §四① 要问的那一把** |

**它就是本批撞上的那一把**:09p 令里 Cowork **引用我上一批的话**,被它算成我的一次新声称(34→35)。
09p 我补了一层「被「」引起来的算转述」,**治的是症状**。
**按 J-90,根治是把令与台账排出它的底数** —— 🔴 **改法待你批,本批一行没改。**

**建议的形状(报批)**:扫描面从 `git ls-files handoff` 收窄为
「**Code 的产出**」= `handoff/night-runs/**` + `handoff/回执*` + `handoff/*报告*` …
🔴 **但这是黑/白名单之争**(判据三),而且文件名约定不是机制,**所以我不自己定,报你裁。**

---

## 二 · 读 `handoff/` 但**只读自己产出**的(有意,合规)

| 刀 | 读什么 | 为什么合规 |
|---|---|---|
| `test-delivery-evidence.mjs:76` | 遍历 `handoff/*.md`,但只认 `<!-- 回归自证 批次=… -->` 这种块 | **那种块只有我会写**。令里没有 ⇒ 令对它的底数贡献 0。**筛的是机制不是文件名** ✅ |
| `tools/eval-citation-check.mjs:22` | 文件名筛 `^(回执|夜班总结|AI客服准确率).*\.md$` 取最近 6 篇 | 三种都是我的产出 |
| `test-db-target-guard.mjs:258` | 读一个具名文件 `handoff/写库脚本护栏三列清单.md` | **那份是 `tools/gen-guard-checklist.mjs` 生成的**,我的产出 |
| `tools/gen-guard-checklist.mjs:29` | **写**那一份 | 同上 |
| `test-dashboard-home.mjs:139,148` | 读 `handoff/night-runs/D154截图` · `段11截图` | 我拍的证据 |
| `test-mp-home-owner.mjs:210` | 读 `handoff/night-runs/段9截图` | 同上 |
| `tools/mp-home-shot.mjs:15` · `tools/white-screen-proof.mjs:37` · `tools/batch-snapshot.mjs:69` | **写**进 `handoff/night-runs/…` | 产出侧,不是判据面 |
| `tools/release-checklist.mjs:37,72` | 写 `handoff/上线批清单_最新.md`;读 `handoff/night-runs` 取最近证据 | 我的产出 |
| `tools/seed-selfcheck-data.mjs:294` · `tools/seed-luvia-bj.mjs:384` | 写/提 `handoff/本地自查账号.txt` | 我的产出(且 gitignore) |

---

## 三 · 🟡 读 **Cowork 产出**、但那正是它该读的(有意)

| 刀 | 读什么 | 为什么是对的 |
|---|---|---|
| `tools/design-token-diff.mjs:27` | `handoff/商家端主页重画_两端设计图_2026-09-03.html` —— **Cowork 的合同图** | **合同图就是基准**,「谁都不许改它来迁就代码」。它读的是**权威**,不是把对方的文字算进自己的底数。**与 J-90 不冲突:J-90 禁的是"算进底数",不是"拿来当标准"** |
| `tools/ext-deps-check.mjs:21` | `handoff/外部件清单.md` | 同理,清单是**数据源**,不是被判对象 |

---

## 四 · 结论

- **会把 Cowork 的文字算进自己底数的:1 把**(`test-untouched-proof`),**已被本批撞出并打了补丁,根治待批**;
- **拿 Cowork 产出当基准/数据源的:2 把**,**是有意的,不该改**;
- **其余 11 把读的全是自己的产出。**

🔴 **一句提炼**:**判据读对方的东西,分两种 —— 「当标准」可以,「算进底数」不行。**
J-90 禁的是后一种。这两种在代码里长得很像(都是 `readFileSync('handoff/…')`),
**所以以后新加读 `handoff/` 的刀,要在注释里写明自己属于哪一种。**
