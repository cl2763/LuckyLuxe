# 09a 回执 · 2026-09-16(执行单 #1–#6 交)

**本令 md5 两处一致**:`a1f238fbe30b66dbbd4a1f62d2ff01fb`(桌面夹 `batch09a_tmp.md` + 仓里 `handoff/`,13,933 字节)
**生产**:只读连过一次(执行单 #1 那几条)· **一行没动** · 之后没再连
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## #1 · 🔴 两件补报 —— **两个指纹零差额,三个数都要改**

| 指纹 | 数 |
|---|---|
| ① `users.google_id` 非空 | **7** |
| ② `user_identities` 里 `provider='google'` 覆盖的人 | **7** |
| **逐 id 对** | **7 / 7 全对上** · 只在①里 **0** · 只在②里 **0** |

> **两个指纹指向同一批行,零差额 ⇒ 这一格按 J-66 闭合。**

**顺带一个教训**:宽口径的「有身份行但没 openid」是 **57** 行 —— 多出来的 50 行跟这件事无关
(它们没有 `google_id`,是邮箱注册/导入的轻档案)。**指纹选宽了,7 会变成 57。**

**②租户**:`[{"tenant_id":"lucky-luxe","n":7}]` —— **全在 lucky-luxe,你猜对了**。与 07z 现测对上
(那条路租户取自 body,给不存在的店就回落默认店)。

**③三个数重算**(底数一并出):

| 数 | 一直在说的 | **真值** | 差 |
|---|---|---|---|
| 全库 users | 77 | **70** | −7 |
| `lucky-luxe` users | 37 | **30** | −7 |
| `lucky-luxe` 没手机号 | 28 | **21** | −7 |

**三个数各少 7,是同一批 7 行**(它们全在 lucky-luxe,且全都没手机号)。
**「没手机号」的比例**:旧说法 28/37 = 76% → **真值 21/30 = 70%**。
结论方向没变(七成没号仍是大问题),**但说出去的数得是真的**。

**J-55 更正已落**:我自己产出的 5 份加了更正条(原文不删)。
⚠️ 一开始我用正则批量加,**误加到了店主台账和令文件上 —— 已撤回**。
**令和台账不是我的产出,不许往上面加更正条。**

全文:`night-runs/D194_两件补报_2026-09-16.md`

---

## #2 · 🔴 D196 那三个数:**0 / 0 / 0**

你给的两种可能里,是**第一种**:

> 「如果是 **0** —— 那 759 条**全部在验别的东西**,而『建单成功了没有』这件事**从来没有任何一条判据守过**。」

用裁 #106 那把读写集尺子分三层数(**每层都闭合到底数**):

| 口 | 该读的表 | 断言总数 | 🔴 **自己写 SQL 直接读那张表** | 只是「路过」(打的接口在 handler 里碰到它)| 完全不碰 | 闭合 |
|---|---|---|---|---|---|---|
| `/admin/settlements` 建单 | `settlement_groups` | 677 | **0** | 26 | 651 | ✅ |
| `/settlements/:code/claim` 扫码认领 | `users` | 54 | **0** | 23 | 31 | ✅ |
| `/admin/coupon-grants/custom` 发券 | `coupons` | 71 | **0** | 14 | 57 | ✅ |

🔴 **三个口,三个 0。**

**那 26 / 23 / 14 条「该咬没咬」是「路过」,不是「在验」** —— 它们打的接口在 handler 里碰到了那张表,
所以表级读集算上了它;**但断言本身一个字都没读它**。逐条看过,验的是留存转没收、客户列表过滤、
`demo_identity`、日结、储值抵扣、跨店可见性……**全是别的事**。

> **单位说明(J-63③)**:677 是**源码里 `check(` 调用**的条数,759 是**跑出来的 `ok` 行**数 ——
> 差在循环里一个 `check(` 发多条。**两个数都是对的,只是单位不同**,这里分层用的是源码那个。

---

## #3 · D196 三条判据补上了,**三条都现证会红**(J-37⑤ 五步齐)

| 条 | 住哪 | 它读什么 | 造病现证 |
|---|---|---|---|
| **㋚5b** D196-① | `test-customer-paths` | `settlement_groups` 那一行 + **腿(`settlements.group_id`)指的是不是它** | 🔴 **红 1,点名 ㋚5b,链通** —— 报文直接显示 `组={} 腿={group_id:"sgrp_…"}`,**腿指着一个不存在的组** |
| **S4b** D196-② | `test-scan-sign` | `settlements.code → user_id → users.wechat_open_id` **三步全走库** | 🔴 **红 1,点名 S4b,链通** —— `{"wechat_open_id":null}` |
| **⓪b** D196-③ | `test-coupon-settle` | `coupons.issued_qty` | 🔴 **红 1,点名 ⓪b,链通** —— `{"issued_qty":0}` |

**补之前**:这三把刀分别是 **759 / 53 / 86 条断言零红**。**补之后**:各红 1,点名,因果链通。

**过程里被自己的新判据咬了两次,两次都咬得对**:
1. ①第一版比 `settlement_groups.booking_id` —— 现测那一列是 **null**(预约挂在腿上不挂在组上)。**我把关系想当然了。**
2. ②第一版取 `claim.data.userId` —— **回执里根本没有这个字段**,取到 `undefined`。
   **「回执里没有的东西,不能假设它有」** —— 而这一条判据的题目恰恰就是「别信回执」。

---

## #4 · 停线总表(J-77③)

`handoff/停线总表.md` —— **一处真相**,以后只改这一份。
每条补齐三格:**①停哪一件 ②还能不能改代码 ③还能不能继续这一班**。

- **停整夜级 5 条**(第 2 条「痕迹非 0」那两格空白,**已按你采纳的口径钉死**);
- **停这一件级 7 条**(默认值 J-77①);
- **永久红线 9 条**(这些没有「停到哪一层」的问题 —— 任何时候都不许做)。

---

## #5 · 裁 #103 那把刀:**那个 0 从此带着扫描面一起出现**

```
ok 3 - ㋝1 🔴 网页顾客端「注册/创建账号」类入口 0 处(剥注释后现扫;**这次扫了 8 / 8 个文件**)
```

按 J-56 连坐:**这把刀历史上报过的所有 0 全部作废**。重跑之后有差没有?——
**没有差**:换成 8 文件真实扫描面、长相还是旧的那一版,命中仍是 **0**(夜13 §三 现测)。
**所以这道缺口当时确实没藏东西 —— 但那个 0 在此之前没有依据。**

---

## #6 · 路径表重出:40 → **62**,「8 条没走过」作废重算

| | 旧(分母 40) | **新(分母 62)** |
|---|---|---|
| 路径总数 | 40 | **62** |
| 走过的 | 36 | **58** |
| 🔴 一次都没走过 | **8**(07m 报的,含两条支付)| **4** |

**分母变大而「没走过」反而变小**:新增那 22 条**全是走过的** —— 它们此前不在扫描面上
(`adminRequest` / `api.*` / `pub` 三族包装器认不出来),而夹具一直在走它们。
🔴 **07m 那份「8/40」按 J-56 标存疑** —— 它是在一个只盖 65% 路径的扫描面上得出的。

全表:`night-runs/顾客端路径表_重出62条_2026-09-16.md`

---

## 数(全部现测)

| | 数 | 增量 |
|---|---|---|
| 主档 | **124 / 124 全过** | ±0 |
| 门关档 | **25 套 · 红 1** | ±0(只剩 `noshow-aftersales`)|
| 小程序档 | **3 套 · 红 0 · 空转 0** | ±0 |
| 断言 | **4,085** | 4,082 → 4,085(**+3**:㋚5b / S4b / ⓪b 各 1)|

**断言增量对账(判据五)**:预期 +3 = D196 三条判据各 1;**现测 +3 ✅**

**棘轮**:`local-server.mjs` **17,621(±0)** · `admin.js` **7,928(±0)** · `customer.js` **2,458(±0)**
**J-60 正门欠账**:**23(±0,本批没降)** —— 见下
**账本**:`8cf9acc` 领先 326 + 本批 87 = **413** ✅

---

## 没做完的(逐条写原因)

| 执行单 | 状态 |
|---|---|
| **#7 其余六口去 fail-fast 重跑** | **未做** —— 要改六个套件的 `check()` 从 throw 改记账,动的是判据本身,**想单独一批做** |
| **#8 涉钱口继续**(已做 10)| **未做**;🔴 **但 11 条追不到口的断言归口结果补报**:夜13 现测 **11 → 4**,归出来的 7 条里 **5 条是涉钱口**(`/bookings` · `/admin/membership/members` · `/admin/finance/transactions` ×2 · `/admin/daily-close`);剩 4 条逐条手查,**涉钱 0** |
| **#9 J-60 23 → ≤18** | 🔴 **没降** —— 剩下 23 处里 **14 处在 `noshow-aftersales`**,而那一套我夜13 三改不对已停(进了待裁队列)。**不绕开它去凑数**,等它那一批一起做 |
| **#9 9 条冻结理由 J-70 复核** | **夜13 已做**:9 条冻的是**夹具**不是**判据**,J-70 管的是判据,**0 条属于「红在真病上」** |
| **D195 实现** | 等你读完图点头 |
