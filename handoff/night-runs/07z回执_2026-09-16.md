# 07z 回执 · 2026-09-16

**本令 md5 两处一致**:`ec024fa2e5d34f954e706dd4f9bc3bb8`(桌面夹 + 仓里 `handoff/`,14,700 字节)
**读数时刻**:2026-09-16 00:35:28 CST
**生产库未动 · 本机库未动**(本批**一次都没向生产发过任何请求**,J-72 遵守)
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## 一 · D194 三步 —— **删了,不是加门**

### 第一步:全仓扫调用点 → **产品零调用**

| 调用方 | 处 | 是什么 |
|---|---|---|
| **网页顾客端 `apps/web/customer.js`** | **0** | 只有两处**注释**提到它;那条死按钮打的 `/auth/google/start` 07u 已拆 |
| **小程序(三端)** | **0** | 全仓零命中 |
| 测试/工具 | 4 | `test-identity-links` ×2 · `test-frontend-routes` ×1(拿它当反向锚)· `e2e-stress-test` ×1 |

**产品调用点 = 0 ⇒ 按裁 #107 第二步,直接删。**

### 第二步:删

```
apps/api/local-server.mjs
  − if (req.method === 'POST' && path === '/auth/google/demo') return json(res, 201, { user: registerGoogleDemoUser(await readBody(req)) })
  − function registerGoogleDemoUser(body) { …13 行… }      ← 唯一调用方就是那条路,留着函数 = 留着半条路
```

四处引用同批改完,**其中一处是 J-58③ 的现场**:

> `test-frontend-routes ④` 的**反向锚**原来就是 `/auth/google/demo`(「后端真有的那条」)——
> **删掉它,锚当场消失**。这正是 J-58③「锚不许选在本次改动会抹掉的字面量」。
> 换成 `/auth/wechat/mini-login`:**删它等于产品没有登录**,不会被顺手删掉。

**行数**:`local-server.mjs` **17,631 → 17,621(−10)**,历史最低棘轮同步下调。
注:我第一版写了 23 行案由注释,**当场把棘轮顶破(17,634)** —— 按裁 #89「顶破只有三个正确反应」,
**摘出去**:代码里只留 4 行指路,案由全文放这份回执。

### 第三步:删完补底层判据 → 新套件 `test-user-write-auth`(11 条)

守的是 J-71 那句:**全站不存在任何「无需认证即可写 `users` / `user_identities` 行」的路。**

| 条 | 内容 | 现测 |
|---|---|---|
| ① | 白名单式:无认证 + 写 users **0 条** | 0(白名单 5 条,**每条带可反驳理由**)|
| ②a/②b | 白名单每条必须写理由 · 棘轮 ≤ 5 只减不增 | 过 |
| ③/③b | **覆盖面本身要有判据**:路由条数 ≥ 240 · `/auth/*` ≥ 4 | 240 / 4 |
| ④/④b | 🔴 `POST /auth/google/demo` **不存在** · `registerGoogleDemoUser` **不存在**(裁 #104:守「不许存在」只许断言不存在)| 过 |
| ⑤a | 🔴 **造病**:塞一条无认证写 users 的路 → ① 必须咬中 | **咬中** |
| ⑤b/⑤c | 反向守:先认人的不咬 · 只读 users 不咬 | 过 |
| ⑥ | 第三栏:「写 users 且租户取自 body」逐条在白名单里,新来的自动红 | 3 条,全在册 |

**probe 两面**(J-58⑥):`user-write-census --probe` → **该中 4 · 不该中 3 · 判错 0**。

🔴 **这把刀第一次跑在真代码上,当场露出自己两个覆盖面缺口**(而 probe 当时是绿的):
1. `if (… && (path === 'a' || path === 'b'))` —— **多一个括号就不认**,`/auth/email/register|login` **整条从普查面上消失**;
2. `… ) return json(...)` **一行式没有花括号** —— `braceBody` 往后抓到**下一条路由的身子**,于是 `/auth/wechat/mini-login` 被报成「不写 users」(它明明建人)。

**根因一句话:我种的靶子只有一种长相,判据就只守得住一种长相。** 两处已治,**各补了一条对应长相的靶子**。

---

## 二 · `/auth/*` 三栏普查(现测,240 条路由里的 4 条)

| 方法 | 路径 | 行 | 要不要认证 | 写 users? | **租户从哪来** |
|---|---|---|---|---|---|
| POST | `/auth/email/register` | 10721 | 不要 | users | 请求头 |
| POST | `/auth/email/login` | 10721 | 不要 | users | 请求头 |
| POST | `/auth/wechat/mini-login` | 10730 | 不要 | users | 🔴 body |
| POST | `/auth/wechat/mini-phone` | 10731 | 不要 | — | — |
| ~~POST~~ | ~~`/auth/google/demo`~~ | ~~10761~~ | ~~不要~~ | ~~users~~ | ~~🔴 body~~ | **← 本批删除** |

**「认证」这一栏问的是「调用前要不要先认人」;登录路自己当然不能先认人** ——
所以白名单理由必须回答另一个问题:**没有任何凭据的人,能不能靠它写出一行 `users`?**

| 路 | 为什么不是洞(可反驳) |
|---|---|
| `/auth/email/register\|login` | 演示门关死。四档现测(07y):门关那一档 + 两种生产口径那两档,一律 403,`users` 一行没动。**可反驳:举出一档它 2xx** |
| `/auth/wechat/mini-login` | 它**就是**登录,凭据是**微信签发的 code**;替身 `fetchJsCode2Session` 硬限 ci/sandbox(`wechat-code-stub.mjs:42` 抛错,原话「这一条是硬的,没有开关能打开它」)。**可反驳:举出一条生产可达的路让它吃到替身** |
| `/admin/auth/login` | 凭据 = 用户名 + 密码;它写的是 `admin_accounts.last_login_at`,普查里那个 users 命中来自**跟进层**。**可反驳:指出路由体里直接写 users 的那一行** |
| `/admin/auth/register` | 双闸:演示门 + `OWNER_EMAILS` 白名单。**可反驳:举出一档两闸都过而邮箱不在白名单** |

**全站「无认证 + 写 users」现扫:0 条。**

---

## 三 · §五 两条只读

### (2) 生产跑的是哪个 commit · 那条路在不在 —— **做完了,而且是坏消息**

`git fetch origin main`(只读,**没碰生产**)之后现测:

```
origin/main = 285b20d   2026-08-26 00:53:19 +0800
本地领先 origin/main:357 个提交(现为 360)
git show origin/main:apps/api/local-server.mjs | grep -c "path === '/auth/google/demo'"  →  1
  11335:  if (req.method === 'POST' && path === '/auth/google/demo') return json(res, 201, { user: registerGoogleDemoUser(await readBody(req)) })
```

🔴 **生产部署的那份代码里,这条路存在,而且一模一样地没有门禁。**
那份代码里 `DEMO_LOGIN_ALLOWED` 还是旧定义(`!IS_PRODUCTION && …`,裁 #90 的「或」改法还没上线),
而这条路**根本不看它** —— 新旧都一样。

**边界**:Railway 连 GitHub 自动部署(Wait for CI),所以「生产 = `origin/main`」是**部署方式推出来的**,
**不是我去生产上验的**。要把它变成现测,只能读生产容器里的文件 —— 那要单独批准。

### (1) 痕迹查询 —— **🔴 查询原文在此,等批,我没有跑**

**指纹为什么干净**(先证再查):在 `origin/main` 那份代码里,**写 `users.google_id` 的只有一处** ——
`:6085`,就在 `registerGoogleDemoUser` 里;写 `provider='google'` 身份行的只有 `:6081/:6086`(同一个函数)
与 `:17657` 的回填迁移(而它是**从 `users.google_id` 派生**的)。
⇒ **生产库里 `google_id` 非空的行,只可能出自这条路。**

```sql
-- ① 有没有被打过(这条路唯一会写出的形态)
SELECT COUNT(*) AS n FROM users WHERE google_id IS NOT NULL AND google_id != '';

-- ② 底数:0 是「没有」还是「没扫到」,靠这一行分(J-58①)
SELECT COUNT(*) AS users_total,
       SUM(CASE WHEN google_id IS NOT NULL AND google_id != '' THEN 1 ELSE 0 END) AS has_google,
       SUM(CASE WHEN wechat_open_id IS NOT NULL THEN 1 ELSE 0 END) AS has_wechat
FROM users;

-- ③ 逐行看形态(**刻意不取姓名/完整邮箱/电话** —— 只取判形态要用的那几格)
SELECT id, tenant_id,
       substr(email, 1, 3) || '***' AS email_head,
       CASE WHEN phone IS NULL THEN 1 ELSE 0 END AS no_phone,
       CASE WHEN wechat_open_id IS NULL THEN 1 ELSE 0 END AS no_wechat,
       created_at
FROM users
WHERE google_id IS NOT NULL AND google_id != ''
ORDER BY created_at;

-- ④ 对应的身份行
SELECT provider, COUNT(*) AS n FROM user_identities
WHERE user_id IN (SELECT id FROM users WHERE google_id IS NOT NULL AND google_id != '')
GROUP BY provider;
```

**四条全是 `SELECT`,不建文件、不改 journal 模式、不带 `-readonly` 就停。**
**批了我再跑;在此之前,「生产上有没有被打过」这句话我不许说 —— 不许说有,也不许说没有。**

---

## 四 · §二 `identity-links` —— **不冻结,而且它已经绿了**

你驳回得对,而且结局比「挂缺陷号」更好:**病修好了,灯就自己灭了。**

- **不冻结**:J-70 收下 —— 报警器响是它在工作,不是它坏了。
- 那条路删掉之后,`②b` 从「必须被拒」改成 **「已删:打过去必须不是 2xx」**。
  这两句**不是一回事**:「被拒」意味着路还在、门关着(**缺口形态**);「打不到」意味着路不存在(**保证形态**)。
  案底就是它自己 —— **它的兄弟加了门,它没加**。
- 顺带补了一个洞:删掉 google 那 3 条断言后,`identity-links` **断言从 10 掉到 8,断言零缩水当场红**。
  **正确的补法不是把删掉的 3 条找回来(那条路没了)**,是**把同一个题目改由一条还活着的路来守,而且两档都守** ——
  正门身份那一段从 `else` 里提出来,**两档都跑**。结果 10 → **13**,覆盖面从「一档」变成「两档」。

### 门关档红数,分两栏报(J-70)

| 栏 | 数 | 逐条 |
|---|---|---|
| **判据欠账** | **3** | `noshow-aftersales` · `schedule-v2` · `staff-portal` |
| **挂缺陷号(红因 = 真病)** | **0** | ——(`identity-links` 原属此栏,**本批随 D194 关帐而绿**)|

---

## 五 · §〇 口径与按族分

**口径轻提收下**:统一按「**本批 X → Y**」。本批 **4 → 3**。
(跨批合计另说:07x 起 8 → 7 → 4 → **3**。)

**剩 3 条按族**:**A 族 3 · B 族 0**。
B 族两条(`card-refund` / `identity-links`)已全部关帐;**剩下的清一色是「夹具走了本档明令关掉的那扇门」**,修法统一 = 夹具换正门。

---

## 六 · 数(全部现测)

| | 数 | 增量与来源 |
|---|---|---|
| 主档 | **124 / 124 全过** | 套件 123 → **124**(+`user-write-auth`)|
| 门关档 | **24 套 · 红 3** | 本批 4 → 3 |
| 小程序档 | 3 套 · 红 3 · 空转 0 | 真因仍是 `MP_AUTOMATOR` 没设(仓外模块没装)|
| 断言 | **4,070** | 4,056 → 4,070(**+14**)|

**断言增量对账(判据五)**:预期 +14 = `user-write-auth` **+11** + `identity-links` 10→13 **+3**;**现测 +14 ✅**

**棘轮**(`wc -l` 现测,带基准):
🔴 `local-server.mjs` **17,631 → 17,621(−10)** —— 历史最低同步下调到 17,621
`admin.js` **7,928(±0)** · `customer.js` **2,755(±0,仍压线,本批没动)**
**J-60 正门欠账**:~~34 → 28~~ 🔴 **更正(08a §〇 店主追问查实)**:真值是 **28 → 28,本批一处没降**;
棘轮 32 → **28**(只是把上限收到实测值)。那个 **34 是污染值** —— 我新判据的 probe 靶子里写了字面量 SQL,
被「夹具直连库贴」那把刀数了进来;去掉字面量就回到 28。**四套夹具改走正门的功劳在 07y(32→28),不在 07z。**
具名冻结 10 → 9 同理(9 是真值,10 是同一处污染)。

**跨批账本**:`8cf9acc` 领先 326 + 本批 34 = **360** ✅

---

## 七 · 过程里被自己的判据咬了三次(都已治,一并报)

| # | 咬我的那把刀 | 我干了什么 | 治法 |
|---|---|---|---|
| 1 | **巨型文件棘轮** | 删路时写了 23 行案由注释 → 17,634 **顶破** | 按裁 #89 **摘出去**(代码留 4 行指路,案由进回执)|
| 2 | **`test-fixture-front-door`(夹具直连库贴)** | 新判据的 **probe 靶子里写了字面量 SQL** → 欠账 32 → 34 当场红 | 靶子的 SQL **拼起来**,不出现字面量(J-61②:判据的靶子不是被测对象)|
| 3 | **`test-demo-gate-scope ④`(判「是不是真环境」只许一处)** | 白名单**理由文字**里写了那两个环境变量名 | 理由改写,**判据的说明文字也算数** |

**三次都不是被复核看出来的,是被工具咬出来的。** 这正是 07y 装自守套件那一段的理由。

---

## 八 · 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| 🔴 **§五(1) 痕迹查询** | **原文已贴,等批**。没批之前不跑 —— **在生产上验证一个写入漏洞等于利用它一次**(J-72) |
| `㋚4` 补反例数据 + 五步现证 | **未做** —— 按你 §九「先交 1–5」,本批先交前五件 |
| A 族剩 3 套(`noshow-aftersales` / `schedule-v2` / `staff-portal`) | **未做**,修法同前三套 |
| §九 ⑧⑨(生产 37 行 · 路径总数重量 · 10 张券 · 82 个口 · 9 条冻结理由按 J-70 复核)| **未做** |
| `customer.js` 摘取 | 本批没动它(2,755 ±0)|
