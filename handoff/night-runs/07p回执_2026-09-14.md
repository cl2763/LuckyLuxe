# 07p 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `38201e6336fd6936a6da8db1775a5bc5`(全 32 位)
**读数时刻**:2026-09-14 22:13:03 CST
> 本批提交(**1** 个,从 `501c33b` 数起)· HEAD `a187ec5` · **领先 `origin/main` 312 个** ——
> 对账:`501c33b` 领先 311 + 本批 1 = **312** ✅
> 这些数都由 `bash tools/batch-footer.sh 501c33b` 现出,**一个都不手写**(裁 #65 / #93)

---

## §一 · 生产只读五问 —— **查询原文贴了,一条都没跑,停在这里等批**

全文:`handoff/生产只读五问_查询原文_待批_2026-09-14.md`(桌面夹同一份)

**五条语句全是 `SELECT` 开头**,打算用 **`sqlite3 -readonly`** ——
那个模式下 `INSERT/UPDATE/DELETE/CREATE/ATTACH` **由引擎拒绝并报错**,不是靠我自觉。
**零 `CREATE TEMP` · 零 `ATTACH` · 零 `PRAGMA` 写入 · 零 append。**

三件先说清的:
1. 五条**都在本机库上先跑通了**(语法、列名都对得上),**到生产不需要临场改语句** ——
   临场改语句正是「手滑写成写语句」最容易发生的时刻;
2. 每条都是 `GROUP BY tenant_id` 的聚合,**返回不超过店铺数那么多行**;
3. 🔴 **五问里一个手机号都不取** —— 要的是个数,不是名单(顾客 PII 不出库)。

> **批了我就跑,并按 §一 第二步报:连的哪个库 / 跑了几条 / 每条原文 / 零写语句的证据。**
> 🔴 若 `railway ssh` 里没有 `sqlite3`,**我停下来问**,不自己往生产上装东西。

---

## §五 · 积分造了真数据再验(你说得对,空态不算验过)

**先查清一件要紧的**:积分「挣」出来的那部分**根本不在 `points_transactions` 里** ——
`pointsEarnRows()`(`local-server.mjs:5932`)是从**已签署的结算单**推的(`floor(subtotal_cents / 100)`),
台账那张表只记**兑换与调整**。所以「按行 id 比」要比的是**那张签署单**。

| | 实测 | 怎么来的 |
|---|---|---|
| 造景 | 开单 + **顾客自己签字**(走正门,不直连库贴) | ㋚5a |
| 库里那张单 | `LU-20260914-SY1C` · 状态 **signed** · 小计 **19,800 分** | `SELECT … FROM settlements WHERE code = ?` |
| 顾客端 `/my/points-history` | 有一行 **`id = stl_mu1bdohl_xh4akm`** —— **就是那张签署单的 id** | ㋚5 |
| 那一行的分值 | **198** = `floor(19800 / 100)` = **198** | 同上 |
| 台账行 | 从 0 条 → 仍是 0 条(**这次多出来的是赚分行,不是台账行**) | 现测 |

**空态那一条也留着**(㋚4):她还没消费过时,顾客端出 0 条、不许 500 ——
**但它现在只是「空态也走得通」,不再冒充「积分跟过去了」。**

---

## §四 · J-58 第四款回放 —— **做成了,两支都真触发**

按你给的出路造了玩具套件 `apps/api/test-knife-selfcheck.mjs`:
**㋛1 一条必然绿**(保证造病后套件整体仍绿,④ 才有机会开口)+
**㋛2 一条故意只验回执**(只看 `status === 200`,**是标本,不许顺手修好**)。

```
══ 造病:【回放A·J-58④】刀落在玩具套件真走的路上(支付落库) ══
   ⟳ 不红 —— 按 J-58 第四款先证「刀咬到了没有」:同一处换成必然会红的形态再跑一次
   🔴 仍绿 —— 路径**确实被执行到了**(必然红那一刀红了),所以这条判据验的是回执不是事实

══ 造病:【回放B·J-58④】刀落在玩具套件走不到的路上(积分换券) ══
   ⟳ 不红 —— 按 J-58 第四款先证「刀咬到了没有」:同一处换成必然会红的形态再跑一次
   🔴 **刀没咬到** —— 换成必然会红的形态也没红,说明这条路径压根没被执行到。**不算守住**
```

**两支都看见了。** 07o 那三次失败的案底留在台子的码里(三次都拿真套件回放,三次都把套件弄红)。
玩具套件按 §四 明令**具名冻结**(它是夹具不是产品判据)。

---

## §七④ · 裁 #98:造病台搬离 4128/4310(**连欠两批,这批清了**)

**根子查明了,不是「忘了拉回来」**:台子原来是调 `run-all-tests.sh` 跑套件,
而**那个脚本自己就用 4128 起主服务**(`run-all-tests.sh:310  PORT=4128 node local-server.mjs`)——
于是每造一次病,你那两台就被打死再拉起一次。

**现在**:台子**自己起一台** —— 私有端口 **4191** + 自己的临时库,**全程不碰 4128/4310**。
自带服务的套件(自己 `spawn local-server.mjs` 的)连服务都不用起,直接跑。

**自证(逐秒采样,不是只看前后)**:

```
[裁#98 自证·开跑前] 4128=200 · 4310=200
[裁#98 自证·跑完后] 4128=200 · 4310=200
✅ 全程没碰:两台开跑前是 200、跑完还是 200(台子用的是自己的端口 4191 + 自己的临时库)
造病全程逐次现测:样本 3 次 · 非 200/200 的 **0** 次
```

**顺带一个意外收获**:隔离跑比走 `run-all-tests.sh` **快两个数量级** ——
`customer-paths` 单跑 **1.0 秒**(同一套走回归脚本要 30+ 秒)。
那 30 秒是回归脚本的开销(预检 / 铺种子 / 起 4128 主服务),不是套件本身。
**也正因为快了,§四 那个回放才做得起来。**

---

## §六 · 追问 → **查出一个真 bug**

**链条逐版 diff(机械,不是回忆)**:

```
起点 07m 收 = 19 套
07m 19 → 07n 21   新增:identity-claim identity-claim   ← 🔴 同一个名字两次
07n 21 → 07o 22   新增:customer-paths
```

**那个「+2」不是两套 —— 是 `identity-claim` 被插进去两遍。**
07n 我用脚本往 `DEMO_GATE_SUITES` 里插它,替换**匹配到两个锚点**,插了两次。
后果:**门关档每轮把同一套跑了两次**,而报表上多算一套。

> 🔴 **你问「另一套是哪来的」,我答过两次,两次都错**(第一次「+1」、第二次「两套」)——
> 直到把链条逐版 diff 才看见是重名。**重名不会让任何判据变红,但它让「几套」这个数说谎。**

**处置**:已去重(22 → **21**),并装护栏 **①a-1「门关档名单零重名」**,
造病验红(再插一个重名 → 当场点名 `auth-surface`)。归族 J-48:**量过的个数与去重个数要分开报。**

---

## 顺手治的两件(都是我这批新写的东西被既有判据咬住,**都咬得对**)

1. **①b 冻结理由原来只认「纯静态·不起服务」一类**,而玩具套件是**第二类:判据夹具**。
   我**没有放松它** —— 把两类写明、各自必填措辞,并加了「**『太慢』不算理由**」的硬条件。
   造病:往清单塞一条「这一套跑起来太慢」→ **当场红**。
2. **造病台里写死的 `OWNER_TOKEN`** 被 `credential-scan ④a` 咬住。
   **不加白名单** —— 这里根本不需要固定值,改成**每跑现生成一把**(`randomUUID()`),连常量都不存在。

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **119 / 119 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **21 套 · 红 8**(去重后) | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 0** | 同上(`cli quit` 自愈生效) |
| 断言(在册) | **4,017** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:4,014 + **2**(`customer-paths` 9 → 11,㋚5a/㋚5)+ **1**(`demo-gate-coverage` 15 → 16,①a-1)= **4,017** ✅

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**。

**棘轮**:`local-server.mjs` **17,650**(不动)· `admin.js` 7,928(不动)· J-60 欠账 28(不动)
**冻结清单 4 → 5**:店主 07p §四**明令**把玩具套件具名冻结 —— **有批,不是我自己抬**。

**跨批账本自检**:`501c33b 领先 311 + 本批 1 = 312` ✅(机械对账)

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
**生产库:五问待批,一条语句都没跑。** 本机库:只做过只读查询(`readOnly: true`)。

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §一 跑五问 | **等批** —— 按令停在第一步 |
| §七⑤ 造病台开跑前检查工作区干净(裁 #99) | **未做,原因:本批时间给了 §四/§五/§七④。下批第一件** |
| §七⑥ 237 条按口聚类 | **未做,同上** |
| §七⑦ 门关档红 8 · J-60 继续降 · 9 条冻结理由 | **未做,没排到** |
| §七⑧ 段 C | **按令没开工** —— 位置等 §一 #1 的数出来当天定 |
| 剩下 4 条没走过的路(`/ai/*` ×2 · `/add-ons` · `/store/deposit-policy`) | **未做**,07o 已点名排期 |
| D191 第三种形态(没有手机号)的修法 | **按令没做** —— 等 §一 #2 的数出来再定,你说过「那是数据缺口不是代码缺陷,三条出路都要图,图要等数」 |
