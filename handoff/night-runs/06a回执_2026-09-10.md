# 06a 回执 —— 同尺重量的真数 · 认库不认名 · 乙档最大那坨其实是我们自己的调色板

> **未推 main** · **不碰生产** · 提交 `8757633` → `6ad336b`(J-39 落地 / CS_CODE_REV / 主体三件)
> 全量回归 **108 套全过** · 整跑 **2 分 42 秒** · 断言 **3,799 → 3,802(+3)**
> 巨型文件棘轮:`local-server.mjs` 17,708 → **17,707**(降 1) · `admin.js` **7,928 → 7,928**

## 一、先认错:「237 → 0」是我拿两把尺子比的

您读两份红榜抬头把它抓出来了 —— **您是对的**。237 是 **3:1** 那把尺子印在修前那份文件里的数,
0 是 **2:1** 这把尺子量的。乙档「437 → 489(+52)」同样是跨尺子的假象。

**同尺重量(两份红榜现在都印着同一个刀号 `6ad336b`(干净,无 +dirty),被测版本也写在抬头)**:

| 同一把尺子(2:1) | 修前 `apps/web @ 07d8067` | 修后 `apps/web @ be04c49` | 差 |
|---|---|---|---|
| **甲档「看不见」** | **77 处**(338 条) | **0 处** | **−77** |
| **乙档「AA 欠账」** | **597 处**(3,107 条) | **106 处**(见 §三) | **−491** |
| 渐变面上判不了的 | 724 个 | 723 个 | −1 |

>  现测这两份:**6 条全过 —— 同一把尺子、量的是不同版本的代码,可以并排比**。

修前那一版是用 `git show 07d8067:` 把三份界面文件退回工作区现量的;
`CS_CODE_REV` 把「量的是哪一版」显式写进抬头(不给就从 git 取,**绝不猜**)。

**您要的「乙档 +52 分解」** —— 分两半答,第二半是我**差点写错、自己查出来的**:

**第一半:那个 +52 不存在,是跨尺子造出来的。** 3:1 那把尺子把一批条目算进甲档,
2:1 这把把它们放回乙档 —— 同一批条目换了个抽屉。同尺(`6ad336b`)下两档都是降的:
甲档 **77 → 0**、乙档 **597 → 106**。

**第二半:但「修面时新添的乙档条目」确实有,而且不是 0。**
我本来想写「一条都没新添」,写之前按配色对逐对比了一遍两份红榜 —— **打脸的是我自己**:
修后那 106 条里,**87 条的配色对是修前没有的**。逐类看:

| 新出现的配色对 | 比值 | 条数 | 怎么来的 |
|---|---|---|---|
| 奶白 `rgb(245,239,227)` 压品牌金 `rgb(200,164,126)` | **2.02** | 58 | 我在「字」那一轮把按钮上的 `#fff` 换成了 `var(--heroink)`(奶白)—— **比原来的白字还低一点**(2.32 → 2.02) |
| 绿 `rgb(46,139,87)` 压各种浅底 | 3.66–4.25 | 14 | 本仓 `--success #47735f`(4.9:1)指到了合同图的 `--good #2e8b57`(3.9:1)—— **统一到合同图,代价是这一类掉进乙档** |
| 棕金 / 灰字压新的浅底 | 3.5–4.4 | 15 | 面换成令牌之后,同一批字压的底色变了一档 |

**这三类的共同点:它们现在都落在「合同图令牌值本身」这个问题上**,所以一并写进
`乙档配色对_请店主裁_2026-09-10.md` 那张表,不自己改。
**按钮那 58 条我可以立刻改回白字**(2.32:1,仍不达 AA)——要不要改、还是直接按走法 2 改成深字压金底(约 3.9:1),您一句话。

**J-39 落地**:
- 红榜抬头现在打印 **产出它的那把刀的提交号**(带 `+dirty` 标记)与**被测代码版本**;
- 新刀 `tools/same-ruler.mjs`:比数之前先比刀号 —— 刀号不同 / 带 `+dirty` / 根本没有那一行,**一律红**;
- **造病**:拿旧的那份 3:1 修前红榜去跟修后比 → **当场红**(它连刀号那一行都没有)。这正是我上一批犯的错。

## 二、`dataScope` —— 我上一批那句话说错了,先更正

我在 05z 待裁 #28 写「4310 的 `dataScope` 是 `live`,**套件护栏拦不住它**」。**这句是错的。**
**现测**(只调护栏本身,没跑任何会写库的套件):

```
对 4128 → 🔴 [测试护栏] 拒绝跑:…连的是**非测试库**(dataScope=live)
对 4310 → 🔴 [测试护栏] 拒绝跑:…连的是**非测试库**(dataScope=live)
```

`assertTestTarget` 要 `dataScope === 'test'` 才放行,**`live` 正是拒绝那一档**。
您据此把它提到 P0 —— **前半句(两台都是 live)属实,后半句(护栏失效)不成立**。这一条我该在上一批就查清楚再报。

**但您指出的三件事成立,这一批全收**:

| 事 | 收法 |
|---|---|
| `dataScope` 只有 test/live,**分不出本机库 / 沙箱库 / 生产库** | 判据搬进 `apps/api/data-scope.mjs` 按**库路径**判,细分名 `ci/sandbox/local/production` 随 `/health` 下发 |
| 护栏信的是**服务自报的一个字符串** | 改成**认库不认名**:字符串说 test 还不够,**库路径必须是 `/tmp/ll-ci-data.XXXX`**;拒绝时**打印库的绝对路径** |
| `LL_TEST_DATA=1` 一个环境变量能打开全部保护 | 现在它**顶不动**本机库/沙箱库(只对「不是这两种目录」的临时库有效) |

- **刀 RR**:假服务自称 `dataScope=test`、库却指向本机库 → **护栏拦住并点名绝对路径**;
  对照组(同一假服务、库改成 `ll-ci-data.XXXX`)→ **放行**(证明这道闸不是「谁来都拦」)。
- **预检加一条**:4128 与 4310 的 `dataScopeName` **不许同名** —— 现在 `4128=local · 4310=sandbox`。
- `test-noshow ㋙C③` 原来锚 `local-server.mjs` 的**源码文本**,判据一搬家就红 → 改成**行为判据**(+3 条断言)。

**停线**:您说这条落地前「不许写 4128」不解除 —— **我没解除**,本批一个字没写 4128(对照表在文末)。

## 三、乙档:最大的一坨**不是合同图的问题**,是我们自己的调色板

顺着您让我出的那张表查下去:乙档 489 处里**最大的一坨 348 条**,前景都是 `rgb(137,127,122)` = `#897f7a`。
它不是合同图的令牌,是 **`styles.css` 自己 `:root` 里另起的一组调色板** ——
`--muted #897f7a` 压白底 **3.9:1**,而合同图的 `--sub #6d6f66` 压白底是 **6.0:1**。
**一件事两处真相,低的那个还把 AA 拖下去。**

把能对上语义的一律指到合同图那份(`muted→sub` · `soft→wall` · `black→hero` · `success→good` · `danger→bad`):
**乙档 489 → 106**。品牌金 `--accent / --accent-dark` **故意没动** —— 那是改合同图,等您裁。

- 请裁的表:`handoff/night-runs/乙档配色对_请店主裁_2026-09-10.md`(按**配色对**归并,带例子与三条走法);
- 过程中我自己撞了一次**「字色令牌当面用」**:`--black` 指到 `--ink`,深色档下甲档从 0 涨到 16 ——
  跟 `.nfy-tab.on` 那个病一模一样,改指 `--hero` 后回 0。**同一个坑我一天里踩了两次**,这条写进注释。

## 四、渐变面 723 个:上棘轮了

`CS_CAP_GRAD=724`,抬头照旧打印「本轮判不了 N 个」。覆盖它们要先把「手机屏/元素在图上哪一块」定位做对,
那件事跟整机 PNG 取像素是同一件(05z 没做成的那一半),一起排。

## 五、`tenant_settings` 那一行:**查清了**

是 `apps/api/test-tier-label.mjs` 拿沙箱的 `demo-lucky-luxe` 做行为现测(按 03e 的裁定,账本类现测只打 4310),
跑完**把值还原**,但 `updated_at` 还不回去 → 逐行指纹记成「旧行消失 1 行」。
已让那把刀每跑一次就自报这一句,以后回执直接引,不用再查一遍。
`ai_usage` 属「服务活着就会变」的心跳族(跑 AI 就累加)。

## 六、写库自报(三库分开)

| 库 | 动没动 | 证据 |
|---|---|---|
| **生产库** | **没连过** | 不推 main、没 `railway ssh` |
| **本机库** | **未动**(刀说的) | 心跳三表逐行过形状刀;原文见附一。**本批一个字没写 4128** |
| **沙箱库** | **有动两行**,而且**这次说得出是谁写的** | `ai_usage`(心跳族)· `tenant_settings/membership_config`(`test-tier-label` 夹具,值已还原、`updated_at` 刷新);原文见附二 |
| 回归临时库 | 每跑新建、跑完即删 | `run-all-tests.sh` |

## 七、L1–L5 自评

- **L1**:✅ 五页两档 10 张 + 整机 1 张,**全部重拍并过 J-38**(先有代码提交,后有图)。
- **L2**:✅ 三条棘轮(甲 0 / 乙 106 / 渐变 724)+ 写死色 34 + 小程序 2,731。
- **L3**:✅ 图都在;判不了的 723 个如实标。
- **L4**:✅ 刀 RR(假服务)· same-ruler 造病 · `--black` 那次是**真数据把我自己咬红**。
- **L5**:⬜ **这一批没做 L5**(没有新交互);上一批网页与小程序三档各 5 轮都在。

---

## 附一:本机库对照表原文(J-23:由刀打印)

```
**本机库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/开批快照/本机库_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/本机库_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 76→87(+11) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     notify_scan_marks  行 54→60(+6) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/开批快照/本机库_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/local-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 7 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notify_scan_marks · 只新增 1 行(10→11)
    jics-nail · notify_scan_marks · 只新增 1 行(10→11)
    hoptest-demo2 · notify_scan_marks · 只新增 1 行(10→11)
    luvia-bj · notification_logs · 只新增 11 行(61→72)
    luvia-bj · notify_scan_marks · 只新增 1 行(4→5)
    demo-ai · notify_scan_marks · 只新增 1 行(10→11)
    demo-basic · notify_scan_marks · 只新增 1 行(10→11)

  🔴 既有行被改动/删除 1 处:
    luvia-bj · reminder_tasks · **旧行消失 11 行**(行 80→80,其中新增 11)
```

**心跳形状刀原文**(`node tools/heartbeat-shape.mjs <库绝对路径> handoff/night-runs/开批快照/本机库_开批.fp.json`):

```
ok 1 - ① 倒推前提成立:reminder_tasks 里 PENDING 的 196 行,updated_at 全都还等于 created_at
ok 2 - ② reminder_tasks:本批状态推进的 11 行,**每一行都只动了 status / sent_at / updated_at**(倒推哈希在开批快照里找得到 = 其余列一个字节没变)
ok 3 - ③ reminder_tasks 行只增不减(开批 270 → 现在 270)
ok 4 - ② notification_logs(只许追加):开批那些行**一条不少**地还在(改一个字节或删一行都会少)
ok 5 - ③ notification_logs 行只增不减(开批 76 → 现在 87)
ok 6 - ② notify_scan_marks(只许追加):开批那些行**一条不少**地还在(改一个字节或删一行都会少)
ok 7 - ③ notify_scan_marks 行只增不减(开批 54 → 现在 60)

════ 心跳白名单放行清单(截至 2026-09-09T19:05:33.720Z 共 18 行)════
  lucky-luxe · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  demo-ai · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  demo-basic · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  hoptest-demo2 · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  jics-nail · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  luvia-bj · notify_scan_marks · 本批新增 1 行(只追加,旧行一条没动) · —→追加 · 排定 (这张表没有时刻列) · 发出 (这张表没有时刻列)
  luvia-bj · reminder_tasks · ntask_mtpcebkc_5dpn1h · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebki_elw5vx · PENDING→SENT · 排定 2026-09-09T00:00:00.000Z · 发出 2026-09-09T00:00:59.651Z
  luvia-bj · reminder_tasks · ntask_mtpcebkn_pq1l96 · PENDING→SENT · 排定 2026-09-09T01:30:00.000Z · 发出 2026-09-09T01:30:59.744Z
  luvia-bj · reminder_tasks · ntask_mtpcebkt_vqfo34 · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebky_i0fyjj · PENDING→SENT · 排定 2026-09-09T03:00:00.000Z · 发出 2026-09-09T03:00:59.825Z
  luvia-bj · reminder_tasks · ntask_mtpcebl3_r581c4 · PENDING→SENT · 排定 2026-09-09T04:30:00.000Z · 发出 2026-09-09T04:30:00.066Z
  luvia-bj · reminder_tasks · ntask_mtpcebla_v5mgln · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblf_5dcfjp · PENDING→SENT · 排定 2026-09-09T06:00:00.000Z · 发出 2026-09-09T06:00:30.787Z
  luvia-bj · reminder_tasks · ntask_mtpceblk_2r4r0d · PENDING→SENT · 排定 2026-09-09T07:30:00.000Z · 发出 2026-09-09T07:30:03.683Z
  luvia-bj · reminder_tasks · ntask_mtpceblp_l2q4zk · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  luvia-bj · reminder_tasks · ntask_mtpceblv_nuubst · PENDING→SENT · 排定 2026-09-09T09:00:00.000Z · 发出 2026-09-09T09:00:03.950Z
  luvia-bj · notification_logs · 本批新增 11 行(只追加,旧行一条没动) · —→追加 · 排定 2026-09-09T00:00:59.651Z · 发出 2026-09-09T09:00:03.950Z
  ── 最早 2026-09-09T00:00:59.651Z · 最晚 2026-09-09T09:00:03.950Z(共 7 个不同时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)
  ── notify_scan_marks:(这张表没有时刻列)(这张表按行内容比,不按时刻)

✅ 心跳形状 7 条全过 —— 上面这些按裁 #23(状态推进)与裁 #26(只许追加)两种形状放行
```

✅ **本机库:未动**(快照与逐行指纹**两把都绿**;服务心跳表 `notification_logs notify_scan_marks` 除外 —— 调度器每天每店写一行、到点把 PENDING 推成 SENT,行行都过了形状刀,放行清单见上,与本批代码无关)
```

## 附二:沙箱库对照表原文

```
**沙箱库对照表原文**(`node tools/db-snapshot.mjs <库绝对路径> --diff handoff/night-runs/开批快照/沙箱库_开批.json`):

```
════ 「未动须有证」对照表 ════
  库:/Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite
  快照:handoff/night-runs/开批快照/沙箱库_开批.json
  🔴 2 张表有差异,**不许写「未动」**:
     notification_logs  行 283→286(+3) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
     reminder_tasks  行 869→875(+6) · 列 持平 · 索引 持平 · 触发器 持平 · 默认值 持平
```

**逐行指纹对照原文**(`node tools/tenant-fingerprint.mjs <库绝对路径> --diff handoff/night-runs/开批快照/沙箱库_开批.fp.json`):

```
════ 逐租户指纹对照 ════
  库: /Users/changliu/Documents/Codex/2026-04-29/new-chat/apps/api/sandbox-data/lucky-luxe.sqlite
  新增租户: (无)
  消失租户: (无)

  ＋ 既有桶里**只新增、未改动** 2 处(没有 tenant_id 列的表,新店的行只能落这儿):
    lucky-luxe · notification_logs · 只新增 3 行(183→186)
    lucky-luxe · reminder_tasks · 只新增 6 行(623→629)

  🔴 既有行被改动/删除 2 处:
    lucky-luxe · ai_usage · **旧行消失 1 行**(行 2→2,其中新增 1)
    demo-lucky-luxe · tenant_settings · **旧行消失 1 行**(行 6→6,其中新增 1)
```

🔴 **沙箱库:有动** —— 逐行指纹查出既有行被改/被删(快照那把刀看不见这一类):
```
    lucky-luxe · ai_usage · **旧行消失 1 行**(行 2→2,其中新增 1)
    demo-lucky-luxe · tenant_settings · **旧行消失 1 行**(行 6→6,其中新增 1)
```
```
