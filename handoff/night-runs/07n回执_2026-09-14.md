# 07n 回执 · 2026-09-14

**指令落地(裁 #46)**:两处 md5 一致 —— `01c23d36531923a0a6fa7ea56fed4d71`(全 32 位)
**读数时刻**:2026-09-14 18:12:53 CST
> 本批提交(**1** 个,从 `f388456` 数起)· HEAD `01ca9a9` · **领先 `origin/main` 308 个** ——
> 对账:`f388456` 领先 307 + 本批 1 = **308** ✅
> 这些数都由 `bash tools/batch-footer.sh f388456` 现出,**一个都不手写**(裁 #65 / #93)

> 每个手写的数后面都带「怎么来的」(J-63)。

---

## §〇 🔴 D191 修好了

### 修法落点(**只动第三条**)

| 处 | 改了什么 |
|---|---|
| `apps/api/identity-kinds.mjs`(新) | provider 分类表 + `notBoundByLoginIdentitySql()` |
| `apps/api/local-server.mjs:5696` | 第三条:`NOT EXISTS(任何身份行)` → `NOT EXISTS(非「不算占用」的身份行)` |

**其余三条一个字没碰** —— 判据 ㋙4 现读那段 SQL 把四条逐条点出来守着(停线自守)。

分类表是**白名单式**:仓里出现过的每个 provider 逐个写明理由。
🔴 **认不出来的 provider 一律当成「已被登录身份占用」** —— 两种错法代价不对称:
错拦一次顾客当场会说;**错认一次两个人的档案合成一份,钱混在一起,没有人会发现。**

### 三条判据 + 四把刀的造病红行

| 刀 | 红行 |
|---|---|
| ① 第三条退回「任何身份行」(D191 原样) | `not ok 6 ㋙1 :: 认成了=user_…ud4s3l 原档案=user_…qmzg50 档案数=2`<br>`not ok 7 ㋙1b :: 前={"余额分":55000,"老系统消费分":588000,"储值流水条":1} 后={"余额分":0,"老系统消费分":0,"储值流水条":0}` |
| ② 把 `email` 错分成「不算占用」 | `not ok 10 ㋙2b :: … i.provider NOT IN ('email', 'phone', …)` |
| ②b 分类表里删掉一项 | `not ok 2 ㋙5 :: 没分类的:google` |
| ③ 把「唯一一条」放宽成 `>= 1` | `not ok 1 ㋙4`(停线自守)+ `not ok 10 ㋙3 :: 同号 2 条 认成了=user_…drbqml` |

**刀①那一行就是你说的那场当场的争执**:`前 ¥550 / ¥5,880 → 后 0 / 0`。

### 🔴 用 `lucky-luxe` 真实形态现证 —— **「她的东西都在」**(不是「认到了」)

夹具走的是真路径:平台导入(**试跑 → 拿回 `balanceSumCents` → 确认**),期初余额进 `legacy` 桶。

| | 认领前 | 认领后 | 怎么来的 |
|---|---|---|---|
| 这个号的档案数 | 1 | **1** | 查库 `SELECT id FROM users WHERE phone=?` |
| 期初余额那一行 | 55,000 分 | **同一行 id 还在她名下** | 逐行按 `id` + `amount_cents` 比对 |
| 老系统累计消费 | 588,000 分 | **588,000 分** | `users.legacy_total_spend_cents` |
| 微信绑定 | (空) | `stub-openid-laowang-d191` | `users.wechat_open_id` |
| **顾客端看得见吗** | — | **`/my/stored-value` 读到 55000** | 拿她自己的 token 现调(位面要对) |

> 🔴 **比的是「那几行本身」不是「总额」**:开了演示闸之后登录会铺演示种子,总额会变大 ——
> 「总额相等」会被噪音顶红,而**「原来那几行还是她的」才是真要证的事**。

### 三个「我自己的夹具错了」,都记着

1. 第一版用 `/admin/stored-value/recharge` 给她充值 → **余额 0**,因为 D25 闸挡着「没绑微信的轻档案不许充值」。
   **用错了路子,验的就不是她真实的样子。** 改走导入带期初余额那条真路。
2. ㋙3 撞车局第一版只造出 1 条同号档案(导入口按手机号去重)。改成**两条不同的正门**:导入 + 轻档案。
3. **㋙2b 原来是空转的** —— 刀② 打下去**没红**,一查才知道那个邮箱档案身上根本没存手机号,
   `u.phone = ?` 压根匹配不上,**怎么改都绿**。

### ⬜ 一个够不着的格子,如实登记(不是验过了)

**㋙2(已绑微信的不被认走)不是分类表在守** —— 守它的是另一条 `u.wechat_open_id IS NULL`。
要压住分类表,需要一个「有非微信登录身份 + 手机号 + 没绑微信」的档案,
而**本仓没有任何正门造得出它**(`/auth/email/register` 不收手机号;`PATCH /admin/customers/:id/profile` 只收 tags/notes/birthday)。
所以这一层现在由**函数层**守(㋙2b),行为层那一格**挂 ⬜ 登记**(㋙2b2),不拿空转的绿糊过去。
**补法要你裁**:给顾客编辑口加手机号字段,还是等有新入口。

---

## §一 · 立 J-60 第二款:顾客能走的路 × 有没有夹具走过

全文:`handoff/顾客能走的路_有没有夹具走过_2026-09-14.md`(`node tools/customer-paths-scan.mjs` 现出)

| | 数 | 怎么来的 |
|---|---|---|
| 顾客能走的路(去重) | **40** | 扫顾客端 55 个源文件里真调的路径 |
| 有夹具真走过的 | **32** | 逐条拿路径去 `apps/api/test-*.mjs` 搜真调用(注释行不算) |
| 🔴 **一次都没走过的** | **8** | 同上 |

**🔴 一次都没走过的 8 条 —— 下一个 D191 的候选名单**:

| 顾客能按的入口 | 后端路径 | 我的看法 |
|---|---|---|
| `miniprogram/utils/api.js` | `/payments/mock/confirm` | 🔴 **涉钱** |
| `apps/web/customer.js` | `/payments/stripe/confirm-session` | 🔴 **涉钱** |
| `miniprogram/utils/api.js` | `/my/coupons` | 卡包 —— 顾客直接看 |
| `miniprogram/utils/api.js` | `/my/points-history` | 积分 —— 顾客直接看 |
| `miniprogram/utils/api.js` 等 2 处 | `/ai/reference-analysis` | 烧 AI 额度 |
| `apps/web/customer.js` | `/ai/social-copy` | 同上 |
| `miniprogram/utils/api.js` 等 2 处 | `/add-ons` | 套餐 |
| `miniprogram/utils/api.js` | `/store/deposit-policy` | 定金口径 |

**只出表,一条夹具都没补**(§一 明令)。

---

## §二 · 裁 #97:重量 + 更正 + 立口径

**重量结果**(修掉 `+1` 之后,`git show fc0195f:… | wc -l` 现测):

| | 我当时写的 | 修好尺子后重量 | |
|---|---|---|---|
| 主档套件数 | 116 | **117** | 🔴 抄错了 |
| `local-server.mjs` 行数 | 17,707 | **17,707** | ✅ **本来就是对的** |

**更正后的那句话**(已回写进 `07m回执`,J-55):
> **套数是抄错的(116 → 真值 117);行数没抄错。**
> 原文「两个都是抄的」是**那把带 `+1` bug 的尺子产出的结论** —— 我在同一段里改了尺子,却没回头重量。

**口径装成了护栏,不是只写进回执**:快照里记**尺子自己的指纹**;
对账时指纹不一致 → 打出「**尺子变过了,用旧尺子量出来的数一律作废重量**」并 **退出码 1**。
造病验过(给尺子加一行 → 退出码 1;还原 → 0)。

---

## §七⑦ · 裁 #91 的期限件:mp 档自愈(**交了**)

`tools/mp-automator-up.sh` —— **先证死 → 再拉起 → 再证活**,三步都做:
- 「证死/证活」认的是 **HTTP 426**(它要 WebSocket),**不认「端口有人听」**(J-37③:07e 现踩过僵死时 `lsof` 照样看得见)。
- 已接进 mp 档开跑前;拉不起来就**明说这一档会空转、按红处理**。
- 造病验过:`pkill` 打死会话 → 脚本自己拉回来并证活。

**本轮 mp 档 3 套真跑了,空转 0**(上一轮空转 3)。

---

## 三个追问(机械核,不推断)

**1. 主档 116 → 117,差 1 是什么?**
`git show <sha>:run-all-tests.sh` 逐版数 `DEFAULT_SUITES`:`e3e428a` 112+4=116 → `f388456` 113+4=117。
**07m 新增的只有 `booking-cancel` 一套,移出 0 套。** 我那句「把两套一起挂进回归」说得含糊了 ——
另一套(`card-refund`/`backend-gate`)是**单跑核验**,不是新挂的套件。

**2. 门关档 17 → 19,另一套是哪来的?**
同法逐版 diff `DEMO_GATE_SUITES`:**新增 `backend-gate` + `booking-cancel` 两套**。
`backend-gate` 我说了;**`booking-cancel` 我漏说了** —— 它是 07m 新写的顾客口判据,两档都该跑,所以一并进了名单。

**3. 「领先 main 302」写进总账** —— 收到,你改。真值 **303**。

---

## 数(收尾报一次)

| | 数 | 怎么来的 |
|---|---|---|
| 主档 | **118 / 118 全过** | `run-all-tests.sh` 现出 |
| 门关档 | **21 套 · 红 8** | 同上 |
| 小程序档 | 3 套 · 红 0 · **空转 0** | 同上(自愈脚本生效) |
| 断言(在册) | **4,004** | `assertion-baseline.json` 求和现测 |
| 退出码 | **0** | — |

**断言底数闭合**:3,992 + **12**(`identity-claim` 新套件)= **4,004** ✅

**门关档剩余的红,逐条**:`auth-surface` · `booking-intake` · `card-refund` · `deposit-config` ·
`identity-links` · `noshow-aftersales` · `schedule-v2` · `staff-portal` —— **8 套,本批一套没清**(§七⑨,没排到)。

**棘轮**(`wc -l < 文件` 现测):`local-server.mjs` 17,656 → **17,650** · `admin.js` 7,928(不动)
**棘轮归因:真修好了** —— `resolveUserByUnionId` 摘进 `identity-kinds.mjs`(认人同域)。

**未动对照**:
> 未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md
(同一批次窗口;逐表现测:本机库业务表零差异,只有通知心跳表增行;生产库零接触。)

**跨批账本自检**:`e3e428a 领先 305 + 本批 … = …` 逐次对上,**本批 `f388456` 307 + 1 = 308 ✅**
(`handoff/night-runs/领先账本.tsv`,不是手写)

**开批快照对账**:`node tools/batch-snapshot.mjs --check handoff/night-runs/07n_开批快照.json` → 每个数回到那一刻都量得出来 ✅

---

## 没做完的(逐条写原因)

| 项 | 状态 |
|---|---|
| §四 裁#98 造病台搬离 4128/4310 | **未做,原因:D191 是上线阻断,本批时间给了它 + §一 出表 + §二 重量。下批第一件** |
| §五 裁#99 造病台开跑前查工作区干净 | **未做,同上** |
| §三 237 条按口聚类 | **未做,同上** |
| §七⑧ J-60 继续降(D191 修好那 10 处通了) | **未做** —— 但**路通了**:D191 一修,那 10 处的正门就走得通了,下批会好走 |
| §七⑨ 段 C · 门关档红 8 · 9 条冻结理由 | **未做,没排到** |
| ㋙2b 行为层那一格 | **够不着,已挂 ⬜ 登记**(本仓没有正门造得出「非微信登录身份 + 手机号 + 没绑微信」);补法要你裁 |
