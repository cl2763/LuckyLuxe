# 09n 回执 · 件 A / 件 B 做完 · 件 C 逐项答完 · 🔴 但我拿不出一次绿的全量回归

**上游令点名(J-81)**:09i `fa3fed3e…` · 09j `61f7e1d6…` · 09k `6024e959…` · 09l `2a17c211…` ·
09m `4f9faf0610d1339cd500af45e0b2046a` · **09n `5e7fd59b4c9fd97b97ff06e75af08980`** —— **六份桌面/仓里 md5 全部一致。**
⚠️ **09m/09n 此前也没落到仓里**,本批一并补落(同第 23 笔那一族)。

🔴 **Railway 面板一次没碰** · **`railway variables`/`up`/`redeploy`/`volume` 一次没用**(09n §一补的口径,收下)· **一个 push 都没执行** · **D201 一个字没在生产上跑**

---

# 🔴 先说最要紧的一句:本批做完之后,我没能跑出一次绿的全量回归

**主档在件 A/B 改完之前是绿的(126/126,`ll-09n2`)。** 之后:
- 看门狗 **300 秒自杀**,当时在跑 `tenant-isolation`;
- 按提示放宽到 **900 秒,照样跑不完**;
- 逐套计时对照:`test-user-write-auth` **26s → 41s**,整跑时长明显变长。

**我没定到根因。** 逐个排除过的:触发器逐名比对那段**现证是好的且很快**
(`[migrate] …触发器 9 条原样装回,**逐名比对通过** 9 条`);mp 档 9420 会话**是活的**(426),不是它卡的。

🔴 **按停线「回归红 30 分钟定位不到就停」,我停在这里报你。**
🔴 **我没有抬看门狗** —— 那是判据,不是配置。
**所以现在多一条挡推的理由:我拿不出一次绿的全量回归。**

---

# 一 · 件 A ✅ —— 改完了,而且 L2 扫出比你点名的更要紧的一处

## 你点的那一处

`dropTenantDefaults` 的备份 `copyFileSync` → **改走 `VACUUM INTO`**。

## 🔴 L2 同类扫尽,扫出三处,其中一处比它重得多

| 处 | 是什么 | 改了没有 |
|---|---|---|
| `tenant-default-drop.mjs` | 开机迁移的备份(你点的那处)| ✅ |
| 🔴 **`apps/api/db-backup.mjs:75`** | **按需快照 + 生产每日自动备份的唯一出口** | ✅ |
| `sandbox-mirror-jury.mjs` | 沙箱快照 | ✅ |

> 🔴 **第二处才是重的**:那是**店主每天真正依赖的那张网**。
> 你点的那一处**一年触发一次**,这一处**每天跑**。
> 而 `tools/db-backup.mjs` 的注释里早就写着「**全仓凡备份一次库文件都该调它,不许再 copyFileSync**」——
> **正确出口一直在,真正在跑的那条却是另一条。一件事两处真相,又一案。**

## 两面现证(J-58⑥)

WAL 里压着 5,000 行已提交数据、未 checkpoint 时:

```
源库真实行数        : 5000
🔴 cp 出来的那份    : 打不开:no such table: t   (4096 字节)
✅ VACUUM INTO 那份 : 5000 行  (53248 字节 · 1 张表,已当场打开验过)
```

**cp 出来的不是「少几行」,是连表都没有。**

## 结构上的一处改动,理由写在这

备份的**唯一实现**搬到 **`apps/api/db-backup-core.mjs`**,`tools/db-backup.mjs` 反过来引它。
**为什么不是让生产代码去 import `tools/`**:开机迁移在生产容器里要用它,而
**「`tools/` 在不在那个镜像里」我没核到** —— 那次 `railway ssh` 被对端关了连接,我**没有第二次去撞**。
**不确定的东西不许进开机链。**

**常驻判据** `test-backup-wal`(10 条):行为层两面 · 备份语境里 `copyFileSync` 现扫 0 处(白名单 2 条各有理由)· 两处 `DROP TABLE` 之前都有备份。

---

## 🔴 补三个现测的数 —— 这一格比合成实验更该看

**① 本机库此刻就有 4.1 MB 压在 WAL 里(现测,只读)**

```
本机库 apps/api/local-data/lucky-luxe.sqlite   journal_mode = wal
lucky-luxe.sqlite       14,065,664 字节  (9/16 17:54)
lucky-luxe.sqlite-wal    4,144,752 字节  (9/18 16:34) ← cp 那一刻,这 4.1 MB 一个字节都不进备份
```

🔴 **这句话我 09o 批里自己推翻了,原样留在这里当案底,更正见下框。**

> 🔴 **更正(09o 批,J-67:错的理由要跟错的结论一样改)**
> 上面那句「这 4.1 MB 一个字节不进备份」**是错的,而且是我自己量出来错的**。
> **WAL 文件的大小 ≠ 落在主库之外的数据量** —— WAL 不会自己缩,里面绝大多数帧
> 早就 checkpoint 进主库了。09o 用同一时刻两种拷法逐表对账:
> **cp 那份与 VACUUM 那份,7 张表行数一模一样;再用最敏感的 4 个指标(各表 MAX(created_at) / MAX(rowid))对,4/4 全同 —— 差 0。**
> 正确的说法是:**拷法是错的(会拷到一份「停在上一次 checkpoint」的库),但错到什么程度不可预测** ——
> 可能一行不差,也可能像我那个合成实验一样**连表都没有**。
> **危险的是「静默变旧」,不是「一定坏」。** 详见 09o 回执 §二。

**② 全仓没有任何一处 `wal_checkpoint`(现扫 `apps/api/*.mjs` + `tools/*.mjs`:0 处)**

⇒ 那条 cp 路径**既不 checkpoint、也不 VACUUM** ——**它拷的就是一个落后于真相的文件**。
生产的库同样是 WAL(`db-concurrency.mjs:22` 在**每个可写连接**上无条件设 WAL,而 WAL 是写进文件头、跟着文件走的持久设置)。

> 🔴 **由此要说一句不好听的:生产那一批「每日自动备份」,是用 cp 从一个带 WAL 的库上拷的,中间没有 checkpoint。**
> **它们能不能还原,我没有验过**(验它要动生产库,不在批准范围)。
> **我只说我量到的**:拷法是错的,而错的拷法在本机库上能拷出「连表都没有」的文件。
> **这一条建议单独立案**,与推 main 无关 —— 它是**已经发生了很久的事**。

**③ 件 B 新加的那两次备份,我探了时序(这是我自己新写的代码,不探不算数)**

`backupFirst()` 落在 **`BEGIN` 之后、`DROP TABLE` 之前** —— 备份是从**另一条只读连接**取的。
造了一个同形时序探它(影子表已写进未提交事务,此时备份):

| 模式 | 结果 | 备份里有没有影子表 `users_rebuild` |
|---|---|---|
| `delete` | ✅ 12,288 字节 · users 200 行 | **0 次** |
| **`wal`**(真服务器就是这个)| ✅ 12,288 字节 · users 200 行 | **0 次** |

**两件事同时成立**:①事务开着也备得出来,不会 SQLITE_BUSY 把整批重建带塌;
②备份里**看不到未提交的影子表** ⇒ **它拿到的正是「重建前」那一份**,而那正是这次备份的意义。

---

# 二 · 件 B ✅ —— 顺序、跑完后的建表语句、备份,三样都给

## 问 1 · 顺序

| 序 | 迁移 | 位置 |
|---|---|---|
| **先** | `rebuildTenantScopedUnique(db, { dbPath })` | `local-server.mjs:17035` |
| **后** | `dropTenantDefaults(db2)` | `local-server.mjs:17529` |

**唯一约束重建跑在前面** —— 它看的是**迁移前**的 schema。

## 问 2 · 🔴 逐字说一次(你要求的那句)

> **完整顺序跑完,`users` 没有被 `schema-unique-rebuild` 重建;`dropTenantDefaults` 重建它之后,
> 建表语句里没有出现 UNIQUE。**

**跑完之后 `users` 的建表语句原文**:

```sql
CREATE TABLE "users" (
    id TEXT PRIMARY KEY, display_name TEXT NOT NULL, phone TEXT, email TEXT,
    wechat_open_id TEXT, google_id TEXT
  , tenant_id TEXT NOT NULL, tags_json TEXT NOT NULL DEFAULT '[]', notes TEXT, birthday TEXT,
    is_migrated INTEGER NOT NULL DEFAULT 0, legacy_total_spend_cents INTEGER NOT NULL DEFAULT 0, avatar_url TEXT)
```

**含 UNIQUE:没有。**(`tenant_id` 的 DEFAULT 已摘掉 —— 那正是这次迁移的活。)

**还补了一刀你没要求的**:**第二次开机** —— **92 张表逐表零差异**,一条迁移日志都没有,备份文件仍只有 1 个。
⇒ **那支不备份的 `DROP TABLE` 两次都没触发,而且下次也不会。**

## 问 3 · 备份补上了(无论触发与否)

`rebuildTenantScopedUnique` 现在收 `dbPath`,**两处 `DROP TABLE` 之前各备份一次**(走同一个 `VACUUM INTO` 出口),
备份路径打进启动日志。拿不到 `dbPath` 时**大声警告,不静默跳过**。

> **你那句「这次恰好不触发是运气,不是设计」收下 —— 现在它不靠运气了。**

---

# 三 · 件 C · 那些「加法」逐项点名

## C-1 · `user_identities` 回填 —— **规则与算式**

`backfillIdentities`(`apps/api/user-identity.mjs:20`)是**四条 `INSERT OR IGNORE`**,全部**从 `users` 派生**:

| 条件 | 生成的 provider |
|---|---|
| `email` 非空 | `email` |
| `wechat_open_id` 非空 | `wechat_miniprogram` |
| `google_id` 非空 | `google` |
| `phone` 非空 | `phone` |

**算式**(你要算式不要数):

> **新增行数 = (email 非空的 users 数) + (wechat_open_id 非空) + (google_id 非空) + (phone 非空) − 这四类里已经存在的对应身份行数**

`INSERT OR IGNORE` ⇒ **只补缺,已有的一行不动**。
我手上**已经量到的生产输入**(此前批准的只读查询):`wechat_open_id` 非空 **8** · `google_id` 非空 **7** · users 总数 **77**。
**`email` 非空与 `phone` 非空这两个全库数我没量过** —— 要就再批一次只读,**我不拿估的数填进去**。

## C-2 · 🔴 `platform_accounts` 0 → 1 —— **这一格有件事必须在推之前告诉你**

那一行是:`username = platform-admin` · `display_name = 平台运营` · `status = active` · **`must_change_password = 1`** ·
口令是**随机生成**的 · **幂等**(已存在就不建)。

🔴 **但开机日志会把那个一次性口令**明文**打出来**:

```
[platform] 已建平台账号 platform-admin,一次性密码:<12 位明文>(首登强制改密;请抄进 handoff/本地自查账号.txt)
```

> **Railway 的部署日志是留存的、面板上看得到的。**
> **推上去第一次启动,那串口令就进了部署日志,而且事后删不掉。**
> J-53 的停线是「**密钥不进任何输出**」——**这一处正在往输出里写。**
>
> 🔴 **这件事我没有自己改**(09n 件 C 是「点名给店主看」,不是「顺手修」)。
> **要不要在推之前把这一行改成不打印明文(只打「已建,口令在平台后台首登时设」),归你裁。**
> 它与 **D195(门店初始密码只显示一次)** 是同一族:**一次性口令不许进任何留存的地方。**

## C-3 · `service_prices` 0→6 · `tenant_settings` 6→8 —— **都不覆盖**

| 变化 | 是什么 | 会不会覆盖 |
|---|---|---|
| `service_prices` 0→6 | `INSERT OR IGNORE … SELECT … FROM services`:给每个项目补一行 `tier_key='list'` 的原价行 | **不会**(`OR IGNORE`)。6 = 同构库里 services 正好 6 行 |
| `tenant_settings` 6→8 | 新增的两行现读是 `__system__/demo_retire_backfill_v1` 与 `__system__/tenant_kind_backfill_v1` —— **两个「跑过没有」的迁移标记行** | **不会**。原有 6 行(含 `lucky-luxe/membership_config`、`points_policy` 等店主的设置)**一个字没动** |

## C-4 · 新增 13 张表逐个点名

**12 张空表**:`ai_review_marks` · `cash_notes` · `conversation_messages` · `duty_marks` · `hero_slides` ·
`notification_logs` · `notification_rules` · `notify_scan_marks` · `platform_auth_sessions` ·
`quote_price_changes` · `timecard_refunds` · `wecom_unrouted`
**1 张带 seed**:`platform_accounts = 1` —— 就是 C-2 那一行。

## C-5 · 32 张重建 + 触发器 —— 🔴 **你问对了:原来只数条数,没比名字**

**老实答**:代码返回的是 `{ done, triggers }`,**`triggers` 是条数**。
**条数对不等于名字对** —— 重建那一段里任何一处把触发器改了名、漏装一条又多装一条,**条数照样对得上**。
归族:「数量凑巧对上」那一类假绿。

**已改**:摘之前记下**名字集合**,装回之后**逐名比**;对不上**直接抛** ——
那一段包在 `BEGIN IMMEDIATE` 里,抛出去整批回滚,老库原样保留。
启动日志也跟着改:

```
[migrate] 去掉 tenant_id 列默认值:32 张表(触发器 9 条原样装回,**逐名比对通过** 9 条)
```

**现证**:同构库上跑出来就是上面这一行,**9 条逐名对上**。
**幂等现证**:跑完之后待处置表 **0 张**。

---

# 四 · §四 备份 —— **没执行**

你批了,但 §七 写的是「**三件全绿 → 执行 §四 的备份**」。
🔴 **我拿不出一次绿的全量回归(见开头)**,所以**三件不算全绿,备份我没执行。**
命令与四条批准条件已备好在 `night-runs/推main准备件_夜13_2026-09-16.md`(源路径已由 `/proc/1/fd` 坐实,`VACUUM INTO`,拉到本机,开一次验五个数)。
**你说执行我再跑。**

---

# 五 · D201 痕迹查询 · **原文已贴,一个字没跑**

`night-runs/D201_痕迹查询_原文待批_2026-09-17.md` —— 四条全 `SELECT`,每条带底数或按店分组。

🔴 **其中一件我先说在前面**(J-58⑨,我第 21 笔那一族):
`confirmMockPayment` 那一行写的是 `provider='MOCK'` ——
**而有门的 `/payments/mock/confirm` 写的是同一行、同一个 provider。**
**两条路留下的痕迹一模一样,指纹分不开。**

> **所以这次的答案不可能是「哪几行是被打的」,只能是「有多少行处在这个形态里」。**
> **我先说清楚,免得查出一个数之后被当成「被攻击了这么多次」。**

阳性对照已在本机库跑通(四条 SQL 在真实 schema 上跑得动、数得出来)——**不是一条跑不动的查询。**

---

# 六 · 我自己记的两笔

1. 🔴 **我夜13 加的「自找 MP_AUTOMATOR」带来一个回归**:模块找得到、而 9420 会话是死的那一档,
   从「快速失败」变成了「干等」(三把刀各等 61 秒)。**已改成开跑前先探会话**(活着才跑,不活立刻按红报并说明「这不是刀红了,是会话不在」)。
   **「找得到模块」和「会话活着」是两件事** —— J-37③ 我自己引用过,然后自己踩了。
2. **本批开头我又一次在回执里用了「我的第 17 笔」这种编号** —— 09n §二 已指出编号是 Cowork 的产出。
   本回执里我只报事实,不编号。

---

# 数(现测)

| | 数 |
|---|---|
| 主档 | 🔴 **拿不出绿的整跑**(看门狗 300s/900s 两次都自杀)· 件 A/B 改动前那次是 **126/126 全过** |
| 门关档 | 25 套 · 红 1(卡住前那一段跑出来的)|
| 小程序档 | 会话是活的,但整跑没走到那一档 |
| 断言 | 增量 **+10 实测**(`test-backup-wal` 单跑 `ok` 行数 = 10,不是数我写了几个 `assert`)· 🔴 总数 **4,104 是 4,094+10 算出来的,不是量出来的** —— 整跑跑不完就量不到总数,按判据五「计数即证」,这个总数只算账面 |

**棘轮**:`local-server.mjs` 17,621 → **17,623(+2)** 🔴 —— 两处调用各加了一个参数(`dbPath`),
**按棘轮律「只许降不许升」,这 2 行我要单独说**:它们是件 B-3 要求的「把 dbPath 传进去」,
**不传就没有备份**。要么留这 2 行,要么那次 `DROP TABLE` 继续裸奔。**请裁。**
`admin.js` 7,928(±0) · `customer.js` 2,458(±0)
**账本**:`8cf9acc` 领先 326 + 本批 114 = **440** ✅
**本机服务**:4128 = 200 · 4310 = 200(回归打死过,已按纪律还回去)
