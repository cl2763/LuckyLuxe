# 小批 11k 回执 · 出口普查判据 · J-114 · 两件贴出来不等

**上游令**:`handoff/小批11k_六条等天亮全裁_开店不许跑seed_立J114_2026-09-22.md`
**md5 三端一致**:桌面 `01542caca4ed3bb00eb110a51cf2af4d` · 仓库 `01542caca4ed3bb00eb110a51cf2af4d` · 令里给的同值

> **生产库全程只读**,本批对它发的每一条都是 `SELECT` / `pragma_table_info`。
>
> 🔴 **本机库与沙箱库我不写「未动」—— 对照表说它们动了,如实贴:**
>
> | 库 | 差异 | 谁动的 |
> |---|---|---|
> | 本机库 | 新增 2 张表(`signed_docs` / `signed_doc_pages`)· `notify_scan_marks` 124→134(+10) | 建表=服务按当前代码启动时跑的迁移;那 10 行=**通知扫描器的心跳**,不是我写的 |
> | 沙箱库 | `notification_logs` 559→570(+11)· `notify_scan_marks` 1069→1101(+32)· `reminder_tasks` 1158→1169(+11) | 同上,全是 4310 跑着时调度器自己写的 |
>
> **列 / 索引 / 触发器 / 默认值四项全部持平**,没有一张表结构变了。
> 涉钱的表(`settlements` / `finance_transactions` / `stored_value_transactions`)**一行没动**。
> 基线是 `11i_本机库快照_收批基线_2026-09-22.json` 与 `11i_沙箱库快照_收批基线_2026-09-22.json`。
> **未动对照:handoff/night-runs/11k回执_出口普查+J114+两件贴出来_2026-09-22.md**

---

## §三 六件,做完五件(第 6 件是你读手册,不占我)

| # | 事 | 状态 |
|---|---|---|
| 1 | 出口普查判据(带上限、只许降、多了少了都红) | ✅ **做了,而且它自己第一次跑就抓到四处我漏的** |
| 2 | 「待定」进词表 | ✅ 两端各一行,判据改成**逐个具名冻结**并加反向守 |
| 3 | D207-b 那 8 行只读贴出来 | ✅ 贴了,**没等** |
| 4 | 北京旗舰店第一段:备份 → 彩排 → 语句贴出来 | ✅ 语句贴了,**没等**;彩排已过 |
| 5 | J-114 + 逐个过那些 seed + 文件头写死 + 进回归 | ✅ **14 支逐支过完,10 支盖章,5 条判据进回归** |
| 6 | 手册你读 | — 不挡任何事 |

🔴 **全程一次没停。**

---

## 一、🔴 出口普查判据:它自己第一次跑就抓到四处

11k §二.2 裁 A,并要求用判据买单。**我写完那条判据,拿它扫全仓,当场多出四处 11j 没盖到的:**

| # | 在哪 | 是什么 | 为什么 11j 没看见 |
|---|---|---|---|
| 11 | `apps/web/store-content.js:51` | 商家端门店**地址输入框**,手写 `/tbd/i` | 11j 的 ④e **只扫 `customer.js`** |
| 12 | `apps/web/store-content.js:58` | 商家端门店**电话输入框**,手写 `/tbd/i` | 同上 |
| 13 | `miniprogram/pages/home/index.js:94` | `callStore` 手写 `/待补充\|TBD/i` | 同上 |
| 14 | `miniprogram/pages/merchant/store/index.js:58` | 小程序商家端门店设置**输入框**,`s.address \|\| ''` | 变量名是 `s.` 不是 `store.`,我上一批的正则抓不到 |

🔴 **后两类比「显示假字」更坏:它们是输入框。**
占位值预填进去,**商家一点保存就把 `Address TBD` 存成了真地址** ——
从「系统里有个待填项」变成「商家亲手填了这个值」,再也分不出来。

**四处全收**(都接到词表唯一出口),`admin.html` 加载词表件并按纪律 bump 了版本
(`ADMIN_BUILD` + 两处 `?v=` → `20260922a-placeholder`)。

### 判据长什么样(J-98 形状)

| 断言 | 内容 |
|---|---|
| ⑦a | **扫描面 195 个文件 >= 下限 195** —— 目录漏掉或文件被搬走立刻红 |
| ⑦b | 🔴 **全仓零手写占位规则** —— 词表件之外不许再写第二份 |
| ⑦c | 🔴 **出口计数 31 ≡ 在册 31**,多了少了都红;**上限只许降不许抬** |
| ⑦d | 🔴 **四处输入框**逐个具名钉住:不许预填占位值 |

**在册 31 处 / 11 个文件**:`customer.js` 7 · `store-content.js` 2 · 小程序 `utils/api.js` 4 ·
`utils/i18n.js` 4 · `home.wxml` 4 · `home/index.js` 2 · `checkout.wxml` 2 ·
`store-location.wxml` 2 · `store-location/index.js` 2 · `booking-done.wxml` 1 · `order-detail.wxml` 1。

**落刀**:造「有人加了第 32 个出口没登记」→ 🔴 红并点名 `customer.js:9`(从 7 涨到 9);还原 → 绿。

---

## 二、「待定」进词表 —— 并把判据从「数个数」改成「逐个具名」

两端各加一个词(第八个)。同时把 ①d 从「长度 === 7」改成**逐个对已授权名单**,
并加 ①e **反向守**:11k 点名**没授权**的「暂无 / 无 / 空 / —」不许混进来。

**落刀**:一端加「待定」另一端不加 → 🔴 ①c + ①d 双红;还原 → 绿。

---

## 三、D207-b 那 8 行(贴了,没等)

`handoff/11k_D207b_八行原文_只读_2026-09-22.md`。要点:

- 🔴 **串味就在一行**:单子 `tenant_id='jics-nail'`,挂的顾客 `users.tenant_id='lucky-luxe'`
- 单号 `LL2988027676` —— **`LL` 是半径的前缀**,单号自己带着来路
- 钱是 `provider=MOCK` / `transaction_id=mock_...` —— **那一百块从没真收到过**(微信支付还没接)
- 🟢 反向守:结算单 **0 张**、账本 **0 笔**、`jics-nail` 账本总数 **0 笔**
- 🔴 **`jics-nail` 全部单数 = 1** —— 删完小婕店是 0 单。她还没开张,这是对的,**但请确认她理解**

---

## 四、北京旗舰店第一段(语句贴了,没等)

`handoff/11k_北京旗舰店第一段_写入语句待看_2026-09-22.md`。

预批①② **生产复核过**(都是 0 行,没吃上一轮的结论);③ 就是那份文件;
④ 备份**等你答了再当场拍**(现在拍到执行那刻就旧了);⑤ 彩排已过。

### 🔴 那支 seed 真会造假人,我量到了,比 11k 说的还多

**本机库里这家店已被那支 seed 跑过一遍**(生产没有,本机有):

| 表 | 行数 |
|---|---|
| `users` | **20**(电话 `1390000****` **连号**) |
| `technicians` / `services` | 3 / 13 |
| `bookings` / `booking_slots` / `payments` | **50 / 200 / 50** |
| `coupons` | 2 |

**合计 338 行。** 对照:**D207 那次在生产上清掉的是 51 行。**
**这支脚本对生产跑一遍,造出来的假数据是 D207 的六倍多。** J-114 的实物证据。

### 彩排

把本机库副本清成生产的样子(清掉 650 行),只跑那两条:

```
写入行数:tenants=1 · stores=1
✅ users/technicians/services/bookings/booking_slots/payments/
   settlements/stored_value_transactions/finance_transactions/coupons 全为 0
🟢 人和钱全为 0 —— 只立了骨架,一个假的都没有
幂等:再跑一次 → ✅ 被唯一键挡住
🟢 反向守:lucky-luxe(74人/162单)· jics-nail(75人/198单)一行没动
```

⚠️ **与预批第 5 条的差别我写清楚了**:用的是**本机库副本**,不是生产备份还原出来的库。
理由:拿生产备份到本机 = 把真顾客数据搬出来;在容器里还原 = 往容器写文件(那条红线我上批刚破过一次,不破第二次)。
🔴 **所以执行那一刻,第 4 条那个备份照拍,一步不省。**

---

## 五、J-114 落地:14 支逐支过完

**分类器按机制定义,两半都扫**:直连 SQL 的 `INSERT`,**和走正门那些会建人/建钱的口**。

| | 支数 |
|---|---|
| 种子脚本总数 | **14** |
| 🔴 会造人或造钱 → **文件头第一行写死「此脚本永不对生产跑」** | **10** |
| 🟢 只碰价目/知识库/租户配置 → 不盖章 | 4(`seed-jics-nail` · `seed-jics-mirror` · `seed-addon-groups` · `seed-services`,**逐支验过**) |

**判据 5 条进 `test-demo-seed-guard`**(76 → 81 项):
① 扫描面下限 ② 危险的每支都盖章 ③ 🟢 **反向守:安全的一支都没被误盖章**
④ 分类器两半都在 ⑤ 具名钉住 `seed-luvia-bj.mjs`

### 🔴 这条判据第一版是错的,我把它记进代码注释了

我一开始**只扫 `INSERT INTO users` 这类 SQL** —— 而 `seed-luvia-bj.mjs` **走的是正门 HTTP 接口**
(`POST /platform/tenants/:id/import/customers`),于是它报**「造人 0」**。
**而那支正是店主点名会塞假顾客的脚本。判据在缺陷存在时报了绿。**
改法不是补一个特例,是**把「造人造钱的口」重新定义成两半**。

---

## 六、固定六项

**① 改了什么**
出口普查判据(4 条)+ 词表加「待定」+ 判据改具名冻结 + 反向守;
四处漏掉的出口全收(两处是输入框);J-114 判据 5 条 + 10 支脚本盖章;
`admin.html`/`admin.js` bump 版本;四份文档。**生产一个字节没写。**

**② 在哪一层验的**
机制层(全仓机械扫描,不靠列举)+ 行为层(`realValue` 直接跑、彩排真跑 INSERT)+ 生产数据层(只读)。

**③ 同类扫尽**
扫描面 **195 个文件**带下限;出口 **31 处**带上限;种子 **14 支**带下限。
**三个数都带分母,不是「都接上了」。**

**④ 用什么数据验的**
J-114 用的是**本机库里那支 seed 真跑出来的 338 行**,不是我想象的;
彩排用的是**清成生产样子的真副本**,别家店数据规模是真的(74/162、75/198)。

**⑤ 重复稳定性**
占位套件跑了 8 轮(含两把刀);全量回归跑了 2 轮;预检拦了 2 次都修完重跑。

**⑥ 残留与不确定**
「等天亮」5 条(两件待你答 + Cowork 挑的三条 + 两条新登记);
🟡 新登记:`i18n.js:484` 英文档店介绍**所有租户共用一句写死的**,原文还带 `in this demo`;
🟡 新登记:那 10 支的文件头**是给人看的,跑起来不拦** —— 要不要加运行时自检。

**断言增量对账(判据五:计数即证)**
**4,393 → 4,406(+13)**,逐套对上:
`store-placeholder` **41 → 49(+8)** · `demo-seed-guard` **76 → 81(+5)**。
⚠️ 我一度以为是 +7,因为记成了 42 —— **42 是加完 ①e、还没加 ⑦ 组的中间态,从没进过任何一次回归基线**。
**差额查到底才算对完账。**

**巨型文件:`local-server.mjs` 17,539(未动)· `admin.js` 7,893(未动)· `customer.js` 2,458(未动)**

---

## 七、L1–L5 自评

| 律 | 做到没 | 说明 |
|---|---|---|
| **L1 末端验证** | 🟡 **部分** | 验到代码层与行为层。**四处新收的出口里两处是输入框,我没在浏览器/开发者工具里打开看过它现在是不是空的。** |
| **L2 同类扫尽** | ✅ | 三个数都带分母与下限/上限 |
| **L3 无图不结案** | 🟡 | 门店设置空态没有图,没标已验证 |
| **L4 反例数据** | ✅ | 拿 seed 真跑出的 338 行验 J-114;拿真副本验彩排 |
| **L5 重复稳定** | 🟡 | 套件多轮;**没做「连续 5 次进出页面」**(无交互类交付) |

🔴 **最该补的仍是 L1**:我这两批一直在证明「代码接上了」,
**但那四个输入框现在打开长什么样,我一次都没看过。** 下一批用开发者工具截图收口。
