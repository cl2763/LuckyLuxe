# 09f · 环境变量权威清单(**从代码里量,没碰 Railway**)

> 出于 2026-09-16 20:04:33 CST · 刀 `tools/env-inventory.mjs` · 常驻判据 `test-env-inventory`(8 条)
> 🔴 **本表只报名字与依据,一个值都没读、没打印、没拷贝**(09f 停线)
> **分工**:Code 量「代码要什么」,店主核「Railway 上有什么」,两边对。

> 🔴 **扫描面**(J-65③):生产运行面 **120** 个模块(**从 `local-server.mjs` 顺 import 可达**,不是「apps/api 下全部」) · 其余面 **171** 个(tools / apps/web / 根脚本 —— **不在生产上跑**)
> 生产运行面上的变量名 **58** 个 · 动态取键 **3** 处

## 四格 + 说不清(底数闭合)

## 底数闭合(J-48)

| 变量名 | 格 | 依据 | 首处代码位置 | 生产面出现 |
|---|---|---|---|---|
| `OWNER_DEMO_TOKEN` | 启动必需 | 走 secret-gate 那道闸:取不到 → `process.exit(1)`(J-53 fail closed) | `apps/api/local-server.mjs:166` | 3 处 |
| `OWNER_TOKEN` | 启动必需 | 走 secret-gate 那道闸:取不到 → `process.exit(1)`(J-53 fail closed) | `apps/api/local-server.mjs:166` | 3 处 |
| `ALLOW_DEMO_ADMIN_LOGIN` | 🔴 必须未设 | 它是演示门的第三道条件(前两道是「环境变量说是生产」与「库域不是 ci/sandbox」)。在生产上这两道会先把门关死,所以单设它打不开门;**但在任何被判成 ci/sandbox 的实例上,设它 = 邮箱登录不校验密码即可登入**。生产上一律不设 —— 它没有任何正当用途。 | `apps/api/data-scope.mjs:82` | 1 处 |
| `AI_API_KEY` | 功能必需 | 它与 `OPENAI_API_KEY` 是**别名对**(读不到就读那个,两个都没有才是空)—— **两个设一个即可,但不能都不设** | `apps/api/ai-utils.mjs:2` | 2 处 |
| `COS_BUCKET` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:9871)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:9871` | 1 处 |
| `COS_REGION` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:9870)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:9870` | 1 处 |
| `COS_SECRET_ID` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:9868)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:9868` | 1 处 |
| `COS_SECRET_KEY` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:9869)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:9869` | 1 处 |
| `DATA_DIR` | 功能必需 | 有 2 处**不带兜底**(首处 apps/api/local-server.mjs:128),不设则那一段拿到 undefined | `apps/api/local-server.mjs:128` | 3 处 |
| `FINANCE_PASSWORD` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:388)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:388` | 1 处 |
| `MERGE_WINDOW_CAP_MS` | 功能必需 | 有 1 处**不带兜底**(首处 apps/api/merge-window.mjs:44),不设则那一段拿到 undefined | `apps/api/merge-window.mjs:44` | 1 处 |
| `MERGE_WINDOW_MS` | 功能必需 | 有 2 处**不带兜底**(首处 apps/api/merge-window.mjs:35),不设则那一段拿到 undefined | `apps/api/merge-window.mjs:35` | 2 处 |
| `MERGE_WINDOW_SECONDS` | 功能必需 | 有 1 处**不带兜底**(首处 apps/api/merge-window.mjs:37),不设则那一段拿到 undefined | `apps/api/merge-window.mjs:37` | 1 处 |
| `OPENAI_API_KEY` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/ai-utils.mjs:4)—— **不报错,但那一段功能是死的** | `apps/api/ai-utils.mjs:4` | 1 处 |
| `RAILWAY_ENVIRONMENT` | 功能必需 | 有 2 处**不带兜底**(首处 apps/api/data-scope.mjs:41),不设则那一段拿到 undefined | `apps/api/data-scope.mjs:41` | 2 处 |
| `TZ` | 功能必需 | 有 1 处**不带兜底**(首处 apps/api/local-server.mjs:120),不设则那一段拿到 undefined | `apps/api/local-server.mjs:120` | 1 处 |
| `WECHAT_APP_SECRET` | 功能必需 | 有 3 处**不带兜底**(首处 apps/api/local-server.mjs:10578),不设则那一段拿到 undefined | `apps/api/local-server.mjs:10578` | 3 处 |
| `WECHAT_MINI_APPID` | 功能必需 | 它与 `WX_MINI_APPID` 是**别名对**(读不到就读那个,两个都没有才是空)—— **两个设一个即可,但不能都不设** | `apps/api/local-server.mjs:394` | 1 处 |
| `WECHAT_MINI_SECRET` | 功能必需 | 它与 `WX_MINI_APPSECRET` 是**别名对**(读不到就读那个,两个都没有才是空)—— **两个设一个即可,但不能都不设** | `apps/api/local-server.mjs:395` | 1 处 |
| `WECOM_AGENT_ID` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:402)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:402` | 1 处 |
| `WECOM_CORP_ID` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:397)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:397` | 1 处 |
| `WECOM_CUSTOMER_SERVICE_AES_KEY` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:400)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:400` | 1 处 |
| `WECOM_CUSTOMER_SERVICE_SECRET` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:398)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:398` | 1 处 |
| `WECOM_CUSTOMER_SERVICE_TOKEN` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:399)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:399` | 1 处 |
| `WECOM_OPEN_KFID` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:401)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:401` | 1 处 |
| `WX_MINI_APPID` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:394)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:394` | 1 处 |
| `WX_MINI_APPSECRET` | 功能必需 | 每一处都兜底成**空**(1 处,首处 apps/api/local-server.mjs:395)—— **不报错,但那一段功能是死的** | `apps/api/local-server.mjs:395` | 1 处 |
| `AI_BASE_URL` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/ai-utils.mjs:3` | 1 处 |
| `AI_ENABLE_THINKING` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/ai-utils.mjs:94` | 1 处 |
| `AI_GATE` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/ai-gate.mjs:170` | 1 处 |
| `AI_MODEL` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/ai-utils.mjs:1` | 1 处 |
| `AI_PROVIDER` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/ai-utils.mjs:2` | 1 处 |
| `AI_REQUIRE_REAL` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/ai-utils.mjs:5` | 1 处 |
| `AI_TIMEOUT_MS` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/ai-utils.mjs:60` | 1 处 |
| `AI_VISION_MODEL` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/ai-utils.mjs:70` | 1 处 |
| `ALLOW_DB_IMPORT` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:10754` | 1 处 |
| `APP_PUBLIC_URL` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(2 处) | `apps/api/local-server.mjs:393` | 2 处 |
| `APP_TIMEZONE` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:119` | 1 处 |
| `BACKUP_ENABLED` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:17602` | 1 处 |
| `BOOKING_HOLD_MINUTES` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:389` | 1 处 |
| `COS_SMOKE` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:9902` | 1 处 |
| `DEFAULT_TENANT_ID` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:176` | 1 处 |
| `DRAFT_PAYMENT_REMINDER_MINUTES` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:390` | 1 处 |
| `FINANCE_EMAILS` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:387` | 1 处 |
| `HOST` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:17569` | 1 处 |
| `HUMAN_REPLY_COOLDOWN_MINUTES` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:391` | 1 处 |
| `LL_TEST_DATA` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/data-scope.mjs:50` | 1 处 |
| `NODE_ENV` | 可选 | 每一处都是**比较式使用**(2 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/data-scope.mjs:41` | 2 处 |
| `NOTIFY_TICK` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:17617` | 1 处 |
| `ONLINE_PAYMENT_READY` | 可选 | 每一处都是**比较式使用**(1 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:7156` | 1 处 |
| `OWNER_EMAILS` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:380` | 1 处 |
| `PORT` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:163` | 1 处 |
| `RAILWAY_GIT_COMMIT_SHA` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(2 处) | `apps/api/app-version.mjs:23` | 2 处 |
| `STAFF_DEMO_PASSWORD` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:382` | 1 处 |
| `STAFF_EMAILS` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:381` | 1 处 |
| `STAFF_TECH_MAP` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:383` | 1 处 |
| `WECOM_NOTIFY_USERID` | 可选 | 每一处读它都兜底成**一个能用的默认值**或比较式使用(1 处) | `apps/api/local-server.mjs:404` | 1 处 |
| `WXPAY_MOCK` | 可选 | 每一处都是**比较式使用**(4 处)—— 不设只是走另一支,没有东西会坏 | `apps/api/local-server.mjs:14005` | 4 处 |


## 🔴 说不清(动态取键,抠不出名字)—— 3 处,按 J-66③ 当最坏那格
- `apps/api/mini-token-secret.mjs:89` 键是 `n` —— return EXPLICIT_ENV_NAMES.some((n) => String(env[n] || '').trim().length > 0)
- `apps/api/secret-gate.mjs:31` 键是 `n` —— const name = envNames.find((n) => String(env[n] || '').trim())
- `apps/api/secret-gate.mjs:32` 键是 `name` —— const explicit = name ? String(env[name]).trim() : ''

> 🔴 **这 3 处其实查得清**:键都来自代码里写死的名字表 ——
> `EXPLICIT_ENV_NAMES = ['WECHAT_MINI_TOKEN_SECRET','WX_MINI_TOKEN_SECRET']`(`mini-token-secret.mjs:46`)
> 与 `envNames: ['OWNER_TOKEN','OWNER_DEMO_TOKEN']`(`owner-token.mjs:44`)。
> **四个名字都已经在「启动必需」那一格里了**,所以这 3 处**不会漏掉任何名字**;
> 但按 J-66③ 我仍把它们单列 —— **「我查清了」和「刀自己抠得出来」是两件事**。
