# 09d 回执 · 2026-09-16(执行单 #1–#3 交 · #4–#8 状态)

**本令 md5 两处一致**:`33566bb59edc8c7cc1fa6a9f57c472d7`(桌面夹 + 仓里 `handoff/`,10,965 字节)
**生产**:本批**一次都没连**
未动对照:handoff/night-runs/07l_未动对照_2026-09-14.md

---

## #1 · 🔴 断言链补齐:`4,073 → 4,082 → 4,085`

**从 `assertion-baseline.json` 的 git 历史现测**(不靠记忆,三个锚点提交逐个 `git show`):

| 锚点 | 提交 | 合计 | 增量 |
|---|---|---|---|
| 08a 回执 | `2ac119c` | **4,073** | —— |
| **夜13 收班** | `6beb658` | **4,082** | **+9** |
| 09a 回执 | `3772703` | **4,085** | **+3** |

**那 9 条逐套点名**(现测 diff):

| 套件 | 从 → 到 | 增量 | 是什么 |
|---|---|---|---|
| `bench-selfguard` | 13 → 18 | **+5** | J-61④ 判据物归位的**两面断言**(⑤a–⑤e)|
| `web-not-signup` | 7 → 9 | **+2** | ㋝0「扫描面逐个都在」· ㋝0b「扫描面下限」|
| `schedule-v2` | 19 → 20 | +1 | ③0 顾客走正门前置 |
| `staff-portal` | 37 → 38 | +1 | staff 前置(本店有可用技师)|
| | | **+9 ✅** | |

**那 3 条**:`coupon-settle` 86→87 · `customer-paths` 14→15 · `scan-sign` 53→54 —— **D196 三条判据各 1**。

> **一处如实说明**:这个数**在夜13 早报里出现过**(§二那张表第 4 行写的是「4,082 · 4,073 → 4,082(+9)」)。
> 但早报我在班中改写过一次,那一行是**改写后才对的**;**链没有单独列出来,所以它到不了你手上**。
> **一个数出现在表格里,不等于那条链是可追的。** 以后收班一律单列一行「底数 → 增量 → 新底数 + 逐套点名」。

---

## #2 · 路径表点名 —— **两条支付都不在里面**,而且 4 其实是 3

**那几条没走过的**:

| 路径 | 在哪 |
|---|---|
| `/add-ons` | `miniprogram/utils/api.js` 等 2 处 |
| `/ai/reference-analysis` | `miniprogram/utils/api.js` 等 2 处 |
| `/ai/social-copy` | `apps/web/customer.js` |

🟢 **07m 点名的那两条支付,现在都在「走过」那一节里**:
`/payments/mock/confirm` 与 `/payments/stripe/confirm-session` —— **07p 的 ㋚1 / ㋚2 补的夹具**。
**所以「8 → 3」不是被好看的总数盖住,是那两条真的被补上了。**

### 🔴 顺带更正我昨天报的「4 条」——**真值是 3,第 4 条是工具误报**

```
miniprogram/utils/api.js:797
  request(`/store/deposit-policy${qs ? `?${qs}` : ''}`)
```

**嵌套模板串**(里层还有一对反引号)—— 捕获正则遇到里层那个反引号就收工,
抠出来的是 `/store/deposit-policy${qs `,于是这条**明明被 `test-deposit-config` 走过**的路,被报成「一次都没走过」。

**方向是假阳**(把走过的报成没走过)——不危险,但那个数是错的。
**已修**(归一化到最后若还留着没闭合的 `${` 就地截断)**+ 补了一条嵌套模板串的靶子**。
现测:**路径 62 · 走过 59 · 没走过 3**。

> 这一条正是 J-58⑨ 那句话的另一面:**抠得宽,7 会变成 57;抠得断,走过的会变成没走过。**

---

## #3 · 🔴 D199 只读盘清 —— **一个字没改**

| | 现测 |
|---|---|
| 规模 | 3,918 行 · 759 条断言 |
| `check()` | 🔴 **`throw` —— fail-fast** |
| 老板/建店 `newShop()` ×6 · 员工账号 ×2 | **走正门** ✅ |
| 🔴 顾客登录 | `mini-login` 带 `demoLogin: true` —— **27 处** |
| 🔴 直连库贴 | **14 处**(11 处是同一句贴微信绑定 · 2 处贴历史脏标签 · 1 处直接 INSERT)|

**先说一条要调格子的**:`:649` `:650` 那两处贴的是「退役·旧口径演示档案」标签 ——
**正门按定义产生不了历史脏态**,它们该进「具名冻结」,不在「欠账」格里。
**所以这一套真实欠账是 12 不是 14** —— 等你点头再调(J-51:冻结要具名)。

### 🔴 三次各错在哪 —— 而根因不是形状,是**范围**

| 次 | 改了什么 | 错在哪 |
|---|---|---|
| 1 | **4 处** mini-login 换正门助手 | 下游取 `pm.data.user.id`,助手返回体少那一格 → `reading 'id'` 炸 |
| 2 | 补齐返回体形状 | 更后面的 `㉕ 未配置租户=不分级` 炸,`undefined/undefined` |
| 3 | 没试,按停线停,原样还原 | —— |

**盘清之后看明白了**:我当时用 `const (pm|cm|cm2|cm9)` 找,**只找到 4 处;而这一套里 `demoLogin: true` 有 27 处。**

> **换了 4 处、留了 23 处 = 把一套夹具改成了两种顾客混用。**
> 正门顾客带手机号、带绑定态、带轻档案历史;演示顾客什么都没有。
> 第 2 次炸的 `㉕`(按充值史两态分梯子)**十有八九就是混用炸的**。
>
> **找漏了改一半,比不改更糟** —— 不改是一致的旧状态,改一半是两种状态混在一起。
> 归族 J-73:**我用一个正则去找「所有要改的地方」,而那个正则只认得其中一种写法。**

### 与 #7 是不是同族 —— **是,而且顺序要反过来**

🔴 **fail-fast 之下,换夹具是盲改** —— 27 处要炸多少次、各炸在哪,**一次也看不全**,每次只看得见第一个。

**建议顺序**:**① 先去 fail-fast(#7)→ ② 再一次换全 27 处 + 11 处直连绑 → ③ 最后调那两处格子。**

**做完的预期**:门关档 **红 1 → 0**(它是最后一套红)· J-60 **23 → 约 11** ·
段四那个「充值口 759 条里 701 条没跑到」一起解决(那一口正是这一套)。

全文:`night-runs/D199_盘清_noshow夹具_2026-09-16.md`

---

## #4–#8 状态

| 执行单 | 状态 |
|---|---|
| **#4 去 fail-fast(单独一批)** | **未做** —— 你批了单独一批;按 #3 的结论,**它应该排在换夹具之前** |
| **#5 40 个口继续** | **未做**(已做 10);🔴 **11 条追不到口的断言归口结果再报一次**:夜13 现测 **11 → 4**,归出来的 7 条里 **5 条是涉钱口**(`/bookings` · `/admin/membership/members` · `/admin/finance/transactions` ×2 · `/admin/daily-close`);剩 4 条逐条手查,**涉钱 0** |
| **#6 推 main 准备件** | **已备,一条 push 都没执行**(`night-runs/推main准备件_夜13_2026-09-16.md`)|
| **#7 门关档红 1** | 只剩 `noshow-aftersales` —— 即 D199 |
| **#7 9 条冻结理由 J-70 复核** | **夜13 已做**:9 条冻的是**夹具**不是**判据**,**0 条属于「红在真病上」** |
| **#8 `customer.js`** | **本批没动**(2,458 ±0)|

---

## 数(全部现测)

| | 数 | 增量 |
|---|---|---|
| 主档 | **124 / 124 全过** | ±0 |
| 门关档 | **25 套 · 红 1** | ±0 |
| 小程序档 | **3 套 · 红 0 · 空转 0** | ±0 |
| 断言 | **4,085** | ±0(**本批只改工具与文档,没加判据**)|

**棘轮**:`local-server.mjs` **17,621(±0)** · `admin.js` **7,928(±0)** · `customer.js` **2,458(±0)**
**J-60**:**23(±0)** —— 按 J-50⑤ 报「**达不到 + 为什么**」:剩下 23 处里 **12–14 处卡在 D199 那一套**,**不绕开它去凑数**
**账本**:`8cf9acc` 领先 326 + 本批 91 = **417** ✅

---

## 本批我自己记的一笔

**昨天报「没走过 4 条」,真值是 3。** 第 4 条是**我的工具把路径抠断了**造成的假阳。
我在同一份回执里刚写完「指纹选宽了,7 会变成 57」,**而我自己的另一把刀正在把走过的路抠成没走过的** ——
**宽和断是同一个毛病的两头,我只看见了一头。**
