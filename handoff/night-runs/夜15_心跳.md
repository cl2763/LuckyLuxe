# 夜15 心跳(每 30 分钟覆盖同一份)

- **当前在做哪一件**:§二 (丙) —— 逐支种子实跑定夺(不按名字猜,J-83)
- **已完成几件**:①§一 出结论(沙箱起得来,缺的是夹具数据,并写清那个 0 的扫描面)
  ②在**临时沙箱**上复现了 CI 的处境(三样夹具全 0)
  ③查明 `requireSandbox` 只认路径里带 `/sandbox-data/` 那一段 —— **临时目录命名成 `…/sandbox-data` 就同时满足「只种临时 DATA_DIR」与「只许沙箱」两条**
- **卡在哪**:🔴 第一支种子 `seed-jics-nail` **跑不通** —— `PUT /admin/kb/facts` 回 400
  `UNKNOWN_KB_KEY: currency`(币种已收归「门店设置」唯一真相,而种子还在往 kb/facts 塞),
  **三样夹具一样都没造出来**
- **下一步**:换别的种子逐支实跑(demo-rich / demo-twin / bigdemo),并把这支坏种子单独立案
- 更新于 19:05:38 UTC
