# 图的哪一节 → 落在哪个端 → 有没有越界(D177 §6 要的那张表)

> 夜班令6 段 8 要求:「顺带自查同族 —— 还有没有别的『§一 的规矩被搬到 §三』或反过来的地方?
> 列一张表(图上哪一节 → 落在哪个端 → 是否越界),这次是轮播,下次可能是别的。」
> 合同 = `handoff/商家端主页重画_两端设计图_2026-09-03.html` v3.1
> 做法:把图里每一条「规矩」逐条对到端上,再回仓里现查那个端有没有做、有没有多做。

| 图上哪一节 | 那一节讲的是哪个端 | 规矩 | 仓里落在哪 | 越界? |
|---|---|---|---|---|
| §一「每一块的规矩」1 | **小程序** | 四维度 + 折线随维度 | 两端都有 | **不算越界**:§三 的视觉稿也画了四个维度按钮与折线,两端都该有 |
| §一 1 | **小程序** | 切维度 / **轮播换指标**时数字滚动 600ms | 网页有滚动、小程序**本批补上**(段 6) | 滚动两端都对;**轮播**曾越界 → 本批 D177 从网页删干净 |
| §一 2 | **小程序** | 五个小数**收在大屏底部一行**,轮播把谁放大谁就空出 | 小程序有(段 4 改成严格五列);网页是**右侧四小牌同时在屏**(§三 的画法) | 无越界 —— 两端本来就是两种排法 |
| §一 2 | **小程序** | 今日预约那格副行「在做 N · 待到店 N」带绿点 | 小程序有;网页是**此刻四格 + 下一位卡**(§三 画法) | 无越界 |
| §一 3 | **小程序** | 小数旁只给方向与幅度,点大数字看完整对比句 | 两端都只给方向与幅度 | 无越界 |
| §一 4 | **小程序** | 折线只有一条、不要图例、末点加白圈 | 网页做了(渐变 + 末点圈);小程序是**金色渐变柱**(端能力,05u 已裁) | 无越界,差异已裁并记在图 v3.3 |
| §三 视觉稿 | **网页** | 左:大数字固定 + 此刻四格 + 下一位卡;右:四小牌 + AI 一句 | 网页照此 | 无越界 |
| §三 side 注 | **网页** | 「同一份 pulse 数据**横着摆**」;旧的本月经营/账本收入等退役 | 网页照此 | 无越界 |
| §五 line 377 | **全屏态** | 轮播每 6 秒一换 / 60 秒重取 / 屏保 5 分钟 | 全屏态照此(本批没动) | 无越界 |
| §五 | **全屏态** | 默认不显示现金业绩与新增持卡 | 全屏态照此 | 无越界 |
| §六 动作表 7 行 | **两端** | 见段 5 | 本批两端补齐 | 之前是**漏做**不是越界 |
| §八 不做清单 | **两端** | 图表页 / 导出 / 自定义指标 / 快捷格 / 多店汇总 / **首页不轮询** | 两端都没做这些;「不轮询」指的是**不重取数据**,轮播是画面(段 5 已把判据按语义拆开) | 无越界 |

## 结论

**全表逐条过完,越界只有轮播这一处**(§一 的规矩被搬到了 §三),本批已删。
剩下的差异都是「两端本来就是两种画法」或「已裁的端能力降级」,不是越界。

**下次怎么防**:图上每一节开头都写了它讲的是哪个端;往某一端加规矩前,
先回图里确认那一节的端 —— 这张表以后每批新增页面时补一行。
