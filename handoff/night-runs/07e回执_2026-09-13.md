# 小批 07e 回执 —— 排班五色交表 · 渐变卡抬级 · OWNER_TOKEN 试点

> 2026-09-13 · 上游 `7120440`(07d)· **未推 main** · **生产库未动 · 本机库未动**
> 密钥一个字没进本文件

---

## 〇、D190 的令没到 —— 按 §六 往下做了

`handoff/` 与桌面交接夹都找过,**没有 D190 的令**(总表里只有那一条 🔴🔴 登记)。
所以按你 §六 的排期做了 2/3/4 三件;**令一到我先做它。**

---

## 一、§二 排班五色:**表已交,一行码没改**(裁 #62)

全文 `handoff/排班状态五色_交表_2026-09-13.md`。

### 🔴 两件你点名之外、但会影响你怎么裁的

**① 代码里没有「请假」「调休」这两个状态。** `stateText()` 现读只有五个:
`working` / `overtime` / `done` / `rest` / `none`。**调休那一格我填不了** —— 不是我没填,是它不存在。

**② 07d 我报给你的那几个数是错的,这张表是重量的。**
07d 我把五个都按「压在 `--paper` 上、不透明」量。现读发现**三个状态的底根本不是 `--paper`**:

- `overtime` 有**行内样式** `background:var(--badbg)` —— 压在红底上;
- `rest` / `none` 带 `disabled` 类,而 `.finance-rule-row.disabled { opacity: 0.5 }` —— **整行半透明**。

| 状态 | 中文 | 现在的值 | 压在什么底上 | 浅档 | 深档 |
|---|---|---|---|---|---|
| `working` | 在岗 | `var(--good)`(07d 已换) | `var(--paper)` | **5.05** ✅ | **9.66** ✅ |
| `overtime` | 超时未走 | `#b0483c` 写死 | `var(--badbg)`(行内) | **4.56** ✅ | **2.79** ⚠️ 乙档 |
| `done` | 已下班 | `#8a8578` 写死 | `var(--paper)` | **3.47** ⚠️ 乙档 | **4.92** ✅ |
| `rest` | 休息 | `#a89d8c` 写死 | `var(--paper)` · **整行 `opacity:.5`** | **1.52** 🔴 **甲档** | **2.63** ⚠️ 乙档 |
| `none` | 未上班 | `#a89d8c` 写死 | `var(--paper)` · **整行 `opacity:.5`** | **1.52** 🔴 **甲档** | **2.63** ⚠️ 乙档 |

### 🔴 换令牌**单独不够** —— rest/none 要连 opacity 一起裁

| rest/none 的改法 | 浅 | 深 |
|---|---|---|
| 只换 `--flat`,**opacity 照旧 .5** | **2.09** 🔴 仍甲档 | **2.61** ⚠️ 仍乙档 |
| 换 `--flat/--flatbg` **并去掉整行 opacity** | **4.93** ✅ | **5.64** ✅ |

**`opacity:.5` 才是这两格的病根**,令牌只是顺手。

### 我对五个语义的一句话理解(表里逐条写了),外加一件请你单独看

**`rest`(排了休息)与 `none`(根本没排班)现在用的是同一个 hex** —— 在排班表上这是两件事。
要不要分开,语义决定,**我不猜**。

### 顺手报两条**判据覆盖面**(只报不改)

这张表的数跟对比度红榜对不上(红榜写甲档 0,这里有两格 1.52)。查清了,**不是我算错**:

1. 🔴 **考勤/排班页不在对比度全扫那 19 页里** —— 红榜从没看过这一页。
2. 🔴 **`contrast-sweep` 不把祖先 `opacity` 折进前景色**(`:223` 只在 `opacity<0.1` 时跳过)——
   一整类「淡化行/禁用态」的字它一律高估。归族 **J-37「在不在 ≠ 看得见」**。
   **要治的话红榜的数会普遍变差一截** —— 那是真实情况,但得你先知道再动。

---

## 二、§三 渐变卡抬级:**只登记,一个字没改**(裁 #63)

已写进总表(2026-09-13 那一行):从「小程序 wxss 令牌化欠账」**拎出来,排上线后第一批**,
与「站内消息记录页接真数据」同批;到时候拿浅深两张并排图拍一次板(J-41)。

---

## 三、§四 `OWNER_TOKEN`:**helper 写好 + 一套试点跑通**(裁 #64)

你要的是「先写好 helper 让**一套**先用上,证明形状可行,再批量切 —— **不许九十个文件一次改完再跑**」。

### 关键设计:**今天一个测试都不许红**

所以这一步**不动 `OWNER_TOKEN` 的取值**。服务照旧用它现在那个值,
只是在 `ci`/`sandbox` 库域**把它写进那一轮的 `DATA_DIR/.owner-token`**,
让测试可以「**从哪拿**」而不是「照抄一串字面量」。

| | 今天 | 切换那一批之后 |
|---|---|---|
| 文件里是什么 | 现在那个值 | 闸接上去 ⇒ **每轮随机的那一把** |
| 试点套件要改几个字 | — | **零** |

**这就是「证明形状可行」**:helper + 文件 + 消费方三件套端到端跑通了,
剩下的只是把另外那些文件从字面量换成 `readOwnerToken()`。

### 试点:`test-admin-accounts`(原来是最糟的那种 —— 裸字面量 `const OWNER = 'owner-demo-token'`)

- 真跑一遍:**16/16 全过**(起了一台 ci 库域的服务,`.owner-token` 确实落了地,0600 权限);
- 前瞻验证:把文件内容换成一把随机串 ⇒ `readOwnerToken()` **跟着变**
  —— 说明它取的是**文件里的现值**,闸接上去自动拿到随机那把。

### 判据四条(`credential-scan ④s–④v`,23 → 27)

| 条 | 验什么 | 造病 |
|---|---|---|
| ④s | 发布口**只在 ci/sandbox 落地**,`local`/`production` 一个字节不落 | 拿掉库域判断 ⇒ 🔴 报 `local=true production=true` |
| ④t | helper 读的是**文件里的现值**,不是字面量 | — |
| ④u | 试点那一套**主来源是 helper** | 改回字面量 ⇒ 🔴 |
| ④v | **迁移棘轮**:还照抄 `owner-demo-token` 的文件 **102 个,只许降** | 归零那天 = 批量切完那天 |

`④v` 那个数**由判据自己量**(J-43),扫描面**递归**(`apps/api/tools/` 这类子目录不留盲区)。
**下一批**:按这个形状批量切,切完把 ④s–④v 连同 `local-server.mjs:OWNER_TOKEN` 那条豁免一起删。

---

## 四、§五 领先数由脚本产出(裁 #65)

新件 `tools/batch-footer.sh`:**提交清单 + 提交数 + 领先数,三个数一起现出**。

**你说的那个 286/287 我查清了**:我不是抄错,是**量早了** —— 报完之后又提交了两次。
**同一个病**:一份交付里,一半的数是量的、一半是抄的(或者量完就过期了)。所以这一行从此也由脚本出。

---

## 五、这一批全量跑了**两遍**,第一遍的红值得记

**第一遍**:`not ok 0`,但三支 mp 刀**各卡 61 秒后「本轮未跑」**,断言 **−12**,覆盖面掉到 109。
`test-assertion-baseline` **没红** —— 因为它有一条「未跑豁免 ≤ 3 套」的设计允许。

🔴 **但那不算跑过。** 查下去:**端口在听 ≠ 会话是活的** ——
自动化会话僵死时 `lsof` 照样看得见 9420,而我 07c 写的那条探针只探端口。
**我为此白跑了一整轮回归才发现。**

改法(归族 J-37):探针改成**探应答** —— 活会话对 HTTP 请求回 **426 Upgrade Required**(它要 WebSocket);
在听却不回 426 ⇒ 照实报「可能僵死」并给重启命令。
**造病**:把探针指到 4310(在听但回 200)⇒ 🔴 报「9420 在听但会话没应答(HTTP 200)」;还原回绿。
重启自动化会话后**第二遍 112/112 全过、覆盖面 112/112、三支 mp 刀真跑了**。

---

## 六、六项结构 + 数

**① 改了什么**:五色交表(不改码)· 渐变卡登记(不改码)· OWNER_TOKEN 试点(helper+发布口+一套切换+四条判据)·
`batch-footer.sh` · 自动化端口探针改成探会话。
**② 在哪一层验的**:表是**现读源码 + 同一把 WCAG 公式**;试点是**真起服务、真落文件、真跑那一套**;探针是**真造一次假会话**。
**③ 同类扫尽**:迁移棘轮**递归**扫全仓(102 个,只许降)· 发布口两个方向各验 · 五个状态逐个重量(不是抽样)。
**④ 用什么数据验的**:真进程、真文件、真随机串替换。
**⑤ 重复稳定性**:全量两遍;试点套件与每把造病刀各红→绿一轮。
**⑥ 残留与不确定**:见 §七。

**回归**:**112/112 全过** · 整跑 **2 分 42 秒** · **断言 3,917 → 3,921(+4)**(全在 `credential-scan` 23→27)·
覆盖面 **112/112** · 零断言套件 0 · **收尾自证 4128 与 4310 都 ✔**。

**棘轮(J-43 逐个重量)—— 一个都没变**:
`local-server.mjs` 17,707 · `admin.js` 7,928 · `customer.js` 2,799 · `party.js` 31 ·
styles.css 写死色 31 · 小程序 wxss 写死色 2,350 · UTC 判天 12。**棘轮对账无红**(降 0、涨 0)。

**写库自报**

| 库 | 结论 | 证据 |
|---|---|---|
| **生产库** | **生产库未动** | 这一批零次 `railway` 调用 |
| **本机库** | **本机库未动**(逐表行数) | 开工前/收尾后各一次:**92 张表,有变化 0 张** |
| **沙箱库** | 有动,全是回归夹具与心跳 | 两遍回归,每遍自造自收 |

> **未动对照:handoff/night-runs/07e_本机库未动对照_2026-09-13.md**

**服务**:4128 ✅ · 4310 ✅ · 微信开发者工具自动化会话 ✅(本批重启过一次,原因见 §五)。

---

## 七、残留与不确定

1. **排班四个状态等你裁**(表已交);rest/none 那两格**要连 `opacity:.5` 一起裁**,只换令牌不够。
2. **代码里没有「请假」「调休」** —— 那两格填不了。
3. **对比度全扫两条覆盖面问题**(考勤页不在扫描面 · 不折祖先 opacity)只报未改。
4. `OWNER_TOKEN` **批量切**没做(你准许停在试点)——迁移棘轮 **102 → 0** 是进度尺。
5. **渐变卡**只登记抬级,没动码。
6. D190 的令没到。
7. 本机库仍只比**逐表行数**。

---

## 八、自评 L1–L5 + 停线自查

| | 做到没有 |
|---|---|
| L1 末端验证 | ✅ 试点**真起服务真落文件真跑套件**;探针**真造一次假会话** |
| L2 同类扫尽 | ✅ 迁移棘轮递归扫全仓;五个状态逐个量(不抽样);发布口两个方向 |
| L3 无图不结案 | ⚠️ 本批没出截图 —— 交付物是**表**与**判据**,不是界面改动;渐变卡的证据是机械解析(07d 已出) |
| L4 反例数据 | ✅ ④s/④u 各一刀;探针一刀;试点做了**前瞻验证**(把文件换成随机串) |
| L5 重复稳定 | ✅ 全量两遍;试点与造病刀各一轮红→绿 |

**停线自查**:未推 main ✅ · 未碰生产 ✅ · **4128 既有行未写** ✅(逐表零差异)·
**未看密钥文件** ✅ · 无 sleep 轮询 ✅ · **没松判据、没抬棘轮** ✅(棘轮一个没变;新立的迁移棘轮初值由判据自量)·
**没改合同图** ✅ · 密钥零字进本文件 ✅。

**没照字面做的**:
1. §二 那张表**「调休」一格空着** —— 代码里没有这个状态(§一①)。
2. §四 我**多做了一点**:除了 helper 与试点,还加了「发布口只在 ci/sandbox 落地」的判据 ——
   因为不加这条,那个文件会在本机/生产的数据目录里也落一份主令牌。这一条我认为不加不行,如不同意请裁。
