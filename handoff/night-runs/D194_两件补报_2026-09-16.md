# 09a 执行单 #1 · 两件补报 —— **两个指纹零差额,三个数都要改**

**读数时刻**:2026-09-16 15:23:29 CST · **只读** · **一行没动**
**读的是哪个库(J-64)**:`/app/apps/api/local-data/lucky-luxe.sqlite` · 5,980,160 字节 · mtime `2026-08-27T04:56:40.243Z` · inode 15
**同构库先跑通**(J-75①):本机库上先跑一遍,4 行、两个指纹 4/4 对上 —— **证明这把尺子在有东西的时候数得出来**,再拿去生产

---

## 一 · 🔴 第二个指纹:**7 / 7 逐一对上,两边零差额**

| 指纹 | 数 |
|---|---|
| ① `users.google_id` 非空 | **7** |
| ② `user_identities` 里 `provider = 'google'` 的身份行覆盖的人 | **7** |
| **两边逐 id 对** | **7 / 7 全对上** |
| 只在①里(有 google_id 但没有 google 身份行) | **0** |
| 只在②里(有 google 身份行但没 google_id) | **0** |

> **两个指纹指向同一批行,零差额 ⇒ 这一格按 J-66 闭合。**

**顺带记一个对照数**:宽口径的「有身份行但 `users.wechat_open_id` 为空」是 **57 行** ——
那 57 里有 50 行**跟这件事无关**(它们没有 `google_id`,是别的来路:邮箱注册 / 导入的轻档案)。
**所以指纹必须收窄到 `provider = 'google'`** —— 宽口径会把 50 个无辜的人算进来。
这一条本身是个教训:**指纹选宽了,7 会变成 57。**

---

## 二 · 🔴 那 7 行的租户:**全在 `lucky-luxe`** —— 你猜对了

`[{"tenant_id":"lucky-luxe","n":7}]`

**与 07z 现测对上**:那条路的租户取自 body,给了不存在的店就**回落默认店**,而默认店就是 `lucky-luxe`。

---

## 三 · 🔴 三个数,逐个重算(底数一并出,J-58①)

```
{"全库":77, "lucky_luxe":37, "lucky_luxe_压测行":7,
 "lucky_luxe_没手机号":28, "lucky_luxe_没手机号_剔掉压测行":21}
```

| 数 | 一直在说的 | **真值** | 差 |
|---|---|---|---|
| 全库 users | 77 | **70** | −7 |
| `lucky-luxe` users | 37 | **30** | −7 |
| `lucky-luxe` 没手机号 | 28 | **21** | −7 |

🔴 **三个数各少 7,而且是同一批 7 行** —— 因为那 7 行**全在 lucky-luxe,且全都没有手机号**。

**「没手机号」那个比例也跟着变**:
- 旧说法:**28 / 37 = 76%**
- **真值:21 / 30 = 70%**

**这个比例是当初「手机号做锚」那套设计的依据之一** —— 数变了,结论方向没变(七成顾客没号仍然是大问题),**但说出去的那个数得是真的。**

---

## 四 · 我自己文件里的更正(J-55,原文不删)

用过 77 / 37 / 28 的是我这几份:`07r回执` · `07q回执` · `生产只读五问_查询原文_第二版待批` · `夜13 段一报告` · `早报_2026-09-16`。
**逐份加更正条,指到这一份。**

---

## 五 · 这一笔归我(第三次同族)

- 第 11 笔:「1,432 条真实历史」—— 数字错了一个量级;
- 第 12 笔:把 37 行当成真顾客;
- **这一次:77 里有 7 行不是人。**

**同一族:一个数,和它被安上的那个意思。**
这一次的具体毛病是:**我把「表里有多少行」直接当成了「有多少顾客」** ——
而这张表里**本来就会有非顾客的行**(压测、导入、演示),**我从来没问过「这 77 行里有几行是人」。**

