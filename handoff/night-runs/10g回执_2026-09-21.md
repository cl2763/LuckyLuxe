# 10g + 10g-补 + 10f 回执 · 2026-09-21

**点名(J-81):**
- `小批10g_立J107一条法只授权它明写的那件事_取数很好_等两件人做的事_2026-09-21.md` md5 `6814f4247d601e902a3e6f62eb02ada8`
- `小批10g补_店主记不清_那一行按最保守档冻结_2026-09-21.md` md5 `6a18111279cf732e742021da955db17f`
- `小批10f_口径定死_平台记账不代收_顾客端措辞要跟着改_2026-09-21.md` md5 `d981e59d731cf7dec80803ff4fb17a2b`(**本批才落地**,10g 提醒我它没落)

三份桌面/仓两头一致。
**合同图(每批重贴):** 签署文件留档 v2 → https://claude.ai/code/artifact/6366076a-97e4-4af4-b711-cc2e7468e94f

---

## 1. 🟢 小程序顾客端:**实看 + 两条负例 + L5,全做完了,而且拿到了像素图**

店主扫码后,按仓里 mp 档自己的跑法开自动化端口(`cli auto --auto-port 9420`)。

**屏幕上(`element.text()` 现读,不是接口回包):**
```
我签署过的文件
会员充值权益确认书
2026-09-20 签署 · 2 页
这些是店里存档的原件,只能查看
```
区块 **378 × 142**,位置在**会员卡与「我的订单」之间** —— 图 v2「跟卡包、订单放一起,不另开入口」✅

| 负例 | 屏幕上 |
|---|---|
| 价格表确认书(**已作废**) | 🟢 **不在** |
| 店里自己的表(**`customer_visible=0`**) | 🟢 **不在** |

**L5 连续 5 次 `reLaunch` 进出**:五次 `378×142`、只有一份、两条负例次次不在、文案逐字相同 —— **零异常**。

🟢 **像素图拿到了**(`/tmp/mp-me-signed.png`)——**小程序这一端不像网页端,截得出来。**

### 🔴 但这一段里最该记的,是我第一版**差点报了一个假缺陷**

第一次跑 L5:**第 1 次好,第 2 次起变成空态**「还没有签署文件」。看起来像个硬缺陷。

**我没有直接报,先去量了(J-99:说「复现不了/是缺陷」之前,先点名那个变量):**
```
注入后      : auth 尾号 …6lirhPm4
第1次进 me  : auth 尾号 …SwaCunxA   ← 🔴 变了
小程序里直打接口: {"status":200, "n":0}
```
**小程序启动时会自己登录一次,把我注入的令牌换成它自己那个人(柳南舟2)的。**
那个人**确实没有签署文件** ⇒ **空态是对的,错的是我的夹具。**

**改法是造景律那句话:景要造在 app 真会走到的那个人身上。** 把三份文件造给柳南舟2,重跑 → 五次全绿。

> **如果我当时按第一版结果报上去,就是一个「看起来完全正确」的假缺陷 ——
> 和 10e 那次 `width:0` 同一族:一个量出来的数,也要问它是在哪一刻、对谁量的。**

### 🔴 顺带按合同图补了一处像素级差异

实拍出来缩略图是个**空米色方块**,而图 v2 的 `.th` 是**画了横线的文件图标**(网页端有、小程序端漏了)。
两端 wxss 都补上(用 `repeating-linear-gradient`,只用 token;**写死色棘轮仍 2350**)。
⚠️ 改后单次复验:区块 378×142、文案不变 ✅;**改后的截图那一次挂住了**(开发者工具截图偶发卡),
**L5 的 5/5 是在这次纯 CSS 改动之前跑的** —— 如实分开写,不把它算进 5/5。

## 2. D207-b:**已按「冻结 · 身份未认定」登记**

登记在 `handoff/D206_主钥匙留痕普查_2026-09-20.md` 末尾新增的 **「D207-b」** 一节,含:
**两单明细表 · 证据两栏(像测试的 / 不像测试的,各自量出来的)· 店主记不清 · 结论「不清、不动、不并、不改 tenant」· 再核时点 = 小婕开张前对账 · 要问她的那句人话(不给机器号)· 以及与那 51 行处置不同的理由。**

## 3. J-107 收到

> **一条法只授权它明写的那件事。「精神上更符合」不是授权。**
> **推论:一次「抽取」必须是行为等价的。**

**本批立刻用上了一次**:`miniprogram/utils/i18n.js` 的 `paid` 键现扫**零调用方**,
按老习惯我会顺手删掉它 —— **10f 授权的是改措辞,不是删键**,所以只改值,死键另账登记。

## 4. 10f「平台记账,不代收」—— 口径收到,§三 做完

### 先报一件好消息:**10f 要的「两端共用词典」,08-25(A2)已经做过了**

`apps/web/customer.js:1574`:
```js
return order?.statusText || ''          // 零回落律:后端没给就空着,不本地编一个
```
**订单状态句早就是后端唯一出口。** 这一刀我**不重做**,只加一条判据**守住别倒回去**。

### 活的违规只有一处,已改

| 位置 | 改前 | 改后 |
|---|---|---|
| `customer-copy.js` `paidDone`(付完定金的 toast,`customer.js:508/1415` 在用) | `定金已支付,预约已确认` | **`门店已确认收到定金,预约已确认`** |
| 同上 en | `Deposit paid. Booking confirmed.` | `The store has confirmed your deposit. Booking confirmed.` |
| `miniprogram/utils/i18n.js` `paid`(**零调用方**) | `已支付` / `Paid` | `门店已确认收款` / `Confirmed by store` |

### 判据(`test-display-text` 病四,5 条)

① 顾客可见面 5 个文件**零**「已支付/已付款」 · ② **反向守**:塞一句进去必须中 ·
③ **反向守**:注释里提到**不许**中(数执行不数提及)· ④ 覆盖面:这几个文件真读到了 ·
⑤ 🔴 **A2 那条不许倒回去**:订单状态句仍是 `order?.statusText || ''`。

## 5. 乙档第二批

⬜ **本批没做。** 上一批(第一批 8 处)刚立的棘轮是 **32 只许降**;
本批优先做完 §三 实看四项 + 10f,**队列按 10g §五 的顺序走,第二批排在推之后。**

## 6. 三行口径

```
本机全量:(见末尾)
GitHub CI:(推完填)
GitHub CI 未跑:
```
