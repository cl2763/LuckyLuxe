# 🟢 店主拍板:**推**。夜班令 14 **路甲激活**

**时间**:2026-09-18 夜(店主睡前)
**店主原话**:「**那就推**」

---

## 这是夜班令 14 §一 路甲的那一个字。路甲成立。

🔴 **但「推」不等于「随便推」。照 09u §二 那七步,一步不许跳:**

1. **CI 全绿** —— 整跑三档,四格照新口径写全;**小程序档按「真跑起来 0 · 绿 0 · 红 0 · 切掉 3」写,不许写成「红 0」。**
2. 🔴 **推前打快照 + 手动备份生产库,四条自查全过才往下走**:
   - 源路径 `/app/apps/api/local-data/lucky-luxe.sqlite`(已坐实,**不许从别处取**);
   - **只许 `VACUUM INTO`**,不许 `cp` / `copyFileSync`;
   - 🔴 **必须拉出容器落到本机**,报本机绝对路径与字节数;
   - 🔴 **打开验可读**:逐表点 `users` / `bookings` / `payments` / `user_identities` / `settlements` 五个数,**与生产那头同时读一次对上**。
   **四条报进 `夜14_备份回报.md`。任何一条不过 → 不推,转路乙。**
3. **推。**
4. **推后六项核验**:
   ① `/health` 200 且库域不是 ci/sandbox
   ② **生产容器里数 `/auth/google/demo` 路径 = 0** —— 🔴 **不许再打一次**(J-72)
   ③ 账本触发器逐名比
   ④ 两家真店五个数与**推前快照**零差异
   ⑤ **新容器 `/tmp/probe.js` 不存在**(同时是「容器真换了」的指纹)
   ⑥ **版本指纹两台都过**,不只验 200
5. 🔴 **任何一项不过 → 停、只读、写进待裁队列、继续做夜14 §三 后面的活。不自己修,不自己回滚。**
   **理由**:生产库 22 天没有新业务写入、两家真店没有真顾客在用、备份已在本机 —— **就算服务起不来挂一夜,实际代价接近零。不要为了「赶紧弄好」去动它。**
6. 🔴 **那 7 行压测残留不许顺手清。**
7. **推后第一次自动备份**:按你自己算出的那个节拍(**推的时刻向后第一个跨过 04:00 UTC 的 6 小时节拍**,不是固定 04:55),跑完**打开逐表对**。

---

## 其余照夜班令 14 不变

- 🔴 **`夜14_为什么停.md` 照写** —— 五档,不许写「其他」而不展开。**它比今晚任何技术结果都重要。**
- 心跳每半小时一行;**断超 40 分钟等于 E 档。**
- **整夜不许停在任何一件上超过 30 分钟。**
- 推完继续 §三 主线七件 + §四 兜底队列。
- 🔴 **停线一条不松**:不碰 Railway 任何子命令 · 只读进容器不产生持久化痕迹 · 只许 `SELECT` 且新查询先贴等批 · 不许松判据/抬棘轮/`--accept` · 不许改 Cowork 产出 · **网页顾客端加「注册/创建账号/新建顾客」一律停下来问。**

---

## 一件新的,给你知道

🟢 **Cowork 现在直接连着仓库了**(店主今晚批的)。我已经自己数过一遍:
`local-server.mjs` **17,613** · `admin.js` **7,928** · `customer.js` **2,458** · `origin/main` **285b20d** · 领先 **452** · `DEV_SCOPES = new Set(['ci','sandbox'])` · `legacy-demo-retire.mjs` 已删 · `data-scope.mjs` 在(7,519 字节)—— **八项全对上。**

🟡 **一处措辞收口**:你报「树干净」,而 `git status --porcelain` 有一行 `?? .claude/`。**不算错**,但今后「树干净」要说清是**「`--porcelain` 为空」**还是**「没有未提交的代码改动」**。

🔴 **这不改变任何规矩**:我读代码是为了核你报的数,**不改你的产出**(J-55②);`handoff/本地自查账号.txt` 与任何 `.env*` 我不读、不打印、不拷贝。
