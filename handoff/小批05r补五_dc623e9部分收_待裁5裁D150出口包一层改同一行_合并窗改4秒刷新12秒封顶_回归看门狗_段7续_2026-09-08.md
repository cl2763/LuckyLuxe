# 小批05r 补五 — `dc623e9` **部分收**(D151 落地、#4 并句、D152 半、占位)· 待裁 #5 裁:D150 **在出口包一层,改写就改流水里同一行**——不动骨架、不留两处真相 · 合并窗设计裁:**等完再答**不变,窗改 4 秒、来一句刷新一次、12 秒封顶 · 回归加看门狗 · 段 7 续:D150 → D152 端到端 → 六通 v4

> 2026-09-08 · Cowork 现查:`merge-window.mjs` 挂在 `handleWecomInbound` 入口(3384 行 superseded 分支);`/health.mergeWindowSeconds: 8`;`handleWecomInbound` 3384–3873 之间 **14 个 return**;`pages/ai-chat` 已不拿 `sending` 挡第二句;`discount-facts.mjs` 58 行;104 套 3,480 · 两处各一份

## 一、收

- D151 合并窗:五个进线口一起吃到、三句并一条按原文顺序入流水、窗长 `/health` 自报——**收**。回归里窗置 0、行为层由 `test-merge-window` 自起带窗实例——**收**(与 `perf-base-migration` 同法,关掉那里不等于没验)。
- #4 并句、`discount-facts.mjs` 事实注入(有券给券名 + 顺序,无券明写不许提券)、两处小的、`guestIdUnsigned` 占位——收。
- **D150 撤干净不留死码、六通 v4 不跑缺一条的样本**——两条都对,这两条比做成一半更值钱。
- 22 分钟才反应过来是自己开的窗——记 §四 看门狗,不记过。

## 二、待裁 #5 · 裁:**两条都不选——在出口包一层,改写就改流水里同一行**

你摆的两条:收敛 14 个 return(动骨架、砸 matrix 66 项)/ 挂在落库层(文本已入流水,替换出两处真相)。第三条:

1. `handleWecomInbound` 改名 `handleWecomInboundCore`,**原名变成一个薄壳**:`const r = await Core(inbound, req); return repeatGuard(r, inbound)`——14 个 return 一个不动,matrix 66 项不用打底。
2. `repeatGuard` 只在 `r.reply` 非空、且顾客这句与上一句被 `classifyTurn` 判为**同主题**时看:拿 `conversation_messages` 最近 N 条 AI 行(全录是唯一读口),
   - 与上一条 AI 行**同文**(去空白后相等)→ **换答法**:同一意图带「换角度」指令再生成一次(按类型 / 按预算区间 / 问他在意什么,三选一轮换);
   - 同主题**第三次**仍同文 → 回「这个我帮您接人工问问」并置 `needs_human`(这才是该转的点);
3. **改写就改同一行**:`r.reply` 落流水那行已经写了——`repeatGuard` 用同一 `message id` **UPDATE** 那一行文本,并在该行 `meta` 记 `rewrittenFrom`(原文留证)。这样「顾客看到的」与「流水里的」仍是一处真相,你担心的那个两处真相不存在。
4. 判据:① 六通套件通二三轮:三句同题 → 第二句与第一句**去空白后不相等**;第三句 → 状态 `needs_human` 且回复含「人工」;② 流水 AI 行文本 === 接口回给顾客的文本(三入口各一条);③ 报价路(早 return 那条)也过守——就拿现测「三问同一件事三次答同一句」那个用例做正例;造病:壳里去掉 `repeatGuard` → ①② 红。
5. 这层壳同时是 D153 的「出口只此一处」的自然落点——D155 的白名单刀改锚壳名,Core 不许被壳以外的地方调用(静态判据一条)。

## 三、合并窗设计 · 裁

「收到第一句后等 8 秒」是我 05q 写的;你问得对——**单句顾客也等 8 秒太久**。裁:**等完再答**这个形态不变(真人客服也是看完再答;「第一句立刻答、第二句到了才合并」会让第一条回复收不回来,合并就没意义),但参数改:**默认 4 秒**,窗内每来一句**刷新**一次,**12 秒封顶**(封顶到了先答手上的,再来的算下一窗);可配 2–10。`/health` 报三个数(窗、封顶、当前开着的窗数)。小程序窗开着时那一行显示「正在看你的消息…」而不是「正在输入」。判据:三句间隔 1 秒 → 一条;两句间隔 6 秒 → 两条(6 > 4);连发 5 句每隔 3 秒 → 12 秒封顶处切成两条;造病:封顶去掉 → 5 句只出一条 → 红。

## 四、回归看门狗(登口径)

整跑有基线(现 ~2 分);`run-all-tests.sh` 加**总时长看门狗:超过基线 2 倍(取 5 分钟)自杀**,打印「哪一套在跑、跑了多久」,回执里那句「22 分钟才反应过来」以后由脚本替你反应。

## 五、段 7 续(顺序)

D150(§二)→ D152 端到端(多轮夹具:问价 → 答「本甲」→ 三段式出现且数字对账;删券 → 只原价;造病:无券出「券后」→ 红;北京店造一张新客券,写库自报 + 备份)→ **六通 v4**(通五三句分开发间隔 1 秒;通六北京店新客券;通三必须无矛盾;文件 `05q_像人六通_原文_v4`)→ 我发店主打第二轮分。

## 六、交齐

D150 壳 + 三判据 + 造病 · 窗三参数 + 四判据 · 看门狗 · D152 多轮夹具三态 · 六通 v4 路径 · 提交号 · 回归/断言增量 · 整跑时长 · 棘轮两数 · 未推 main。
