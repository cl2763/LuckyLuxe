# 小批 11g · 🟢 代理这一侧通了 · 🔴 Railway 那张证书只剩 34 天,要单独盯 · 记功四笔
2026-09-21 · Cowork 出令

**上游令点名(J-81):** `小批11f…` `614cb139e4c9b0eb1e622d851f41bb33` · `小批11f补…` `63ab6e2d9b3fd4539df2692b8e4ce760`。

---

## 〇 先收最要紧的那一句

> **官网动前 200 · 装完 reload 后 200 · 签完证书 reload 后 200。**

**三次都量了,而且是按我写的顺序量的。没装 Caddy,官网那份配置一个字没碰。**
**这一件做对了,后面的都成立;这一件做错了,后面全部不算。收。**

---

## 一 🟢 代理接通,而你说的是「代理这一侧通了」不是「通了」—— 判得准

```
① commit 一致   api → ed93184 · Railway → ed93184            🟢
② 真证书        Let's Encrypt · curl 校验链 TLS 0(没加 -k)   🟢
③ 境内实访      ⛔ 你验不了(在境外链路上)
④ 小程序跑通    🟡 API_BASE 改了,那一次还没跑
```
🟢 **「③④ 没过,所以我不说『通了』,说『代理这一侧通了』」—— 这一句是 J-95/J-108 长在身上了。**

🟢 **而你自己加的那两条比令里写的更有说服力:**
- `/platform` · `/sandbox` · `/wechat-simulator` **全 404** —— 不该转的真没转
- `/stores` **400**(要店号)· `/my` **401**(要登录) —— 🔴 **「400/401 正是真打到后端了的证据」,这句话说得比 200 更硬**:200 可能是任何一层给的,**400/401 是后端的业务逻辑在说话。**

---

## 二 🟢 转发口径:你那句话我收下,并写进台账

> 「后端**今天已经整个在公网上**(`www.luckyluxeatelier.com`),代理加的是**一条境内路由,不是新的暴露面** —— 所以『只转必要路径』在安全上收益有限,在漏转上风险不小。」

**对。而你的处理方式比两头都好:** 仍按令做白名单(26 个前缀**从路由定义量出来,不凭记忆**),**但名单外 404 并单独记日志** —— **把漏转从「悄悄坏掉」变成「一行看得见的日志」。**
**这正是 J-86 的正面用法:与其让它安静地失败,不如让它出声。**

---

## 三 🟢 那条红线:一句话结论,而且是现跑的

> **全站不存在任何「不登录就能写 `users` / `user_identities`」的路。** `test-user-write-auth` **11/11 现跑**,不是引用旧结论。

**「这次是真对外开门,所以重跑」—— 判得对。** 一个上周成立的结论,在开门这一刻要重新量一次。

---

## 四 🔴 你顺手看见的那一条,比这一批任何一件都急

```
官网 api.jingshengyouji.com     89 天
官网 www.jingshengyouji.com     86 天
🔴 www.luckyluxeatelier.com     34 天   ← Railway 自管
```

**`www.luckyluxeatelier.com` 是生产后端的地址。它 34 天后到期,而我们对它一无所知。**

**要你查三件(只查不动):**
1. **Railway 那张证书是不是自动续期** —— **在哪看、怎么看,写清楚**(J-83:先证明这个机制在那台机器上存在)
2. **续期失败会不会有通知,通知到谁**
3. 🔴 **如果查不出「它会自动续」的证据 ⇒ 当成「不会」处理**,把它写进推前清单和夜跑告警,并报我

**理由**:代理这条链是 `小程序 → api.jingshengyouji.com(北京) → www.luckyluxeatelier.com(Railway)`。
**后面那一段的证书挂了,前面接得再漂亮也没用。**

🟢 告警那九条已经进回归(剩 20 天必报 · 剩 22 天不许报 · **取不到也必须报**)—— **「取不到也必须报」这一条最要紧,它治的正是 J-86。**

---

## 五 ⛔ 绑邮箱:仍然不许猜,我去问店主

令里写死「邮箱由店主给」,**你没做,对。** 我已经去问她了,她给了我再转给你。

---

## 六 记功四笔

1. **官网三次量,按顺序量** —— 这一件是整批的地基。
2. **「400/401 正是真打到后端了的证据」** —— 比拿 200 当证据硬一个量级。
3. **白名单从路由定义量出来 + 名单外 404 记日志** —— 既守了令,又把令的副作用变成可见的。
4. 🟢 **两处与令不同,你都量过并纠正,而且「一件都没让她重做」** —— **这就是 J-110 落地的样子**(`api` 解析她早点完了 · 公安备案她早在办)。

---

## 七 接下来

1. **等店主三件**:境内实访 · 邮箱 · 微信后台填合法域名(**按顺序,实访不通不许填**)
2. **§四 Railway 证书三查**(只查不动)
3. **《有迹商家使用手册》通用版** ← 仍是最要紧的文档件
4. **后台盘点表**(三列,状态只许三种)
5. 提审前置清单 · 小婕开张单 · `tenants` 主体列实现计划
6. D207-b 那 8 行:彩排 → 报我 → 批 → 执行

🔴 **AI 修图仍一个字别碰。**

---

## 八 回执

首行点名本令文件名 + md5。**§四 那三查,查不出证据就明写「查不出」,不许写成「应该会自动续」。**

**停线一个字没松。**
