# 夜班令 3(替代夜班令 2)— 约六小时任务量:收热修 → 提速三刀 → 新店 luvia-bj → D145 + 五通 v3 → **主页重画批开工(合同 v3.1)** · 店主明早统一审

> 2026-09-07 夜 · 店主要睡了,今晚不审、明早统一审(待审页 + 五通 v3 + 主页首段截图)· Cowork 代拟 · 两处各一份:桌面交接夹 + 仓内 `handoff/`
> 一句话:前半夜把 05o 做完,后半夜开主页重画——合同是 `handoff/商家端主页重画_两端设计图_2026-09-03.html`(v3.1,店主 09-03 点头,两处 md5 一致),今晚做到网页后台首页能看为止。总时长按 **六小时**规划,到 5.5 小时不再开新段,写总结。

---

## 〇、时间表(按段计,做完一段就提交、写一行到总结)

| 段 | 内容 | 预算 | 提交 |
|---|---|---|---|
| 0 | 收 Cowork 待审页热修(夜班令 2 §〇原文照做:读 diff → 认可就提交 → 三条判据) | 15 分 | `05o-0` |
| 1 | 05o §三 提速三刀:预检脚本 / 批内相关套件 / 每套计时 | 40 分 | `05o-1` |
| 2 | 05o §一 新店 `luvia-bj`「LUVIA 半径美甲美睫 · 北京旗舰店」两库建店 + mock + 三店判据;两店各跑改三店 | 60 分 | `05o-2` |
| 3 | 05o §二 D145 `classifyTurn` + 五通常驻套件 + 造病;五通 v3 + 到底率三跑(两尺)+ 店主读版 v3(三店) | 75 分 | `05o-3` |
| 4 | 05o 小件:作废三份抬头、删仓根 `db-snapshot.json`、备份刀指向统一;**第一次全量**(计时那行第一次出现) | 20 分 | `05o-4` |
| 5 | **主页重画批 第一段**(§一 下):口径两问现测作答 → §四 三接口 `pulse / now / todo` + 判据 | 90 分 | `06a` |
| 6 | 主页重画 第二段:§三 **网页后台首页**替代现 Dashboard(新文件 `apps/web/dashboard-home.js`,`admin.js` 不长)+ 三态 + 截图 & DOM 证据;**第二次全量** | 60 分 | `06b` |
| — | 到 5.5 小时:停止开新段;`夜班总结_2026-09-08.md` | 10 分 | — |

小程序两视角(§一/§二 打卡门)与全屏态(§五)**留明天**,今晚不碰 `miniprogram/`。

---

## 一、主页重画批 · 开工令(合同 = 图 v3.1,不复述图)

- **先答口径两问**(图 §四末):①充值/购卡收款在账本里记的 `category`/`pay_channel`;②次卡消耗记在哪张表、有没有能落到「当期」的时间戳。**现测答,不许猜**;答不上的指标那一格不上、不许 0 顶。
- **三接口**进独立模块 `apps/api/dashboard-pulse.mjs`(`local-server.mjs` 不长,棘轮 17,751):`GET /admin/dashboard/pulse?period=today|week|month|year`(六指标、`spark` 7|12、`deltaPct` prev=0 → null、`locked` 时不下发 value、员工身份**后端裁字段**)· `GET /admin/dashboard/now`(与今日台面同源函数,不另算)· `GET /admin/dashboard/todo`(五项固定,0 也返回)。
- **判据照图 §七逐条**,每条先造病验红;重点四条:大屏今日营收 === 日结页 === 财务页(同一夹具三处比);已日结日 === `daily_closes.revenue_cents`;`bookings.value` 与副行三数 === 今日台面同一时刻;员工身份响应体不含店级 metrics。**三店各跑**(luvia-bj 看 ¥ 与北京时间)。
- **网页首页**(§三):同一份 pulse 横着摆;左「此刻」四格 + 「下一位」卡;右四小牌 + AI 一句;下左**现有今日台面原样嵌入**、下右今日要处理;旧「本月经营 / 本月账本收入 / 收入解锁 / 来源」退役;右上「⤢ 全屏大屏」按钮**先只占位不做**(§五 明天)。三态(加载/无数据/失败)+ 休息日各自真话节点、互斥。
- **截图**:老板 × 四维度 + 三态 + 休息日,每张附 DOM 证据;登录用本地走查账号(从 `handoff/本地自查账号.txt` 读,不进代码/回执)。
- 不做:图表页、导出、自定义指标、快捷格、多店汇总、首页轮询。

---

## 二、D145 的锚(店主今晚不打分,用 Cowork 05o §二 那张表当锚)

五通常驻套件的每轮断言就按 05o §二 表「该怎样」那一列写;明早店主读 v3 打分,分数再回流成第二轮锚。

## 三、停线

写 4128 既有行停(新店只 INSERT);四红线修法不明停;沙箱模型今晚增量 >600 万 token 停;回归红 30 分钟定位不到停、不改判据凑绿;**全量回归今晚最多 2 次**(段 4、段 6),预检不算;不推 main、不碰生产、不看密钥文件;不碰 `miniprogram/`。任何一段超预算 1 倍,停那一段写原因,跳下一段。

## 四、早上交

`夜班总结_2026-09-08.md` 六行:做到哪一段 · 各段提交号 · 停在哪 · token 增量 · 全量跑了几次各多少分钟 · 明早店主要看的三样(待审页 / 五通 v3 / 网页新首页)各怎么打开。回执按段可合并。
