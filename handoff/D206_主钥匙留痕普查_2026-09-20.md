# D206 普查清单 · 主钥匙那条路的留痕 —— **只列不改**(10c §二)

> 2026-09-20 · 扫描面 **130 个非测试模块** · 命中「取 `adminSession` 身份字段」的行 **72 行**
> 判据地基(现读 `local-server.mjs:1349`):`OWNER_TOKEN` 分支回的是
> `{ role:'owner', provider:'demo-token', technicianId:null, tenantId }` —— **没有 `id`/`email`/`displayName`/`name`/`username`**。

---

## 🔴 先纠一件事:D206 立案时说的「留痕都会是空的」—— **量出来不是这样**

10c §二 的描述是从我 10b 那个 `voided_by` 空串推过来的。**我先量了两笔**:

```
主钥匙 PUT /admin/kb/facts  → tenant_kb_facts.updated_by = "owner"      ← 不是空串
主钥匙 POST …/signed-docs   → signed_docs.created_by     = "平台主钥匙"  ← 我改过 actorOf 的那条
```

全仓主流写法是 **`adminSession.email || 'owner'`**(49 处),所以主钥匙落库的是**字符串 `'owner'`**。
**我 10b 那处之所以是空串,是因为我自己写了 `|| ''`** —— 那是我的写法,不是全仓的写法。

⇒ **真正的病不是「空」,是「说不出是哪一个人」。** 面比「空」那一档大得多,但性质轻一级。
(J-88:先量再报;这一条如果照着「37 处全是空的」报上去,就是把自己的一个笔误说成了平台级事故。)

---

## 三档分类(按主钥匙走这条路时**实际落库的值**分)

| 档 | 落库是什么 | 处数 | 性质 |
|---|---|---|---|
| **甲** | **空串 / undefined** | **4** | 🔴 真的记不下是谁 |
| **乙** | 一个角色词(`'owner'` / `'staff'` / `'admin'` …) | **67** | 🟠 知道是"某个老板",**不知道是哪一个人** |
| **丙** | 已走 `actorOf`(说得出就写谁,说不出就抛错) | **1**(签署留档,10b 改的) | ✅ |

---

## 🔴 甲档四处 —— **其中两处是钱**

| # | 位置 | 写的是什么 | 表达式 |
|---|---|---|---|
| 1 | 🔴 `store-content-routes.mjs:68` | **现金手记 `createdBy`** —— 抽屉里钞票多了少了 | `adminSession.username \|\| adminSession.displayName \|\| ''` |
| 2 | 🔴 `store-content-routes.mjs:75` | **现金手记冲销 `createdBy`** | `adminSession.username \|\| ''` |
| 3 | `local-server.mjs:14485` | 服务小记的 `technician_name`(前面两级兜底通常兜得住) | `(tech && tech.name) \|\| body.technicianName \|\| (adminSession.email \|\| '')` |
| 4 | `local-server.mjs:2100` | AI 复核标记的操作人 | `adminSession?.email \|\| ''` |

**第 1、2 处是这张表上最要紧的两行**:现金手记是**抽屉账**,
按《一个动作一个入口律》它专管「抽屉里的钞票多了还是少了」——
**一笔说不出是谁记的现金调整,在盘点差异里等于没记。**

---

## 🟠 乙档 67 处 —— 按兜底字面量分组

| 兜底值 | 处数 | 涉及文件 |
|---|---|---|
| `'owner'` | **49** | `local-server.mjs`(37)· `kb-routes.mjs`(5)· `refund-routes.mjs`(3)· `business-hours-routes.mjs` · `conversation-routes.mjs` · `finance-reverse.mjs` · `store-content-routes.mjs` |
| `'staff'` | 8 | `local-server.mjs` 8348 / 8411 / 8419 / 11551 / 11553 / 11786 / 11808 / 15444 |
| `'admin'` | 7 | `local-server.mjs` 14487 / 15205 / 15223 / 15285 / 15640 / 15656 / 15681 |
| `'simulator'` | 1 | `local-server.mjs:2171` |
| `'manual_staff'` | 1 | `local-server.mjs:4064` |
| `'员工'` | 1 | `local-server.mjs:4149` |

**乙档里值得单独看一眼的**(都动钱或动账本):
- `finance-reverse.mjs:32` —— **冲销**的操作人
- `refund-routes.mjs:22 / 48 / 82` —— **退款**三处
- `local-server.mjs:9530`(业绩分配)· `9587 / 9592 / 9619`(日结确认 / 重开)· `14868 / 14907 / 14910`(工资锁定 / 发放)

---

## 逐行明细

附 `/tmp/d206-raw.txt` 的同一份输出已并入本文件上面两张表;
复跑命令(扫描器在 scratchpad,不入库):`node <scratchpad>/d206-scan.mjs`

---

## 🔴 我不改,但要先说清一件事:改法不止一种

1. **最小**:甲档 4 处改走一个 `actorOf` 式出口(说得出写谁,说不出抛错)。
2. **中**:乙档也改 —— 但要先回答「**主钥匙到底该署名成什么**」。
   我在签署留档里用的是 **`'平台主钥匙'`**,它比 `'owner'` 准(它说的是**哪条路**,不是**哪个角色**)。
3. **根治**:`requireAdmin` 的 `OWNER_TOKEN` 分支**本身就该带一个身份**(例如 `id:'platform-owner-token'`),
   这样 72 处一行不改就都对了。**这一条最省事,但它改的是认证层,要你点头。**

**三条我都没做**,按 10c §二「先只要清单,不要改」。


---

# 10d §三 分级(J-105 立后追加)

> **J-105**:留痕里的「谁」,**在涉钱的路径上必须是一个人,不能是一个角色**。
> 「`owner`」回答的是「用哪把钥匙」,不是「哪个人」。**出了纠纷要找的是人。**

## 甲档四处 —— 🟢 **两处钱已修,已现测**

| 位置 | 改前 | 改后(主钥匙现测落库值) |
|---|---|---|
| `store-content-routes.mjs` 现金手记**建单** | `adminSession.username \|\| adminSession.displayName \|\| ''` → **空串** | ✅ **`平台主钥匙`** |
| `store-content-routes.mjs` 现金手记**冲销** | `adminSession.username \|\| ''` → **空串** | ✅ **`平台主钥匙`** |
| `store-content-routes.mjs`「**记一笔**」 | `adminSession.email \|\| 'owner'`(乙档,但涉钱) | ✅ **`平台主钥匙`** |
| `local-server.mjs:14485` 小记 `technician_name` | 前两级兜底通常兜得住 | ⬜ 未改(不涉钱,排队) |
| `local-server.mjs:2100` AI 复核标记 | `adminSession?.email \|\| ''` | ⬜ 未改(不涉钱,排队) |

**做法:`actorOf` 抽成唯一出口 `apps/api/actor-name.mjs`**(10b 我在 `signed-docs.mjs` 里写过一份,
这次抽出来,**不并排留第二份** —— 判据 ④c6 盯着「全仓 `function actorOf` 只许有一处」)。

## 乙档 66 处分级(扫描器:`scratchpad/d206-grade.mjs`)

```
乙档合计 66 处 → 🔴 涉钱 40 处 · 不涉钱 26 处

🔴 涉钱(按 J-105 必须改成一个人):
   local-server.mjs  [coupon_grants]  5 处: 8779, 12462, 12609, 12610, 12631
   local-server.mjs  [settlements]  3 处: 8411, 8419, 11786
   local-server.mjs  [daily_closes]  3 处: 9587, 9592, 9619
   local-server.mjs  [salary_payrolls]  3 处: 14868, 14907, 14910
   refund-routes.mjs  [operator: adminSession.email ]  3 处: 22, 48, 82
   local-server.mjs  [member_timecards]  2 处: 8754, 8763
   local-server.mjs  [createdBy: adminSession.email ]  2 处: 14176, 15504
   finance-reverse.mjs  [createdBy: adminSession.email ]  1 处: 32
   local-server.mjs  [booking_status_history]  1 处: 8348
   local-server.mjs  [settlement_amendments]  1 处: 8741
   local-server.mjs  [points_transactions]  1 处: 8770
   local-server.mjs  [settlement_technicians]  1 处: 9530
   local-server.mjs  [return json(res, 200, setSettlementCoupon(id, ]  1 处: 11808
   local-server.mjs  [adminSession.email ]  1 处: 11901
   local-server.mjs  [technician_schedules]  1 处: 13410
   local-server.mjs  [const svOperator = adminSession.email ]  1 处: 13661
   local-server.mjs  [finance_targets]  1 处: 14050
   local-server.mjs  [staff_compensation]  1 处: 14135
   local-server.mjs  [finance_recurring_rules]  1 处: 14238
   local-server.mjs  [actor: adminSession.email ]  1 处: 15223
   local-server.mjs  [const out = disposeNoShowDeposit({ booking, ac]  1 处: 15242
   local-server.mjs  [return json(res, 200, revokeNoShowDisposal({ b]  1 处: 15258
   local-server.mjs  [return json(res, 200, revokeDepositReceipt({ b]  1 处: 15267
   local-server.mjs  [const { token, expiresAt } = issueSignToken(ro]  1 处: 15444
   local-server.mjs  [actorEmail: adminSession.email ]  1 处: 15640
   local-server.mjs  [bookings]  1 处: 15656

🟠 不涉钱(可落角色词,但必须是明写常量):
   kb-routes.mjs  [tenant_kb_entries]  3 处: 93, 128, 152
   local-server.mjs  [quote_requests]  2 处: 11551, 11553
   local-server.mjs  [plan_change_requests]  2 处: 13948, 14300
   local-server.mjs  [stores]  2 处: 14351, 14352
   local-server.mjs  [staff_nudges]  2 处: 15142, 15147
   business-hours-routes.mjs  [business_hours]  1 处: 62
   conversation-routes.mjs  [platform_ops_log]  1 处: 39
   kb-routes.mjs  [tenant_kb_facts]  1 处: 80
   kb-routes.mjs  [tenant_kb_documents]  1 处: 109
   local-server.mjs  [createdBy: adminSession?.email ]  1 处: 2171
   local-server.mjs  [ai_learning_examples]  1 处: 4064
   local-server.mjs  [wechat_conversations]  1 处: 4129
   local-server.mjs  [: { humanReleasedAt: now, humanReleasedBy: adm]  1 处: 4130
   local-server.mjs  [staffName: body.staffName ]  1 处: 4149
   local-server.mjs  [tenant_entitlements]  1 处: 14321
   local-server.mjs  [service_notes]  1 处: 14487
   local-server.mjs  [attendance_records]  1 处: 15102
   local-server.mjs  [bookings]  1 处: 15205
   local-server.mjs  [archive_bind_tokens]  1 处: 15285
   local-server.mjs  [booking_status_history]  1 处: 15681
```

### 口径建议(等你裁)

- 🔴 **涉钱 40 处**:按 J-105 全部改走 `actorOf`。**其中我建议先做这几族** ——
  `refund-routes`(退款 3)· `finance-reverse`(冲销 1)· `daily_closes`(日结确认/重开 3)·
  `salary_payrolls`(工资锁定/发放 3)· `settlement_*`(签署/更正/分配 5)。**这几族出纠纷时最要紧。**
- 🟠 **不涉钱 26 处**:保留角色词,但要求**明写常量**(现状已经是),**不许退回 `|| ''`**。
  建一条常驻断言守住「不涉钱那一档只许是明写常量」即可,不必逐处改。

### 🔴 但最省事的那条路仍然是第三条(10c 已提,10d 未裁)

**`requireAdmin` 的 `OWNER_TOKEN` 分支本身带一个身份**(例如 `id:'platform-owner-token'`),
**70 处一行不改就都对了。** 它改的是认证层,要你点头。
