# 原生弹窗全仓清单(D50 全扫 · 2026-08-17,交 Cowork 归批)

> 会员与营销页已全部返工为页内表单弹层(openFormModal,D50 当批);下表=剩余 31 处存量,标注用途与建议归批。
> CI ㊶ 已锁增量:admin.js 原生弹窗总数 ≤ 31,只减不增;新增代码禁 prompt/confirm/alert。

| 行号 | 类型 | 用途(上下文摘录) | 建议归批 |
|---|---|---|---|
| 2649 | prompt | const from = window.prompt(owner.lang === 'zh' ? '从哪个月开始?(格式 2026-01)' : 'From m | 其他 → S4/S5(网页后台波,出图时统一弹层化) |
| 5499 | prompt | const note = window.prompt(zh | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5531 | confirm | const ok = window.confirm(zh | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5567 | confirm | if (!window.confirm(zh ? '把本周每位技师的上/休模式复制到未来 4 周?(会覆盖那几周已有的排班)' : 'Copy this wee | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5585 | prompt | const name = window.prompt(zh ? '技师姓名:' : 'Technician name:') | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5587 | prompt | const title = window.prompt(zh ? '职称(可留空,例如:美甲师/美睫师):' : 'Title (optional):') \|\| | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5669 | prompt | window.prompt( | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5685 | prompt | const name = window.prompt(zh ? '技师姓名:' : 'Name:', tech.name) | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5687 | prompt | const title = window.prompt(zh ? '职称:' : 'Title:', tech.title \|\| '') | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5699 | confirm | if (nowActive && !window.confirm(zh ? `停用「${tech.name}」?停用后不再接受新预约,历史数据保留。` : `D | 排班/技师管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 5953 | prompt | if (!copied) window.prompt(zh ? '自动复制被浏览器拦截,请手动复制:' : 'Copy manually:', msg) | AI图库/复制兜底 → S4/S5(网页后台波,出图时统一弹层化) |
| 6558 | prompt | const pick = window.prompt(`处置爽约定金 ${dd.outstandingText}(${bk.user?.displayName  | 售后/爽约处置 → 待办11 家族(店主早已点名,优先) |
| 6560 | prompt | const note = window.prompt('备注(选填)', '') \|\| '' | 售后/爽约处置 → 待办11 家族(店主早已点名,优先) |
| 6576 | prompt | const act = window.prompt('售后处理:输入 1 = 写进展;2 = 标记已解决(需处理结果);3 = 关闭(仅老板,需原因)', '1 | 售后/爽约处置 → 待办11 家族(店主早已点名,优先) |
| 6580 | prompt | const text = window.prompt(label, '') | 售后/爽约处置 → 待办11 家族(店主早已点名,优先) |
| 7180 | prompt | const input = window.prompt( | 员工账号 → S4/S5(网页后台波,出图时统一弹层化) |
| 7194 | confirm | if (!window.confirm(owner.lang === 'zh' ? '重置该员工的登录密码?旧密码立即失效。' : 'Reset this pa | 员工账号 → S4/S5(网页后台波,出图时统一弹层化) |
| 7391 | prompt | const reason = window.prompt(owner.lang === 'zh' ? '重开日结必须写原因(会留痕):' : 'Reason ( | 财务/日结 → S4/S5(网页后台波,出图时统一弹层化) |
| 7616 | confirm | const confirmed = window.confirm(owner.lang === 'zh' | 财务/日结 → S4/S5(网页后台波,出图时统一弹层化) |
| 8595 | confirm | if (!window.confirm(`申请把套餐变更为「${name}」？\n提交后我们会联系你确认功能与费用，确认前现有服务不受影响。`)) return | 其他 → S4/S5(网页后台波,出图时统一弹层化) |
| 8605 | confirm | if (!window.confirm('AI 智能包需要按你门店的项目、价格和话术做一次配置。\n提交申请后我们会尽快联系你，配置完成即开通，试用期 3 个月 | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8636 | confirm | if (window.confirm(`模拟支付 ${subMoney(r.order.amountCents)}${kind === 'ai' ? ' 开通  | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8641 | alert | window.alert(`订单 ${subMoney(r.order.amountCents)} 已创建，平台确认收款后自动${kind === 'ai' ? | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8934 | prompt | const name = window.prompt(pzh() ? '大类名称(如:美甲单色)' : 'Category name') | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8936 | prompt | const key = window.prompt(pzh() ? '英文标识(小写字母/数字/短横,如 nail_solid;卸甲类请用 removal)'  | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8945 | prompt | const name = window.prompt(pzh() ? '大类名称' : 'Category name', cat.name) | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 8954 | confirm | if (!window.confirm(pzh() ? `删除大类「${cat.name}」?` : `Delete ${cat.name}?`)) retur | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 9000 | confirm | if (!window.confirm(pzh() ? `删除「${item.nameZh}」?有历史订单的项目会自动改为下架。` : `Delete ${it | 价目表管理 → S4/S5(网页后台波,出图时统一弹层化) |
| 9121 | prompt | const amount = window.prompt(pzh() ? '充值金额' : 'Recharge amount', '1000') | 其他 → S4/S5(网页后台波,出图时统一弹层化) |
| 9123 | prompt | const giftPct = window.prompt(pzh() ? '赠送比例(%,留空=不送)' : 'Gift percent', '') | 其他 → S4/S5(网页后台波,出图时统一弹层化) |
| 9138 | confirm | if (!window.confirm(pzh() ? '删除这个充值档位?' : 'Delete this tier?')) return | 其他 → S4/S5(网页后台波,出图时统一弹层化) |

注:6576-6580 售后处理「输入 1/2/3」链=店主待办11 点名的同类,建议优先;5953 为复制失败的降级提示(prompt 当展示框用),可改 toast+可选文本。