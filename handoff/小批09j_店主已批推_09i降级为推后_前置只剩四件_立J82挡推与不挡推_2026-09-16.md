# 小批 09j · 🟢 **店主已批准推 main** · 09i 整体降级为「推后清单」· 推前只剩四件 · 立 J-82

**日期**:2026-09-16 · **发件**:Cowork · **收件**:Code
**上游令**:本令 + 09i(md5 `fa3fed3e2cc1db88f38e6c967f8112f5`)。**按 J-81,回执首行点名这两份到手与否。**

---

## 〇 店主原话与我的认错

> 「我觉得没问题了就可以 push,所以现在给 code 的 09i 是要干嘛用的?」

**她问得对,而且戳中的是我的毛病,不是她理解错。**

🔴 **记我第 25 笔** —— **09i 我是按「这一批查出了什么」组织的,不是按「哪几条挡在推之前」组织的。**
我把**产品的毛病**和**推这个动作本身的风险**混进了同一张单子,于是那张单子读起来像是「又多了一堆事,还是不能推」。
**她昨天刚问过我「为什么一直要等我批」。这是同一个毛病的第二次出现 —— 我在制造一种「永远还差一件」的感觉。**
**我自己立的 J-77③ 写着:每条停线必须写明停到哪一层。09i 里每条停线我都写了层,但我没有对整份令做分层。**

> **J-82(立)· 任何一份令,凡涉及一个待批准的动作(推 main、上线、对外开放),必须在开头把条目分成两栏:「挡这个动作的」与「不挡、推后做的」。**
> **判别式只有一句:🔴 做了这个动作,这一条会不会变坏?** 会 → 挡;不会 → 不挡,哪怕它本身很严重。
> **一条很严重但推了也不变坏的缺陷,不许挡推** —— 因为挡着它,真正会变坏的那件(D194 在生产上开着)每多一天就多一天。

---

## 一 🟢 分栏结论

### **不挡推(09i 绝大部分,推完再做)**

- **D200 那道没接上的门** —— **推不推它都一样开着**(接线状态不变),**而推了会顺手把 D194 关掉。净赚。** D200 的停线照旧只停「对外开放 / 接通境内代理 / 备案域名指到后端」,**不停推。**
- **底数 58→60 那 2 个点名 · 主档 125 不动举证 · 密钥类 8 处逐处点名** —— 这些变量**生产上已经是那个样子**,字面量回落**推前推后都在代码里**,推这个动作不改变其中任何一条。
- **边界外(`.prisma` / `.env.example` / Dockerfile / `package.json` scripts)第二把刀** —— 不挡(`railway.json` 除外,见下)。
- **J-81 · 我的第 23/24/25 笔** —— 记账。

### 🔴 **挡推(只剩四件,每一件都在回答「推这个动作会不会弄坏什么」)**

---

## 二 前置四件

### 🔴 件 1 · 备份路径坐实到 `lsof` 那一层(= 09i 退回一,唯一一条原地不动的)

**这是四件里最重的,理由一句话:备份是唯一的回头路,备份做错了这次推 main 就没法回滚。**
**二选一(09i 原文不变)**:
- (a) 证明 `/health` 的 `dbFile.path` 是从**实际打开的句柄**推出来的、不是配置回显 —— 给 `文件:行`;
- (b) 取到路径后在同一容器里用 `lsof` 对一次,**不一致就停、不备份、不推**。

### 🔴 件 2 · 库的落点,推前推后是不是同一个

**不是问「`DATA_DIR` 不设会怎样」**(它已经是设了或没设,推不改变它)。**是问**:
1. **旧代码(`origin/main = 285b20d`)决定库路径的那段** vs **要推的这份**,**是不是同一套逻辑、同一个变量名、同一个回落?**
   🔴 **如果这 418 个提交里改动过库路径的算法 —— 那么推上去的一瞬间,服务可能去开另一个文件。** 这是推这个动作**唯一**能直接弄丢数据的路子。
2. 差集报出来:**旧 → 新,读的变量名、拼法、回落,逐项对照。** 一致 → 一句「一致」+ 两边 `文件:行`;不一致 → **停,先报我。**
3. 店主侧:`DATA_DIR` **设了 / 没设**(我已去问)。

### 🔴 件 3 · `railway.json` 在这 418 个提交里改了没有

它钉死了线上跑什么命令(`node … local-server.mjs`),**是你自己引用的**。
**报**:`git diff 285b20d..HEAD -- railway.json`(以及 `Dockerfile` / `package.json` 的 `start` 脚本,如果存在)——**有改动就逐行报,没改动就报「零差异」。**
**理由**:启动命令变了 = 推上去跑的根本不是我们验的那个东西。**这是最便宜的一件,几秒钟。**

### 🔴🔴 件 4 · 门禁接线的推前推后差集 —— **这一件是我 09i 该问而没问的**

D200 让我意识到一个更一般的问题,**而这个问题才是推 main 真正的安全问题**:

> 🔴 **这 418 个提交里,有没有任何一个口,从「有门」变成了「没门」?**

D194 是**删掉一条没门的路**(好事)。**但没有人查过反方向。**
**要求**:
1. 列出**推前(285b20d)**所有鉴权/门禁中间件的**接线点**(哪个中间件挂在哪条路由上);
2. 列出**推后(HEAD)**的同一张表;
3. 🔴 **报差集,三栏**:**新增了门的** · **没变的** · **🔴 门没了的**;
4. **「门没了的」哪怕 1 条,立刻停、报我,不自己修。**
5. 扫描面照 J-65③④ 报(扫了几个文件 / 按 import 可达性)。
6. **D200 是这张表里的一行**,不是单独一件 —— 但要点名它推前推后的状态(**如果生产那份是接上的、本地这份没接上,那就是「门没了」,件 4 直接红,推停**)。

🔴 **这一件我给你时间,不要为了快而缩面。** 它是四件里唯一可能把「推」变成「不推」的。

---

## 三 四件做完的动作(照 09f §四不变)

1. CI 全绿;
2. 🔴 **推前打快照 + 手动备份生产库**(路径按件 1 坐实的那条,写绝对路径);
3. 推;
4. **四项核验**:①`/health` 200 且库域不是 ci/sandbox ②**生产容器里数 `/auth/google/demo` 路径 = 0**(**不许再打一次**,J-72)③账本触发器逐名比 ④两家真店五个数与**推前快照**零差异;
5. 🔴 **任何一项不过 → 立刻停、报我、不自己修、不自己回滚**(回滚也是动作,要我批);
6. 🔴 **那 7 行压测残留不许顺手清。**

---

## 四 停线

- 🔴 **件 1–4 任何一件红 → 停推,报我。** 其余照常。
- 🔴 **Railway 一个字不许碰**(读、写、核对,一次都不许)。
- 🔴 **不许向生产发任何会写入的请求**(`GET /health` 可)。
- 🔴 **店主已批「四件过了就推」,但她还有一个动作没做完**(撤销那处 `DATABASE_URL` 暂存删除 + 报 `DATA_DIR`)。**她那两件没回之前,不许推。** 我会转达,**你不许替她判断这两件不重要。**

---

## 五 回执要求

1. **首行按 J-81 点名上游令 + md5。**
2. **件 1–4 逐件,每件带证据与扫描面。四件全绿 → 明说「四件全绿,等店主那两件」。**
3. **09i 其余条目不必在本批回答**,列一张「推后待办」清单即可(名字 + 一句话),**推完再逐条做。**
4. 🔴 **仍然:一个 push 都不许执行,直到我说「四件全绿 + 店主两件已回」。**
