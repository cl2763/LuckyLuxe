# 小批 11m —— 店主六答落地 · 多伦多测试单预批判别式 · 北京店永久 · AI 修图入口三态

**出令时刻**:2026-09-22
**前置**:`小批11l` · 回执提交 `874d188` · 店主 2026-09-22 六答(原文见 §〇)

---

## §〇 店主原话(2026-09-22,逐条)

| 问 | 答 |
|---|---|
| 「Lucky Member」是不是你测试用的微信 | **应该是测试用的微信** |
| `LL-I28970PU` 是真顾客吗 | **不是真顾客** |
| (她补的一句)| 🔴 **「这里面没有任何真顾客的下单,全都是我测试用的」** |
| 北京旗舰店要不要订阅到期日 | **不要,做成永久的** |
| AI 修图入口放「今日要处理」第一张 | **可以** |
| 三态开关由平台后台控 | **可以**;提审带哪个态 **先看修图反馈** |
| 技师看得到剩余张数 | **合适** |
| 第二入口第一版不做 | **同意** |
| `avalin` 留不留 / 正门缺 currency 报 400 还是默认 | **没答** —— 继续挂等天亮,今晚不动 |

### 🔴 那句「这里面没有任何真顾客」我怎么用(J-109 · 错 #42)

她这句话是在回答「Lucky Member 和 LL-I28970PU」时说的,**「这里面」指的是多伦多店那 161 + 68 单**。
**我不把它放大成「多伦多店整库清空」。** 顾客档案(`users`)、技师、作品、储值、积分 —— **一样都不在这句授权里。**
今晚只动**单**,而且只动**她看得见的那两批**。

---

## §一 多伦多店:两批测试单 —— 预批判别式,全中即已批

### 要删的集合 S

S = (三位假技师 Mia Chen / Ava Lin / Lina Zhou 名下的 161 单) ∪ (`appointment_start >= 2030-01-01` 的 68 单),**按 `booking.id` 去重**。
连带:每单的 `booking_slots` / `booking_status_history` / `payments`(**和 D207-b 同四张表,不多一张**)。

| # | 条件 | 不成立怎么办 |
|---|---|---|
| 1 | 独立备份 `VACUUM INTO`,文件名日期 `date -u` 量 | — |
| 2 | 只读:S 的 id 全列出来存档;**`|S|` 贴出来**(161 + 68 去重后是多少,我不猜) | — |
| 3 | 只读:S 里每一单的顾客 ∈ { Lucky Member 的 user_id, `user_mt1iifi2_8970pu`, **或未绑微信** } —— 🔴 **有一单的顾客绑了微信且不是这两个人 ⇒ 不批,停这一件,把那个人贴出来** | 停,登记 |
| 4 | 只读:`settlements` / `finance_transactions` 指向 S 的 = 0;**不是 0 ⇒ 不批**(涉钱,不预批) | 停,登记 |
| 5 | 只读:`lucky-luxe` **不在 S 里**的单有几张、是谁的 —— **只贴不动**,这是她那句「没有任何真顾客」的对照;那几张明天问她 | — |
| 6 | 彩排(本机副本,**顾客要真有单**,J-58①):删后 lucky-luxe 单数 = 删前 − |S|;`users` 行数不变;账本笔数不变;别家店不动 | — |
| 7 | 生产:`WHERE id IN (字面量)`,四张表逐表影响行数与彩排相等;**不等立刻停,不许重跑** | 停 |

**七条全中 ⇒ 已批。** 删完四项只读核验 + **240 个排班格释放了没有**(那是她关心的那个数)。

### 今晚不动的(明写)
- **三位技师的 `technicians` 行**:不删也不停用。`Ava Lin` 挂着真员工账号 `avalin`,店主没答留不留;**三个一起等**。
- **`users` 74 行、那 21 个档案**:不动。「怎么处理」她没答。
- **Lucky Member 那个微信绑定**:不动。是她自己的测试号,解绑是她的事。

---

## §二 北京旗舰店:永久 —— 走现成的路,不新加开关

店主答「永久」。我读了 `local-server.mjs:12701`:平台 PATCH 已经支持 `planExpiresAt = ''` ⇒ `plan_expires_at = NULL`;`:12664` 对 NULL 给 `daysLeft: null`。
**所以路已经有:建店(默认年付)→ 平台后台把到期日清空 → 永久。**

要做的两件:
1. **量**:`grep plan_expires_at` 的每一处(我数了 11 处),逐处确认 **NULL 被当「不过期」而不是「已过期」** —— 贴表,一行一处。**有一处把 NULL 当过期,先修那处。**
2. **看界面**:平台后台租户列表有没有「清空到期日」的入口。没有就加一个按钮「设为永久(内部店)」,调 `:12701` 那条路,不另写。

🔴 **店主建店的顺序变成**:D210 推上去 → 她点新建商家 → 她在列表里点「设为永久」。**两步都是她的手。**

---

## §三 AI 修图入口:四件全批,本批只做「壳」,流程等 12a

店主批了:位置 · 三态开关 · 技师看得到额度 · 第二入口不做。**提审带哪个态,等 12a 反馈。**

### 本批做
1. 租户设置 `ai_retouch ∈ {off, soon, on}`,默认 `off`(**新租户建出来就是 off**)
2. 平台后台租户配置页一个下拉;🔴 **`on` 这一档现在 PATCH 进去要 409「流程未上线」** —— 流程没做出来之前,谁也拨不到 on。**判据钉住这条:造病拨 on,必须 409。**
3. 商家首页「今日要处理」第一张卡:`off` 不渲染;`soon` 淡态 + 标签「敬请期待」+ 点击只 toast「正在验证画质,开放后通知你」;老板与技师两种视角都出。**`on` 态的渲染这批不写**(写了就是假入口)。
4. 判据:①off 时 wxml 里没有这张卡 ②soon 时点击不跳页(只 toast)③平台拨 on ⇒ 409 ④两种角色都出卡
5. 🔴 **L1 末端验证欠了三批 —— 本批必须收口**:D210 那一屏、11j/11k 四个输入框、这张卡的 soon 态,**各一张截图**进回执。**没截图这一批不算交。**

### 本批不做
选图 / 预设 / 对比三步 —— **全等 12a 两轮结果**。12a 归另一个对话管,你不碰 `tools/ai-retouch-eval/`。

### 提审前置清单加一行
「AI 修图卡带 `soon` 还是 `off` 上审核版 —— 等 12a 第一轮对比包出来店主定」。

---

## §四 顺序

1. **§一 多伦多两批测试单**(判别式,全中即上生产)
2. **§三 AI 修图壳 + 截图收口**
3. **§二 北京店永久两件**(量 NULL 11 处 + 界面按钮)
4. 等天亮更新:`avalin` · 21 个档案 · 正门缺 currency · 三位技师行 · **§一.5 那几张不在 S 里的单是谁的**

推 main 条件不变。生产删除独立备份。

——Cowork,2026-09-22
