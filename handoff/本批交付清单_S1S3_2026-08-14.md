# 本批交付清单 · S1+S3 服务与价目合并页(图=合同 v1.2,08-14 四问全拍)

> 逐屏提取,一项一行;实现记假设;逐屏并排核验;交核验单。■=待做 ☑=完成 ⚠=有差异(见差异说明)

## A. 数据与后端(合同规则⓪②③⑤⑥)
- ☑ A1 items 加 storefront 上架位(try/catch ALTER ×2:storefront + is_timecard;独立于 isActive)
- ☑ A2 展示价=该项目最低可用价档+「起」(服务端 startingPriceCentsOf 算好下发 startingPriceCents/priceFromLabelZh·En,顾客端零运算)
- ☑ A3 公开 /services 只回 item_kind='main' AND COALESCE(storefront,0)=1 AND 非次卡(规则①)
- ☑ A4 价档适用域=service_prices 行存在性(没挂=开单不出现;pick 缺档回落 list 的计算层不动;CI ok132-134)
- ☑ A5 次卡剥离:迁移打标 is_timecard(unit='times'/名称含 次卡/守护(3/足护(9,逐条打印)+两条编辑路由 400 TIMecard_MIGRATED;**现库候选=0 条(jics「焕颜足护」unit=once 是普通护理,未误伤)**
- ☑ A6 迁移一次到位:s1_storefront 幂等标记;49 租户归位;NULL 残留 0;一致性断言入 CI(ok129/131)
- ☑ A7 旧双轨收口:老 /admin/services POST 默认 storefront=1+is_timecard=0;老 PATCH 同挂次卡拒绝;**平台路由 /platform/tenants/:id/services POST 同补默认(运行时新租户不经启动迁移,不补=新店顾客端全空,施工中自查发现)**
- ☑ A8 上下架开关接口:PATCH /admin/pricing/items/:id {storefront};员工 403(staffMayRead 是 GET-only,结构性 owner-only);双端同源同字段

## B. 网页端「服务与价目」(图 §二§三,build 20260815a-s1s3)
- ☑ B1 侧栏合并:「服务管理」「价目表」→「服务与价目」一项(旧 services 路由并入,书签兼容跳 pricing)
- ☑ B2 页内两页签:①上架服务(顾客可见)②结算单目录(开单用·顾客不可见)
- ☑ B3 页签①行卡:缩略图/名称/已上架·已下架 chip/「关联项目:X(目录同项)· 展示价 ¥xxx 起(价档描述)· 约 N 分钟」/开关/修改钮 —— 实测 jics 截图对照通过
- ☑ B4 页签①底部「＋新建上架服务(从结算单目录选项目关联,或全新创建)」→ 点开=选择器(未上架项目一键上架 / 全新创建 / 收起),DOM 实点验证
- ☑ B5 页签① callout 原文照录
- ☑ B6 页签② 项目行价档 chips:普通/分享/会员/疗程 已挂实心;「疗程价(未挂 · 开单时不出现)」划线 na 态 —— 截图对照通过
- ☑ B7 页签②「＋主项目」入口(原有编辑器,新增项目只进目录不自动上架=POST items main 默认 storefront=1?见差异③)
- ☑ B8 页签② 加项段(标题带「只在开单时出现」)+「＋加项」入口
- ☑ B9 页签②「次卡 已迁出」段:原文提示行+「去会员与营销」跳转钮(delegated nav)
- ☑ B10 页签② callout 价档适用域原文照录
- ☑ B11 价档编辑器:**改为行内编辑器非弹层,且是「单项目×四价档」不是「单价档×多项目」**(见差异①);说明句原文入编辑器
- ☑ B12 金额一律 money()(门店币种映射;chips 曾用裸数字 pMoney,施工中已换)

## C. 商家/员工小程序镜像屏(图 §四,拍板④)
- ☑ C1 管理宫格瓦片改「服务与目录」(副题:上架服务 · 结算单目录(只读镜像));页 title 同步
- ☑ C2 页签①:服务行(名/「¥xxx 起 · N 分钟」storeMoney)+老板可拨开关(storefront 同字段);员工只读文字态(假设①);note 原文
- ☑ C3 页签②:项目行+已挂价档 chips 全量/加项行/「🔒 仅查看 · 编辑去网页端。次卡见『会员与营销』。」+去会员与营销链接
- ☑ C4 开关双端同源(闭环③):同一 PATCH 字段;CI ok135-137 下架即时消失/拨回即时恢复;网页实测 17→16→17
- 注:旧 service-edit 编辑页入口已断(镜像屏不再跳转);页面文件保留待 S2 卫生批清除(devtools 缓存纪律:不在功能批删源文件)

## D. 顾客端「服务」Tab(图 §五)
- ☑ D1 数据源=公开 /services(后端已按模块①过滤);布局零改动
- ☑ D2 加项/次卡 0 出现(CI ok128/130 双租户)
- ☑ D3 价格显示「¥xxx 起」:wxml 换 priceFromLabelZh/En(旧后端兜底旧 label);**顾客网页端同病同修**(customer.js 列表卡片换 fromPriceLabel,详情页保留基础价/固定价语义,双端同病检查律)
- ☑ D4 空态沿用既有(CI ok143:全下架=空数组 200)

## E. 断言与验证
- ☑ E1 CI ㉛ 组 16 条入 test-noshow-aftersales.mjs(143 断言全绿):规则①⑤⑥/闭环③/越权/双轨收口/空态/次卡拦截(机制级)
- ☑ E2 组合矩阵(见下)
- ☑ E3 L5 开关往返 + 顾客端实拍 + 并排核验单(见验收报告)
- ☑ E4 42 套件全绿(EXIT=0)+双租户全路径 13/13+10/10+矩阵 20 户全绿+镜像屏 L5 5/5+网页 L5 5/5

## 假设清单(定稿)
1. 小程序开关权限:拍板④只说「手机可直接上/下架」——按「老板可拨、员工只读」实现;后端 staffMayRead=GET-only 结构性保证员工 403(CI ok138)。
2. 次卡识别口径:unit='times' 或名称含 次卡/守护(3/足护(9;迁移逐条打印。现库 0 条命中,「焕颜足护」(unit=once,¥580 普通护理)正确未误伤。
3. 「全新创建」上架服务=建单行自关联(同一行既是目录项目又是橱窗条目,规则⑤不存第二份价格);「从目录选项目关联」=把未上架主项目拨上架。
4. 图未画「大类」「计价规则/试算」两个既有子页:按「开单用配置属结算单目录」保留为模块②内子页签(大类/项目与加项/计价规则),默认进「项目与加项」(图示默认屏)。
5. 员工端小程序无「服务与目录」宫格入口(现状权限模型,员工开单在结算页内已可读价目);合同「有权限的员工」在小程序=无入口。要给员工开,店主一句话。
6. 网页模块② callout 里「¥xxx 起」为合同原句照录(机制说明文案,非渲染价格;渲染价格全部走 money()/storeMoney())。

## 差异说明(与图不符处,报店主裁决)
① **价档编辑器形态**:图画的弹层=「单价档(疗程价 ¥199)×适用项目多选」;实库价档是**每项目每档独立价格**(jics 两条疗程价金额不同),「一档一价多选应用」会丢每项目差价。故保留「单项目×四价档」行内编辑器(留空=未挂=开单不出现,语义与图一致),图上那句说明文案原句照录。若店主要「按档批量挂多项目」的入口,S2 可加批量操作,不动数据模型。
② **模块①缩略图**:图用 emoji 占位,实现用服务真实图片(imageUrl 44px 圆角),信息密度同。
③ **B7「不自动上架给顾客」**:图注写新增项目「只进结算单,不自动上架」;实现中 POST items(main)默认 storefront=1(与「全新创建」共用口子)。**如需严格按图**,模块②入口建项应传 storefront:false——已按图改:见验收报告「B7 补丁」条目。

## 随报质询答复:守护(3次)/足护(9次)在哪?(店主 08-14 问,SQL 实锤)
**结论:这两张卡在现库任何表里都不存在——它们是合同图的示例数据(图注「示例数据取自 Jie'Nail」),jics 现库从未建过。「候选=0」是结构性正确,不是漏检。**证据:
1. 全库逐表按名称扫(`%守护%`/`%足护%`):只命中 `services`「焕颜足护」(¥580,unit=once,普通护理)及其 `settlement_items` 结算快照行——无任何次卡行。
2. 次卡在本系统的家=`membership_packages` 表 `kind='times'`(网页「会员套餐/券」页「+次卡」钮建的就是它),**从来不是 services 行**。按 `%次%` 扫:全库唯一真次卡=lucky-luxe「美甲10次卡」(`pkg_k926`,kind=times,10 次,is_active=1)。
3. jics 的 membership_packages 只有一条「充1000送50」充值套餐,零次卡。
4. 执行含义:①真次卡「美甲10次卡」已住在 S2 目的地(会员与营销页),无需迁移;②合同③的 services 侧剥离机制(迁移打标+两路由编辑拒绝+跳转段)已建成,作用=防御性兜底(将来有人把次卡建成 services 行也会被拦)+导航;③S2 出图时直接管理 membership_packages 的 times 类,无迁移债。

## 店主拍板记录(08-14 对照表 v1.1 两板,S1 批内零代码动作,记档待后续波)
- 网页补「数据分析」页 → 归 S6 波
- 网页顾客端=全功能对齐(境外主端)→ S2 卡包/充值商城、S8 订单详情、S9 会员/积分商城,每波网页顾客屏同批画/同批施工/同受四之九;不再允许「小程序有网页没有」净新增
- S2 前置:网页顾客端自有登录方案(手机号+验证码/邮箱+密码,与微信身份同档案互认)→ Cowork 出方案店主拍,Code 暂不动码

## 复核-1/复核-2 并案结论(2026-08-15,生产实查)
**同根假设不成立,两条各有其因;生产库数据形状与本地一致、迁移到位(标记在/NULL=0)。**

### 复核-1「价目表里仍有次卡」——生产库零次卡行(SQL 全量 dump 为证)
- railway ssh 直连生产 Volume 库:jics 全部 27 行(10 加项+17 主项目)、lucky 全部 7 行逐行 dump——**没有任何次卡行**;`membership_packages`/`recharge_tiers` 两租户为空;全库唯一 times 卡=demo-ai 演示租户「护理卡*3」。
- 店主看到的「次卡」最可能=新模块②底部「次卡 已迁出」段的**说明文案**——我照抄了合同图例句「守护(3 次)/ 足护(9 次)等按次核销的卡…」,而这两张卡生产库根本不存在,读起来就像"次卡还在"。已改条件化:有真次卡才列名,现库无次卡时明示「本店服务目录中当前没有次卡条目」。
- 规则⑥增补已执行:生产迁移单独验证(s1_storefront 标记两租户在/storefront NULL=0/公开接口两租户零加项零次卡全带「起」)。

### 复核-2「起字四处全无」——真凶:网页顾客端整页白屏(TDZ),非「起」缺失
- **根因(生产实锤)**:customer.js 文案字面量在**文件顶层**立即执行 `moneyY(50)`,而其依赖的 `const CUR` 声明在文案之后 → `Cannot access 'CUR' before initialization`,**整个顾客页 SPA 顶层崩死、服务列表根本不渲染**。该行由 08-10 币种红线批(3c948e0)引入——生产网页顾客端白屏至今 5 天,一直无人走查此页(网页顾客端历来不在走查清单,正好落在店主拍板②「网页顾客端=全功能对齐」要补的盲区上)。
- 修复:CUR/money 块前置到文件头(零逻辑变化);同类机制扫尽(三个网页 JS 全部顶层模板调用逐处核:仅 customer.js copy 两处中招,admin/share 均在函数体内);**永久护栏 CI ㉜**:node stub-DOM 加载 customer.js/share.js,顶层抛任何异常=红(147→145 断言全绿;并用出事版本反证护栏能抓:坏版红/好版绿)。
- 四端分端结论:
  - **网页顾客页=真缺陷已修**(上述);
  - **网页商家模块①**:生产 build 20260815a 顶层健康、「服务与价目」侧栏与起价渲染代码在场(浏览器实测);静态层 no-store 无缓存问题——请店主修复上线后再看一眼,若仍无起请截图再报;
  - **手机小程序=预期**:正式包还是旧版,「¥xxx 起」在新代码包里,需发版(入发版清单);
  - **devtools 最新编译**:我方**整只重启 devtools**后实拍有起(s1-customer-services.png)——只点「编译」吃不到新 wxml(卫生批实锤的编译缓存机制),请店主重启 devtools 后复验。
