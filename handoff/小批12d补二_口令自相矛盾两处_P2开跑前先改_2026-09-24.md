# 小批 12d 补二 —— 我读了 `round4-prompts.mjs`,组装出来的口令有两处自相矛盾;P2 开跑前先改

停线不变。本令只动 `tools/ai-retouch-eval/round4-prompts.mjs` 与其自检,不动 `bucket-prompts.mjs` 的第三轮口令(留作历史对照)。

## §零 两处矛盾,都是我的令造成的(记我第 57 笔),Code 逐字照令没错,但自检没抓到「与前缀打架」
1. **手部探针(P1)**:`handPromptFor = PREFIX + EXPO_A/B + TONE + HAND`。EXPO_A 里有「**保留皮肤真实纹理和指节褶皱,不做平涂**」,EXPO_B 里有「**保留真实纹理和指节褶皱,不做美白平涂**」,而 HAND 是「**手部皮肤轻度磨皮**」。同一条口令里既要保纹理又要磨皮 —— 和第一轮「绝对不改甲色 vs 冷白皮」是同一种病。我在 12d 补 §三-2 写「其余一字不动」,把这个矛盾锁了进去。
2. **背景探针(P2)**:`bgPromptFor = PREFIX + BG_KEEP + 背景句`。PREFIX 里写着「**同一背景**」「**背景内容和构图不改**」「**画面比例与原图一致**」,BG_KEEP 写「**只改背景**」。而且 12d 补 §三-3 要求的**色温句 + 甲色句没进背景口令**。自检只查了四个禁词,没查「与前缀打架」。

## §一 P1:已跑的 3 张不作废,剩 3 张照跑,但磨皮那一项不得归因
- 已出的 `H_A浅_23 / H_A深_18 / H_B深_02` 与后 3 张,**只用来看**:白净句是否假白、甲面色调句对深/浅甲是否管用、R:B 是否守住。**「磨皮有没有效」这一项在 P1 上不许下结论**(口令自己在打架)。
- **P1b**(6 次 ≈ ¥1.92):把 EXPO_A/EXPO_B 里「保留(皮肤)真实纹理和指节褶皱,不做(美白)平涂」这一分句**整句换成** HAND 句,其余字不动;导出为 `EXPO_A_HAND` / `EXPO_B_HAND`(不覆盖原 PRESET)。同六张再跑一次。对比包手部区加一列「P1b」。

## §二 P2 开跑前必改
- 新建 `BG_PREFIX`:从 PREFIX 复制,**删掉**「同一背景」「背景内容和构图不改」两处,「画面比例与原图一致」保留;其余逐字不动。
- `bgPromptFor = BG_PREFIX + 色温句 + 甲色句 + BG_KEEP + 背景句 + SUFFIX`。色温句 = 第三轮那句「白平衡以原图为准,保留原图本来的暖调,不做色温校正」;甲色句 = PREFIX 里「指甲的形状、长度、颜色、图案、贴钻位置和数量与原图完全一致」那一句(它已在 BG_PREFIX 里,不重复)。
- 自检加一条**通用规则**:组装后的全文里,**同一宾语不许同时出现「不改/不变」与「换/改」**(背景、皮肤纹理、甲色三项写死成检查对),红则不许跑。落刀:把 PREFIX 原样塞回 → 红且点名「背景:不改 vs 只改」。

## §三 D220 立案(下批修,不在本轮)
口令里带 markdown 星号 `**不要整体提亮**` 原样送模型。第三轮起就带着,本轮为了归因不动。12b 落地时口令一律纯文本,自检加「无 `*`」一条。

## §四 判据
- `round4-prompts.mjs` 自检新增「矛盾对」检查,三对各落一刀红、还原绿。
- P1b 口令全文 grep 不到「真实纹理」「不做平涂」「不做美白平涂」。
- P2 口令全文 grep 不到「同一背景」「背景内容和构图不改」,grep 得到「保留原图本来的暖调」。
- 预算:P1b 1.92 + P2 前三种 1.92 + 扩图 0.32 ≈ ¥4.16;累计 ≈ ¥54.8;后两种背景(1.28)等她确认,总额仍 ≤ 60。
