# 小批 07a · 白屏路径 · 渲染抛错 · 去重与声称

**开单:2026-09-11** · 上游:夜 8(`72c4299` → `8b4a574`,**十一个**,领先 main 254)
**裁 #45–#49** · 立律 **J-46 / J-47 / J-48 / J-49**

---

## 〇、先记我一笔(第六笔):夜班令 8 我根本没送到

你开头第一句说「仓里没有这份文件」—— **对的,是我的错。**

昨夜桥断了,我把令写出来交给了聊天窗,然后对店主说「你直接丢给 Code 就能跑」——
**把「落地」这一步推给了她。** 指令没落到仓里,就等于**没下**。

而我当时还在台账里把它当成「已开单」在说。**这是我这几批里最实的一次失职。**

> 🔴 **立律 J-46 —— 指令落地律。**
> 一道指令**必须同时落到两处文件**(桌面交接夹 + 仓里 `handoff/`)**且 md5 一致**,才算下达。
> 落不了地(桥断、机器不在)就**只许记「已写 · 未送达」**,**不许记成「已开单」**,
> 更不许在下一轮拿它当「我已经让你做了」的依据。

**记你一功**:你没等、按 06i §三 我自己排的顺序往下跑,**而且开头第一句就把这件事说出来**。
⑤配色批你没碰,理由写的是「要拿并排图找店主拍板,不是我能定的」—— **这条边界你守得准。**

夜班令 8 我已经补进仓里,**标题上标了「未送达 · 存档」** —— 它是记录,不是待办;
里面还成立的几件(尺子扩面的机器证明、J-45 扩到所有造景步骤)并进本批。

---

## 一、我现查到的(全部我自己跑的)

**对上的**:L4 **13/13 · 没扫 0**(三份红榜覆盖面小节我读过)· 渐变面新刀 后台 220 · 顾客端 38 · 甲 0 乙 0 · 第三筐 3 个「被裁掉没画出来」· 棘轮六把无一变大 · 4128 既有行**一条没改**。

**两处对不上,都不大,但都要说:**

1. **提交是 11 个,不是 10。** 你写的范围起点 `4b06810` **漏掉了 `72c4299`(段 0a)** ——
   而那恰好是这一夜的**第一个**提交,也是装着购物车那处修改的那一个。
   06g 那次漏的是末尾,这次漏的是开头。**别再手写范围了** ——
   抬头的提交清单由脚本产出(`git log --oneline <上一批的头>..HEAD` 原样贴,并打印 `--count`)。
2. **你报 111/111 全绿,我这边 `test-untouched-proof` 是红的。**
   **而这条红,是你的回执自己造成的** —— 见 §六。**不是你跑错,是它在你跑完之后才存在。**

---

## 二、裁 #45 —— 那不是夹具的问题,是产品的**一条白屏路径**

你的定性是「两处都是夹具/判据自己的问题,**不是产品少东西**」。
**前半句对,后半句我不收。**

现查:

- `apps/web/customer.js` 里 **9 处**完全无保护:
  `1644` `1672` `1720`(购物车)· `1857` `2029` `2149` `2150` `2151` `2185`(我的订单 / 订单详情 / 作品档案)——
  `order.technician.name` · `order.store.name` · `item.technician.id`。
  **而同一个文件的作品集那一段用的是 `?.`**(`1338` `1359` `1362` `1367`)——
  **同一个文件里两套写法**,这本身就是病。
- 服务端给这两个字段的是 **`.get()`**:`local-server.mjs:5309`(订单序列化)、`:4605`(预约草稿序列化),
  `store` 同款在 `:5310` / `:4606`。**`.get()` 查不到返回的是 `undefined`,不是空对象。**
- 而 `booking_drafts.technician_id` **本来就可空** —— `apps/api/booking-drafts.mjs:56` 明写 `|| null`。
  **「顾客还没选技师」是正常业务态**,不是坏数据。

**所以夹具塞 null 不是造了个假病,是提前演了一遍「那一行不见了」会发生什么。**
`bookings.technician_id` 是 `NOT NULL`、也没有硬删技师的口子 —— 所以**今天不会天天炸**;
但草稿那一路现在就可能是 `undefined`,而订单那一路只要有一行对不上就整页白。

**修法按类不按处**(和你修那条链接蓝一个道理):

1. 服务端两处(4605/5309,连 store 共四行)给出**明确的占位**或返回 `null`,不许把 `undefined` 漏给前端;
2. 前端那 9 处收到**一个出口**(照 `moneyParts` 那个办法),拿不到名字时显示「未指定」,**不许九处各加一个 `?.`**;
3. 判据:全仓顾客端**零处**直接取 `.technician.` / `.store.` 的属性而不过那个出口;造病点名 `file:line`。

---

## 三、裁 #46 → 立律 J-47:**渲染抛错会伪装成「功能没做」**

你回执里这句是全批最值钱的:

> 整个购物车渲染当场抛错,连带「去结算」那条 summary-bar 也不出现,
> **看起来却像「点不到结算入口」**。

**你自己差点被它骗过去 —— 而我如果只读你的红榜,也会被骗过去。**

> 🔴 **J-47:扫页面的刀必须同时收页面错误** —— `window.onerror` · `unhandledrejection` · `console.error`。
> **有错先报错。** 「元素没找到」和「这一页渲染崩了」是**两个结论**,不许混成一个。
> 一页只要抛过错,那一页这一轮的所有「没找到」结论**一律作废**,按「这一页崩了」报。

**判据**:造病 —— 往任一页塞一个必抛的渲染错 → 刀必须报「这一页抛错了」并点名,
**而不是报「少了某个元素」**。反向守:正常页不许误报。

这条和 J-37(「在不在」不等于「看得见」)、J-45(「到了那一页」不等于「到了那个状态」)是**一家三兄弟**:
**「没看见」有三种成因 —— 没画出来、画出来但看不见、压根崩了。判据要分得出是哪一种。**

---

## 四、裁 #47 —— 跨租户过滤漏的不是三处,是**十处**;这一笔也记我

现查 `local-server.mjs` 里按 id 查 `technicians` / `stores`:

- **带** `AND tenant_id = ?` 的:`7386` `8159` `8514` `10920` `10933`
- **不带**的:`4605` `4606` `5309` `5310` `13323` `13331` `13597` `13626` `14268` `14498`
  另有 `5544` 的子查询 `(SELECT name FROM technicians WHERE id = b.technician_id)`

**我不把它拔高成新的红线。** 这几处查的 id 来自本店自己的单,要真漏出去,
得先有一张**挂错店的单** —— 而那正是 D126 / D130 / D131 / D132 治过的。
**定性是「纵深没做满」,不是「门开着」。**

**但「四条红线全清」那句话是我写进台账的** —— 漏了十处没查出来,**是我核得不够细,记我一笔。**

**本批**:十处(含那个子查询)补上租户条件;并立**白名单式判据** ——
全仓 `FROM technicians WHERE id = ?` / `FROM stores WHERE id = ?` **不带租户条件的必须 0 处**,
新写一处当场红并点名 `file:line`。**有确实不该带的,逐条写理由进白名单,条数上棘轮。**

---

## 五、裁 #48 → 立律 J-48:**去重只许少写行,不许少量节点**

你自己踩了两面并写在码上,两面合起来才是完整的病:

- `painted` 当过滤门槛 → 量成数 **594 → 66**,而「被裁」那筐按选择器去重只显 **4 个** —— **数量被去重藏住了**;
- `seen` 卡在量之前 → 255 个一样的「还没有图片」只算 1 个 → **594 → 68**。

> 🔴 **J-48:量在前,去重在后。**
> 去重只许用于**少写几行报告**,不许用于**少量几个节点**。
> 报告里必须**同时给两个数**:**量过的个数** 与 **去重后的行数**,且前者 ≥ 后者。

**判据**:刀自己打印这两个数并断言大小关系;把去重挪到量之前 → **造病必须红**。

**你那句「判据不许悄悄缩覆盖面,而去重是最容易把缩水藏起来的地方」—— 原话收进律里。**

---

## 六、裁 #49 → 立律 J-49:**声称要靠标记,不靠措辞**(那条红是假红,棘轮不许抬)

我这边 `test-untouched-proof` 现在是红的:

```
not ok 5 - ③ 回执自证棘轮:…存量 35 篇 ≤ 34
  35 篇:… | handoff/night-runs/夜8回执_2026-09-10.md | …
```

**新增的那一篇,就是你昨夜的回执。** 而它「犯规」的那一行是:

> 但「本机库未动」这句话这一夜**我不能说**,因为它确实多了 4 行心跳。照实写。

**这是全批最诚实的一句话** —— 而判据按字面搜「未动」,把**否认**当成了**声称**,当场把你判红。

**所以:这是假红。棘轮 34 不许抬到 35。**(抬了就是拿棘轮去吸收一个判据缺陷。)

> 🔴 **J-49:需要举证的断言,判据认的是结构化标记,不是自由文本里的词。**
> 「某某库未动」「零变化」「全绿」这类话,要成立必须带一行机器可读的标记,例如:
> `未动对照:handoff/night-runs/xxx对照表.md`
> **有标记才算声称,没标记就不是声称。**
> 否则判据会把「这句话我不能说」当成「我说了这句话」——
> **而且作者越诚实、越会写限定语,越容易被误判。**

**本批**:
1. 把 `test-untouched-proof ③` 改成认标记;**棘轮留在 34**,改完重量,降了就往下收。
2. 造病两条:①写了标记但对照表文件不存在 → 红(J-27 同族);②写了「未动」却没标记 → 红。
   **反向守**:像昨夜那样**否认**自己能说这句话的 → **不许红**。
3. **回执落盘后补跑这一把刀。** 你昨夜的 111/111 是在回执存在之前测的 ——
   **回执自己在扫描面里**,写完不补跑,回执就成了下一批的红。这一条写进收尾清单。

---

## 七、收下的 · 欠的

**收下,不用补**:

- **写库那条的分寸**:停线是「写 4128 既有行」停,现测一条没改;而你**不说「本机库未动」**,
  因为掉线重启后通知调度往 `notify_scan_marks` 追加了 4 行,`heartbeat-shape` 9 条全过并逐行打印。
  **「既有行没动」和「库没动」是两句话,你分清了** —— 这正是 J-23 要的。(判据的毛病见 §六,不是你的。)
- **覆盖面那句**:`contrast-sweep` 报后台渐变面 718 个节点,新刀量到 220 个位置,
  你写明「这不等于渐变面全覆盖了」。**这句话保住了这一批的诚信**,别去掉。
- **没做的三件照实列**(配色批 / 图标两套图 / 财务子标签页)—— 排期问题,不是违令。

**欠的两件,本批要补**:

1. **新刀只跑了一遍。** 一把**改了五版才敢出数**的刀,单跑一次的数不能当基线 ——
   **先跑 5 次看稳不稳**(同参数、同页面),5 次结果一致才把数写进任何报告;不一致就列出漂在哪。
2. **财务子标签页**没走 —— 走一遍,把 220 这个数补成它该有的样子,并更新覆盖面小节。

---

## 八、顺序 · 停线

**顺序**:
① §六(假红 + J-49,**先把红清掉,否则下一批一开局就是红**)
② §二 白屏路径(顾客端那 9 处 + 服务端四行)
③ §三 J-47 刀收页面错误
④ §四 十处租户条件 + 白名单判据
⑤ §五 J-48 两个数
⑥ §七 新刀跑 5 次 + 财务子标签页
⑦ 配色批备料(**只出并排图和数,一个像素不许动合同图**)
⑧ 待裁 #34 图标两套图

**停线一条不松**:不推 main(领先 **254**)· 不碰生产 · **写 4128 既有行仍停**(D185 §二我还没批)·
不看密钥文件 · 不许 sleep 轮询 · **不许松判据 / 抬棘轮 / 换尺子**(§六 那个 34 尤其不许抬)·
不许改合同图来凑代码。密码一个字都不进回执、不进队列、不进任何交接文件。

---

## 九、回执要写到

1. 提交清单**由脚本产出**并贴原文(含 `--count`),抬头排在最后一个代码提交之后。
2. §六 改完后 `test-untouched-proof` 的现测:**棘轮多少、现测多少、降了没有**;两条造病 + 一条反向守的原文。
3. §二 的出口长什么样;顾客端**零处直取**的判据现测;造病点名。
4. §三 造病:塞一个必抛的渲染错 → 刀报「这一页崩了」的原文。
5. §四 十处补完后,白名单式判据现测 **0 处**;白名单里若有条目,逐条理由。
6. §五 两个数(量过的个数 / 去重后的行数)在报告里的原文。
7. §七 新刀 5 次的结果(5 行数字并排);财务子标签页走完后的覆盖面小节。
8. **收尾清单里新增一条**:回执落盘后补跑 `test-untouched-proof`,把结果贴进回执。
9. 任何一条没照字面做,写「我没照字面做,原因是 X」。

---

**这一单的由来,两句话**:
**一句是我的** —— 夜班令 8 我写了却没送到,还当它已经下了;指令不落地就等于没下(J-46)。
**一句是你的** —— 你写下「『本机库未动』这句话我不能说」,判据按字面把这句诚实判成了违规。
**判据认词不认标记,就会专门惩罚说话最谨慎的那个人。** 这条比那个数字值钱得多。
