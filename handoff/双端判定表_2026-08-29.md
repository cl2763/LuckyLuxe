# 《双端判定表》(2026-08-29)· 只判不改,交店主排序

> 批次三第一步。基数 = 欠账总表的机核结果(后端 155 条 `/admin/*` 路由:两端都用 84 /
> 只网页 35 / 只小程序 21 / 两端都没人调 15)。**逐条判「该不该双端」,不是逐条拉平。**
>
> 判定依据的优先级:①店主已拍的裁定(台账)>②08-14 三原则(网页=总和 / 小程序=快捷执行 /
> 涉钱重规则只在网页编辑)——**该表已查出两行是错的,凡引用它的地方我都单独核过现状,不整表照抄**
> >③机核现状。**档位三档:上线前必办 / 拉平批做 / 合理只在一端(写理由)。**

---

## 一、🔴 上线前必办(1 族)

| 项 | 现状(网页 · 小程序) | 判定 | 依据 |
|---|---|---|---|
| **网页开单一族**:直接排单 `/admin/bookings/direct` · 结算预览 `/admin/settlements/preview` · 顾客快查 `/admin/customers/lookup` + 开单/结算整套 UI | ❌ · ✅ | **该双端 · 上线前必办** | 境外商家没有微信小程序,买了系统**开不了单** —— 与 08-14 拍板②(顾客端对齐,理由正是「境外没有小程序」)同一个理由,当时只推到顾客端,商家端漏了。08-19 店主自己就撞过一次(想验退卡发现网页没有开单) |

## 二、拉平批做(= 批次三主体,按块分组)

### 块 1 · 小程序账户调整(店主 08-27 已裁「账户调整=双端」)

| 项 | 现状 | 判定 | 备注 |
|---|---|---|---|
| 账户调整面板 `/admin/account-adjust/facts` | ✅ · ❌ | 该双端 | 充值/赠送/退卡/冲销四段 |
| 退卡 `/admin/stored-value/refund` | ✅ · ❌ | 该双端 | ⚠️ 连门禁一起搬:仅老板 + 财务密码(小程序已有 `getFinanceKey` 那道门,接同一把) |

### 块 2 · 员工端双端等同(店主 08-27 已裁,含配套死口收编)

| 项 | 现状 | 判定 | 备注 |
|---|---|---|---|
| 我的业绩 `/admin/my-performance` | ❌ · ✅ | 该双端(网页补) | 店主原话「内容上都要是一样的」 |
| 员工工作台(工作台/报价器/我的排班) | ❌ · ✅ | 该双端(网页补) | 大件;打卡除外(见「合理只在一端」) |
| 补偿自查 `/admin/my-compensation-estimate` | ✅ · ❌ | 该双端(小程序补) | — |
| 工资自查 `/admin/salary/my-estimate` | **两端都没人调** | 漏接 → 并本块 | 🔴 与上一行是**两条口做同类事**(工资预估 vs 补偿),先收敛成一条再接 UI,别把分叉抬进两端 |
| 员工可见性三态 `/admin/staff-visibility` | **两端都没人调** | 漏接 → 并本块 | 它就是本块的配置开关(纯业绩/业绩+工资/纯工资),没 UI 等于这个功能不存在 |
| 已发布作品 `/admin/published-works` | ❌ · ✅ | 该双端(网页补) | — |
| 员工提醒 `/admin/staff-nudges/mine` | ❌ · ✅ | 该双端(网页补) | — |
| 服务小记图片入口 | ❌ · ❌ | 新功能,并本块 | 08-27 查明两边都没有 |

### 块 3 · 编辑口归位(店主 08-27 已裁「会员与营销编辑=网页」,现状与裁定相反)

| 项 | 现状 | 判定 | 备注 |
|---|---|---|---|
| 充值套餐 `/admin/recharge-packages` | ❌ · ✅(编辑在小程序) | 该双端:**网页补编辑,小程序降级为快捷查看+代充值** | 台账 08-27 原文就是这么裁的 |
| 次卡套餐 `/admin/timecard-packages` | ❌ · ✅ | 同上 | — |
| 次卡设置 `/admin/timecard-settings` | ✅ · ❌ | 网页有=对;小程序**在卖**次卡但改不了设置=符合「重规则只在网页」 | 本行不欠,列出来是给块 3 定边界 |

### 块 4 · 配置类网页补齐(原则①「网页=总和」,现状只在小程序)

| 项 | 现状 | 判定 | 备注 |
|---|---|---|---|
| 预约规则 `/admin/booking-rules` | ❌ · ✅ | 网页补 | 提前几天可约、时段粒度 —— 配置该有总和位 |
| 排班设置 `/admin/schedule-settings` | ❌ · ✅ | 网页补 | — |
| 客群规则 `/admin/segment-rules` | ❌ · ✅ | 网页补 | — |
| 按日排班 `/admin/schedule-day` | ❌ · ✅(网页走 `schedule-week`) | 🔴 **分叉债**:同一件事两种粒度两套路由 | 先收敛后拉平,别把两套都补齐 |

### 块 5 · 查看类网页补齐(低优)

| 项 | 现状 | 判定 |
|---|---|---|
| 会员名册 `/admin/membership/members` | ❌ · ✅ | 网页补(查看) |
| 储值流水 `/admin/stored-value/txns` | ❌ · ✅(网页只有账户列表) | 网页补(查看) |
| 召回摘要 `/admin/recall-digest` | ❌ · ✅ | 网页补(查看) |
| 工资待发 `/admin/salary/pending-payout` | ❌ · ✅(网页有 payroll 草稿) | 先对口径再定,疑似与 payroll 重叠 |

## 三、合理只在一端(不做,理由逐条写)

| 项 | 只在 | 理由 |
|---|---|---|
| 打卡 `/admin/attendance/clock` | 小程序 | 要定位/WiFi,人在店里手机上做;网页打卡=坐在电脑前打卡,失去意义 |
| 门店 WiFi 配置 `/admin/store-wifi` | 小程序 | 要读**手机当前连的 WiFi**,网页读不到 |
| 核销券 `/admin/coupons/redeem` | 小程序 | 顾客到店出示、店员扫码,现场动作 |
| 门店时钟 `/admin/store-clock` | 小程序 | 实现差异不是功能差异:网页用前端 `storeToday()` 同源换算 |
| 权益读口 `/admin/tenant/entitlements` | 小程序 | 网页读 `tenant/plan` 等价口,同一后端事实 |
| AI 洞察四条(booking-summary / customer-insight / cs-feedback / logic-notes) | 网页 | 依附客服工作台(网页专属工具);模拟器三条同理 |
| AI 知识库四条 `/admin/kb*` | 网页 | 重规则编辑(08-14 原则②) |
| 价目规则 `/admin/pricing/rules·preview·categories/*` | 网页 | 同上;小程序保留项目编辑快捷口(现状即是) |
| 财务深功能(summary/trend/verify/recurring/compensation/coupon-discounts/payroll×2) | 网页 | 重规则+发放确认;小程序 finance 页已有简版(progress/txns/estimate),**不是缺席是分工** |
| 注册 `/admin/auth/register`、演示铺数 `/admin/demo/*` | 网页 | 开号/运维动作 |
| 消息模板×2 + 提醒任务 `/admin/reminder-tasks/*` | 网页 | P3 前置配置;P3 批落地时再定小程序要不要只读展示 |
| 商城自购 `/admin/mall/self-purchase` · 预约草稿 `/admin/booking-drafts` | 网页 | 低频管理动作(自购)/ 依附客服工作台(草稿)。**握不准,标给店主一票** |
| 轮播自管 `/admin/hero-slides`(A10) | 网页 | 传图排版在电脑上做更实际;属门店内容配置 —— **同样标给店主一票** |

## 四、两端都没人调的 15 条 · 单独判死代码 / 漏接(不混进拉平)

| 项 | 判定 | 依据(现查,不是推理) |
|---|---|---|
| `/admin/recharge-tiers` ×3 变体 | **死口,候刀** | A5 收敛时已退役(`admin.js:8256`「整块退役——CI 零读方断言防复活」),路由是残留 |
| `/admin/merchant-leads` ×2 | **疑似死口,候查** | 平台侧已有 `/platform/leads` + page-leads 面板;这两条是仅默认租户的旧位面 |
| `/admin/finance/change-password` | **疑似死口,候查** | 网页财务设置真调的是 `lock-settings`(admin.js:2921) |
| `/admin/stored-value/consume` | **墓碑,保留** | 手动耗卡已裁永久关闭,410 就是墓碑本体,留着挡旧调用 |
| `/admin/ops/import-db` | 合理无 UI | 运维专用(数据迁移) |
| `/admin/touch-rules` | 挂起在册 | 企微双通道的触达配置,企微还没批下来 —— 不是欠账是未到期 |
| `/admin/identity-merge-queue` + `/admin/users/*/identities` | 挂起在册(等出图) | 代码注释原文「处理动作等出图」—— openid 冲突合并是 D66 族,**不许没图就施工** |
| `/admin/salary/my-estimate` · `/admin/staff-visibility` | **漏接** | 已并入块 2(员工端) |
| `/admin/finance/deposit-conservation`(+`/repair`) | **漏接,单独裁** | 定金守恒对账工具,没入口=从没人跑过。接进网页财务还是做成定时自检,**请店主选** |

## 五、顾客端(E 组,与 08-14 拍板②对表)

| 页面 | 现状 | 判定 |
|---|---|---|
| AI 客服 | ❌ · ✅ | 已有裁定(08-25「排上线后」),不算欠账 |
| 消息中心 | ❌ · ✅ | 待排(A1 清单 a6 登记过「单独排期」) |
| 积分/商城结构 | 两端结构不同 | 拉平批做(逐屏对) |
| 其余 | 齐 | 08-25 A1 清单已逐屏核过 |

---

## 六、给店主的排序建议(你排,不是我排)

1. **上线前必办**:网页开单族(块 0)—— 唯一会让付了钱的商家当场卡死的。
2. **块 1 + 块 2**(账户调整 + 员工端)—— 都是你 08-27 亲手裁过的。
3. **块 3**(编辑口归位)—— 现状与你的裁定**相反**,拖越久小程序编辑口上长的东西越多。
4. **块 4 收敛 + 死口候刀** —— 便宜,且把「两套路由做一件事」的分叉债还了。
5. **块 5 + 顾客端** —— 查看类,最后。

**两处要你一票**:①轮播自管与商城自购/预约草稿是否接受「合理只在网页」;
②`deposit-conservation` 接 UI 还是做定时自检。

## 七、这张表的完整性自证

- 155 条路由**每一条**都有归属:84 齐 + 上表 71 条(1+2+3+4+5 块 + 合理单端 + 死口/挂起)——
  谁想复核,拿欠账总表逐条对,不该有落单的;
- 判死口的每一条都给了**现查证据**(代码行号),不是推理;
- 表出来**未动一行产品代码**(第二节说的「先收敛后拉平」都是待办,不是已做)。
