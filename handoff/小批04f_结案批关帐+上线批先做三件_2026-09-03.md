# 小批04f — 04e 验收(`6cd41b9`/`0af70b8`:收)· **结案批关帐** · 等图期间先做上线批三件

> 2026-09-03 深夜 · Cowork 亲核于店主 Mac · 两处各一份:桌面交接夹 + 仓内 `handoff/`
> 一句话:C2 自证成立、D135 加重收进册;D136 销号成立;结案批交齐文件合格,**结案批关帐**。下一批「AI 预约闭环」等我的图;等图期间做上线硬门槛批里**不碰界面、不碰 AI 那一路**的三件。

---

## 〇、总裁

| 件 | 裁 |
|---|---|
| C2 重跑:`quoted_at`/`expires_at` 前后原值 + `qs-banner expired` DOM + 第 3 轮仍 `quote_draft` 且 **`expires_at` 被改回未来** | **收**;D135 加一层「过期旧价自动续期」,并入闭环批;两条教训入常驻:`/respond` 只润色、`/mark-quoted` 才标已报价;**造态要造在判据真正读的那一列** |
| D136:两店 `depositPolicyText` 与模型回答逐项一致;「Silver 会员」来自等级体系 | **销号**;这份并排表留作闭环批「事实一致性」判据的锚(金额 / 可否抵扣 / 三档比例) |
| 结案批交齐:修 20 · 答 6 · 挂 9,挂的逐条有归属;硬数齐;四库四名齐 | **合格,结案批关帐** |
| 「19 个提交全在本地」 | 措辞要准:03q→04e 是 19 个,但 **`origin/main..HEAD` 是 86 个**(最早 `350ba10` 08-26)——**自 08-26 起一次都没推过**。上线批第一行就是它 |
| 桌面总表第 282 行的令牌原值 | **我已抹**:桌面三处(台账 / 03g / 总表)全部替换成「sess_〈已作废令牌,原值已从档案抹去〉」,仓内 `handoff/` 现扫 0 处;git 历史里 7 个提交仍含该串——令牌 03p 已作废,**不改写历史**,记入上线批备注 |
| 沙箱 64 通走查会话 | 留着;店主看完再清 |

---

## 一、等图期间先做(上线硬门槛批 · 不碰界面、不碰 AI 路)

1. **D134 现修**(小):`PUT /admin/kb/facts` 不认识的键 → `400 UNKNOWN_KB_KEY` 点名;网页后台保存处按 400 提示;判据:传一个陌生键必 400 且库不动;造病:去掉校验 → 红。
2. **去 DEFAULT 31 张表 · 迁移脚本 + 彩排**:SQLite 去列默认要重建表——按「新表建 → 复制 → 改名 → 重建索引/触发器」一个事务做;脚本走 `requireTarget` / 默认 dry-run / `--apply` / 先备份 / 逐表行·列·索引·触发器四栏前后对照;**只在本机库副本与沙箱彩排**,判据:31 张表 `pragma_table_info` 的 `tenant_id.dflt_value` 全为 null,`test-tenant-explicit` 的在案行由「仍在」翻成「已去掉」,反向守:任一表仍有 DEFAULT → 红;账本触发器 12 条重建后 `test-ledger-guards` 必须仍全绿(**包了事务必须验回滚**:造一次中途失败,库回到迁移前)。生产跑随部署,另批。
3. **版本指纹**:后端 `/health.version`(提交号 + 构建时间)、网页后台页脚与小程序「关于」页各显示同一指纹,三端同号才算同一版本;判据:三处读出的指纹相等;造病:改一端 → 红。

三件各自提交、各自回执,不合并。做完停,等图。

---

## 二、交齐清单

三件的提交号 / 判据 / 造病 / 回归 / 断言增量 / 两库对照(四栏)/ 棘轮只降;未推 main 照写(数字用 `origin/main..HEAD`)。

---

## 附 · Cowork 亲核命令原文

```
git log --oneline -3 ; git show --stat 6cd41b9 ; git log origin/main..HEAD --oneline | wc -l → 86 ; git log origin/main..HEAD --format='%h %ci' | tail -1 → 350ba10 2026-08-26
git grep -c sess_msnk2ktp | wc -l → 0 ; grep -rl … handoff/ → 0 ; git log --all -S'sess_msnk2ktp' --oneline | wc -l → 7
桌面交接夹三文件 sed 脱敏,剩余命中 0/0/0
读 handoff/结案批交齐_2026-09-03.md 全文 89 行
curl 4310/health → tenantFallback 0/0 · aiUsage 全 0 ; 沙箱今日会话 64
```
