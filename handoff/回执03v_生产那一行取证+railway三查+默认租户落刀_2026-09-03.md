# 回执 03v — 生产那一行取证 · railway 三查 · 默认租户落刀

**生产全程只读**:四次查询,全部 `DatabaseSync(..., { readOnly: true })`,
脚本经 base64 走 `node -e 'eval(...)'` 传入 —— **连一个临时文件都没往容器里写**。

---

## 一、生产那一行 —— 取证结果:**它不像夹具,像真单**

```
MATCHED_ROWS=1
id=booking_mt1iixg3_046wjx
单租户 = jics-nail(小婕店)     人租户 = lucky-luxe(旗舰店)
created_at = 2026-08-20T12:44:40.275Z
source     = wechat_miniprogram          ← 不是脚本,不是 owner_direct
status     = CONFIRMED
notes      = (空)                        ← 本机库那 5 行都带「O7 walk fixture,验后撤」

闭包:payments = 1(provider = MOCK,amount_cents = 10000)
      settlements = 0 · finance_transactions = 0 · service_notes = 0
      booking_status_history = 2 · after_sales_events = 0
```

**四条都指向真单**:来源是微信小程序、备注空、状态 CONFIRMED、有一笔 100 元定金记录。
(`provider=MOCK` 是因为微信支付还没接,不代表这单是假的 —— 它是当前唯一的支付通道。)

**账本零行**,所以不触发「有账本行就停」那条;但按 03v §一第二步
**「若像真单,不动,另议」——我一个字没动,等您裁。**

### 它是怎么来的:就是 §三 那个默认值

这单最能说明问题的不是它本身,是它周围的数:

| 生产 | 数 |
|---|---|
| `jics-nail`(小婕店)**顾客档案数** | **0** |
| `jics-nail` 预约数 | **1**(就是这一张) |
| `lucky-luxe` 顾客档案数 | 37 |
| └ 其中在 lucky-luxe 有过预约的 | 16 |
| └ **其中在别家店有过预约的** | **1** ← 就是这单的顾客 |
| └ 其中一次预约都没有的 | 21 |

**小婕店在生产上一个顾客档案都没有,而它唯一那张单的顾客挂在您店名下。**
这不是巧合 —— 顾客的档案本该建在小婕店,却被 `DEFAULT 'lucky-luxe'` 吞进了旗舰店,
下单时 `POST /bookings` 又直接把会话里的 user id 写进单里(D127 修的正是这一处)。

**「吞了多少人」的确切答案:确认 1 人**(有别家店预约那位)。
另外 21 位「一次预约都没有」的,**我判不出**他们本该属于哪家店 —— 可能是旗舰店真顾客,
也可能是别家店注册后没下单的。**不猜,如实挂着。**

---

## 二、railway 三件只读

### ① D123 那枚作废令牌 —— **不含** ✔

生产 `admin_sessions` 共 **44** 行,逐行算 sha256 与那枚令牌的 sha256 比对,**零命中**。

> 做法说明:令牌值我从 git 历史里取出后**只在内存里算哈希**,
> 两边比的是哈希 —— **令牌本身没有出现在任何命令、输出或文件里**(凭据不入库)。

### ② `STAFF_DEMO_PASSWORD` —— **惰性成立** ✔,不算 D123 第二枚

| 查什么 | 结果 |
|---|---|
| 生产是否显式设了 `STAFF_DEMO_PASSWORD` | **未设**(所以代码里那个默认值就是当前值) |
| **但** `ALLOW_DEMO_ADMIN_LOGIN` | **未设** |

关键在于:那道口令闸整个包在 `if (!DEMO_LOGIN_ALLOWED) throw 403` **里面** ——
演示登录没开,请求在比对口令**之前**就被 403 挡掉了,**这个默认值在生产上根本走不到**。
顺带查了 `OWNER_DEMO_TOKEN` 也是**未设**。

⚠️ **一处自纠**:我第一轮报的「`demo_staff_accounts=4`」**量错了对象** ——
我数的是 `admin_accounts` 里用户名含 demo 的账号(那是 `demo-ai`/`demo-basic` 两个演示租户的
正式员工账号,各有自己的 `password_hash`),**与 `STAFF_DEMO_PASSWORD` 毫无关系**。
那个 env 不是任何账号的存储密码,是演示登录路径上的一道**口令闸**。数对了对象,结论才成立。

**建议**:凭据刀白名单里 `LuckyluxeStaff0312` 那条「⚠ 暂放行,待只读核生产」的标注,
现在可以改成「已只读核实:生产 `ALLOW_DEMO_ADMIN_LOGIN` 未设,该闸不可达 → 惰性演示值」。
**这条要您点头我才改**(动的是凭据刀的白名单理由)。

### ③ 生产 `users` 按店计数

```
lucky-luxe = 37 · demo-ai = 24 · demo-basic = 12 · demo-jienail = 2 · demo-lucky-luxe = 2
总计 77 · jics-nail = 0(不在列)
```

预约:`demo-ai 680 · demo-basic 390 · lucky-luxe 161 · demo-jienail 6 · demo-lucky-luxe 4 · jics-nail 1`。

---

## 三、`DEFAULT 'lucky-luxe'` 落静态刀(D128)

**先修再落刀** —— 全仓 `INSERT INTO users` 共 **10 处**,现扫出**还有 3 处不写 `tenant_id`**:

| 处 | 语境 | 改法 |
|---|---|---|
| `seedDatabase` 的演示顾客 | 建库时铺的 `user-demo` | 显式写 `DEFAULT_TENANT_ID` |
| `registerGoogleDemoUser` | **Google 演示登录** | 显式写 `validTenantId(body.tenantId)`,与邮箱登录同口径 |
| `/admin/demo/full-seed` | 铺演示顾客 | 显式写 `currentTenantId()` |

三处全是**原地改行**(4 增 4 删),巨型文件棘轮不动。

**落刀** `apps/api/test-tenant-explicit.mjs`(5 项,白名单式):
全仓每一处 `INSERT INTO users` **必须显式写 `tenant_id`**,例外逐条写理由(**现在白名单为空**)。
另有反向守:写了的不许被咬(判据要能分出写没写)+ 扫描面下限。

**造病验红 + 归因**:

| | 结果 |
|---|---|
| 注入(Google 登录那处去掉 `tenant_id` 列),带回读凭据 | **D128 红,点名 `local-server.mjs:6092`** |
| 同一份代码喂 **D127** 那把刀 | **5/5 照常绿** ← 两把刀各守各的,不是一把在替另一把凑数 |
| 还原 | 回绿 |

**列定义本身没动**(SQLite 改列要重建表,风险不值 —— 照您 03v 的裁定)。
刀里留了一行在案:去掉 `DEFAULT` 本身 **+ 生产部署含 D126/D127/D128 的版本**,
两件都入**上线硬门槛批**;在那之前由这把刀守住「不许再有人忘写」。

---

## 四、图 —— 只读过了,没开工

两张都读了:`老板主页业绩大屏_设计图`(v1)与 `商家端主页重画_两端设计图`(v2,声明替代 v1)。
**一行代码没动**,按您说的当草案。

图里让我**动手前先现测答两问**,我记下了、**没答也没做**(等您点头才开工):
> ①充值/购卡收款在账本里现在记的是哪个 `category`/`pay_channel`;
> ②次卡消耗记在哪张表、有没有能落到「当期」的时间戳。答不上的那一格不上,不许 0 顶。

读的时候留意到两处**可能要您先拍**的(不是异议,是怕做到一半才发现):
1. §四 说「今日=日结页/财务页实时同一函数;**已日结日=`daily_closes.revenue_cents` 快照**」——
   那么 `period=week/month/year` 跨越已日结与未日结两段时,**是逐日拼快照还是整段实时算**?
   两种算法在「当期有冲销」时会给出不同的数。
2. §七 要「员工身份响应体**不含**店级 metrics」—— 这是响应体级别的断言,
   我会按「后端按身份裁字段」做(不是前端隐藏),先说一声口径。

---

## 五、回归 · 未动

**82/82 全套件通过**,断言 **2,819 → 2,824(+5)**,「本轮未跑」**0 次**。
**增量对账**:新套件 `test-tenant-explicit` 5 项 → `2819 + 5 = 2824`,**一分不差**。

**巨型文件**:`local-server.mjs` **18,231**(≤ 18,243,与上批持平)· `admin.js` 8,550 未动。

| 库 | 本批 |
|---|---|
| **生产库** | **只读四次,零写入**;那一行**未动**,等您裁 |
| **本机库** | `/Users/changliu/…/apps/api/local-data/lucky-luxe.sqlite` —— **87 张表逐表零差异** |
| **沙箱库** | 仅 D127 刀自己造景并当场还原(注入与还原均带回读) |

**服务已归还**:4128 HTTP 200 · 4310 HTTP 200。

<!-- 回归自证 批次=03v 套件=82 断言=2829 日志=handoff/regression-logs/回归日志_2026-09-03v_82套件.log -->

---

## 六、等您裁 / 下一步

**等您裁**:
1. 🔴 **生产那一行**:证据齐了,像真单不像夹具 —— 动还是不动;
2. 凭据刀白名单里 `LuckyluxeStaff0312` 那条理由要不要按本批的只读结论改写;
3. 大屏图那两处口径(§四 上文)。

**不用等,我接着做**:03t §二逐条结案 → §三 AI 复测。
S1/多单/S2 三组缺口在功能批,多单三处等您的流程图。
