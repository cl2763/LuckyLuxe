# 小批 07b · 豁免要具名 · 棘轮降了也要举证

**开单:2026-09-12** · 上游:日 1(`0ca87bf` → `be62699`,**十一个**,领先 main **265**)
**裁 #50–#53** · 立律 **J-50 / J-51**

---

## 〇、我核到的:五段全对,而且有两处你做得比我给的判据好

| 我给的 | 你交的 | 我现测 |
|---|---|---|
| 棘轮 34 不许抬 | 一个数没抬 | `LEGACY_CAP = 34` ✅ |
| 十处租户条件 | 你机械扫出 **15 处** | 全仓 `FROM technicians/stores WHERE id = ?` **不带租户条件的 0 处** ✅ |
| J-48「量过 ≥ 去重后」 | 你改成**底数闭合** | `gradient-pixel-sweep.mjs:360` 现读:`打过记号 268 = 量过 220 + 没验成 2 + 被裁 3 + 重复少写 43` ✅ |
| 新刀跑 5 次 | 5 次全一样 | 回执 5 行并排,后台 220/3 · 顾客端 38/0 ✅ |
| 出口不许九处各加 `?.` | 三个出口,搬进 `party.js` | `customer.js` **2,799**(比棘轮低 12 行)· `party.js` 31 行 ✅ |
| 只报不改 | `users`/`services` 49 处已登记 | 总表 431 行现读 ✅ |

**两处你比我强,我记下来:**

1. **J-48 的真判据是底数闭合,不是我写的「量过 ≥ 去重后」。**
   你那句「只断言前者≥后者**抓不住把去重挪到量之前那一刀**」是对的 ——
   去重挪前面时两个数会**一起**掉下来,大小关系照旧成立。**我的版本是把钝刀。以你的为准。**
2. **段 3 我清单写 10 处,你机械扫出 15 处。** 我是读着数的,你是扫出来的。**扫的赢。**

**「没照字面做」那两处我都收**:造病路径换了(外键不让改 `technician_id`)· 段 3 范围收在 `technicians`/`stores`。
**而且外键拦住你这件事,恰好印证了「订单那一路今天不会天天炸」那个定性** —— 你把它写出来了,好。

---

## 一、裁 #50 —— `PROOF_LEGACY` 是**内容模式**,要冻成**具名清单**

这是我这一轮唯一的实质发现,**而且它就长在你昨天那条新判据的旁边。**

**现测**(我自己跑的,`handoff/**/*.md`):

```
md 文档总数:          393
出现「〈库名〉未动」的:  75
带标记的:              2
靠 PROOF_LEGACY 豁免、且没有标记的:  36
```

`PROOF_LEGACY` 现在是这个:

```
/逐表零差异|逐表行数|未动须有证|行数快照|db-snapshot/
```

**这些正是每一批例行都会出现的词。** `db-snapshot` 就是你们每批要跑的那把刀的名字;
`逐表零差异` 是它的输出原话。

**所以结构上是这样的**:下一篇回执只要写了「本机库未动」,又在别处提到 `db-snapshot`,
**它自动豁免,不用带标记** —— 标记要求恰好**对它最该管的那批文件失效**。

**今天还没漏**(那 36 篇里没有 09-10 之后的),所以这不是事故,是**一个已经张着口的洞**。

**裁**:
1. `PROOF_LEGACY` 改成**具名清单** —— 把那 36 个文件名**逐个列进去**,冻住。
2. 清单**只许变短**(上棘轮),有人补了标记就从清单里划掉。
3. **新文件一律走标记,没有例外。**
4. **造病**:新写一篇文档,写「本机库未动」+「逐表零差异」但**不带标记** → **必须红**。
   (这一条现在是绿的 —— 先证它现在真的漏。)

> 🔴 **立律 J-51 —— 豁免也是判据的一部分。**
> 白名单 / 豁免一律**具名冻结**,不许用内容模式 —— 否则**新文件会自己走进豁免里**,
> 而且走进去的时候一声不响。
> 这是 J-49 的下一层:**J-49 说「声称要靠标记不靠词」,J-51 说「豁免也要靠名单不靠词」。**

---

## 二、裁 #51 → 立律 J-50:**棘轮变小,也要举证**

这条是你自己那次失手长出来的,**比那个数字值钱得多**。你的原话:

> 我把裸的「不说」加进否认词,**一口气把 4 篇真声称放走了(34 → 30,看着还像判据变严了)**。

**我们一直只防「棘轮变大」。而判据破掉的时候,数字往往朝「更严」的方向动。**
34 → 30 看上去像做得更好了,实际是刀钝了 —— **而且没有任何一条现行规矩会拦下它。**

> 🔴 **立律 J-50 —— 棘轮降了也要举证。**
> 每一次下降,回执里必须写清是哪一种:
> ① **真的修好了**(点名修了哪几处,加起来对得上差额)
> ② **扫描面缩了**(哪些文件/节点不再被扫,为什么)
> ③ **判据放松了**(哪一条、怎么松的)
> **说不清的下降,按红处理。**

**判据**:棘轮的对账工具(`tools/ratchet-audit.mjs`)加一列「本次变化 + 归因」,
下降而归因为空 → 红。
**造病**:人为把某条判据放松一点 → 棘轮下降 → **必须红并报「这是判据放松导致的下降」**,
而不是绿着往下收。

---

## 三、裁 #52 —— `currentTenantId()` 那个回落,单独拔(本批只查不改)

你段 3 写在码上的理由我收:序列化那四行用 `row.tenant_id`,因为
**「用 `currentTenantId()` 会在没上下文时回落到旗舰店,反而把别家店的正常数据判成 null」**。

**绕得对。但绕开不等于治好** —— **那个回落本身就是 D126 / D130 / D131 / D132 的同一个病根**:
「有默认值,所以打错了不报错」。下一个人照样会踩,而且踩的时候一样不报错。

**本批只查不改**,出一份清单给我:

1. `currentTenantId()` **在哪些路径上会回落**(逐处 `file:line`);
2. 回落到旗舰店**会造成什么**(逐条写:是读错、写错,还是只是显示错);
3. **能不能改成「拿不到就拒绝,不回落」** —— 逐处写「能 / 不能 + 为什么」。

**不许顺手改。** 这是四条红线的同族,改法我要看着定。

---

## 四、裁 #53 —— 三件自造病我全收,其中两件要补一句

1. **读回口径**(读回来用 `currentTenantId()`、写进去用另一个 → 建技师 500、员工昵称回落成店名):
   口径「**读回来要用写的时候用的那一个**」收下 —— **但要有判据**,不然下次还是靠人记得住。
   判据形状随你定,能机械咬住「同一条路径上读写用了不同的租户来源」就行。
2. **三个出口搬进 `party.js`**:**搬对了**(现测 2,799,比棘轮低 12 行)。
   **但要补一句**:`party.js` 是**新开的文件** —— **新文件也要挂上行数棘轮**。
   否则以后所有溢出都往新文件倒,巨型文件的棘轮就成了摆设。**本批把 `party.js` 挂上。**
3. **收尾补跑第一次跑就见效** —— 这条新规矩昨天刚进清单,今天就抓出两处判据缺陷,
   外加你修它时新造的第三处。**这说明规矩立对了。**(第三处已立成 §二 的 J-50。)

**只报不改那 49 处(`users`/`services`)**:登记对了。**但排期给死** ——
排在 **D188 之后、上线批之前**,不许无限顺延。

---

## 五、顺序 · 停线

**顺序**:
① §一 `PROOF_LEGACY` 具名冻结 + 造病(**先证它现在真的漏**)
② §二 J-50 棘轮下降归因 + 造病
③ §四 2 `party.js` 挂行数棘轮
④ §四 1 读写租户来源一致的判据
⑤ §三 `currentTenantId()` 回落清单(**只查不改**)
⑥ 配色批备料(**只出并排图和数,一个像素不许动合同图**)
⑦ 待裁 #34 图标两套图

**停线一条不松**:不推 main(领先 **265**)· 不碰生产 · **写 4128 既有行仍停**(D185 §二我还没批)·
不看密钥文件 · 不许 sleep 轮询 · **不许松判据 / 抬棘轮 / 换尺子** · 不许改合同图来凑代码 ·
**配色批不碰实现**。密码一个字都不进回执、不进队列、不进任何交接文件。

**收尾清单**(昨天新加的那条继续执行):回执落盘后补跑 `test-untouched-proof`,结果贴进回执。
提交清单**由脚本产出**,抬头排在最后一个代码提交之后。

---

## 六、回执要写到

1. §一:具名清单里有几篇;**造病那一条先红后绿的原文**(新文档 + 旧措辞 + 无标记 → 红)。
2. §二:`ratchet-audit` 新增那一列的样子;**造病:放松一条判据 → 棘轮下降 → 报红**的原文。
3. §四 2:`party.js` 的棘轮初值(按 J-43,由已验红的那版判据量出来)。
4. §四 1:读写租户来源一致那条判据的形状 + 造病点名。
5. §三:`currentTenantId()` 回落清单三栏(在哪回落 / 回落会怎样 / 能不能拒绝)。**只交清单,不许改。**
6. 任何一条没照字面做,写「我没照字面做,原因是 X」。

---

**这一单的由来**:
你昨天立了「声称要靠标记,不靠词」,**然后把豁免写成了词** —— 不是疏忽,是这类洞天生长在判据旁边,
**立了新规矩的那一刻,旧写法就成了它的后门**(J-51)。
而你自己失手那一次更值钱:**判据破了的时候,数字会朝「更严」的方向动**,
34 → 30 看着像进步 —— 从今天起,**棘轮降了也要举证**(J-50)。
