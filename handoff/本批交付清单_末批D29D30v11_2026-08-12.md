# 本批交付清单 · 末批(D29+D30+弹层 v1.1) · 2026-08-12

## D29 工资入口误拦(先答再修)

**L2 复盘(复发登记,D27 同族第 2 例)**:D27 批的同类范围圈的是**「toast+回退撞转场」这个
崩溃形状**(修了流水/记一笔两页),不是「getFinanceKey 前置门禁」这个完整类 —— 工资页的拦法
不同(keyMissing 空态,不回退不崩),于是逃出了当时的扫描。**上次没兜住的原因:按症状圈类,
没按机制圈类。** 永久护栏:本批按机制全仓机械扫 `getFinanceKey()` 全部 6 处用点,逐处判定归档
(见下);此后凡新增财务门禁入口必须 lock-aware(先问 lock-status)。

**全仓扫描证据(getFinanceKey 用点 6 处,0 残留)**:
| 位置 | 判定 | 处置 |
|---|---|---|
| finance-txns(流水) | D27 已修 lock-aware | ✅ |
| finance-entry(记一笔) | D27 已修 lock-aware | ✅ |
| **salary-month(工资试算/发放)** | ❌ 误拦正主:不问门禁直接 keyMissing | **本批修 lock-aware** |
| **member 页代充 modal** | ❌ 同病:门禁关的店也弹「需先解锁财务」 | **本批修 lock-aware**(裁决②"member 页不动"指不放行技师,不含误拦门禁关的店) |
| me 页 financeOn | 显示态非拦截 | 不改 |
| finance 页 init | 本就 lock-aware | ✅ |
| 网页老板端 | 后端 403 才弹解锁,天然 lock-aware | ✅ |

L5:工资入口实点 ×5(门禁关+无钥匙=店主态)→ 直进试算,keyMissing=false。✅ 5/5

## D30 弹层链接打不开(先对账再修)

**对账(如实)**:我在 2784799 批报的「链接实点 5/5 落确认卡」验的是**绑定码弹层**(那批修的
就是它);店主从「继续结算」进的是**签署码弹层** —— 它的链接从来没挂处理器,**同类没扫**,
是我 L2 的漏(修了绑定码、没把"弹层里的链接"当一类扫签署码)。认账。

**修法(店主拍板)**:两弹层统一 —— 占位码下方两颗按钮**「打开链接」「复制链接」**,链接文本
降级为纯展示;打开→落对应页(绑定码→本人确认卡,签署码→签署页);沙盒占位期形态,
真码上线后此二钮随占位一起撤(发版清单不变)。
L5 实点:签署码 打开→签署页/复制→读回剪贴板≡qr.url ×5 ✅;绑定码同款 ×5 ✅。

## 弹层 v1.1(图已改)

底栏「关闭」删除(✕/点空白已够);已签=一颗通栏「查看签署原图」;未签底栏整条不显示。
L5:两法关(✕/空白轮换)+关后遮罩 0+原图进出 ×5 ✅。

## 上批一问答复(CI ⑱ 口径)

微信报错原文说 "4 Chinese characters",官方文档口径是「confirmText 最多 **4 个字符**」(中英同计)。
⑱ 已从「汉字>4」升级为**「总字符>4 即红」**(取更严),全仓预扫 0 超限后落地。

## 假设清单
- A-1 工资页门禁开+钥匙过期仍走既有 FINANCE_LOCKED 兜底(keyMissing 空态),未改;
- A-2 签署码「打开链接」落 /pages/sign?code=(与顾客真扫同一落点);
- A-3 member 页代充仍是老板入口(权限口径未动,只修了门禁关误拦)。
