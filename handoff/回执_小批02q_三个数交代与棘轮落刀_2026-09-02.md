# 回执 · 小批 02q —— 三个数交代 + 棘轮落刀

## ① 断言数 2,675 → 2,635:**两把尺子量两批日志,一条断言都没少**

**结论先说:这两个数不是同一个量。**

我把三批存档日志翻出来对了一遍,发现回执里报的「绿行」跟日志 `^ok` 行数**每次都差 40**:

| 批次 | 存档日志 `^ok` | 当时回执报的绿行 | 差 |
|---|---|---|---|
| 01u | 2,594 | 2,634 | +40 |
| 01v | 2,607 | 2,647 | +40 |
| 01w | 2,621 | 2,661 | +40 |

差得这么稳,只可能是口径。逼出来了 —— **旧「绿行」= `^ok ` + `checks passed` 汇总行 + `✅` 收尾行**:
`grep -cE '^ok |checks passed|✅'` 在 01w 日志上 = **2,661**,与回执分毫不差。
也就是说旧口径**把套件汇总行也当成绿行数了**(那不是断言,是每套跑完的那句"通过 N 项")。

拿这把旧尺子回量:

| 日志 | 旧尺(绿行) | 新尺(纯断言) |
|---|---|---|
| 02o 存档 | **2,675** ← 就是您引的那个数 | 2,634 |
| 02p 本批 | **2,676** | **2,635** |

**同一把尺子对比,两把都是 +1。** 2,675 是 02o 的旧尺,2,635 是 02p 的新尺,横着比等于拿两把尺子比两批。

**那 +1 落在哪、有没有 check 被删** —— 我做了逐套件断言数全量 diff(上次那个 diff 只覆盖打
`[name] all N` 格式的 19 套,`image-placeholder` 打的是 `✅ … 通过 N 项`,**漏在扫描面外了**,
这本身就是「判据的覆盖面要有判据」的一个小案例):

```
逐套件差异(02o → 02p),65 套逐套对:
  test-image-placeholder    25 → 26      ← 自守②,唯一变化
  其余 64 套                一条不差
  总计                      2634 → 2635
```

**七处恒真改写是原地改条件,没删任何 check** —— 逐套数据在此,不是我说了算。

**我认的那一半**:换指标名我没交代。您 02h 定过「基线也是断言」,悄悄换基线和悄悄改断言是一回事。
**新口径我认为该用**(汇总行不是断言,数进去是虚胖),但换尺子必须报批而不是默默换。
现在起两个数**都报**,并在回执里标明哪把尺子。

## ② `admin.js` +31:您猜得基本对,措辞是我的错

逐提交量出来了(`git cat-file blob`):

| 提交 | admin.js | 批次 |
|---|---|---|
| 1f3a2c1 | **8,521** | 01u ← 在案棘轮值 |
| 98cd3e8 | **8,549** | 02c/02d(**+28**) |
| b3d1d24 | **8,552** | 02e(**+3**) |
| 586e757 | 8,552 | 02p(**±0**,我这批只动测试文件) |

- **+28 = 02c 的 D107 签署块**(`signBlock` / `sig-row` / 「查看签署原件 ›」)—— 与您的判断一致;
- **+3 = 02e 假图第八处**(`blankServiceEditor` 的 `imageUrl: ''` + 3 行注释),也是您批过的内容;
- 我这一批 admin.js **一行没动**。

**「只降不升」是错的措辞,我认全责。** 内容您批过不等于数字报过,而我在回执里写的是一句
覆盖全体的「均只降不升」—— **名字说的和事实不一致,正是我这批刚在代码里清掉的那个病,长在了措辞里。**
自此:**棘轮数字每次都报,涨就说涨、降才说降**,且不再由我人肉措辞(见③,数字改由刀打印)。

## ③ 棘轮到底有没有刀在守:**没有。已落一把。**

您 grep 不到是对的 —— 《代码结构公约》③ 的行数红线 08-24 就立了,棘轮律也立了,
但**从来只活在回执措辞里**,跟 `check(..., true)` 同族:看起来在守,其实没人守。

**新刀** `apps/api/test-file-ratchet.mjs`(已入回归清单,排在 `mp-placeholder-size` 前),五条:

| # | 判什么 |
|---|---|
| ① | **白名单式**:全仓 155 个业务文件逐个必须落进「未超公约上限」或「在棘轮表内」——**新长出来的超线文件自动红**,不靠人记得加名单 |
| ② | 棘轮 6 项零上涨(改表 = 申请提高上限,diff 会顶到您眼前) |
| ③ | 两个巨型文件**单独点名钉死**:`local-server.mjs ≤ 18243` 且 `admin.js ≤ 8552`(您 02q 明令要能 grep 到钉的是哪个数) |
| ④ | 反向守:扫描面没缩水(条数下限 140)——文件搬走导致"扫不到所以全绿"必红 |
| ⑤ | 棘轮表零僵尸条目(表里文件改名/删除必须同步改表,防表本身腐烂) |

**取白名单式而非列举那两个文件**,是因为黑名单判据数不到没列的:第三个巨型文件长出来时,
列举式判据看不见它。

**棘轮表 6 项**(前两项是您批过的数,后四项 09-02 首次落刀按现状冻结,受"不许再涨"约束):
```
apps/api/local-server.mjs                     18243   店主 08-24 立 / 09-02 复核在案数一致
apps/web/admin.js                              8552   店主 09-02 补批
apps/web/customer.js                           2811   09-02 现状冻结
miniprogram/pages/me/index.js                   638   09-02 现状冻结
miniprogram/pages/merchant/settlement/index.js  929   09-02 现状冻结
miniprogram/pages/merchant/orders/index.js      829   09-02 现状冻结
```

**每次回归它自己打印这一行**,回执直接抄,不再靠人肉 `wc -l` 加措辞自觉:
```
[棘轮现值] local-server.mjs 18243/18243 · admin.js 8552/8552 · customer.js 2811/2811 · me 638/638 · settlement 929/929 · orders 829/829
```
另有 `[收紧提示]`:降到天花板以下时提示主动收紧(不红)。

### 落刀预判与造病验红

**预判:落地应红 0 处**(表按现状播种)→ **实跑 0 红**,对上。
但绿着入库的刀必须证明它会红。这把刀有三层,**逐层各挨一刀**(判据四:同一把刀要能分出哪层在守):

```
刀A  admin.js 追加 1 行 → 8554
     ❌ ②「apps/web/admin.js 8554 > 棘轮 8552(+2)」  ❌ ③「admin=8554」
     ✅ ①④⑤ 仍绿                        ← 棘轮层在守
     ↩ cp 还原,cmp 字节一致(8552)

刀B  凭空造一个 1300 行的 apps/api/fake-giant.mjs(超 1200 上限、不在表里)
     ❌ ①「156 个业务文件…1 处超线且不在棘轮表」
     ✅ ②③④⑤ 仍绿                       ← 白名单层在守(这是"新文件自动红"那句话的实证)
     ↩ 删除,确认不存在

刀C  把表里 customer.js 改成 customer-RENAMED.js(模拟表腐烂)
     ❌ ⑤「零僵尸条目 :: apps/web/customer-RENAMED.js」
     ❌ ① 连带红 —— 这是对的:文件掉出白名单就该红
     ↩ cp 还原,cmp 字节一致

三刀还原后复跑:✅ ok1–ok5 全绿
```

**三层各红各的、层与层分得开** —— 不是一把"数了个数"的刀。

## 四 · 回报固定六项

1. **改了什么**:新增 `test-file-ratchet.mjs`(5 条)并入回归清单;`run-all-tests.sh` 清单加一项。**产品代码零改动**。
2. **在哪一层验的**:文件系统实测行数层 + 三次真造病(改文件 / 造新文件 / 改表)。
3. **同类扫尽**:全仓三档 155 个业务文件现扫,超线 6 处已全部入表;
   「有数字没有刀」这一类,除这两个棘轮外还有没有第三处 —— **答:回执里报的数字目前只剩"断言基线"一个还没刀,
   已由本刀的 `[棘轮现值]` 打印解决行数那一半;断言数那一半留待您裁**(见下"残留")。
4. **用什么数据验的**:反例三种(涨 1 行 / 长出新超线文件 / 表指向不存在的文件),而非只跑当前快照。
5. **重复稳定性**:棘轮刀本批连跑 5 次(1 次预判 + 3 次造病 + 1 次还原复跑),结果一致;全量回归中再跑 1 次。
6. **残留与不确定**:
   - **断言基线本身还没有刀**。它是跨批次的数,单次运行无从判断"上一批是多少",要守得存基线文件。
     **要不要落,请您裁** —— 不落我就每批照实报两把尺子的数。
   - 棘轮表后四项(customer.js / 三个小程序页)是我按现状冻结的,**您没有逐个批过这四个数**,
     若认为哪个该更低,给数我收紧。
   - 公约③ 还有一条「超线文件不许再加新功能」——这条是**语义**判断,机械刀判不了,仍靠纪律。

**自评 L1–L5**:L1 末端(文件系统实测,过)· L2 同类扫尽(三档 155 个全扫,过)·
L3 无图不结案(不涉界面)· L4 反例数据(三种造病,过)· L5 重复稳定(5 次一致,过)。

---

**棘轮数字(按新规矩每批报,涨就说涨)**:
`local-server.mjs 18,243/18,243`(持平)· `admin.js 8,552/8,552`(持平,本批未动)·
`customer.js 2,811` · `me 638` · `settlement 929` · `orders 829`(均持平)。

**断言数(两把尺子都报)**:新尺(纯断言)2,635 → **2,640(+5**,恰为 file-ratchet 五条,与预判一致);
旧尺(绿行,含汇总行)2,676 → **2,682**。

**全量回归**:✅ **70/70 套件通过**(日志 `handoff/回归日志_2026-09-02q_70套件.log`)。
**清场留痕**:`pgrep` 空 · 9420 空 · 3799 空 · 4128/4310 = 200/200。
**生产库未动 · 本机库未动**。**未推 main**。
