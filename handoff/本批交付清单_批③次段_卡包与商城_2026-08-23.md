# 本批交付清单 · 批③次段:顾客端卡包 + 充值/买卡商城 · 2026-08-23

> **合同**:《S2充值次卡券三线_UI设计图_2026-08-17.html》**§五 顾客端 · 卡包+商城**(手机屏=合同,网页顾客端同批同构、响应式,不另出图)+ §二 已拍口径区 + §三 商家网页端收编条 + §十-2 支付过渡红线。
> **状态**:**清单先交,未开工**(合同式交付第 1 条)。Cowork/店主核过再动代码。
> **纪律**:一项一行;图上没画到的按最合理做并记假设;逐屏并排核验;新增代码禁原生弹窗(㊶ 基线只减不增);涉钱零新径;渲染句后端唯一。
> **摸底结论(开工前查的现状,决定哪些是"新建"哪些是"改造")**:
> - 已有:顾客小程序 `pages/coupons`(券)、`pages/stored-value`(储值)、`pages/points`(积分商城);后端 `/my/coupons`、`/my/stored-value`;商家端 `mall_self_purchase` 开关(支付未接通=锁,`PAYMENT_CHANNEL_OFFLINE`)。
> - 没有:**顾客持卡出口**(`/my/timecards` 不存在,只有商家口 `/admin/customers/:id/timecards`)、**卡包页**、**充值/买卡商城页**、**套餐的「上架商城」标记**(`membership_packages` 无该列)。
> - 网页顾客端:资产卡有积分/券/储值,**无卡包/商城入口**(积分商城处还写着「后续接入」)。
> - 网页端「耗卡」按钮**已取消**(后端 410 常驻),但**帮助文案仍教人去点「耗卡」**(admin.js:2272)=本批顺手清的文案残留。

---

## A 组 · 顾客端「卡包」页(小程序,§五 上半屏)

### 屏A1 卡包入口
- ☑ A1-1 「我的」页新增「卡包」菜单项(与消息/储值/优惠券/门店同族样式),点进 `pages/card-pack`
- ☑ A1-2 入口右侧角标=可用卡券总数(次卡剩余>0 + 未过期未使用券;**0 张不显示角标**,不占位)
- ☑ A1-3 未登录点入口=走既有 requireLogin 动线(不新造登录弹层)

### 屏A2 卡包列表(三类聚合)
- ☑ A2-1 三类聚合一屏:**次卡**(剩 n/N)、**优惠券**(面额+门槛)、**储值**(余额一行,点进现有储值页)
- ☑ A2-2 次卡行:卡名 + 「剩 2/3」+ 到期日(**长期有效**时显示「长期有效」,不显示空日期)
- ☑ A2-3 券行:券名 + 面额 + 使用门槛句(「满 200 可用」)+ 到期日;句子**后端唯一**(与结算页选券面板同源)
- ☑ A2-4 储值行:余额(币种走 `curOf()`,不写死币符)+ 「去充值」按钮(→ 商城,见 B 组)
- ☑ A2-5 排序:可用在前、快到期在前;**已用完/已过期不在本页**(设计图口径:卡包=能用的;历史查记录层)
- ☑ A2-6 空态:三类都空=一句「还没有卡券」+「去看看充值套餐」按钮(不放例句、不放假数据)
- ☑ A2-7(裁案三落地:购买空串不渲染,赠送/活动/积分兑换/售后返还才出小字) 每张只标**到期日**(设计图 08-17 修订原文:来源不上屏,记录层保留供审计)——**与今日口头「三类标来源」冲突,见 §待拍 ①**
- ☑ A2-8 点次卡/券行=展开该张的使用说明(适用项目组/门槛/有效期),**不做核销动作**(核销只在开单结算)

### 屏A3 数据与口径
- ☑ A3-1 新增后端 `GET /my/timecards`(顾客自己的持卡;剩 0/过期不下发,与商家口 `/admin/customers/:id/timecards` **同一函数**,不写第二份)
- ☑ A3-2 卡包页三类数据全部后端下发(前端零计算、零拼句);金额一律 cents 直出
- ☑ A3-3 越权:只能看自己的(token 用户);跨店隔离(x-tenant-id 按当前门店)
- ☑ A3-4 售后返还次数后卡包实时反映(剩余次数回涨=同一读方,不另存字段)

## B 组 · 顾客端「充值/买卡商城」(小程序,§五 下半屏)

### 屏B1 商城入口
- ☑ B1-1 **储值卡页=充值套餐唯一出口**(拍板):`pages/stored-value` 顶部/底部「去充值」→ 商城页;卡包储值行同一入口
- ☑ B1-2 「我的」页**不新增**第二个商城入口(唯一出口纪律;积分商城是另一件事,不合并不改名)

### 屏B2 商城列表
- ☑ B2-1 两类商品同屏:**充值套餐**(充 ¥500 得 ¥550 / 赠 ¥50)+ **次卡**(美甲10次卡 ¥880 / 单次折算 ¥88)
- ☑ B2-2 商品来源=商家端「会员与营销」里**勾了「上架商城」**的套餐(见 C 组;未勾=顾客端不出现)
- ☑ B2-3 赠送额单独成行(「赠 ¥50」),**不与实收混成一个数**(§十-8 赠送=营销让利)
- ☑ B2-4 次卡卡片显示:名称 / 总次数 / 价格 / 单次折算 / 关联项目组 / 有效期(长期有效=写「长期有效」)
- ☑ B2-5 空态:没有上架商品=「本店暂未上架充值套餐」(不放例句)

### 屏B3 购买按钮(支付过渡红线,§十-2 + 规则⑧)
- ☑ B3-1 线上支付**未接通**时:按钮=「**到店购买**」(灰底可点=只出说明,不发起任何支付)
- ☑ B3-2 点「到店购买」=同屏展开一句话说明(「到店后由店员为你办理,可用微信/现金支付」)+ 关闭;**不跳转、不弹原生弹窗、不产生订单**
- ☑ B3-3 **过渡期任何位置不得出现「支付成功」字样**(全仓机械扫描断言常驻)
- ◐ B3-4(后端句位已留 buyButtonText;支付链路归批⑤) 商家端开关 `mall_self_purchase` 打开且支付接通后,按钮句改「立即购买」——**本批只留后端句位,不做支付链路**(批⑤)
- ☑ B3-5 按钮句**后端唯一**(`buyButtonText` 随商品下发),前端不 if/else 拼两套话

### 屏B4 数据与口径
- ☑ B4-1 新增后端 `GET /my/mall`(公开/登录皆可读:上架的充值套餐+次卡;含 `buyButtonText`、`selfPurchaseEnabled`、`locked`)
- ☑ B4-2 **本批零支付径**:不新增任何创建订单/扣款路由;涉钱零新径断言(带 amountCents 的请求不产生账目行)
- ☑ B4-3 充值/购卡的账**只走既有引擎**(随单充值 / 单独代充 / 现场购卡),商城页不产生第二条入账路径

## C 组 · 商家端「会员与营销」补件(网页老板端,§三)

- ☑ C1(查明 S2批① 已有 mall_visible,撤回自造 mall_listed,只留幂等 ALTER) `membership_packages` 加列 `mall_listed`(走 `try/catch ALTER TABLE ADD COLUMN`,老库跟上;`test-schema-consistency` 必须绿)
- ☑ C2 充值套餐/次卡卡片增「**上架商城**」勾选(勾=顾客端商城可见);当前状态一行自证(「上架商城 ✓ · 购买后余额 +300」)
- ☑ C3 「线上自助购买」总开关沿用现有(支付未接通=锁+灰+提示句),**与逐个套餐的上架勾选是两层**:总开关控"能不能自助付",上架勾选控"顾客看不看得见"
- ☑ C4 **券只线下发放**(拍板):商城/卡包**不出现任何领券入口**;券的发放仍只在商家端(老板/员工特批留痕)——本批加机械扫描断言:顾客端零「领券/兑换券」控件
- ☑ C5 网页财务「储值卡」页**帮助文案残留清除**(admin.js:2272 仍写「顾客用卡消费时点『耗卡』」——按钮早已取消,文案还在教人点)→ 改「储值扣款只随结算单签字自动入账」
- ☑ C6 「本月耗卡 / 耗卡率」两个**指标**保留(那是账目指标,不是手动动作);只清"点耗卡"的动作性文案

## D 组 · 网页顾客端同构(§五 末条:同批同构,四之九双端同病)

- ☑ D1 网页顾客端「我的」增**卡包**入口(与小程序同句同排序)
- ☑ D2 网页卡包页=A 组同结构(三类聚合/同句/同空态),响应式,不另出图
- ☑ D3 网页商城页=B 组同结构;按钮句同源(`buyButtonText`)
- ☑ D4 网页资产卡「积分商城后续接入」这句随本批复核:积分商城**不在本批**,保留原句或改「敬请期待」——**待拍 ②**
- ◐ D5(网页端实点走查已过:卡包三态/商城说明句;小程序实拍在跑) 双端同病检查:A/B 组每一项都在网页端过一遍,验收面含两端结论

## E 组 · 断言与回归(常驻)

- ☑ E1 `/my/timecards` 与商家口同函数(单一实现)+ 越权/跨店 4xx
- ☑ E2 卡包三类计数与记录层一致(次卡剩余=total−used;券=active 且未过期;储值=账本 Σ)
- ☑ E3 商城只出上架商品(未勾 `mall_listed` 的不出现);总开关锁定时按钮句=「到店购买」
- ☑ E4 **全仓零「支付成功」**字样(机械扫描,过渡期红线)
- ☑ E5 顾客端零领券控件(券只线下发放)
- ☑ E6 涉钱零新径:本批新增路由全部只读,带金额字段不产生账目行
- ☑ E7 双端 wiring:卡包/商城两页在小程序与网页各自挂上、句子同源
- ◐ E8(边界/空态/越权/幂等已覆盖;并发与异常输入随下一推补) 六类 corner case:边界(剩 1 次/券当天到期/余额 0)、空态(三类全空)、并发(同一张券两端同时用=已有闸)、越权(拿别人 token 读卡包)、幂等(ALTER 重跑数字不动)、异常输入(不存在的套餐 id)

---

## 待拍(开工前请店主/Cowork 定,两处与设计图或口径有冲突)

1. **卡包是否标"来源"**:今日口头令写「卡包三类标来源」,但设计图 §五 有 **08-17 修订**明写「每张只标到期日期,**来源不上屏**,记录层保留供审计」。二者冲突。
   - 案一(按设计图):卡包只标到期,来源仅记录层——界面最干净;
   - 案二(按今日口头):每张加一行小字来源(购买/赠送/活动/售后返还)——顾客能自证"这张哪来的",但屏上信息翻倍。
   **我不自己判**,请拍一个;拍案二我按"小字灰色一行、句后端唯一"做。
2. **网页顾客端「积分商城后续接入」这句**:本批不做积分商城,这句保留还是改「敬请期待」?(纯文案,顺手改)

## 假设清单(图上没画到的,按最合理做并记录)

1. 卡包排序=可用在前、快到期在前;已用完/过期不进本页(设计图只画了可用态)。
2. 商城次卡卡片显示"单次折算"(设计图有)与"关联项目组/有效期"(设计图 §三 有该字段,§五 卡片未画,但顾客买前必须看得到)。
3. 「到店购买」的说明句用同屏展开(不弹层,遵四之七)。
4. 卡包储值行点击=进现有 `pages/stored-value`,不新造储值明细页。

## 施工顺序(审过即按此推进,今天能推多远推多远)

1. 后端 C1(加列)+ B4-1/A3-1(两个只读口)——**先立数据出口**,双端才有同源可用。
2. A 组卡包(小程序)→ D1/D2(网页同构)。
3. B 组商城(小程序)→ D3(网页同构)。
4. C2/C3/C4/C5 商家端补件与文案清理。
5. E 组断言 + 42 套件全绿 + 双端实拍 + 日志落仓(批名:批③次段)。

---

## 裁定A 返工(店主 08-23)· **已被勘误推翻,见下节**

> **判定规则(入 S11 总图)**:顾客端「我拥有的东西」**只许一条路径**——我的 → 我的资产 → 类别。
> 今后新资产类型(含 S15 抽奖奖品)一律挂这条路径,**不许再在「我的」页并列新入口**。

- ☑ A-1 「我的」页资产族入口收敛为 **1 个**(「我的资产」);卡包 / 我的券包 / 积分商城 / 会员权益四个并列入口撤下
- ☑ A-2 会员卡上的三个数字块(积分/券/储值)**只展示不另开路径**,点任意一处落资产总页;等级徽章同理(路径唯一)
- ☑ A-3 「我的资产」重做为**分类总页**:卡包(次卡+券)/储值(→储值页)/积分(→积分页,兑换与商城入口留在积分页内)/会员权益
- ☑ A-4 各行数字同源:后端唯一出口 `/my/assets`(`assetsOverviewOf` 复用 `cardPackOf`/`pointsBalance`/会员出口,本函数零计算)
- ☑ A-5 卡包页本体未动(第一推实拍已合格),只改挂点与入口
- ☑ A-6 网页顾客端同构:菜单资产族只剩一格,`renderAssetsWeb` 改分类总页(四行同源)
- ☑ A-7 常驻护栏:机械扫描「我的」页资产族入口数=1(卡包/券包/积分商城/会员权益并列入口零残留)
- ☑ A-8 同源断言扩到本页:资产页卡包行(次卡/券/合计三个数)≡ 卡包页;储值行 ≡ 卡包储值行;积分行 ≡ 积分页余额;角标 ≡ 三处同一个数

**假设(图上没画,按最合理做并记录)**:①会员卡数字块与等级徽章都落资产总页(而不是各自直达),换取"路径唯一";
②「充值 · 次卡」商城入口不属资产族,已从「我的」菜单撤下,入口保留在 储值页「去充值」/卡包储值行/资产页储值行——**若您要商城在「我的」页也有一格,一句话我加回**。

## 裁定B(payment-success 路径名)· 已落地

- ☑ B-1 查明引用**共 2 处**:`app.json` 页面清单、`checkout/index.js` 提交预约后的跳转
- ☑ B-2 改名 `pages/payment-success` → **`pages/booking-done`**(线上支付未接通,这页本来就是「预约提交成功」不是付款成功;页面内文案原本也没有「支付成功」字样)
- ☑ B-3 断言常驻:`payment-success` 路径名全仓零残留

## 本推补齐的清单项

- ☑ E8 边界/空态/异常输入:新档案三类全空=空态句+角标 0;商城口带无效参数照常 200(只读口不炸);并发面沿用既有券占用闸(一单一张,㋀ 已覆盖)
- ◐ B3-4 支付接通后的「立即购买」句位仍留后端(链路归批⑤,本批不做)


## 裁定A 勘误(店主 08-23 推翻重做)· 已落地

> **我做过头了**:真冗余只有「我的券包」与「卡包」这一对,我却把储值/积分/会员权益一并降级、
> 还把会员黑卡三块从直达改成落总页 —— 最高频三件事各多点一次,黑卡变哑,店主实测「比原来更乱」。

- ☑ 勘-1 **黑卡三块恢复可点直达**:积分→积分页(原位)、「优惠券」改名**卡包**→卡包页(券+次卡)、储值→储值页(原位);等级徽章恢复直达权益页。黑卡=顾客快捷区,不许变哑。
- ☑ 勘-2 **「我的券包」退休**:入口撤 → 页面停用(app.json 撤注册)→ 确认全仓零引用 → 目录删除(`pages/coupons`);积分兑换成功的落点同步改「看卡包」。
- ☑ 勘-3 **储值、积分保持原位置与入口**,不降级、不改名、不塞总页。
- ☑ 勘-4 **命名归一**:统一叫「卡包」;`pages/assets`(我的资产总页)退休并删除;菜单那格与黑卡那格**同名同页**,角标与页内张数同源。
- ☑ 勘-5 保留裁定A 里对的部分:`cardPackOf`/`usableTimecardsOf` 同源出口、门槛句唯一出口、来源小字案三、`/my/assets` 只读口(留作数据源,不再作为页面入口)。
- ☑ 勘-6 护栏断言改写:**「券包」与「卡包」不得同时存在两个入口** + 命名归一(两页均已退休且零引用)+ **黑卡三块必须可点直达**(反向断言:不许落总页)。
- ☑ 勘-7 网页顾客端同构:菜单格改名「卡包」直落卡包页;会员卡三块改为可点(积分/卡包/储值);`renderAssetsWeb` 保留为老路由兜底,直接落卡包页(不留第二个名字)。

### 判定规则修订(入 S11 总图,替换前一版)
> **同一类资产只许一个页面、一个名字;但高频资产允许从会员黑卡直达。**
> 「唯一实现」约束的是**页面与数据出口**,不是入口数量。
