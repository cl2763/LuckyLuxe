# 写库脚本护栏 · 三列清单(**本文件由刀生成,不要手改**)

> 生成器:`tools/gen-guard-checklist.mjs` · 尺子:`tools/guard-scan.mjs`(**唯一一份**,
> `test-db-target-guard` / `test-txn-rollback` 用的是同一个 `scanWriteSites()`)
> 生成于提交 `a236329`
>
> **为什么不手写**(店主 03z §一 裁):手写那份停在 09-02 —— 第二列 10 处还写着 ⬜ 而护栏早已接上,
> 第三列九处也已验过回滚,清单没跟着动。更要紧的是:清单写着「包了事务」的九处里,
> `ledger-guards` 现测**根本没有事务**。**清单是人写的,刀是机器咬的,以刀为准。**
>
> **为什么光"由刀生成"还不够**(店主 04a §一 病二 裁):03z 那次,清单在**同一个提交上就过期了**
> —— 生成它的尺子和守它的刀各写了一份,两份各自漂。所以现在:①尺子只此一份(`guard-scan.mjs`);
> ②`test-db-target-guard` 常驻守「本文件 ≡ 当前提交现扫输出」,不一致就红并让你重跑生成器。

## 尺子(与刀同一把)

```
扫描面   git ls-files 递归全量的 tools/**.mjs|sh + apps/api/**.mjs|sh
排除面   「刀本身」——(a) 路径含 /test- 或 /run- 的;(b) 显式声明的两个:
           · `tools/guard-scan.mjs` —— 尺子本体 —— 正则里写着 DatabaseSync / BEGIN IMMEDIATE / requireTarget 当判据,扫它等于在量尺子
           · `tools/gen-guard-checklist.mjs` —— 清单生成器 —— 同上;它不写库,它只把现扫结果排版成 md
         排除理由:刀的源码里把 DatabaseSync / BEGIN IMMEDIATE / requireTarget 当**尺子的字面量**写着,
         扫到刀自己身上量的是尺子不是产品。两个方向都会说谎:既会把生成器当成"含事务的写库点"
         (03z 那份清单就这么多列了一行),也会把它当成"接了护栏"(现测:①c 因此对它是绿的)。
写库定义 直连 sqlite 写(DatabaseSync / .prepare().run / db.exec)或走 HTTP 打写口(POST|PUT|PATCH|DELETE)
分类     A 独立可跑脚本 = 护栏的对象;B 被 local-server 可达或被任何 tracked 文件 import = 服务统一自报
剥注释   保住行号(`\s` 含换行会吃掉空行,行号一错全错 —— 店主 03x 收编)
```

## 现测底数

| | 数 |
|---|---|
| 候选文件 | 236 |
| 会写库 | 111(A 类 56 · B 类 55) |
| A 类该有护栏 | 54 / 56(差额是下面那张「不是数据库」表) |
| **A 类未接护栏** | **0** — 全部接上 |
| 含事务 | 18 |

### 「会写库但打的不是数据库」· 2 个(不在护栏判据里,逐条写理由)

- `tools/ops-cos-delete.mjs` —— 打的是腾讯云 COS 对象存储,不是数据库;钥匙从 env 读、--yes 才真删。什么时候要动:若它开始写库
- `tools/smoke-cos-upload.mjs` —— COS 上传冒烟,不碰数据库。什么时候要动:若它开始写库

## A 类 · 56 个

| 脚本 | 形态 | 护栏(去默认目标 + 自报) | 事务 |
|---|---|---|---|
| `apps/api/e2e-stress-test.mjs` | HTTP | ✔ requireTarget | — |
| `apps/api/probe-intents.mjs` | HTTP | ✔ requireTarget | — |
| `apps/api/sandbox-mirror-jury.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `apps/api/smoke-qwen.mjs` | HTTP | ✔ requireTarget | — |
| `apps/api/tools/demo-seed.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `apps/api/tools/purge-smoke-conversations.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/ai-eval/audit-contamination.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/ai-eval/booking-rate.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/ai-eval/fact-hit.mjs` | HTTP | ✔ requireTarget | — |
| `tools/ai-eval/human-five.mjs` | HTTP | ✔ requireTarget | — |
| `tools/ai-eval/rejudge.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/ai-eval/scenario12/run.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/audit-test-tenants.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/board-overlap-proof.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/close-eval-conversations.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/configure-jienail.mjs` | HTTP | ✔ requireTarget | — |
| `tools/contrast-sweep.mjs` | HTTP | ✔ requireTarget | — |
| `tools/db-snapshot.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/drop-tenant-default.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/heartbeat-shape.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/migrate-cross-tenant-bookings.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/mp-ai-chat-proof.mjs` | HTTP | ✔ requireTarget | — |
| `tools/ops-cos-delete.mjs` | HTTP | — | — |
| `tools/ops-rename-tenant-display.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/ops-reset-finance-lock.mjs` | HTTP | ✔ requireTarget | — |
| `tools/prepare-migration.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/repair-identity-tenant.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/seed-addon-groups.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-bigdemo.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/seed-customer-doc-scene.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/seed-demo-coupons.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-demo-rich.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/seed-demo-today.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-demo-twin.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-jics-mirror.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/seed-jics-nail.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-luvia-bj.mjs` | HTTP | ✔ requireTarget | — |
| `tools/seed-newcomer-coupon.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/seed-selfcheck-data.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/baseline_grp_fixture.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/d40_five.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/d41_cta.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/d41_flip.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/full_path_regression.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/lineup_walk.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/matrix_walk.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/r5_lineup.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/r6_samples.mjs` | 直连库+HTTP | ✔ requireTarget | — |
| `tools/sim/s1_mirror.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/switcher_walk.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/v14_walk.mjs` | HTTP | ✔ requireTarget | — |
| `tools/sim/xj6_tiers.mjs` | HTTP | ✔ requireTarget | — |
| `tools/smoke-cos-upload.mjs` | HTTP | — | — |
| `tools/tenant-fingerprint.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/verify-jics-kb.mjs` | HTTP | ✔ requireTarget | — |
| `tools/white-screen-proof.mjs` | HTTP | ✔ requireTarget | — |

## B 类 · 55 个

| 脚本 | 形态 | 护栏(去默认目标 + 自报) | 事务 |
|---|---|---|---|
| `apps/api/account-refund-schema.mjs` | — | — | — |
| `apps/api/account-refund.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/admin-auth.mjs` | — | — | — |
| `apps/api/ai-review-routes.mjs` | — | — | — |
| `apps/api/ai-utils.mjs` | HTTP | — | — |
| `apps/api/booking-drafts.mjs` | — | — | — |
| `apps/api/cash-notes.mjs` | — | — | — |
| `apps/api/conversation-log.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/conversation-routes.mjs` | — | — | — |
| `apps/api/customer-login-fixture.mjs` | HTTP | — | — |
| `apps/api/dashboard-pulse.mjs` | — | — | — |
| `apps/api/db-concurrency.mjs` | — | — | — |
| `apps/api/demo-facts.mjs` | — | — | — |
| `apps/api/demo-mark.mjs` | — | — | — |
| `apps/api/demo-reset.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/deposit-audit.mjs` | — | — | — |
| `apps/api/finance-ledger.mjs` | — | — | — |
| `apps/api/hero-slides.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/import-customers.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/import-services.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/kb-routes.mjs` | — | — | — |
| `apps/api/ledger-guards.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/legacy-demo-retire.mjs` | — | — | — |
| `apps/api/local-server.mjs` | 直连库+HTTP | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/membership-config.mjs` | — | — | — |
| `apps/api/message-templates.mjs` | — | — | — |
| `apps/api/mini-phone.mjs` | — | — | — |
| `apps/api/notify-scheduler.mjs` | — | — | — |
| `apps/api/other-tenant-names.mjs` | 直连库 | — | — |
| `apps/api/platform-auth.mjs` | — | — | — |
| `apps/api/platform-ops.mjs` | — | — | — |
| `apps/api/platform-session.mjs` | — | — | — |
| `apps/api/pricing-categories.mjs` | — | — | — |
| `apps/api/quote-state.mjs` | — | — | — |
| `apps/api/reminder-tasks.mjs` | — | — | — |
| `apps/api/repeat-guard.mjs` | — | — | — |
| `apps/api/schedule-board.mjs` | — | — | — |
| `apps/api/schema-unique-rebuild.mjs` | — | — | — |
| `apps/api/store-identity.mjs` | — | — | — |
| `apps/api/store-matrix.mjs` | HTTP | — | — |
| `apps/api/stored-value-reversal.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/stored-value.mjs` | — | — | — |
| `apps/api/tenant-default-drop.mjs` | — | — | — |
| `apps/api/tenant-fill-triggers.mjs` | — | — | — |
| `apps/api/tenant-profile.mjs` | — | — | — |
| `apps/api/tenant-visibility.mjs` | — | — | — |
| `apps/api/user-columns.mjs` | — | — | — |
| `apps/api/user-identity.mjs` | — | — | — |
| `apps/api/wecom-conversation.mjs` | — | — | — |
| `apps/api/wecom-record.mjs` | — | — | — |
| `apps/api/wecom-routing.mjs` | — | — | — |
| `apps/api/write-intake.mjs` | — | — | — |
| `tools/clean-test-tenants.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/db-backup.mjs` | 直连库 | ✔ requireTarget | — |
| `tools/db-target.mjs` | 直连库+HTTP | ✔ requireTarget | — |

## 第三列 · 「包了事务必须验回滚」

含事务的 **18** 处,逐个由 `test-txn-rollback` 守:
静态查 BEGIN / COMMIT / ROLLBACK 三件;行为层在**沙箱库的临时副本**上造中间步失败,验第一步零行落地,
再跑真路径验两步都落地(反向守 —— 一把「怎么跑都零行」的刀证明不了回滚)。

| 脚本 | 形态 | 护栏(去默认目标 + 自报) | 事务 |
|---|---|---|---|
| `apps/api/account-refund.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/conversation-log.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/demo-reset.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/hero-slides.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/import-customers.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/import-services.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/ledger-guards.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/local-server.mjs` | 直连库+HTTP | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/sandbox-mirror-jury.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `apps/api/stored-value-reversal.mjs` | — | — | ✔ 有事务(含 ROLLBACK) |
| `apps/api/tools/demo-seed.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `apps/api/tools/purge-smoke-conversations.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/clean-test-tenants.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/drop-tenant-default.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/migrate-cross-tenant-bookings.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/repair-identity-tenant.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/seed-demo-rich.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
| `tools/seed-jics-mirror.mjs` | 直连库 | ✔ requireTarget | ✔ 有事务(含 ROLLBACK) |
