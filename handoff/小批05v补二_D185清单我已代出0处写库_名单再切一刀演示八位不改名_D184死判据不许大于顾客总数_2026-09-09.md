# 小批05v 补二 — **D185 的只读清单我替你出了**(`handoff/night-runs/D185_4128待清单_2026-09-09.md`,全程只读、0 处写库)· 名单**再切一刀**:标着「(演示)」的那八位**不许改名** · **D184 加一条死判据:新增持卡任何时候不许大于该店顾客总数**(604 > 75,这一条就定案了)

> 2026-09-09 08:25 CST 醒来第 2 次 · 现查:`git log` 仍停在 `5d07e6f`(无新提交)· `claude auth status` 仍未登录 · Code 停着等店主粘话 · 我这一轮只做只读,没写库、没写 4128 · 两处各一份

## 一、D185 §一 的活我做完了,你只做 §二(等批)

清单已在 `handoff/night-runs/D185_4128待清单_2026-09-09.md`(md5 `c7557226`,桌面交接夹 + 仓各一份)。里头有:逐位夹具顾客(挂单/结算/储值/次卡四个数)· 积压报价按租户与按月的分布 · 「今天的行」单列 · **将要执行的 SQL 原文** · 执行后必须交的证据清单。

现查两条事实,替这件事定了性:

- 积压报价 **69 条,日期全落在 2026-07-14 → 2026-08-08,今天 0 条** —— 没有一条是店里真有人在等;
- 夹具名顾客 **lucky-luxe 26 · jics-nail 12**,**没有一位挂着钱**(储值流水最多 6 笔、结算最多 8 笔,全是评测造的)。

**你现在不用再做清单,直接等我的批。** 批下来之前:**4128 一个字不许写**。

## 二、名单再切一刀(这条是新的,补进 D185)

`demo-cust-01..08` 那八位——「林小雅(演示)」「Emma Liu(演示)」「王梦琪(演示)」…—— **不许改名**。

理由:它们本来就**老老实实标着「(演示)」**,店主看着不刺眼,也不会误当成真顾客;改成像真人的名字,反而是把「这是演示数据」这条信息抹掉了,方向正好反了。

要改的只有**名字里带随机 id 段或「测试 / 闸测 / 演示2-」的那些**:`运营字段测试-mrm0lewr`、`储值测试-mrm0leqb`、`测试-统一口径-0X`、`演示2-0X纯新客`、`临时新客测试` 之类。

**顺带回查段 1**:沙箱库那次改名(28 位)有没有把「(演示)」这八位也一起改了?改了的话**按这条改回去**,并在回执里写清楚改回了几位。判据加一条:改名刀的白名单**必须排除**「以「(演示)」结尾的显示名」;造病:把 `demo-cust-01` 塞进待改名单 → 红并点名。

## 三、D184 加一条**死判据**(比 12 组全等更早生效)

我又追了一层,现在可以断定**不是数据漂移**:

- 图上其它数确实漂了(现金 5,614 → 库里现在 5,760;总卡耗 378 → 238),因为夜里种子还在加行 —— 所以光说「屏幕和库不一样」还不够定案;
- **但新增持卡这条不用比时点也能定案**:它的口径是「当期首次开卡的顾客,**按顾客去重**」,而 `lucky-luxe` 这家店在库里**总共只有 75 位顾客**。**604 比这家店的顾客总数还多 8 倍** —— 去重后的人数不可能超过人数本身。

**裁**:D184 的判据抬头先加这一条死判据 ——

> **「新增持卡」在任何店、任何周期,都不许大于该店 `users` 表里该租户的顾客总数。** 大于即红,并把两个数(算出来的值 / 该店顾客总数)一起打印。

这条不用查历史、不用对时点,一跑就咬得住;12 组全等那条照旧做,当第二道。造病:把去重那层 `GROUP BY user_id` 拿掉 → 死判据当场红。

## 四、顺序不变

**D184(死判据 + 12 组全等)→ 裁 #21 三条路 → D186 → D185 §二(等我批,先别动)→ 段 10/11。**

交齐照 05v 补一 §六;另加:段 1 改名白名单回查结果(改回几位)· D184 死判据 + 造病。

**停线照旧**:写 4128 既有行停 · 四红线修法不明停 · 沙箱模型 >600 万 token/夜停 · 回归红 30 分钟定位不到停 · 不推 main · 不碰生产 · 不看密钥文件 · 不许 sleep 轮询。
