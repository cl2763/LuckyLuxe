# J-62 第二款 · 第一遍静态筛

扫了 127 个套件。断言「写成功」的 **516** 条(另 20 条验的是「该拒」,不在本刀范围):
  · **A 类(嫌疑名单)**:只读这一次调用的返回体 —— **246 条**
  · B 类:从事实那头读回来了 —— 270 条(从事实那头读回来了(条件本身或条件里的变量来自 GET / 查库))

⚠️ 静态筛只缩小范围,**不能替代造病** —— 判别式只有一个:注掉落库,它红不红。

## 🔴 按**口**聚类(造病的单位是「落库那一行」,不是「断言」)

| | 数 |
|---|---|
| A 类断言 | 246 条 |
| **它们落在多少个口上** | **83 个** |
| 追不到口的断言 | 11 条(单列,不混进上面那堆)|
| 其中 **涉钱的口** | 35 个 |
| 其中 **顾客能走的口** | 5 个 |

> **一个口造一次病就够** —— 237 条不用造 237 次,造 83 次。

## 涉钱的口(优先造病)

- `/settlements/:x/sign` —— 挂着 20 条 A 类断言
- `/admin/settlements` —— 挂着 17 条 A 类断言
- `/admin/settlements/preview` —— 挂着 14 条 A 类断言
- `/admin/settlements/:x/amend` —— 挂着 9 条 A 类断言
- `/admin/stored-value/recharge` —— 挂着 9 条 A 类断言
- `/admin/settlements/:x/allocate` —— 挂着 9 条 A 类断言
- `/admin/finance/transactions` —— 挂着 6 条 A 类断言
- `/admin/settlements/:x/void` —— 挂着 5 条 A 类断言
- `/admin/stored-value/refund` —— 挂着 5 条 A 类断言
- `/admin/bookings/:x/deposit-receipt` —— 挂着 4 条 A 类断言
- `/admin/pricing/categories` —— 挂着 4 条 A 类断言
- `/admin/coupon-grants/custom` —— 挂着 4 条 A 类断言
- `/settlements/:x/coupon` —— 挂着 4 条 A 类断言
- `:x/bookings/:x/cancel` —— 挂着 3 条 A 类断言
- `/admin/deposit-config` —— 挂着 3 条 A 类断言
- `/settlements/:x/claim` —— 挂着 3 条 A 类断言
- `/admin/finance/deposit-conservation/repair` —— 挂着 2 条 A 类断言
- `:x/payments/mock/confirm` —— 挂着 2 条 A 类断言
- `/platform/tenants/:x/finance-lock/reset` —— 挂着 2 条 A 类断言
- `/admin/packages` —— 挂着 2 条 A 类断言
- `/admin/settlements/:x/sign-token` —— 挂着 2 条 A 类断言
- `/admin/timecards/:x/refund` —— 挂着 1 条 A 类断言
- `/admin/cash-notes/:x/reverse` —— 挂着 1 条 A 类断言
- `/admin/settlements/:x/coupon` —— 挂着 1 条 A 类断言
- `:x/payments/stripe/confirm-session` —— 挂着 1 条 A 类断言

## 顾客能走的口(次优先)

- `/my/bookings/:x/after-sales` —— 挂着 2 条
- `/auth/wechat/mini-login` —— 挂着 2 条
- `/auth/email/login` —— 挂着 1 条
- `/auth/email/register` —— 挂着 1 条
- `/bookings` —— 挂着 1 条
