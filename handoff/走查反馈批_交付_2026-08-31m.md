# 店主走查中间反馈批 · 交付(2026-08-31m)

> 安全保证:**生产库未动(只读现状两查除外)· 本机库未动**;沙箱=对齐财务默认关(动前快照
> lucky-luxe.pre-finlock-off-*)。未推 main。做完不说可以关帐。
> 回执顺序照令:五 → 一 → 二 → 三 → 四 → 六 → 七。

## 五 · 小婕店周一有排班:归因(证据)

**是我造景盖的,不是缺陷。** 沙箱库现测:她的每周设置周一 `is_closed=1`(她没记错);
但我 08-31 造景时给 2026-08-31(店本地周一)写了**特殊日「临时营业 09:00-21:00」**
(note 原文就是「走查造景:临时营业」),而排班/台面的优先级=特殊日先于每周设置 → 周一亮了。
**转店主的两条规则(请 Cowork 转)**:
> ① **特殊日期 > 每周设置**:每周营业时间是"平常怎么开门";特殊日期是"某一天例外"(节日休息、
> 临时加开)。同一天两边都有说法时,听特殊日期的 —— 所以走查镜像里周一营业,是我为了让您
> 当天能看到台面,给那天加了条"临时营业"的例外,您的周一休息设置一直没动。
> ② **老板代排可绕休息日**(既有裁定):您自己在后台给顾客排单,休息日也排得进(比如休息日
> 给熟客加个钟);顾客自己在线约,休息日约不进。

## 一 · 通知与回访:照图重做 ✅(退回件,两端同批)

**N6 归因一行**:她的 4310 上没有网页入口,是**挂载位错** —— 旧入口挂在「营业时间」抽屉的展开体内,
门店设置行清单里根本没有这一行,不点开营业时间永远看不见(与版本无关;我此前 L1 截图也得先展开营业时间才拍到)。

照图七条逐条:
| 合同 | 落法 |
|---|---|
| 一 入口与结构 | 两端统一=门店设置→通知与回访:网页新增**独立 settings 行**;小程序门店设置页新增入口行、manage 网格旧入口摘除;页内规则/记录两页签(黑白规格) |
| 二 两组八类 | 预约通知组(默认开:新预约确认/改期/取消/预约前提醒)+ 关怀回访组(默认关:次卡到期/生日/定期回访/券临期),组题+说明句两端逐字同串 |
| 三 每卡四件套 | 开关(点即存,**只动开关不碰参数** —— 重做途中咬获并修死「部分 PUT 把自定义提前量打回默认」的真缺陷,断言常驻)+ 参数(数字框+后端闸)+ **文案模板多行可编辑**(变量芯片点击插入/实时预览走 `POST /admin/notify/preview` 与落库同一渲染出口/恢复默认)+ 保存 |
| 四 零糙 | 版式按图与账调同规格(页签/卡距/字号);像素终验=店主走查 |
| 五 诚实句 | 页顶常驻,两端逐字同句(断言钉) |
| 六 记录页 | 三色状态点(已记/待发/失败+原因可点开;另有灰=已撤带原因 —— **图上没画撤销态,按空白处报裁**)+ 事由 + 顾客名 + 渲染后全文 + 类型筛选;员工可读、规则仅老板(N5 断言) |
| 七 文案编辑不等 ICP | 模板列 `notification_rules.template_text` 落库;八类各有默认模板与**变量白名单**;未知变量保存/预览双口 400;空=自动恢复默认;新通知即用新文案(N1 行为断言) |

**判据**:test-notify-scheduler 36→**49**(N1 新文案渲染/N2 零 {xx} 残留+未知变量 400/N3 置空还原/
两端同三口+同文案机械/N5/N6/部分 PUT 保参数)。
**变异刀三把**:N1(渲染无视自定义→红,红文附被忽略的默认句)/N4(单端整口改名→红)/N6(摘入口行→红)。
**J 族两案(刀 N4/N6 头一刀没红咬出)**:`includes` 子串盲 ——「rules-v2」含「rules」、「TitleX」含
「Title」照样绿;判据改**引号/属性闭合整串钉**后两刀均红。另记刀法教训:改口刀要把该端点**全部**调用
一起改名,只改一处会被同文件其余原串遮住。

## 二 · 直排时间可改 ✅

**先对合同**:小程序直排面板**本来就能改**起始时间(wxml 306:`<picker mode="time">`「选时段 ›」,
DOM 现册)—— 店主手上网页版没有=单端缺。**对齐**:网页直排面板加「起始时间」(type=time,
默认=空档起点,min/max=该空档);两端同批补**空档内软钳**(越界 toast 一句退回),后端 409 闸原样。

## 三 · 网页老板侧写小记 ✅

新模块 service-note-modal.js:客户卡四按钮补齐「写服务小记」(文本+图片≤9,同一
`POST /admin/service-notes`、同一 data: 存储栈);员工端「我的客人」写口未动。

## 四 · 财务密码默认关 ✅(店主裁定)

- 新店默认关=既有现状(test-finance-lock ①「新店默认不开」常驻断言,零改动);
- 沙箱存量对齐:**开着的 2 家(两镜像,是我走查造景开的)→ 0**(快照先行;哈希保留,想开去财务设置原位再开);
- 生产只读现状:**6 家全部 enabled=0**(两家 demo 店留有历史哈希但未启用)—— **零店需要改,无需报批动生产**。

## 六 · AI 功能面盘点 ✅(只报不改)

全表见 handoff/AI功能面盘点_2026-08-31m.md。一句话:**国内上线不用换供应商**(生产已指
阿里云香港 Qwen 专属部署),唯一欠的动作=生产真模型冒烟(上线对表已有);沙箱没 key 时以
mock/模板句降级,链路可走查;「AI 图库」实际不调大模型(历史称呼)。

## 七 · 登记两条 ✅(不做)

1. **UI 总审批**入队尾(店主裁定:全功能完成后,整站字体/字号/字距/emoji/间距排查)—— 登于本回执与台账,现在不动;
2. **手机号授权**入上线材料对表(国内版可开,需小程序认证+类目);身份模型照旧:平台身份=微信/手机号,
   店内身份=每店档案+会员码(每店唯一),一号多店=多档案(O-id1 合法形)。

## 回报固定六项

1. **改了什么**:notify-scheduler 模板域(默认模板/变量白名单/渲染出口/preview 口/部分 PUT 保参数/记录带顾客名,
   441→471 行)+ 两端通知页整体重写 + N6 双入口重挂(预约规则行同病同修)+ 直排时间两端 + 写小记弹层 +
   沙箱财务锁对齐;套件 notify 49 / money-inputs 白名单键随重构改名。
2. **在哪一层验**:断言层(49 项含行为 N1-N3)+ 三刀红-绿 + 沙箱/生产库层现测(五/四)。
3. **同类扫尽**:N6 挂载位病=营业时间抽屉内全查(booking-rules 同病同修);includes 子串盲=本套件两处全加界定符
   (**全仓其它套件同病待扫,登记下批**)。
4. **用什么数据验**:自定义文案 TESTX 形/未知变量形/置空形/单端改名形/她截图点名的入口缺失形。
5. **重复稳定性**:trio 绿→三刀各红→逐字节还原→全量回归(见下)。
6. **残留与不确定**:①记录页「已撤」灰点=图外空白,报裁;②includes 子串盲全仓扫描登记下批;
   ③零糙像素终验=店主走查(网页)+开发者工具(小程序,先清缓存);④棘轮:local-server 18,127(±0)、
   admin.js 8,505→**8,508(+3:小记钮/委托/挂载注释)**随批报;⑤31l 值日表等件顺延、营销三件照旧在队。

### 回归结果(补记)
- **✅ 全部 65 个套件通过,2,514 项断言全绿**(上批 2,501 → +13:notify-scheduler 36→49)。
  日志:handoff/回归日志_2026-08-31m_65套件.log。端口已还:4128/4310 双活。
- L1 现测(沙箱镜像老板视角):门店设置行清单里「通知与回访」独立行在(N6);展开=规则页照图成像 ——
  黑白页签/诚实句/两组说明/开着的卡四件套全展开(模板框+五芯片+**预览实渲染真店名**
  「小美,您在 Lucky Luxe Ontario 的预约已确认:9月2日 14:00 · 手部美甲。」)/关着的生日卡只剩标题行+开关;
  参数框 120 分/7 天/30 天=库值。整页截图已录会话。
