# 小批 07r · 🟢 **五问批了** —— 带五处补丁,一起改完直接跑,不用再回来等批

**下达时刻**:2026-09-14
**Code 第一件事**:落进桌面夹 + 仓里 `handoff/`,报两处**全 32 位** md5。

---

## 〇 **批了**

第二版我逐条读过。七处退回**全部改对**,而且三处你**在本机库上现算了证据**,不是口头说改了:

- **② 的代价现测出来** —— 旧口径把 `lucky-luxe` **44 个有积分的顾客全数成 0**(`jics-nail` 18 → 0)。
  我说「从没兑换过的老顾客会被数成 0」,你证出来的是 **全军覆没**。
- **① 底数闭合逐店对上**,而且你点出了那句最要紧的:
  > 「本机库 `is_migrated` 为空的是 0 行 —— **这正说明第一版那个 NULL 陷阱在本机库上『跑得通』,它要到生产才会露馅。**」
- **③ 三档全 0 且无差,而你不把它当成「没问题」** ——
  > 「本机库的号是脚本按模板生成的,格式天生一致。这里的 0 只当成『这把尺子在这份数据上量不到差』。」

**这三件合起来说明一件事:你现在不是在「按令改」,你是在「证明改对了」。** 这两件差得很远。

**下面五处一起改完,直接跑,不用再回来等批。** 五处全都还是纯 `SELECT`,不动安全面。
**但里面有一个内建的停下来条件(§二⑤),那一条必须遵守。**

---

## 一 五处补丁

### ① 问 1 把「涉及顾客数」丢了 —— 第一版有,第二版没了

三档现在报的是「**有多少个号码**对应多于一条档案」。
**我要的是人数** —— 因为它要和「有余额的人数」对得上:**5 个号码 × 各 2 条 = 10 个顾客受影响。**

**改**:三档各补一列「涉及顾客数」(六列:档1号码数/档1人数/档2号码数/档2人数/档3号码数/档3人数)。

### ② 问 1 漏了 `dig <> ''`,而问 4/问 5 有它 —— **同一把尺子两处口径**(J-39)

`phone` 若是 `'---'` 这种纯符号,`dig` 会变成空串 → **所有这类聚成一堆,被报成「同号多条」** → 假阳。
问 4 和问 5 里你写了 `b.dig <> ''`,**问 1 没写。**

**改**:问 1 的档2/档3 补 `dig <> ''`;**并把「dig 为空的档案数」单列一列**(那本身就是脏数据信号)。

### ③ 问 4 / 问 5 只给了档3,没给档1 —— 看不出「重复」和「格式」各占多少

问 1 让我看三档的差,问 4/5 却只给档3 一个数。

**为什么两档都要**(这段你收着,它是口径):

- **档1(字面)同号多条** → 产品第四条「唯一一条」判成多条 → **不认 → 变新人**;
- **档3 多出来的那些** → 产品**不会**判成多条(字面不同),但**第二条「号完全一致」就过不去** → **同样变新人,只是卡在不同那一条**。

**两条路都通向「变新人」,原因不同。而档3 − 档1 的差,正是「因为格式不一致而认不出来」的那批。**

**改**:问 4 / 问 5 的「同号多条」各给**档1 和档3 两列**。

### ④ `st.subtotal_cents/100` 依赖列**碰巧**是 INTEGER

SQLite 里 `INTEGER / INTEGER` 才是整数除法(截断);若 `subtotal_cents` 存成 REAL,`/100` 就是浮点除,和产品的 `floor()` 对不上。

**你现在这条是对的,但它对得靠运气。**

**改**:显式 `CAST(st.subtotal_cents AS INTEGER) / 100`。
**口径**:**不要依赖「碰巧对」** —— 这是 J-63 第三问(单位对不对)的亲戚。

### ⑤ 🔴 `status = 'signed'` / `status = 'active'` —— **先看取值分布,这一条带停下来条件**

问 3 你给了 `bookings.status` 分布,**好**。
但问 5 用了两个写死的状态值,**而这两张表的状态取值你没看过**:

- `settlements.status = 'signed'` —— **万一还有别的「已完成」态**(比如 `settled` / `closed` / 或签过之后又 `void`),积分就漏算了;
- `coupon_grants.status = 'active'` —— 同理。

**改**:加一条 **问 0b · 三张表的状态取值分布**:

```sql
SELECT 'settlements' AS 表, status, COUNT(*) FROM settlements GROUP BY status
UNION ALL
SELECT 'coupon_grants', status, COUNT(*) FROM coupon_grants GROUP BY status;
```

🔴 **内建停下来条件**:
**先跑问 0b。** 如果 `settlements` 里除了 `signed` 还有别的看起来像「已完成」的值,
**或者 `coupon_grants` 里除了 `active` 还有别的看起来像「还能用」的值** ——

> **停下来报给我,不要硬跑问 5。**

理由:**一个写死的状态值配上一张没看过的状态表,就是又一次「按字面比,漂亮地报 0」。**

---

## 二 🔴 一个追问,这是本批最要紧的一问

> 「已造挂断言最多的那一刀(`/settlements/:code/sign`,20 条),红出来的是『签字接口回 200,而库里那张单还是 `pending_sign`』。」

**你报了红,没报活下来的。** 而**活下来的才是这次要找的东西**。

**问**:那一刀下去,**20 条里红了几条?仍绿几条?仍绿的逐条点名。**

**为什么**:07m 那次的教训就是这个形状 ——
**`/my/stored-value/confirm` 那一刀下去,套件红了,但红的是隔壁那条幂等,「声称成功」那条原样活下来。**
**「有一条红了」不等于「这个口的判据是好的」。**

**这一条对后面 82 个口同样适用**:**造病的产出不是「红了没有」,是「哪几条活下来了」。**
请把这句写进造病台的报告格式:**每一刀固定输出「红 N 条 / 仍绿 M 条 / 仍绿的名单」。**

---

## 三 🔴 立 J-58 第五款 · **新刀先证咬得到,再信它报的 0**

> 「那把新刀第一版是写坏的:正则末尾 `\b` 跟在 `--` 后面永远匹配不上,**全仓扫出「零处」——看起来干净,其实是瞎的**。是我给它加的 `--probe` 自守当场戳穿的。」

**这个字符在这个仓里咬过不止一次了** —— 早先那次是模板串里的 `\b` 变成退格,**整条分支静默滑出扫描面**。两次的长相一模一样:**报一个漂亮的 0。**

你自己把款文说出来了:**「先证刀咬得到,再信它报的 0。」**

### 款文

**每一把新写的扫描器,必须自带一个 `--probe` 自守**:种一个**必然命中**的样本,刀必须报出它。
**probe 报不出来 → 这把刀此刻报的一切 0 全部作废。**

**这是 J-58 一族的第五款**:
①判据零命中不算通过 · ②反向守红了只许重新选锚 · ③锚不许选在本次改动会抹掉的字面量上 · ④造病不红不算守住 · **⑤新刀先证咬得到,再信它报的 0。**

**你今天把这条用在自己身上两次**(这里,和 §一③ 那三个 0)。**款文只是把你做的事写下来。**

---

## 四 记你三功 + 认我一处

1. 🔴 **「把规矩解释清楚的那段话,自己把规矩顶红了。」**
   预检的 `grep` 咬住了造病台里的注释案底和「拒绝开跑」那句解释文案 —— **J-61① 数执行不数提及的又一例**,而且是最讽刺的一种。改成按位置判引号外。
2. **裁 #99 三条路都验**(脏 → 退出码 2 拒跑 / 强开 → 大声说出来 / 干净 → 放行)。**「强开要大声说出来」这一条你自己加的,我没要求。**
3. **A 类 246 条落在 83 个口上** —— 一个口造一次就够,不是 246 次。

**认我一处**:**我估 40–60 个口,实际 83。我估错了,你照实报了。**
我拿 J-63 要求别人,自己那个估计也是拍出来的 —— **差别只在于我当时标了「估」,而不是当成数说出去。**

---

## 五 一个数,我要你顺手记着

本机库上「**有余额 + 没手机号**」`lucky-luxe` 已经是 **8 人**,有积分且没手机号 **25 人**。

**这是测试数据,一个都不算数。** 但它说明这条查询**量得出这批人** —— 而生产库上这批人是真的,**她们兜里有钱,而上线第一天扫码会变成新人。**

---

## 六 顺序

```
① 五处补丁改完 →（问 0b 先跑，撞上 §一⑤ 的停下来条件就停）→ 跑完六条 → 报数
② §二 那个追问：签署口 20 条里红几条 / 仍绿几条 / 仍绿点名 + 报告格式固定下来
③ §三 J-58 五款：给所有现存扫描器补 --probe 自守（先出「有几把刀、几把有 probe」三个数）
④ 其余 82 个口的造病 —— 按涉钱 35 / 顾客能走 5 优先
⑤ 门关档红 8 · J-60 继续降 · 9 条冻结理由
⑥ 段 C 与 D191 第三/第四形态 —— 等五问的数
```

---

## 七 停线

生产只读那几条**全部照旧,一个字不松**,并加 §一⑤ 那条内建停下来条件。

**再写一遍最要紧的三条**:

- **只许 `SELECT`,append 也是写。**
- **`-readonly` 打不开 → 停。** 不去掉它,不改 journal 模式,不创建任何文件。
- **没有 `sqlite3` → 停。** 不往生产上装东西。

---

## 八 回执

1. 第一行:两处全 32 位 md5。
2. 五处补丁**逐条对号**说「改成什么」。
3. **问 0b 的结果**,以及有没有撞上停下来条件。
4. 六条的完整结果 + 连的哪个库 + 跑了几条 + 每条原文 + **零写语句的证据** + **指纹那一条的读数时刻**。
5. §二 追问:红 N / 仍绿 M / 仍绿点名 + 报告格式改好了。
6. §三:现存扫描器**三个数**(几把刀 / 几把有 probe / 几把没有)。
7. 三档 / 状态分布里任何「看起来很干净」的 0,**按 J-58④⑤ 说明是真 0 还是量不到**。
8. 底数闭合 · 未动对照结构化标记 · `batch-footer` 现出的数。
9. **每个手写的数带一句「怎么来的」。**
10. 没做完的写「未做,原因 X」。
