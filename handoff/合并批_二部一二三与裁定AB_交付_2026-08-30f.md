# 合并批交付:台面归因对拍 + D89 + 服务小记图片 + 裁定A/B 改造(2026-08-30f)

> 安全保证:**生产库未动 · 本机库未动**;沙箱=走查景与真传验收(账号按裁4 入档)。
> 未推 main。做完不说可以关帐。顺序照令:二部一 → 二部二 → 二部三 → 裁定A/B。

---

## 二部一 · 台面空档:归因对拍(零动刀,证据上交)

证据:核验截图_2026-08-30/台面空档归因对拍_二部一.md。三问三答的结论:
- (a) 网页绿区起点=`ceil(门店现在/30)*30` —— 她看到点不了的那段=过去部分 + **未来但被半点对齐吃掉的 ≤29 分钟**;
- (b) **仓码两端逐字同刀**(套件钉恰 1 处);「小程序没这个问题」的真相=**她真机跑的是 08-23 上传的体验版 1.2.0(早于 D88)**,旧版还在放行过去时段 —— 不是两端逻辑分叉;
- (c) 「空档 N h」徽章与绿区**同源**(freeTotal 在同一裁剪循环累加),零分叉。
- 同一分钟对拍(门店 14:00,同一份响应喂两端裁剪式):逐项相等 + 网页实拍(上午零绿区、绿区 14:00 起)。
- **按令不自行决定**:三个候选方向(维持半点对齐 / 裁到「现在」不对齐 / 回旧版)已列,拿图给店主裁;
  无论裁哪个,D88 三样不回退(已各自常驻)。她真机要见新行为需重传体验版。

## 二部二 · D89 placeholder 截半

- 根因一条:placeholder 层吃 CSS 行高(继承父容器紧行高),原生文字层不受影响 —— 打字正常、占位截半。
- 规格路线:app.wxss 全局两条(input 行高 normal+min-height 76rpx+border-box;textarea 1.5)——
  零逐框补丁;类规则显式 height 的框优先级天然更高。
- 同类扫尽:运行时表面 95 框 × 21 页 × 38 类位,修前后表=核验截图_2026-08-30/D89修前后表_全端输入框.md。
- 判据常驻(money-inputs ⑤):全局规格锚点 + 行高配对机械扫(47 条规则);
  **挨刀第一刀没红=判据洞**(同类多规则只扫第一条)→ matchAll 修 → 第二刀红点名 → 绿。J 族 +1。

## 二部三 · 服务小记图片(小合同五条)

| 条 | 落法 |
|---|---|
| ① 入口双端 | 小程序小记页「＋添加图片」(拍照/相册,chooseMedia→base64,≤9,草稿可移)+ 网页「我的客人」选图钮(FileReader→base64);后端终闸:第 10 张 400「最多 9 张」、非 data:image 400 |
| ② 归属与可见 | images_json 随小记行(只追加,无改删路由);读口=限权两条(customers/:id/notes + my-customers 详情,写小记技师与老板可见);小程序画像时间线同批带缩略 |
| ③ 默认私密 | 零上墙牵动(写口不碰 gallery_status,断言钉住);galleryStatus 分立未动 |
| ④ 空态零回落 | 无图=图块整个不出现(两端条件渲染);读口空=[] |
| ⑤ 存储复用 | 图库同款 base64 data: 通道;顾客端零暴露(/my 无小记口,行为断言 404) |

验收(接批次九员工链):**沙箱员工 shiyi 真传一张**(API 写1读1)+ **网页员工登录真传一张**
(构造真 File 走 FileReader 全链:草稿缩略 1 → 保存 → 小记卡回显带图卡 ×2)。
套件 test-staff-portal 8→23 项(小记图片六条)。
**过程咬获两案**:①迁移顺序缺陷 —— images_json 的 ALTER 初放早于 service_notes 的 CREATE,
全新库「表未建先 ALTER」直接起不来(老库掩盖);已挪位+注释留案。
②我自己的 heredoc 幻影 —— 一个编辑块忘写 `s=s.replace(...)` 把原文原样写回、且 assert 失败静默,
排查半天疑神疑鬼;教训入册:**编辑块必须回显自证(写后即读打印)**,本批后半全部照做。

## 裁定A · 前置闸=双水位证明

- 实付余额≥该笔实付 **且** 赠送余额≥该笔赠送(水位口径与 refundFacts 同源:paidRefundable/bonusRemaining);
  任一侧不足 → 400「余额已不足以证明这笔未消费,请走退卡」;2 秒窗歧义拒不变;后端终闸。
- 三形常驻:水位不足拒(290<300)/ **放宽生效**(之后动过钱但两侧足 → 201,旧保守闸会拒的形)/
  **侧水位构造形**(总余额 540 富余、赠送侧 40<100 → 拒 —— 冲了就打负)。
- **刀①**(只验总余额不分侧)→ 侧水位案红(总额闸放行了该拒的形)→ 还原绿;
  **刀②**(冲后任一侧为负的形必拒)=侧水位案常驻红卫。

## 裁定B · 红字记当日 + 负腿点名注(动作落点律)

- 维持红字记冲销当日(与账本冲销/金额更正同口径)。
- 抽屉算式:冲销红字从「现金充值」**拆出单列负行**(净额与应有数算式分毫不变),
  行内 note 点名「冲销单 sv_xx(原充值 YYYY-MM-DD)」—— 负数可以出现,不许无解释地出现;
  两端渲染随行注(后端句,rows 结构原有通道)。既有流水自证行保留。
- 判据常驻(负行+note 正则);**刀**(摘 note)→ 红 → 还原绿。

## 回报固定六项
1. **改了什么**:归因对拍报告;app.wxss D89 规格;小记图片全链(local-server 写读+staff-scope+
   两端三处 UI);stored-value-reversal 双水位;account-refund 冲销负行+注;dailyclose/daily-close-rows 注行;
   套件:money-inputs 13、staff-portal 23、mini-account-adjust 39。
2. **在哪一层验**:断言层 + 沙箱真传实拍(员工 API 链 + 网页 File 全链)+ 归因=同分钟同响应对拍。
3. **同类扫尽**:D89=95 框全扫修前后表;小记图=三读口逐个接(notes/my-customers/画像时间线);
   水位=facts 同源 SQL。
4. **用什么数据验**:她点名的病例框;放宽形/侧亏形/歧义形;10 张与非图攻击;体验版版本考古。
5. **重复稳定性**:各套件清库复跑绿;四刀(D89/水位/负注/+既有)红-绿闭环。
6. **残留与不确定**:①台面裁剪方向待店主看图裁(证据已备);②D89/小记图/输入框像素终验=店主真机,
   **且需重传体验版**(1.2.0=08-23 码,含 D88/D89/小记图全部要重传才可见);③heredoc 幻影教训已立
   (编辑块回显自证);④local-server 18,288 → **18,305(+17,报批:小记图写读闸贴在既有路由内;
   排班域搬家批一并抵扣)**;admin.js 8,482(±0)。

### 回归结果(补记)
- **✅ 全部 64 个套件通过,2,447 项断言全绿**(上批 2,428 → +19:money-inputs 12→13、
  staff-portal 8→23、mini-account-adjust 36→39)。staff-portal 全新库 23 项 TAP 全 ok(清库幂等成立)。
  日志:handoff/回归日志_2026-08-30f_64套件.log。
- 端口已还:4128(本机库)/4310(沙箱)双活,/health 双 ok(adminBuild d2880282)。
