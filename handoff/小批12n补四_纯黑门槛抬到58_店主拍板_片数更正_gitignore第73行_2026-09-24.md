# 小批 12n补四 —— 纯黑门槛抬到 58(店主拍板)· 片数更正 · §四 要先改根 .gitignore 第 73 行

对象:修图对话。围栏同前:只动 tools/ai-retouch-eval、tools/segment、产出夹;不改 apps/、不推、不碰生产;git 只 `git commit -- <路径>`,不 `add -A`,不 `add -f`;遇 index.lock 不删。¥0。

## 〇 我复核了什么(机械核,不是读回执)
自己写脚本从 `_input` 原图 + 28 个 annotations/*.json 的「多边形_全图像素」按 `median_after_p85_trim` 重算:
回执表里 02/39/46/74/38/18/28/65/71/23 的 Y、C **全部对上**(到 0.1)。量尺没问题。

## 一 店主拍板(2026-09-24):`纯黑_Y上限` 35 → **58**,18 号走腿 A
- 只改 `tools/ai-retouch-eval/nail-tier.json` 的 `纯黑_Y上限` 这一个值。90 / 25 / `纯黑_C上限` 10 **一个都不动**。
- 为什么是 58 不是 55:28 张里 C<10 的深色,黑甲最高 38 号 54.0,往上第一张非黑是 71 号墨绿 61.5。55 离 38 号只有 1 分;58 两边都留约 4 分。
- 18 号(暗紫猫眼,Y 39.5 / C 3.6)与 46 号亮面黑(39.0 / 3.6)在 (Y,C) 上不可分 —— **任何门槛都分不开,这是尺子的量程,不是门槛问题**。店主口径:「几乎是黑的暗紫」当黑处理、走 A。
  → 在 nail-tier.json 写一条 `已知接受的分歧`:18 号人眼「深·不鲜明」、机器「纯黑」,店主 2026-09-24 接受。人眼标签**不改**。
- `门槛谁定` 字段补一句:纯黑_Y上限 58,店主 2026-09-24 拍板(12n补四 §一)。
- 改完重跑 nail-tone-true.py,贴分布。**我预期**:纯黑 6 张(02 39 46 18 74 38),其余档不变。对不上就停下报,不许再挪门槛去凑。
- 人眼一致数报两个:含 18 / 不含 18(18 记为「已接受分歧」)。

## 二 回执更正:151 片 → **154 片**
28 个 json 的多边形逐个数:154 片,没有一片小于 50 像素被跳过。23 张时 124 + 本批 71(6)+74(6)+39(6)+46(7)+38(5)=30 → 154。
请查 151 是怎么来的,在回执里改掉,**写清差的 3 片是哪来的**。

## 三 §三 离线评估表加两列(下一回合做时一并出)
1. **逐片 Y/C 列**。黑甲整张中位数被装饰片拉高,这正是候选 B(逐片判档)的论据,表里要看得见:
   46 片6/片8(银箔)Y 168/181 · 38 片6(蝴蝶结)Y 119 / C 27 · 74 片5 Y 108 · 39 片5 Y 73。
2. **23 号**(人眼「浅」,机器 Y 68.5 / C 6.8 → 深·不鲜明 → 腿 A′)是现行判法下已有的错判,在表里单列,三种判法各判成什么。
六条验收照旧;门槛以 §一 改后的 nail-tier.json 为准。只报不改。

## 四 §四 标注入 git:**先改根 .gitignore,不是加 tools/segment/.gitignore**
- 根 `.gitignore:73` 是 `tools/segment/annotations/`(整个目录)。git 不会进被忽略的目录,**子目录里的 .gitignore 放不回来**,`git add` 会静默什么都不加。
- 做法:把第 73 行改成
  ```
  tools/segment/annotations/*
  !tools/segment/annotations/[0-9]*.json
  !tools/segment/annotations/_标注集清单.md
  !tools/segment/annotations/_补标挑图理由_*.md
  ```
  `裁片/`、`自核图/` 继续挡着。**禁止 `git add -f`**。
- 复核:`anno.py`、`nail-tier.json`、`同套甲对照表.json` **已经在库里**,不用新加,只提交改动。
- 提交路径:`.gitignore`、`tools/segment/annotations/[0-9]*.json` 28 个、两份 md、`tools/ai-retouch-eval/nail-tier.json`。
- 贴:`git check-ignore -v tools/segment/annotations/02.json`(应无输出)、`git check-ignore -v tools/segment/annotations/裁片`(应命中)、`git diff --cached --stat`(应 32 个文件左右、无图片、无 venv)。不推。

## 五 46 号倾角
「从自核图读两端点再算」这条收下。46 号改后逐片 Y 22/27/30/46/39 与黑甲一致,修正成立。
