# 小批 11u —— 按《平台后台_商家管理重排+资料库_设计图_v1》做 · D215 / D216 / D217 / D218 / D219 合令

**接 11t。合同图 = 夹子里《平台后台_商家管理重排+资料库_设计图_v1_2026-09-23.html》(店主 09-23 拍板五件,见 §〇)。图 = 合同,不许凭感觉排。**

## §〇 店主五答(逐字要点)
1. 三个常用按钮「配置 / 重置老板密码 / 设为永久(样板店换重置演示数据)」**同意**
2. **门店数列保留**(将来会有两家门店的商家)—— 图上「去掉门店数」那句作废,列顺序:租户 · 名称 · 归属 · 套餐·到期 · AI 包 · 配置进度 · 门店 · 本月/累计单 · 老板账号 · 操作
3. 入口手册**可以给商家看**
4. 商家端「联系有迹」电话 **`15698550698`**
5. 顾客端价格显示 A 的画法 **先这样**

## §一 D217 · 商家管理 + 商家配置(按图一、图二)
- `.main` 放宽到视口(≤1600)· 表格列如 §〇.2 · 操作列 3 常用 + 「更多」(隐藏/放回选店页 · 转样板店/真实商户 · 企微客服号 · **停用**在最下并红字)
- 样板店行淡底,标签文案「样板店 · 展示/测试用」;样板店第三个常用按钮 = 「重置演示数据」(走 `demo-reset` 现成门)
- 「配置进度」列:五盏灯 + 后端那一句(D197 已有,只是画到列表里);点灯跳商家配置页对应标签
- AI 包标签可点 → 弹层复用 `setAi()` 四个操作;**不许第二份实现**
- 新建商家表单两行各五格等宽;首期下拉加「永久」(D215);「演示店」勾选框文案改「样板店」
- 商家配置页:顶上五步条(点步跳标签)· 配置卡放宽 900 · 新增标签页「企微客服号」(接现成 `wecom-kfid` 口)与「价目表导入」(接 11o `serviceImportApi`,试跑→报告→执行)
- 判据:列顺序静态断言 · 操作列每行恰 3 按钮 + 1 更多 · 样板店/真店按钮集合不同 · AI 弹层调的是 `setAi` · **L1 截图三张(1920 宽):商家管理 / 商家配置 / 新建商家展开)**,可见性判据带文字长度下限(11t 登记那条,本批一起补)

## §二 D215 · 永久
建店 `initialTerm='forever'` ⇒ `plan_expires_at=NULL`;列表「设为永久」走 PATCH `planExpiresAt=''`;已永久的显示「长期」标签不显按钮。阳性对照:选年付 ⇒ 非 NULL。**`luvia-bj` 店主自己点。**

## §三 D216 · 第二电话
`stores.phone2`(幂等 ALTER);三处表单(建店 / 平台配置 / 商家后台门店设置)各加框;顾客端门店信息页两个都显示、拨打默认主号;**出口普查上限跟着重扫,`phone2` 过占位词表**。

## §四 D218 · 资料库(按图三)
### 平台侧
菜单「资料库」:拖入 PDF/MD ≤20MB · 标题 · 一句说明 · 适用对象(所有商家 / 指定)· 上架 / 下架 / 替换新版(旧版可回退)/ 复制链接 / 被打开次数;**上传口复用现有密钥扫描,命中拒收**;存 COS,库只存元数据 `platform_documents`;签名链接 7 天。
### 商家侧 —— 店主问「出现在哪」,两处,定死:
- **网页商家后台**:左侧栏**最下一项**「帮助与手册」(`admin.html:386` 那个 `<aside>`,排在「AI 图库」之后);页面只读列表 + 点开新页看 PDF;页底「联系有迹:15698550698(工作日 10–19 点)」
- **小程序商家端**:底栏「管理」页 → 「店铺设置」那一组**最后一行**加「帮助与手册」(`manage/index.js:67`,`E.help`),老板与员工都可见;点进只读列表,PDF 用 `wx.openDocument`
### 首批三本(md → PDF,内容一字不改)
《有迹商家使用手册 通用版 v1》·《商家入驻手册 v4》·《入口手册》(**所有商家可见**,店主定;但先把入口手册里「本机 4310/4128」那一段拿掉 —— 那是店主自己的,商家用不着,**拿掉前把两版都贴回执**)
### 判据
上架 → 两端可见;指定对象外看不见;下架 404;链接过期 403;上传含口令样本拒收;L1 截图三张(平台资料库页 / 网页帮助页 / 小程序管理页那一行)

## §五 D219 · 顾客端价格显示 A
- 列表卡:**`¥360`** + 小字 `会员 ¥288`(非会员 / 未登录);会员登录后主价显自己档位价
- 详情页:原价 / 分享价 / 会员价三档并排
- `priceFromLabelZh/En` 那条「最低可用档 … 起」**对非会员停用**;AI 客服话术喂的 `priceLabel` 不动(红线)
- 判据:未登录 GET 列表 ⇒ 主价 = 原价;会员令牌 ⇒ 主价 = 会员价;**阳性对照:塞一个会员价 > 原价的项 ⇒ 红**;截图两张(未登录 / 会员)

## §六 顺序
§一 D217(店主天天开的那页)→ §四 D218 → §五 D219 → §二 D215 → §三 D216 → 11n
**每一段各自提交、各自 L1 截图,不合一个大提交。推 main 照 J-117。**
