# 裁定2 储值冲销 + 批次八(店群陪审团)+ 批次九(员工号证据)· 交付(2026-08-30d)

> 安全保证:**生产库未动 · 本机库未动(镜像拷贝对本机库=SQLite URI mode=ro 只读挂载,机械保证)**。
> 沙箱库动前快照:sandbox-data/lucky-luxe.pre-mirror-2026-08-30T17-41-27.sqlite。
> 未推 main。做完不说可以关帐。回执顺序照令:裁定2 → 八 → 九。

---

## 一 · 裁定2:储值行冲销(合同五条逐条落)

**新开口(准开)**:`POST /admin/stored-value/txns/:id/reverse`(refund-routes 账调域;
/admin/stored-value 前缀天然在财务钥匙闸内;requireRefundRight=老板+财务门,与退卡同权)。
逻辑=新模块 `stored-value-reversal.mjs`;新列 `reversal_of`(ALTER try/catch,纪律8,schema-consistency 自动守)。

| 合同条 | 落法 | 判据 |
|---|---|---|
| ① 仅冲错记充值,退钱走退卡 | 只收 type=recharge;对赠送行点 → 400「请对充值行点冲销」;refund 行 → 400 走退卡句 | ok29 |
| ② 前置闸(后端终闸) | **保守可证版**:该笔之后这位顾客零 consume/refund 流水(池式记账无法按笔归属 —— 之后动过钱就无法证明这笔分毫未动,一律拒);句=合同原文「已产生消费,请走退卡。」 | ok35 + 刀B 红 |
| ③ 一刀双行、同一事务、损益不动 | BEGIN IMMEDIATE 内写两条红字反向(冲充值带**原渠道**负数 + 冲赠送 marketing);⚠️ **现实勘误**:充值本就不写账本收款行 —— 抽屉「现金应有数」一直直接累加储值行(rechargeCash 口径),故「账本收款行反向」落地为**抽屉同一口径吃反向行**(当日现金−,损益恒 0,合同意图分毫不差;机制与合同字面的差异在此言明) | ok30/32 + 刀A 红 |
| ④ 入口=账调冲销 tab,储值行与账本行同列一表,两端同批 | 两端 loadTxns 双读口合流(finance + stored txns),行尾同一「冲销」钮按 kind 分口;/admin/stored-value/txns 读口扩形(userId/bonus/reversal 行/reversed 标 —— 路由集不变) | ok28/33 |
| ⑤ 联动判据 + 变异刀 | 四参考数(实付/赠送/余额)/抽屉/流水标记全联动断言(ok31-33);**刀A=抽屉脱钩→红**(合同刀原文的机制版)、刀B=前置闸拆→红 | 全红后回绿,36 项 ×3 复跑稳定 |

**刀A 第一刀连带咬出真缺陷**:赠送行原按 created_at 逐字相等找 —— 同事务两行差 1ms 就**静默丢赠送**
(静默失败器族)。加固为「2 秒窗内最近一条 + 窗内不得夹另一笔充值(歧义即 400 请人工)」。

## 二 · 批次八:店群陪审团

**沙箱店群(店主走查面)**:
- **两真店镜像**:sandbox-mirror-jury.mjs 整租户置换拷贝(源=本机库 **URI mode=ro 只读挂载**;
  沙箱先快照;账本 append-only 触发器拷贝窗内摘下、逐字装回 19=19;admin_sessions 不迁)。
  **镜像核对逐数=源**:lucky-luxe users74/bookings166/settlements119/fin142/sv46;jics-nail 74/198/148/50/68。
  身份不标错(id/名原样);镜像老板密码走平台重置口即时分叉(真店口令零进入沙箱使用面),
  沙箱号按裁4 记 本地自查账号.txt。
- **22 家编造矩阵店**(共享模块 store-matrix.mjs,一份矩阵两处用):3 币种×满/素配 + 未设置×2(含 D84 态A)
  + 七天全关(老库形态)+ 今日特休 + 零客 + 40 客大店 + 只次卡 + 只会员 + 8 组合位。
- **逐店通过矩阵**:核验截图_2026-08-30/店群陪审团_逐店矩阵.md —— 沙箱运行时现取 **45 家,45/45 全绿**
  (镜像 2 + 矩阵 22 + 既有各态店 21;每店 I1 旗标↔库相符 / I2 台面三态+storeNow / I4 日结损益恒0 / I5 隔离)。

**CI 常驻套件 test-store-jury**(入册,排全清单**最后**=看得到前序全部套件留下的各态店):
- 矩阵 22 家运行时自建 → **店铺集合运行时从库取**(CI 现测 112→215 家,零硬编码清单);
- 不变量组 I1-I6 逐店迭代,**一家红整批红**(check 抛错即带店名);矩阵店特征逐家现测(未设置=墙旗标/特休=isClosed+注/币种句随店);
- **欠账补课:强制页链五连**(未设置店夹具 ×5:旗标→保存→立即生效,API 层;网页 UI 单轮实拍在强制设置批已做)。
- **挨刀第一刀没红 → 咬出抽样盲**:I6 每店随手抽第一位顾客,带冲销行的那位永远抽不到 ——
  改「优先抽最可能露馅的」(reversal 行数倒序)后刀红点名(maa 店 55000≠0)→ 回绿。J 族 +1。

## 三 · 批次九:员工号证据

核验截图_2026-08-30/员工号证据_批次九.txt —— 沙箱走查店员工号 shiyi(首登改密真走):
- 正向五步:登录 role=staff → 我的客人(只读句后端出)→ 排单数据 → **自排 201** → 开单预览 200 → 小记读口;
- 反向八口全 403(含**本批新开的储值冲销口**、替他人排单「员工只能给自己排单」);
- 权限差异表(店主 vs 员工同店同接口现测)+ 小程序入口不可见(guardOwner 文件层 + 接口终闸双闸,既有断言引注)。
- 全程沙箱,生产墙内零接触。

## 回报固定六项
1. **改了什么**:stored-value-reversal.mjs(新)/store-matrix.mjs(新)/test-store-jury.mjs(新)/
   sandbox-mirror-jury.mjs(新,手动脚本)/refund-routes+stored-value+account-refund+local-server(冲销域)/
   两端账调冲销 tab 合流/test-mini-account-adjust 25→36。
2. **在哪一层验**:断言层(全量回归)+ 沙箱现测(45/45 逐店矩阵、员工号八口 403)+ 库层(镜像逐数核对)。
3. **同类扫尽**:冲销读写=facts/抽屉/流水三读口逐个接反向行;矩阵=维度组合白名单式生成;
   店铺集合=运行时全取(前序套件残留店全部入审)。
4. **用什么数据验**:错记充值+赠送(整笔冲)/动过一分钱的(拒)/赠送行直点(拒)/未登录(拒);
   陪审=112~215 家各态店(含七天全关老库形态、未设置有单 D84 态A)。
5. **重复稳定性**:裁定2 套件 ×3 复跑全绿;强制页链五连;两刀+一陪审刀红-绿闭环。
6. **残留与不确定**:①前置闸为**保守可证版**(之后有任何消费即拒,即使消费理论上烧的是别笔)——
   池式记账的归属极限,合同没写到按笔归属的算法,按「回来问」挂出:如需放宽(按余额水位证明),请裁;
   ②冲销红字行 occurredOn=冲销当日(与账本冲销既有口径一致)—— 跨日冲销时当日抽屉出现负腿,
   与「错记那天就多算了」天然对不平,已在流水行自证(冲销单注明原行),如需回记原日请裁;
   ③陪审 I3 币种句只在矩阵店验到 symbol 级(<CODE> 前缀为前端模板位);
   ④local-server 18,272 → **18,288(+16,报批:裁2 迁移+装配+读口扩形都贴在既有结构上;
   排班域搬家批按已批方案一并抵扣)**;admin.js 8,482(±0)。

### 回归结果(补记)
- **✅ 全部 64 个套件通过,2,428 项断言全绿**(上批 63/2,412 → +1 套件 +16 断言;
  陪审团在 CI 全量位跑出「矩阵 22 家 + 运行时全集 86 家」逐店全过)。
  日志:handoff/回归日志_2026-08-30d_64套件.log。
- 端口已还:4128(本机库)/4310(沙箱,含镜像+店群)双活。
