# 夜班令 4 — 八小时接力:Code 段与段之间**不停**;停了由 Cowork 每小时醒来代审、写下一段、用命令行重新拉起 · 店主早上只看总结

> 2026-09-07 夜 · 店主拍板「夜里代我审 + 命令行拉起 Code」· 两处各一份 · 合同:主页重画图 v3.1 + 小批05q + 05p 未完段

## 〇、今晚的段(按序,做完一段立刻下一段,**不等任何人**)

| 段 | 内容 | 预算 |
|---|---|---|
| 5 | 主页重画:口径两问现测 → `dashboard-pulse.mjs` 三接口 + 图 §七 判据(三店)| 90 分 |
| 6 | 主页重画:网页后台首页 `dashboard-home.js` 替代 Dashboard + 三态 + 休息日 + 截图 DOM 证据(真登录,账号从本地文件读)| 60 分 |
| 7 | 05q §二 D150–D153 + §四 六通 v4 | 90 分 |
| 8 | 05q §三 D149 平台密码登录(两库建 `platform-admin`,一次性密码进本地文件) | 60 分 |
| 9 | 主页重画:小程序老板视角(§一)| 90 分 |
| 10 | 主页重画:员工打卡门(§二)| 60 分 |
| 11 | 主页重画:全屏态(§五)| 45 分 |

段 9–11 要改 `miniprogram/`——今晚**允许**(店主已授权无人确认模式),但小程序三把刀必须真跑(自起会话),改前后各一次。

## 一、接力规矩(Code 侧)

1. 每段结束:提交 + 回执(可短)+ 在 `handoff/night-runs/进度_2026-09-08.md` **追加一行**「HH:MM 段 N 完成 提交号 / 全量 N 分 / 断言 ±」;然后**直接开下一段**,不停、不问;
2. 遇到要裁的事:写进 `handoff/night-runs/待裁_2026-09-08.md`(一事一条:问题 / 你倾向 / 不裁会怎样),**跳到下一段继续**,不等;Cowork 醒来裁完写进同一文件的「裁」列,你下一次读到就照做;
3. 停线(碰到就停这一段、写原因、跳下一段;全部停线同夜班令 3):写 4128 既有行停;四红线修法不明停;沙箱模型今晚 >600 万 token 停模型跑;回归红 30 分钟定位不到停;不推 main;不碰生产;不看密钥文件;不许 sleep 轮询;
4. 单段超预算一倍 → 停那段写原因跳下一段;
5. 全部段完成或全部停住 → 写 `夜班总结_2026-09-09.md`(六行)→ 退出;
6. 进程如果因任何原因退出而段没做完,Cowork 会用命令行把你重新拉起来,提示词只有一句「读 `handoff/night-runs/进度_*.md` 与 `待裁_*.md`,按夜班令 4 从没做完的那段继续」——所以**进度文件要写到能让下一个你无缝接上**。

## 二、接力规矩(Cowork 侧,店主知情)

- 店主睡前:在终端跑一次 `claude` 完成登录(命令行现在提示 OAuth 过期,登不上就拉不起来),退出;把当前交互式 Code 的段做完提交后**关掉**(同一仓里只许一个 Code 在跑);
- Cowork 每 60 分钟醒一次:看 `进度_*.md` / `git log` / `待裁_*.md`;有待裁就裁;Code 进程不在且段没完 → `claude -p` 拉起(无人确认模式,只在仓目录内);到 08:00 CST 不再拉起,写一页 `夜班Cowork核记_2026-09-09.md`;
- Cowork 夜里只做「核 + 裁 + 拉起」,不改代码;红线四条任一被碰 → 不拉起,留给店主早上。

## 三、早上店主看的

`夜班总结_2026-09-09.md`(六行)· `夜班Cowork核记` · 六通 v4(Cowork 发到聊天窗)· 网页新首页怎么打开 · 平台密码登录怎么用(账号在本地文件哪一节)。
