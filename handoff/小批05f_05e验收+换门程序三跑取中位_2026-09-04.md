# 小批05f — 05e 验收(`0e001a3`/`501c6a0`:收)· 换门程序:三跑取中位,达标即换 · 换门后给店主 12 场景走查

> 2026-09-04 · Cowork 亲核于店主 Mac · 两处各一份

## 〇、亲核
- 改标 134/26/40 与勾定一致;3a/3b 落 `ai-gate.mjs`(187/200),`hasAppointmentInquiryIntent` 已搬入门模块并收窄(book≠本店业务不进采集);默认门仍 `keyword`(143);97 套;`local-server.mjs` 18,029;沙箱真模型自动还回(`aiUsage.calls=1`);未推 main 103;工作区仅店主自己的 `luvia-card/`(非本批产物,不入库不删)。
- 四数(改标后模型门):93.1% / 静默 0 / 实质作答 2.5%(1 句,修在跑后)/ 安全 0000。隔离行 93.8%(新标签+旧行为)有用,收。
- 「不用『应该会变成 0』充当数据」——对。J-14(客气话翻转动作判定)/ J-15(死代码假守卫)收;「未动」回执又贴转述——下批模板先贴原文。

## 一、裁:换门程序
1. **三跑取中位**:同一构建(含 book 修法),模型门跑 **3 轮 × (200 句 + 80 条边角)**;两项比率各取中位数,安全四线三轮都必须 0000。关键词门基线不重跑(未改)。费用估 3 轮 ≈ 210 万 token,店主 03t 已点头本批花费;逐轮四数与 token 都报,不只报中位。
2. **达标即换**:中位 范围内 ≥90% 且 实质作答 ≤2% 且 安全 0000 → 默认门切 `model`;`AI_GATE=keyword` 保留作回滚开关;默认模式下的套件断言**明改**为新口径(3a 不静默、3b 有回复转人工、D133 反面),旧口径断言不留。不达标 → 如实报四数,不动标签、不改门槛,回来我裁下一步。
3. **跑间噪声**:3% 记进口径;以后凡与门槛差距 <5 个点的结论都要三跑取中位,不许单跑下结论(入常驻规矩)。

## 二、换门后立即做:店主要看的走查
用模型门重跑 04d §三 那 12 场景(两店各 6,含 A/B/C 三组),按同一格式出第二份走查文档,与 09-03 基线(4/12/21)并排;每轮记 `tier/source/provider`。这份是给店主读的,不是给刀读的。

## 三、然后 → ② 事实闸(图 §三,六个槽 + 出口校验;D136 并排表为锚)。
