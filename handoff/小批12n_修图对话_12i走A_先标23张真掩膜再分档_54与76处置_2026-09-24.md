# 小批 12n(修图对话)—— 12i 走 A;先标 12i 候选 + 角例共 23 张真甲面掩膜,用真掩膜重算分档再跑;54/76 处置;Safari 店主点

## §零 收
- 12i 按判据停、不调门槛、掩膜画出来看(14 红甲整手当皮肤、53 蓝粉最艳没标出、49/44 捡到衣服背景)—— 对。**「判据在缺陷存在时照样绿」这句要写进 12i 的记录**:「甲面区取不到 ⇒ 默认 A」的兜底从没资格触发,因为掩膜不是取不到,是取错。这是 J-112 的又一形状:兜底条件永远为假的兜底 = 没有兜底。
- 88 张像素去重(41 张 MAD 0.000 / 44 张 ≥ 25.8)—— 收,这才是「那 10 个带 2 的」的终审证据。
- 50 号再裁 340 px 五片不少;自动人脸检测器跑不成**不记 0 张**、显式判 empty、44 张顶部拼图逐格看 —— 记功。
- 围栏①判据换成「我写过的文件清单」—— 对;Cowork/Codex 同时改 apps/ 是上线对话在做 D217,两边隔离成立。
- `confirm` 拦 → 页内黄条 —— 对。

## §一 12i 裁:**A**
1. 12j 标注顺序改为:**先标 12i §三 的 15 张候选 + 角例 8 张(共 23 张,以 88 张新编号为准)**,100% 裁片逐片手描 + 自核图(12j §一-2 不变)。
2. 用这 23 张的真甲面掩膜重算 甲面 Y / C,出分档表;**门槛 90 / 25 是起手值**,分布给店主看,由她定;三档各 ≥ 5 张才开跑 15 张(12i §五 不变)。若真掩膜下「深·鲜明」仍 < 5 张 → 从 88 张里按人眼挑红/黑/荧光补标到 ≥ 5(标注是 ¥0,补标不是调门槛)。
3. 15 张两版 + 回贴全(12i §三)照旧,¥9.60,上限 ¥12。
4. B 不做;C 不做(店主的眼睛要看的是「三档规律成不成立」,不分档跑等于没答她的问题)。

## §二 54 / 76 处置
- **54 号**(无脸露脖锁骨,指甲从 y=0 起):**入集,不再裁**;规矩是「不许出现顾客的脸」,脖锁骨不是脸。对比包里照常;但**送模型那一版加一条**:模型出图若在颈部生成任何人脸特征 → 该张作废(判据:人眼看 + 记录)。
- **76 号**(「Lucky nail」水印):**入标注集、不入模型探针**(水印会被模型糊掉或改写,归因污染);清单标「仅标注」。

## §三 Safari
店主平时用 Chrome。12k 的 Safari 判据改记「店主用 Chrome,Safari 不验」,不再要她点。

## §四 围栏照旧
本对话只动 `tools/ai-retouch-eval/`、`tools/segment/`、产出夹;不推、不碰生产;回执归一本台账。
