# 小批 10e · 🔴 那一单要先让店主**认得出来** · 立 J-106 · 第二步推之前必须实看
2026-09-21 · Cowork 出令

**上游令点名(J-81):** `小批10d…` md5 `6c8baa72529a1698d380c262c7caae3d` —— 回执收到,六件全做完,本批未推,生产一个字没写。

---

## 〇 🔴 你问「`LL-I28970PU` 是谁」—— 这个问题店主答不了,要改问法

**你给的是一个机器号。店主脑子里没有机器号,她只记得「哪天、谁、做了什么」。**
**问一个人答不了的问题,等于没问(J-94 同族:问之前先确认这东西是不是她能回答的形状)。**

**改成这样问 —— 生产只读,把这几样取出来:**
1. **两单各自的:日期 · 项目名 · 金额 · 状态 · 哪家店**
2. 顾客 `created_at` 与**注册渠道**(小程序?网页?哪个入口)
3. `jics-nail` 那一单的**技师/服务人**字段(有就给)
4. 顾客 `users.tenant_id` 到底是 `lucky-luxe` 还是别的 —— **一行写清「人在哪家店、单在哪家店」**

**取到之后我来问店主:「X 月 X 日,小婕店里有一单 XX 项目 XX 元,是不是你或小婕自己试的?」——这句她能答。**

🟡 **我这边有一条既有事实可以先摆上**(量出来的,不是猜的):**`jics-nail` 生产上顾客数一直是 0。**
⇒ **那一单几乎不可能是小婕的真实顾客。** 最可能是你们两个人自己试用时下的 —— **但「最可能」是推出来的,所以按上面取数,让店主自己认。**

🔴 **在她认出来之前:那一行一个字都不许动。** 它是业务数据,不是演示数据(`demo_seed=null`),处置口径跟那 51 行**不是一回事**。

**并且把它单独挂出来:D207-b。** 51 行是演示身份脏数据(无牙、排队清);**这 1 行是「小婕店里唯一的订单,她那边看不见这位顾客」——是功能后果,不是卫生问题。**

---

## 一 🟢 §〇 那个闸:收,判得干净

静态三个调用点都到不了 `null` 那一支 · `currentTenantId()` 与 `validTenantId()` 都回落 · 第三个是零调用死注入 · **而且造病台上阳性对照是活的**(真属于 lucky-luxe 的人认得到 `vG`,那 51 行的形状认不到、另建了新顾客)。

**「尺子咬得动」这一句每次都有 —— 这是 J-91 落地成习惯的样子。收。**

---

## 二 立 J-106(你第四次栽在同一个毛病上,而这次的做法是对的)

前三次「数提及不数执行」你就地补一条;**第四次你把 `<!-- -->` 补进 `codeOnly()` 本身,一处治全部。**

### 立 J-106
> **同一个毛病第三次出现,就不许再打补丁 —— 必须去改那个产生它的东西。**
> 前两次补丁是修 bug;**第三次还在补丁,修的就不是 bug 了,是在给这个毛病铺路。**

同族 J-78(批量替换前先量全)、公约④(先搜复用)。**这一条从今天起对你我都生效(J-79)。**

---

## 三 第二步:**判据我收,但推之前必须实看** —— 这一条不让步

你自评两条没做到,**自评得对,我照你的自评裁:**

🔴 **顾客端这一屏的全部意义就是「顾客看得到」。判据 97 条全绿,证明的是「接口给得对」;它证明不了「顾客眼睛看得见」。**

**推之前要补齐:**
1. **网页顾客端**:顾客登录态下,档案页那一栏 —— **实看 + 像素**
2. **小程序顾客端**:「我的」下面那一栏 —— **实看 + 像素**
3. **两条负例也要看**:作废的那份**不出现** · `customer_visible=0` 的那份**不出现**(这两条判据验过,但店主要看到的是「它真的不在屏幕上」)
4. **L5 连续 5 次进出** —— 补上

**像素拿不到(像屏1 那样四次黑帧)⇒ 照屏1 那个办法:写死「已逐字核对 · 未取得像素图」,不许含糊。** 但**实看这一步不许省** —— 黑帧是取像素的问题,不是看不了的问题。

---

## 四 其余四件

1. **J-105 三处现测全落成 `平台主钥匙`** —— 🟢 收。**而且你把「记一笔」也一起改了(令里只点了两处)—— 判得对:那也是钱。**
2. **乙档涉钱 40 处** —— 分批修,**先修会落进对账 / 结算 / 储值的那些**,一批不超过 10 处,每批两面验。不涉钱那 26 处只要「明写常量,不许 `|| ''`」,排后。
3. ⬜ **`ai_response_feedback` / `ai_learning_examples` 这把尺子看不见** —— 🟢 **如实说「验不到」而不是「验过没问题」,这正是我要的(J-65③)。** 排推后:要么给它们一条能对的判据(写入时的会话租户 vs 行上的 `tenant_id`),要么在尺子里明写「这两张表没有租户父对象,不在量程内」——**两条路都行,不许沉默。**
4. **门关档**:`signed-docs` 该进没进 → **加进去而不是开豁免** —— 🟢 记一功。「判据自己写着:做对事情让『该跑』变多」,用得准。

---

## 五 顺序(写死)

**§〇 取数(只读,十分钟)→ 报我 → 我问店主认人 → 认完再定那一行怎么处置**
**并行:§三 实看四项 → 补齐 → 推 → CI 绿 → 轻量六项 → 报我**

🔴 **D207 那 51 行的清理,仍然等我批 `SELECT` 原文之后才动 —— 本令没批。**

---

## 六 交回执要点(J-81)

首行点名本令文件名 + md5。逐条回:
1. §〇 四样取数(**给店主看得懂的形状,不要只给 id**)
2. §三 实看四项 + 两条负例的屏幕结果
3. 推的结果:commit · CI run id · 轻量六项
4. 乙档第一批(≤10 处)修完的现测
5. 三行口径

**停线一个字没松。** 生产只读 · 那 1 行与那 51 行**一个字都不许写** · 顾客端不许加建号口。
