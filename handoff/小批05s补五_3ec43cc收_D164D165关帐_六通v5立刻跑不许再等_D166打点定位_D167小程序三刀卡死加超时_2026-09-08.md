# 小批05s 补五 — `3ec43cc` **收,D164 / D165 关帐**(北京店三轮亲测:券名句在、接回的两次都是「本甲还是延长」)· 🔴 **六通 v5 立刻跑,不许再以「缺件」推迟**——这是第四次推迟,店主在等打分 · D166 按待裁 #11 打点定位,**排在 v5 之后** · D167 小程序三刀卡死加超时

> 2026-09-08 夜 · Cowork 亲测(4310 小程序口,北京店,同一访客三轮):「手绘定制多少钱」→「现在有「北京店开业礼 · 满 300 减 50」立减 ¥50 可以用;具体价格要技师看过后报给您。 请问这款是做本甲还是需要延长?」✅;「明天下午三点有位吗」→ 真时段 + 两替代 + 接回「本甲还是延长」✅;「有优惠吗」→ 券 + 接回同一句 ✅ · 两处各一份

## 一、收

- D164:待答句从流水里取(最近一条以采集问句收尾的 AI 句),取不到一个字不加——收,关帐。
- D165:券名句放采集问句前,没券也说「具体价格要技师看过后报」;出口卫生对采集模板放行(「守过头和不守一样坏」)——收,关帐。
- D166 撤干净、`namedService` 0 处、待裁 #11 写了定位办法——收其不留半成品。

## 二、🔴 六通 v5 · 现在就跑,这一批第一件事,不许再等

「不跑缺件样本」是我在 D150 那次立的——**那次缺的是一整条主行为**;这次缺的 D166 是「问时长取哪个项目」一句话,小程序三套刀与对话内容无关。用同一个理由推迟四次,店主拿不到任何可以打分的东西,这比样本缺一句更坏。裁:

1. **立刻**在窗 = 4 的活服务上跑六通 v5(同脚本;通五三句间隔 1 秒、欢迎语不算回答;通六 `fixed` 问价 + 「最便宜的」+ 采集中插「明天下午三点有位吗」),落仓 `handoff/ai-eval-results/05q_像人六通_原文_v5_2026-09-08.md`,新会话,`ranOn` + 星期几 + 夹具店休息日;
2. 文件抬头**如实写一行**「D166(问时长取会话里点名项目)未修,通 X 若出现全店范围时长以此为准读」——缺什么写什么,不是不交;
3. 跑完先交 v5,再做 §三 §四。以后「样本缺件」只许在**主行为缺失**时推迟,且推迟要报 Cowork 裁,不许自己定。

## 三、D166 · 按待裁 #11 打点(v5 之后,≤ 40 分)

`answerForTurn` 三个调用点各加一次性 `console.log('[d166-probe]', site)`(跑完删干净,静态判据:仓里 0 处 `d166-probe`),重放我那四轮,看轮 4「做一次要多久」从哪个点出;找到后把「会话里已点名的项目」接到那一点(采集状态 `serviceId` 优先,其次最近一条含项目名的顾客句),`base_duration_min` 出分钟数并接回 `pendingQuestion`;判据 + 造病(取全店范围 → 红)。

## 四、D167 · 小程序三刀卡死 → 加超时,不许再拖垮整轮

现状:DevTools 自动化会话卡死,看门狗两次 300 秒自杀——看门狗尽了职,但整轮回归被三套与本批无关的刀拖掉。裁:① `mp-*` 三套各自带**单套超时**(60 秒),超时算「本轮未跑」并在回执里点名,**不许让整轮自杀**;② automator 连接前先探 DevTools 自动化端口是否活着,死的就重启开发者工具自动化会话一次再试;③ 判据:人为把端口关掉 → 三套 60 秒内报「未跑」、整轮其余 105 套照跑;造病:去掉超时 → 看门狗 300 秒自杀 → 红。

## 五、顺序与交齐

**v5(§二)→ D166(§三)→ D167(§四)→ 段 9/10 补件(员工测试号真拍、金额出口、休息日截图)。** 交齐:v5 路径 + 抬头缺件行 · D166 出口点名 + 判据 + `d166-probe` 0 处 · D167 三判据 + 造病 · 提交号 · 回归/断言增量 · 整跑 · 棘轮两数 · 未推 main。
