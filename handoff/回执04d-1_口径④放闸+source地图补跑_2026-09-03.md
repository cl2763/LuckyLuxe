# 回执 04d-1 — 口径④ 放 fail-closed · source 地图补一跑(先交的那一小批)

> 生产库未动 · 本机库**表/行/列/索引零差异** · 沙箱库只有调度器写的两表(见 §五)
> 未推 main。回归 **89/89**,断言 2,894 → 2,897(**+3**,一分不差)。

---

## 一、口径④:放闸的落法

`resolveTenant` 的 **27 个调用点全在顾客侧公开路由**上(`/stores` `/services` `/availability` `/my/*`
`/auth/email/*` `/ai/*` `/store/deposit-policy` `/sandbox/demo-roster`),后台走 `adminSession`、
平台走 `isPlatform`、企微 webhook 已按口径③ 由 `open_kfid` 定 —— 都不经这里。逐个核过才动的手。

**落法**:缺失或无效 → `400 TENANT_REQUIRED`,不回落。
`validTenantId` 那一层**仍可回落**(后台/平台也在用)—— 不许回落的是 `resolveTenant` 这一层,两层分开。

顺手按公约②把这一域搬出去 → `apps/api/tenant-gate.mjs`(48 行)。

### 夹具同批补头 13 个套件

customer-profile / deposit-config / identity-links / schedule-v2 / stored-value / card-refund /
business-hours / hours-gate / special-dates / noshow-aftersales / today-board / entitlements / tenant-kb。
**补的是「请求带不带 `x-tenant-id`」,判据一个字没放宽。**

> 🔴 中途栽了一次值得记:我第一版把默认头插在 `...extraHeaders` **之后** ——
> 结果**默认值盖住了每次调用自己传的租户**,`test-deposit-config` 当场
> `STORE_TENANT_MISMATCH`。默认值必须排在 headers 对象**最前面**,让后面任何一处都能覆盖它。
> 归族「一个字段只回答一个问题」:默认值的位置决定了它是「兜底」还是「霸占」。

### 判据两层(能被同一把刀分开)

| 层 | 判据 | 造病 |
|---|---|---|
| 套件级 | `test-conversation-tenant` ⑤ 没带头 → 400 TENANT_REQUIRED · ⑤b 无效租户 → 400 · ⑤c **反向守**:带对了 200(一把「谁来都 400」的闸跟关门一样) | **造病 M**:去掉 `test-tenant-kb` 的头 → 该套件当场红(`failed: AI deposit answer uses updated fact`),还原后回绿 |
| 回归级 | `run-all-tests.sh` 收尾:每个还活着的 CI 服务进程 `/health.tenantFallback` 必须 **0/0** —— **读 health 不读日志**(日志会被下一次跑覆盖) | 同上一把刀:夹具裸奔时计数非 0 |

两层管的不是一件事:套件级验「拒不拒」,回归级验「整轮里有没有夹具还在裸奔」。

实测收尾输出:
```
   [口径④] 端口 4128 tenantFallback 合计 0
   [口径④] 端口 4131 tenantFallback 合计 0
```
> ⚠️ 一处说准:收尾只查**还活着**的端口,4129(auto-return 专用)在收尾前已被脚本停掉,
> 所以它是用日志补的一句 —— `grep -c tenant-fallback /tmp/ll-ci-4129.log = 0`。三个进程都干净。

### 两端真机(HTTP 码原样)

| 端 | 登录 | 可约时段 / 门店 | AI 客服 | 不带门店标识 |
|---|---|---|---|---|
| **网页顾客端**(浏览器实开 `4310/web/customer.html?store=lucky-luxe`) | **200** | **200** | **200** | **400 · 400 · 400** |
| **小程序**(automator 实连 9420,页面运行时里发的请求) | **200** | **200** | **200** | **400 · 400 · 400** |

顺带记一条产品事实:**两个客户端本来就不会裸奔** ——
小程序 `utils/api.js:188` 拿不到租户直接 `reject NO_TENANT`;网页 `customer.js` 空租户时
「不发任何数据请求,直接弹选择门店」。所以这道闸对真实用户是**纯增益**,不改变他们的路径。

---

## 二、source 地图 · 补一跑(店主 04d §二)

按您指出的两个洞重跑:**每条探针独立外部用户 id + 干净会话**,再各跑一遍**带状态版**(先把会话推到采集完成),表加**会话状态**列。

| # | 场景 | 干净会话:status / source / 会话状态 | 无 key | 带状态版(前置 needs_human) | 判定(干净会话) |
|---|---|---|---|---|---|
| A | 报价意向第一句 | 201 `quote_intake_template` [ai_replied] | 201 | 201 无回复 [needs_human] | 规则层抢答 |
| B | 报价采集填完 | 201 `quote_intake_state` [needs_human] | 201 | 201 无回复 | 规则层抢答 |
| C | 采集完成后再说一句 | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| **D** | **营业时间问答** | 201 **(无 source = 模型直出)** [ai_replied] | **500** | 201 无回复 | **到模型层** |
| E | 售后(掉甲) | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| F | 情绪/投诉 | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| **G** | **定金规则** | 201 **(无 source = 模型直出)** [ai_replied] | **500** | 201 无回复 | **到模型层** |
| H | 直接预约 | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| I | 英文报价 | 201 `quote_intake_template` [ai_replied] | 201 | 201 无回复 | 规则层抢答 |
| J | 知识库问题(停车) | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| K | 待报价期间追问 | 201 无回复 [needs_human] | 201 | 201 无回复 | 不产生回复 |
| L | 美睫首次 | 201 `quote_intake_template` [ai_replied] | 201 | 201 无回复 | 规则层抢答 |
| — | 技师报价润色 | — | **500** | — | **到模型层**(`staff_quote_polished`) |

### 补跑答了您那个问题,答案是「两个都成立」

- 您怀疑的**会话状态污染确实存在**:「带状态」那一列 **12 条全是「不产生回复」** ——
  只要会话进了 `needs_human`,后面说什么都被「等人工中一律不答」吞掉;
- 但**干净会话下那 6 条仍然是「不产生回复」** —— 所以 04c 那张表的结论没错,
  只是当时**分不清**;现在两列并排,分得清了。

### 口径(写进地图,判据锚它)

**`reply.source` 为空 = 模型直出**(`createCustomerServiceReply` 的返回不带 source);
有 source = 规则层;`reply` 为 null = 不产生回复。
12 场景每轮记 `source`/`provider`/预期 source 三字段,就是锚这条口径。

**未能触发的 source 10 种**(照您的要求写「未能触发」,不写「不到模型」):
`after_sales_route` / `quote_intake_manual_review` / `quote_special_manual_review` / `quote_draft_guard` /
`quote_draft_slot_guard` / `quote_draft` / `quote_waiting_state` / `tenant_kb` / `first_lash_visit_notice` /
`entitlement_gate` —— 12 场景铺景会走到,到时补进地图。

---

## 三、回归 · 棘轮 · 库

**89/89**,断言 **2,894 → 2,897(+3)** = `test-conversation-tenant` 的 ⑤/⑤b/⑤c,一分不差。「本轮未跑」0。

| 文件 | 04c | 04d | |
|---|---|---|---|
| `apps/api/local-server.mjs` | 18,167 | **18,144** | ↓23(租户闸搬出) |
| `apps/web/admin.js` | 8,550 | **8,550** | 持平 |

| 库 | 差异 |
|---|---|
| **生产库** | 未动(本批没碰) |
| **本机库** | ✅ **表/行/列/索引零差异** |
| **沙箱库** | `notification_logs` +1 · `reminder_tasks` +2 —— 回归拉起 4310 时调度器写的(产品正常路径);两端走查造的两个顾客档案已清 |

---

## 四、下一步

12 场景按 04d §三 矩阵开跑,不再重排。走查文档头部放这张补跑版地图 + 矩阵。

<!-- 回归自证 批次=04d1 套件=89 断言=2902 日志=handoff/regression-logs/回归日志_2026-09-03_04d1_89套件.log -->
