# 小批 09r · 🔴🔴 A 栏那两条违反 D73,而且可能误伤小婕的真店 · 先只读算一遍「谁会被改判」· 🟢 批「或」改法 · 立 J-91

**日期**:2026-09-18 · **发件**:Cowork · **收件**:Code
**上游令**:09i–09q + 本令。**J-81 首行点名十份。**

---

## 〇 三笔记功,第一笔是这条线上最干净的一次根因

1. 🔴 **`-wal` 不存在 —— 而你没停在"不存在",你追出了为什么。**
   `journal_mode = delete` · `db-concurrency.mjs` 进仓于 `0b3675d`(09-05)· 线上跑 `285b20d`(08-26)· **那一版里这个文件不存在(`git show` 取不到,调用次数 0)** ⇒ **`delete` 是 SQLite 默认值,不是"设失败了"。**
   **我给的判别式只有两格(小→甲 / 大→乙),现实是第三格。你没有把第三格硬塞进我那两格里,而是重新画了格子。记大功。**

2. 🔴 **你推翻了自己 09p「相邻②」的两半,而且给了根因**:基准里那两行标记**本来就在**,**「一份不可能露馅的数据」**;摘掉重做才分出差。
   > **J-91(立)· 一个实验的夹具,必须先证明它能露馅。**
   > 拿一份「被测条件已经满足」的数据去做对照,**那份数据不可能显示出差别** —— **它给出的"无差异"是夹具的性质,不是被测对象的性质。**
   > **与 J-58⑧ 同族但方向相反**:阳性对照问「这把刀数得出来吗」;这一条问「这份数据能显示出差别吗」。
   **你那句"判据律我引了这么多遍,自己造夹具又踩一次" —— 这一族我也踩过(第 13、17、21、27、28 笔),不是你一个人的毛病,是这件事本身难。**

3. 🔴 **裁#90 那个「或」的第二个判据,你选对了而且理由是昨天那一刀的直接应用**:
   **「必须是 `!DEV_SCOPES.has(...)` 而不是 `=== 'production'` —— 生产被判成 `'local'`,拿 `=== 'production'` 等于没加。」**
   **昨天那个发现今天就用上了,而且用在了一个不用它就会白改的地方。**

---

## 一 🔴🔴 A 栏那两条,不只是"多做两件事" —— 它们在做一件我们早就明令废弃的事

你写:
> A 栏独有两件,**是「改内容」不是「加行」**:`backfillTenantKindOnce` **按 id 前缀改判** `tenants.kind`;`retireLegacyDemoArchives` 给**名字含「演示」**的 users 打退役标。**生产 6 家店里有没有会被误圈的,我在本机测不出来。**

### 🔴 第一条直接撞上 D73

**D73 裁过:演示店走显式勾选,不再看 id 前缀。**(对账表 SOP §0 那一行写着「`mIsDemo` 显式勾选(**D73:不再看 id 前缀**)」。)
**而 `backfillTenantKindOnce` 做的正是"按 id 前缀判这家店是什么"。**

🔴 **而且有一个具体的、现成的靶子**:
**`jics-nail`(小婕的真店)与 `jics-store`(沙箱镜像)前缀相同。**
**任何按 `jics-` 判类型的规则,都会把这两家判成同一类。**

⇒ **这不是"A 栏多两件事",是"A 栏会用一条废弃的判法去动生产的店铺类型,而我们已知有两家店会撞在一起"。**

### 🔴 所以这一件现在挡推,而且与 `IS_PRODUCTION` 设不设**无关**

- **已设(B 栏)** → 这两条跳过 → 本次推安全,**但那两条迁移的判法仍然是错的**,推后要处置;
- **没设(A 栏)** → 这两条会跑 → 🔴 **不许推**,先落地「或」或先设上那个变量。

**两种情况都要先做下面这一件只读的事。**

---

## 二 🔴 先算一遍:那 6 家店里,谁会被改判

**你写"我在本机测不出来" —— 但你能算。** 这是纯只读 + 本地计算,不需要在生产上跑任何迁移。

1. **只读取生产 `tenants` 表**:`id` · `kind`(或等价的类型列)· `name` · 显式演示标记那一列。**6 行,逐行列出。**
2. **把 `backfillTenantKindOnce` 的规则原文抄出来**(`文件:行`),**照它算一遍**,报三栏:
   - **会被改判的**(`id` · 现在是什么 · 会被改成什么)—— 🔴 **哪怕只有 1 家,单独红报;**
   - **算完不变的**;
   - **规则没覆盖到的**(如果有)。
   **三栏加起来 = 6,闭合**(J-66)。
3. **`retireLegacyDemoArchives` 同样办**:只读数一遍生产 `users` 里**名字含「演示」**的行数,**按店分开**;非 0 就逐行列出(店 · 名字 · 创建时间)。
   🔴 **特别看 `lucky-luxe` 和 `jics-nail` 这两家真店** —— 那里的每一行都是真顾客。
4. 🔴 **只许 `SELECT`。** 一个字的写都不许(J-72)。**查询原文先贴,我批了再跑。**
5. **阳性对照**(J-58⑧):这两条查询先在一份「有东西」的库上各跑一次,证明它们数得出来,再报生产的数。

### 判

- **三栏里"会被改判的"= 0,且名字含「演示」的真店行数 = 0** → 🟢 **这一件解除**,那两条迁移的判法照旧要改(推后)。
- 🔴 **非 0** → **推停死**,报我。**不许自己去改那两条迁移来"让它变成 0"** —— 那是改代码去迁就一个我还没看过的事实。

---

## 三 🟢 批「或」改法,但加一条尾巴

`TREAT_AS_REAL = IS_PRODUCTION || !DEV_SCOPES.has(DATA_SCOPE_NAME)` —— 🟢 **批。**

**三向我都看了**:①已设 → 行为一字不变 ②没设 → 改前会跑(缺口)、改后跳过 ③四种设错 → 一律跳过。**误差方向全部朝"跳过"那边倒,对。**

**你报的那个代价我接受**:本机库也会被判成真环境,那两条迁移以后在本机也不跑了(现测标记都已在,当下无影响;本机库重置后不会自动跑)。沙箱与回归临时库不受影响。
**理由:那两条的判法本身就是错的(见 §一),让它跳过正是我们要的。**

🔴 **但加一条尾巴,不许省**:
> **改完之后,那两条迁移等于被冻结了。** 要么**正式销号**(写明为什么不再需要),要么**改掉判法再启用**。
> **不许让它们悬在那里** —— 一条谁都不会再跑、但还躺在开机路径上的迁移,**下一次有人改了环境变量就会突然活过来。**
**列进推后待办,给案号。**

---

## 四 🔴 推完第一次开机,库会从 `delete` 变成 `wal` —— 这一条要写进交给店主的清单

你自己列出来了(两栏都会),而且你指出**同批自洽**:WAL 一上来 cp 的风险变真,**而把备份改成 `VACUUM INTO` 的 09n 件 A 正是同一次推。** ✅ **这个观察对,收下。**

🔴 **但要补两件**:
1. **确认 `db-backup.mjs` 就是生产每日自动备份的那个出口**(你 09o 说过是"唯一出口",再点一次名,`文件:行` + 调度是从哪触发的)。
2. 🔴 **加进推后待办:推完第二天 04:55 那次自动备份跑完后,打开验一次**(逐表点行数,与生产对)。
   **理由:那是第一次在 WAL 下跑的自动备份。09n 件 A 改对了没有,只有那一次能证明。**

### 而我给店主的那句话要再改一次(第三次)

我 09q 让她理解成:**「现在不会漏,一旦有真顾客开始用才会漏。」**
🔴 **准确的说法是**:**现在不会漏,因为不是 WAL;推完之后会变成 WAL,那时候才谈得上漏 —— 而改好的备份法跟这次推是同一批上线的。**
**我这一句已经改了三遍(第 28 笔的延长线)。三遍都是同一个毛病:我把一个条件句的条件说丢了。**

---

## 五 🟢 §四① 收下,并给根治的批

**扫描面 214 个文件;命中 31,其中 17 个是 AI 意图名,真读文件的 14 个;不分作者扫整棵 `handoff/` 的只有 1 把** —— 正是撞上那把 `test-untouched-proof.mjs:101`;11 把只读自己产出;**2 把读我的产出,但那正是它该读的**(合同图、外部件清单)。

🔴 **你那句提炼收下,立为 J-90 第二款**:
> **J-90②· 判据读对方的产出分两种:「当标准」可以,「算进底数」不行。**
> **两种在代码里长得一样** —— 所以新加的判据必须在注释里写明属于哪一种。

**根治批**:把 `test-untouched-proof` 的扫描面**限定到 Code 自己的产出**,不再扫整棵 `handoff/`。
🔴 **两面**:①我的令文里出现的任何安全声称 → **不算进它的底数**;②它自己回执里真出现一条新的安全保证 → **必须照样算 1**。**09p 那层「引述不算」的补丁留着当第二道,不撤。**

---

## 六 现在的挡推清单

| | |
|---|---|
| 🔴🔴 **§二 那两条迁移的影响面** | 先只读算一遍「6 家店谁会被改判 + 真店里名字含『演示』的行数」。**非 0 → 推停死** |
| 🔴 **`IS_PRODUCTION`** | 店主核一眼(我已在问)。**已设 → 走 B 栏;没设 → 先落地「或」再推** |
| ✅ 已解除 | 全量回归 · `scopeOf` · D203 · 件 A/B/C · 库落点 · `railway.json` · 门禁回归 · `-wal`(甲档坐实) |
| 🟡 不挡 | D199 · D200 · D201 · D202 · 那两条迁移的判法(推后销号或改判) · 09m 后台清单 |

🔴 **一个 push 都不许执行。**

**数收下**:主档 **128/128 · 断言 4,144 · not ok 0** · 小程序档四格闭合 ✅ · 整跑 5m57s、看门狗 744s 没开枪 · 棘轮三项全 ±0 · 账本 **447** ✅ · 领先 448 · 4128/4310 = 200 **且都过版本指纹** · 树干净 · **一个 push 没执行** ✅
